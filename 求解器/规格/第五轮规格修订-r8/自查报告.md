# 第8轮规格自查报告

日期：2026-09-19。状态：三条存活发现及KQ-07、KQ-08规格答复已落文；规格自查、schema/配置一致性和本轮回归通过。本文记录S线检查，不报告K线运行认证。

## 1. 复现

在`/home/zhuran24/zmd-research-fresh/求解器`执行：

```bash
python -B 规格/第五轮规格修订-r8/check_round8.py
```

此入口依次重建并比对schema字节、运行规格自查、以本目录为输出目录运行第五轮自查，再做本轮反例与辖域扫描。已有AJV2020只读加载，不安装依赖、不创建字节码，不调用cargo。编译约束仍为工作区共享target；本轮没有编译产物。

## 2. 结果

| 检查 | 结果与证据 | 范围 |
|---|---|---|
| 当前规格自查 | PASS；[规格自查.log](规格自查.log) | 正式规则114行、任务15行、约束56条映射；99轴的声明/配置逐字段一致；输入投影无缺失、生命周期或指定说明差异 |
| 第五轮原有检查 | 183项PASS；[自查结果.json](自查结果.json)、[第五轮自查.log](第五轮自查.log) | 当前StateSeed字段与closure_key、规范键、台账/循环schema正负例及局部推演 |
| 本轮回归 | 22项PASS；[本轮自查结果.json](本轮自查结果.json)、[本轮自查.log](本轮自查.log) | 包括下列前件、字段、准入和旧句检查；计数包括执行入口检查，不能与183简单相加称独立语义定理 |
| 停止前件 | 60组一致；[停止条件复算.json](停止条件复算.json) | 答复/转移与独立可观察性判据；未指派空格双成品继续，增加指派停止 |
| 字段名 | 正例通过、改名负例双重拒收；[字段名复算.json](字段名复算.json) | 缺现行必填字段和多旧字段同时报错；schema没有兼容别名 |
| schema与配置 | 重新生成schema与既有字节相等；[schema生成.log](schema生成.log) | 30个定义、15个证书输入轴与当前99轴配置一致，原schema未变 |
| KQ-08局部反例 | 79999/80000回1矿容量结果不同；[回矿容量复算.json](回矿容量复算.json) | 零成功回矿账不能代替容量前检查；不是Rust执行测试 |
| 全文辖域 | 16份正文/交接/修订记录无已定位旧句；[辖域扫描.json](辖域扫描.json) | 旧复核快照、旧日志及脚本负例明确不作为现行契约；另作人工阅读，字符串扫描不证明语义穷尽 |

## 3. 来源与写入范围

[开工指纹](开工指纹.json)记录任务书、当前规格及只读观察对象。正式三文件及候选约束继续与原指纹严格相等。K线已开始第五轮实现，旧基线中13份源码有变化；自查对这类活动依赖记录当前字节和差异，见[活动内核来源](活动内核来源.json)，没有刷新旧基线或把变更伪报为本席修改。当前字段与schema检查仍读取实际源码和试样，不靠旧快照通过。

本席实际写入仅在规格线拥有的文件及本目录；schema再生成经字节比对不产生内容变化。K线疑问、源码、数据与R线约减没有本席写操作；并行变动和本席交付文件分别见[交付清单](交付清单.json)。新证据仅为脚本、日志、JSON、Markdown，没有target、.cargo-home、registry、仓库副本或模拟器改动；没有commit/push。

## 4. 读者视角自审与交接

按未来首次阅读者重读修改段落及其完整接口上下文：统一状态头；现行接口与第6/7轮交接史料分区；U/E/A定义、两个传输分支、D.3范围、字段名、种子来源、D.2容量前守卫及逐访问验收均有明确落点。修订记录逐id说明改动与据，旧宽停止条件已标撤回；覆盖表原文列未改。链接与最终来源字节检查见[交付自审](交付自审.json)。

本轮三条发现及两个新增疑问均已在规格侧处理。K线仍需按最终配置与修改请求迁移、跑种子与回矿前置停止测试、重生成依赖记录并独立回放周期；本席未运行其cargo。级二F不足、所有种子/参数/读法覆盖及动态约束普遍相容性仍为既有未决，详见[修订论证](修订论证.md)与受限转移§6.5。
