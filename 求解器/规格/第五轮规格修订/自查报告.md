# 第五轮规格修订自查报告

日期：2026-09-19。状态：S1–S4第一次修订已完成；机械自查通过，K线实现迁移及独立复核尚未执行。所有推导在现行正文，处理历史在两份修订记录；本目录不包含编译缓存、registry或仓库副本。

## 1. 复现与结果

在仓库根执行：

```bash
python -B 求解器/规格/第五轮规格修订/build_schema.py
python -B 求解器/规格/check_revision.py
python -B 求解器/规格/第五轮规格修订/check_round5.py
```

schema生成使用本目录的单份`运行记录v2基线.schema.json`作结构基线，保留v2的StateSeed/增量结构后生成当前v3与cycle-v1。它只是格式JSON，不是工程/仓库快照。schema元验证与正负例使用已有本地AJV2020（只读路径见脚本），不安装依赖。首次使用系统Python的jsonschema不可用，已改为已有AJV；初次结构试样漏Quantity.category已修正。这两项均是测试入口/试样问题，不通过降低校验放行。

- [既有自查.log](既有自查.log)：PASS；规则114行、任务15行、约束56条原文逐行映射，99轴/声明/JSON逐项一致；输入投影无缺轴、生命周期或指定描述失配；原修订发现追溯与正式源快照仍一致。
- [自查结果.json](自查结果.json)：PASS；183项检查，含AJV2020元验证、StateSeed当前Rust字段逐名落点、closure_key 11项、键平移/数组置换/矿量/成品标签不变、植物/非仓库数量改变必须异键、非空O拒绝。
- [提升容量卡点.json](提升容量卡点.json)：79999+2整箱拒收而“未满”仍真；另核回矿入库对矿量的容量依赖。它是局部守卫反例/证明卡点，不是已可达完整布局的低产循环证书。
- [轮询均分局部推演.json](轮询均分局部推演.json)：6种模板顺序、各12组，源为有同级三边的非运输U、目标独占且每tick先腾空时成功序均A/B/C循环；给定边界条件的递推，不替代真实几何/供料/下游验证。
- schema正例与负例包含full/delta漏台账、漏无线/出库/补给/代表调整、缺port_meeting、15轴缺一、full_base缺提升、停止/未决缺原因。结构正例明确标为测试对象，仅在内存，不伪造可交付证书。
- [开工只读指纹.json](开工只读指纹.json)：三份正式文件、候选约束及17份内核源码共21份保持字节一致；没有改K线疑问、数据或R线文件。未运行cargo，未产生编译产物。

## 2. 读者视角自审

重读运行语义、选择点清单、轴表、声明、输入、转移、输出、普遍场景及交接请求，检查当前状态头、条文行号、链接、旧接口句与量词。修正了旧“未实现Rust”、v1分支表现状、after_closure首项、替代标签字段占位、全部循环一律停止的旧辖域；历史记录/旧复核保持史料，不用旧指纹给新实现背书。规范键精确JSON形状与Python映射参考已补入§6.2，避免只给自然语言表却没有编码。

逐字段表不等于自动证明语义穷尽；脚本只能核字段有落点与测试不变量。§6的逐步证明限D，O非空、有回矿、可观察成品取货等不属于获证域。原F到级二在完整批容量处不足，F_safe只是待另证的强化；不把未解决事项改成通过。密集结点尚无本席端到端周期记录，要求K线验证其它来源周期正流量后再比较顺序。

## 3. 交接未决

K线须实现新的分支表、两矿模式、生产键/G代表化、v3台账、after_closure续跑、I事件及独立证书重跑，再迁移旧输入/投影/记录和运行其测试。R线文件不由本席改动；其快照与参数域须按最终规格重核。一般动态约束相容性、非精确玩家拿取族可实现性、级二提升、三种族的全称覆盖仍未完成。没有因此降低任务范围、宣布正式目标无解或提高真实布局下界。
