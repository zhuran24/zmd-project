# 第三轮规格修订自查

史料：2026-09-19第4/5轮发现落地时的96轴自查快照。下列通过状态、命令与数字只针对当时版本；本文件不宣称第6轮修改后的现状。当前自查见[第6轮报告](../第6轮修订验证/自查报告.md)。

## 1. 可复现命令与结果

在仓库根运行：

```bash
python -B 求解器/规格/check_revision.py > 求解器/规格/第三轮任务验证/自查结果.json
python -B 求解器/规格/第三轮任务验证/核对转移回归.py > 求解器/规格/第三轮任务验证/转移回归结果.json
python -B 求解器/数据/工具/formal_catalog.py
```

本轮三命令均退出0。规格门禁53项通过，算例回归16项通过；正式目录工具核三份源的字节指纹、完整行、分节、据、任务与结构化阈值一致。完整结果分别见[自查结果](自查结果.json)与[转移回归结果](转移回归结果.json)。没有修改Rust代码，因此未重复运行契约线cargo测试。

规格门禁覆盖：规则114行、任务15行、正式约束56条；55项独立清单映射；第4轮11项与第5轮17项共28条发现有改动/落点/据，六正文实际字节均不同于开工；原91轴加5新轴共96项与逐轴表、配置JSON全等；18单位与18配方逐项使用当前目录；相遇/仓库入口和定位旧句回归。漏四个必选轴、恢复旧选择点、退回错误章节指针均由内存负例拒绝。

当前目录SHA-256：`74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43`。只读门禁使用本轮开工快照，25037个文件指纹未变（三正式源、候选及模拟器文件）；B/C线共享数据/代码/输入不列为只读。当前目录每次重新读取并逐项核，不把历史63acbb92…当现行版本。开工规格快照仅用于证明改动，不用于冻结其它线。

## 2. 语义与算术回归范围

- 双端授权和物理可动的8个组合：无权不判、单侧失配失败前移、成功双方前移；不能以成功合取吞掉已获权尝试。
- 汇流器逐级第二条/单成员级、最高可动级与指针分离、箱体最小非空格、窗口5 tick及独立阻断原因。
- 库存不变但指针有3状态的空尝试环：必须等完整调度状态重复。
- positive_area的24台与closed_touch的32台整齐构造逐台核相交/互斥；侧旁两组列数与跃变位置；这些只证条件算术与构造，不证明达标或一般二维最优堆叠上界。
- 7个整数观察边界的源矿→传送带→粉碎机→传送带→箱小算例：完成先于闭包、入料立即开工、两段运输各足1 tick，每tick包含在制的物料收支守恒。该算例不是C线合成布局的黄金轨迹，也不是完整执行器。

## 3. 逐辖域复读与读者自审

读者视角复读了六正文、新受限转移定义、96行配置、修订记录及接口答复，核对以下终态：

1. 变更历史集中于修订记录和本自查，现行正文不再宣称“两轮零改动”或把旧待补算法当本版状态。
2. 供电两读法保留；最严读法只用于单调供电/可建造义务，额外通电行为仍有覆盖损失；四条必要条件没有被加上额外前件。
3. 内部分级有两个方向，隔带直连排除；箱体取货不能跳过最小非空格；约束专有名的据栏有约束·前缀。
4. 起轮的解释、计数范围、单成员级与级内记忆分开；双端失败后效和事件实例序能定位到具体转移条款。
5. T8的窗口耗尽永久锁闭、全盘停表已排除；身份/累计未被一并裁定；断边不观察对端货物。
6. 三形状×四朝向、调试经重建改形与尚缺依据的直接改形区分；仓库空格沿用已有轴，指派与物品内容分开。
7. 当前计数96唯一，91仅作原表历史计数；跨文件T编号、文件链接及覆盖入口存在。C线终态三语法、级序/空格序/阶段/阻断原因字段已对接，见[接口对接](接口对接.md)。

机械检查只定位已知错误，不能代替语义蕴含证明。受限转移§5提供支持域内的条件终止与唯一性论证；未因此把缓存容量改成一批或宣布一般状态有限。

## 4. 未决与交付边界

- 本解释组合对约束·轮询均分、约束·密集结点等全部前件的普遍相容性仍须后续复核；一旦证伪须撤出选值。
- 一般时间/相位、动态分支、其它双端/起轮/内部判定读法、无终点阻尼、离线与调试/重建后效仍未全部覆盖。遇到未支持事件按逐轴政策停，不作为物理不可能。
- 缓存及时间的可靠有限抽象、全部初态和非精确操作历史覆盖、仓库完整周期提升仍未结清。
- C线的输入、输出schema、可跑样例与黄金轨迹是其独立交付；A线只核接口，不冒领修改或轨迹认证。后续三视角复核/两席否证由工作流按任务书接续。

全部本轮写入在求解器/规格/；三正式源、候选、模拟器未变，不commit、不push，不回退既有工作树改动。
