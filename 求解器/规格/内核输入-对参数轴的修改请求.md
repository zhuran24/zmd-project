# 内核输入对共享参数轴的修改请求

日期：2026-09-19。性质：接口交接史料及现行待办；截止2026-09-19。§1–6为已并入的第三轮交接记录，§7为第四轮已并入请求；当前99轴。

## 1. 三个可输入接口

`judgment.order` 采用 `event-order-v1`：排序所有重复/新启用事件的模板及实例，见内核输入 §5.2。`damping.branch`当前采用`damping-branch-v2`：以`(fork_unit,available_channels)`为键、`outgoing_channel`为值，整场固定局部可用集合表，fixedness=by_available_set，见§5.3；查询起点/其它图状态/历史依赖仍是覆盖损失。`StateSeed.logistics.poll_memory` 采用 `poll-memory-v1`，每单位两侧、每级成员和 next_channel、current_level，见 §6.3。请求轴表相应行和清单 T2/T3/T4 链接到这些语法；状态编码不另增语义轴。

## 2. 传送带形状与阶段

请求登记 `initialization.belt_shape_lifecycle`（T5/T11，U），至少区分“本版在每次建成时给形状、两次建成之间固定”与“直接改形是否另有授权仍待审”；第二方向未给正面依据前不可执行。本版值建议 `fixed_between_builds`。12 个有序边对为 3 个旋转轨道 × 4 朝向：旋转同时加方向，不改相对夹角，因此 `port_layout` 不是 rotation 的别名。据：规则 L60、L11。

规则 L10 重建为拆后再造，L60 的任意边允许再造时选另一轨道，任务 L11 允许调试操作；故调试经重建改形有表达路径，库存/进度去向仍受 initialization.rebuild_inventory 的未决限制。任务 L8 排除零干预后玩家改形。L10/L11/L60 未单独证明“两次建造间绝无其它可改形机制”，故该部分保留受限选择及覆盖损失，不能据旋转的不足反推只有重建。内核输入 §3.2 已要求 rotate 不改变 port_layout，rebuild 前后快照可改变；不新增无据设定动作。

## 3. 既有轴的取值与覆盖入口

1. T8：删除窗口用尽永不恢复及全原因阻断停表；身份阻断专属停表如保留须另列带原因辖域的值。内核输入表同步第三轮任务书 §2.2 的排除。
2. T12：empty_identity 沿用 warehouse.empty_slot_identity，指派 item=null 格时必须给已解决决策，不新增轴。规则覆盖表 L13/L14/L41/L73 及任务 L6 链接内核输入 §4、§6。
3. 不相交与供电义务：内核输入 §2.1 改据约束“不得依赖的量”的最严读法推理；不再把规则 L8/L11/L41 当占格互斥依据。power.cell_rule 两读法均保留，达标供电义务只计 positive_area。覆盖表相应入口链接 §2.1。
4. 本版可跑样例需要 time.instant_order、time.manufacture_events、polling.level_tie、connection.port_meeting 的最终选值及 polling.dual_permission 的失败后效。请求在受限模型声明给稳定编码。样例将锁定实际读到的声明指纹；若声明未落地，不能声称该依赖已解决。

## 4. 版本协调

开工轴表为 91 项。新增正交轴后总数须按现表计算，不为保持数字 91 漏填新轴；输出应保留“原 91 项全部赋值”及新增轴列表。形状生命周期、双端失败后效和汇流器起轮辖域如由规格线另取轴名，以正式并入名为准，C 线迁移编码而不复制第二套轴名。该文件请求不是改写三份正式文件的授权。

## 5. 第三轮首次对接记录（第6轮扩轴前的历史快照）

规格线已并入 connection.belt_shape、initialization.belt_shape_lifecycle、polling.split_merge_scope、polling.split_merge_singleton、polling.both_failure，当时96轴，现行99轴见§6。内核输入表及三个样例已按现行表迁移；四条新增known编码也已对齐。本线未直接修改共享三表。

受限模型最终选 judgment.buffer_event_class=manufacture_subactions，输入EventOrder已改为只列PC/制造/传输模板；完整StateSeed增加semantic_context.arbitration（level_order及warehouse_empty_slot_order）和judgment_context.phase，级id按other/direct:PC/ungraded稳定类别。上述为转移定义要求的显式输入编码，请共享正文引用内核输入§5.2、§6.3时使用这些终态字段。96项本版选值直接与内核配置-v1.json比较，运行参考锁其字节指纹，变化时拒绝静默沿用。


## 6. 第三轮存活发现后的接口请求及并入状态

1. 选择点参数轴的 judgment.order 行、选择点清单 T2 及规则覆盖表 L25/任务L14，请引用内核输入§5.2的两种版本：kernel_profile_v1 仍使用 global/event-order-v1，通用表示新增 event-order-v2 周期槽函数，per_instant不再只有有限表。周期求值为floor((t-origin)/slot_width) mod len(orders)，每个槽完整模板序；任意远的时刻可求值。不是新增轴，也不将per_instant默并入本版运行配置。依据：K3-r3-L1-01；规则判定、任务判定次序、约束判定先后。
2. 规则覆盖表 L25及任务L8、L11–12、运行语义StateSeed引用，请指到内核输入§5.2新增的in_boundary/in_closure。continuation存边界事件去重、闭包完整调度投影及当前轮前缀，tick_context不重置。受限转移定义§1已要求中途种子带阶段及去重；现给了具体编码。格式读回测试与通用直接续跑支持分开，后者仍未实现。依据：K3-r3-L1-02；规则原子判定、操作精度及端口速率。
3. 规则覆盖表L13、L17、L23、L29–30、L59及任务书输出记录义务的接口说明可链接内核输出§3和runtime_record.py：记录内容须与实际输入和完整重算相等，不能只过schema或另算正确摘要。逐轴覆盖状态与配置处置分列；无箱、无多级平局、无阻尼求值均记未触发。依据：K3-r3-L2-01/02、K3-r3-L3-03。
4. 指纹链现在包括参数投影及其声明/配置/轴表来源、check_examples和全部执行校验依赖。共享声明变更后先迁移/重核投影，旧来源不能混入新记录。原96项非输入接口值无变更；已跟随规格线第6轮加入3轴，当前输入99项。本次同时修复来源登记和消费校验。依据：K3-r3-L2-03、K3-r3-L3-04。

本请求未由C线直接改共享三表，也未修改A线配置、受限模型声明或转移定义。规格线[第6轮答复](参数轴-对内核输入请求的答复.md)已明确§1–6并入；本线重新核对当前T2/轴表的v1/v2入口、StateSeed continuation及覆盖表的完整记录/来源校验引用。


第6轮并行新增轴已由本线消费：gate.concurrent_expiry=atomic_batch、manufacturing.recipe_quantity_match=at_least、manufacturing.recipe_extra_items=allow。当前99轴；无准入口和超量/额外物种实验的前缀不据填值宣称机制覆盖。制造同种单格守卫已同步；边界恢复不允许落在多门维护原子事务内部。上述迁移不撤销本节1–4项接口请求。

## 7. 第四轮接口请求及并入结果

1. **K3-r4-L1-01**：选择点参数轴`judgment.order`、选择点清单T2和规则覆盖表规则L25/L36、任务L14的表示入口，请补内核输入§5.2的`event-order-v3`/`order-expr-v1`。纯函数AST读取有理t，经floor_slot、整数运算/谓词及条件排序返回同一模板全集的排列，能表示双空箱仅在正的2的幂次到期换序。v1常序、v2周期继续有效；不新增物理轴，不改kernel_profile_v1的global选值。函数全域定义性、合法性和非周期证据见[第四轮处理报告](内核输入修订验证-r4/存活发现处理.md)§1。共享正文若列通用版本全集，请一并纳入v3；本版仅global的运行说明仍成立。
2. **K3-r4-L3-01/02**：规则覆盖表L13–18、L25/L28及任务L14的接口核验入口，请指向内核输入§2.3/§3.1及内核输出§2–3：仓库不占单位槽位命名域；非runtime历史不占J/C运行前缀；新事件注册对全历史去重，验收跨tick及pending核身份和消费。这些是已有实体/事件可区分性的编码义务，不增语义选择轴。
3. **K3-r4-L3-03**：交付时先核版本和完整记录，再按最终实际字节生成清单并逐项重读复核；后续共享同步若改变被列交付物，须重新验证并重新封存。当前交付入口为[第四轮清单](内核输入修订验证-r4/交付清单.json)，第三轮清单保留原轮次语境并注明现字节复核时点，不以刷新哈希代替重跑。

本线未修改共享三表；规格线[第7轮答复](参数轴-对内核输入请求的答复.md)已明确§7三项并入。本线回核轴表judgment.order、清单T2、覆盖表L25/L36/任务L14及文末身份入口，v3表达式与全局身份引用均存在。本版仍global/v1，未因并入表示入口扩大运行支持。前三轮记录中的版本列表属于对应时点，不是现行版本全集。
