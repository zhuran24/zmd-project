# 第6轮规格修订自查

日期：2026-09-19。状态：本轮规格、局部回归、Rust契约检查、当前99轴样例及黄金完整记录核验均通过。不是Rust语义内核、全称运行族或真实达标布局认证。

## 1. 修订与重跑入口

五项处置见[发现处置](发现处置.json)，逐项论证见[修订论证](修订论证.md)。本轮新增三轴：gate.concurrent_expiry、manufacturing.recipe_quantity_match、manufacturing.recipe_extra_items；原96轴保留，共99项。

在仓库根执行：

```bash
python -B 求解器/规格/第6轮修订验证/run_checks.py
```

该入口保存本目录检查日志，并调用既有黄金重算程序刷新 `求解器/数据/样例/混做粉碎机两下游-运行记录.json`。它不生成新输入、不更改运行配置的锁定常量。实际命令、退出码及stderr完整记录见[检查汇总](检查汇总.json)。

## 2. 检查结果与适用范围

| 项目 | 结果 | 证据与边界 |
|---|---|---|
| 规格覆盖及当前接口 | 58项通过，99轴集合及编码/含义/损失/据一致 | [规格自查](规格自查结果.json)；规则114行、任务15行、正式约束56条逐项映射；5条第6轮语义说明在内核输入无差异 |
| 原转移算例 | 16项通过 | [原回归](原转移回归结果.json)；调度/条件几何/7个边界单料流水，不替代本轮反例 |
| 第6轮回归 | 19项通过 | [修订回归](修订回归结果.json)；含库存逐步状态、旧非法后态负例、15个容量组合、门遍历序置换、仓库竞争停止及匹配两轴 |
| 正式目录回源 | 通过 | [目录日志](正式目录.log)；18单位/18配方与正式来源，不把旧目录指纹当当前版本 |
| cargo test | 1项单元测试＋30项集成测试通过，共31项；0失败 | [Rust日志](cargo-test.log)；topology送料契约crate，没有据此称语义内核已实现 |
| 候选B | 4925项静态通过、0项静态失败、59项不能静态检 | [完整报告](候选B校验报告.md)；不认证几何、起动、实际吞吐或循环 |
| 三个当前样例 | 全部通过；19个负例、8个表示回归通过 | [样例检查](样例检查结果.json)；桥接/分流样例为静态结构，粉碎机样例另含完整有限运行输入 |
| 黄金轨迹 | 时刻0–3共4个观察点、2个完整制造周期，与原手工期望一致 | [重算结果](黄金记录重算结果.json)及[完整记录核验](黄金完整核验结果.json)；不仅比摘要，还核参数投影、当前来源指纹及持久记录与完整逐事件重算相等 |

全部命令退出0。最后一次黄金完整核验在读取期间锁住共享依赖的实际字节，前后无漂移；不是在进程中替换SUPPORTED_PROFILE_SHA256后运行。无准入口、无箱体的有限黄金前缀不能覆盖新恢复/仓库竞争机制，这两项由独立局部反例回归核验，未冒称生产内核测试。

## 3. 并行接口与来源链

开工时C线为96轴；规格新增后C线自行迁移99轴、样例参数和制造两个跨格守卫。本席最后检查发现内核输入轴表有5条旧语义说明及3条缺少具体语法入口的重复投影，按现行A线轴表同步8行；记录见[输入投影同步](输入投影同步.json)。未更改C线状态格式或源码。随后用既有黄金程序重算绑定该文档指纹的运行记录。

初次自查中一段预设“原输入仍96轴”的辅助兼容检查，因C线已并行迁移而断言失败；这不是原样例失败。最终删除该临时迁移分支，改为直接校验当前99轴原产物，未修改运行器哈希门禁。所有终态证据来自最后一次直接重跑。

内核输入请求第1–6节均已读并在A线合入。event-order-v2属于per_instant通用表示，本版仍运行global/event-order-v1；in_boundary/in_closure的continuation能表示中途状态，通用直接续跑仍未实现。多门批维护不能以半提交状态作中途恢复锚点。逐轴赋值、配置处置及本次实际触发范围分列。

第4/5轮自查、接口快照及交付指纹保留历史属性；两个Markdown入口已标明历史96轴快照，不冒充现行99轴版本。本轮交付以本目录清单和指纹为准。

## 4. 全文辖域扫描及读者自审

逐项回读现行运行语义、T1/T3/T7/T8/T12、99行轴表/模型表、配置、转移、四义务、覆盖表和输入投影，完成读者视角自审：

1. 制造物种通则覆盖装载、外部收货、内部输入/输出与完成的全部子动作；缓存例外不被误取消，错料不被擅自禁收，非法出缓存保持completed。
2. 恢复从完整旧图到完整新图只映射一次；空级与存活级分别处理；身份维护不留下另一处未排序逐边更新。
3. 空格序仅解单物种目标，不称能解多物种分配；len(U)>=2且len(E)>=1先停止，不写库存、不起冷却；正常容量拒收另处理。
4. 配方论域、完备度、数量关系、额外物种关系分开；混装多候选例补allow前件，exact/forbid仍待审；旧“符合”总称不替代具体轴。
5. 两条旧覆盖发现归因按原aFindings纠正；供电裁定不复议。其它席原归因和旧复核反例均保留史料，不因出现旧字符串而篡改原报告。
6. 现行数字99唯一；91/96仅在说明历史构成时保留。选择点编号、文件链接、源行及参数投影相互对应；正文呈现当前定义，变更过程集中在台账与本报告。
7. 工程停止、未获证明、有限实验通过与全称认证分别表述；未更新L/U，未宣布整体ready_for_kernel。

## 5. 保护范围与剩余事项

开工只读清单共25037文件，含三正式源、候选约束与模拟器；末次字节指纹全部一致，模拟器文件集合也一致，见检查汇总。只在求解器内写入；没有commit/push，没有回退既有工作树改动。并行B/C线的其它变化与本席写入区分，交付清单只列本席实际修改/新增产物（构建缓存除外）。

五项本轮发现均有处理。尚待后续的是：竞争无身份仓库空格时的真实批量分配机制（本版已有明确停止条件）；其它恢复/配方读法的联合规则审查；一般缓存/时间有限化、初态与离线覆盖、完整周期提升及全称达标。这些是保留的覆盖边界，不把本轮回归通过提升为完成这些义务。
