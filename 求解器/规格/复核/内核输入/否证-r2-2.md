# 内核输入格式第二轮否证：第 2 席

日期：2026-09-19。性质：GPT 源第 2 席的判读存档。状态：18 条发现逐项完成；2 条按当前产物已否证，16 条未能否证。读取基准为 2026-09-19 17:02 UTC 起的磁盘快照；本档案不随其他席位的后续修改自动更新。

产物路径：/home/zhuran24/zmd-research-fresh/求解器/规格/复核/内核输入/否证-r2-2.md。

## 1. 依据、快照及判读方法

两轮任务书已先完整读取，路径分别为：

- /tmp/claude-1000/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0/scratchpad/step1/任务书.md。
- 同目录的任务书2.md。

第一轮 §1、§4 规定三份正式文件为规则依据、未定不直接授权取值。第二轮 §2 的 T7 分配读法、T6 单位级判定、现行“仓库外”守恒域均沿用；§3.C 要求布局、建造、离线、设定、参数、初态调试及契约映射的表示，§4 规定否证须有证据。下面的“规则”“任务”“约束”分别指仓库根的《明日方舟：终末地》游戏规则.txt、求解任务.txt、求解约束.txt；三份正式原文均已回读。“输入”指求解器/规格/内核输入.md，“检查器”指求解器/数据/样例/check_examples.py。

另已回读被指的运行语义、受限模型声明、参数轴与选择点相关原文、送料契约、当前目录、三个样例、生成器、检查报告、第一轮两席否证及第二轮三份复核。目录、样例和候选 B 契约以 JSON 全量读取；几何从目录独立重算，不调用原检查器的 geometry。候选约束只读指纹，不作为游戏规则。

refuted=false 表示没有足以否掉核心发现的证据，不表示其每个附带断言均已成立。工程字段缺口、程序子集拒收与物理非法分开判断；open 是待审方向，不是合法性证明。

### 1.1 影响本轮结论的后续修复

发现列表读取的目录失配已发生变化。当前三个样例的 catalog.sha256 均为：

74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43

它与当前目录实际字节相等。求解器/数据/修订记录.md L101 与修订验证/r3/样例目录引用刷新.json 记载 B 线已刷新三个引用；当前样例 mtime 为 16:56:42 UTC，检查结果.json 为 16:56:42 UTC。本席没有实施这次刷新。

为核对刷新范围，逐个把现样例内存副本的目录指纹恢复为 63acbb…，按原缩进和末尾换行序列化，得到的三个 SHA-256 分别为：

| 样例 | 恢复旧目录指纹后的内存字节 SHA-256 |
|---|---|
| 桥接器双通路 | def32415eeedee63a3f02c10f255355b507d14c0b19ff0950fc202ecfc528c3e |
| 分流器三路轮询 | b951dee66c6c29f0e6bb483b0c771aad02ac276f134c219dc240b03628e8a6bc |
| 混做粉碎机两下游 | 45dfd5f8dd7aca0a5e2dea0c932ed5a056041b3ee25c603fcf148f61d4f936e3 |

它们逐一等于否证-r1-2.md L30–32 所记的旧样例指纹。因此有证据区分“样例目录引用已修复”与“规格表示缺口已修复”：前者发生了，后者没有随之发生。输入规格仍是 03712e…，检查器仍是 a463ae…；内核输入-修订记录.md 仍不存在。

### 1.2 工具实测摘要

在仓库根执行，不写报告：

~~~bash
python -B 求解器/数据/样例/check_examples.py
python -B 求解器/数据/样例/check_examples.py --self-test
~~~

两条均退出 0，status=通过；第二条包含 11 个正确拒收负例。对于文档的完整 --self-test --report 命令，本席调用同一个 main()，仅以 mock 拦截最终 Path.write_text；解析、全部检查和报告生成照常执行，生成的报告与现有检查结果.json 逐字节相等，既有文件没有被写入。现报告 artifact_sha256 中全部文件指纹也与磁盘相符。

独立几何复算逐单位枚举矩形格，按输入 §2.1 的四个旋转换码变换格坐标及法向；再两两枚举端口，要求邻格相对、存取互补且至少一端运输单位；BC 按制造格角色与数量逐一列出。所得 PC/BC 不仅数量一致，记录集合也逐条等于样例声明：

| 样例 | 占格 | 端口 | PC | BC | 越界或重叠 | PC/BC 与声明 |
|---|---:|---:|---:|---:|---|---|
| 桥接器双通路 | 122 | 48 | 4 | 0 | 无 | 相等 |
| 分流器三路轮询 | 128 | 50 | 4 | 0 | 无 | 相等 |
| 混做粉碎机两下游 | 149 | 64 | 9 | 8 | 无 | 相等 |

数字为候选几何的算术计数；程序退出码和错误文本为本轮工具实测。它们不认证运行、起动、轮询均分、吞吐或全部可达循环。

## 2. 逐项判读

### K-r2-L1-1

**refuted=true：当前产物已覆盖所称加载阻断。**

回源：第二轮任务书 §3.C.7 要求三个合法合成输入。输入 L27 用 path 与 sha256 指定共享目录；检查器 L194 比较实际字节。本轮逐个读取三例均已是 74d21a…，与目录相等；两条只读复跑均通过，完整报告生成内容与现报告相等，见 §1.1–1.2。故“当前连加载都过不去、检查结果仍是过期通过记录”不符合当前快照。

历史故障不被否认：只在内存恢复旧 63acbb…，立即重现“共享目录哈希不匹配，须重新核对”。这是一项在复核报告之后已被修复的问题，不是证明先前的实测造假。

附带推论也不足以保留当前阻断：L9 所说的是 parameters.axis_registry 的参数轴表，不是 catalog；字节指纹本身已经精确指定目录内容，任务书没有要求另存 version 或自动接受目录变化。L275 还提供样例再生成入口，生成器 L44 读取当前目录哈希、L64 在写入前检查。它不是一般输入的自动语义迁移器，但不能因此把“没有自动迁移”提升为既有样例不可修复。§5 明许 B 的复核与 A/C 并行，也不能由 B 后续改目录反推 B.1 初次前置未履行。

### K-r2-L1-2

**refuted=false：未能否证跨事件流引用及合并顺序的表示缺口。**

回源：任务 L11–13 给不精确调试与离线条件；规则 L10、L28–32 使重建和接通记录影响轮询及级排序，L64 使准入口改设定/阻断与当时边集合相关。第二轮 §3.C.2、C.5 要求建造史、离线和调试序列。输入 L80–82 是一次蓝图的 moments/time_relations，L86 给离线两锚点，L234 的 after 明确只指“前序操作 id”。全文未定义一个把建成事件、操作、离线事件纳入同一引用域的对象，也未规定同名冲突、跨域引用与历史合并。

尝试否证时，发现的两项辅助理由需要纠正：L86 没有明文说两锚点只能指建成事件；若允许它们指操作，离线记录 before_event=某操作就能表示“操作在该离线之后”，不必反向再给操作新增一个 after 字段。timing=imprecise 与严格偏序亦不矛盾，偏序不要求精确到 tick。因此不能把“反方向没有字段”或“无精确时刻便不能排先后”当作证明。

但上述可行解释是需要补写的跨域约定，现文没有它；when.condition 的 Decision 也没有既定事件谓词语法，L17 反而禁止将任意字符串当执行函数。因此未能证明现稿已覆盖。发现所举两段历史“都合法、必有不同结果”还需阶段域、后效及状态见证，不能仅由离线未限定阶段就完成全规则审查。本条保留接口未闭合，不将这些额外结论当成已证事实。

### K-r2-L1-3

**refuted=false：未能否证成品拿取缺少具体编码。**

回源：任务 L8 正面授权零干预期从仓库拿成品，L12 仍限制操作精度；规则 L14、L41 把仓库纳入基地内的核心格。运行语义 L136 已给拿取动作的物品、非负整数件数、不得超过库存、仓库减量且不计交付等守卫。输入 L236 却仅指定 product_withdrawal 为 Decision，L17 没给该 value 的字段、策略语言或引用类型；L232 的 withdraw_product 则属于有限调试数组，未获定义为零干预事件流。语义机制轴 warehouse.withdrawal_policy/timing 也没有补出表示。

对“必要性”作条件化核算：若存在一个正交付、完整仓库状态闭合的周期 P，按约束·取货口配置，52 个普通出库口都只出矿。对一种成品记周期入库 I、玩家拿走 W，在没有另经全规则审查的去货机制时有 Δ库存=I−W=0，故 W=I>0。任务目标给两种成品分别 I≥0.6P、I≥0.55P。这说明表示拿取关系对完整达标周期有实际用途。

这不是已证明存在这样的周期，也不授权按周期末精确拿取，更不把任何未审离线损失当作已排除。输入 L236 明写仓库纳入完整循环，L195 的 periodic_lift 仍为义务；给义务留 unresolved 不会产生拿取历史的字段，故缺口存活。

### K-r2-L1-4

**refuted=false：未能否证几何阶段及动作衔接未定义。**

回源：规则 L11 允许旋转，L10、L16、L28 约束重建、通道与接通；任务 L2 评价布局空矩形，L8、L11–12 约束动作阶段。输入 L42、L49–53 给一份 origin/rotation/occupied_cells/PC，L232 同时列 rotate/rebuild，L185 将旋转阶段留 open。它没有说明 layout 在建造时、初态锚点还是调试终态成立，也没有把某次几何变化关联到 construction 的事件及 PC 派生历史。

单份几何不在信息论上必然不够：若明确它为初始几何，并定义动作序列的结构后效与坐标约定，终态可从历史导出，不必再冗余存一份快照；“两条路都必须补另一份几何”的论证过强。L232 的“记录不等于授权”也正确保留了阶段语义未定，不能把 open 当成“调试旋转已证合法”。

可是现稿既没给初始快照定位，也没给该导出约定，故无法据此否证。三个样例 debug_operations=[]，检查器 L330 拒绝非空调试；本席的静态占格/PC 复核仍成立。本条不是这三个无动作结构样例已经算错，而是一般历史输入的阶段解释缺口。

### K-r2-L2-01

**refuted=false：未能否证规格未完成本轮修订。**

证据独立于 mtime：当前输入 SHA-256=03712e5375fe4291f38e5309acd2514d281d0499296616e16d6106ccd2a5256b，与两份第一轮否证档案分别 L20、L21 的读取快照一致。检查器也仍为 a463ae…。规格目录没有内核输入-修订记录.md。两席第一轮各判 13 条 false、2 条 true，现有被指字段和程序限制再次读到并复现；第二轮 §3.C 的修订环节没有由这些当前字节证明完成。

本席另回查原会话文件 /home/zhuran24/.claude/projects/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0.jsonl L384：type=user、origin.kind=human、时间 2026-09-19T15:55:57.932Z，原话确为：

> 理解了，不过这样下去claude侧的额度反而可能会先用完，那后面只能把部分的审查工作，或者说，除主力开发以外的部分其他工作也交给codex了，这次就先算了，下次

其明确讨论对象是工作分派，没有明确说停止内核输入修订；不能据这段原话证明任务已被撤销。原起草人的第二轮执行回复没有在已定位的工作流状态文件中找到独立原件，因此关于起草人“完全未读任务书”的行为描述仅为复核材料转述，不作为本席的已证事实。scratchpad/step1/round2_state.json 的 drafts.C 仍是首次起草摘要，不能冒充第二轮停工原件。

收窄：样例与检查报告后来已由 B 线更新，不能再称本轮所有产物一字未改；“至多 2 轮”亦非必须恰跑两轮。存活事实是 C 线规格及既有缺口没有修订，现有用户原话不足以否掉此执行缺口。

### K-r2-L2-02

**refuted=false：未能否证 warehouse_slots.item 的设定分类无正面依据。**

规则 L13 只规定格内容与同种单格，L14 只定义仓库；L41/L73 中“可自行设定”的宾语是端口连接哪个仓库格。L21 给开关，L64 给准入口身份和限额，L22 只对已有可设项授权更改，任务 L8 也没有另给格物种设置权限。

输入 L30 明称 settings 是“明确可设定项”，L92、L98 却把格到物种表放进来；L99 另有真正对应端口指派的 warehouse_assignments。即便仓库槽位符号表可以作为合法工程信息，正文没有把它与可改设定区分或明确为库存投影。运行语义 L28 的对应分类也没有该项。故归类缺口不能否证。

但不能从 JSON 类别自动推定现程序已实现改物种动作。L232 的 assign_warehouse_slot 可按 payload={port,slot} 修改合法连线；第一轮已否证“不能指名端口”的发现，现文仍覆盖它。本条不借分类问题复活那条已否证指控。

### K-r2-L2-03

**refuted=false：未能否证桥接器逐轴待定方向无值可填。**

规则 L63 把方向交给先接邻端，L9/L28 与任务 L13 约束相关历史；运行语义 L27 要求“每对端口的已定/待定方向”。输入 L38/L51 只给必填目录整数索引。当前目录四项确为南进/北进 × 西进/东进，全部同时给两轴定向；check_examples.py 真正的整数限制在 L146–147，不是发现引用的 L133。内存置 null 即报“端口型索引非法”。

否证尝试：L51 已明说“预期导出结果，不能作为设定”，L80 保留全规则相容建造次序域。故给一个历史见证的确定方向，不等于擅自删除鲁棒量化域；离线也不必然改变已成方向。L60 的规范端口 id 不含角色，不能称端口 id 必然改变。

上述边界没有让字段能记录某一轴未接触时的待定身份。PC 表为 null 只推迟边表导出，不解除必填索引。因此保留表示缺口，不靠“每个未定历史都已合法”的未经证明前件支持它。

### K-r2-L2-04

**refuted=false：未能否证重新接通事件的存储义务没有落实。**

规则 L10 的重建明确使通道重新接通；L28–32 使这份历史参与后续轮询和优先级。输入 L47 已说重建保留设计实例 id、建造事件另有 id，L70 又明确要求重建接通事件另存 id。然而 L80–82 的 construction 是 blueprint_once，给 selected_order 时 moments 与一次实例集合对应；L209 只给 construction_then_offline_events 派生标签。L232 的 rebuild payload={unit}，没有再建成事件或事件到 PC 的类型。

操作 id 和 when.after 确实已经存在，因此“全文没有任何 id 可命名重建”不对；但把操作 id 当作再建成/各接通事件身份，需要明确对应、时间与失效记录，原文没有。把 selected_order=null 后的 moments 自行扩成重建史也未经定义，不能拿程序对子集的限制充当一般语法定理，亦不能靠空值直接补出新语义。

“任何合法输入都无法满足”应限于需要承载实际重建接通的输入；空调试根本不触发该义务。检查器不执行非空调试，未实测出错误调度。收窄后缺口仍在。

### K-r2-L2-05

**refuted=false：未能否证离线计数/窗口后效没有具名轴。**

规则 L64 同时约束累计数、窗口数、首件开窗与重开窗；约束·准入累计在循环域约束限流。运行语义 L116 明列“库存/计数/进度”离线后效未定，选择点清单 L95 也点名累计数。输入 L88 却称清单包括运行语义 §3.1 的项目。

逐条核输入 L164–179 与轴表 L68–83：offline 六轴是 order_domain/events/cursor_effect/inventory_effect/progress_effect/direction_effect；gate.counter_edit 只管改身份/额度，counter_start 管设限前后，window_clock 管阻断时走钟，recovery 各有阻断恢复触发域。运行语义 L30 的 progress 是制造和传输冷却，不包含准入口累计计数。没有文字可把 progress_effect 扩成任意状态后效。

L225 的 gate_counters 是某状态三个值；L86 的 effects 必须引用已有接续轴。初值不能替代离线转移关系，故全称覆盖自述缺项。L213 已要求遇未解后效报 unresolved/unsupported，所以缺轴不表示默认保持，也不授权清零。

### K-r2-L3-05

**refuted=true：当前目录、样例与报告已重新自洽。**

独立核对一致性侧的被指文件：当前 catalog 内容哈希为 74d21a…，schema=static-catalog-v2；三个输入的引用全部同值。现检查报告的全部 artifact_sha256 匹配实际文件；以拦截写入的方式运行完整 --self-test --report，输出与磁盘报告逐字节一致。§9 所报 4/4/9 个 PC、0/0/8 个 BC 和 11 个正确拒收负例均在现快照重现。因此当前“退出 1、通过报告失效”的核心断言已被修复事实否掉。

该发现的历史测试保留：旧引用的内存副本确实失败。schema 是结构版本，sha256 是精确内容身份；两轮任务书没有规定增加目录内容必须换 schema，也没有规定输入要重复存目录 version。哈希失配是未核对依赖版本，检查器没有把它说成物理非法；输入 §8 针对超出支持子集的 unsupported 纪律不要求把所有完整性错误也归成该类别。

现有再生成命令及 B 线已完成的核对刷新说明维护路径存在，不等于所有一般输入都已有自动迁移。潜在的后续版本维护改进不能使已恢复的三个当前输入仍被判为加载阻断。第二轮 §5 的 B 复核并行安排也允许目录在初次 B.1 完成后继续修订。

### K-r2-L3-01

**refuted=false：未能否证 C 线规格未落实上一轮存活意见。**

本条独立对照输入全文、第一轮两席档案与本轮任务书：当前 50122 字节输入哈希仍是 03712e…，检查器哈希仍是 a463ae…；StateSeed 缺口、仓库设定归类、桥方向、来源映射、子集诊断、候选指纹六类问题逐一仍在，且内核输入-修订记录.md 不存在。第二轮 §3.C 的循环包含修订，当前对象不足以证明该步完成。

§2 K-r2-L2-01 已给原始用户消息的位置与原文，讨论的是审查等工作的改派。没有从原话读到明确取消内核输入修订。本席不把自然语言承接判断升级为对起草人意图的确定归因；起草人“不读”的叙述仍未取得独立执行原件。

发现“样例和检查结果也全为 r1 字节”的证据已过期：B 线只更新样例哈希和报告，见 §1.1 的可逆字节核对；这既纠正其附带事实，也不消除规格未改的核心。完整性批评可以如实登记尚未收口，不是逻辑上绝对无法开展；也不能从“至多 2 轮”推出必须完整跑两轮。

### K-r2-L3-02

**refuted=false：未能否证 semantic_context 的运行历史未进入输入表示。**

规则 L25 要求同刻逐个判定，约束·端口速率 L30 要每端口每 tick 至多一件。运行语义 L23/L33 明列未决事件日程、tick 内已发生移动记录；受限模型声明 L19 的重复判据包含本刻端口额度。参数排序/重试机制只定义“如何排”，不等于当前实际已执行哪些事件。

输入 L219–226 的 StateSeed 只有 inventory/progress/logistics/environment，已有 entered_at、gate_counters 与 poll_memory 各有特定用途，没有上述日程/成功移动记录或其可靠导出关系。不能从库存快照、格内现物入格时刻自动还原已离开或已制造消耗的物品曾用过哪个端口，也不能在 time.instant_end 仍未定时默设所有输入都位于额度清零边界。

反向证据 L228 明承认“不是已有完整 State 恢复器”；L279 也不声称检查运行恢复。故不判它已经错误续跑或伪报完整恢复。保留的是第二轮 §3.C.4–5、输入 L7 的状态接口仍缺已列出的运行信息，通用升级提示并不说明它们已能表示或可无损省略。

### K-r2-L3-03

**refuted=false：未能否证只接一轴的桥无法如实记录另一轴待定。**

规则 L63 逐对给先接方向，没有要求桥的两对都必须有邻接端；约束·满速独占 L56 还明确某一路前件不扩及另一对。运行语义 L27 已列逐对待定状态。目录的四个布局都同时固定南北、东西两轴；输入 L51 无逐轴未知值。

本席将桥样例东西两个箱的 y 坐标仅在内存移至 30，独立复算仍不重叠/越界，水平轴无邻端，竖直两边仍相遇；更新相应 PC 与路径后，检查器在 L249 报“unsupported: 未接桥轴方向无法在此子集核验”。这只是单轴几何与当前拒收的见证，不是已获起动和运行认证的完整任务布局。

为闲置轴任填一种角色，当前静态图可能同观测；但草稿既未规定这样的抽象，也未证明它对后续接触/调试保持行为。程序正确报不支持，不能据此认定一般格式已能编码待定状态，故缺口存活。

### K-r2-L3-04

**refuted=false：未能否证 settings 与库存身份的接口分类冲突。**

本条逐项比对运行语义 L28 与输入 L30、L92、L98。前者的明文可设定项是开关、端口到仓库格指派、准入口身份/限额；后者额外包含格物种记录。规则 L13/L14 是容纳和仓库定义，L41 的授权宾语是端口连接的格，均不能导出玩家任设格的物种。任务 L8 限制动作，不会把运行状态自然变化全部冻结。

样例当前仍有 quantity=0 而 item 非空的格；例如 warehouse_0 为源石粉末，电池/胶囊也都预列。制造 empty_slot_identity 的辖域不能自动扩到仓库。若这只是符号表，需说明空身份如何随库存及引用演化，不能继续一面称它为可改设定、一面靠静态身份解释库存。

收窄两点：样例有限列出配方物种，不直接与“不宣称游戏物品域有限”矛盾；任务禁止玩家改设定，也不证明实现必然把所有空格永久锁种。永久冻结是分类混乱可能造成的实现风险，不是本席已实测行为。核心归类和后效接口仍未被覆盖。

### K-r2-L3-06

**refuted=false：未能否证桥定向检查未按已登记解释轴条件化。**

规则 L16 要存取互补才成通道，L28 联系形成与建造，L63 要先接邻端决定方向。选择点参数轴 L47、输入 L143 把“物理邻接先接/有向通道形成先接”列为 open；选择点清单 L55 特别要求历史核验条件化。输入 L84 却无条件要求找“最早出现的已知相邻端口”。检查器 L243–255 收集所有面对面邻端，先按建造 rank 取最早，再查角色互补；未读取该轴的取值，样例该轴仍 unresolved。

独立内存变体把 west_box 改为 r90，重新按几何删除一条失效 PC 及失效路径。水平轴核算为：

| 桥端 | 桥端角色 | 邻端 | 邻端角色 | 首次邻接 rank |
|---|---|---|---|---:|
| bridge:west:0 | input | west_box:south:0 | input | 4 |
| bridge:east:0 | output | east_box:south:0 | input | 5 |

共 3 条 PC；西侧同类不成通道，东侧互补成通道。程序确报“桥方向与先接邻端不互补”，无 unsupported 前缀。原 bridge_two_lanes 断言随后也将不再适用于这个少一路的变体；本席没有把变体说成仍满足原场景意图，记录的是在到达场景断言前发生的桥先接分支拒收。

此变体在“有向通道先接”的解释下满足局部方向方程，仍不能凭自洽证明整个建造史合法：轴表明说 open 不等于获准，选择点 L55 明说联立解不保证可执行历史。所以“另一读法下合法布局已经被物理证伪”的强说法不采。没有条件化的通则、未注明采用哪个分支及错误分类缺口仍在，不能因反例的强措辞过头而整条否证。

### K-r2-L3-07

**refuted=false：未能否证来源绑定覆盖及一致性条件不够明确。**

回源：规则 L41/L73 要真实核心/边界口，约束·取货口配置 L34 规定达标来源；第二轮 §3.C.6 要与契约映射。当前送料契约 L118–126 定义 identity 的四字段、来源域及互异，当前 contract.json 全量读取仍为 219 台机器、52 个来源、315 个 LF，52 个 identity 均“待求”且其余字段 null。三例 binding 均 null。

输入 L246 对机器显式要求全覆盖，L247 对 source_map 只要求逐个互异、物品相符和边界序号。未写 sources 中已指定 identity 如何约束该表，也未写完整绑定必须包含全部来源；不能由互异推出覆盖。对 ORE000:out:1，port_map 与 source_map 的相等关系未展开。若已指定身份为左边界第 3 口而绑定下边界第 1 口，需要一个明确比对步骤判错，现文未列出。

反向证据：L247 的互异已部分落实 all_different，不能说完全没有一致性条件；L249 的“等逻辑端口”“角色、端点须匹配”没有排除 ORE/CORE，并可被合理读作隐含交换关系。即使给这种宽读，来源全覆盖和已指定 identity 的核验仍没落实。L338 明拒收非空 binding，因此没有程序放过矛盾绑定的实测，也不指控现候选已有此矛盾。

### K-r2-L3-08

**refuted=false：未能否证最小子集说明与诊断存在不一致。**

回源：规则 L9/L28 规定建造先后，没有禁止保存真传递关系；第一轮 §4 的枚举默认“包括”。输入 L80 明许 selected_order=null，L82 未将 time_relations 限成恰好相邻链，L98 明许 item=null；L279 则承诺超子集明确报 unsupported。

从当前可通过基线逐一修改内存副本，实测：

| 修改 | 原始检查信息 |
|---|---|
| selected_order=null | 建造见证必须每单位恰一次 |
| 已有相邻链外增加 build_0<build_2 | 符号时刻关系须与见证次序相符 |
| 增加一条新 id、item=null、初量 0 的仓库格 | 仓库格 id 重复或未知物品 |
| 增加两条不同 id 的上述空格 | 仓库格 id 重复或未知物品 |
| anchor 改成 specified（对照） | unsupported: 此检查子集不解释该 Decision 状态 |

前三类无前缀，分别由 L207、L228/L234、L282 触发。增加真关系没有改变时间约束的可满足集合，不是将“包括”扩大成任意未知 JSON 键。L284 的 set(slots.values()) 若未来放行两个 null 会再把它们当同物种重复；当前先被 L282 拦住，此项是代码推论，不称已经走到 L284 的实测。

L279 已登记“仓库格绑定目录已知物品”，所以拒收 null 本身属于已声明的子集，不能说该限制完全未登；其未区分 unsupported 的诊断仍有问题。selected_order 非空和恰为相邻链没有同样登记。顶层 status 是“失败或不支持”，不等于明确宣告布局物理非法；但也没满足逐项明确分类的承诺。

### K-r2-L3-09

**refuted=false：未能否证无语义依赖的候选指纹导致硬失败。**

第一轮 §1/§4、第二轮 §4 区分候选与正式约束。输入 L7 所引来源、几何检查和参数检查均不使用候选条文。检查器 L55–60 却将候选字节指纹放入 SOURCE_HASHES，L430–431 在结构检查后比较整个字典；L461–463 将 CheckError 转成退出 1。

当前所有源指纹相符，基线通过。随后仅 mock Path.read_bytes：候选文件返回原字节加一个换行，其余返回原字节；同一 main() 抛出“正式/候选文件指纹与读取快照不同，须人工重新核对”。文件本身没有写入。结构、目录、轴表不变而硬条件翻为假，因果链已复现。

保存候选指纹可用于一次执行的只读审计，本身不等于采用候选规则；这解释不了以后无关候选笔记变化为什么必须使静态检查失败。保留工程耦合问题，不扩大为候选已被升级成正式约束，也不称当前尚未修改候选时基线就失败。

## 3. 可重放的只读诊断

以下片段使用当前样例；不运行生成器，不落盘变体，不写既有检查报告。第一段的 --report 通过 mock 捕获输出，验证完整报告管线而不触碰文件。

~~~python
import copy
import hashlib
import importlib.util
import io
import json
import sys
from contextlib import redirect_stdout
from pathlib import Path
from unittest.mock import patch

base = Path("求解器/数据/样例").resolve()
spec = importlib.util.spec_from_file_location("checker", base / "check_examples.py")
checker = importlib.util.module_from_spec(spec)
spec.loader.exec_module(checker)
path = base / "桥接器双通路.json"
original = checker.load_json(path)

def probe(label, data):
    try:
        result = checker.check(data, path)["status"]
    except checker.CheckError as error:
        result = str(error)
    print(label, result)

probe("baseline", copy.deepcopy(original))
data = copy.deepcopy(original)
data["construction"]["selected_order"] = None
probe("null_order", data)

data = copy.deepcopy(original)
data["construction"]["time_relations"].append({
    "before": "build_0", "after": "build_2",
    "relation": "strict", "basis": ["既有链的传递推论"]
})
probe("extra_true_relation", data)

for count in (1, 2):
    data = copy.deepcopy(original)
    for index in range(count):
        slot = "empty_" + str(index)
        data["settings"]["warehouse_slots"].append({"id": slot, "item": None})
        data["initial_state"]["warehouse"]["slots"].append({
            "slot": slot, "quantity": {"value": "0", "category": "候选"}
        })
    probe("empty_slots_" + str(count), data)

report = base / "检查结果.json"
captured = {}
def capture_write(target, text, *args, **kwargs):
    assert target == report
    captured["text"] = text
    return len(text)

with patch.object(sys, "argv", ["check_examples.py", "--self-test", "--report", str(report)]):
    with patch.object(Path, "write_text", capture_write), redirect_stdout(io.StringIO()):
        checker.main()
assert captured["text"].encode() == report.read_bytes()
print("report_bytes_match", True)

# 只改变候选文件在内存中的读取结果，不改磁盘。
read_bytes = Path.read_bytes
candidate = checker.ROOT / "候选约束.txt"
def mocked_read(target):
    raw = read_bytes(target)
    return raw + b"\n" if target == candidate else raw

with patch.object(Path, "read_bytes", mocked_read):
    with patch.object(sys, "argv", ["check_examples.py"]), redirect_stdout(io.StringIO()):
        try:
            checker.main()
        except checker.CheckError as error:
            failure = str(error)
        else:
            raise AssertionError("候选指纹变动未触发预期失败")
print("candidate_append_in_memory", failure)
~~~

运行方式为在仓库根以 python -B 读取该片段。独立几何复算的结果和方法在 §1.2，桥变体的单位变动、派生表及首次拒收位置在各条给出；它们与这里用于诊断检查器的探针分开，不把调用原检查器当成独立几何证明。

## 4. 快照指纹

以下文件仅作为本次读取和验证的基准；它们没有被本席改写。时间均为 UTC，避免与发现列表的纽约当地时间混淆。

| 文件（相对仓库根） | SHA-256 |
|---|---|
| 《明日方舟：终末地》游戏规则.txt | abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670 |
| 求解任务.txt | 10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864 |
| 求解约束.txt | f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6 |
| 候选约束.txt | a0ac3f9c82f371132b7de29fe47e4c6c587904680bc2786523de0c825fd5efd9 |
| 求解器/规格/内核输入.md | 03712e5375fe4291f38e5309acd2514d281d0499296616e16d6106ccd2a5256b |
| 求解器/规格/运行语义.md | e5a56eb511c0452fb81f7a00c68b9ebb1e902abefb36eaacf25d09bc4f4f686d |
| 求解器/规格/选择点参数轴.md | 490a4c82d299a2030049cfc1596e5f4841cabf7b47a9c9de7d0da7ed75147f49 |
| 求解器/规格/选择点清单.md | 4ddfc29100ca28a3defea9b370d0dadee9e3b6d48978871b31f5f7e5a5b158bb |
| 求解器/规格/受限模型声明.md | 7df9baa7860881bbd0a8fbb4616da86d44459330b5341f1096c5b9637c473827 |
| 求解器/数据/正式静态目录.json | 74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43 |
| 求解器/数据/送料契约.md | 1ce714aaa4f2ccd251ebd22190fbf3234e56948a35c5d931a9e8b78c566f0201 |
| 求解器/数据/候选B/contract.json | a555045972861bc60a84ade0cfaf81a037a1abfff89be1aeee578b9ef0e1a2db |
| 求解器/数据/样例/桥接器双通路.json | 457e872989ef3a92d9cb135126731a6b413507e74d400032196e16aa8bcb34e1 |
| 求解器/数据/样例/分流器三路轮询.json | 519edb40ea28331c6c7b2a42ad91ef2453488d6ed8d36c26cd45e6551ffba7e3 |
| 求解器/数据/样例/混做粉碎机两下游.json | 009c4adacf998f9b10d2dd42283b592c2be7645be5c9720a4e8de83cb0bb959b |
| 求解器/数据/样例/check_examples.py | a463aec57b8e4398edf956b9aa961c7536e9baf1f27c65cacba437d968cafa26 |
| 求解器/数据/样例/generate_examples.py | 11268cd730238697c4bc49b33f7da69d68737e29339e617a6ab1adadd1b331f8 |
| 求解器/数据/样例/检查结果.json | 1c3d07d805be5c4a5312068b3e631fb936096a94b0cfd89398da6b746104e8ca |

### 4.1 收尾核对与并行修改

17:12 UTC 比对本轮读取基准：三份正式文件、候选约束、内核输入规格、运行语义、参数轴、选择点清单、受限声明、目录、三个样例、生成器、检查器、检查报告及候选 B 契约的字节均未改变。送料契约.md 在本席工作期间由其他流程改变，收尾 SHA-256 为 90b8db0295aca96d45e6811bfaa4f9c17e87bd3340e56368773139b63e483bea；上表保留的是起始读取快照。重新回读其 L111–126，identity 四字段、来源域及 all_different 条款与本次所引内容一致，没有新增 source_map/port_map 对接条件，故不改变 K-r2-L3-07 的判读。

报告读者自审已逐项检查：18 个 id 均有独立判读且与结构化区一一相同，2 条 true 均限定到已修复的当前产物，16 条 false 均给出“未能否证”及存活范围；规则依据、快照时点、程序拒收与合法性推论分开。正文纠正了发现中的过强表述，没有将字段占位、open 或历史实测冒充完整执行证明。代码片段使用 Python 标准库、仅内存变体与拦截写入。本席的文件写入仅为本报告；未操作模拟器，未提交或推送。

## 5. 结构化判定

~~~json
{
  "report_path": "/home/zhuran24/zmd-research-fresh/求解器/规格/复核/内核输入/否证-r2-2.md",
  "verdicts": [
    {
      "id": "K-r2-L1-1",
      "refuted": true,
      "reason": "当前快照已覆盖：三个样例均已更新为目录指纹 74d21a5b…，只读复跑与 --self-test 均退出 0，报告字节可复现。历史故障可复现，但已非当前阻断；L9 实际指参数轴表，哈希锁定本身不是格式错误。"
    },
    {
      "id": "K-r2-L1-2",
      "refuted": false,
      "reason": "未能否证：跨建成、调试、离线事件的统一引用域及合并顺序未定义。需收窄：若允许跨流引用，before_event 能反向定序，imprecise 也不妨碍偏序；现文仍未明确允许这种编码。"
    },
    {
      "id": "K-r2-L1-3",
      "refuted": false,
      "reason": "未能否证：product_withdrawal 只有 Decision 占位，没有拿取历史或策略的值结构。正交付完整循环须有相应成品出库收支；此条件不证明循环必存在，也不授权精确定时拿取。"
    },
    {
      "id": "K-r2-L1-4",
      "refuted": false,
      "reason": "未能否证：layout 未绑定几何阶段，也未定义调试结构动作如何衔接建成史。未必需要另存终态快照，但须给初始几何与动作推导规则；三个空调试样例的静态检查不因此失效。"
    },
    {
      "id": "K-r2-L2-01",
      "refuted": false,
      "reason": "未能否证：规格及检查器仍为 r1 字节，修订记录缺失，原有表示缺口仍在。已回查用户原声，其讨论对象是审查改派，没有明确取消规格修订；样例哈希后来已刷新，不能再称全部产物未变。"
    },
    {
      "id": "K-r2-L2-02",
      "refuted": false,
      "reason": "未能否证：settings 明称可设定项，却包含 warehouse_slots.item；规则仅授权端口到格指派，没有格物种设定。保留分类缺口，不据此声称检查器已实现改写物种的动作。"
    },
    {
      "id": "K-r2-L2-03",
      "refuted": false,
      "reason": "未能否证：必填整数 port_layout 只含两轴已定的四种组合，不能表示逐轴待定。单一历史见证本身不等于鲁棒域被删，端口 id 也不因角色变化必然改变；这些收窄不补上缺失类型。"
    },
    {
      "id": "K-r2-L2-04",
      "refuted": false,
      "reason": "未能否证：L70 要求重建接通事件另存 id，但蓝图 moments 与 rebuild 操作之间没有事件类型、时刻及派生关系。操作 id 已存在，空调试可不触发此义务；缺口限于需记录重建接通的输入。"
    },
    {
      "id": "K-r2-L2-05",
      "refuted": false,
      "reason": "未能否证：六个 offline 轴未承接准入口两个计数及窗口起点的离线后效；gate_counters 是状态值，counter_edit 等轴另有触发域。缺轴不意味着默认保持或获准清零。"
    },
    {
      "id": "K-r2-L3-05",
      "refuted": true,
      "reason": "当前已修复：样例引用现目录哈希，三例及 11 个负例通过，拦截写入后完整运行 --self-test --report 所得内容与现报告逐字节相同。schema 与内容指纹分工明确，无须因每次内容更新更换 schema；历史失配不否认。"
    },
    {
      "id": "K-r2-L3-01",
      "refuted": false,
      "reason": "未能否证：内核输入规格仍为 03712e… 且没有修订记录，r1 存活缺口未落实。用户原声未明确撤销任务；后续 B 线仅刷新样例目录引用，不能替代 C 线规格修订，也不能再断言样例仍是 r1 字节。"
    },
    {
      "id": "K-r2-L3-02",
      "refuted": false,
      "reason": "未能否证：StateSeed 缺少未决事件日程与 tick 内移动记录，参数机制不等于当前调度历史；没有可省条件或完整导出方法。L228 已承认非完整恢复器，故不指控现程序伪称完成运行恢复。"
    },
    {
      "id": "K-r2-L3-03",
      "refuted": false,
      "reason": "未能否证：只接一轴的桥缺少另一轴待定值；四个目录布局均同时定两轴。独立构造单轴几何后程序报 unsupported，说明检查边界，不能证明一般格式已有待定方向编码。"
    },
    {
      "id": "K-r2-L3-04",
      "refuted": false,
      "reason": "未能否证：仓库格物种被归入 settings，与运行语义的明文设定划分不一致，空格身份后效亦未说明。但有限样例预列物种不直接证明物品域被裁有限，永久冻结只是实现风险。"
    },
    {
      "id": "K-r2-L3-06",
      "refuted": false,
      "reason": "未能否证：桥定向代码无条件取最早相邻端，未按 open 的 bridge_first_contact 分支；变体复现“不互补”且无 unsupported 前缀。另一读法下的自洽不能冒充完整合法建造史证明，保留条件化与诊断缺口。"
    },
    {
      "id": "K-r2-L3-07",
      "refuted": false,
      "reason": "未能否证：source_map 缺少全部来源覆盖及与已指定 identity、逻辑端口映射相符的明确核验条件。“逐个互异”已部分落实 all_different，ORE/CORE 也未被语法排除；现检查器拒收非空绑定，尚无错误绑定获准实测。"
    },
    {
      "id": "K-r2-L3-08",
      "refuted": false,
      "reason": "未能否证：null 建造见证、追加真传递关系、空仓库格均复现无 unsupported 前缀的拒收，前两项子集限制未列明。已知物品限制其实已列入 §8；两个 null 的去重问题是静态推论，尚先被前一道检查挡住。"
    },
    {
      "id": "K-r2-L3-09",
      "refuted": false,
      "reason": "未能否证：仅在内存给候选文件追加换行，同一 main() 即触发指纹硬失败；候选内容未参与几何语义检查。保留无关依赖导致复现失败的事实，不将保存候选指纹等同于采纳候选规则。"
    }
  ]
}
~~~
