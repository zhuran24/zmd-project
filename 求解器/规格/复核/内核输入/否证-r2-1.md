# 内核输入第 2 轮逐项否证：第 1 席

日期：2026-09-19。性质：GPT 源第 1 席的判读存档；证据截点为本日 13:04:43（America/New_York，17:04:43 UTC）的复现快照及交付前指纹复核。18 条发现中，2 条对当前快照已否证，16 条未能否证。false 不表示发现的每个附带断言均获证明；各条明确保留范围。

产物路径：/home/zhuran24/zmd-research-fresh/求解器/规格/复核/内核输入/否证-r2-1.md。

## 1. 来源与快照边界

两轮任务书已先行完整读取：

- [第一轮任务书](/tmp/claude-1000/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0/scratchpad/step1/任务书.md)：§1、§4 确定三份正式文件的权威、候选未审及“未定不是取值的来源”。
- [第二轮任务书](/tmp/claude-1000/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0/scratchpad/step1/任务书2.md)：§2 的 T7 分配、T6 判定粒度及守恒勘误已定事项沿用；§3.C 要求布局、建造/离线、设定、参数、初态调试、送料映射及三个合成输入；§4 规定否证标准；§5 明确目录依赖与后续收口。

“规则”“任务”“约束”分别指仓库根《明日方舟：终末地》游戏规则.txt、求解任务.txt、求解约束.txt，均已回源全文核读。下文“输入”指求解器/规格/内核输入.md；“语义”“轴表”“受限声明”分别指运行语义.md、选择点参数轴.md、受限模型声明.md；“检查器”指求解器/数据/样例/check_examples.py。行号对应附录指纹。送料契约现版来源字段比发现所引旧版下移一行，现为 L118–126。

回读对象还包括选择点清单的相关条款、完整性批评、两席 r1 否证和三份 r2 复核原文、样例生成器、目录及候选 B 契约。三个 JSON 与目录、契约均全量加载；参数表 87 行的名称及说明逐项对齐。旧复核只用于核对历史记录，当前判断另由正式条文、被指原文及本次试验支持。候选约束仅核指纹依赖，不引用其候选论断。

### 1.1 不能沿用的旧快照

三例现在的 catalog.sha256 均为 74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43，与当前目录相等。三例 mtime 为本日 12:56:42，晚于发现记录的 12:53 失败。当前报告也在 12:56:42 更新。

将三例各自的内存副本仅把 catalog.sha256 换回 63acbb92067fd9ba4229f7b83de8b5054023b6b4e520f9e7077d48b562896230，按原有缩进和末尾换行编码，得到的哈希逐一等于否证-r1-2.md L30–32 的旧样例哈希。这证明当前三例相对该 r1 快照只更新了目录指纹，不是语义缺口的修订。输入规格仍为 03712e…，内核输入-修订记录.md 仍不存在。

本席没有修改这些文件。K-r2-L1-1、K-r2-L3-05 的 true 只否证“当前仍然不能加载、当前报告已失效”，不抹去旧快照的失败。

## 2. 逐项判读

### K-r2-L1-1

**refuted=true：当前加载失败已被新快照否证。**

回源：第二轮任务书 §3.B.1、§3.C.7、§5 要求共享目录和可检的合成布局；规则 L8、L11、L16 及各单位条文约束几何。输入 L27 用路径和字节哈希固定具体目录，检查器 L191–196 先验哈希再加载。当前目录和三例所填哈希相同，普通检查与 --self-test 均实跑退出 0；三例通过，11 个负例正确拒收。现有检查结果.json 与本次自测输出解析后的对象完全相等，artifact_sha256 的每一项均匹配磁盘。

输入 L272 的带报告命令为保护既有产物，使用原参数并仅截获 Path.write_text：检查全部完成，到达一次正确目标写入，拟写 JSON 与既有报告相等，没有实际写盘。故不是仅绕开哈希后得到局部通过。仅把内存哈希换回旧值时仍可复现原失败，说明旧报告指向的是较早输入，不是当前输入。

附带主张也不足以维持阻断：L9 的“变化时先核对再迁移”明指轴表，不是目录；L275 已给再生成入口，生成器 L35–44 读取当前目录并计算哈希、L64 在写入前检查。该入口不是任意版本自动兼容证明，但不能称完全没有更新路径。任务书没有要求目录每次兼容内容更新都改 schema，或每个消费者另存 version。识别内容的 SHA-256 已覆盖目录中的 version 字节。

### K-r2-L1-2

**refuted=false，未能否证：跨流事件关系的引用域及合并定义仍欠明确。**

规则 L10、L28–32 使重建接通历史参与轮询及分级；L64 的阻断改变当时边集合；任务 L11–13 要求处理不精确调试及离线。第二轮任务书 §3.C.2、C.5 要求这两类输入。输入 L80–82 定义一次蓝图的 moments/time_relations，L86 定义离线锚点，L234 将 when.after 限为前序操作。原文没有给三个事件集合的联合引用类型、跨流 id 冲突约束及各类关系的合并规则；L209 的派生标签也未展开到调试重建。when.condition 只是带依据的可观察条件，没有已定义的事件谓词编码；不能自行发明来补洞。

发现的部分论证过强，不能原样采纳：L86 没有明文说 after_event/before_event 只准建成事件；若允许它们引操作 id，则 before_event 本来就能表达“离线在操作之前”，无需 when.after 再引离线。imprecise 禁的是精确 tick 和计时，不禁严格先后；strict 关系本身足以表达跨类事件定序。这些纠正没有证明草稿已定义这种联合引用，因此尚不足以否证整个接口缺口。

“调试内离线与改准入的两段历史都合法且结果不同”没有给出完整初态、后效解释及轨迹，不能因 offline.events 为 open 就视为已获合法运行证明。保留的是表示契约欠定义，不把这一叙述当成已经验证的反例。

### K-r2-L1-3

**refuted=false，未能否证：零干预期成品拿取的具体值结构未定义。**

任务 L8 明确授权拿取成品，L12 禁精确 tick 操作；规则 L14/L41 使仓库属于完整基地状态且每种一格、容量 80000。语义 L136 已给拿取物种、非负整数数量及不超现存量的动作边界。输入 L236 却只给 product_withdrawal=Decision，L17 只规定通用外壳，没有拿取事件、策略、数量族、定序及条件的专用编码。L232 的 withdraw_product 属于有限调试数组，未定义为 L236 的策略值或零干预事件族；不能靠复用同名动作自行扩其阶段。

必要性可在正确论域内证明：设某个完整达标循环周期为 P>0，对任一成品有 ΔW=入库量−离库量=0。任务目标与约束“周期倍数”使周期入库量为 0.6P 或 0.55P>0；“取货口配置”让全部 52 个物理出库端口只取两矿。零干预期剩余有正面授权的成品出库是任务 L8 的拿取。因此这样的完整循环必须有相应离库量。登记 periodic_lift 为待证不能代替事件表示。

此论证不证明所有合法运行必进入循环，也不自动排除无循环的逻辑情形；可用策略的观测能力、响应界及全部实际时刻仍待 T12 审查。当前静态样例允许此量 unresolved，没有错误运行证据。

### K-r2-L1-4

**refuted=false，未能否证：几何锚点及结构历史关系不明确。**

规则 L11 允许旋转，L10 定义重建，L16/L28–32 使几何变化涉及边及调度历史；任务 L2 比较布局空矩形，L11–12 约束调试。第二轮任务书 §3.C.1、C.2、C.5 要求几何、建造和调试对接。输入 L42–54 的 layout 只有一组原点/旋转/占格/通道，没有阶段或事件锚点；L185 把旋转阶段留 open，L232 的 rotate 只给目标 rotation，未说明它从哪份几何开始演化、何时作用于 PC 及 construction。

但“必须再存一份最终几何，否则两条路都会算错”并非必然。若明定 layout 为初始结构，并定义旋转位置和事件后效，后续几何可以从历史导出，无须重复存储。真正缺的是锚点和导出规则，不是快照数量本身；同样，open 不等于所有旋转方式已获准。

输入 L279 已将检查结论限于静态结构，检查器 L330 拒绝非空调试。当前三例空调试，故其静态占格/PC 结论有明确的提交几何对象；本条不推翻三例的实测通过，也不证明执行器已经错误核验最终布局。一般含结构调试的表示缺口仍未找到覆盖依据。

### K-r2-L2-01

**refuted=false，未能否证：所要求的格式实质修订没有证据。**

对照第二轮任务书 §3.C、§4、§5，当前输入规格的 50122 字节、03712e… 哈希与两席 r1 否证记载相同；内核输入-修订记录.md 不存在。两份 r1 否证均为 15 条中 13 false、2 true；本次逐条回源确认所涉结构缺口没有被当前规格修复。这是正文不变的证据，不仅是 mtime 推断。

已读原始会话记录：主会话 JSONL L384 的 type=user 消息，及起草子会话 agent-a226e3a602156f6b9.jsonl L1 的 relay，逐字为：“理解了，不过这样下去claude侧的额度反而可能会先用完，那后面只能把部分的审查工作，或者说，除主力开发以外的部分其他工作也交给codex了，这次就先算了，下次”。起草子会话 L11 的 StructuredOutput 明确 files=[]，摘要写“本次修订任务先不做”“未将……修订任务转发给 codex”“本轮无实际执行内容，无文件改动”。其 L2 的计算任务则要求逐条修订及追加指定修订记录。原文足以证明确有空执行及停工归因；用户句子没有明确说停止内核输入修订，直接论述对象是后续改派。

同时保留权限上下文：起草席收到的 harness L1 明说 relay 为该子任务唯一用户声音，计算任务不能覆盖或扩展它。因此不能略掉该上下文，武断宣判起草席违反某条更高权限命令；也不以本席猜测替代用户意图。能确定的是“用户明确要求本次格式停修”的摘要超出了可见原话，且没有实质修订成果可用于否证发现。当前三例及报告后来已更新，故“任何产物都仍是 r1 字节”须收窄为规格及其未解决问题。原始记录路径见 §3.3。

### K-r2-L2-02

**refuted=false，未能否证：格物种被错误归入明确可设定项。**

回源规则 L13/L14 只给物品格性质和仓库定义；L41/L73 中“可自行设定”的对象是某取货端口连接哪个仓库格。L21/L64 另授权开关与准入设定，L22 不会把未授权的格物种改写变成可设定动作。任务 L6 的初始内容也不是另一个随意设物种授权。第二轮任务书 §3.C.3 要求的仓库设定正是端口格指派。

输入 L30、L92、L98 把 warehouse_slots 的 {id,item} 列进“明确可设定项”，L99 又另有真正对应授权的 {port,slot}。L217 只记仓库 slot/quantity，物种借用 settings 表；没有例外说明前一张表仅是库存投影。语义 L28 不含这种物种设定，冲突实际存在。

把表放进 settings 不等于已经实现“任改库存物种”的可执行动作。尤其 L232 的 assign_warehouse_slot 可按 L99 用 {port,slot} 表示合法指派，r1 已否证的动作寻址问题不能借此复活。保留分类及接口自由度误述，不宣称当前程序执行了未授权动作。

### K-r2-L2-03

**refuted=false，未能否证：逐轴待定方向没有值。**

规则 L63 要求桥角色来自先接端，规则 L9/L28 及任务 L13 约束建造与重排；语义 L27 明列桥每对方向“已定/待定”。输入 L38/L51 要必填目录索引；当前目录四个 layout 均为竖轴二向×横轴二向，不含逐轴待定。实测给桥 port_layout=null 被拒“端口型索引非法”，准确代码位置是检查器 L146–147，发现所写 L133 实际为原点读取。

反向证据是 L51 称其“预期导出结果，不能作为设定”，L80 保留全部规则一致建造史，L84 不准由检查器排除一般有环桥布局。因此单个候选历史给一个方向并不自动删去其他接通值，也不能说离线一定翻向。但这些条款没有给不知一轴/全未知的方向状态编码。physical_channels=null 仅推迟导出 PC，不取消索引必填。

端口 id 本身由规范边和位置组成、与角色无关（L60）；有向 PC 的顺序及存在集合才可能随角色变化。保留一般表示缺口，不扩大成所有 id 必变或所有循环依赖方向都合法。

### K-r2-L2-04

**refuted=false，未能否证：操作身份与重新接通事件尚未建立关系。**

规则 L10 明写拆后再造、通道重新接通；L28–32 要以新记录参与后续次序。输入 L70 又明确要求重建后另存接通事件 id。第二轮任务书 §3.C.2、C.5 要求这类历史可交接。

L80–82 的 construction.mode=blueprint_once，非空见证每实例一次；L232 的 rebuild payload={unit}，虽有独立操作 id，未把它定义为新建成事件、或将该 id 与各 PC 的重新接通事件关联。L234 的 effects 没有事件值结构，L209 的 construction_then_offline_events 未展开重建接续。不能由读者把动作 id 随意兼作多条接通事件身份来算已有覆盖。

发现的“任何合法输入都不能满足”须限定在确有重建接通历史要记录的输入。空调试对此可不适用，selected_order=null 时也不能直接套检查器的每实例一次子集限制到全部 moments。最小程序拒非空调试，未产生已验证的错误调度轨迹。

### K-r2-L2-05

**refuted=false，未能否证：计数/窗口的离线接续漏轴。**

规则 L64 的累计、窗口计数和首件窗口起点直接决定能否接收；约束“准入累计”保留相应上限。任务 L13 只明确接通先后变化，不授权清零。语义 §3.1 L116 和选择点 T10 L95 明列计数、窗口及后效未定。

逐项核输入 L164–179 和轴表：offline 六项没有准入口计数/窗口后效。gate.counter_edit 触发于改身份/额度；window_clock 针对阻断；recovery 各项处理恢复；语义 progress 分组是制造进度/传输冷却，不能默扩为所有历史。L225 的 gate_counters 是当前值，L86 的 effects 又须引用 §5 接续轴，当前缺乏两计数及起点的离线关联位置。

L88 自称清单包括语义 §3.1，虽不宣称穷尽，仍不能遗漏其中已经点名的内容。L213 确实规定 unresolved/unsupported，否掉“因此默认保持”的推断；不否掉缺少具名接续接口本身。

### K-r2-L3-05

**refuted=true：当前样例和报告已经恢复一致，所称版本设计必然阻断也不能成立。**

独立核输入 L7/L27、检查器 L194–196、目录 schema/version 和三例 hash：当前全部引用 74d21a…，普通检查及自测退出 0；现存报告的每个产物哈希匹配，报告内容与本次 --self-test 相同。PC/BC、占格与端口独立复算见 §3.1。用原带报告参数截获唯一写入，拟写报告也相同。旧 hash 内存变体仍报原失败，故本结论是快照失效，不否认 12:53 的历史实测。

送料契约现版 L165 明写 schema 保持原几何接口，version 标内容版本。内容变化不必然表示 schema 不兼容；路径＋内容哈希足以唯一固定所读字节，version 不作为额外输入字段并未丢失可追溯性。第二轮任务书没有要求投影哈希、自动迁移或兼容更新免审。B.1 先完成与之后 B 复核继续修改并不矛盾；任务书 §5 明说 B 的复核与 A/C 并行。

对真正 hash 不符，拒收的是“声明的依赖内容不等于实际内容”，不是已支持语义的超子集取值。L279 的 unsupported 纪律不能直接推出所有一致性错误都必须带该前缀；报错本身也明说须重新核对，没有宣称布局物理非法。因此附带的 schema/version/前缀论述不足以在当前已一致时维持该阻断发现。

### K-r2-L3-01

**refuted=false，未能否证：格式修订仍为空，不能将后来的指纹更新记作修订完成。**

独立对照第二轮任务书 §3.C 的工作流及 §5 收口条件：内核输入.md 与 r1 两席所存哈希完全一致，指定修订记录不存在。原始起草子会话 L11 的摘要和未决均承认未读任务书、未转发修订、未写成果；本次逐项核的桥状态、仓库分类、状态种子等缺口也仍在。这足以维持“实质修订没有发生”。

原始 relay L1、主会话 L384 的原话，及 harness 的权限限定已在 K-r2-L2-01 引明。原话谈“把部分审查工作……也交给 codex”，没有明确以格式修订为“算了”的宾语；起草摘要称用户明确停修，依据不足。但不将语言解释升级成对用户意图的确定裁定。

需收窄两点：三例及报告在 12:56:42 已发生指纹迁移，“所有产物逐字节未变”不再成立；“至多 2 轮”是上限，不表示必须恰走满两轮，也不逻辑推出无法做一次报告未完成事项的完整性批评。保留未完成的修订与归因问题，不以轮数本身判错。

### K-r2-L3-02

**refuted=false，未能否证：已登记运行上下文缺少状态种子接口。**

规则 L25 的同刻串行判定、约束“端口速率”的每端口每 tick 上限，需要核验相应实际历史。语义 L23/L33 明列未决事件日程与 tick 内已发生移动记录；受限声明 L19 的时刻归约也要求本刻端口额度。第二轮任务书 §3.C.4、C.5 要求参数与初态接口。

输入 L219–226 的 StateSeed 只有 inventory/progress/logistics/environment；全文不存在这两个历史内容的字段或等价重建约定。排序及重试参数给机制，不给当前进度；entered_at 给入格时刻，尚无证明它能唯一恢复所有已消耗端口额度和待决事件。L217 的锚点与 L234/L238 的不精确调试也没有提供“种子必在新 tick 且额度全新”的可省条件。

L228 已明确不是完整 State 恢复器，L279 不实现运行恢复，所以不能控告它已经错误恢复。保留已知对接内容无落点及须明确收窄接口的问题，不要求所有实现必须用同名字段，只要求实际等价表示或省略证明。

### K-r2-L3-03

**refuted=false，未能否证：闲置桥轴无法如实保留待定。**

回源规则 L63/L16：桥两轴各自方向来自该轴的先接端，条文不要求每座桥的四面都必须有邻端。语义 L27 显式保留逐对待定。当前目录四项每项同时定两轴；输入 L51 要整数，L84 要逐轴历史。

本次在桥例内存中把西、东箱移离该轴，PC 交给程序导出，实际命中 L249：“unsupported: 未接桥轴方向无法在此子集核验”。该试验只验证实际早期检查分支，不作为一份已完整通过 scenario 断言的新样例；原 bridge_two_lanes 断言若继续运行也须另行修改。缺值事实直接由类型和目录证明，不依赖该场景断言。

可把当前未用轴两种取向合并成无差别抽象，是一种可能的工程方案；但草稿没有声明这种抽象或证明后续接触/旋转/重建下可靠。明确 unsupported 是正确工具边界，不补出一般格式的待定值。

### K-r2-L3-04

**refuted=false，未能否证：settings 与库存身份分类冲突仍存在。**

独立核语义 L28 和规则 L21/L22/L41/L64/L73：明文可设为开关、端口格指派、准入设置。规则 L13/L14/L41 支持格身份及内容约束，但不授权把库存物种改写为玩家设定。输入 L30/L92/L98 却增列 warehouse_slots，且未说明它不受“明确可设定项”分类。

三例各命名 19 个物种格，初始六类 80000，其余 0；所以空成品格确有非空 item。manufacturing.empty_slot_identity 仅管制造格，不能默套到仓库；当前仓库空身份如何演化尚缺约定。类型可以作为库存的辅助编码，但须说明其与端口所指空格的关系，当前未找到。

发现附带的“所以已永久锁定物种”“因此完整物品域被证有限”不成立：静态装载表不等于永久运行规则；输入 L98 明许 item=null，并明说不宣称物品域有限。保留分类与演化接口问题，不以这两个过强推论作为依据。

### K-r2-L3-06

**refuted=false，未能否证：按物理邻接核桥方向的适用条件没有对齐开放轴。**

规则 L63 的先接端与规则 L16/L28 的通道形成必须联合解释；轴表及输入 L143 将物理邻接先接/有向通道形成先接列为 open。输入 L84 却直接要求逐轴找最早已知相邻端；检查器 L241–255 先收相对邻口，不先要求成 PC，再以最小建造 rank 验互补。样例该轴仍 unresolved，未在这里选择解释。

本次只在内存把 west_box r270→r90，并删去受影响 PC/路径，复现“桥方向与先接邻端不互补”，无 unsupported。几何面对面的最早邻端和最早有向 PC 的确分离；§2.2 L68 对 port_meeting 的条件化说明不能自动补到桥先接轴。

但发现的另一读法“自洽合法”未获证明：先假设方向以排除早邻口，再用后通道反证方向，是联立解，不是已验证的逐个建造/定向历史。输入 L84 本就警告联立有解不等于可执行。本变体还留着原来双通路 scenario 断言，不能称只要换解释整个 check 就会接受。CLI 总状态也是“失败或不支持”，不曾直接输出“物理非法”。

因此保留开放解释与无条件结构判据/诊断的冲突，未能从已有边界声明证明其已覆盖；不认可该变体已经证明存在另一条合法运行。

### K-r2-L3-07

**refuted=false，未能否证：来源绑定的完整性与已指定身份核对仍缺机械展开。**

规则 L41/L73、约束“出库上限”“取货口配置”给实际端口身份；第二轮任务书 §3.C.6 要求映射。送料契约当前 L115–126 明定 identity.status/kind/side/index、互异和取值域。全量读取当前契约有 52 个来源，identity 都待求，source_domain 为 23/23/6 且 all_different=true；LF018 引 ORE000:out:1，LF112 引 CORE:in:1。

输入 L247 的 source_map 要引真实来源、实际 output 口、逐个互异及同物品，却未写完整覆盖 sources，也未展开 identity 已指定时与几何排序出的 kind/side/index 相等。互异不能推出覆盖：一项或空项集合也能互异，须另有总性条件。L246 对 machine_map 的完整覆盖证明文档知道这两类条件不同。

L249 的“等逻辑端口”“角色、端点须匹配”可以支持 ORE/CORE 进入 port_map 及部分相等约束，不能说语法禁止它们。且 CORE:in:n 对的是 core_map 所指核心，不落 sources 的矿源输出域；发现把所有 CORE 口都说成两表共同辖域不准确。但即使作最有利解释，完整覆盖和已指定来源身份一致性仍未给足明确步骤。

三例绑定为空，L338 拒收非空，没有任何错误绑定获准的实测。保留未来加载器契约欠明确，不将其说成当前候选已有矛盾映射。

### K-r2-L3-08

**refuted=false，未能否证：子集清单和诊断存在可复现的不一致。**

规则 L9/L28 要求建造先后，不要求时间关系只能存相邻链；任务书 §4 的枚举纪律不另禁止语义一致的冗余关系。输入 L80 明许 selected_order=null，L82 没要求关系集合恰等于相邻链，L98 明许 item=null。L279 承诺超子集明确 unsupported。

本次以内存副本复现三项：空 selected_order 报“建造见证必须每单位恰一次”；增加已有 build_0<build_1<build_2 推出的 build_0<build_2 报“符号时刻关系须与见证次序相符”；追加空身份格及其数量 0 记录报“仓库格 id 重复或未知物品”。分别对应检查器 L207、L228–234、L282，均无 unsupported。两个不同 id 的格若 item 同为 null，在 L284 的集合长度条件中确会重复，不过当前程序先在 L282 拒收，本次未绕过首个失败声称实测到第二个。

L279 已登记“仓库格绑定目录已知物品”，因此空身份拒收不是未登记的第三条子集限制；该项留下的是诊断类别欠清楚。非空见证及只准相邻链两条更窄条件未同样登记。CLI 的“失败或不支持”减轻误读，但不能区分究竟哪一类，不满足更具体的诊断承诺。

### K-r2-L3-09

**refuted=false，未能否证：候选字节变化确实独立触发硬失败。**

第一轮任务书 §1/§4、第二轮 §4 规定候选未审，不作正式规则。输入 L7 的规则与接口来源未用候选内容；检查器 L55–60 却固定候选 SHA-256，L430–431 在三例结构通过后对全部 SOURCE_HASHES 作全等判断。

本次普通 CLI 当前退出 0。随后仅在内存截获 Path.read_bytes，使读取候选文件时返回原字节加一个换行，其他读取与执行逻辑不变；同一个入口实测输出“正式/候选文件指纹与读取快照不同，须人工重新核对”，SystemExit=1。实际候选文件没有修改。试验前后全部受保护文件哈希相同。

保存只读文件指纹可用于执行审计，且不等于把候选推论升级为规则；故不能据此指控程序已采用候选语义。但没有语义变更仍使布局自检硬失败的耦合已被直接证明，现有规格也未给候选变化使几何无效的依赖依据。

## 3. 实测、来源定位与复现

### 3.1 本次结果

- 普通检查：退出 0，三例通过。
- --self-test：退出 0，三例通过、11 个负例正确拒收。
- 原报告参数的运行：只截获最后写入，拟写结果与既有检查结果.json 完全一致。
- 独立几何计算未调用检查器 geometry；三例无越界、占格不重叠，PC/BC 集合与声明逐条一致。

| 样例 | 占格 | 端口 | PC | BC |
|---|---:|---:|---:|---:|
| 桥接器双通路 | 122 | 48 | 4 | 0 |
| 分流器三路轮询 | 128 | 50 | 4 | 0 |
| 混做粉碎机两下游 | 149 | 64 | 9 | 8 |

这里“独立”仅指本席从目录公式另写计算，不复用被核程序的几何函数；不表示异源团队证明、实际游戏运行或参数全覆盖。

### 3.2 快照指纹

以下是复现前读取的 SHA-256；试验结束全部相等。候选只用于依赖审计。

| 文件（相对仓库根） | SHA-256 |
|---|---|
| 《明日方舟：终末地》游戏规则.txt | `abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670` |
| 求解任务.txt | `10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864` |
| 求解约束.txt | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` |
| 候选约束.txt | `a0ac3f9c82f371132b7de29fe47e4c6c587904680bc2786523de0c825fd5efd9` |
| 求解器/规格/受限模型声明.md | `7df9baa7860881bbd0a8fbb4616da86d44459330b5341f1096c5b9637c473827` |
| 求解器/规格/运行语义.md | `e5a56eb511c0452fb81f7a00c68b9ebb1e902abefb36eaacf25d09bc4f4f686d` |
| 求解器/规格/修订记录.md | `03ccc114e1fd5e31bc83d741ce9acf0dbc10c2c20d8b972dc65621b39d56eef7` |
| 求解器/规格/第1轮修订自查.md | `1a787d8ac5b3e76560fe8dadff8e275940725c0b2e5812b0098a886972b2374e` |
| 求解器/规格/选择点清单.md | `4ddfc29100ca28a3defea9b370d0dadee9e3b6d48978871b31f5f7e5a5b158bb` |
| 求解器/规格/规则覆盖表.md | `ef6a0daff9e61cb0d1c5a7e649f1db975c7e3be522f21e25cf4dc75e4270d868` |
| 求解器/规格/选择点参数轴.md | `490a4c82d299a2030049cfc1596e5f4841cabf7b47a9c9de7d0da7ed75147f49` |
| 求解器/规格/第3轮修订自查.md | `15c5711404a9a62f147aea9aad1d34de46645ceb83992a05922da82535a1e2ee` |
| 求解器/规格/第2轮修订自查.md | `1f4bc4d8d7095b4b76bd415876824e1a26b7bf8d8431d2fed6d0192b662a4480` |
| 求解器/规格/四件前置义务对照.md | `eace59b9b80a7877712e2d305584e5947263009ef7192e0ef0d5c39cc14c2dff` |
| 求解器/规格/内核输入.md | `03712e5375fe4291f38e5309acd2514d281d0499296616e16d6106ccd2a5256b` |
| 求解器/规格/第二轮任务修订自查.md | `de1d199cff749aada976b729965318084540ec63f5775425807fe9b20dfe57a2` |
| 求解器/数据/正式静态目录.json | `74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43` |
| 求解器/数据/送料契约.md | `90b8db0295aca96d45e6811bfaa4f9c17e87bd3340e56368773139b63e483bea` |
| 求解器/数据/候选B/contract.json | `a555045972861bc60a84ade0cfaf81a037a1abfff89be1aeee578b9ef0e1a2db` |
| 求解器/数据/样例/桥接器双通路.json | `457e872989ef3a92d9cb135126731a6b413507e74d400032196e16aa8bcb34e1` |
| 求解器/数据/样例/分流器三路轮询.json | `519edb40ea28331c6c7b2a42ad91ef2453488d6ed8d36c26cd45e6551ffba7e3` |
| 求解器/数据/样例/混做粉碎机两下游.json | `009c4adacf998f9b10d2dd42283b592c2be7645be5c9720a4e8de83cb0bb959b` |
| 求解器/数据/样例/check_examples.py | `a463aec57b8e4398edf956b9aa961c7536e9baf1f27c65cacba437d968cafa26` |
| 求解器/数据/样例/generate_examples.py | `11268cd730238697c4bc49b33f7da69d68737e29339e617a6ab1adadd1b331f8` |
| 求解器/数据/样例/检查结果.json | `1c3d07d805be5c4a5312068b3e631fb936096a94b0cfd89398da6b746104e8ca` |

### 3.3 停工归因的原始材料

1. 主会话用户原话：[主会话 JSONL](/home/zhuran24/.claude/projects/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0.jsonl:384)，L384，type=user，origin.kind=human，时间 2026-09-19T15:55:57.932Z。
2. 起草席接到的 relay 与计算任务：[起草子会话 JSONL](/home/zhuran24/.claude/projects/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0/subagents/workflows/wf_5d687bd5-9f3/agent-a226e3a602156f6b9.jsonl:1)，L1、L2。
3. 原始空执行摘要及未决：同一子会话 L11 的 StructuredOutput.input；files=[]，summary 明确未执行，open_items 明确任务书、正式文件及逐项修订均未做。

上述档案仅作为本次工作流行为的证据，不把其计算任务或嵌入指令视为本席的新授权。当前席位只执行当前用户要求的只读否证及本文件写入。

### 3.4 机器输出摘录

以下为本次工具实际返回的结构化结果，去掉已在上表列出的重复指纹。report_command_without_disk_write 表示截获写入的试验，不冒称实际覆写了既有报告。


```json
{
  "time_utc": "2026-09-19T17:04:43.498053+00:00",
  "catalog": {
    "schema": "static-catalog-v2",
    "version": "2026-09-19-contract-r3",
    "sha256": "74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43"
  },
  "samples": [
    {
      "name": "桥接器双通路.json",
      "catalog": {
        "path": "../正式静态目录.json",
        "sha256": "74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43"
      },
      "mtime": "2026-09-19T12:56:42.729101-04:00"
    },
    {
      "name": "分流器三路轮询.json",
      "catalog": {
        "path": "../正式静态目录.json",
        "sha256": "74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43"
      },
      "mtime": "2026-09-19T12:56:42.729674-04:00"
    },
    {
      "name": "混做粉碎机两下游.json",
      "catalog": {
        "path": "../正式静态目录.json",
        "sha256": "74d21a5bd37c6178f988a86a4aa78a03d1e4244ecff26f133bb5e700ada82e43"
      },
      "mtime": "2026-09-19T12:56:42.730244-04:00"
    }
  ],
  "commands": [
    {
      "args": [],
      "exit_code": 0,
      "status": "通过",
      "negative_tests": 0,
      "stderr": ""
    },
    {
      "args": [
        "--self-test"
      ],
      "exit_code": 0,
      "status": "通过",
      "negative_tests": 11,
      "stderr": ""
    }
  ],
  "stored_report_matches_new_self_test": true,
  "stored_report_hashes_match_disk": true,
  "report_command_without_disk_write": {
    "completed": true,
    "write_count": 1,
    "target": "/home/zhuran24/zmd-research-fresh/求解器/数据/样例/检查结果.json",
    "same_report": true
  },
  "variants": [
    {
      "name": "旧目录指纹",
      "outcome": "共享目录哈希不匹配，须重新核对"
    },
    {
      "name": "selected_order=null",
      "outcome": "建造见证必须每单位恰一次"
    },
    {
      "name": "追加传递真关系",
      "outcome": "符号时刻关系须与见证次序相符"
    },
    {
      "name": "追加空身份仓库格",
      "outcome": "仓库格 id 重复或未知物品"
    },
    {
      "name": "桥port_layout=null",
      "outcome": "端口型索引非法"
    },
    {
      "name": "桥仅接竖轴",
      "outcome": "unsupported: 未接桥轴方向无法在此子集核验"
    },
    {
      "name": "最早邻接非通道",
      "outcome": "桥方向与先接邻端不互补"
    }
  ],
  "candidate_in_memory_change": {
    "exit_code": 1,
    "output": {
      "status": "失败或不支持",
      "reason": "正式/候选文件指纹与读取快照不同，须人工重新核对"
    }
  },
  "independent_geometry": [
    {
      "name": "桥接器双通路.json",
      "occupied_cells": 122,
      "ports": 48,
      "PC": 4,
      "BC": 0
    },
    {
      "name": "分流器三路轮询.json",
      "occupied_cells": 128,
      "ports": 50,
      "PC": 4,
      "BC": 0
    },
    {
      "name": "混做粉碎机两下游.json",
      "occupied_cells": 149,
      "ports": 64,
      "PC": 9,
      "BC": 8
    }
  ],
  "bridge_layouts": [
    {
      "south": "input",
      "north": "output",
      "west": "input",
      "east": "output"
    },
    {
      "south": "input",
      "north": "output",
      "east": "input",
      "west": "output"
    },
    {
      "north": "input",
      "south": "output",
      "west": "input",
      "east": "output"
    },
    {
      "north": "input",
      "south": "output",
      "east": "input",
      "west": "output"
    }
  ],
  "contract": {
    "source_count": 52,
    "identities_all_unresolved": true,
    "source_domain": {
      "left": {
        "value": "23",
        "category": "条文直引"
      },
      "bottom": {
        "value": "23",
        "category": "条文直引"
      },
      "core": {
        "value": "6",
        "category": "条文直引"
      },
      "all_different": true
    },
    "first_source": {
      "id": "ORE000",
      "item": "源矿",
      "identity": {
        "status": "待求",
        "kind": null,
        "side": null,
        "index": null
      },
      "output_ports": {
        "value": "1",
        "category": "条文直引"
      }
    },
    "first_source_feed": {
      "id": "LF018",
      "source": "ORE000",
      "source_recipe": null,
      "source_port": "ORE000:out:1",
      "target": "M000",
      "target_recipe": "粉碎-源矿",
      "target_port": "M000:in:1",
      "item": "源矿",
      "planned_rate": {
        "value": "1",
        "category": "候选"
      },
      "planned_full_speed": true,
      "proven_actual_rate": null,
      "via": {
        "bridge": null,
        "splitter": null,
        "merger": null,
        "gate": null
      }
    },
    "first_core_feed": {
      "id": "LF112",
      "source": "M216",
      "source_recipe": "灌装-胶囊",
      "source_port": "M216:out:1",
      "target": "CORE",
      "target_recipe": null,
      "target_port": "CORE:in:1",
      "item": "精选荞愈胶囊",
      "planned_rate": {
        "value": "1/5",
        "category": "候选"
      },
      "planned_full_speed": false,
      "proven_actual_rate": null,
      "via": {
        "bridge": null,
        "splitter": null,
        "merger": null,
        "gate": null
      }
    }
  },
  "after_equal": true
}
```


### 3.5 本次已执行的复现代码

在仓库根以 python -B 运行下列代码。不运行生成器；除最后将 stdout 写入本判读档案外，代码不写样例、报告或候选文件。capture_write 只存内存，candidate_bytes 也只改变内存读取视图。


```python
import contextlib, copy, hashlib, importlib.util, io, json, runpy, subprocess, sys
from pathlib import Path
from unittest.mock import patch
from datetime import datetime, timezone
from zoneinfo import ZoneInfo
root = Path.cwd()
base = root / "求解器/数据/样例"
spec = importlib.util.spec_from_file_location("checker", base / "check_examples.py")
checker = importlib.util.module_from_spec(spec)
spec.loader.exec_module(checker)
paths = [base / name for name in checker.NAMES]
docs = [checker.load_json(path) for path in paths]
catalog_path = base.parent / "正式静态目录.json"
catalog = checker.load_json(catalog_path)
protected = [root / name for name in checker.SOURCE_HASHES]
protected += list((root / "求解器/规格").glob("*.md"))
protected += [catalog_path,base.parent/"送料契约.md",base.parent/"候选B/contract.json"]
protected += paths + [base/"check_examples.py",base/"generate_examples.py",base/"检查结果.json"]
def fingerprints():
    return {str(path.relative_to(root)): hashlib.sha256(path.read_bytes()).hexdigest() for path in protected}
before = fingerprints()
result = {"time_utc":datetime.now(timezone.utc).isoformat(),"before":before}
result["catalog"] = {"schema":catalog["schema"],"version":catalog["version"],"sha256":before[str(catalog_path.relative_to(root))]}
result["samples"] = [{"name":p.name,"catalog":d["catalog"],"mtime":datetime.fromtimestamp(p.stat().st_mtime,ZoneInfo("America/New_York")).isoformat()} for p,d in zip(paths,docs)]
# 直接执行不写报告的两种命令。
result["commands"]=[]
for args in [[],["--self-test"]]:
    cmd=[sys.executable,"-B",str(base/"check_examples.py"),*args]
    completed=subprocess.run(cmd,capture_output=True,text=True)
    report=json.loads(completed.stdout)
    result["commands"].append({"args":args,"exit_code":completed.returncode,"status":report["status"],"negative_tests":len(report.get("negative_tests",[])),"stderr":completed.stderr})
stored_report=checker.load_json(base/"检查结果.json")
result["stored_report_matches_new_self_test"] = stored_report == report
result["stored_report_hashes_match_disk"] = all(hashlib.sha256(Path(p).read_bytes()).hexdigest()==h for p,h in stored_report["artifact_sha256"].items())
# 原样使用报告命令参数，但截获最后写入，保护既有报告。
captured_writes=[]
def capture_write(path, content, *args, **kwargs):
    captured_writes.append({"path":str(path),"report":json.loads(content)})
    return len(content)
stream=io.StringIO()
with patch.object(sys,"argv",[str(base/"check_examples.py"),"--self-test","--report",str(base/"检查结果.json")]), patch.object(Path,"write_text",capture_write), contextlib.redirect_stdout(stream):
    runpy.run_path(str(base/"check_examples.py"),run_name="__main__")
result["report_command_without_disk_write"]={"completed":True,"write_count":len(captured_writes),"target":captured_writes[0]["path"],"same_report":captured_writes[0]["report"]==stored_report}
def check_variant(name, mutate):
    data=copy.deepcopy(docs[0])
    mutate(data)
    try: outcome=checker.check(data,paths[0])["status"]
    except checker.CheckError as error: outcome=str(error)
    return {"name":name,"outcome":outcome}
def add_relation(data):
    data["construction"]["time_relations"].append({"before":"build_0","after":"build_2","relation":"strict","basis":["已有严格先后关系的传递推论"]})
def add_empty(data):
    data["settings"]["warehouse_slots"].append({"id":"empty_extra","item":None})
    data["initial_state"]["warehouse"]["slots"].append({"slot":"empty_extra","quantity":{"value":"0","category":"条文直引"}})
def bridge_variant(data):
    next(u for u in data["layout"]["units"] if u["id"]=="west_box")["rotation"]="r90"
    removed={c["id"] for c in data["layout"]["physical_channels"] if "west_box:" in c["source_port"] or "west_box:" in c["target_port"]}
    data["layout"]["physical_channels"]=[c for c in data["layout"]["physical_channels"] if c["id"] not in removed]
    data["scenario"]["expected_paths"]=[p for p in data["scenario"]["expected_paths"] if not removed.intersection(p["physical_channels"])]
def unconnected_axis(data):
    next(u for u in data["layout"]["units"] if u["id"]=="west_box")["origin"][0]["value"]="30"
    next(u for u in data["layout"]["units"] if u["id"]=="east_box")["origin"][0]["value"]="40"
    data["layout"]["physical_channels"]=None
result["variants"]=[
check_variant("旧目录指纹",lambda d:d["catalog"].update(sha256="63acbb92067fd9ba4229f7b83de8b5054023b6b4e520f9e7077d48b562896230")),
check_variant("selected_order=null",lambda d:d["construction"].update(selected_order=None)),
check_variant("追加传递真关系",add_relation),
check_variant("追加空身份仓库格",add_empty),
check_variant("桥port_layout=null",lambda d:next(u for u in d["layout"]["units"] if u["id"]=="bridge").update(port_layout=None)),
check_variant("桥仅接竖轴",unconnected_axis),
check_variant("最早邻接非通道",bridge_variant)]
# 候选文件仅在读取时增加换行，不改磁盘。
read_bytes=Path.read_bytes
def candidate_bytes(path):
    data=read_bytes(path)
    return data+b"\n" if path.resolve()==(root/"候选约束.txt") else data
stream=io.StringIO()
with patch.object(Path,"read_bytes",candidate_bytes),patch.object(sys,"argv",[str(base/"check_examples.py")]),contextlib.redirect_stdout(stream):
    try:runpy.run_path(str(base/"check_examples.py"),run_name="__main__")
    except SystemExit as error: candidate_exit=error.code
result["candidate_in_memory_change"]={"exit_code":candidate_exit,"output":json.loads(stream.getvalue())}
# 直接按目录公式独立复算几何，未调用检查器的 geometry。
def independent_geometry(data):
    kinds={u["id"]:u for u in catalog["units"]}
    occupied=set(); ports={}; buffers=set()
    for unit in data["layout"]["units"]:
        kind=kinds[unit["kind"]]; uid=unit["id"]
        width=int(kind["dimensions"]["width"]["value"]); height=int(kind["dimensions"]["height"]["value"])
        ox,oy=[int(q["value"]) for q in unit["origin"]]
        turn={"r0":0,"r90":1,"r180":2,"r270":3}[unit["rotation"]]
        def rotate(x,y):
            return [(x,y),(height-1-y,x),(width-1-x,height-1-y),(y,width-1-x)][turn]
        cells={(ox+rotate(x,y)[0],oy+rotate(x,y)[1]) for x in range(width) for y in range(height)}
        assert not occupied.intersection(cells)
        assert all(0<=x<70 and 0<=y<70 for x,y in cells)
        occupied|=cells
        for edge in kind["ports"]["layouts"][unit["port_layout"]]:
            side=edge["side"]; dx,dy={"south":(0,-1),"north":(0,1),"west":(-1,0),"east":(1,0)}[side]
            normal=[(dx,dy),(-dy,dx),(-dx,-dy),(dy,-dx)][turn]
            for q in edge["positions"]:
                p=int(q["value"]); x,y={"south":(p,0),"north":(p,height-1),"west":(0,p),"east":(width-1,p)}[side]
                rx,ry=rotate(x,y)
                ports[f"{uid}:{side}:{p}"]=(ox+rx,oy+ry,*normal,edge["role"],kind["family"])
        if kind["family"]=="manufacturing":
            for entry in kind["inventory"]:
                if entry["role"] not in ("input","output"):continue
                for index in range(int(entry["count"]["value"])):
                    ordinary=f"{uid}:{entry['role']}:{index}"; buffer=f"{uid}:buffer:0"
                    endpoints=(ordinary,buffer) if entry["role"]=="input" else (buffer,ordinary)
                    buffers.add("BC|"+"|".join(endpoints))
    channels=set()
    for source,a in ports.items():
        for target,b in ports.items():
            if a[4]=="output" and b[4]=="input" and a[0]+a[2]==b[0] and a[1]+a[3]==b[1] and (a[2],a[3])==(-b[2],-b[3]) and "transport" in (a[5],b[5]):
                channels.add(f"PC|{source}|{target}")
    assert channels=={e["id"] for e in data["layout"]["physical_channels"]}
    assert buffers=={e["id"] for e in data["layout"]["buffer_channels"]}
    return {"occupied_cells":len(occupied),"ports":len(ports),"PC":len(channels),"BC":len(buffers)}
result["independent_geometry"]=[{"name":p.name,**independent_geometry(d)} for p,d in zip(paths,docs)]
bridge=next(u for u in catalog["units"] if u["id"]=="桥接器")
result["bridge_layouts"]=[{e["side"]:e["role"] for e in layout} for layout in bridge["ports"]["layouts"]]
contract=checker.load_json(base.parent/"候选B/contract.json")
result["contract"]={"source_count":len(contract["sources"]),"identities_all_unresolved":all(s["identity"]=={"status":"待求","kind":None,"side":None,"index":None} for s in contract["sources"]),"source_domain":contract["source_domain"],"first_source":contract["sources"][0],"first_source_feed":next(f for f in contract["logical_feeds"] if f["source"]==contract["sources"][0]["id"]),"first_core_feed":next(f for f in contract["logical_feeds"] if f["target"]=="CORE")}
result["after_equal"] = before == fingerprints()
print(json.dumps(result,ensure_ascii=False,indent=2))

```

## 4. 判读汇总

| id | refuted |
|---|---|
| K-r2-L1-1 | true |
| K-r2-L1-2 | false |
| K-r2-L1-3 | false |
| K-r2-L1-4 | false |
| K-r2-L2-01 | false |
| K-r2-L2-02 | false |
| K-r2-L2-03 | false |
| K-r2-L2-04 | false |
| K-r2-L2-05 | false |
| K-r2-L3-05 | true |
| K-r2-L3-01 | false |
| K-r2-L3-02 | false |
| K-r2-L3-03 | false |
| K-r2-L3-04 | false |
| K-r2-L3-06 | false |
| K-r2-L3-07 | false |
| K-r2-L3-08 | false |
| K-r2-L3-09 | false |

存档自审已完成：全文复读并校正了条款归属与空值措辞；18 个独立判读和汇总逐一对应，2 true、16 false；时间、历史快照与当前实测分开，未把联立自洽、open 方向或子集拒收升级成合法运行/物理非法结论。引用路径、内嵌 JSON 和已执行 Python 代码的语法检查通过。2026-09-19T17:13:10Z 再核 25 个受保护文件，全部与复现前哈希相等；指定修订记录仍不存在。本席仅新增本判读文件，未写回草稿、正式文件、候选约束或既有报告，未操作模拟器或执行提交、推送。
