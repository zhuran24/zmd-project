# 内核输入格式第一轮否证：第 2 席

日期：2026-09-19。性质：本轮读取快照的判读档案；GPT 源，经 Codex 转发。结论：15 条发现中，2 条已否证，13 条未能否证。未能否证不表示认可发现中的每个附带推断；各节分别限定存活的缺口。

产物路径：`/home/zhuran24/zmd-research-fresh/求解器/规格/复核/内核输入/否证-r1-2.md`。

## 读取依据与判读边界

完整读取的两轮任务书位于 `/tmp/claude-1000/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0/scratchpad/step1/任务书.md` 与同目录 `任务书2.md`。第一轮 §1、§4 要求从三份正式文件推规则、未定不能直接授权取值；第二轮 §3.C 要求表示建造、离线、初态、调试和契约映射，§4 给出否证标准。第二轮 §2 已定的 T7 分配读法、T6 单位级判定及现行“仓库外”计数域均作为当前依据，未使用旧档案中的相反说法。

本文的“规则”“任务”“约束”分别指仓库根的三份正式文件；“输入”指 `求解器/规格/内核输入.md`。同时回读了运行语义、选择点清单、选择点参数轴、受限模型声明、完整性批评、送料契约、目录、候选 B 契约及三个样例和检查器。规则依据与工程文件的要求分开判断。候选约束仅用于核对检查器的文件依赖，不作为游戏规则。

读取快照的 SHA-256：

| 文件（相对仓库根） | SHA-256 |
|---|---|
| 《明日方舟：终末地》游戏规则.txt | `abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670` |
| 求解任务.txt | `10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864` |
| 求解约束.txt | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` |
| 候选约束.txt | `a0ac3f9c82f371132b7de29fe47e4c6c587904680bc2786523de0c825fd5efd9` |
| 求解器/规格/内核输入.md | `03712e5375fe4291f38e5309acd2514d281d0499296616e16d6106ccd2a5256b` |
| 求解器/规格/运行语义.md | `e5a56eb511c0452fb81f7a00c68b9ebb1e902abefb36eaacf25d09bc4f4f686d` |
| 求解器/规格/选择点参数轴.md | `490a4c82d299a2030049cfc1596e5f4841cabf7b47a9c9de7d0da7ed75147f49` |
| 求解器/规格/选择点清单.md | `4ddfc29100ca28a3defea9b370d0dadee9e3b6d48978871b31f5f7e5a5b158bb` |
| 求解器/规格/受限模型声明.md | `7df9baa7860881bbd0a8fbb4616da86d44459330b5341f1096c5b9637c473827` |
| 求解器/数据/正式静态目录.json | `63acbb92067fd9ba4229f7b83de8b5054023b6b4e520f9e7077d48b562896230` |
| 求解器/数据/送料契约.md | `138ba7f15059de5bbe49615fa466880db3a0a415f7c054c024e7347d08635922` |
| 求解器/数据/候选B/contract.json | `d0c887c73674a58424d05257f002953a2ee4d0b888614d52cc0b2b60ebd58ff2` |
| 求解器/数据/样例/check_examples.py | `a463aec57b8e4398edf956b9aa961c7536e9baf1f27c65cacba437d968cafa26` |
| 求解器/数据/样例/桥接器双通路.json | `def32415eeedee63a3f02c10f255355b507d14c0b19ff0950fc202ecfc528c3e` |
| 求解器/数据/样例/分流器三路轮询.json | `b951dee66c6c29f0e6bb483b0c771aad02ac276f134c219dc240b03628e8a6bc` |
| 求解器/数据/样例/混做粉碎机两下游.json | `45dfd5f8dd7aca0a5e2dea0c932ed5a056041b3ee25c603fcf148f61d4f936e3` |

## K-r1-L1-1

**refuted=false。未能否证：重建接通记录与当前接通序的接口没有闭合。**

规则 L10 明定重建为拆除后再建、通道重新接通；L28 把接通联系到较晚建成时刻，L29/L31/L32 再使用接通先后决定轮询和分级。任务 L11 允许调试期由玩家完成部分工作，第二轮任务书 §3.C.2、C.5 分别要求建造史和调试序列。

输入 L47 已写“重建保留设计实例 id，建造事件另有 id”，L70 要求重建接通事件另存 id。但 L80–82 的 construction 是一次蓝图，非空 selected_order 每实例一次；L209 的 connection.order 固定派生标签为 `construction_then_offline_events`。L232–234 的 rebuild 有操作 id、`when.after` 和不精确时机，故发现中“没有任何事件可挂”不能理解成连操作身份也不存在。然而没有规定操作 id 与再建成事件、重新接通事件的对应关系，也没有把它们接入该派生标签所指的历史。`initialization.rebuild_inventory` 仅管库存后效；任意 Decision.value 不能靠读者自行发明语法补齐，输入 L17 明确不把任意字符串当执行函数。

因此“必须记录的新事件没有明确编码及合并规则”存活。现有检查器 L330 拒收全部非空调试，不能称已实测出错误轮询；“会算出旧次序”只是在忽略缺口继续实现时的风险。同理，规则 L10 本身没有充分授权重建一定能搬位置或拆后永久不建，不以这两项附带主张作为本条成立的依据。

## K-r1-L1-2

**refuted=false。未能否证：离线方向后效虽已登记，方向状态和边表接续的表示仍缺定义。**

规则 L63 把桥接器每对边的角色交给先接邻端；任务 L13 只明写接通先后可能改变。运行语义 §3.1 明说是否追溯改变已成方向未写明，不得自行重定向。因此不能以任务 L13 推出“离线必翻向”。输入 L179 的 `offline.direction_effect` 确实保留该争点，否定了“草稿完全没意识到此问题”的强说法。

但输入 L38/L51 要求单个非空 port_layout 索引，目录四个布局都是两轴已定角色；L68/L70 的 PC 导出依赖存取角色。L86 的离线 effects 只引用轴，没有定义已审方向后效如何更新哪一轴、何时切换 PC 集合，以及 new_connection_order 覆盖切换前还是切换后的边。既没有这套接续编码，也没有证明所有合法离线后效都保持角色，故现有轴名不足以否证表示缺口。

须纠正发现的 ID 推断：输入 L60 的端口 id 只含实例、规范边、位置，不含存取角色。固定 rotation 改 port_layout 不会改变桥接器四个端口 id；实测 layout0→layout1 时 id 集合不变，只有东西口角色互换。PC 的有向端点顺序及存在集合可能改变，不能说端口 id 和全部 PC id 必然一起改变。此纠正未补齐动态方向与边表的缺口。

## K-r1-L1-3

**refuted=false。未能否证：仓库格物种被归入明文设定，且空格身份演化没有对应轴。**

规则 L13 约束格内单种及单位内同种单格，L14 定义仓库，均未授权玩家改格的物种。L41/L73 的“可自行设定”修饰取货端口连接哪一格；L22 只赋予已授权设定随时更改的能力。任务 L6 的六类初始满也不授权预先设定其他空格的永久物种。

输入 L30 把 settings 定义成“明确可设定项”，L92/L98 却放入 `{id,item}`；§6 的仓库初态只有 `{slot,quantity}`。§5 的制造空格身份轴有明确 manufacturing 辖域，不能无文据套到仓库；八条 warehouse 轴也没有仓库空格保留身份或新物种选格关系。三个样例在 quantity=0 时仍命名成品格物种，检查器只验证该静态表，不能证明其运行身份永久不变。

“仓库格数量不限、同种至多一格”允许按物种组织当前库存，但不单独证明可被端口长期引用的空格身份可以无损等同于物种。约束“矿系不入库”L66 的植物入库禁令还有种植机恰 32 台的前件，不能用它删掉一般仓库演化问题。存活的是归类与动态接口缺口，不是已经证明玩家实际能够用现有检查器改物种。

## K-r1-L1-4

**refuted=true。已否证：实例局部引用不要求同一物理布局只有一种编码。**

规则 L11 允许旋转；规则 L41/L63/L76 确实存在发现所述几何对称性。但规则和两轮任务书都未要求按物理布局的旋转对称商空间产生全局唯一 id。输入 L47 先由调用者命名实例，L60 明确定义 id 使用规范边而非世界边，L70 再从这些身份生成 PC；“旋转后 id 不改”说的是跟随实例的规范端口身份，不是同一世界格边在所有等价重编码中同名。

发现列出的 scenario.expected_paths、离线事件、feed_map 都属于同一输入对象，指向该对象的 PC 命名空间。契约原文件只有 LF 和逻辑端口，不含外部 PC 表；输入 §7 把具体绑定放在本地 contract_binding 中，并锁定契约字节。它没有承诺另一个任意重编码布局可以不改引用就复用这些 PC id。

实际用桥接器样例的内存副本把 `(r0,layout0)` 改成 `(r180,layout3)`，按相同世界格边和角色建立端口双射，并同步替换 PC 表与 expected_paths 后，检查器仍返回“静态结构通过”。四个桥口的对应为 south↔north、west↔east；世界端口集合完全相同。这是有效的别名重编码，不是悬空或歧义引用。供电桩无端口，更不能由四向同形推出 PC 引用冲突。规范化可用于去重，但不是现有引用格式成立的必要条件。本结论不否证桥接器待定方向或动态后效的另两类缺口。

## K-r1-L2-1

**refuted=false。未能否证：warehouse_slots.item 与 settings 的声明冲突。**

独立回读规则 L21/L22/L41/L64/L73：明确可设的是开关、端口到仓库格的连线、准入口身份和限额，没有格到物种的玩家设定。规则 L13 是容纳限制，不能作为新设定的正面来源；任务 L8 的零干预例外也仅是成品拿取。

输入 L30/L92/L98 的组合会把格物种纳入“明确可设定项”，而运行语义 L28 的 settings 清单没有这一项。检查器 L280–284 还要求每条 item 属于已知物品；对样例中 quantity=0 的电池格改为文档允许的 item=null，实测拒绝“仓库格 id 重复或未知物品”。不过此检查仅证明静态子集拒收空身份，不足以证明内核已实现永久物种绑定。即使把 warehouse_slots 辩解成符号表，也须在正文区分它与玩家设定并说明空身份后效；现文没有这个区分，不能否证发现。

## K-r1-L2-2

**refuted=false。未能否证：必填单个 layout 不能记录桥接器逐轴待定状态。**

规则 L63 的角色必须由先接邻端导出，规则 L9/L28 与任务 L13 限制建造和接通历史；约束“不得依赖的量”要求覆盖合法的取值，不能手选一个桥方向代替覆盖。运行语义 L27 已明确列出“每对端口的已定/待定方向”。

输入 L51 把 layout 称作预期导出结果、L84 保留平局和桥间互依赖，确实否定“玩家可任设”的读法；然而 L38 要求它非空，目录只给两个方向均已定的四个索引，既无逐轴 unresolved，也无由历史派生的方向对象。把 connection.bridge_tie 等轴设 unresolved，不能把这个已经填死的布局字段改成未定。

本条不能仅由“一个样例填一个值”推出量化缩域：样例完全可以给某条历史的见证。存活的理由是一般格式尚不能显式记录未定轴，与运行语义接口不一致。相邻桥循环是否有合法可执行历史仍待核，不以其必然合法为论据。

## K-r1-L2-3

**refuted=false。未能否证：另存重新接通事件的要求未落实为类型及时间关系。**

规则 L10/L28/L29/L31/L32 要求重建影响接通记账及由它派生的次序；第二轮任务书 §3.C.2、C.5 要求两类历史对接。输入 L70 的“重建后的接通事件必须另存事件 id”是明确工程义务。construction 的 mode 是 blueprint_once，L80–82 将其限定为首次建造；检查器 L207/L216/L220 也按一次完整排列验证。debug_operations 的 id 标识操作，when.after 标识操作先后，未定义再建成时刻或事件到 PC 重新接通的映射。

因此不能把新的重建事件塞进 moments 而仍声称符合这套已定义的单次蓝图语法，也不能直接把操作 id 当作 L70 所要求的接通事件 id。effects 是未完成语义的 Decision，不是已有记录类型。发现“全文没有任何事件及时间关系”若泛指操作关系则过强，但它指出的重新接通事件接口缺失成立，未能否证。实测程序拒收非空调试，未实测到运行错误。

## K-r1-L2-4

**refuted=false。未能否证：离线计数与窗口接续未映射到具名轴。**

规则 L64 同时规定累计上限、窗口收件上限、首件起窗和窗口结束后的重启；约束“准入累计”L62 约束循环通过量。运行语义 §3.1（L116）明确列出库存/计数/进度的离线后效不能由沉默裁定；选择点 T10 亦明确含累计数。

逐条核输入 L164–179 与共享轴表：gate.counter_edit 的触发是改身份/额度；gate.counter_start 是设限前后记账；gate.window_clock 是阻断时走时钟；三个 recovery 轴处理阻断恢复。offline 六轴分别处理次序域、事件族、指针、库存、制造/冷却进度、桥方向。它们没有规定离线前后的 total_received、window_received、window_started_at 如何关联。运行语义 L30 的 progress 也未把准入口计数划进去，不能把 offline.progress_effect 自行扩成全部历史状态。

输入 L225 的 gate_counters 只给初态当前值，L86 的 effects 又要求引用接续轴；初值不是转移关系。故“依赖清单包括 §3.1”的承诺有漏项。但“等于默认保持”不是文档现成结论：输入 L213 明令未知后效报 unresolved/unsupported。存活的是不能显式记录该接续机制，不是授权清零或证明默认保持。

## K-r1-L2-5

**refuted=true。已否证：target 可以指所属单位，payload 已能指具体端口。**

规则 L41/L73 授权改端口连接的仓库格，L22 允许调试期更改；任务 L11/L12 允许不精确调试操作。输入 L99 已定义授权记录 `warehouse_assignments[]={port,slot}`，L232 规定 assign_warehouse_slot 的 payload 使用 §4 相应记录。target 的单位身份和 payload 内的端口身份可以同时存在，规则或草稿没有要求所有寻址信息必须挤在 target 一个字段中。

例如三个样例都实际含有以下单位、端口和槽位，故操作主体可以是 core，具体被改连线可以是 core:west:1：

```json
{
  "id": "assign_core_west",
  "action": "assign_warehouse_slot",
  "target": "core",
  "payload": {"port": "core:west:1", "slot": "warehouse_13"},
  "when": {
    "after": null,
    "condition": {"status": "unresolved", "value": null, "basis": ["任务调试期：条件待审"]},
    "timing": "imprecise"
  },
  "effects": {"status": "unresolved", "value": null, "basis": ["规则 L41、L22；调试后效待审"]}
}
```

warehouse_13 在样例中对应蓝铁矿。此表示符合“target 指单位”和“payload 是相应记录”，精确指名端口而不精确定时，也没有更改 warehouse_slots 的物种。发现的二分“要么新增物种设定、要么无法指端口”因这份已有字段组成的表示而不成立。可建议把动作与记录的对应写成显式表，但不能据 target 不接受端口就说动作不可表示。检查器不支持任何非空调试程序，故此处只证字段表达能力，不称该程序已通过执行校验。warehouse_slots 的归类缺口仍由其它独立发现保留。

## K-r1-L3-01

**refuted=false。未能否证：已有运行上下文的两个字段未在 StateSeed 中落地。**

规则 L25 要求同刻逐个判定，约束“端口速率”L30 要求每端口每 tick 至多一件。运行语义 L23/L33 明列 semantic_context 中的未决事件日程和 tick 内已发生移动记录；受限模型声明 L19 的重复判据还要求本刻端口额度。第二轮任务书 §3.C.4、C.5 要求参数与初态的对接。

输入 §5 的 judgment.order/time.retry_schedule 等可承接排序或日程机制，不等于当前进行到哪一步、哪些端口本 tick 已经用额的运行记录。L219–226 的 StateSeed 只有 inventory/progress/logistics/environment；entered_at 能核滞留，但不是逐端口成功记录；gate_counters 也仅管准入口。全文没有这两个运行内容的独立字段或可重建它们的完整执行历史格式。

L228 已声明 StateSeed 不是完整恢复器，因此不能把遗漏夸大成“程序伪称恢复了完整状态”；然而这也没有把已知的两个接口内容表示出来。调试结束不是用户可以精确指定的时刻（L234/L238），尚不能以“所有种子状态都在新 tick、预算必然全新”消除此缺口。也未据这些局部字段证明两项可唯一导出，故未能否证。

## K-r1-L3-02

**refuted=false。未能否证：只接一轴时另一轴的待定身份没有编码。**

规则 L63 要求两轴独立拥有格，并把角色交给各自先接邻端；没有规定另一轴无邻端时采用默认方向。规则没有要求桥两轴都使用，约束“满速独占”也明确不把某轴的满速前件扩到另一轴。运行语义 L27 要求逐轴已定/待定方向。

目录四个 layout 确实分别枚举竖向两种×横向两种，全部已定。输入 L51 必填索引，L84 要用先接历史核验每轴；未接轴既没有先接见证，也没有 unresolved 位置。检查器 L249 如实报 `unsupported: 未接桥轴方向无法在此子集核验`，这是正确声明检查边界，不能反过来充当一般格式已经能记录待定方向的证据。

即使另一轴暂时无物流、两种填法在当前静态图上可能观测等价，也没有给出允许将运行语义的待定状态合并成任意已定方向的证明，尤其还存在调试后续接通。故未能否证。

## K-r1-L3-03

**refuted=false。未能否证：运行语义和输入对 settings 的划分不一致。**

独立核对运行语义 L28：只有需电开关、取货端口到仓库格的指派、准入口身份和限额，性质为明文可设定项。输入 L30 同样这样定义 settings，但 L92/L98 增加格到物种的记录。规则 L13/L14/L41 支持仓库库存和格结构，不能推导随时改格物种的玩家权限。

静态的物种符号表本身可以是合法工程辅助数据，因此不能只凭字段存在就证明已经新增可执行动作；真正矛盾在于把它与 L22 所管的设定放入同一被明确命名的类别，并且没有交代空格被端口引用后的身份演化。样例电池、胶囊格数量均为零但 item 非空，与 manufacturing.empty_slot_identity 的未定语义也未建立任何仓库侧对应。未找到足以把这份表解释成纯初态/运行导出信息的明确条款，故未能否证。

## K-r1-L3-04

**refuted=false。未能否证：来源与逻辑端口两张映射的闭合核验条件不够明确。**

送料契约“逻辑送料记录表”明定 source/target 可以引用机器、来源变量或核心；“来源身份”要求 ORE 对应真实仓库取货口或核心输出。回读 contract.json 得 schema=feeding-v2、219 台机器、52 个来源、315 条 LF（文件计数）。LF018 的首口为 ORE000:out:1，LF112 的末口为 CORE:in:1，均不止 M 型端口。规则 L41/L73 和约束“取货口配置”要求真实逐口身份，任务书 §3.C.6 要求映射对接。

输入 L249 的“等逻辑端口”不能读成排除 ORE/CORE；L250 的“路径首尾口对应 port_map”也为把这些端口纳入提供了依据。这可以缩小发现，但 source_map L247 没有明确覆盖全部 sources，也没有写 `port_map(ORE000:out:1)=source_map(ORE000)`。相比 L246 机器映射明确全覆盖，来源只写“逐个互异”。当 source 身份仍为待求而两个映射选择不同的同物种 output 端口时，仅“角色、端点须匹配”中的虚拟端点归属尚不足以给出明确的判错步骤；读取者必须自行补上这条交换关系。

当前三例 binding 均为 null，检查器 L338 拒收非空，因此没有已证被程序放过的错误绑定，也没有候选 B 物理不可行结论。存活的是未来加载器接口欠明确，未能给出草稿已经完整覆盖来源映射及其一致性的证据。

## K-r1-L3-05

**refuted=false。未能否证：最小子集说明与拒收诊断确有遗漏。**

第二轮任务书 §3.C.7 要求最小合法性样例；第一轮 §4 的枚举默认“包括”。输入 L80 允许 selected_order=null；L82 的 time_relations 未要求必须且仅有相邻链；L98 允许空且未指派种类的 item=null。输入 L279 允许检查器仅支持子集，但承诺超界明确报 unsupported，并列出若干附加限制。

以桥接器样例的独立内存副本分别测试，结果如下：

| 改动 | 检查器结果 |
|---|---|
| selected_order=null，保留其余历史 | `建造见证必须每单位恰一次` |
| 给已有 build_0<build_1<build_2 链增加 build_0<build_2 真关系 | `符号时刻关系须与见证次序相符` |
| 数量为零的 warehouse_18 的 item 改为 null | `仓库格 id 重复或未知物品` |

前三处对应程序 L207、L228–234、L282，均无 unsupported 前缀。末项已在 L279 作为子集限制说明，但诊断混合了重复 id/未知物品；前两项的更窄条件未明确列出。CLI 总标题“失败或不支持”会减轻误读，却不区分究竟哪类，也达不到 L279 的明确承诺。检查器无需在此轮实现一般求解，但应准确报告未支持范围；本条因此未能否证。

## K-r1-L3-06

**refuted=false。未能否证：候选文件确为无语义依赖的硬失败条件。**

两轮任务书都写候选不是正式约束。输入 L7 的规则来源、目录与接口没有引入候选约束内容。程序 L55–60 却把候选文件列入固定 SOURCE_HASHES，L430–431 在三例结构检查后对整个字典作相等判断；候选文件只参与读字节、哈希及报告，没有参与几何或轴检查。

原始快照下 `python -B 求解器/数据/样例/check_examples.py --self-test` 退出 0，三例通过，11 项负例正确拒绝。随后仅在 Python 内存中替换 Path.read_bytes：对候选文件返回原字节加一个换行，所有磁盘文件保持不变；运行同一个 main() 即抛出 `正式/候选文件指纹与读取快照不同，须人工重新核对`。程序入口 L461–463 会将此异常转为退出 1。由此复现了“不改任何相关语义、仅候选字节变化仍使复现失败”的因果链。

保存候选指纹可以用于保护只读源或记录来源，不等于把候选升级成正式规则；因此发现不能据此声称程序已采用候选推导。但跨次执行无条件钉死无关候选字节确会阻断结构复现，现文没有这项有效性依赖的理由，故未能否证。此测试未实际追加或改写候选文件。

## 工具实测的复现方法与范围

以下测试均可在仓库根以 Python 标准库重放。`-B` 禁止产生 pyc；不传 `--report`，不运行生成器。计数及返回字符串是本轮工具实测，几何等价是对两份候选编码的算术核验，均不是游戏运行认证。

```bash
python -B 求解器/数据/样例/check_examples.py --self-test
```

内存变体的最小复现如下；它只读草稿和样例，不向磁盘写变体。

```python
import copy
import runpy
from pathlib import Path

root = Path.cwd()
module = runpy.run_path(str(root / "求解器/数据/样例/check_examples.py"))
path = module["BASE"] / "桥接器双通路.json"
original = module["load_json"](path)

def check_variant(label, data):
    try:
        result = module["check"](data, path)["status"]
    except module["CheckError"] as error:
        result = str(error)
    print(label, result)

data = copy.deepcopy(original)
data["construction"]["selected_order"] = None
check_variant("null_order", data)

data = copy.deepcopy(original)
data["construction"]["time_relations"].append({
    "before": "build_0", "after": "build_2", "relation": "strict",
    "basis": ["已有相邻链的传递推论"]
})
check_variant("extra_relation", data)

data = copy.deepcopy(original)
next(row for row in data["settings"]["warehouse_slots"]
     if row["id"] == "warehouse_18")["item"] = None
check_variant("empty_slot", data)

# 只模拟候选字节变化，恢复方法后磁盘文件仍为原字节。
original_read = Path.read_bytes
candidate = root / "候选约束.txt"
def mock_read(path):
    value = original_read(path)
    return value + b"\n" if path == candidate else value

try:
    Path.read_bytes = mock_read
    fingerprints = {
        name: module["hashlib"].sha256((root / name).read_bytes()).hexdigest()
        for name in module["SOURCE_HASHES"]
    }
    module["require"](
        fingerprints == module["SOURCE_HASHES"],
        "正式/候选文件指纹与读取快照不同，须人工重新核对"
    )
finally:
    Path.read_bytes = original_read
```

桥接器重编码测试使用相同原始样例，调用检查器 geometry() 分别导出两份端口表，以 `(unit,cell,normal,role,axis)` 为键建立旧、新 id 双射；将桥改为 r180/layout3 后，用该双射更新全部 PC 及 expected_paths。两份世界端口签名集合相等，更新后 check() 通过。单独保持 r0、把 layout0 改成 layout1 的几何投影显示端口 id 集合不变，west/east 的 role 改变；这一步只用于核验 id 组成，不声称改变角色后的历史合法。

本档案只新增本文件；没有修改被审草稿、正式文件、候选约束或样例，没有操作模拟器，也没有提交或推送。读者自审核对了 15 个独立判定、原文行号、两条否证的作用范围和实测与推论的区别。未定后效仍须联合规则审查，13 个未能否证结论不授权任何新机制。
