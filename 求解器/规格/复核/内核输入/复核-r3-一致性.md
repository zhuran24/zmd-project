# 内核输入第三轮一致性复核

日期：2026-09-19。性质：第三轮一致性复核与复算证据。结论：发现 3 项重要问题、1 项次要问题；未发现原始三个样例的占格、端口相遇或交付黄金前缀的物理状态错误。发现集中在运行输入验证、运行记录验证及来源指纹。有限前缀通过不表示实际混做、下游生产或全部可达循环已认证。

## 1. 依据、对象与复现

本次完整读取 `/tmp/claude-1000/-home-zhuran24-zmd-research-fresh/2a1af1b1-ede2-4b4a-8f95-41e78df48af0/scratchpad/step1/` 内的《任务书3.md》《任务书.md》《任务书2.md》。第三轮第 0 节的“下次”是本轮复核分工调整，未作为跳过任务的理由。游戏行为只以仓库根三份正式文件判断；主席已定的最严读法、窗口恢复排除、空格身份及传送带形状按任务书执行。候选约束、模拟器和历史批评不作为规则来源。

复核对象包括任务列举的输入/输出规格、schema、修订记录、共享轴修改请求、三个样例、参数投影、生成/检查/参考执行/回归脚本、黄金轨迹、运行记录及 r3 验证目录全部列名产物。对照文件包括运行语义、受限模型声明、受限转移定义、内核配置、选择点参数轴、参数轴请求答复、正式静态目录和送料契约。所有被列 JSON 均完整解析；schema 另通过重建后的完整 JSON 相等比较核对。大指纹清单只用来核其声明和条目结构，不将原席“25037 个保护文件未改”的历史主张冒称本席全量历史审计结果。

本报告及全部新增证据均位于 `求解器/规格/复核/内核输入/`。未修改被复核文件、三份正式文件、候选约束或模拟器；未执行 git commit/push。原脚本有固定输出路径，因此用内存拦截 `Path.write_text` 保存重算结果，再与原文件比较，没有直接执行会覆盖交付物的 `run_checks.py`。

复现入口（在仓库根）：

```bash
python -B 求解器/规格/复核/内核输入/r3-一致性证据/复算与反例.py
```

入口执行独立几何及事件重放、原生成器/黄金重算/17 项回归/schema 生成器的只读重跑、19 项既有负例和 8 组表示回归，并生成三类反例。反例文件保留原始相对依赖路径，变更仅限各反例列明的字段，脚本因此以原输入路径调用检查接口；不应直接交给按副本路径解析依赖的 CLI。

主要证据：

- [复算与反例.py](r3-一致性证据/复算与反例.py)：独立核验与反例生成代码。
- [独立几何.json](r3-一致性证据/独立几何.json)：逐实例完整占格、全部 PC/BC、正面积供电。
- [独立轨迹核对.json](r3-一致性证据/独立轨迹核对.json)：逐时刻事件及状态核对。
- [逐轴对照.json](r3-一致性证据/逐轴对照.json)：96 项的字段名、分组、处置、具体值及参数投影逐项对照。
- [原程序重跑.json](r3-一致性证据/原程序重跑.json)、[原检查入口.log](r3-一致性证据/原检查入口.log)：已有检查的复现结果。
- [反例检查结果.json](r3-一致性证据/反例检查结果.json)：本席额外反例的实际结果。
- [正式目录核对.json](r3-一致性证据/正式目录核对.json)：三份正式源指纹及 18 条配方独立解析核对。

复核期间检测到运行记录与共享送料契约字节发生外部变化，详见[复核期间源文件变化.json](r3-一致性证据/复核期间源文件变化.json)。未把这一现象归责为被审代码缺陷，也未恢复或覆盖它们。运行记录最终读取内容另存[被核运行记录.json](r3-一致性证据/被核运行记录.json)，并与只读重新生成结果做完整 JSON 相等比较；送料契约当前来源身份和三种边章节已再次核对。发现 04 的两端指纹在复核起点已不相等，不由本次观测到的这两文件变化引起。

## 2. 发现

### K3-r3-L3-01：运行状态 Decision 外壳漏验

- **where**：`/home/zhuran24/zmd-research-fresh/求解器/数据/样例/runtime_example.py:177–187`；关联 `/home/zhuran24/zmd-research-fresh/求解器/数据/样例/check_examples.py:89–98`。
- **claim**：运行检查只读取若干 StateSeed 决策的 value，未验证其 status、必需字段和 basis，导致未解或格式非法的运行上下文被接受并实际运行。
- **severity**：重要。
- **evidence**：第三轮任务书 §3.C 要求“StateSeed 完整”；内核输入 §1.1（第 17 行）规定 `unresolved` 的 `value=null`，basis 非空；§5.2、§6.3（第 252、310 行）要求恢复排序上下文、并在恢复数据与计时/预算等冲突时拒收。规则 L25“同一时刻的多个判定逐个进行”、任务 L14“同一时刻多个判定的先后固定”使排序上下文是执行条件，不能忽略其未解状态。

复现：复制交付 v3 输入，仅把 `StateSeed.semantic_context.judgment_context.status` 从 `specified` 改成 `unresolved`，保留原 value，原 `checker.check` 仍返回“完整运行输入检查通过（有限合成子集）”，`check_golden_trace.run` 继续完成四个时刻。其依据不是允许执行 unresolved，而是第 185 行只比较 value，未调用已有的 `checker.decision`。

同一漏验路径还能放过：tick_context.status=unresolved、withdrawal_memory.status=unknown、删去 tick_context.basis、删去 connection_order.basis。五个变体均已执行；具体数据见[未解判定上下文反例](r3-一致性证据/反例-未解判定上下文.json)及反例检查结果。

这不否定原始样例的各个 Decision 本来有效；缺陷是“完整输入检查”没有落实其已声明的拒收边界。修复应逐层核 Decision 的封闭字段、状态和值关联、依据，再检查具体状态值；不应把未知状态静默当 specified。

### K3-r3-L3-02：初始可移动级与空格仲裁使用固定样例答案

- **where**：`/home/zhuran24/zmd-research-fresh/求解器/数据/样例/runtime_example.py:65–78、175–183`。
- **claim**：运行检查把 ore_source 输出和 feed_belt 输入的 current_level 以及仓库空格仲裁表写成固定答案，没有从实际仓库指派和库存重算，因此接受矛盾种子并拒绝相应的正确派生状态。
- **severity**：重要。
- **evidence**：规则 L15“存货/取货端口：把物品存进/取出本单位物品格的端口”、L30“移动权只在能移动的通道中级别最高的那一级里轮询”、L73 取货口连接仓库某格；内核输入 §4 第 118 行明确“空格没有物品可取，历史身份不生成物品”，§6.3 第 308 行要求“初态在新时刻扫描前也须按物理守卫计算当前级”；第 284 行要求仲裁序覆盖已声明无身份空仓库格。受限转移定义 §3.1、§4.1 分别明确源必须有货、空格序必须完整。

反例保持单位、通道、建造史、96 轴取值不变：在初始仓库和 StateSeed 当前仓库同步加 `empty_extra`，item=null、quantity=0、empty_identity=specified/null；将 ore_source 的仓库指派改为该格。空格身份已解决，未违反主席的拒收要求。保留原种子的两个 current_level 以及 `warehouse_empty_slot_order=[]`。

此时该通道源格无物品，源输出侧和进料带输入侧都不应有可移动级；新增无历史空格又使空格仲裁表必须包含 empty_extra。检查器仍返回完整运行输入通过。原因分别是第 76 行以实例名设定 current_level、第 183 行无条件期望空格仲裁为空。实际参考运行随后通过 refresh 把两个 current_level 改为 null，四个时刻全部完成 0 批；这证实它是在运行中纠正被放过的派生状态，而非初态一致性验证通过。

反向试验将两个 current_level 改为 null 并补齐空格仲裁表，检查器反而以“轮询记忆成员/侧/级/位置或当前级不符”拒收。见[空源反例](r3-一致性证据/反例-空仓库源与错误派生状态.json)、[正确状态被拒绝结果](r3-一致性证据/正确空源派生状态被拒绝.json)。

本项不是要求立即支持任意布局：对于超出有限子集的源指派，可以明确 unsupported；但不能把其矛盾状态报告为完整运行输入通过。应从当前物理守卫与已声明空格构造期望状态，或完整限定本例固定源及仓库集合。

### K3-r3-L3-03：运行记录回归没有核实被交付轨迹

- **where**：`/home/zhuran24/zmd-research-fresh/求解器/数据/样例/test_runtime_input.py:131–153`；关联 `/home/zhuran24/zmd-research-fresh/求解器/规格/内核输出.md:27、37`。
- **claim**：回归只把重新计算的摘要与黄金摘要比较，对磁盘运行记录仅做 schema 和来源指纹检查，没有校验其完整状态、事件后效或摘要一致性，错误的交付轨迹仍可获“通过”。
- **severity**：重要。
- **evidence**：规则 L17 规定制造存货/取货格“放置上限 50”；第三轮任务书 §3.C 要求运行记录保存“实际轨迹、验证范围”；内核输出第 27 行规定“summary 只是可读投影，以完整状态为准，校验器必须核两者一致”，第 37 行要求分别验证输入完整性、事件守卫/后效/时序，未实现项须列出，不能仅凭 schema 通过说轨迹合法。

复现时不改磁盘被审文件，只拦截回归对 OUTPUT 的读取，返回一个内存副本：将 t=2 的 `crusher:input:0` 填入 999 件源矿，同时将同一 tick 的 summary.warehouse_ore 改为 12345。前者超过正式容量 50 且与实际执行相矛盾；后者与完整仓库状态 79997 不一致。其余输入、黄金、来源哈希均不改。

完整执行 `test_runtime_input.main()` 后仍输出“通过，tests=17”。第 131–134 行的 actual 来自另一次 `run(data)`；第 135 行读取 output 后，程序没有比较 `output.trace` 与 actual，而 schema 只检查 Quantity 字符串结构、summary 对象等。该回归所核验的输入及来源指纹无法发现只发生于输出内容的这种损坏；另有交付清单的文件哈希不等于本回归已经检查了它。

证据为[错误运行记录副本](r3-一致性证据/反例-错误运行记录.json)与[错误记录仍通过的日志](r3-一致性证据/错误运行记录仍通过.log)。本席独立重放原始实际记录未发现该错误，因此本项是验证缺口，不是声称原记录已装有 999 件。应比较交付记录与独立重算的完整状态/事件及摘要，或提供完整事件验证器；若暂不做，须明确标明交付记录语义未被回归验收。

### K3-r3-L3-04：参数投影的受限模型来源指纹失效

- **where**：`/home/zhuran24/zmd-research-fresh/求解器/数据/样例/kernel_profile_v1参数赋值.json:4–6`；关联 `/home/zhuran24/zmd-research-fresh/求解器/数据/样例/runtime_example.py:130–137`。
- **claim**：参数赋值文件声明的受限模型 SHA-256 与其引用的当前文件不相等，生成器再次投影也不能逐字节/逐字段复现这个来源记录。
- **severity**：次要。
- **evidence**：第三轮任务书 §3.C 要求记录 profile、逐字段赋值和指纹；内核输入 §5.4 称该文件是受限模型声明的序列化投影，修改请求 §3.4 明确“样例将锁定实际读到的声明指纹”；内核输出 §1 要求“指纹不匹配需重新核验，不允许刷新后沿用旧结论”。

实际记录值为 `c05b2fe73edfb93c2aacba2db0a2412fc850f81700c562f7c42cc5bdc6eeae17`，当前 `求解器/规格/受限模型声明.md` 为 `e499b39b60200618ab69a0a30a4be29efd29992d64428d9c9647e9c6c8cf236f`。只读重新调用 write_profile 后，完整 JSON 比较唯一差异就是 profile_source.sha256；其余 96 行的 decision、disposition、coverage_loss 均一致。

因此不据此指控具体选值错误，严重性限次要；但后续使用者无法把该指纹当成现版来源确认。证据见逐轴对照与原程序重跑。应在复核声明内容及投影一致后同步这一引用，并让回归真正检查该文件的来源哈希，避免只查另一份输出 fingerprints。

## 3. 字段、目录及送料映射核对

### 3.1 State 与 Parameters

StateSeed 与运行语义 §2 的 layout/settings/inventory/progress/logistics/environment/semantic_context 七组可以对接：几何/设定使用快照引用，仓库库存独立保存；制造 input/output/buffer 以及桥两独立格使用目录 role；进度含 phase/recipe/remaining；当前通道、逐侧逐级记忆、准入口独立阻断原因、统一接通史、时间与阶段及判定上下文均有编码。没有发现这些交付样例的机型、配方或槽位引用不存在。完整性验证的缺陷另见发现 01、02，不以“有字段”代替“已正确校验”。

参数轴表 §2、内核输入 §5 表、内核配置、运行例三个生命周期对象及参数投影有相同的 96 个字段；原 91 项保留，增加的 5 项为 polling.split_merge_scope、polling.split_merge_singleton、polling.both_failure、connection.belt_shape、initialization.belt_shape_lifecycle。无漏名/重复名。运行例 judgment.order 因 global 放入 fixed，其余 F/O/U 与轴表一致。已定值、本版选值和 stop 原样对齐配置；所有“由输入全称量化”接口给具体见证，当前 StateSeed.parameter_values 与输入值及生命周期一致。

stop 是覆盖处置，未当作实际游戏后效；空分支表对应此运行例无分流器，空传输相位对应无箱体。静态桥和分流例仍有未解轴，未被宣称为可执行实例。接口中保留真实时间与符号时间，不把当前整数轨迹推广为全部时间域。

### 3.2 正式目录

目录 18 类单位的 id、family、尺寸、端口型和 inventory.role 与输入引用一致；18 条配方的机型、物品用量与耗时另外从正式规则 L80–114 独立解析，逐条相等。现行目录回源脚本退出 0，核对正式全文、56 条约束、任务、单位和配方结构。数字类别和目录未定项仍与规格分开：桥容量 null 未解释为无限，制造混装与容量域由参数控制，供电覆盖按正面积运行值计算。

### 3.3 送料契约

内核输入 §7 的 machine_map/source_map/core_map/port_map/feed_map 分别对应送料契约 machines/sources/core/logical_feeds；机器 kind 与配方 id 采用目录编码。来源身份由边界方位、从角排序序号或核心六口规范表导出；source_domain/all_different 及候选 B 全 52 口总性已明确，未拿逻辑标签代替坐标。

PC 为端口实体通道，BC 为制造缓存通道，LF 为计划送料；二非运输单位直接相邻不生成 PC。储存箱是非运输端点，必须切分 LF，无线入库不生成 PC；内核输入明确 feeding-v2 目前不支持带箱完整绑定，不恢复已禁的 via.storage。三例 contract_binding 均为 null，当前检查器对非空明确 unsupported；没有外部契约映射实测通过的虚假结论，也没有要求本轮完成候选 B 物理布局。

## 4. 样例独立几何核算

本席用旋转后的矩形占格及**格边线段中点**相等、法向相反计算相遇，没有调用被审 checker.geometry/rotate_cell。每个占格逐点查基地范围和互斥，全部导出通道与 JSON 声明逐条相等。

| 样例 | 实例数 | 占格 | PC | BC | 结论 |
|---|---:|---:|---:|---:|---|
| 桥接器双通路 | 7 | 122 | 4 | 0 | 南箱→桥竖轴→北箱、西箱→桥横轴→东箱；没有跨轴路径 |
| 分流器三路轮询 | 9 | 128 | 4 | 0 | 南箱→分流器，再分别向北/东/西三箱；源口无额外意外相遇 |
| 混做粉碎机两下游 | 14 | 153 | 11 | 8 | 源口→供料带→粉碎机，两条输出带路分别到研磨机 |

桥例建造顺序使 south_box 成为竖轴先接邻端，west_box 成为横轴先接邻端；两种已登记先接解释在此无平局、非桥见证上结果一致。桥两格独立未被拿来主张调度独立。分流器三个出口接通见证依次形成，不靠数组默认来裁定运行轮询。

运行例面积算术为核心 81＋粉碎机 9＋两研磨机 48＋供电桩 4＋源口 3＋8 条带 8＝153。源口占 (29…31,0)，向北中点口面对 feed_belt(30,1)，其北口面对 crusher(30,2) 南侧第 0 口。a 路为 (30,5)→(30,6)→(30,7)→grinder_a:south:4；b 路为 (32,5)→(33,5)→(33,6)→(33,7)→grinder_b:south:0。折带旋转与 port_layout 的组合按目录相等，形状编码 straight/turn_left/turn_right 与入出边夹角 2/1/3 一致。

运行例供电桩原点 (34,3)，中心 (35,4)，正面积覆盖格列 29…40、行 −2…9，三台制造机各与其有正面积相交。供电是几何推导，不取输入任意 powered=true。桥例/分流例的静态检查不要求全部箱体运行；独立供电结果逐箱保存于几何证据，不将缺电等同布局非法。

## 5. 黄金前缀逐步复核

规则依据为 L18 批次闸门、L23 运输滞留、L24 零时移动、L25 串行判定、L29–32 轮询分级、L35 制造与 L81 源矿 1→粉末 1/1 tick；端口预算来自正式约束“每个端口每 tick 至多通过 1 个物品”。同刻排序与失败后效使用本版明示读法，不把该读法提升为唯一规则。

起点条件：非仓库 empty_on_build、开关开、核心六口无相遇、feed_belt 最后在 t=0 建成。之前无仓库向网络的通道，空网络不能自产物品；因此从所给条件史得到空库存、idle 和无当 tick 端口消耗。该论证只给条件见证，不能证明所有初态、时距或调试结束时刻。

| 时刻 | 核实的成功链 | 闭包后关键状态 | 扫描轮数/日志数 |
|---|---|---|---|
| 0 | 源口进 feed_belt，运输年龄为 0 不能继续 | 仓库 79999；进料带源矿 1；尚未开工 | 2 / 28 |
| 1 | 进料带出矿；制造模板归集并立刻开工；下一轮源口补矿 | 仓库 79998；进料带 1、缓存在制源矿 1，剩余 1 | 3 / 42 |
| 2 | 第一批到时完成；第二件入粉碎机；旧批整批出缓存并开新批；下一轮出到 a0、补矿 | 仓库 79997；a0 粉末 1、缓存源矿 1、进料带 1；next=b | 3 / 43 |
| 3 | 第二批完成；a0 足龄进 a1；旧批出缓存并开第三批；下一轮出到 b0、补矿 | 仓库 79996；a1、b0 各粉末 1，缓存源矿 1、进料带 1；next=a | 3 / 43 |

独立重放读取交付输入模板序，但自行实现源/目标选择、物理可动、两端授权、失败前移、完成边界和批次门控；没有调用被审 run 或其库存更新函数。逐条事件 id/operation/target/outcome、完整普通库存及入格时刻、仓库库存、进度、两侧全部轮询状态、成功移动、端口预算和 BC 穿越均与记录一致；完整输出 JSON 也与被审脚本只读重算一致。黄金摘要与这些状态相符。

出库 4 件＝2 件已制粉末＋1 件在制源矿＋1 件进料带源矿。每次运输出格足 1 tick，每口每 tick 不超过 1；缓存不套外部端口额度。两制造周期分别为 [1,2] 和 [2,3]，达到本轮“至少一个完整周期”的要求。没有以文件名“混做”宣称实际已混做，两研磨机在该段始终 idle。

## 6. 参数轴修改请求逐项取据

| 请求位置 | 核验结论及正式/任务依据 |
|---|---|
| §1 judgment.order、damping.branch、poll_memory 三编码 | 第三轮 §3.C 明确要求；排序以规则 L25 和任务 L14 定实例先后，阻尼表对应 L27 只选一支，轮询按 L29–32 分侧/分级。新增的是表示结构，不把状态编码另列物理轴；共享表已链接相应节。 |
| §2 传送带生命周期和三形状 | L60 有 4×3＝12 个有序异边对，旋转 L11 保相对夹角，分三轨道；L10 拆后再造和任务 L11 支持调试重选轨道，任务 L8 排除零干预后玩家改形。请求没有把“绝无其他机制”擅自定死，fixed_between_builds 仍明确是本版选择。共享新增 connection.belt_shape 与生命周期轴已迁移。 |
| §3.1 T8 窗口排除 | 遵照第三轮 §2.2；L64“走完后由下一件物品重新起算”与准入累计/来源定序排除窗口永不恢复及全原因停表。身份专属停表仍带辖域待审，未无据删除。 |
| §3.2 空格身份 | 第三轮 §2.3 明确要求；L13/14/41/73 的仓库格与端口指派没有生成库存授权。沿用 warehouse.empty_slot_identity，无新增同义轴；静态未解身份拒收、已解无身份可表示。运行派生状态检查另有发现 02。 |
| §3.3 占格与供电 | 遵照第三轮 §2.1，不再引用 L8/L11/L41 证明互斥；positive_area 是全称目标的最严供电义务，保留 closed_touch 的行为差异。没有再把最严合法性原则推广到轮询/分流。 |
| §3.4 四条前置选值与 both 失败后效 | 第三轮 §3.A 明确要求。当前受限声明及配置已有 chronological_phases、completion_before_closure、fixed_arbitration、shared_edge_opposite；both 失败后效已拆为 advance_authorized，单侧授权可触发失败，未授权侧不前移，与 L29 的尝试后传权协调。具体条件轨迹已重算，不表示普遍规则相容性获证。来源锁定执行差异见发现 04。 |
| §4 版本协调 | 第三轮 §4 明定 A 线拥有共享三表写权；96 项总数按当前表算，不为维持 91 漏项。额外 5 轴与共享正式并入名相同，无第二套私有别名。 |
| §5 最终对接记录 | manufacture_subactions、级仲裁、空格仲裁、judgment_context.phase、稳定级 id 及三原因阻断均有共享规格接口；未发现提出这些状态字段需要额外游戏机制。记录所说“当前96轴”属实；实现验证边界仍有发现 01–03。 |

## 7. 结论边界与交付自审

已有 19 项负例、8 组表示回归和 17 项新增回归在本次只读重跑均通过；通过这些既有测试不能抵销本席新增反例。没有要求本轮补 Rust 内核、实际混做或全称循环证明，这些已披露的未决不重复列为发现。非空送料映射、其他合法初态、分叉跨次变化、离线/操作后效、完整仓库周期提升也不因本次前缀通过而获证。

报告已按首次独立读者视角复读：结论数量、路径、行号、规则引文、反例操作、原始轨迹与校验器缺陷的区别均可独立追溯；交付物中不含操作对话指令。原始任务的主席已定事项没有复议；严重性按当前实际影响给出，没有因为缺全称认证把局部通过结论判错。
