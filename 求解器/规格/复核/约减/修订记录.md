# 约减线修订记录

性质：按轮追加的修订史料；截止日期：2026-09-19。当前论证以[参数扫描约减](../../参数扫描约减.md)为准，当前机器核验以本目录[验证结果](验证结果.json)及其源指纹为准。

## 第1轮：RD-r1-L2-1

发现：`damping.branch`的“无进一步约减”遗漏固定布局中不可读表行的投影。处置：接受，已修订，无本条未处理项。

修改：

- 正文§2第2项、§2.1给出永久出边不变量、可读行安全超集、投影类数公式和逐事务后继保持证明；完整v2输入各行仍保留，不可读行规范填充。
- 正文§2.1落实本版单级不请求阻尼的充分条件与级数单调上界。分流器的三支直达箱体时24表投影为3；四处分叉的完整表域96投影为12；两样例固定运行段均有1个行为代表。完整编码、投影类数和行为代表数不混用，不声称门控超图实际可达。
- `count_classes.py`增加分支结构计数、完整代表表生成和原表规范化；计数JSON保存每行是否保留、永久/可切边、全几何级数上界和完整行为代表表。未知字段、缺行、重复行和行外选择不得靠规范填充变成合法输入。
- 新增`verify_branch_projection.py`并接入`verify_reduction.py`。只读调用否证席已有独立几何/穷举纯函数，核当前样例，不覆盖原复核或否证记录。新增`分支表约减验证.json`保存全部97个原表点到投影代表和行为代表的映射。
- 全文核对§1、§3.4、§5.2–5.3、§6、§7，删除仍声称未获不可读行证明的当前结论；Python参考入口状态绑定每次机器探针及源指纹，保留未完成任意判定序差分的边界。保守操作图明确为全局刷新读写上界，不依赖新实现每次实际刷新全图。

依据：正式规则L16（自动成通道）、L27（阻尼）、L31（取货分级）、L64（仅断准入口存货端通道）；任务L8（零干预）、L13（离线）；约束·分叉分支。无结构变化、方向已解、无离线前提来自[受限转移定义](../../受限转移定义.md)§1；唯一删边/恢复位置见§2.1–2.2、§3.4；单级不请求阻尼前件见§3.1及第五轮任务书§2.5；完整表接口见[内核输入](../../内核输入.md)§5.3。两份[否证1](否证-r1-1.md)、[否证2](否证-r1-2.md)均保留该发现；本轮重新核验结构与表投影，不把既往有限Rust实跑当全状态证明。

自查：在求解器目录执行`python -B 规格/复核/约减/count_classes.py`、同脚本`--check`、`python -B 规格/复核/约减/verify_reduction.py`；最终日志见[修订-r1-自查.log](修订-r1-自查.log)。组合计数仍为1100图/124470排列，76图相邻交换分量检查通过；judgment.order原四个类数不变。分支核验覆盖1表×1图及96表×4图，后者1536次假设局部查询值比较相等；缺行、多级误用1代表的负例拒绝，三出支全可切时保留24表。整体状态`passed_with_runtime_limitations`，运行接入限制不等于本发现未解决。并行迁移期间第一次Python基线分别运行4、12时刻，重核内核源码指纹后的探针又因“参数轴表哈希变化，须重新对齐”拒收；两次结果均留日志，不绕过校验。正文不硬写易漂移的入口结论，当前状态以验证JSON及源指纹为准。源指纹稳定后交付；正文以未来读者视角通读，交叉引用及三个数量口径已核对。

范围：只修改本线正文和本目录脚本/日志/JSON/Markdown；未修改正式文件、候选约束、样例、内核或模拟器，未编译、提交或推送。
