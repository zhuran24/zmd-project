# 第9轮规格线一致性复核

日期：2026-09-19。复核席：GPT 源，一致性视角。被审版本：`round5-specification-r8`。结论：**本轮未发现需要新增登记的 S 线一致性缺陷，findings=[]。** 此结论限本报告列明的字段、编码、登记和修订落点，不表示 K 线实现已通过运行验收，也不表示完整目标、级二提升或全称覆盖获证。

## 1. 依据、对象与证据范围

先完整读取任务书5，再读取任务书1–4；任务书5 §0、任务书3 §0的辖域说明均有效。游戏判断依据仅为三份正式文件：

| 正式来源 | 本次核对的字节 SHA-256 | 本次主要使用范围 |
|---|---|---|
| [游戏规则](../../../《明日方舟：终末地》游戏规则.txt) | `abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670` | L13–18 库存与缓存，L20/L23–25 工作量、年龄与判定，L27–32 阻尼/轮询，L35–36 制造/传输，L41/L64/L72–73 容量、门、仓库接口 |
| [求解任务](../../../求解任务.txt) | `10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864` | L2 目标率及量词，L6–9 仓库/矿供给/拿取/循环，L12–14 操作精度、离线和判定次序 |
| [求解约束](../../../求解约束.txt) | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` | 分叉分支、判定先后、端口速率、入库途径、轮询均分和密集结点及其前件 |

任务书中的已定事项用来核执行是否一致，不增加第四份游戏规则。候选约束仅核保护指纹；没有据其内容裁定机制，没有使用面积条件选择行为。第四轮完整性批评、R4-Q1/Q2、KQ-01–08作为问题及接口沿革读取，不直接作为规则证明。

受审对象涵盖任务指定的规格正文、配置、两份修订记录、三个自查脚本及 r8 的全部所列 Markdown/JSON/日志；另对照现行 `内核输出.schema.json`、`model.rs`、`transition.rs::closure_key`、`cycle.rs` 的编码段及 `input.rs`/`polling.rs` 的分支入口。完整文件指纹见[本席读取指纹](r9-一致性证据/本席读取指纹.json)，r8 原始清单复核见[r8来源复核](r9-一致性证据/r8来源复核.json)。

r8 清单中35个带哈希文件当前全部匹配。其25项自查来源中，仅 K 线拥有的 `内核实现-对规格的疑问.md` 与当时记录不同；当前仍为 KQ-01–08，已回读。该历史字节差异没有混算为 S 线修改，也没有刷新旧来源记录。

## 2. StateSeed、生产键与同刻 closure_key

### 2.1 完整状态与实际闭包键

输入 §6 的完整 `StateSeed` 恰为八个顶层：`layout_snapshot`、`settings_anchor`、`warehouse`、`inventory`、`progress`、`logistics`、`environment`、`semantic_context`。Rust `State` 与输出 schema 的 `StateSeed` 同集；`nonwarehouse` 只是输入封套的历史字段名，不能据此漏仓库。

本席按输入正文独立列出17个 Rust 状态相关类型的68个直接成员，再与当前 `model.rs` 逐类型做集合相等检查，未发现漏项或额外成员。这里的68是类型成员数，包含容器成员，不冒称 JSON 叶字段数。完整清单见[状态字段与闭包键](r9-一致性证据/状态字段与闭包键.json)。Decision 中的动态对象还按下节人工展开，没有用仅搜字段名字代替层级核对。

当前 `transition.rs:613–627` 的同刻键确为以下11项，次序与受限转移 §6.2 相符：

| 元组项 | 当前代码成员 | 输入对应对象 | 生产键的差异 |
|---:|---|---|---|
| 1 | `state.warehouse` | 当前仓库格表及 unlisted | 限 D 抽象两矿数量及两成品行；其余保留 |
| 2 | `state.inventory` | 普通格/运输格/缓存内容 | 逐件 entered_at 转年龄 |
| 3 | `state.progress` | 制造阶段与冷却 | 保留剩余工作量及全部批次描述 |
| 4 | `memory` | 解码后的 poll_memory | 保留每侧、每级、成员环及游标 |
| 5 | `state.logistics.active_channels` | 现存 PC | 保留集合 |
| 6 | `state.logistics.blocked_channels` | 阻断 PC | 保留集合 |
| 7 | `state.logistics.gate_counters` | 门计数/原因/起点 | 起点转 elapsed，累计数不删 |
| 8 | `state.logistics.connection_order` | 接通关系与历史引用 | 保留固定历史常量及当前关系 |
| 9 | `state.semantic_context.arbitration` | 级全序与当前空格序 | 级序完整保留，D.3要求空格序为空 |
| 10 | `pending` | 活动待办事件 | 截止转偏移，事件身份随之规范 |
| 11 | `usage` | 当前端口额度图 | 保留每口用量，显式零与未列零等价 |

同刻时钟及阶段固定在当前闭包，布局、设定和参数属于引擎固定上下文；跨刻生产键则另保留锚点和参数并锁定完整上下文。故“这些量不在私有元组中”不等于遗漏可变守卫。受限转移 §6.2 明令不得把生产键替换旧闭包键，当前代码也是两个独立接口。

### 2.2 动态叶字段逐项复核

表中路径相对于 StateSeed；`sc`表示`semantic_context`。字段删除均限受限转移 §6.1 的 D 域与 after_closure。

| 字段组 | 输入/规格位置 | 核对结果及理由 |
|---|---|---|
| layout_snapshot；settings_anchor.event/side | 输入 §6、转移 §6.2 L150–151 | 锚点保留，完整布局与设定另锁定；不是仅凭同名 id 跨运行判等。供电由固定几何与参数导出。 |
| warehouse.unlisted | 输入 L311–315；转移 L152 | 保留 empty；没有把未列矿/植物当可任意补给。 |
| warehouse.slots[].slot/item/quantity/empty_identity.status/value | 输入 L311；转移 L153–154 | 两矿保留身份，正数量替为 sufficient；成品的非空/历史身份整行移出；其他物种完整保留。两成品接收标记单列。category/basis只进附件。 |
| inventory[].slot/contents[].item/quantity | 输入 L321；转移 L155–156 | 全部保留，含缓存与桥两格；无序记录规范排序，同种不同年龄不合并。 |
| contents[].entered_at.kind/value | 输入 L321；转移 L157 | 非空时刻转 `age=t-entered_at`，null仍null，非运输记录也保守保留；没有饱和到1。 |
| progress[].unit/phase/recipe/candidate_recipes/locked_recipe | 输入 L322/353；转移 L158 | 全保留；候选集合规范排序不改变参数里的配方优先序。 |
| progress[].remaining；cooldowns[].slot/remaining | 输入 L322；转移 L159–160 | 已是剩余时长，保持null和0的区别；不再减观察时刻。 |
| logistics.active_channels/blocked_channels | 输入 L323；转移 L161 | 两个PC集合保留，与门原因分别存。 |
| poll_memory.status/value.schema | 输入 §6.3；转移 L162 | 已解封套及poll-memory-v1保留；不是把未知记忆默认为空。 |
| sides[].unit/side/graded/current_level | 输入 L349–351；转移 L163 | 每单位两侧完整保存，桥不另拆四侧；无可动级与未分级情形分别处理。 |
| levels[].id/members/next_channel | 输入 L349–351；转移 L164 | 级标签及完整接通环保留，members不可任意排序；失败后游标仍是有效状态。 |
| gate_counters[].unit/total_received/window_received/blocked_reasons | 输入 L323；转移 L165 | 两计数及三种独立原因完整保留；无累计限额也不删除total_received。 |
| gate_counters[].window_started_at | 输入 L323；转移 L166 | 转为elapsed，不取模5；null表示未开窗。 |
| connection_order.status/value | 输入 L323/327；转移 L167 | 当前顺序/历史引用保留；静态建造时间不随当前t漂移。 |
| environment.time/stage/online | 输入 L324；转移 L168–169 | time规范为0，stage/online保留且由D限制；没有在时间平移中改变运行阶段。 |
| withdrawal_memory.status/value.once_fired/pending_rules[].rule/event | 输入 L324/§6.2；转移 L170 | D域无动作/待响应记忆保留为空标记；级二另量化拿取族，不在生产键里猜策略后效。 |
| sc.arbitration.level_order | 输入 L327；转移 L171 | 全体几何可能级的独立全序保留，暂空级标签不删。 |
| sc.arbitration.warehouse_empty_slot_order | 输入 L327；转移 L172 | D.3先拒非空O，再规范为空；普通有限运行仍存真实动态O，未把两域混同。 |
| sc.parameter_values[].axis/lifetime/value.status/value | 输入 L327/353；转移 L173 | 99轴及实际值、生命周期逐项保留；初始化时间是固定参数，不能随t平移。 |
| judgment_context.status/value.instant/phase/order_scope | 输入 §5.2；转移 L174 | after_closure/global保留，instant归0。 |
| judgment_context.value.round/next_template/ordered_events/next_event/continuation | 输入 L278–291；转移 L175 | 已完成扫描的审计前缀移出；next_event必须null、continuation必须不存在，否则拒收。中途种子没有被混入生产键域。 |
| pending_events.status/value[].operation/target/status/predecessors | 输入 L327；转移 L176 | 保留操作/目标/waiting；仅制造完成或门到期；依赖数组须空，不删除任意后继依赖。 |
| pending[].trigger.kind/value | 输入 L327；转移 L177 | at_time转deadline−t；制造remaining判完成，预计截止仍保守留下偏移，窗口截止仍受start+5约束。 |
| pending[].event | 输入 §3.1；转移 L178 | C/W身份转(operation,target,deadline−t)，保留事件引用一致性；不是复用执行id。完整状态及重跑仍用实际新id。 |
| tick_context.status/value.window_start/window_end | 输入 L329；转移 L179 | 规范为0/1偏移，对当前完整窗口校验后使用。 |
| tick_context.value.port_usage[].port/quantity | 输入 L329；转移 L180 | 用量映射保留；未列口按已核0，不能删掉实际1件额度。 |
| tick_context.value.movements[].event/channel/item/quantity | 输入 L329；转移 L181 | 已办账移出键而完整保留证书；实际库存/额度已经入键，不能把账删除误作额度重置。 |
| tick_context.value.internal_passages[].event/batch/channel | 输入 L329；转移 L182 | 已办BC审计身份移出键；未完成批次仍由progress与缓存完整表示。 |
| 全部Decision.basis、Quantity.category | 输入 §1.1；转移 L146/183 | 验证后作为证据附件，不作物理守卫；未来若读取它们影响后效须升级键版本。 |

`cycle_key_reference.py` 与当前 `cycle.rs::encode_key` 的主编码路径均按这些步骤处理；检查了矿代表、成品历史格识别、年龄、elapsed、pending偏移、当前窗口和端口零项。当前域明确整数时钟，未把 Python 可表示的有理时间误称 Rust 已支持的运行域。本席没有运行 Rust 差分测试，不据源码阅读宣布整个周期执行器通过。

### 2.3 KQ-07/08与字段表的衔接

输入 §6.4 明确区分新调度起点与检查点恢复：`level_order`由调用方显式给，after_closure保留实际游标、参数、账及事件；对应输入 §6.3 和转移 §6.2 的保留字段，没有新增未定义状态字段。规则L29–32中的轮询历史依赖不允许从相同库存反演唯一游标，这一限制已在正文、修改请求和修订记录同步。

D.2现在排除的是读取矿容量的候选尝试，包含最终拒收者：现存PC源选择器发现矿候选指向核心，在目标容量及physical/授权前停止；有资格箱全批含矿在容量规划前停止。79999与80000各回1件，容量判定不同，即使后者成功入库账为0也不能删除数量依赖。这个局部算术来自规则L13/L36/L41；任务L7保证取矿充分，不保证无限接收。转移 §2.1、§6.1、§6.3、§6.5，输出 §5.3和三处参数登记均已对齐。它是生产抽象的保守准入，不是普通运行新增游戏守卫。

## 3. 输出正文与schema

逐项比较40个封闭对象的字段集合、required集合及additionalProperties，另核Event仅detail可省、全状态/增量轨迹均引用带台账的Tick。完整机器清单及每个对象的实际schema见[输出字段清单](r9-一致性证据/输出字段清单.json)。字段对应关系如下：

| 正文 | schema落点 | 结论 |
|---|---|---|
| §1顶层、来源、验证范围 | RunRecord及producer/fingerprints/input_history/uncovered_axes/validation_scope | 字段集一致；validation_scope为唯一字段名。completed所需四个非空对象有条件限制；三个全称/达标标志固定false。 |
| §2逐刻状态与事件 | Tick、StateSeed、Event | 事件类型含gate_identity_maintenance；detail为可选字符串，正文要求该字符串可解析为指定维护内容。 |
| §2.0六路明细及总账 | CoreInbound、WirelessInbound、PortOutbound、ExternalSupply、PlayerWithdrawal、RepresentativeAdjustment、WarehouseTotal、WarehouseLedger | 路径、端口、slot、物种和Quantity字段对应；actual_inbound仅两入库路，代表调整独立。 |
| §2.1两种记录 | FullTrace、CheckpointTrace、DeltaTick、DeltaOperation | k>0、object_replace_v1、replace及非空对象路径一致；两编码均要求warehouse_ledger。 |
| §5.1共用对象 | CycleResult、seed、parameter_point、reading、support_domain、budget、stop | 17个顶层字段一致；input_axes恰15个；相遇读法显式；停止与预算结构齐全。 |
| §5.2周期内容 | Cycle、CycleKey、CycleRate、Acceptance、ProofSource、normalization、lift | 两完整端点、完整规范键、周期台账、实际率、接收域、提升材料均有字段；级一execution_mode与schema条件相符。 |
| §5.3结果分级 | CycleResult.allOf | cycle_found有周期且stop=null；counterexample有周期及low_rate；技术停止/未决无cycle；inconclusive要求resource和前缀材料；full_base成功至少要求proved提升对象。 |

71个AJV2020正负例全部符合预期，含各台账对象逐字段删除、未知字段、旧验证范围拼写拒收；结果见[schema正负例](r9-一致性证据/schema正负例.json)。本席在内存执行生成器，移去唯一write_text和末尾打印语句；生成字节与当前schema完全一致，未重写被审schema。当前schema哈希为`5c3cb1ace272c34439381ba770b000b26bdb745db20e52ca4008ff13be0d657e`。

**结构与语义的边界也一致。** schema复用通用Quantity，不能单独保证每笔件数正整数、核心移动恰1、周期P>0、率计算正确；Decision.value、规范键的内部值、replay_input的完整合法性也另交输入/语义验收。正文§1、§3、§5.3已明文要求这些检查，因此本席不把“schema单独能接受某个语义坏值”自动判为字段缺陷。相反，若K线验收器只检schema便发证，将违反这些已写义务。

当前full_base的语法保留不代表可发证：正文§5.2明确在级二未证时拒绝成功发证，与periodic_lift的停止处置一致。after_closure首项t+1是跨状态语义，不由Time形状schema验证；正文§2、§2.1、§5.3已用同一口径，周期账区间统一为(a,b]。

## 4. 三处参数登记与分支转移

独立检查99轴集合、生命周期和输入说明投影；原规格检查另逐轴比较声明与JSON的处置、值、含义、依据、覆盖损失和补齐节点。99轴保持55本版选值、18超出覆盖即停、15由输入全称量化、11已定。

| 轴 | 配置与声明 | 轴表及转移对应 | 结论 |
|---|---|---|---|
| warehouse.external_supply | U；由输入全称量化；ore-supply-v2，default_mode=sufficient | 轴表L100、输入§5.4明确须显式选sufficient或explicit_ore_history；循环用前者，后者保留有限差分用途 | `default_mode`没有变成漏字段时静默默认；不减少15输入轴。回矿容量前的D.2限制及普通容量后效均登记。 |
| warehouse.periodic_lift | U；超出覆盖即停；触发为请求级二完整基地周期提升 | 轴表L103、转移§6.5定位F的79999+2容量卡点 | 停止不挡级一；obligation不是可枚举true；未声称完整基地已证。 |
| damping.branch | U；由输入全称量化；damping-branch-v2 | 轴表L41、输入§5.3、转移§3.4，按当前局部出支集合查固定表 | 跨集合重查与整场表固定不矛盾；查询起点、其他图部分和历史依赖均列未覆盖。 |

输入 §5.3 与转移 §3.4的对应逐项为：

1. 外层五字段`schema/fixedness/choices/evaluations/on_missing`分别要求v2、by_available_set、完整表、空数组、unresolved；行对象为`fork_unit/available_channels/outgoing_channel`。
2. 可用集合是现存几何出支，不按physical、库存、端口额度或对端授权筛选。表中集合排序只作集合编码。
3. 每个需查询分叉的全部非空子集均须预填，含单成员集；所选支属于该集，键不重复。当前失活的行不能被删除。
4. 图变更提交后重查新集合；所选旧支失活本身不再停止；恢复旧集合必回到原选择。空集合或所选无终点路径仍报damping.no_terminal，缺表仍报damping.branch，不能默取第一支。
5. v1若按不同查询起点对同一fork异选，不能无声迁成v2；正文明确说明该覆盖损失。

条件复算用三出支的7个非空子集，对应24张局部完整确定表；示例集合从abc→ab→b→abc时依表选择c→b→b→c，验证“表不变而选择随可用集变化”的含义，见[分支局部复算](r9-一致性证据/分支局部复算.json)。这只核两处定义的同一函数，不证明所有图或全部动态约束。

## 5. 修订记录与本轮存活项闭合

| 登记项 | 现行正文落点 | 本席复核 |
|---|---|---|
| S5-r8-L1-01 | 答复L26；转移§4.3；四件义务§2 | U/E/assigned_slots齐全。停止为至少两新物种且E中有取货指派，全未指派继续规范落格。 |
| S5-r8-L2-01 | 同上；覆盖表L48的规则L36行；修订记录L189/L232/L243 | 同一缺陷合并实现但逐id登记；旧宽条件已说明被收窄，未以旧历史行冒充当前条件。 |
| S5-r8-L2-02 | 答复L22；输出§1/§3；RunRecord | validation_scope一致；旧verification_scope已由负例双重拒收，无兼容别名。 |
| KQ-07 | 输入§6.4；答复L28；修改请求KQ-07；输入修订记录L131 | 新起轮派生与历史恢复分域，独立级序入口、检查点保留均落正文，未只写修订记录。 |
| KQ-08 | 转移§2.1/§6.1/§6.3/§6.5；输出§5.3；修改请求KQ-08 | 检查位置是矿容量读取前，满仓失败候选亦在域外，零成功回矿账不够；登记与正文一致。 |
| 第五轮S1–S4 | 转移§6、输出§2/§5、输入§5.3/§5.4、普遍审查场景 | 字段键、两级区分、实际入库账、v2表、场景前件及K线交接都有实质落点；未把场景局部推演登记成Rust完整运行。 |

r8停止真值表60组另行逐行复算，现行转移与答复中的谓词完全相同；U有两个物种、E有一个未指派空格时继续，加该格指派则停止，见[停止前件独立复算](r9-一致性证据/停止前件独立复算.json)。该检查覆盖有限前件组合；一般未指派标签置换的依据仍是规则L13、L41、L73及无改指派/拿取/离线、retain_history的条件归纳。

旧修订记录按日期和轮次保留历史，已撤回的相遇唯一性、宽停止条件及旧版本均有后续条目定位；本席没有要求将史料中的v1/v2名称改成现行v3。r8声称schema字节不变与当前35项清单吻合；其本轮22项是含子入口的自查计数，不能与183项相加当作独立证明数。

## 6. 实际复验与剩余边界

| 本席执行 | 结果 | 证据 |
|---|---|---|
| check_revision.py | PASS，99轴及输入投影一致；114行规则、15行任务、56条约束的登记核对通过 | [规格自查复跑](r9-一致性证据/规格自查复跑.json) |
| check_round5.py，输出改指本复核目录 | 183项PASS | [第五轮复跑结果](r9-一致性证据/自查结果.json) |
| 本席check_consistency.py | 230项PASS，包含60行停止前件；71个schema试样符合预期 | [一致性自查](r9-一致性证据/一致性自查.json) |
| r8交付哈希核对 | 35项匹配；schema内存重建字节一致 | [r8来源复核](r9-一致性证据/r8来源复核.json) |

复现命令（仓库根目录）：

```bash
python -B 求解器/规格/check_revision.py
python -B 求解器/规格/第五轮规格修订/check_round5.py --output-dir 求解器/规格/复核/r9-一致性证据
python -B 求解器/规格/复核/r9-一致性证据/check_consistency.py
```

未直接运行r8的check_round8.py：它调用的schema生成器会向被审schema执行write_text，与本席“不得改被审文件”的写权不合。本席读取该脚本及其全部所列输出，另用内存重建、独立字段清单、原第五轮复跑和60组前件复算核验相关内容；没有把这说成原22项入口被原样复跑。

仍未解决但已正确披露的事项：级二F的容量前提不足；参数/读法/种子族全称覆盖；非精确拿取族可实现性；动态约束普遍相容性；K线最终规格迁移及从循环起点独立重跑。无界门累计或精确年龄导致没重键时仍只能报未决。这些属于已登记边界，本轮不重复制造发现。

交付封存时观测到K线的`tests_round5.rs`、`main.rs`、`cycle.rs`有并行更新，原读取指纹保留。已再次读取`cycle.rs::encode_key`的完整字段处理段，仍与本报告字段表一致；`model.rs`和`closure_key`所在的`transition.rs`未变。本结论不包含这次并行更新的CLI或运行测试验收。

本席写入仅为本报告及`复核/r9-一致性证据/`内脚本、JSON和日志。没有修改被审正文、配置、schema、三份正式文件、候选约束或模拟器；没有编译、创建编译缓存、复制仓库、commit或push。交付前按未来首次阅读者重读全文，核对结论范围、计数、字段路径、引用和原始字节；最终检查见[交付自审](r9-一致性证据/交付自审.json)。
