# 第9轮规格复核：判等独立推演

日期：2026-09-19。席位：规格线复核，GPT源，独立推演视角。结论：在《受限转移定义》明示的D子域、生产抽象G及其停止边界内，未发现本轮应登记的新问题；`findings=[]`。这不表示级二提升、全部规则相容性、种子可达性或K线实现已经通过。

## 1. 依据、对象与复核顺序

规范裁判仅为仓库根的《明日方舟：终末地》游戏规则.txt、求解任务.txt、求解约束.txt。任务书5§2及任务书3§2的已定事项作为本轮执行要求，不重新表决。候选约束仅核字节未变，未作为推理依据；未用面积分档、模拟器或先前研究结论。

先完整读取任务书5，再读取同目录任务书1–4；随后回源规则、求解任务及正式约束，读取受限转移§1–§5和输入状态定义。未读取S1证明时形成[判等独立推演-r9.md](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/判等独立推演-r9.md)，之后才读取受限转移§6、修订论证、自查脚本及本轮产物。独立稿保留初始判断，本报告记录逐项比较及其适用范围。

重点核查对象为受限转移§2–§6、内核输入§3–§6、内核输出§2/§5，以及参数轴、声明、配置、运行语义、T12、四件义务、规则覆盖、接口答复、内核修改请求和两份修订记录中的相关投影。第8轮脚本、日志、JSON证据均作来源及范围核对；其历史证据不自动升级为本席实跑结果。当前Rust `model.rs`只读用于交叉核对字段，未以实现行为替代正式规则。

来源锁定见[开工指纹.json](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/r9-独立推演-证据/开工指纹.json)。三份正式源SHA-256分别为：

| 正式源 | SHA-256 |
|---|---|
| 游戏规则 | `abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670` |
| 求解任务 | `10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864` |
| 求解约束 | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` |

被核《受限转移定义》SHA-256为`55e3caee8ab40d1003b6002f1283c46734ef77a122e2870a2455fb4f14355450`。交付前复算显示40份开工登记文件未变化；第8轮交付清单中35份带哈希文件也全部与当前字节一致。此项只确认本报告对应的版本。

## 2. 含绝对时刻字段逐项对照

“可归一”必须同时保证守卫、后效及活动事件引用可对应。仅将时间写成相对数，不足以处理任意外部日程。当前§6以after_closure、global模板序、整数边界和无未来外部事件限定比较域，与独立推演要求一致。

| 字段或依赖 | 独立推论与正文对照 | 结论 |
|---|---|---|
| `environment.time` | §6.2 L168归零，L130/L134固定锚点与环境；规则L23、L35–36、L64的有效时间仍由各相对量承载 | 可删共同原点，不删经过时长 |
| 运输格及桥两格`entered_at` | L157变`t-entered_at`；§3.1读年龄≥1，§3.3新入格记当前时刻；边界后年龄同步加1 | 守卫及更新保持 |
| 非运输`entered_at` | 本版`residence.nontransport=zero`可以不读，但L157仍保留精确年龄 | 比必要条件更保守，可能漏检，不制造假周期 |
| 制造`remaining` | L159原样保留；§2.1 L21只以剩余工作量归零完成，暂停保持，与规则L20一致 | 不把预计截止当实际完成条件 |
| 箱`cooldowns[].remaining` | L160本身为相对时长；§2.1按功能状态推进，§4.3以0判资格 | 无须再减当前时刻 |
| 门`window_started_at` | L166变elapsed，null保持null；规则L64首件开窗、满5后清窗，不能只取模5 | 起点、到期及重新开窗对应 |
| `judgment_context.instant` | L174归零；phase保留after_closure/global | 边界阶段不可混比 |
| 扫描round、next_template、ordered_events | L175只删除已闭包审计；next_event必须null、continuation不得存在 | 不会丢失下一刻调度位置；不适用于中途恢复 |
| pending绝对截止 | L177保留deadline−t；制造预计值保守保留，但不决定完成；窗口截止核start+5 | 相对日程更新保持；预计值漂移只降低检测完备性 |
| pending事件id | L178按operation、target、相对截止归一，同一实例引用必须一致 | 没有粗暴抹去字符串导致活动实例合并 |
| pending依赖 | L176仅接waiting、空predecessors，after_event/condition依赖不在D | 独立推演要求的活动依赖保留由明确限域满足 |
| tick两边界 | L179保留start−t=0、end−t=1 | 与整数step_tick同步 |
| tick端口额度 | L180仍保留端口件数，虽下一边界会清零 | 不能因账本是审计而连额度一起删除 |
| tick移动与BC的event/batch引用 | L181–182只删除已提交且不再被守卫读取的账；未完批次仍在progress | 前件明确，完整记录仍留证书 |
| 建造、接通的历史时刻 | L167保留固定常量/关系，§3.1只比较先后及并列，§3.4恢复原记录 | 不应变成随t增长的历史年龄；保守保留常量正确 |
| 参数中的初始化时间、模板日程 | L173保留固定参数原值，L134排除per_instant及未来外部事件 | 不在固定参数点内部偷换时钟依赖 |
| 显式补矿events/time/through | §6.1只接sufficient；§2.1/输入§5.4把显式有限历史留给普通有限运行 | 不将有限日程尾部错误循环播放 |
| 拿取策略记忆 | L170要求G中无动作/空活动记忆；级二另外量化F | 不以清空策略记忆代替环境提升证明 |
| 固定layout/settings锚点和其它历史id | L128–151锁定完整上下文；D.1要求没有未来运行身份预占，输入§3.1/输出§2另核I/J/C/W保留域 | 键相等不跨种子、参数或未解析上下文成立 |
| basis、category和日志计数 | L146/L183仅删除已核且不参与守卫的元数据 | 不能据此删除实际状态值或历史校验义务 |

StateSeed的八个顶层对象及Rust当前字段已对照。非时间状态也有表项：非仓库库存、制造锁定、箱冷却、实际边、门原因/计数、各侧各级成员与续接位、级仲裁全序、参数值及剩余仓库真实状态均保留。没有发现从§2–§5推得必须参与后继、却在§6.2无前件删除的字段。

精确年龄、没有上限时仍增长的门累计、暂停制造的审计截止偏移都可能让语义重复而键不重。这属于键偏细导致的漏检；§6.2 L165、§6.4 L207及四件义务§3明确未证明有限抽象。预算内没有重键不能解释为没有循环。本轮未把该已登记边界另报成新发现。

## 3. 仓库两矿与两成品抽象的读取依赖

### 3.1 矿量不只是“非空”

规则L13/L41规定单种单格及80000容量；任务L7保证供应充分，没有授权无限回仓容量。转移§4.1核心PC读`q+1≤80000`，§4.3整箱读`q+b≤80000`。设相同候选矿一件、q分别79999/80000，即得成功/拒收差异；拒收方成功回矿账为零也不能消除容量观察。

§6.1 D.2 L135已覆盖这一独立推得的漏洞：现存PC的源矿候选指向核心时，在目标容量、其它physical条件、级排序及授权之前停止；有资格传输的箱含矿时在容量规划之前停止。它甚至不以年龄或预算未满足为由跳过候选，故前件偏保守而不依赖被删的q。其触发只读图、源物种、箱库存及资格，这些在键中相等；同键两侧可以同步停止。普通finite_concrete保留真实容量后效，没有把合法坏运行删成物理非法。

输入§5.4、运行语义§5、参数轴`warehouse.external_supply`、声明及JSON、接口答复、修改请求KQ-08、输出§5.3均保留这一容量读取前检查。动态D.2须核整个周期每次访问及独立回放，端点或零回矿总账不够。未发现正文和交接在这一点仍有走样。

### 3.2 取空成品格会不会影响非仓库状态

一般会。指派端口读取的是仓库格标签对应的实际物品，而不是“成品物种的总量”。若该格有出口，拿取至空改变源可动，进而可能影响门身份、阻断图、级序、轮询及制造。若身份释放后重新进入匿名序，新物种落格也可能通过该指派口被观察。

当前§4.1 L69–73明确retain_history：取空将item转为empty_identity，原标签保留，匿名E/O不增加，再入同种仍复用历史格。§6.1 D.3另要求成品非空/历史格没有取货指派、O当前为空且以后不新增匿名格，并对全部规范新标签作物种归属检查。因而：

1. 不存在能直接读取成品具体数量的取货口。
2. 成品首次入库不会消耗可被指派观察的匿名格，也不改变其它物种的规范标签。
3. 取到0不改E/O，同种重入不分配新标签；普通非成品仓库格及其指派、真实数量仍逐项保留。
4. 不同代表的成品行可以在键中消失，而非成品物理守卫保持相同。

独立稿给出的“全部出口仅两矿”是一组更窄的充分条件。正文允许其它非成品仓库格被指派仍可成立：那些格没有被抽象，键逐项保留其可观察内容。正文以O为空收窄更广的未指派匿名格对称论证，也不构成缺陷；§6已明确该损失。

普通有限运行的§4.3不可混用D.3：当U至少两种、E存在但均未被取货口指派时，仍按UTF-8规范配对继续核容量；只有E中有指派才触发竞争停止。第8轮接口及覆盖修订已补齐此区别。任务书4收窄停止条件与任务书5新增生产抽象域的辖域没有互相覆盖。

### 3.3 成品容量与级二精确卡点

`q<80000`只保证可加一件。箱体同种成品可在多格累计，按§4.3全批b判断；q=79999、b=2时真实箱拒收，而生产代表q=0成功。拒收之后q仍未满，说明仅从F的未满条件无法推出逐事务容量前提。

§6.5 L217给出同一局部守卫反例，L222明确它不是完整可达布局的周期否证。L218没有把“有入库接口”误当整箱可收，L219没有用F_safe暗换原F，L220也指出平均率不能给最大突发或等待间隔。原F的级二提升因此保持unresolved，符合本轮“写不通则给精确卡点”的交付条件；不应将已承认的卡点再报成新缺陷。

## 4. §6.3–§6.4证明步骤复核

### 4.1 代表化、边界及单次访问

G每tick开始将成品取零代表，不将其记为玩家操作；D.5给本tick每物种总入量B<80000。结构界由核心几何PC每口至多一件、每箱六格各50且本tick最多传一次相加得到，故不仅第一件，而且本tick任何整批的所有成品入库都容得下。其它物种真实库存不变；含矿尝试先按D.2同停。

相等剩余工作量及开关/供电使制造和冷却同步更新；相等门elapsed使到期批事务同步，运输年龄同步增长。给相同模板，物理集合不读轮询指针，目标非成品容量保留，成品容量由代表化及B保证，故物理集合一致。相同级、接通顺序、仲裁和局部分支表导出同授权。成功、失败或不构成判定的分支一致，且失败只推进判前被授权侧。制造、传输分别保持同批、同资格、同冷却和同事务入库量。

这些步骤分别对应§6.3 L193–197；非仓库库存和物流守卫没有依赖被删除变量的遗漏。证明限本版明确组合，不能升格成对所有读法的正式规则定理。

### 4.2 生产键相等不足以直接判同刻闭包，但正文没有这样做

生产键删除仓库量，同刻closure_key仍保留真实仓库和O。必须证明双方首次完整闭包重复的轮号相同，才能得到相同after_closure续接状态。

在一次G内，两矿各自数量恒定；成品无出口且无玩家拿取，只增不减，历史身份只会首次建立。若一侧两次扫描边界的具体closure_key相等，中间必无成品正入库或首次建立身份；另一侧入库增量相同，也无正入库。本版身份只在成功入库时首次建立，因此另一侧也无身份新增；不要求两代表原来就有相同的成品历史格。其它键分量在单次访问归纳中保持对应，矿常量差及成品标签差不影响各自内部的“前后相等”判断。因此两侧重复边界的关系一致，首次重复的轮数一致。§6.3 L198给出此论证，未用跨时刻键替换同刻闭包键。

### 4.3 周期长度、接收域与平均率

若a、b均为真实完成的G闭包状态、b>a、期间D动态前件都通过，键相等则逐tick归纳可重复(a,b]的生产状态与入库标签。P=b−a不变，逐物种周期入库为核心PC与箱无线实际入量之和，率为D_i/P。代表调整、矿补给和玩家拿取均不是新交付；无需额外要求每个滑动30tick窗口达标。

输出§2首项区分before_boundary的t与after_closure的t+1，§5.2周期账恰取(a,b]，§5.3从起点重跑P次并比完整状态、事件和账。这与独立推导一致，未把起点已执行事件再计一次。接收域仍按指定读法逐边界核，不由键相等或入库平均率代替。

## 5. 脚本、产物与实际验证范围

本席实际执行：

```bash
python -B 求解器/规格/check_revision.py
python -B 求解器/规格/第五轮规格修订/check_round5.py --output-dir 求解器/规格/复核/r9-独立推演-证据
python -B 求解器/规格/复核/r9-独立推演-证据/check_independent.py
```

| 项目 | 当前结果 | 证据与限制 |
|---|---|---|
| 规格自查、声明/JSON/输入轴投影 | PASS，99轴 | [规格自查重跑.log](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/r9-独立推演-证据/规格自查重跑.log)；覆盖定位及一致性，不证明普遍语义 |
| 原第五轮自查 | PASS，183项 | [自查结果.json](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/r9-独立推演-证据/自查结果.json)；字段定位、schema试样及局部算术 |
| 本席局部复算 | PASS，29项 | [独立局部复算.json](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/r9-独立推演-证据/独立局部复算.json)；让年龄、门起点、制造及窗口pending同时非空，共同平移1/5/31/10^18，并核11种保留字段变化；试样不是合法运行种子 |
| 第8轮交付来源 | 35份哈希一致 | [复核验证结果.json](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/r9-独立推演-证据/复核验证结果.json) |
| 第8轮60组停止前件、170个本地链接 | 无错误、无缺失 | 同上；是现有产物核对，不能冒称重跑全部22项入口 |
| 本轮版本稳定 | 开工登记40份均未变化 | 同上；仅按登记文件核字节 |

`check_round8.py`会写回被审目录并运行schema生成器，因此没有直接运行该入口。已读取完整脚本、现有22项结果及生成日志，核其范围表述；没有为重跑而写被审文件。没有运行cargo、Rust内核场景、性能基准或独立周期证书回放；K线实测结论不在本席认证范围。

所有新文件在`求解器/规格/复核/`，仅为脚本、日志、JSON、Markdown。未改被复核文件、正式三文件或候选约束，未操作模拟器，未创建编译缓存或整仓快照，未commit/push。

## 6. 未决事项与最终判读

级二原F的容量前提仍不足；F_safe可执行性及其合法响应族没有证明。种子可达性、全部参数/种子/读法覆盖、无界年龄/计数的可靠有限化，以及轮询均分等动态约束的普遍相容性仍未结清。K线须按最终规格核实际调用位置、检查点保留、来源迁移和独立周期回放。

以上均在当前正文和交付说明中如实保留。独立推演与§6的差异是正文采用了更保守的准入域和更细的键，不是无依据扩大结论。未发现应以`S5-r9-L3-`登记的新阻断、重要或次要问题。


## 附录：在读取§6之前形成的独立推演全文

# 循环判等的独立推演（第9轮）

日期：2026-09-19。状态：独立推演存档；本文件形成时尚未阅读《受限转移定义》§6及第五轮规格修订的证明、自查报告。它给出之后逐项比对的基线，不表示对所有参数、初态或读法的认证。

## 1. 依据与所问命题

规则依据为仓库根目录的《明日方舟：终末地》游戏规则.txt、求解任务.txt、求解约束.txt。工程转移依据为《受限转移定义》§1–§5；状态字段依据为《内核输入》§3–§6。第五轮任务书§2.1已决定两级认证与仓库抽象；本推演核查该决定能在什么前件下执行，不重新裁定已定读法。未用候选约束或面积分档推论。

须证明的是带输出的后继一致性：同一布局、设定、参数点和接通环境中，若两个合法闭包状态的键相等，则下一时刻同为停止，或后继键相等，且同一段内逐物种入库量相等。只证明末状态相等而未证明事件物流相同，不足以保存目标平均率。

## 2. 绝对时刻逐字段处理

规则L23只读运输停留时差；L35及配方只读已完成的有效工作量；L36只读传输冷却；L64读从首件起经过的5 tick。任务L14的同刻固定性本身不推出跨时刻固定序，因而以下处理限于§2.2的global模板序。

| 字段或引用 | 独立判定 | 必要前件与后继 |
|---|---|---|
| `environment.time=t` | 不直接入键，作为其余时钟的基准 | 只比较同一整数边界相位；若有日历驱动事件，不能只删t |
| `inventory[].contents[].entered_at=e`，运输格含桥两轴 | 用年龄`t-e`；空格无年龄 | 移动守卫为年龄≥1；未移动时加1，新入格为0。精确年龄足以保真，但不自动给有限键域；阈值饱和另需证明 |
| 非运输库存的`entered_at` | 本版无额外停留守卫，可删或保守保留相对年龄 | 若另一读法启用非运输停留则须保留必要年龄，不可沿用删除规则 |
| `progress.remaining`、`cooldowns[].remaining` | 已是剩余时长，原样保留 | 暂停时保持、运行时递减；不能用预计完成日历时刻代替remaining，因为规则L20要求进度保留 |
| `gate_counters.window_started_at=w` | null保持null，非空变`t-w` | 在5 tick阈值触发维护，关闭窗口置null，下一件重新置年龄0；计数及各阻断原因必须保留 |
| `judgment_context.instant` | 归零 | 比较点固定为after_closure；中途状态还需保存真实扫描游标与seen记忆，不能挪用边界键 |
| `judgment_context.round/next_template/ordered_events/next_event` | 只有在已闭包、下一刻重新扫描且不再被任何活动引用读取时可省略已消费部分 | 它们不都是时刻字段；若恢复in_closure，直接删除会改变首次重复位置和失败轮询后效 |
| `tick_context.window_start/window_end` | 相对`t`的两个偏移 | 闭包后的本刻额度下一边界重置；若省略必须明确只对已闭包比较，且没有跨边界依赖 |
| `tick_context.movements/internal_passages`里的event和batch | 已消费纯审计可不入键；活动引用必须保留关联 | 字符串中含t不是删除整条记录的理由；不能漏掉未消费依赖 |
| `pending_events[].trigger=at_time(d)` | 活动截止用`d-t` | 时间推进后余量减1；制造预计时刻若仅审计，则守卫仍以remaining为准。外部有限历史截止另见下项 |
| `pending_events[].event/predecessors/after_event` | 对活动事件图作一致重命名 | 保留operation、target、trigger、status、先后关系及同一事件的共享身份；不可只把字符串中的数字抹掉造成不同事件合并 |
| 显式矿石历史`events[].time`及`through` | 不能在固定输入历史下任意移除或反复循环使用 | 余下整个事件日程和有限终止边界是可观察环境；用sufficient并排除外部后继事件才可免除此状态 |
| per_instant排序的`origin/instant`及函数中的时刻常量 | 一般不能删除 | 周期函数至少要保留相位；任意非周期函数无可靠周期归一保证。本版global排除它们 |
| 建成/接通/重建/恢复历史中的时刻 | 本段只保留能导出排序的全预序、并列及固定仲裁；不必保留距当前时刻的年龄 | 规则L28–L32只比较接通先后；§3.4恢复沿用原建造记录。若改为按恢复时刻重排或有离线则须重审 |
| `settings_anchor/layout_snapshot`和构造/调试事件id | 固定上下文保留引用及其解释，不对每个已过去事件计年龄 | 当前设定、几何、桥方向不可删除；同键比较限相同上下文 |
| `withdrawal_memory`中的已执行及待响应事件 | 无拿取运行段为空；在真实拿取环境下不能擅自清除策略记忆 | 级二投影一致性可对每个已给外部日程作条件归纳，不能据此把策略改成即时补空机制 |
| 输出日志时间、完成审计身份、历史累计统计 | 不参与物理守卫者可排除，证据记录仍保存 | 不能将累计交付差直接用作忽略拿取后的仓库库存差 |

非时刻状态仍须保留：全部非仓库物种与数量、制造阶段及锁配方、箱体冷却、门累计与窗口计数/原因、实际边、轮询各级成员/续接位置/当前级、分级与阻尼表、输入仲裁及各参数。它们均可进入下一次物理判断或调度。

## 3. 两矿存量被抽象后尚存的读点

1. §4.1核心及仓库取货口先按指派格找物种与数量；§3.1据此判物理可动。sufficient使每矿唯一正库存格在出矿事务中扣1补1，因此这里数量仅经“至少1”被读，身份及指派仍须保存。规则L13/L41限定单格及容量，任务L7只授权供应充足。
2. §4.1对任何PC回矿入库检查`q+1≤80000`；§4.3对整箱矿物汇总检查`q+b≤80000`。这两处直接读取q，即使实际拒收、从未成功回矿，也会改变运输源是否清空、箱冷却/库存及后续轮询。因此“实际矿入库账为0”不足以证明独立。
3. 反例算术：同一运输源有一件蓝铁矿，目标核心格分别有79999和80000；源已滞留、额度未用。忽略矿量后的生产键相同，前者可收、后者不可收。若箱含两件矿，79998与79999同样给出成功/失败。必须在容量读发生之前统一停止，或在进入抽象前静态排除所有回矿路径；不得先以矿容量算授权再检查。
4. 正式约束“矿系不入库”说明达标循环的必要后果，不能直接把尚未证明达标的候选轨迹中所有回矿尝试删除。停止是本版覆盖界限，不能作为轨迹合法性的反证。

## 4. 两成品存量、标签与空格序

### 4.1 数量与容量

核心PC每次入库一件，但整箱传输按物种总量b原子检查，规则L41容量固定80000。因此“q<80000”只保证下一件可收，不保证整箱b可收。若q=79999、b=2，批量拒收且q仍<80000；玩家拿取0件也不改变这一事实。故对满足“库存始终未满”的所有日程直接归纳生产轨迹一致，必须另给可观察接收域或逐批容量余量条件。平均0.6不限制单个时刻的入库突发量，也不能保证某个拿取间隔不会碰边界。

### 4.2 取空后的标签传播

retain_history读法中，清空将item转到empty_identity，不重新进入匿名空格序O；同种下一次入库仍落原格。因此不会因取空本身改变O。其他读法若释放身份，将该格重新并入O，其顺序会影响以后新物种落在哪个格。

有端口指派的格不能任意改名或删除：规则L41/L73的取货读取指定格，标签重分配可以改变输出物种，继而改变准入口身份阻断、通道图、阻尼、轮询和制造。即使当前该格为空，只要之后能被新物种占用就可能可观察。无指派的匿名格则可通过一致置换解释；其空格序、引用与新格标签都须同时对应，不能只排序仓库数组。

足够的隔离条件是全部取货指派只指向两矿的永久格，成品和匿名格均无指派；保留其它物种的真实库存与容量，并按物种表示其无指派格身份。在该域中，成品标签及消耗匿名格导致的序变化不能被物流取货读到。但仍须逐次保证其它物种的单格和容量守卫相同，并处理规范新标签碰撞等编码拒收；不能让重命名把非法编码变成合法编码而宣称相同后继。

### 4.3 接收域与级二范围

任务L2“仓库同时可以接受两成品”与L8拿取例外不直接给出每批必收。对F中任一日程的证明须明确“可接受”是逐件余量还是整批可收，并保证该条件在整段真实运行中成立。加强为每次尝试前每个物种q+b≤80000可支持条件归纳，但这是更小日程族，不能冒称已覆盖原F。原F证明失败时，级一只能自称生产部分周期。

## 5. 后继与平均率的独立证明骨架

比较同参数闭包锚点，先核上述环境和仓库隔离前件。边界中的remaining、冷却、门年龄按相同相对量更新；完成只转换同一批原料；窗口批事务删除相同原因并产生同一图。按同一模板序归纳：源货、目标容量、年龄及门守卫相同，授权相同，成功/失败相同，轮询后效相同，制造与传输的整批数量相同。若任何尝试触及被删除的矿量或未经保证的成品容量，应在读取前同停，不继续归纳。

于是每轮物理状态及轮询投影保持对应，同刻首次重复位置一致，后继键相等。保留每次核心PC入库及箱无线入库的物种/件数，得到周期内入库向量D相同；周期长度P未变，平均率D/P相同。日志时刻和格标签可重命名，真实入库量不能以代表仓库存量差替代。

这一骨架只证明固定支持域的转移和奖励保持；没有证明每条轨迹可从蓝图及不精确定时调试到达，没有证明非周期轨迹不存在，没有证明所有种子、参数和相遇读法都达标，也没有给出仓库字面完整状态周期。

## 6. 与后续对照的关系

上述推演是§6审阅之前的固定基线。实际逐项对照、成立问题与已明确登记的卡点分开记在[复核-r9-独立推演.md](/home/zhuran24/zmd-research-fresh/求解器/规格/复核/复核-r9-独立推演.md)，不以已被正文承认的未证义务凑新发现。

## 最终 JSON 结果

```json
{
  "findings": [],
  "notes": "独立推演与逐项复核已写入 /home/zhuran24/zmd-research-fresh/求解器/规格/复核/判等独立推演-r9.md 和 /home/zhuran24/zmd-research-fresh/求解器/规格/复核/复核-r9-独立推演.md；完整转发稿见 /home/zhuran24/zmd-research-fresh/求解器/规格/复核/r9-独立推演-证据/reply.md。D域内未发现新问题；级二F提升和全称覆盖仍未获证，本席未执行K线运行验证。"
}
```
