# 第四轮前置任务验证报告

日期：2026-09-19。性质：本席执行与验证存档。结论：§3第2–5项已落实；第1项的挪位、重算、实例登记及批评前提修订已做，但“挪位即可使两读法PC集合相同”的要求有几何反证，不能记全部任务完成。该未决独立于全部检查通过的事实。

## 1. 阅读、保护与任务边界

依指定顺序完整读任务书4、任务书3、任务书、任务书2；按任务书定位回读三份正式文件、当前运行语义/受限转移/轴表/受限声明/输入输出接口、批评-2/3及样例、生成器、参考运行和验证入口。任务书3§0的分工调整延后没有取消任何工作。本次只负责任务书4§3，未进入§4 Rust内核实现，也未另起复核席。

所有产物在求解器内；未修改三份正式文件、候选约束、模拟器；未commit/push。开工源及主要可编辑产物指纹见[开工指纹](开工指纹.json)，收尾逐项重读核验在[交付清单](交付清单.json)。仓库已有未提交内容未作清理或回滚。

## 2. 五项处置与论证

| 任务 | 结果与主要落点 |
|---|---|
| 1. 三例挪位、重算、同PC | 桥布局东移，分流布局南移并接真实供料，grinder_a西移改接边端；三个输入、接通历史及投影重新生成。粉碎机前四刻仍逐字段等于原手工期望，完成两批。两谓词完整集合机械枚举；连续同类端口的相邻闭段必共享端点，保留实例用途时无法仅挪位消除差异。详见R4-Q1，未伪报共同轨迹。批评-2§7.4已补前提。 |
| 2. checkpoint_delta | 输出正文/schema、checkpoint_delta.py给检查点间隔、规范对象替换增量、局部形状与跨段验收；保留原全状态格式。两例生成两种格式。检查点自身、尾段、事件和重建StateSeed都与独立完整重算比较，篡改不能由后续检查点遮掉。 |
| 3. 仓库停止边界 | 仅在len(U)≥2且E中至少一格被warehouse_assignments[].slot引用时停。未指派格以UTF-8物种序配O(S)首部，稳定维护空格序；无改指派/拿取/离线、retain_history的条件对称性论证在受限转移§4.3，引用规则L13/L41/L73及任务L8。输入§5、配置、模型、轴表、T12和义务对照同步。 |
| 4. 两覆盖表互指 | 两文件抬头互链，分别说明逐行运行语义去向与目录/契约静态门禁职责。 |
| 5. 分流器运行 | v3、99轴、完整StateSeed、真实仓库进料、单成员与三成员分流环、双直连分流器汇流平局、两门同刻到期。五条指定轴全部exercised；级序交换改变胜者，两门t=6同批恢复。完整推演见分流器运行说明。 |

代码改动过程中定位到级仲裁表定义域歧义，已明确为布局全部几何可能级标签的全序，暂空级保留仲裁标签，活跃级才参与比较；仓库空格序仍是随状态稳定删项的当前序。理由及落点见R4-Q2，不把新增表示解释成新游戏机制。

## 3. 复现命令与通过结果

以下在`/home/zhuran24/zmd-research-fresh/求解器`运行；执行次序确保记录指纹绑定最后一版正文、代码、输入和参数来源。最终退出码、原始输出及产物SHA-256见交付清单；检查通过仅有下列适用范围。

| 命令 | 结果 |
|---|---|
| `python -B 数据/样例/generate_examples.py` | 1个v2静态、2个v3完整运行输入通过；生成器可复现输入与投影。 |
| `python -B 数据/样例/check_examples.py --self-test --report 数据/样例/检查结果.json` | 三例通过，99轴，19个拒收负例与8个表示回归通过。 |
| `python -B 数据/样例/check_golden_trace.py` | golden_match=true，4时刻，2完整制造周期，事件数28/42/43/43。 |
| `python -B 数据/样例/test_runtime_input.py` | 原72项运行输入、身份、黄金、输出schema与篡改回归通过。 |
| `python -B 规格/check_revision.py` | PASS，99轴逐项及接口一致，规则114行、任务15行、正式约束56条回源通过；非语义完备性证明。 |
| `cargo test --locked --offline` | 1项库测试＋30项集成测试通过，0失败；doc-tests为0项。 |
| `python -B 数据/样例/check_splitter_trace.py` | 12时刻完整及checkpoint_delta记录通过，t=11仓库79974，普通格26件；事件数36/54/54/72/72/72/74/72/72/72/72/72。 |
| `python -B 数据/样例/test_round4.py` | 新增74项回归通过：手工递推、收支、真正级序交换、两门批恢复、显式不支持拒收、两例k=1/2/4/5无损重建及压缩记录篡改负例。 |
| `python -B 数据/样例/check_port_meeting.py` | 完成枚举并真实报告三例same_pc_set=false；脚本退出0表示枚举成功，绝不表示共同PC条件成立。 |

分流器前缀保持普通件数守恒；主三箱实际分配10/0/0，附属探针箱9件，两门各1，四个分流器各1，汇流器1，总26。三路不是均分，这一结果保留在产物中；本输入不满足正式轮询均分的全部前件，不能拿它直接反驳约束。未改变黄金摘要来迁就实现，新增非判定窗口事件也不混计为通道判定。

## 4. 读者视角自审与验收边界

已重读本轮正文和运行说明，核对现状标题、样例数量、单位/格/PC数、五条轴的真实事件依据、机器配置版本及保护边界；修订过程仅留修订记录/本报告。交叉引用、JSON语法、全部本轮产物路径及来源指纹在收尾核对。旧轮次报告保留历史意义，不用旧指纹证明本次运行。

R4-Q1仍未决：需要任务书修改验收前提或后续语义审查，不能由执行席擅改closed_segment_touch。其它运行域边界如无线传输、身份不符、多级取货阻尼、中途续跑以及全称达标认证均明确记录；未将有限轨迹或测试全绿冒充这些义务完成。

完整文件列表、各命令退出码和最终来源重验见[交付清单.json](交付清单.json)；新增负例明细见[回归结果.json](回归结果.json)，端口反例见[端口相遇比较.json](端口相遇比较.json)。
