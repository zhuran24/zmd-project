#!/usr/bin/env python3
"""Build the final review from independently computed, checked artifacts."""
from pathlib import Path
import json,sys,hashlib
from datetime import datetime
from zoneinfo import ZoneInfo
import scipy,ortools,numpy

BASE=Path('/home/zhuran24/zmd-research-fresh/求解器/候选约束轮次/第60-62轮/复核62')
REPORT=BASE.parent/'复核62.md'

def read(name):return json.loads((BASE/name).read_text())

def main():
    v=read('verification_summary.json');mip=read('candidate_summary.json');cp=read('cp_certification_summary.json')
    assert not v['original22_missing'] and not v['extra_vs_original22']
    assert not v['retained8_missing'] and not v['retained8_extra']
    assert v['cp_statuses']=={'INFEASIBLE':22} and not v['final_input_changes']
    assert v['witness_check'] and v['geometry_check']
    scan=read('filter_results.json');manifest=read('input_manifest.json')['inputs'];geo=read('geometry_checks.json')
    root=Path('/home/zhuran24/zmd-research-fresh');old=root/'求解器/几何/1113放松/input_snapshot'
    stamp=datetime.now(ZoneInfo('America/New_York')).isoformat(timespec='seconds')
    body=f'''# 第60轮「1113 位置」独立数值复核（复核62）

日期：{stamp}。状态：已完成。

## 结论

**结论：未否证。**

从零编写的程序独立复现了 **1800→1288→1260→1200→22** 的位置筛选，22 个位置及 40 个位置/P 分支均与原报告一致。随后对这 22 个位置分别放开 P=10、11、12 求最小 S：14 个位置的最小值大于 187，剩余 8 个位置的最小值为 179、186 或 187，与候选位置条文一致。

22 个最小值均有 HiGHS 最优解、单独编写的逐格见证检查，以及另一套 CP-SAT 编码在 `S≤最小值−1` 下返回 INFEASIBLE 的下界复核。没有用转置数值替代任何一个位置的联合模型求解。

候选条文本身无需替换。其推导说明中 **b=13 的 200 是允许第0行/列对接的弱版最小值；禁止这些对接的版本最小值是201**。异源报告表格已区分这两个值，候选推导把二者口径混写；两者都超过187，不改变位置排除结论。

本结论以 **56条快照** 为数学依据；另逐条比较了 **71条当前版**。本次模型使用的必要条件在当前版仍成立，未发现版本改动使被排除位置恢复的情况。剩余8个位置只是在这里的边界放松中未被排除，**没有证明其中任何一个存在完整达标布局**，也没有求71条全部条件下的可行性。

## 1. 依据、版本与输入边界

只从原目录读取两份说明报告和三份正式输入快照，未读取、导入或执行原目录内的模型脚本、JSON结果、投影列表或缓存。原报告中的位置及数值仅作待比对命题；所有复算见证由本席新程序产生。未读取同轮另一复核席的文件。

| 输入 | SHA-256 |
|---|---|
| 56条求解约束快照 | `{manifest[str(old/'求解约束.txt')]['sha256']}` |
| 71条当前求解约束 | `{manifest[str(root/'求解约束.txt')]['sha256']}` |
| 规则快照 | `{manifest[str(old/'《明日方舟：终末地》游戏规则.txt')]['sha256']}` |
| 当前规则 | `{manifest[str(root/'《明日方舟：终末地》游戏规则.txt')]['sha256']}` |
| 求解任务（两版相同） | `{manifest[str(root/'求解任务.txt')]['sha256']}` |

全部9份实际读取材料的哈希及字节数见 [input_manifest.json](复核62/input_manifest.json)，结束核对见 [input_manifest_final.json](复核62/input_manifest_final.json)，读取前后均未变化。版本逐条差异见 [version_comparison.json](复核62/version_comparison.json)。

## 2. S 的定义和必要放松

棋盘格坐标为0…69。矩形记为半开区域 R=[a,a+W)×[b,b+H)，(a,b) 是左下角格。

- P：全布局供电桩总数，取10…12；不是显式边界桩数。
- J：2×2桩占格含第1列、第69列、第1行或第69行的桩数；角桩仍只计一个J。
- X：第69列第1…68行与第69行第1…68列上，不属制造单位、核心、桩或R的格数；(69,69)不属桩或R时再加2。
- Y：R外与其边正交相邻、且不属制造单位、核心或桩的格数。贴棋盘边界的一侧不计不存在的邻格，不含对角格。

X和Y是按方向记账的两笔费用，同一格可以在二者中各出现一次。见证检查逐格重建这两个集合，没有把它们先取并集再计数。

56条快照的「面积预算」在A=1113时给出九机型恰为下限、无协议储存箱、P≤12；「供电下限」给P≥10。制造单位可按尺寸聚合为小机131、中机48、大机38，共217台、3291格；这217台均须有正制造量。由「内带缺口」「面积预算」：

`4×1113 + 16P − 2J + X + Y ≤ 4639`，即 **S=16P−2J+X+Y≤187**。

边带的139格由46个取货口占138格，只余单格q。机身宽至少2，不能占q；q不能承载循环正流量的一进一出，因此有效对接格不得在第0行/列。此处只排除制造单位或核心所需的正流量对接，没有把棋盘第1行/列也排除。

每个桩的亏额是23减去该桩的制造台数上界：一般边界桩亏9，双边界角桩亏15；满足侧旁投影条件、间隔g=0…6时亏额为(10,9,9,6,5,4,1)。同桩适用多个条件时取**最大亏额**，不相加。总亏额≤23P−217。覆盖区间为[p−5,p+7)×[q−5,q+7)，侧旁条件检验的是整段12格投影包含关系。

### 联合二维模型

[boundary62.py](复核62/boundary62.py) 从真实尺寸及整数坐标生成所有碰X、Y或右上角目标格的制造单位、核心和桩。小机、中机各保留两条端口轴，大机为6×4上下端口或4×6左右端口，核心保留两种取货轴。

保留逐格机身不重叠；制造单位两条端口边各有至少一个界内、R外、未被显式机身占用的对接格；核心6个取货对接格全部可用，14个存货对接格中至少2个可用。制造台数仅取上限131/48/38，核心≤1。P全开10…12，无区间下界切割。

记显式桩数为p、其中边界桩数为j，省略部分的边界桩数为h，施加p+h≤P、J=j+h、显式亏额+9h≤23P−217。省略的非边界桩不收费。未碰目标格的边界桩只能贴第1行/列；右边和上边的桩必触目标格或侵入R，因而不会漏减J。

本模型省略未触边机身、边带的实际运输占格、供电覆盖、运输数量与方向、流量和运行状态；也放宽核心离带条件。真实布局保留全部碰目标格机身后仍满足这些约束，且P、J、X、Y不变，因此这是必要条件放松。模型不可行可以排除位置；模型可行只给出边界几何见证。

[mip62.py](复核62/mip62.py) 用线性等式表示每格占用，采用另一套0-1编码和HiGHS最小化S。CP-SAT使用条件端口约束。后者按HiGHS所得整数s逐位置检查S≤s−1；HiGHS见证另经 [check62.py](复核62/check62.py) 用直接格集合、独立的端口和亏额计算核验。三者共同给出联合放松的最小值。

## 3. 1800个位置到22个位置

21×53有(70−21+1)(70−53+1)=50×18=900个位置，转置亦900个，共1800个。初始枚举包括贴左下边的非法位置，随后才逐条筛去。

| 步骤 | 本次减少 | 本次剩余 | 与原报告比较 |
|---|---:|---:|---|
| 棋盘范围内枚举两朝向 | — | 1800 | 一致 |
| 面积>1005，a,b≥4 | 512 | 1288 | 一致 |
| a=4时H≤21，及转置条件 | 28 | 1260 | 一致 |
| 对47种共同边带实际数端口，a=4或b=4时m≤6 | 60 | 1200 | 一致 |
| 独立区间整数规划下界 | 1178 | 22 | 位置集合一致 |

每边的23个3格取货口只能在坐标3k处留空，k=0…23；左边与下边至少一边的空格为(0,0)，故共同模式是24+23=47种。另从70格的一维平铺枚举验证了这47种模式，每种占138个不同格、46个不同取货对接格。

### 区间整数规划的构造

[filter62.py](复核62/filter62.py) 沿X边段及Y四边，把单体真实占位投影成区间。只有单体机身和所需端口自身合法时才保留；核心还检查核心离带、核心邻格以及存在相容的共同边带。区间若越出侧边跨度，标为截断块；截断块间的额外邻接限制全部放宽。

对两块完整投影，禁止无空隙的M–M、M–C、C–M、C–P、P–C。M表示制造单位，C表示核心，P表示桩。理由是：机身从同一侧边向同一侧伸展，较浅制造单位的整个活跃端口边会被堵；核心旁的2格桩会堵住核心取货端口偏移1的邻格。该判断只施加于完整投影。相隔不同边段的机身相撞、端口冲突全部放宽。

每个区间模型限制核心≤1、桩数≤P、桩亏额合计≤23P−217。X对角桩单独分支，两条边只计一次桩数/亏额，角格费用仍按2计；固定角桩必须不侵入R。Y中一台机身至多碰一侧：同时碰相邻侧会侵入R角格，而最长9格的机身跨不过21格短边。X中只有角桩可能跨两边。故没有重复扣资源导致的错误收紧。

X、Y各借用完整一套核心与桩资源，分别求最小值再相加。这会放宽真实共同布局。P=10/11/12时由J≤1/4/6，X+Y允许量分别为29/19/7，只有各P分支均超过允许量时才排除位置。

实现使用0-1区间覆盖、相邻完整块不兼容约束和资源预算，**没有实现或调用原DP转移**。等价区间实例的复用以完整区间候选、长度、固定格及资源预算规范化后相同为条件。共实际求解并保存{v['distinct_interval_models']}个不同整数区间模型，其整数见证和数值最优界全部重新核对。

| 朝向 | P=10 | P=11 | P=12 |
|---|---|---|---|
| 21×53 | a=49，b=5…14、17 | a=49，b=6…12、17 | (49,17) |
| 53×21 | b=49，a=5…14、17 | b=49，a=6…12、17 | (17,49) |

共22个不同位置、40个位置/P分支。逐位置依据见 [filter_results.json](复核62/filter_results.json)，每个区间模型的候选、预算、最优见证及界见 [interval_cache.jsonl](复核62/interval_cache.jsonl)。与原22个位置相比，新增集合和缺失集合均为空。

以下是21×53的独立X/Y下界，表中为“minX+minY”，不是联合最小值；转置一致。

| b（a=49） | P=10，允许29 | P=11，允许19 | P=12，允许7 |
|---:|---:|---:|---:|
'''
    for r in scan['survivors']:
        if r['R'][2]!=21:continue
        body+='| '+str(r['R'][1])+' | '+' | '.join(f"{x['X']}+{x['Y']}" for x in r['bounds'])+' |\n'
    body+='''
## 4. 14个排除位置逐个复算

下表每行都是一次独立的联合模型最小化，P=10…12全部开放。P、J、X、Y列给出本次得到的一份最优见证；同一位置可能有X/Y分配不同但S相同的其他最优见证。CP-SAT列是与该最小值配套的不可行性结果，故没有把有限时的UNKNOWN作为证明。

| 朝向 | 左下角 | P | J | X | Y | 最小S | 超过187 | CP-SAT检查 |
|---|---|---:|---:|---:|---:|---:|---:|---|
'''
    for r in mip:
        if r['S']<=187:continue
        a,b,W,H=r['R'];s=r['S']
        body+=f"| {W}×{H} | ({a},{b}) | {r['P']} | {r['J']} | {r['X']} | {r['Y']} | {s} | {s-187} | S≤{s-1}：INFEASIBLE |\n"
    body+='''
14个位置全部确认可以排除。两个朝向逐位置的HiGHS见证见 `复核62/mip_a_b.json`，相应CP-SAT回执及完整日志见 `复核62/cp_below_min_a_b.json/.log`；a、b替换为表中坐标。汇总见 [candidate_summary.json](复核62/candidate_summary.json) 与 [cp_certification_summary.json](复核62/cp_certification_summary.json)。

## 5. 剩余8个位置的核验

| 朝向 | 左下角 | P | J | X | Y | 最小S | 距187余量 | CP-SAT检查 |
|---|---|---:|---:|---:|---:|---:|---:|---|
'''
    for r in mip:
        if r['S']>187:continue
        a,b,W,H=r['R'];s=r['S']
        body+=f"| {W}×{H} | ({a},{b}) | {r['P']} | {r['J']} | {r['X']} | {r['Y']} | {s} | {187-s} | S≤{s-1}：INFEASIBLE |\n"
    body+='''
8份见证均通过机身不重叠、边界与R避让、制造双边对接、核心6取货/至少2存货对接、桩数及亏额预算、X/Y重计的逐格检查，确实满足本模型的S≤187。其显式机身表保存在各自JSON的`chosen`字段，对接空格见 [witness_checks.json](复核62/witness_checks.json)。

这些见证省略了未碰目标格的机器及全部工厂运行条件，不能证明217台机器可完整摆下、获得供电、布线并达到目标。第2d步在“与异源报告可比的边界放松”层面全部完成；完整布局可行性仍未解决。本轮未尝试用71条新条件进一步排除这8个位置。

## 6. 差异追查

### b=13：200与201来自两种端口口径

在同一套新程序中，只改变“第0行/列是否可作为端口邻格”一个开关，另按P逐分支优化：

| 对接口径 | P=10 | P=11 | P=12 | P全开最小值 |
|---|---:|---:|---:|---:|
'''
    weak=[read(f'weak_49_13_P{p}.json')['S'] for p in (10,11,12)]
    strict=[read(f'strict_49_13_P{p}.json')['S'] for p in (10,11,12)]
    body+='| 允许第0行/列，弱版 | '+' | '.join(map(str,weak))+f" | {read('weak_49_13_PNone.json')['S']} |\n"
    body+='| 禁止第0行/列，有效正流量口径 | '+' | '.join(map(str,strict))+f" | {min(strict)} |\n"
    body+='''
200/204/213恰好复现异源报告弱版数据，201复现其edge0表格。故不是坐标转置错误，也不是56→71条变化；候选推导中将“端口不在第0行/列”和“b=13最小200”并写，是把弱版数值放进严格版本的叙述。应把该处注释写为“严格版本201；放宽第0行/列时200”，或明确200仅是足以排除的较弱下界。位置条文本身不需改动。

其余排除位置、剩余8个位置、初筛数量及40个分支均未出现不一致。本席得到的某些最优见证X/Y分拆与原报告例子不同，是不同最优解；S及所用定义一致，不能把某份见证的X单独误当全局minX。

### 56条与71条的影响

逐条比对确认新增15条、修改3条，求解任务不变。新增的“增机面积界”针对粉碎69台、采种17台，A=1113已经要求68台和16台，因此该新增分支不影响本次位置排除。其余14条涉及库存、流量、轮询、批次或植物运行条件；本次放松未使用它们，不能据此宣称剩余8个位置也满足这些新条件。

“密集结点”的必然变化改为可能变化、“来源定序”的余量条件修订，都未进入位置模型。“矿系不入库”增加满仓物品经箱物理中转的例外；A=1113无箱，本次也未以物品身份限制核心端口几何，故不影响此排除链。规则中“开关”措辞也有变化，但未进入几何模型。

当前版的机型下限、面积预算、供电下限、侧旁供电、矩形离带、矿石走廊、边带排布、核心离带/邻格、内带缺口等直接依据保持原文。因此同一必要位置限制适用于当前版；这里没有把“新条件可能进一步收紧”误表述为“两个版本完整可行集相同”。

## 7. 校验、复现与执行记录

独立核查结果见 [verification_summary.json](复核62/verification_summary.json)：

'''
    body+=f'''- 完整枚举1800个不同位置，22个保留位置与原报告集合相同，40个P分支相同。
- {v['distinct_interval_models']}个区间整数模型的见证、资源预算、相邻规则及最优界通过回读核验。
- 22个联合模型最小值均由HiGHS给出最优解；22次CP-SAT的严格小一界均INFEASIBLE。
- 29份联合见证通过独立逐格检查（22个位置及b=13的7个口径/分支对照）。
- 30个测试位置穷举{geo['total_anchor_attempts']:,}个机身锚点，与目标格反推枚举的合法候选零差异；{geo['total_forbidden_pairs']:,}对完整相邻块的禁止关系由真实机身/端口碰撞逐对确认。
- 9份读取材料的结束哈希与开始时一致。

环境：Python {sys.version.split()[0]}、OR-Tools {ortools.__version__}、SciPy {scipy.__version__}（HiGHS）、NumPy {numpy.__version__}。HiGHS最优界按1e−6核对整数值；一项相对gap为约1.07×10⁻¹⁴，属浮点余量，其下界188与整数见证188一致，且CP-SAT独立排除了S≤187。这里交付的是程序、最优见证和求解器下界回执，不是LRAT等形式证明文件。

22个联合HiGHS求解时间合计{sum(r['solve_seconds'] for r in mip):.3f}秒；22个CP-SAT下界复核合计{sum(r['solve_seconds'] for r in cp):.3f}秒，不含建模及前置枚举。完整日志与每次时间留在对应JSON/日志中。

### 复现命令

以下命令顺序执行；均使用绝对路径。首条可复用本席保存的区间最优缓存；要从零复算，可先把`interval_cache.jsonl`移动到本席子目录内的另一个备份文件名。候选和CP批处理会复用本席已完成回执，单位置命令可指定新的`--name`独立重跑。

```bash
python -B {BASE}/filter62.py
python -B {BASE}/run_candidates62.py
python -B {BASE}/cp_certify62.py
python -B {BASE}/check62.py
python -B {BASE}/geometry_audit62.py
python -B {BASE}/check_results62.py
```

独立重跑某个位置的最小值及对应不可行界：

```bash
python -B {BASE}/mip62.py 49 5 --seconds 600 --name repeat_mip_49_5
python -B {BASE}/boundary62.py 49 5 --cap 187 --workers 1 --seconds 300 --name repeat_cp_49_5
```

### 执行范围与线程例外

未执行git操作，未修改正式文件、候选约束或1113放松目录，未编译；新产物仅在本报告及`复核62/`。所有自写Python命令使用`-B`避免写入字节码缓存。

**有一次线程预算违规，不能声称全程满足≤4。** 最初4-worker CP-SAT探索尚未退出时启动了一个单线程HiGHS，约在2026-09-22 18:00:52至18:00:56（America/New_York）有最多5个求解worker的短暂重叠。该CP探索随后中止，留下`joint_49_13.json/.log`（状态FEASIBLE，仅探索记录，不用作最优性依据）。后续候选最优值、全部区间筛选及CP下界复核均采用单实例1线程，各批计算并发合计不超过4；最高同时为区间求解、CP下界复核、单线程几何检查三项。最初HiGHS结果也由后续独立CP下界和逐格检查核验。线程例外是执行合规缺陷，不改变已复算的数学数值。
'''
    REPORT.write_text(body)
    print(str(REPORT))

if __name__=='__main__':main()
