#!/usr/bin/env python3
"""三审第 86—87 轮修正版：改候选充分条件.txt 里 4 份的状态行，并在候选约束.txt 进度段后加一段。只能跑一次。"""
from pathlib import Path

HERE = Path(__file__).resolve().parent
REPO = HERE.parents[2]
OLD_SAME = ('    状态：第 86 轮 codex、第 87 轮 opus 复核未否证，待主会话三审。同一原候选的另一份修正版已于 09-26 三审采纳为'
            '「{}」，三审时只看这份有没有多出的内容。')
OLD_PLAIN = '    状态：第 86 轮 codex、第 87 轮 opus 复核未否证，待主会话三审。'
PRE = '第 86 轮 codex、第 87 轮 opus 复核未否证；主会话三审（2026-09-26）：'

S_STATUS = {
    '纯料通道与单主料研磨机不入停机态（第 84 轮修正版，改自第 83 轮修正版）：': (
        OLD_SAME.format('双料机器各原料专用进路'),
        PRE + '与已采纳的「双料机器各原料专用进路」逐句同义（固定双料配方，初存与此后进料只有这两种原料，'
        '每种原料一条存货通道、上一个运输物品格只空着或放这种原料，结论与不管的范围都相同），没有多出的内容，不单独收。'),
    '纯料通道与单主料研磨机不入停机态（第 85 轮修正版，改自第 83 轮修正版）：': (
        OLD_SAME.format('双料机器各原料专用进路'),
        PRE + '已被「双料机器各原料专用进路」覆盖，不单独收：「它要做的每个配方」在现行三种大制造单位下只能是一个配方'
        '（封装机、灌装机各一个，研磨机限一种主料后只剩一个）；（一）要上一个运输单位只放这种原料，比已采纳条文只管这个运输物品格更强；'
        '多出的只有「满足（一）的一个办法」（逆着通道追到的运输物品格调试期结束时只有这种原料、追到的来源此后只送这种原料），'
        '这是「物品只经通道移动」的逐次归纳，已采纳条文的前提照字面就这样核，不另写。三审穷举五个两料配方各 2601 种存货：'
        '两条来路首件都在且都收不下时必够开一批，死锁 0 个。'),
    '一批k件配k条取货通道的均分（第 85 轮修正版，改自第 82 轮修正版）：': (
        OLD_SAME.format('整批k件配k条取货通道的均分'),
        PRE + '已被「整批k件配k条取货通道的均分」覆盖（前提逐项相同，都按任意相位写），不单独收；'
        '第 87 轮指出的「段首件数」要按段首时刻的判定之前读，已补进已采纳条文（三审随机模拟 8 万次：按判定之前读 0 违例，'
        '按段首时刻判定全部完成后读有违例）。'),
    '采种双出口专线单元的种群下界（第 85 轮修正版，改自第 82 轮修正版）：': (
        OLD_PLAIN,
        PRE + '通过，已采纳，写进求解充分条件「采种单元的回路存量下界」：任意相位的证明逐步核过（卡住时 Φ≥L1+L2+176.5、'
        '不卡住的一段里 B 至多比 A 多取 1 粒，都只用滞留、同一时刻做到没有可动为止与配方时长，没有用整数时刻或共同相位）；'
        '前提只留下界用到的（C、A 一直有电开着，CA、AC 与 A、C 各格和缓存格只有本线物品），B、K 与下游怎样运行都行，'
        '起点可取调试期结束或其后任一时刻；「全厂专用进路接法达标」的采种单元与 Φ 改为引用这一条。'
        '三审小容量全状态图 20 组、容量 50 随机相位模拟 1.8 万次，违例 0。'),
}


def patch(path, table):
    lines = path.read_text().split('\n')
    done = set()
    for i, line in enumerate(lines):
        for head, (old, status) in table.items():
            if line.startswith(head):
                j = i + 1
                while lines[j].startswith('    ') and not lines[j].startswith('    状态：'):
                    j += 1
                assert lines[j] == old, (head, lines[j])
                lines[j] = '    状态：' + status
                assert head not in done
                done.add(head)
    assert done == set(table), set(table) - done
    return '\n'.join(lines)


def main():
    s = REPO / '候选充分条件.txt'
    s.write_text(patch(s, S_STATUS))
    c = REPO / '候选约束.txt'
    text = c.read_text()
    anchor = next(l for l in text.split('\n') if l.startswith('进度（2026-09-26，第 86—87 轮）：'))
    assert text.count(anchor) == 1
    para = ('进度（2026-09-26，第 86—87 轮修正版三审）：主会话三审第 86 轮 codex、第 87 轮 opus 两轮都未否证的 4 份修正版'
            '（都在 候选充分条件.txt），否证 0 份。「采种双出口专线单元的种群下界」（第 85 轮修正版，改自第 82 轮修正版）通过，'
            '写成求解充分条件新条「采种单元的回路存量下界」：Φ≥min(Φ(s)−1/2, L1+L2+176) 对各单位事件时刻的任意相位成立，'
            '证明逐步核过，没有用整数时刻或共同相位；前提只留 C、A 一直有电开着和 CA、AC、A、C 各格与缓存格物品都对，'
            'B、K 与下游怎样运行都行，起点可取调试期结束或其后任一时刻；「全厂专用进路接法达标」的采种单元与 Φ 改为引用这一条'
            '（该条其余部分仍带共同相位前提）。另三份与已采纳条目是同一件事，不单独收：「纯料通道与单主料研磨机不入停机态」'
            '第 84、85 轮两份已被「双料机器各原料专用进路」覆盖（第 85 轮版多出的「满足（一）的一个办法」是逐次归纳，不另写）；'
            '「一批k件配k条取货通道的均分」第 85 轮版已被「整批k件配k条取货通道的均分」覆盖，只把第 87 轮指出的读法'
            '（段首件数取段首时刻的判定之前）补进已采纳条文。求解充分条件 9 条变 10 条；求解约束未改，内核不用同步。'
            '三审报告、自写复算程序与输出在 求解器/候选约束轮次/三审-第86-87轮/。种群下界的第 86 轮两份修正版（第 88、89 轮复核中）'
            '与这次采纳的是同一件事，复核完后三审只看有没有多出的内容。')
    text = text.replace(anchor, anchor + '\n\n' + para, 1)
    c.write_text(text)


if __name__ == '__main__':
    main()
