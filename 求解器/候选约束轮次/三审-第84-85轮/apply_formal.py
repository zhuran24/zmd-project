#!/usr/bin/env python3
"""三审第 84—85 轮修正版：把通过的条目写进 求解约束.txt、求解充分条件.txt。

每处替换都断言原文恰出现一次；只能跑一次（原文换掉后再跑会断言失败）。
"""
import hashlib
from pathlib import Path

HERE = Path(__file__).resolve().parent
REPO = HERE.parents[2]
C = REPO / '求解约束.txt'
S = REPO / '求解充分条件.txt'
C_SHA = '4813a423fca1d155c22bd99ca2601ef5f0ece0e0e8d52d3369173bbe7adc0304'
S_SHA_LINES = 10


def once(text, old, new):
    assert text.count(old) == 1, (old[:40], text.count(old))
    return text.replace(old, new)


def main():
    assert hashlib.sha256(C.read_bytes()).hexdigest() == C_SHA
    c = C.read_text()

    # 1. 存货误料停机与可用台数：并进停机态合计与可用台数（第 82 轮修正版）
    old_body = ('每个可到达循环态中，扣除这类研磨机与存货格有误料的研磨机（按并集）后仍须至少 32 台。\n'
                '    据：物品格、存货/取货端口、存货/取货物品格、缓存格、制造、小制造单位、中制造单位、大制造单位、配方、零干预运行、调试期、物料流量、机型下限、循环态\n')
    new_body = ('每个可到达循环态中，扣除这类研磨机与存货格有误料的研磨机（按并集）后仍须至少 32 台。'
                '处于「存货首件死锁」的制造单位也不能再开始新批次；取货物品格里是物品 X（或取货物品格空着、缓存格里是做好的一批 X），'
                '而它的取货端口凡与别的单位的存货端口相遇，所遇的都是此后一直拒收 X 的物品准入口（设了放行种类且不是 X，或累计收下上限已用尽）时，'
                '它至多再做几批 X 把取货物品格装满，此后也不能开批。扣除这四类（按并集）后，调试期后的每个可到达状态（不只循环态）中'
                '九种机型仍须分别至少 68、51、32、6、6、32、16、3、3 台：从这个状态起玩家按时拿取成品、不再离线，会进入仓库一直收两种成品的'
                '可到达循环态，这四类机器在其中没有批次；台数恰为机型下限的机型，调试期后一台都不能进入这四类。'
                '零干预期间研磨机进入两种主料占格只有一种走法：一格放着一种主料、另一格空着时，另一种主料进了空格'
                '（这时缓存格里可以还有一批含砂叶粉末的在制品）。\n'
                '    据：物品格、存货/取货端口、存货/取货物品格、缓存格、制造、小制造单位、中制造单位、大制造单位、配方、物品准入口、'
                '零干预运行、调试期、拿取成品、离线、判定次序、物料流量、机型下限、存货首件死锁、循环态\n')
    # 2. 新条 存货首件死锁（接在其后）：第 82、83 轮两份修正版合成
    dead = ('存货首件死锁：零干预期间，一台制造单位 M 的存货物品格凑不齐它任何配方的一批，M 至少有一条存货通道，'
            '每条存货通道上 M 的上一个运输物品格都放着一件 M 此刻收不下的物品（称首件；收不下指存货物品格里已有这种物品且满 50，'
            '或没有这种物品而全部存货物品格都有货），并且这些运输单位都不是分流器（是分流器时另两个取货端口都不与任何存货端口相遇），'
            '则 M 此后永远不能再开批；首件可以是 M 的误料。桥接器也是这样：同一轴上相接的桥接器连成一条直线，远端单位对它只供货、'
            '只收货或不相接，物品在轴上不能互相越过，所以首件要么退不走，要么轴上再不会来新件。'
            '存货物品格与首件都没有误料、研磨机两格也不是两种主料时，小、中制造单位没有这种状态，大制造单位只有两类：'
            '（一）只有一格有货且满 50，首件都是这种物品；（二）两格都有货但凑不齐一批（研磨机是格里的主料只剩 1 件，'
            '封装机是钢制零件少于 10 件或致密源石粉末少于 15 件，灌装机是钢质瓶或细磨荞花粉末少于 10 件），'
            '首件都是格里满 50 的那种，或（只有研磨机会有）另外两种主料。首件里有误料时另有：塑形机格里只有 1 件钢块、首件都是误料；'
            '大制造单位两格都有货、凑不齐一批，首件都是误料或（二）里收不下的种类\n'
            '    据：物品格、存货/取货端口、存货/取货物品格、缓存格、制造、配方、小制造单位、中制造单位、大制造单位、传送带、桥接器、'
            '物品准入口、分流器、汇流器、通道、端口对接、移动、零干预运行、离线\n')
    c = once(c, old_body, new_body + dead)

    # 3. 新条 成品停收的停批范围（接在 传输按仓库余量判定 之后）
    anchor = ('    据：传输、协议储存箱、协议核心、物品格、判定、判定次序、零干预运行、拿取成品、取货口配置、入库途径、非成品零入库、'
              '传输相位、判定先后、循环态\n')
    freeze = ('成品停收的停批范围：调试期后仓库对某种成品一直停收的一段时间里（仓库里这种成品满 80000、玩家不拿取），'
              '把仓库外各物品的件数（运输物品格、制造单位的存货与取货物品格、协议储存箱都有容量，缓存格一次只有一批）与仓库里非矿物品的件数（每种至多 80000）'
              '合起来记账，得：（一）一直停收高容谷地电池、且这期间源矿不入库时，封装、配件、研磨制致密源石粉末、粉碎源矿四个配方与源矿取货'
              '都只能再发生有限次；（二）一直停收精选荞愈胶囊时，灌装、塑形、研磨制细磨荞花粉末、粉碎荞花、荞花采种、荞花种植六个配方'
              '都只能再发生有限次；（三）两种一直都停收、且这期间两种矿石都不入库时，除粉碎机把蓝铁块做成蓝铁粉末、精炼炉把蓝铁粉末做成'
              '蓝铁块外，其余 16 个配方与两种矿石取货都只能再发生有限次。停收一直持续时，这些配方在有限时间后不再开批；一台机器的全部配方'
              '都不再开批后，它的存货物品格只进不出，有限时间后不再变化。停收途中与停收以后的状态都是可到达状态，同样要满足'
              '「存货误料停机与可用台数」；仓库恢复接收后的每个可到达循环态仍须达标\n'
              '    据：协议核心、仓库、仓库物品、外部过程、拿取成品、零干预运行、入库途径、配方、制造、缓存格、存货/取货物品格、物品格、'
              '运输单位、桥接器、协议储存箱、存货误料停机与可用台数、目标、循环态\n')
    c = once(c, anchor, anchor + freeze)
    C.write_text(c)

    # 4. 求解充分条件 5 条
    s = S.read_text()
    assert len(s.splitlines()) == S_SHA_LINES and s.endswith('\n')
    add = (
        '双料机器各原料专用进路：一台大制造单位 M 只做一个固定配方 aA＋bB（研磨机即固定一种主料与砂叶粉末），调试期结束时它的两个'
        '存货物品格里只有 A、B 或空着，此后进入它存货物品格的也只有 A、B；A、B 各有一条存货通道，这条通道上 M 的上一个运输物品格在调试期后'
        '一直只可能空着或放着这种原料。则 M 调试期后不会进入研磨机两种主料占格，也不会进入「存货首件死锁」：若各首件都在且都收不下，'
        'A、B 的格都已满 50，必够开一批。本条不管来料断不断、取货侧堵不堵，也不给批次率\n'
        '    据：物品格、存货/取货物品格、缓存格、制造、配方、大制造单位、存货误料停机与可用台数、存货首件死锁\n'
        '整批k件配k条取货通道的均分：某制造单位的取货物品格只由整批进入、每批恰 k 件同一种物品（k≥2），恰有 k 条取货通道从这个格取这种物品；'
        '每条通道的首运输物品格只从这个取货物品格收货，空着时总收下这种物品（所以首运输单位不能是另有来路的汇流器，也不能是会阻断这种物品的'
        '物品准入口）。从任一时刻起把时间每 1 tick 分成一段，称为刻；一条通道在某刻「就绪」指它的首运输物品格在这一刻里某个时点是空的。'
        '若一段连续的刻里这 k 条通道每刻都就绪，则段内任两条通道取走的件数相差至多 1；段首这个格里的件数是 k 的倍数时，段内每刻 k 条通道'
        '要么各取 1 件、要么都不取。因此在一个可到达循环态中，若这 k 条通道每刻都就绪，它们的流量相等，各为这个格出货量的 1/k。'
        '这里只用到同一时刻能完成的移动都在该时刻完成、每条通道每刻至多取 1 件、相邻两批进格至少隔 1 tick，'
        '与各单位事件时刻的相位、轮询次序、接通先后及其改变、判定先后都无关\n'
        '    据：物品格、存货/取货物品格、缓存格、制造、移动、滞留、判定、轮询、优先级、端口速率、接通先后、判定先后\n'
        '植物专机一一配对满库存不断料：对同一种植物，取 n 台只做它采种的采种机、2n 台只做它种植的种植机、n 台只做它粉碎的粉碎机：'
        '采种机的两条种子进路一一接到全部种植机，种植机的一条植株进路一一接到全部采种机和粉碎机，每台粉碎机有 k 条粉末进路（荞花 k=2，砂叶 k=3）。'
        '全部进路只由传送带和方向已定的桥接器的一轴首尾相接，互不争用物品格，这些机器没有别的通道，路上没有物品准入口和协议储存箱；'
        '机器一直有供电状态、开着。若开始时各机存货物品格里有 50 件本机原料，取货物品格里至少 50−k_v 件本机产物（采种机 k_v=2、种植机 1、'
        '粉碎机 k），各进路都满、路上全是这条进路该送的物品且都已停留至少 1 tick，缓存格空着或只有本机这个配方的一批，'
        '则此后每次开批用掉的原料都在同一时刻补回，取货物品格始终至少 50−3k_v 件；粉末进路末端的下游能收下这件、且同一条进路相邻两次取件'
        '至少隔 1 tick 时，任意停取再恢复的取件都能当即满足，离线改变接通先后也不影响。这对各单位事件时刻的任意相位成立。'
        '本条不说明这个起态怎样调出，也不说明全厂下游会有这样的取件节奏\n'
        '    据：存货/取货物品格、缓存格、制造、配方、移动、判定、滞留、桥接器、传送带、端口速率、离线、零干预运行\n'
        '专用进路下缓存格不空的传递：本条只对这样的运行成立：所涉各单位的制造开始与完成、运输物品格的收发都发生在同一个相位 θ 加整数 tick 上'
        '（规则推不出这一点，须对所考察的运行另证），同一时刻能完成的移动都在该时刻完成。专用进路指只运一种物品、由传送带、桥接器的一轴'
        '或放行种类就是这种物品且不设收下上限的物品准入口首尾相接、每个运输物品格只从前一格（或起点单位）收货只往后一格（或终点单位）送货、'
        '路上没有协议储存箱的一串运输单位。在这样一个可到达循环态中，设制造单位 X 只做一个配方，一直有供电状态、开着，'
        '存货物品格、取货物品格和缓存格里只有这个配方的物品；配方时长 d tick，每批用原料 i 共 a_i 件（a_i≤50），原料 i 经 c_i 条专用进路进 X，'
        'd·c_i≥a_i；每条进路的起点是设为这种矿石的仓库取货口或协议核心取货端口，或是一台一直有供电状态、开着、只做一个 1 tick 配方、'
        '取货物品格里只有它的产物、取货通道不多于每批产物件数、在这个循环态中每个 θ 加整数的时刻判定做完后缓存格都不空的制造单位。'
        '则 X 在这个循环态中每个 θ 加整数的时刻判定做完后缓存格都不空；若还有 d=1、X 的取货通道不多于每批产物件数、各首运输物品格只从 X 收货'
        '且空着时总收下 X 的产物，则 X 的每条取货通道在它的首运输物品格有空的每个这样的时刻都取到 1 件\n'
        '    据：物品格、存货/取货物品格、缓存格、制造、需电功能、开关、移动、滞留、判定、配方、传送带、桥接器、物品准入口、仓库取货口、'
        '协议核心、外部过程、零干预运行、端口速率、循环态\n'
        '全厂专用进路接法达标：本条只对这样的运行成立：调试期结束后所考察的全部运行（含离线、仓库停收与恢复）里，全部制造单位的制造开始'
        '与完成、运输物品格的收发和仓库收货的变化都发生在同一个相位 θ 加整数 tick 上（规则推不出这一点，须另证），同一时刻能完成的移动'
        '都在该时刻完成。专用进路同「专用进路下缓存格不空的传递」，这里路上也没有分流器、汇流器。采种单元指一台采种机 C、两台种植机 A、B、'
        '一台粉碎机 K 只处理同一种植物：C 的两条取货通道经专用进路分别进 A、B，A 的一条取货通道经专用进路回 C，B 的一条经专用进路进 K；'
        '记 C 到 A、A 到 C 两条进路的运输物品格数为 L1、L2，回路存量 Φ 为 C 到 A 进路上的种子数、A 存货物品格的种子数、A 取货物品格与'
        ' A 到 C 进路上的植株数、C 存货物品格的植株数之和，A、C 的缓存格有东西时各再记 1，再加 C 取货物品格种子数的一半。'
        '接法：34 条设为蓝铁矿的取货通道（仓库取货口或协议核心取货端口）各经专用进路进一台精炼炉，每台的蓝铁块进一台粉碎机；'
        '18 条设为源矿的取货通道各进一台粉碎机；17 台研磨机各收两台蓝铁块粉碎机的蓝铁粉末、9 台研磨机各收两台源矿粉碎机的源石粉末（每台来源各一条）；'
        '11 个砂叶、6 个荞花采种单元，砂叶单元的粉碎机共 32 条取货通道、每台至多 3 条，32 台研磨机各收其中 1 条砂叶粉末，'
        '每个荞花单元的粉碎机两条取货通道同进一台研磨机（共 6 台）；17 台精炼炉各收一台蓝铁粉末研磨机的致密蓝铁粉末，其中 6 台各供一台配件机，'
        '10 台两两供 5 台塑形机，1 台供第 6 台塑形机；3 台封装机各收两台配件机的钢制零件和三台源石研磨机的致密源石粉末（每台来源各一条）；'
        '两台灌装机各收两台双路供钢的塑形机的钢质瓶和两台荞花研磨机的细磨荞花粉末，第三台收第 5 台与第 6 台塑形机的钢质瓶和另两台荞花研磨机的'
        '细磨荞花粉末；6 台封装机、灌装机各一条专用进路进协议核心的存货端口。以上通道都是专用进路，每台制造单位除这些通道外没有别的有通道的端口；'
        '共粉碎机 69、精炼炉 51、研磨机 32、塑形机 6、配件机 6、种植机 34、采种机 17、封装机 3、灌装机 3 台，没有协议储存箱。'
        '若全部制造单位一直有供电状态且开着，调试期结束、同一时刻的判定全部完成时各物品格（含缓存格）只有本线的物品，每个采种单元'
        ' Φ≥L1+L2+5/2，则在上述运行范围内、协议核心一直收得下两种成品的每个可到达循环态中，52 条矿石取货通道各平均 1 件/tick，'
        '高容谷地电池 0.6 个/tick、精选荞愈胶囊 0.55 个/tick 进仓库。要据此认证一张布局达标，还须证明它的全部实际运行都在这个范围里，'
        '并给出合法几何与可操作的调试\n'
        '    据：目标、配方、制造、缓存格、存货/取货物品格、物品格、移动、滞留、判定、轮询、需电功能、开关、协议核心、仓库取货口、'
        '外部过程、零干预运行、离线、判定次序、拿取成品、循环态、取货口配置、端口速率、专用进路下缓存格不空的传递\n')
    S.write_text(s + add)
    print('求解约束.txt', hashlib.sha256(C.read_bytes()).hexdigest())
    print('求解充分条件.txt', hashlib.sha256(S.read_bytes()).hexdigest())


if __name__ == '__main__':
    main()
