#!/usr/bin/env python3
"""登记本轮已复核的部分修复、仍成立的反例和固定搜索域。"""
import json,hashlib
from pathlib import Path
HERE=Path(__file__).resolve().parent;BASE=HERE.parent;ROOT=BASE.parents[2]
read=lambda n:json.loads((HERE/n).read_text())
sha=lambda p:hashlib.sha256(p.read_bytes()).hexdigest()
r=read('独立LP.json');g=r['geometry'];change=read('候选改动明细.json');summary=read('复验汇总.json')
candidate=(HERE/'候选只读快照.json').read_bytes()
assert hashlib.sha256(candidate).hexdigest()==r['candidate_sha256']==change['after_sha256']
assert summary['structure_three_way_equal'] and summary['connected_mineral_sources']==52
assert summary['powered_manufacturers']==219 and r['strict']['status']==2
# A concurrent edit must not be overwritten.
assert (BASE/'生成/候选.json').read_bytes()==(HERE/'候选-修复前.json').read_bytes()
assert (BASE/'生成/报告.md').read_bytes()==(HERE/'报告-修复前.md').read_bytes()
minimum_in={'粉碎机':68,'精炼炉':51,'研磨机':95,'塑形机':11,'配件机':6,'种植机':32,'采种机':16,'封装机':15,'灌装机':11}
minimum_out={'粉碎机':95,'精炼炉':51,'研磨机':32,'塑形机':6,'配件机':6,'种植机':32,'采种机':32,'封装机':1,'灌装机':1}
table='\n'.join(f"|{k}|{g['machine_input_channels'][k]} / {minimum_in[k]}|{g['machine_output_channels'][k]} / {minimum_out[k]}|" for k in minimum_in)
trials=[]
for n in ['矿口联合修复','全机局部矿口修复','道路连通矿口修复','矿口成品联合修复','供电矿口修复','扩展桩位矿口修复','供电矿口成品联合修复','增桩矿口修复','增桩矿口成品修复']:
 d=read(n+'.json');trials.append({'name':n,**{k:d[k] for k in ['status','wall','workers','scope']},'adopted_placement':n=='增桩矿口修复'})
(HERE/'搜索范围台账.json').write_text(json.dumps(trials,ensure_ascii=False,indent=2)+'\n')
trial_table='\n'.join(f"|{d['name']}|{d['status']}|{d['wall']:.2f}|{d['workers']}|" for d in trials)
report=f'''# 第一张全厂静态候选：第2轮修复结果

日期：2026-09-22。状态：**部分修复；全部修正尚未完成；pass=false**。

当前候选的52个真实矿口均有到首加工机的完整实体路径，219台制造单位均供电且开启。整厂仅布出131条端到端路径，仍无达标连续平均流；两种成品的固定图产率上界均为0。最大空矩形36格只是几何量，L、U均不更新。

唯一当前输入为[候选.json](候选.json)，SHA-256：`{r['candidate_sha256']}`。本报告中的数值均对应此字节版本；[复核快照](../修复第2轮证据/候选只读快照.json)与其完全相同。

## 闸门问题与归因

[第2轮闸门](../闸门-第2轮.md)指出四个真实矿口断开，九种机型进出料通道大面积不足、末级进料与回库路径缺失，基础连续LP不可行，N5b/P3失败；两份检查器与第三份重建的通道逐条一致。这些是候选本身的缺陷。

四个断矿口原为OUT_L01、OUT_L04、OUT_B00、OUT_B06。本轮已消除这四处断口，并把52条矿源路径作为布线过程中不可拆除的硬路径。结构接通不证明循环态满速：下游仍缺路径，严格LP仍不能接收52口的满速供矿。

另有正式约束版本变化：当前为72条、SHA `0c05976f6063f3332db6ee19d7746052ef35148d7c2a12dfa396c7bb8753f30f`。与闸门初次版本相比，唯一文本变化是第33条删除末尾关于箱子中转的分句。当前候选无箱，该条无触发对象。语义审查和逐行差异见[正式版本审查](../修复第2轮证据/正式版本审查.json)。候选指纹已同步当前真实文件。

两份检查器仍只支持旧指纹；它们对新版本的拒绝是版本支持边界，不是本轮追出的漏边或流量算法错误。**检查器A、B目录均未修改**；没有用降低检查条件消除候选反例。

## 当前候选

|项目|第2轮闸门输入|当前修复候选|
|---|---:|---:|
|制造单位|219|219|
|有完整首加工路径的矿口|48/52|52/52|
|有供电且开启的制造单位|219|219|
|供电桩|26|27|
|完整端到端路径|135|131|
|未布出的搜索送料记录|180/315|184/315|
|结构通道|561|555|
|结构S / R / E|135 / 135 / 291|131 / 131 / 293|
|运输单位 / 桥|414 / 12|404 / 20|
|占格|4062|4056|
|电池 / 胶囊入核心路径|0 / 1|1 / 0|
|最大空矩形面积 / 短边|36 / 6|36 / 6|
|两种成品达标联合LP|不可行|不可行|

本轮优先完成矿口结构连接和供电约束；其余已布路径数量下降，胶囊入核心路径消失，不能据此声称整厂距离达标更近或本轮已完成全部修正。

九机型台数仍为粉碎69、精炼51、研磨32、塑形6、配件6、种植32、采种17、封装3、灌装3。制造开关全部开启，每机单配方，矿口设定仍为蓝铁矿34、源矿18。核心保持x=11…19、y=17…25、Din=1；没有箱、分流器、汇流器、准入口或虚拟接口。最大空矩形仍为x=64…69、y=64…69。

## 文件修改

- `生成/候选.json`：改为`factory-static-repair-0002`，更新正式指纹；改变36个制造机位的几何配置并重指派部分角色，合计115个机器ID记录变化，台数与逐ID配方不变；移动P000并增加P026，供电桩共27座；重新绑定真实矿口，重建运输单位与全部555条结构通道；更新`fixed-layout`限制登记。`flow_witness`仍为空，不附`logical_feeds`，未把315条搜索指派强加为LP约束。
- `生成/报告.md`：记载当前候选、已修复断口、路径数量回退、剩余结构反例、版本入口拒绝和精确LP否证。
- `修复第2轮证据/`：保存修复前字节快照、构造脚本、试验日志、当前只读快照、两份原入口结果、组件诊断、独立LP矩阵与证书，以及改动和完整性清单。

逐ID前后变化、机位集合变化、矿口/核心设定、桩位及元数据差异见[候选改动明细](../修复第2轮证据/候选改动明细.json)。所有模型只用于构造受限候选，限制与失败覆盖域保留在候选和[搜索范围台账](../修复第2轮证据/搜索范围台账.json)中。

## 复核结果与剩余缺口

原CLI按当前正式文件执行：A退出1、`rejected`；B退出2、`INVALID_INPUT`。A的候选声明与当前文件已一致，但支持指纹仍旧；B报告不支持该正式版本。原始结果为[A当前入口](../修复第2轮证据/A当前入口.json)、[B当前入口](../修复第2轮证据/B当前入口.json)。二者均止于版本层，不能说它们完成了当前版本的72条检查。

另行直接调用原A/B的几何、受限类和流组件，结果明示`static_certified=false`，不修改或替换原CLI输出。A组件、B组件、第三份独立脚本的555条通道逐端口完全相等；均未发现此次输入的结构重建错误，219台机器供电、52矿口接通。组件诊断：[A](../修复第2轮证据/A组件复核.json)、[B](../修复第2轮证据/B组件复核.json)，[复验汇总](../修复第2轮证据/复验汇总.json)。组件检查不包含全部正式条目的认证。

以下结构数是正流支持数的上界，已经低于正式必要下限，足以继续拒绝本候选：

|机型|结构输入 / 必要下限|结构输出 / 必要下限|
|---|---:|---:|
{table}

封装M213/M214/M215的输入数为0/1/1，灌装M216/M217/M218为1/0/1。只有M213的一条电池路径到核心，且M213没有输入。胶囊完全没有入核心路径。结构S=R=131，分别低于312、307；无箱、无分汇流的S+R=262，也低于624。N5b/P3仍失败，完整P2的正流等速义务未取得见证。

独立连续LP使用19物品、18配方，按真实格、桥轴、制造输入/输出分别建守恒和容量；52矿口逐口=1，非成品入库=0，两成品目标为3/5、11/20。未预设流量分母。矩阵774变量、18583行（17383等式、1200不等式），严格模型、关闭预处理的严格模型、矿源放宽但保留两目标的模型均不可行。

精确空行反例为胶囊目标`0 ≤ −11/20`。矿源放宽为≤1、撤去成品目标后，19次单项最大产率及全部通道总流量最大值均为0。总流量对偶证书有684个非零乘子，774列以Fraction精确回放；由所有通道流非负及总和≤0，全部通道流和219个配方批率均被迫为0。这也与原模型的矿口等式下限1冲突。两种成品的缺口仍分别为0.6、0.55个/tick，不能称为已获得达标流。

[独立LP结果](../修复第2轮证据/独立LP.json)、[矩阵](../修复第2轮证据/独立LP矩阵.json)、[无求解器证书回放](../修复第2轮证据/独立证书回放.json)均绑定当前候选SHA。A组件另独立给出精确Farkas界1063/20；该数是矩阵证书界，不是成品产率损失。

## 受限搜索记录

下表为执行记录，完整固定项、开放自由度和覆盖域见搜索范围台账。`INFEASIBLE`仅针对所列构造模型，未另交CP-SAT独立不可行证明；`UNKNOWN`不表示无解。无容量公共道路连通条件是额外形状限制，不能用于排除一般布局。

|试验|结果|秒|求解器workers|
|---|---|---:|---:|
{trial_table}

“矿口成品联合修复”在发现供电未纳入后于212.70秒主动中止，保留UNKNOWN，不当作300秒超时或无解。未保证供电的52矿口中间结果经A组件发现六台缺电，未交付；另一次人工局部摆放存在重叠，亦未交付。最终采用增桩模型的可行摆放，再运行三个顺序布线种子；按矿口完整、供电完整、路径数量和运输占格选择种子24。六条成品回库联合模型在本轮180秒内未找到可导出候选。

所有失败仅涉及明确固定的219台、单配方、边带、核心位置、局部移动域、供电和P2P支持选择。其他位置、台数、配方、路径和支持未被排除；不降低U。运行条件、调试、初态、内核及全部相关可达循环态不在本轮范围，也未由静态试验认证。

## 复算与文件完整性

从仓库根目录运行：

```bash
python -B 求解器/构造/第一张全厂候选/修复第2轮证据/复验.py
```

脚本顺序执行原A/B入口、组件诊断、独立LP和精确回放，内部限制LP/BLAS/OMP为1线程；其外层成功退出只表示检查执行并核对一致，不表示候选通过。脚本只写本轮证据目录。构造模型至多5 workers；曾并行一个1-worker试验或一个单线程诊断/布线进程，求解线程配置合计不超过6。

正式三文件、候选约束、检查器A/B和会议3原文件的起止字节核对见[完整性检查](../修复第2轮证据/完整性检查.json)。原第1、2轮闸门及其证据未改。当前文件与旧生成图、旧哈希清单、旧复核说明须按各自轮次区分；旧资料不描述本候选。新增/修改文件见[文件变更清单](../修复第2轮证据/文件变更清单.json)。未执行git或cargo test，未修改内核源码，写入范围限于第一张全厂候选目录。
'''
(BASE/'生成/候选.json').write_bytes(candidate)
(BASE/'生成/报告.md').write_text(report,encoding='utf-8')
print(r['candidate_sha256'])
