# 「矿系不入库」修订的内核依赖与最小改动清单

日期：2026-09-22。状态：依赖审计完成；甲、乙均为待实施方案，未修改代码、正式文件或候选约束。以下相对路径均以 `/home/zhuran24/zmd-research-fresh/求解器` 为基准，`../` 指研究仓库根；行号为本次读取版本。

历史出处已核实：`../候选约束.txt:143–148` 的第5轮候选，只在种植机恰32台时证明植物系零入库；`:173–176` 的第6轮修正版保留这项前提，并记载2026-09-17采纳。因此“植物系物品”来自旧证明范围。现行 `../求解约束.txt:74–75`「非成品零入库」明确取消该台数前提，旧植物豁免已过时。第57–59轮依据见 `候选约束轮次/第57-59轮/G2-推导.md`、`G2-复核58.md:38–68`、`复核59.md:75–94`。

本审计不把“箱内可在两次判定间进出”的局部机制当成达标全厂反例，也不判定方案甲保留的箱体限制是否为必要条件；这里仅给出两种正式改写落地时的代码影响。

## 依赖清单

**正式依据。** `../求解约束.txt:72–77` 分别是「矿系不入库」「非成品零入库」「传输按仓库余量判定」；`:8–11` 是「传输相位」「判定先后」；`:40–41` 是「入库途径」。`../《明日方舟：终末地》游戏规则.txt:13–16` 是「物品格」「仓库」「存货/取货端口」「通道」，`:23–25` 是「滞留」「移动」「判定」，`:36` 是「传输」，`:41` 是「协议核心」，`:59–68` 是运输单位及子类，`:72` 是「协议储存箱」。`../求解任务.txt:2,8–10,15` 是「目标」「零干预运行」「拿取成品」「循环态」「判定次序」。以下“核心只收成品”均指达标循环态的正入库流量，不是禁止调试期或普通有限运行出现非成品入库。

代码问题首先调用了 CodeGraph，`projectPath=/home/zhuran24/zmd-research-fresh/求解器`；其结果定位到目录结构与三个22c维护脚本，源码同名结果混有历史副本，未覆盖的函数、测试和非代码数据再以只读检索补齐。

| 位置／函数 | 当前依赖与影响 | 对应正式依据 |
|---|---|---|
| `数据/工具/formal_catalog.py:44–54,56–92`，`formal_projection()` | 第77行用正则 `种植机恰 (\d+) 台` 从「矿系不入库」提取 `plant_trigger`。删末句后 `extract()` 的断言失败，目录回源、转换器和调用它的测试都会受阻。 | 矿系不入库；现应由非成品零入库取代台数前提。 |
| 同文件 `:26–41,144–159,172–183`，`parse_constraints/compare/verify` | 约束固定71条；对 `sources`、`constraints`、`task`、`static_checks` 完整比较，包含键集合。不能只删常量消费者而保留旧目录，也不能只换SHA。甲乙都保留条目名和条目数，无需改71。 | 三份正式原文及各条“据”；矿系不入库、非成品零入库。 |
| `crates/topology/src/lib.rs:1088–1111`，`validate()` | 唯一现行业务消费者：只遍历目标为核心的 `logical_feeds`；成品直接通过，种植机台数不等于 `plant_trigger` 时还允许七种植物系物品。第1108行说明文字重复这一旧条件。检查名为 `矿系不入库/计划`。 | 非成品零入库；入库途径；协议核心、存货/取货端口。 |
| 同文件 `:217–230,496–509,1291–1326`，`StaticChecks/Catalog::count/catalog/validate` | 常量为通用映射，不是固定字段结构；第501行遍历产生 `目录/阈值/plant_trigger` 报告行，删键即少一行。第1308行对「矿系不入库」给出专门的未覆盖说明；两条新约束目前落入默认“缺少几何”说明，状态仍为Unknown。第230行编译时嵌入整个目录，必须重编译。 | 非成品零入库、传输按仓库余量判定；目标、循环态。 |
| 同文件 `:520–523,566–572,617–643`；`数据/工具/convert_candidate_b.py:117–123` | 拓扑校验只支持候选B的无箱送料契约；箱子不能作为受支持端点。现行代码没有按“开着传输的箱子只输送某几类物品”筛除实体箱子。不能把第1088行的核心检查说成已实现箱体条文。 | 协议储存箱、传输；目标；契约的无箱限制是候选范围，不是正式规则。 |
| `数据/正式静态目录.json:655–682,3877–3884,4205起` | `constraints[30]` 为矿系不入库，`[31]` 为非成品零入库，`[32]` 为传输按仓库余量判定；保存原文、据、源行号。常量含 `plant_trigger=32` 及原文摘录；`sources` 另存完整正式文本和SHA，矿系原文在第4279行。当前常量33项，删此键后32项。 | 上述三条约束。 |
| `crates/topology/tests/validation.rs:708–738`，`plant_storage_condition_uses_current_machine_count()` | 加一台种植机，把核心入料改成荞花，按精确检查名寻找并断言 `Status::Pass`。测试还断言整体存在其他失败，故只证明旧守卫行为，未证明变异候选可行。甲乙都必须改变该期望。 | 非成品零入库。 |
| 同文件 `:18–33,151–168,666–683,740–843`；`crates/topology/src/lib.rs:1332起` | 分别覆盖候选B零失败、零速率不能算活通道、71条正式报告行、带箱端点不支持、编译内置目录回源及变异/Python回归。`src` 内测试中的种植机32是机型下限对照，不是 `plant_trigger`，不应删除。未发现其他现行Rust测试精确断言该旧说明或植物豁免结果。 | 机型下限、端口速率、非成品零入库；完整正式目录。 |
| `数据/工具/test_formal_catalog.py:33–67,69–115` | 现有测试不点名 `plant_trigger`；间接经 `verify()` 覆盖目录。阈值负例改的是 `transport_s`。缺少“新版无种植机末句仍能投影”和“遗留plant_trigger键拒收”的针对性回归。第40–53行会写 `target` 临时目录，本轮未运行。 | 非成品零入库；正式原文与投影一致性。 |
| `数据/工具/check_revision.py:18–24,29,66–99,106–113` | 调用 `verify()`；核71条、来源清单、两张覆盖表及候选报告。第95行要求报告零失败，第97–99行要求所有正式条目名称、据和分节；没有精确锁定 `矿系不入库/计划` 或旧说明。运行会写 `数据/修订验证`，本轮未执行。 | 完整正式约束；目标；非成品零入库。 |
| `数据/工具/convert_candidate_b.py:25–26,124–130`；`数据/候选B/来源清单.json`、`交付哈希.json` | 转换器经回源门禁读目录，生成的来源清单包含四份研究文本及目录SHA；这是间接依赖。纯措辞修订不要求改变候选B的送料记录。旧交付哈希是既有交付证据；新交付另产哈希，不把旧证据换成新版本。 | 三份正式源；候选B计划与目标的证据边界。 |
| `crates/kernel/src/catalog.rs:70–95,204–221`；`crates/kernel/src/tests.rs:165–166` | `reference()` 核输入引用SHA，`Catalog::load()` 将磁盘目录与编译内置目录全量比较。内核从 `static_checks.constants` 实际只取 `port_rate`，不取 `plant_trigger`；仍因整个目录变化而必须重编译及重锁输入。 | 端口速率、物品格、协议核心；正式来源一致性。 |
| `数据/样例/check_examples.py:46–50,708,722–747`；`数据/样例/**`；`crates/kernel/tests/fixtures/**` | 样例检查器有三份正式源SHA常量。按22c脚本的扫描域，现有45个样例及9个fixture为 `kernel-input-v3`，均通过 `catalog` 引用目录；另有2个 `profile-assignment-v2`。未发现这些现行样例直接断言矿系检查名、旧植物说明或plant_trigger值。例：`数据/样例/任务7内核/无线多格全收.json:2–4`。只改实际变化的引用，未变的轴/配置SHA不应伪造更新。 | 传输、协议储存箱、物品格；三份正式源。 |
| `crates/kernel/src/warehouse.rs:105–193,251–326`，`plan_receipt/transfer`；`engine.rs:997–1046`，`target`；`transition.rs:204–225` | 普通运行按仓库真实余量接收；无线按 `min(箱内数,余量)`，空箱和全拒收也重启冷却；箱体物理接收按格编号/物种/容量，不看“仅成品”白名单。核心成功移动即提交入库账。甲乙均不应把达标必要条件加成普通转移守卫。 | 传输、协议核心、协议储存箱、物品格、存货/取货端口；传输按仓库余量判定。 |
| `crates/kernel/src/warehouse.rs:276–285`；`transition.rs:216–222`；`cycle.rs:285–304`，`check_production_candidates()`；`tests_round5.rs:246–278` | 另有 `production_abstraction` 的两种原矿回仓停止条件，返回 `unsupported(cycle.domain.D2)`，包含满仓时的回矿候选。它是仓库容量抽象的支持域限制，不是一般“矿系不入库”物理禁令，更不是钢块或植物白名单。甲乙不据此扩域或删除守卫。 | 矿系不入库仅约束达标循环；物品格、传输、协议核心、外部过程；规格受限域证明另见下行。 |
| `规格/运行语义.md:9–17,199,226`；`规格/受限转移定义.md:90–97,125–139`；`规格/第五轮规格修订-r8/修订论证.md:39`；`规格/推导/三种相位不改产量-v2.md:123` | 运行语义明确“必要条件不得用来删掉合法坏运行”；§4.3已写判前快照及按余量传输；受限域D.2明写工程停止；修订论证明确成功入库为零不推出更强准入。相位推导引用的是含矿入库守恒，不依赖植物豁免。运行语义的源指纹和部分行号已旧，按版本更新或明确历史，不改已有证明结论。 | 目标、循环态、传输、物品格、协议核心；矿系不入库、非成品零入库、传输相位、判定先后。 |
| `数据/规则覆盖表.md:181起`；`规格/规则覆盖表.md:182`；`规格/check_revision.py:22–52` | 两表仍定位旧的矿系条目：数据表写 `constraints[27]`、据行67，规格表写源行66。现应对应 `[30]`、源行72/据行73。规格自查还硬编码56条并比对旧轮四份只读指纹；其间接依赖会阻止整套文档验证。运行语义第9行已把旧覆盖表称为史料，不能默默把历史覆盖当现行门禁。 | 当前71条完整原文；矿系不入库及两条新约束。 |
| `数据/候选B/校验报告.md:34,4891,4974`；`cargo-test.log:30`；`验证记录.md:9–18`；`数据/修订记录.md:89` | 保存阈值行、旧检查说明/通过结果、原条文、旧测试通过及旧报告计数。候选报告若作为现行输入须重新生成；带日期的验证记录与修订记录属历史，不能就地抹去。 | 旧版矿系不入库；当前非成品零入库。 |
| `内核维护/2026-09-22c/regen_constraints.py:7–11`、`relock.py:8–28`、`check_positive.py:9–20` | 重建脚本只替换 `constraints`，**不替换 `static_checks`**；删plant_trigger时照跑会在verify失败。relock负责源快照、样例检查器SHA和输入引用，不负责重建语义投影。正向脚本含固定输出目录、release二进制及固定旧目录SHA，必须按新轮次适配，不能覆盖22c历史。 | 正式约束全文、目录一致性；传输样例仅是有限执行验证。 |

**历史回归证据的额外消费者。** `数据/复核/复算-r4/compare.py:146–153` 明确将 `plant_trigger=32` 放进预期常量集合，逐项核值及出处；`:163–170,231–242` 又核报告和覆盖表。若移植为新回归，需删该预期键并对齐新报告；原归档脚本和输出不改。`数据/复核/复核-r3-校验器正确性.md:42` 的P4记录“荞花入核心”应失败并点名该检查；保留旧检查名时它的名称无需迁移。`数据/复核/复算-r4/报告及字段逐项审计.tsv:752–753,822,5679`、`数据/复核/复算-r5/报告条目索引.json:117,24402–24404` 保存旧阈值、检查名、说明和结果，属于旧证据，不是现行执行规则。

**重复归档位置。** 下表以目录加文件集合列出相同内容的副本；花括号表示逐个文件/目录展开，不表示新文件。报告副本通常在第34行保存阈值、第4891行保存检查名与旧说明、第4974行保存正式条文；偏移例外另列。依据均为该归档版本的「矿系不入库」，不作为当前规则。

| 归档目录或文件集合 | 所含依赖与处置 |
|---|---|
| `规格/{内核输入修订验证-r3,内核输入修订验证-r4}/{候选B校验.log,候选B校验报告.md}`；`规格/{第6轮修订验证,第7轮修订验证}/候选B校验报告.md` | 上述报告行。历史不改。 |
| `数据/复核/r4-L1-字段覆盖证据/原版-CLI报告.md`；`r5-字段覆盖/校验报告.md`；`复算-r4/重生成校验报告.md`；`复算-r5/{候选CLI.stdout.log,重生成校验报告.md}`（后四项均相对 `数据/复核/`） | 上述报告行。历史不改。 |
| `数据/复核/r4-校验器证据/{候选B校验.stdout,变异-供电宽度-候选B校验.stdout,变异-研磨耗时-候选B校验.stdout}`；同目录 `{变异-供电宽度,变异-机型下限,变异-研磨耗时,对照-运输阈值}.json` | 报告行；各目录副本第3736行plant_trigger。 |
| `数据/复核/否证-r4-1-证据/{baseline,core_port_category,power_coverage,unused_recipe_duration,used_recipe_duration}` 对应的 `-cli.stdout` 与 `.catalog.json` | 报告行；目录第3736行。 |
| `数据/复核/否证-r4-2-证据/` 及其 `初次共享构建目录结果-不作为证据/` 下，`{baseline,core_port_category,power_coverage,unused_recipe_duration,used_recipe_duration}-cli.stdout` 与 `{baseline,cooldown_control,core_port_category,power_coverage,unused_recipe_duration,used_recipe_duration}-catalog.json` | 报告行；目录第3736行。明确标作不作为证据的结果不升级。 |
| `数据/复核/复核-r5-校验器正确性-证据/{core_port_category,power_coverage_width,unused_recipe_duration,used_recipe_input}.json`；`候选B-独立校验报告.md`；`{fake_actual_certificate,false_fanout_certificate,false_numeric_category,false_target,local_balance,missing_same_item_obligation,nonore_full_speed,overload_exact,unsupported_merger}.stdout.log` | 目录第3736行；报告检查行分别为4890、4891、4890、4890、4887、4890、4890、4885、4890；false_numeric_category阈值行33，其余34。 |
| `数据/复核/复算-r5/门禁变异-{供电宽度,供电高度,未用配方类别,未用配方耗时,核心类别,研磨产出,研磨投入}.json`；`数据/复核/复算-r1/报告复现.md:4227`；`内核维护/2026-09-22/test.log:207` | 目录第3736行；更早版本报告检查；旧植物条件测试通过日志。均保留。 |

历史源码及报告的嵌套副本按以下完整目录集合归类。每项下再接 `求解器/`。源码同型依赖为 `crates/topology/src/lib.rs:1089,1095,1108`、`crates/topology/tests/validation.rs:709–738`、`数据/工具/formal_catalog.py:74或77`；目录副本为 `数据/正式静态目录.json:3736`；报告副本为 `数据/候选B/校验报告.md:34,4891`。

- 含上述源码与目录：`数据/复核/r4-L1-字段覆盖证据/{snapshot,testrepo}`、`数据/复核/r4-校验器证据/隔离副本`、`数据/复核/r5-字段覆盖/isolated`、`数据/复核/否证-r4-1-证据/{isolated,snapshot}`、`数据/复核/否证-r4-2-证据/snapshot`、`数据/复核/否证-r4-2-证据/cases/{baseline,core_port_category,power_coverage,unused_recipe_duration,used_recipe_duration}`、`数据/复核/复核-r5-校验器正确性-证据/被审快照`、`数据/复核/复算-r4/{被审快照,隔离重跑}`、`数据/复核/复算-r5/{输入快照,隔离运行}`。除 `r4-校验器证据/隔离副本`、`复算-r4/隔离重跑` 外，其余还含上述候选报告。
- 只有相应报告：`规格/复核/{r7-独立选择点-证据,r7-覆盖证据}/被审快照/求解器/规格/第6轮修订验证/候选B校验报告.md:34,4891`。
- 目录及规格报告副本：`规格/复核/内核输入/r4-覆盖证据/被审快照`、`规格/复核/内核输入/复核-r4-一致性-证据/被审快照`、`规格/复核/内核输入/复核-r4-可导出性-证据/{被审快照,隔离运行}`、`规格/复核/内核输入/否证-r4-1-证据/读取快照`、`规格/复核/内核输入/否证-r4-2-证据/被核快照`。其报告后缀为 `求解器/规格/内核输入修订验证-r3/{候选B校验.log,候选B校验报告.md}:34,4891`。
- 目录副本：`规格/复核/内核输入/复核-r4-一致性-证据/标识冲突隔离副本`、`规格/复核/内核输入/否证-r4-1-证据/事件冲突隔离`、`规格/复核/内核输入/否证-r4-2-证据/事件碰撞隔离副本`。`规格/复核/内核输入/复核-r3-可导出性-证据/被审快照` 另有 `求解器/数据/工具/formal_catalog.py:77` 和目录副本。

这些嵌套源码属于被审版本，不能随现行源码批量替换。现行业务代码读取plant_trigger的位置只有topology上述一处；其余为生成、全量校验、通用报告或历史复算依赖。

## 方案甲改动

前提：正式「矿系不入库」保留条目名，删“植物系物品”许可和末尾“种植机恰32台……”句；箱子仍保留“成品或仓库该格已满的物品”的措辞。条目仍为71条。以下均为将来实施清单。

1. **删除旧阈值提取。** 删除 `formal_catalog.py:77` 的plant_trigger规格项；不改正则去其他条目重新取32，不保留伪造默认值，也不改其他真正带台数前提的约束。验证：新版投影无plant_trigger，其他32项常量及其依据不变；`verify()`通过。
2. **收紧核心计划检查。** 在 `topology::validate():1088–1111` 去掉整个“台数不等于阈值且属于七种植物”的分支，判据只剩 `cat.task.targets.contains_key(&e.item)`。最小方案保留检查名 `矿系不入库/计划`，更新detail为“达标循环态计划的核心正入库流量仅允许两种成品；不依赖种植机台数；据：非成品零入库、入库途径、协议核心”。现有契约另行拒绝零速率记录，无需在此引入满仓例外：满仓物品无法形成核心正入库流量。验证：成品通过，所有非成品在32台和33台都失败。
3. **修正报告覆盖说明。** `validate():1294–1315` 为「非成品零入库」补“仅核核心计划物品，实际周期两途径零入库未认证”；为「传输按仓库余量判定」补“缺判定前箱内库存、仓库余量及相位/次序覆盖证据”。维持Unknown。矿系条目说明明确核心投影已核、箱体部分未核。验证：三条均有独立 `正式条目/...` 行，不能因核心通过而把箱体判定标Pass。
4. **更新并补齐回归。** 将 `plant_storage_condition_uses_current_machine_count` 改成“不随台数放宽”的测试并改期望为Fail；覆盖32/33台、七种原植物白名单、十种含矿非成品、两成品正例。仍逐项定位检查结果，避免其他失败掩盖本检查漏检。Python增加无旧末句投影成功、目录残留plant_trigger键被拒绝的负例。原机型下限32、满载/回路存量等条件式测试不动。
5. **重建目录的完整受影响投影。** 更新 `sources` 与 `constraints`，并同步 `static_checks` 删除plant_trigger；`task`投影复核应不变，单位和配方应不变。若沿用22c的regen脚本，在新维护脚本中同时赋值 `d['constraints']=proj['constraints']`、`d['static_checks']=proj['static_checks']`；不要修改22c历史脚本来伪装旧轮已做此事。验证：33→32常量、71条不变、完整回源通过。
6. **更新现行消费物。** 按验证步骤重锁样例和fixture目录引用、样例检查器的正式源SHA、候选B来源清单；重编译topology和kernel；重新生成现行候选报告。候选B契约、单位配方、内核普通转移与生产抽象D.2不需要语义改动。旧交付证据原样保留，新报告/日志/哈希另记本轮。
7. **对齐现行文档入口。** 重建全部71条的覆盖，明确包含「非成品零入库」「传输按仓库余量判定」，行号和索引从正式源重算。`规格/check_revision.py:37` 的56改为当前71，或改为与已回源目录的条目数比较；`:43–47` 改读新轮修订后基线，保留旧轮指纹原件。旧规格覆盖表已有史料定位，若继续保留史料，应同步修改规格自查第9行及数据自查第77行的读入路径；若维护原路径，则先保留历史版本并明确切换为现行表。同步运行语义的当前源快照，保留“必要条件不是转移守卫”的界限。这些覆盖/门禁失配已有基线证据，不算本次新引入的算法回归。历史修订记录和归档报告不批改。

方案甲**不要求新增箱体物理白名单**。现行topology无箱，箱体条文继续列未覆盖；普通kernel必须仍能模拟不达标运行。是否需要把保留的箱体半句另实现为达标候选检查，须先完成其必要性审定，不属于本次最小迁移。

## 方案乙改动

前提：在甲基础上，删除「矿系不入库」中箱子半句，或将其弱化为对「传输按仓库余量判定」的引用；保留该独立条目和「非成品零入库」。条目仍为71条。

1. **完整执行甲的第1–7项。** 植物旧豁免、plant_trigger、直接回归、投影、指纹与报告的改动相同。核心只允许成品正入库的检查不放宽。
2. **额外仅改条文投影与箱体覆盖说明。** 矿系条目不再宣称已核箱子物理中转；箱体的动态义务由「传输按仓库余量判定」报告行承接。没有现行箱体白名单代码需要删除，也不需要改feeding-v2数据结构。
3. **保留当前普通传输实现。** `plan_receipt/transfer`、箱体端口选择器、5tick冷却、核心成功入库记账不变；它们按物理规则运行。不得把“判定前非成品数量×余量必须为零”放进普通转移守卫：若某合法坏运行实际传入非成品，应执行并记录，随后由达标循环态审查判失败。
4. **不动生产抽象支持域。** `cycle.domain.D2` 限制及其测试继续保留。将这项工程限制删掉需要重新证明容量抽象，不是乙的文本弱化自动授权的语义结果；报告unsupported不能解释为原布局违法。
5. **箱体新增测试只验证机制边界。** 在普通有限执行模式补“钢块满仓时物理中转且无线量零”“仓库有余量但判定时箱为空”“仓库有余量且判定时非成品在箱则发生真实传输”三类；覆盖相关事件顺序及冷却相位。此类测试不声称存在满足全厂目标的反例，不凭一个相位宣布全称达标。

乙相对甲的**业务算法改动增量为零**；差异是正式目录内容、报告解释及箱体机制回归。若未来扩展完整循环证书，应按每次有资格判定前的实际库存与余量验证，而不是禁止一切物理中转；该扩展不列为此次必须新增的求解器功能。

## 验证步骤

本轮已执行的只读基线：`python -B 数据/工具/formal_catalog.py` 退出0，71条约束完整回源通过；`python -B 规格/check_revision.py` 退出1，失败为 `AssertionError: ('《明日方舟：终末地》游戏规则.txt', 13)`。后者是修订前已有的旧覆盖失配；代码还保留56条断言和旧轮指纹，不能声称当前规格总门禁通过。本轮未运行会写target/数据目录的cargo、Python测试、重建或重锁脚本。

以下是两方案实施后的验证顺序，须在届时可写的修订副本/目录执行；不是本轮已执行的操作。

1. **保存基线并限定线程。** 记录正式源、目录、现行代码与待更新输入的SHA，另存旧证据。顺序执行测试，建议设置 `CARGO_BUILD_JOBS=6`、`RUST_TEST_THREADS=1`、`OMP_NUM_THREADS=1`、`OPENBLAS_NUM_THREADS=1`、`MKL_NUM_THREADS=1`、`PYTHONDONTWRITEBYTECODE=1`；cargo用 `-j 6` 和 `--test-threads=1`，不并发启动多组求解器。改写文本应先作为已审定输入准备好，再更新代码投影。
2. **核最小文本差异。** 三条条目名仍在、条目总数71、两成品定义不变；甲仅删植物许可与台数末句，乙再删/弱化箱句。逐条核「非成品零入库」「传输按仓库余量判定」未被顺带削弱。源行号从实际文本重算。
3. **修提取器与消费者后重建，再重锁。** 先按甲第5项生成 `sources/constraints/static_checks`，运行 `python -B 数据/工具/formal_catalog.py`。确认plant_trigger消失、其他常量和单位/配方未漂移。然后参照22c的relock更新新目录version与源SHA、`check_examples.py::SOURCE_HASHES`、45个样例/9个fixture的catalog引用及实际变化的参数来源引用。顺序不能倒置；否则后写目录会再次使已锁SHA过期。
4. **处理来源清单及文档门禁。** `数据/候选B/来源清单.json` 指向正式源和目录，必须按审定新字节复核后更新；不要为此无条件运行会改写契约的转换器，旧外部CSV等未变条目保持原哈希。准备新轮数据自查所需的 `只读文件指纹.json` 与 `样例检查.json`；记录“修订后快照在验证期间不变”，不要把正式文件更新前后相同写成已验证。对齐两份现行覆盖入口、71条及运行语义源快照后，才运行文档总门禁；旧轮只读指纹文件不得直接重锁。
5. **目录与拓扑定向回归。** 运行 `python -B 数据/工具/test_formal_catalog.py`，再运行 `cargo test -p topology -j 6 -- --test-threads=1`。前者及cargo中的Python回归会使用副本内target临时目录。验收：32/33台所有非成品核心正流均Fail；两成品Pass；候选B仍零失败；无plant_trigger报告行；三条相关正式覆盖行维持Unknown；回源及变异拒收通过。若保留旧检查名，不应要求历史结果全文与新报告一致。
6. **内核机制回归及完整测试。** 运行 `cargo test --workspace -j 6 -- --test-threads=1`，覆盖kernel的 `transfer_capacity_and_empty_cooldown`、`transfer_conservation`、`box_selectors`、`transfer_cooldown_pause`、`round5_ore_return_candidate_stops_before_capacity_or_age` 及新增箱体案例。核满仓留箱、空箱起冷却、按余量部分接收和真实入库账；D.2仍按原受限模式返回unsupported。发生普通非成品入库不是转移器应主动拦截的错误。此前topology定向测试可按新失败情况决定是否重复，不需反复全跑。
7. **重新构建并产出当前报告。** `cargo build --release --workspace -j 6`；用新 `target/release/topology 数据/候选B/contract.json` 的标准输出生成本轮报告。保留 `矿系不入库/计划` 名称、更新说明、删阈值行；报告分组计数按实测记录，不沿用旧4925/59等值。更新本轮交付哈希，不重标旧cargo日志/旧证书为新版通过。
8. **重锁正向与拒收对照。** 在新维护目录适配22c `check_positive.py`：用新release binary和新输出目录，执行 `seed → check → run --ticks 6 → verify-record`；均应退出0。再把输入catalog SHA换成此次修订前的目录SHA，应非零拒收。22c脚本硬编码的 `c252…` 是更早版本，不能冒称是此次基线；本轮读取的目录SHA见下。还应核“旧编译内置目录＋新磁盘目录”的内容不一致拒收，不能靠只重锁输入让旧binary通过。
9. **样例与交付自查。** 运行 `python -B 数据/样例/check_examples.py --self-test --report <新报告路径>`；其路径须在求解器内且不能覆盖输入。再运行适配后的 `数据/工具/check_revision.py --output-dir <数据/修订验证/新轮次>`，以及已切到现行覆盖/快照的规格自查。前者依赖外部CSV、来源清单及两份预置报告；缺项应明确失败原因，不跳过后宣称全套通过。全部新报告都只证明对应静态或有限执行范围。

箱体判定用例的最小期望矩阵如下；`n`、`f` 分别为有资格传输判定前的该物品箱内数量与仓库余量。用例选取当前支持、落格与残留分配确定的状态；同种跨多格部分接收等已有未支持分支仍返回unsupported，不能把下表当成其已实现声明。

| 场景 | 普通内核应做什么 | 达标循环态审查含义 |
|---|---|---|
| 非成品 `n>0,f=0` | 无线发送0，保留未送件，重启冷却；物理端口仍按规则工作 | 不因这一点排除物理中转；仍需全厂及可达性验证。 |
| 非成品 `n=0,f>0` | 发送0，空箱也重启冷却 | 此次判定满足必要条件；不推出其它相位/次序都满足。 |
| 非成品 `n>0,f>0` | 真正发送 `min(n,f)` 并记账，不用目标条件拦转移 | 若发生在候选声称达标的循环中，则不满足非成品零入库。 |
| 核心非成品来货且满仓 | 不能成功存入，不形成正入库流 | 不为正速率送料计划增加“满仓可通过”例外。 |
| 物品在判定之间进箱再出箱 | 按事件顺序执行；箱子不套运输格1tick滞留 | 局部轨迹证据不等于某合法布局所有可达循环达标。 |

本次读取的基线SHA：`求解约束.txt = cf44821f07779490b9b1d913bfb87d7757ae8abfe784c912bb7891868780f7f3`；`数据/正式静态目录.json = 6e609fbab15104489f7e00a03c64b3f89cd77668de26789622d78be27a0ee3ff`。执行修订前需再读取，避免把并行工作产生的新版本误认成本次基线。

## 报告文件路径

`/home/zhuran24/zmd-research-fresh/求解器/约束修订/2026-09-22-矿系不入库/C-内核依赖.md`

该目录及报告已创建。交付范围为本方案清单；代码、正式文件、候选约束和历史证据未作修改，未执行git操作。
