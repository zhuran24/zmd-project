# 2026-09-26 三审同步记录（r26：正式约束 72 条改为 71 条）

执行记录。日期：2026-09-26。开工 HEAD `0e21280`（三审采纳提交）。状态：目录与引用同步完成；指定安全集全部通过；历史证据目录前后全量哈希一致。本记录不构成目标循环或全厂达标认证。

## 起因

主会话三审第 63—80 轮候选约束（报告见 [三审报告](../../候选约束轮次/三审-第63-80轮/三审报告.md)），求解约束改动：删去「1113 位置」；「供电下限」「箱体过站」「空矩形上界」「面积预算」四条正文与据改写。条数由 72 变 71，内核写死的条数与目录投影随之同步。

## 结论与计数

目录版本 `2026-09-26-r26-constraints-71`，SHA-256 `630ab63d053d99f4d5621e3172d4cd1d6c86be6bfd84d1c8bc45b57819e7c72c`（旧 r25 为 `3566e2ec…`）。共改 **69 个业务文件**：同步脚本一次写入 67 个，候选 B 报告与验证记录另 2 个。

| 正式来源 | SHA-256 |
|---|---|
| 《明日方舟：终末地》游戏规则.txt | `6e64e3903a65536c530b363c9f3aef8c1bb2a1c1193e866799125dd047159924`（未变） |
| 求解任务.txt | `1630ca1febec79b324aa3afb110be2d3330298e266c68b06415c3d1516108bac`（未变） |
| 求解约束.txt | `fb073baa70562daf5d44f519a29e4b201d709074d96642add4f930a189bda7b6`（旧 `0c05976f…`） |

| 改动 | 文件 |
|---|---|
| 目录重建：用改为 71 条的 `formal_catalog.py` 回源投影；只有约束投影、求解约束来源全文与 SHA、版本变化；任务、18 类单位、18 条配方、32 项常量、19 项物料流量逐对象不变；被删的只有「1113 位置」，正文或据变化的恰为上述四条 | 数据/正式静态目录.json |
| 写死的条数 72→71 | 数据/工具/formal_catalog.py（断言与输出文字）、数据/工具/test_formal_catalog.py、数据/工具/check_revision.py、规格/check_revision.py、crates/topology/tests/validation.rs |
| 54 份内核输入（45 个样例、9 个 fixture）只换目录 SHA，其余字节不变 | 数据/样例/**、crates/kernel/tests/fixtures/* |
| 候选 B 来源清单：求解约束、目录两项按新值；候选约束.txt 一项的旧指纹自 22h 以来已过期（流水线追加第 63—80 轮、本轮改状态行），按现行字节同步 | 数据/候选B/来源清单.json |
| 正式约束 SHA | 数据/样例/check_examples.py（SOURCE_HASHES）、规格/运行语义.md（并把「72 条约束覆盖」改为 71 条） |
| 规格自查只读快照入口改到本目录 [只读文件指纹.json](只读文件指纹.json) | 规格/check_revision.py |
| 两张规则覆盖表的正式约束节按新目录重排序号、源行、`constraints[i]` 与据行，去向文字按条目名沿用；标题条数改 71 | 规格/规则覆盖表.md、数据/规则覆盖表.md |
| 条数文字 | 数据/候选B/转换说明.md |
| 追加 r26 节 | 规格/修订记录.md |
| 候选 B 报告：用本轮构建的 `target/debug/topology` 重新生成（报告写到标准输出，落本目录 [candidate-b-report.log](candidate-b-report.log)，再原样写回）。与旧报告相比只差目录版本行、「不能静态检」75→74 项、四条改写条目的原文行和删去的「1113 位置」行；4924 项通过、0 项失败 | 数据/候选B/校验报告.md、数据/候选B/验证记录.md |

所有内容与授权检查先于第一次业务写入；原字节备份在 [before/](before/)，逐文件前后 SHA 见 [sync-plan.json](sync-plan.json)、[sync-changes.json](sync-changes.json)。

## 实际命令与结果

脚本沿用 2026-09-22g：[guard.py](guard.py)（只多允许 topology 读候选 B 契约一条命令）、[work.py](work.py)（本轮同步与审计）、[run.sh](run.sh)、[safe-suite.sh](safe-suite.sh)。共享 `CARGO_TARGET_DIR=求解器/target`，默认 dev/test profile，**没有新建 profile**，所以没有要删的 `target/<profile>/`。cargo 一律 `-j 4`，Rust 测试 `--test-threads=1`。完整 argv、环境、耗时、返回码见 [commands.jsonl](commands.jsonl)。

| 步骤 | 命令（cwd：求解器） | 退出码 | 结果 |
|---|---|---:|---|
| [cargo-build](cargo-build.log) | `cargo build --workspace -j 4` | 0 | 通过 |
| [candidate-b-report](candidate-b-report.log) | `target/debug/topology 数据/候选B/contract.json` | 0 | 4924 通过、0 失败、74 不能静态检 |
| [cargo-check](cargo-check.log) | `cargo check --workspace -j 4` | 0 | 通过 |
| [cargo-clippy](cargo-clippy.log) | `cargo clippy --workspace -j 4` | 0 | 无警告 |
| [kernel-lib](kernel-lib.log) | `cargo test -p kernel --lib` | 0 | 108 通过 / 0 失败 |
| [topology-lib](topology-lib.log) | `cargo test -p topology --lib` | 0 | 1 通过 / 0 失败 |
| [kernel-reference](kernel-reference.log) | `cargo test -p kernel --test reference` | 0 | 3 通过 / 0 失败 |
| [topology-validation](topology-validation.log) | `cargo test -p topology --test validation` | 0 | 30 通过 / 0 失败 |
| [kernel-doc](kernel-doc.log) | `cargo test -p kernel --doc` | 0 | 0 项 |
| [topology-doc](topology-doc.log) | `cargo test -p topology --doc` | 0 | 0 项 |
| [catalog-verify](catalog-verify.log) | `python3 数据/工具/formal_catalog.py` | 0 | 71 条约束上下文等全部一致 |
| [catalog-regressions](catalog-regressions.log) | `python3 数据/工具/test_formal_catalog.py` | 0 | 8 项通过 |

Rust 安全测试合计 142 通过、0 失败。没有运行 `cargo test --workspace`、七个 CLI 测试目标或任何写历史证据目录的脚本；没有跑 kernel 的 seed/check/run 正反链。每条命令前后活动源码与输入的哈希都没有变化。

[审计](audit.json)（`run.sh audit`）：用新 `formal_catalog.py` 回源核目录；56 份输入／参数赋值的 catalog、轴表、配置、声明引用全部与实际文件 SHA 相符；候选 B 16 项来源全部相符；SOURCE_HASHES 与目录来源一致；运行语义含三份来源 SHA；两张覆盖表的规则 114 行、任务 16 行与约束 71 行逐行对得上；报告含全部 71 个正式条目且不再含「1113 位置」；只读指纹 4 份与现行文件一致。旧目录 SHA 与旧正式约束 SHA 的全仓 `rg --hidden --no-ignore` 检索（[after-old-sha-inventory.json](after-old-sha-inventory.json)）在活动文件中残留 0；余下命中为内核维护旧记录 2120 个、候选约束轮次 62 个、构造 35 个、候选简化轮次 9 个、几何 6 个、规则修订 5 个、老项目 3 个及规格修订记录的旧节。

## 历史证据全量哈希

| 历史目录（相对求解器） | 文件数 |
|---|---:|
| crates/kernel/evidence | 572 |
| crates/kernel/复核 | 2094 |
| 数据/复核 | 815 |

共 3481 个文件、1018807436 字节，含 1 个 gitignore 忽略文件（[清单](ignored-history-files.json)）。按路径、字节数和完整 SHA-256 比较，每条命令前后、同步与审计前后以及交付时共 29 次与初始基线比较，新增 0、删除 0、修改 0。基线 [initial-history.json](initial-history.json)，交付 [delivery-history.json](delivery-history.json)、[差异](delivery-history-diff.json)。

## 留下的事

- `求解器/构造/`（第一张全厂候选，另一条工作线）有 35 个文件引用旧目录或旧正式约束 SHA，其检查器按 72 条写的地方本轮没有动。
- `规格/运行语义.md` 第 229 行仍说「A=1113 的达标必要台数和无箱按约束 L113 核」。正式上界已降到 1110，这句的前提已经落空；它是规格里的说明文字，不在本轮只改 SHA 与条数的范围内，留给下次内核规格修订。
- 候选约束.txt 会被推导流水线继续追加，规格自查的只读指纹届时又会过期（与以往相同）。
- 上一轮记录列的既有问题（规格/check_revision.py 的 T12 断言、样例检查把桥轴对象当 Decision、两处实现状态文字）本轮未动。
