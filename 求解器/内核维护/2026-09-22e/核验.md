# 72 条约束内核同步独立核验

日期：2026-09-22。状态：核验完成，整体不通过（`pass: false`）。核验对象为工作区相对开工 HEAD `e487600c268ba905512d38f7b0b7b9cd2458db23` 的未提交同步改动；正式约束修订来自 `5b4b3e3cd990582fa381abba5dd4842cd33b0c9d`。收尾 HEAD 为 `d049026a0237a0a623d2b49a8c31a8eb99804cf7`，其并发提交仅涉及另一条线的保护目录和方案，未改变本次被核代码、目录或三处历史树，详见第 6 节。

核心入库新语义、目录与来源、输入引用、安全 Rust 测试、正向有限运行及旧 SHA 拒收均通过。本次未发现这些改动的新功能回归，也未发现运行期间历史证据变化。整体不通过的原因是现行验收入口和说明仍有已确认的不一致，交接记录的历史损失状态也已过时；不能给出“所有项目均无问题”的结论。问题中哪些是既有缺陷、哪些是记录过时，分别列在下表，不能归咎于本次矿系语义修改。

## 1. 问题与适用范围

| 编号 | 位置 | 期望、实际与证据 | 归属及影响 |
|---|---|---|---|
| I-01 | `规格/check_revision.py:115`；`规格/选择点清单.md:126–132` | 自查要求 T12 正文同时包含“操作精度”和“周期末”；当前正文两者均没有。独立读取当前文件确认该条件为假，T12 段与 HEAD 相同。旧执行日志也停在这个断言。见 [当前静态核对](verify/document-issues-evidence.json)、[既有执行日志](resume/spec-check-final.log)。 | 既有验收入口与正文失配。此次只读确认，未运行白名单之外的规格总自查，未删断言或改正文。规格总门禁不能登记为通过。 |
| I-02 | `数据/样例/runtime_example.py:112–122,262`；`数据/样例/check_examples.py:90–91,159–166` | 递归检查把含 `status` 且含 `basis` 的对象当成 Decision，要求 `status/value/basis`；合法桥轴对象却要求 `status/input_side/basis`。桥轴因此被误分到另一种结构。当前源码仍有该冲突，runtime_example.py 与 HEAD 相同；[既有样例执行日志](resume/examples.log)的失败位置正是 `root.layout.units[2].bridge_axes.horizontal`。 | 既有样例验收入口缺陷。未重跑不在允许测试清单内的 `check_examples.py --self-test`，不把旧日志作为本次运行。Rust 134 项通过不能替代这条 Python 总入口。 |
| I-03 | `规格/内核配置-v1.json:404,514`；`规格/受限模型声明.md:56,71` | `transfer.partial_acceptance.coverage_loss` 仍称 Rust 为旧实现、须迁移；说明表写已实现部分接收及台账。`gate.identity_recovery.coverage_loss` 仍称身份锁存实现待替换；说明表写已按原几何候选恢复三原因。两项实现状态文字互相矛盾。见 [原文摘录](verify/document-issues-evidence.json)。 | 当前说明一致性问题；配置文件与 HEAD 相同，不是本轮新引入的配置行为改变。此次未扩大到修改配置或裁定完整真实对应。 |
| I-04 | `内核维护/2026-09-22e/记录.md` 的“历史快照比较”“未通过项目与边界” | 记录仍称五个大文件无法恢复、gzip 新增来源未确认。当前五个文件全部与 `before-tests-history.json` 的 SHA 一致；gzip 来源可定位到已提交的 `e487600`，解压 SHA 与原 JSON 一致。当前 HEAD 已把这些大文件纳入跟踪，三处历史目录只剩一个被忽略文件。见 [基线比较](verify/baseline-comparisons.json)、[归档来源](verify/archive-addition-provenance.json)。 | 交接记录的现状说明过时，不是本次发现五个文件仍损坏。需要区分当时停止状态和当前 Git 归档状态；此次保留原记录，不回写历史。 |

以上问题未在本次修复。本次职责为独立核验；所有核验产物均在本目录的 `verify/`，结论为本文件。

## 2. 分项结论

| 项目 | 结论 | 独立证据 |
|---|---|---|
| 旧语义、旧条数的代码与文档覆盖 | 相关迁移通过；当前验收说明仍有 I-01— I-04 | 全仓检索、活动命中分类、两表逐行核对、114 个输入引用回源 |
| 核心入库语义及改名测试 | 通过 | 38 组物品/台数组合所在测试通过；另有 33 台荞花差异输入 |
| 正式目录 | 通过 | 独立执行 `formal_catalog.py` 的 verify；三源 SHA/全文、72 条原文及据一致 |
| 历史证据 | 任务规定的当前 HEAD 与测试前字节检查通过 | 3482 文件全量 SHA；既有 3481 文件与旧快照一致；额外 gzip 为已提交归档；18 条命令前后无变化 |
| 安全构建与测试 | 通过 | debug/release、check、clippy 返回 0；134 项 Rust 测试通过，doc 两包各 0 项 |
| 正向链 | 通过 | seed → check → 6 tick run → verify-record，四步返回 0 |
| 旧目录 SHA 拒收 | 通过 | 返回 2、`invalid_input`，原因准确指向目录源文件指纹不符 |

这里的通过限于被核项目。6 tick 回放、静态核心计划以及有限测试均不认证全厂达标、全部参数或全部可达循环。

## 3. 覆盖与目录

检索使用 `rg --hidden --no-ignore`，遍历工作仓库中的可识别文本；排除 `.git`、编译 `target` 和本次 `verify` 输出。没有把历史目录从检索中排除。二进制压缩件由历史哈希核验覆盖，未当成普通文本 grep。三类原始命中清单为：

- [`plant_trigger`：191 个文件](verify/plant_trigger-files.txt)。活动数据工具中的剩余命中是两个专门回归：证明新投影不含该键、证明人为塞回该键会被严格拒绝。活动 Rust 业务代码、当前目录不再消费或保留它。
- [旧条数及相关代码模式：838 个文件](verify/old_count-files.txt)。包含来源 SHA、字段索引等假阳性，不能把命中数当成缺陷数。
- [被删除句子及旧说明：215 个文件](verify/deleted_sentence-files.txt)。活动核心实现及当前目录没有残留旧许可；其余主要为旧输入、证据、提案、版本差异和归档。

活动代码/文档候选逐行记录见 [grep-active-candidates.json](verify/grep-active-candidates.json)。关键保留项经读取上下文区分如下：

- `规格/规则覆盖表.md:244` 的 56 条、15 行位于明确标注截至 2026-09-19 的旧自核记录；当前两表实际为规则 114 行、任务 16 行、约束 72 条。
- `数据/候选B/验证记录.md` 已在开头登记当前结果，并把下文标成旧轮史料；`数据/修订记录.md` 的 56 条是历史修订记录。
- 检查器 A 说明明确绑定旧 71 条、不支持当前 72 条；检查器 B 的 71 条断言位于旧版本兼容/拒收用例，另有 72 条分支。没有运行这些内核范围之外的原型自测。
- 几何模型的 71 条开工快照、三份推导的旧行号/指纹均有版本说明；新来源按条名引用。保留历史原文不等于继续用旧句执行当前核心检查。

独立目录核验命令为 `python3 -B 数据/工具/formal_catalog.py`，返回 0，实际进入 `verify()`。日志见 [stdout](verify/formal-catalog-verify.stdout.log) 和 [命令结果](verify/formal-catalog-verify.result.json)。此外没有调用它的投影函数来替代独立计数，而是另按磁盘条文及据行逐项比对，结果如下：

| 对象 | 当前 SHA-256 / 结果 |
|---|---|
| 游戏规则 | `52df4c12ce90975b861a6a663bd37873e03c9549658d200dc7e95fabc4bc29f3` |
| 求解任务 | `1630ca1febec79b324aa3afb110be2d3330298e266c68b06415c3d1516108bac` |
| 求解约束 | `a67c18dec5f6ae59c41e8620d270007f20c2d3e5b202521d0cca12f616bca3df` |
| 正式静态目录 | `bfbee742587e8e9a08c8231483bef578ee4494450447d0d783466cc28b29dd7a` |
| 目录版本 | `2026-09-22-r23-constraints-72` |
| 条数/常量 | 72 条；常量 33 → 32，仅删除 `plant_trigger` |

三个 sources 的 SHA 和完整 lines 与磁盘一致。相对 HEAD，条文/据的变化集合恰为「矿系不入库、非成品零入库、回路守恒、回路存量、植株半分、1113 位置」；task、18 类单位、18 配方、物料流量和其余常量不变。修订记录 r23 已登记正确目录 SHA。

两张覆盖表均逐行核了 114 行规则、16 行任务；两套 72 条映射的条名、源行或目录索引、据行均一致。56 份活动 kernel 输入/参数赋值内共 114 个 `path/sha256` 引用，以及候选 B 来源清单 16 个引用，均回源到现存字节。详见 [static-audit.json](verify/static-audit.json)。

本次重新运行候选 B，输出与当前 `数据/候选B/校验报告.md` 逐字节一致：4924 通过、0 失败、75 不能静态检。见 [报告比较](verify/candidate-report-comparison.json)。

## 4. 语义与差异输入

`crates/topology/src/lib.rs` 中的具名检查现在仅对目标为核心的 logical_feeds 调用 `cat.task.targets.contains_key(&e.item)`。目录目标键独立核实为高容谷地电池、精选荞愈胶囊，已没有植物类别或种植机台数的豁免。检查仍叫 `矿系不入库/计划`，说明已注明依据为「非成品零入库、入库途径、协议核心」。

`core_storage_plan_accepts_only_products_at_any_grower_count` 实际遍历 32、33 台两种配置，每种核七种植物、十种含矿非成品、两成品，共 38 组；逐项断言这一检查的状态，另断言三个正式循环义务仍为 Unknown。本次 `topology --test validation` 确实执行该测试并通过，见 [测试日志](verify/topology-validation.stdout.log)。其他检查失败不会代替它的具名断言。

另构造并保存了 [33 台种植机、荞花入核心的契约](verify/semantics-33-growers-buckflower.json)：从候选 B 克隆一台种植机并改唯一 id；把 `LF112` 的物品从精选荞愈胶囊改为荞花，保留正速率 `1/5`、目标 `CORE`。

- HEAD 旧谓词使用旧目录 `plant_trigger=32`，因 `33 != 32` 且荞花在七种植物表中，该具名检查会通过。本次提取了 [原 Rust 谓词](verify/old-core-predicate.rs.txt)，逐字对应逻辑求值；没有运行或声称运行旧版二进制。
- 新 release 二进制实际返回 1，并把 `矿系不入库/计划` 放入“能检且不通过”分节。该变异还有其他失败，结论专门定位这一行，不以总退出码替代语义核对。
- 「矿系不入库」「非成品零入库」「传输按仓库余量判定」三行位于“不能静态检”分节，未被核心计划检查提升为完整循环证书。

详见 [差异核验结果](verify/semantic-discriminator.json)、[实际新报告](verify/semantics-new-topology.stdout.log)。`crates/kernel/src` 相对 HEAD 没有业务修改；普通转移没有因本次同步加入仅成品入库守卫。

本次辅助脚本初次按行内 `| Fail |` 解析报告，因实际报告以分节表示状态而断言失败。修正为分节内精确查找检查名后，使用原始未改写 stdout 完成核对，无须重复运行 topology。旧脚本及说明分别保存在 [初版脚本](verify/explicit_checks.initial-parser.py)、[解析修正记录](verify/verifier-parser-correction.json)。这是核验脚本错误，已修正，不是被核实现失败。

## 5. 安全命令、正向链与旧 SHA

全部 Cargo 命令 cwd 为求解器，使用共享 `target`，`-j 4`；所有 Rust 测试附加 `-- --test-threads=1`。命令由 [run_guarded.py](verify/run_guarded.py) 校验清单并逐命令做历史全量哈希；实际 argv、退出码、时间和前后差异保存在 [commands.jsonl](verify/commands.jsonl)。

| 实际命令 | 退出码 | 结果 |
|---|---:|---|
| `cargo build --workspace -j 4` | 0 | debug 构建通过 |
| `cargo build --workspace --release -j 4` | 0 | release 构建通过 |
| `cargo check --workspace --all-targets -j 4` | 0 | 通过 |
| `cargo clippy --workspace --all-targets -j 4` | 0 | 通过，日志无警告 |
| `cargo test -p kernel --lib -j 4 -- --test-threads=1` | 0 | 100 通过 |
| `cargo test -p topology --lib -j 4 -- --test-threads=1` | 0 | 1 通过 |
| `cargo test -p kernel --test reference -j 4 -- --test-threads=1` | 0 | 3 通过 |
| `cargo test -p topology --test validation -j 4 -- --test-threads=1` | 0 | 30 通过 |
| `cargo test -p kernel --doc -j 4 -- --test-threads=1` | 0 | 0 项 |
| `cargo test -p topology --doc -j 4 -- --test-threads=1` | 0 | 0 项 |

没有运行 `cargo test --workspace` 或七个禁用 CLI 测试目标。允许的 topology validation 目标内部会调用已有 Python 目录回归；这属于该白名单目标原有执行内容，执行前已读其入口，历史快照无变化。未另行运行任何其他测试命令。

正向输入来自 `数据/样例/任务7内核/无线多格全收.json`，副本与所有输出均写到 `verify/`。配置为 `规格/内核配置-v1.json`，使用本次 release 构建的 kernel，二进制 SHA 为 `c4208ba9c40292e0bfd0443e8098e5c2d2a33c3c79c201c341e29ba3e0643c06`。

| 步骤 | 输入/输出 | 退出码与状态 |
|---|---|---|
| seed | `positive-input.json` → `positive-seed.json` | 0；输出 schema 为 kernel-input-v3 |
| check | `positive-seed.json` | 0，input_checked |
| run --ticks 6 | `positive-seed.json` → `positive-record.json` | 0，completed |
| verify-record | `positive-record.json` | 0，input_checked |
| 旧 SHA seed | `old-sha-input.json` | 2，invalid_input；目录源文件指纹不符 |

旧 SHA 直接取自 `git show HEAD:求解器/数据/正式静态目录.json` 的原始字节，值为 `6e609fbab15104489f7e00a03c64b3f89cd77668de26789622d78be27a0ee3ff`。负例只改变正例副本的 `catalog.sha256`，没有混入其他坏引用。拒绝原因确实指向 `数据/正式静态目录.json`，不是其他输入失败。完整结果见 [positive-chain-summary.json](verify/positive-chain-summary.json)。

## 6. 历史证据及只读边界

本次开工快照覆盖三处目录全部文件，包括忽略文件，合计 3482 个、1,021,912,489 字节：

| 目录 | 文件数 |
|---|---:|
| crates/kernel/evidence | 573 |
| crates/kernel/复核 | 2094 |
| 数据/复核 | 815 |

普通 `git status --short --untracked-files=all` 在这三处为空。这里的“相对 HEAD 无改动无新增”与“比早期文件清单多一个 gzip”是两个不同的比较：

1. 相对原 `before-tests-history.json` 和 `resume/initial-history.json`，既有 3481 个文件均保持 SHA，一项删除或修改都没有；额外文件只有 `crates/kernel/复核/r3-测试与证据/密集制造闭环序3核验-重跑周期.json.gz`。
2. 该 gzip 是提交 `e487600` 保存的归档副本，11,867,571 字节，SHA 为 `838f8c2dee07a909b5ec91cca51f3a0c38b637a76b956a3c06782ce3b16165a5`。其解压 SHA 与原 JSON 都为 `77787d90d936143ccca1ae021242bd201229e8e9cd6b28f6b82531e1bafb00af`。原 JSON 是当前三处目录唯一的忽略文件，且与测试前快照一致。
3. 相对 `resume/delivery-history.json`，当前 3482 个文件完全一致。
4. 处置清单所列五个原忽略大记录，现在全部已被跟踪，SHA 均与原测试前快照相符。这里只证明与所给快照一致，不推定更早、未纳入该快照的历史损失已全部修复。

开工差异最初触发了暂停，原始 [preflight-summary.json](verify/preflight-summary.json) 保留 `hard_stop: true`。只读查明已提交归档来源、解压内容和当前 HEAD 状态后，按任务指定的两种基线分别判定，记录 [暂停解除依据](verify/history-preflight-resolution.json)，才开始运行。没有删除 gzip、覆写历史或重置旧快照来取得通过。

全部 18 条受守卫命令的 before/after 快照与本次开工快照一致，增、删、改均为 0。最终全量结果见 [final-integrity.json](verify/final-integrity.json)；原始快照与跨轮比较见 [initial-history.json](verify/initial-history.json)、[baseline-comparisons.json](verify/baseline-comparisons.json)。当前 Git 状态另存 [history-git-status-final.txt](verify/history-git-status-final.txt)。

收尾辅助检查另发现 HEAD 发生变化，因而其“HEAD 未变”断言返回 1；历史文件与保护源哈希检查均通过。只读比较确认新提交 `d049026` 的 788 个路径全部属于 `内核维护/2026-09-22d/` 或 `内核维护/代码体检方案.md`，恰为另一条线的范围，未改变本次被核代码、正式目录、输入或三处历史树。完整路径清单见 [head-change-audit.json](verify/head-change-audit.json)。原 `final-integrity.json` 中 `HEAD_unchanged: false` 保留，不改成 true；该并发提交不算本次操作，也不以它作为重新运行测试的理由。

本次没有改三份正式文件、候选约束、代码体检方案、2026-09-22d、会议归档或历史证据；没有 git add/commit/restore，没有运行自动恢复脚本。业务工作区保留原样，只新增核验脚本、输入、日志、快照和本报告。三份正式文件及候选的开工指纹见 [protected-sources.json](verify/protected-sources.json)，收尾再次比较结果见 final-integrity。

## 7. 交付判定

`pass: false`。迁移相关功能检查可以按本报告列出的范围采信；“现行说明与所有验收入口无问题”不能采信。I-01— I-03 仍待各自维护范围处理，I-04 须在后续交接中按当前字节和 Git 版本描述。没有把这些既有问题写成新语义回归，也没有用本次安全测试通过覆盖它们。

完整核验报告路径：`/home/zhuran24/zmd-research-fresh/求解器/内核维护/2026-09-22e/核验.md`。
