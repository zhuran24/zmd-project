# 内核代码体检与增量维护方案

日期：2026-09-22。状态：待实施。实施范围为 `crates/kernel`、`crates/topology` 及其活动测试和数据工具；第 0 批先保存保护清单、恢复可恢复历史并独立提交，再改造测试隔离。本文各批验收均为待执行要求；方案审查完成不代表恢复、改码或回归已完成。已有实测结果注明核实报告及适用时点。

内核用于对固定布局、显式参数和受限运行模型作执行、回放与诊断。维护的完成标准是回归可靠、记录来源可核验、候选能通过稳定接口接入，以及实际使用中的资源成本可控。`diagnostic_cycle` 保持其诊断等级；真实调度对应和全称覆盖仍按各自的证明义务处理。

以下文件路径相对 `/home/zhuran24/zmd-research-fresh/求解器`，文档链接相对本文件；裸 Rust 文件名指 `crates/kernel/src/`，`topology::` 指 `crates/topology/src/lib.rs`。R1–R5 的活动代码基线为 `5574911`；实施前重新登记当前 HEAD、工作区字节和活动目标。

## 1. 两条工作线与批次顺序

| 工作线 | 工作内容 | 验收标准 |
|---|---|---|
| **结构等价线** | 搬函数、搬测试、缩小内部职责、局部内部类型化、经证明的冗余代码清理 | 输入接受域、首个 Stop、逐刻事件与完整状态、物料账、周期键、CLI 契约和证据等级严格等价；只有按 §3.5 程序登记的来源映射和测量字段采用专门比较；公开 API 及原始 JSON 字节契约冻结。另附分支、提交点和缓存失效顺序对照。 |
| **缺陷修复/API 加固线** | 周期键入口一致性、公开可变状态收口、构建来源绑定、恢复接口及新增资源预算 | 每项有最小反例、目标行为、调用方/协议迁移表和回归断言。反例须表现出预定变化；变更范围外继续通过结构等价标准。预期行为差异单列为变更契约，计时/来源白名单只处理比较表示。 |

第 0 批是基础设施与历史恢复批：只改变测试输出管理和已登记的恢复目标，不改变生产实现、公开 API 或运行时验证脚本。其专用验收见 §3.5。生产维护的一个实施单元只采用一条线的验收契约。修复通过后冻结新基线，后续结构搬移从该基线比较。内部算法优化采用结构等价线的语义验收，并追加资源验收；新增参数、停止结果或文件协议采用加固线。

结构线冻结所有公开可达 `pub` 项的存在、签名、可见性及 serde 形状；仓库内无调用者不构成删除公共 API 的依据。公共项删除、可见性收窄和对外类型替换进入加固线，附调用方迁移表。§4.3 来源协议验收前，不新增、删除、改名非测试编译源文件，函数抽取限于原文件内。

结构差分固定同一输入、配置、三份正式源、规格语义文件、正式目录及编译期嵌入副本、Python/Node/AJV 等验证依赖、起点和预算；每批使用 §3.4 的冻结来源树。逐层比较：

| 观察层 | 比较内容 |
|---|---|
| 装载 | 接受/拒绝与首个 Stop 的全部字段，包括多错误输入的首错次序 |
| 逐刻运行 | 事件次序、完整 StateSeed、库存/进度/轮询/门/冷却/pending、物料台账及 summary |
| 记录与恢复 | full/delta 落盘字节契约及解码后的完整记录、检查点、完整前缀、恢复后的后续轨迹 |
| 周期与证据 | 精确键、候选确认/回放、实际入库增量、预算停止、basis、证据等级和未完成义务 |
| 外部接口 | Rust API 的可用性与结果、CLI 退出码、stdout/stderr、文件集合/格式及路径解析 |

有限语料差分与读写顺序审查共同构成结构批验收；覆盖范围随输入清单交付。

| 批次 | 主要工作 | 对外行为变化 | 完成门槛 |
|---|---|---|---|
| **第 0 批：安全测试与基线** | 保护清单、历史恢复与独立提交、隔离输出、冻结来源与产物、A/A、字段白名单 | 测试产物的位置、生命周期和测试诊断日志改变；历史文件恢复为指定原字节；内核运行语义保持 | 恢复逐项对账、连续两轮安全回归、保护清单与编译闭包通过、同路径同 profile 的生产二进制字节相同、A/A 通过 |
| **第 1 批：状态边界与来源绑定** | 受校验的候选/种子入口、API 收口、周期键一致性、构建身份 | Rust 调用方式、非法状态处置、记录来源协议按各子批契约改变 | R1/R2 反例及 R5 停止态（§4.2）回归通过，合法轨迹等价，迁移调用方编译通过；跨文件结构抽取前通过 §4.3 |
| **第 2 批：内部责任划分** | 静态模型、动态状态、缓存、审计、停止状态的读写边界 | 保持既有外部行为 | 读写/失效/失败点可逐项对应，机制性质检查与差分通过 |
| **第 3 批：长函数与测试搬迁** | 有维护收益的函数抽取、按机制组织测试、按入口证据清理代码 | 保持既有外部行为；测试全名按双射迁移 | 全量语义差分、测试映射和断言保真通过 |
| **第 4 批：资源预算与实测优化** | 长跑测量、事件身份保留、记录和周期搜索预算 | 等价优化保持行为；新增预算/流式协议分别声明变化 | 语义安全网与预登记资源目标同时通过 |
| **可选整理批：索引与快照归档** | 活动代码索引、确有收益的快照整树归档 | 归档路径按显式映射变化，活动接口保持 | 引用解析、全字节校验和安全回归通过 |

实施优先解决实际损害及候选接入障碍。第 1 批先固定候选所需输入/恢复契约，再按交付记录实际依赖的边界实施 API 与来源修复。第 2–4 批按表推进有具体需求的子项；纯整理按维护收益安排。几何研究通过受校验的输入适配器独立推进。

## 2. 执行环境、输出目录与共用入口

全部编译使用共享 `CARGO_TARGET_DIR=/home/zhuran24/zmd-research-fresh/求解器/target`。运行目录只保存脚本、探针源码、日志、JSON、Markdown；源码树实拷只放 `/tmp`，编译产物只放共享 target。测试进程串行；Cargo jobs=2、codegen-units=1，测试及计算池单线程，同时在算的不超过 6 核。核查看实际在跑的线程（运行态线程数或 CPU 时间÷墙钟），rustc、LLVM、jobserver 等在等待的辅助线程不计：rustc 在 jobs=1 时也会开到 8—11 个线程，大多空闲（2026-09-23 第 0 批实测）。保存 CPU affinity、实际进程/线程配置；构建工具若超预算须进一步限并发。

以下 Bash 块均保存为脚本，以 `bash -euo pipefail /绝对路径/脚本.sh /绝对路径/env.sh` 执行，适用于从 fish 或新的工具 shell 启动。初始化脚本不接收 env.sh；后续块首行加载同一份环境文件，不依赖前一次 shell 的变量。待实现的运行器接口在正文明确标明，不能在工具尚未实现时当作可执行命令。

初始化脚本（保存为 `/tmp/kernel-health-init.sh`，执行 `bash -euo pipefail /tmp/kernel-health-init.sh`）：

```bash
set -euo pipefail
cd /home/zhuran24/zmd-research-fresh/求解器
export HEALTH_REPO="$PWD" HEALTH_SRC="$PWD"
export HEALTH_PROFILE="healthsafe$(date +%s)$$"
export CARGO_TARGET_DIR="$PWD/target"
export CARGO_BUILD_JOBS=2 CARGO_PROFILE_DEV_CODEGEN_UNITS=1
export CARGO_PROFILE_TEST_CODEGEN_UNITS=1 CARGO_PROFILE_RELEASE_CODEGEN_UNITS=1
export RUST_TEST_THREADS=1 RAYON_NUM_THREADS=1 OMP_NUM_THREADS=1
export OPENBLAS_NUM_THREADS=1 PYTHONDONTWRITEBYTECODE=1 GIT_OPTIONAL_LOCKS=0
export HEALTH_RUN
HEALTH_RUN=$(mktemp -d "$PWD/内核维护/体检-$(date +%Y%m%d-%H%M%S)-XXXXXX")
export KERNEL_TEST_EVIDENCE_DIR="$HEALTH_RUN/cargo-test-evidence"
mkdir "$KERNEL_TEST_EVIDENCE_DIR"
python -B - <<'PY'
import os, shlex
from pathlib import Path
keys = ['HEALTH_REPO','HEALTH_SRC','HEALTH_RUN','HEALTH_PROFILE','CARGO_TARGET_DIR',
        'CARGO_BUILD_JOBS','CARGO_PROFILE_DEV_CODEGEN_UNITS',
        'CARGO_PROFILE_TEST_CODEGEN_UNITS','CARGO_PROFILE_RELEASE_CODEGEN_UNITS',
        'RUST_TEST_THREADS','RAYON_NUM_THREADS','OMP_NUM_THREADS',
        'OPENBLAS_NUM_THREADS','PYTHONDONTWRITEBYTECODE','GIT_OPTIONAL_LOCKS',
        'KERNEL_TEST_EVIDENCE_DIR']
path = Path(os.environ['HEALTH_RUN']) / 'env.sh'
path.write_text(''.join('export '+k+'='+shlex.quote(os.environ[k])+'\n' for k in keys))
print(path)
PY
rustc -Vv > "$HEALTH_RUN/rustc.log"
cargo -V > "$HEALTH_RUN/cargo.log"
cargo metadata --locked --offline --no-deps --format-version 1 > "$HEALTH_RUN/cargo-metadata.json"
git -C .. rev-parse HEAD > "$HEALTH_RUN/head.log"
git -C .. -c core.quotePath=false status --short > "$HEALTH_RUN/worktree.log"
```

工具链、Python/Node/AJV、GNU time、`sha256sum` 的解析后路径、版本与文件哈希进入环境清单。离线依赖缺失先单独补齐并登记，再执行固定命令。

第 0 批拟新增两个工具，命令契约如下：

| 拟新增文件 | 职责与输出 |
|---|---|
| `数据/工具/kernel_file_guard.py` | `snapshot --root .. --out <json>` 记录文件、目录（含空目录）、链接、权限、大小、SHA-256 和链接目标；`verify --before <json> --out <json>` 核新增、删除、改写。覆盖跟踪、未跟踪及忽略项。 |
| `数据/工具/kernel_regression.py` | `tests`、`aa`、`capture`、`compare` 按 cases/binaries/fields 清单执行；保存 argv、cwd、环境、退出码、stdout/stderr、产物及首个差异；核源码闭包、构建来源和隔离验收凭据。 |

恢复前的保护清单不依赖这两个工具实现，可使用经只读审查的一次性盘点脚本。从仓库根盘点三份正式源、规格、源码、样例、fixture、黄金、历史证据和已有维护目录；只排除明确登记的 target、Git 内部、工具缓存和本次输出根。目录及忽略文件同样受保护。源码编辑、历史恢复和测试运行各自建立检查点；测试允许写入仅为本次新建空输出目录及专属 target 临时目录。其他会话的变化单列前后哈希及归属依据，不自动回退。

测试产物根固定为 `$HEALTH_RUN/cargo-test-evidence`，其名字匹配已有 Git ignore 规则；仍须实际用 `git check-ignore` 核验，不对该目录使用强制添加。大型 A/A、A/B 和性能原始产物放共享 target 的专属目录，运行目录保存索引、大小、哈希及摘要，保留原字节供复核。可提交范围见 §9.3。

代码问题先查询 CodeGraph，`projectPath=/home/zhuran24/zmd-research-fresh/求解器`；按活动 Cargo 模块图筛选，缺失部分才补读。每批保留实际编译及 Clippy 诊断，新告警逐项处理。

## 3. 第 0 批：恢复历史、隔离测试与冻结基线

### 3.1 保存保护清单后，首先恢复可恢复历史

本步骤先于测试隔离改码。开始即保存保护清单、当前工作区差异、R3 恢复清单及源材料的 SHA-256；暂停会写恢复目标的任务，按 §8.0 限制回归。恢复只读取已核实的 Git blob/匹配副本，不运行内核或测试。恢复完成后独立提交，再进入 §3.2。恢复提交只包含逐项登记的恢复路径、来源说明、缺失标记和 journal，不夹带活动重锁或源码改动。

依据为 [restore_manifest.json](2026-09-22d/R3-测试写历史/restore_manifest.json) 的 `files`。当日 `7da52a7` 没有 evidence 变更；`a7539f5` 改写 159 个旧文件并新增 7 个，`220f7b6` 再改其中 106 个旧文件及 5 个新增文件。审查基点 `5b4b3e3` 的 HEAD 仍保留受损字节，不能用 HEAD 或 `220f7b6^` 作为统一恢复点；以后须记录恢复提交的完整 ID。

| 集合 | 处置和验收 |
|---|---|
| 159 个入库旧文件 | 从逐项 `source_commit=f8f6129` 恢复完整 blob；`equivalent_commit=7da52a7` 字节相同，按 `expected_sha256` 验收。目录计数为 revision-r2 2、revision-r4/cli 23、round6/cli 12、round6/regressions/revision-r3 79、round6/revision-r5/cli 43。 |
| 7 个当天新增文件 | 保留现场字节及来源；提取 a7539f5、220f7b6 的两个版本，保持完整相对路径。实施恢复时将历史目录里的新增路径移出，归到对应重锁运行档案；不冒充旧轮次证据。 |
| R3 时未跟踪的 14 个漂移 | `pre_day_hash_drift.json` 中原 `tracked=false` 的目标以 `before_sha256` 为准，实施前重核现时跟踪身份。1 项有同哈希副本；其余 13 项状态为“原字节缺失”，不能用重生成结果冒充恢复。 |
| 首次已恢复的 3 个 round5 文件 | 对照 `2026-09-22/early-test-output-restoration.json` 核原哈希；它们直接位于 round5 下，与新增 round5/round5_cli 不同。 |

恢复执行器须实现以下完整事务步骤，并把每一步及异常写进 `restore-journal.json`：

1. 用 `git ls-files --error-unmatch -- <path>` 确认 159 项仍被跟踪；对 R3 时未跟踪的 14 项同时执行 ls-files 和 `git check-ignore -- <path>`，记录当前跟踪/忽略状态。若后续已入库，核新增提交与字节后按当前跟踪文件处置，不要求它继续被忽略；入库不改变原始目标哈希。根路径、路径穿越、符号链接、缺失目标、源 blob 哈希不符都停止；这些错误不能当作“意外当前哈希”放行。
2. 核全部源 blob、目标哈希及等价提交。已知未跟踪恢复目标为 `crates/kernel/evidence/round6/revision-r5/cli/分流器三路轮询-absolute.json`，来源是 `数据/样例/分流器三路轮询-运行记录-v3-kernel.json`；实际字节必须匹配 `before_sha256`。
3. 读取全部目标的当前字节，当前哈希与旧审计值不同只记 `unexpected_current`，不因此阻止有确定来源的恢复。先将所有现场字节按原相对路径备份到本次 `cargo-test-evidence/history-before-restoration/`；包括 13 个缺失原件路径及 7 个新增路径。校验备份哈希后才能开始覆盖。
4. 在保持恢复目标单写者的前提下，每次写前重读并断言 `p.read_bytes() == now`；发现并发变动立即停止。用目标同目录的唯一临时文件写入已核源字节，保留权限、flush/fsync，重新核当前字节后 `os.replace` 原子替换，核替换后完整哈希。journal 也采用临时文件加替换；记录 pending/completed/error，使中断后可按目标哈希核销，不宣称整批原子完成。
5. 13 项在所查范围内未找到原字节。按原大小查 `/home`、`/tmp`、`/var/tmp`、`/mnt`、`/media`、`/opt`、`/srv`，并查四个本地 Git 对象库：本仓库、`/home/zhuran24/zmd-project-cc`、`/home/zhuran24/.claude/skills`、`/home/zhuran24/文档/ChatGPT/New project`。原目标见 [查找清单](2026-09-22d/审查-opus/unresolved_targets.json)，2026-09-22 再查见 [核验结果](2026-09-22d/审查处理核验/extra-facts.json)：可访问范围与四库均未命中这 13 项；find 因部分 systemd 私有目录无权限及 /media 不存在返回 1，且使用 `-xdev`，不构成全机穷尽证明。已知可恢复的第 14 项另有同哈希 Git blob。旧记录 36 个来源指纹中有 20 个不等于 f8f6129 同路径 blob，不能把该提交重跑当作原字节恢复依据。
6. 实施恢复时由 owner 选择 13 项现存替代字节原地保留或移入本次归档；两种方式均在相关原目录新增 `原字节缺失.json`，逐路径写原 SHA-256、原字节数、现存 SHA-256、处置位置和查找依据。原大小取 `2026-09-22/before.json`，与 opus 查找清单交叉核对。已知同哈希来源的 1 项如仍未入库，是否强制入库单独决定；已经入库则不重复提出该选择。两者均不阻塞按哈希恢复。
7. 恢复与标记验收通过后，只暂存清单列出的路径，核暂存差异及全部大文件，再单独提交，提交说明引用 journal。现有 post-commit 会启动推送，实施者须按当前会话授权范围安排提交。记录提交 ID 和提交后复核结果，之后才开始隔离改码。

七项版本提取脚本只向新输出目录写入（保存为运行目录的 `extract-relock-versions.sh`，用 §2 的 Bash 调用方式）：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
for c in a7539f5 220f7b6; do
  for rel in round5/round5_cli/{invalid-cycle-input,invalid-cycle-result,relocated-seed,resource-statistics}.json \
             round6/revision-r5/cli/bounded-reference-batch-unsupported.log \
             round6/revision-r5/cli/bounded-reference-package/{分流器三路轮询,混做粉碎机两下游}-reference.json; do
    dest="$HEALTH_RUN/cargo-test-evidence/relock-history/$c/$rel"
    test ! -e "$dest"
    mkdir -p "$(dirname "$dest")"
    git -C "$HEALTH_REPO/.." show "$c:求解器/crates/kernel/evidence/$rel" > "$dest"
    sha256sum "$dest"
  done
done
```

a7539f5 版归 `内核维护/2026-09-22b/cargo-test-outputs/<evidence 相对路径>`，220f7b6 版归 `内核维护/2026-09-22c/cargo-test-outputs/<evidence 相对路径>`；provenance 记录原路径、提交、哈希、大小。当前现场若又有第三种字节则留在本次运行目录，不能误标为 c 版。历史原路径的移出和两个版本的提取是不同操作，一次 `git mv` 不能取得两个历史版本。这些小型历史归档按 §9.3 逐文件审查入库。

**恢复验收：**159+1 个可恢复目标完整哈希匹配，7 个新增项脱离旧轮次目录且两个提交版本可定位，原三份 round5 恢复哈希仍相同，13 项缺失有原目录标记和完整清单。`history_recovery=partial` 可与 `regression_ready=true` 并存，后者还要求新基线和活动测试不依赖缺失原字节。旧件原字节缺失不因测试通过而核销。

### 3.2 统一测试输出并验收副本来源

恢复提交完成后改造七个活动 Cargo CLI 入口：

| `crates/kernel/tests/` 入口 | 原写出位置 |
|---|---|
| `round5_cli.rs` | `evidence/round5/round5_cli/` |
| `revision_cli.rs` → `revision_cli.py` | `evidence/revision-r2/prior-regression-results.json` 及过程目录 |
| `revision_r2_cli.rs` → `revision_r2_cli.py` | `evidence/revision-r2/regression-results.json` 及过程目录 |
| `revision_r3_cli.rs` → `revision_r3_cli.py` | `evidence/round6/regressions/revision-r3/` |
| `revision_r4_cli.rs` → `revision_r4_cli.py` | `evidence/revision-r4/cli/` |
| `revision_r5_cli.rs` → `revision_r5_cli.py` | `evidence/round6/revision-r5/cli/` 及材料包 |
| `round6_cli.rs` → `round6_cli.py` | `evidence/round6/cli/` |

路径台账见 [R3 §3](2026-09-22d/R3-测试写历史.md)。六个 Python 入口目前忽略 `KERNEL_TEST_EVIDENCE_DIR`；只设置变量不能隔离它们。

新建 `crates/kernel/tests/support/mod.rs` 与 `crates/kernel/tests/evidence_paths.py`，统一为每测试、每执行实例分配新空目录。输出根白名单只有本次 `HEALTH_RUN` 下的新空目录，以及共享 target 下本次专属测试临时根；其余拒绝。解析绝对路径、真实路径和所有祖先链接，不能通过符号链接逃回历史目录。Rust 显式把实例目录传给 Python；缺省只允许共享 target 的唯一临时目录，失败保留并报告。日志、变异输入、附带 record、材料包及子进程全部遵守同一边界。

Python 主体放显式 `main`，import 无写入；只读输入与输出使用不同变量。`revision_r3_cli.py` 的 `gate-expired-after-closure-input.json`、`tiny-expired-cycle.json` 在运行主体前必须存在并匹配登记哈希；缺失硬失败，禁止 `if exists` 静默减覆盖。若为归档而迁移，按原字节复制到只读 fixtures 并保存来源映射、两组负例名和计数。

按 [活动写点清单](2026-09-22d/R3-测试写历史/active-source-writers.md)核全部子进程。`benchmark_round6.py --out-dir` 仍写样例，进入 suite 前迁移该副作用；黄金生成器只使用已核只读函数。`test_formal_catalog.py` 的临时正式源副本放 `/tmp`，编译仍用共享 target。第 0 批禁止修改生产 `src`、`tests/verify_all.py`、`tests/audit_task7.py` 及其实际导入依赖；确需修改运行时验证逻辑，另立行为批并重新建立 A。两份已跟踪 pyc 单列缓存清理，不混进语义变更。

在 `/tmp` 实拷中先验收，副本不得有符号链接，也不得缺少被忽略但活动测试需要的只读资源；复制资源逐项登记哈希。先构建测试但不运行（脚本第二参数是副本求解器绝对路径）：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
export HEALTH_COPY="${2:?传入 /tmp 副本求解器根}"
cd "$HEALTH_COPY"
case "$PWD/" in /tmp/*/) ;; *) exit 2 ;; esac
export HEALTH_PROFILE="healthtest$(date +%s)$$"
cargo test --locked --offline --workspace --tests --no-run -j 2 \
  --profile "$HEALTH_PROFILE" --config "profile.$HEALTH_PROFILE.inherits=\"dev\"" \
  --config "profile.$HEALTH_PROFILE.codegen-units=1" --message-format=json \
  > "$HEALTH_RUN/test-build.jsonl.log" 2> "$HEALTH_RUN/test-build.stderr.log"
python -B - <<'PY'
import json, os
from pathlib import Path
run, root = Path(os.environ['HEALTH_RUN']), Path(os.environ['HEALTH_COPY']).resolve()
rows = [json.loads(s) for s in (run/'test-build.jsonl.log').read_text().splitlines() if s.startswith('{')]
artifacts = [r for r in rows if r.get('reason') == 'compiler-artifact'
             and r.get('executable') and r.get('profile', {}).get('test')]
assert artifacts, 'no test harnesses'
for r in artifacts:
    assert r['fresh'] is False, r
    assert Path(r['manifest_path']).resolve().is_relative_to(root), r
    assert Path(r['target']['src_path']).resolve().is_relative_to(root), r
(run/'test-harnesses.json').write_text(json.dumps(artifacts, ensure_ascii=False, indent=2)+'\n')
PY
```

运行器先核 `test-harnesses.json` 与 metadata 目标集合完全对应、内嵌路径/dep-info/二进制哈希均来自副本，另核被调用 kernel bin 的构建来源；随后直接执行清单中的 harness 绝对路径，先 `--list` 留档，再 `--test-threads=1`，不让第二次 Cargo 调用重新选择产物。实际 Python `__file__`、ROOT 及读取路径须在副本或已登记外部依赖内。首次构建任何 harness 为 Fresh、缺失或指回原仓库都拒绝运行。

**验收：**显式根、无配置、两轮重复、两个实例并发（同时在算的不超过 6 核）、不同 cwd、中文/空格路径、子进程失败及非法根拒收；每次核包括空目录和忽略项的保护清单。`isolation-accepted.json` 记录通过的用例、源/脚本/运行器/依赖哈希、profile、所有 harness 与 bin 来源、输出落点及保护结果。正式 CLI suite 执行前重核这些绑定，文件存在本身不足以验收。路径或实现变化使凭据失效，须重新验收。原仓库入口只在相应源码版本的隔离验收完成后运行。

### 3.3 分开安全命令与受守卫的 CLI suite

下列安全脚本只包含经写点核查的 build/check/Clippy、库测试、reference、topology validation 和 doc。从冻结根或登记的稳定工作区执行；源码/依赖变化后重新核写入边界。运行器逐命令保存退出码和输出，失败即停；预期非零的 CLI 反例由运行器显式核预期码。

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
cd "$HEALTH_SRC"
health_profile_args=(--profile "${HEALTH_PROFILE:?缺少本阶段独立 profile}"
  --config "profile.$HEALTH_PROFILE.inherits=\"dev\""
  --config "profile.$HEALTH_PROFILE.codegen-units=1")
cargo build --locked --offline --workspace -j 2 "${health_profile_args[@]}"
cargo check --locked --offline --workspace --all-targets -j 2 "${health_profile_args[@]}"
cargo clippy --locked --offline --workspace --all-targets -j 2 "${health_profile_args[@]}" --message-format=json
cargo test --locked --offline -p kernel --lib -j 2 "${health_profile_args[@]}" -- --test-threads=1
cargo test --locked --offline -p topology --lib -j 2 "${health_profile_args[@]}" -- --test-threads=1
cargo test --locked --offline -p kernel --test reference -j 2 "${health_profile_args[@]}" -- --test-threads=1
cargo test --locked --offline -p topology --test validation -j 2 "${health_profile_args[@]}" -- --test-threads=1
cargo test --locked --offline -p kernel --doc -j 2 "${health_profile_args[@]}" -- --test-threads=1
cargo test --locked --offline -p topology --doc -j 2 "${health_profile_args[@]}" -- --test-threads=1
```

七个 CLI 入口使用另一个脚本；其中 `check-isolation` 是第 0 批运行器须实现的凭据核验接口：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
cd "$HEALTH_SRC"
test -s "$HEALTH_RUN/isolation-accepted.json" || exit 2
health_profile_args=(--profile "${HEALTH_PROFILE:?缺少本阶段独立 profile}"
  --config "profile.$HEALTH_PROFILE.inherits=\"dev\""
  --config "profile.$HEALTH_PROFILE.codegen-units=1")
python -B 数据/工具/kernel_regression.py check-isolation \
  --acceptance "$HEALTH_RUN/isolation-accepted.json" --source-root "$HEALTH_SRC" --profile "$HEALTH_PROFILE"
for health_test in revision_cli revision_r2_cli revision_r3_cli revision_r4_cli revision_r5_cli round5_cli round6_cli; do
  cargo test --locked --offline -p kernel --test "$health_test" -j 2 "${health_profile_args[@]}" -- --test-threads=1
done
```

每个源码物理根/版本只用其本阶段独立 profile；切换根或版本时重建绑定，不能沿用上阶段 env.sh 中的 profile。共享 target 的副本测试按 §3.2 直接运行已验收 harness；上述 CLI Cargo 循环用于原仓库的已验收入口。两个脚本互不串接，禁止退回共享默认 profile 或以全工作区测试命令替代。

按 metadata 枚举所有 harness，保存 `--list` 和本次实际通过数。历史计数 kernel lib 100、topology lib 1、reference 3、topology validation 30、CLI 7，合计 141；134 是去掉七个 CLI 的历史口径。两个 bin harness 的零测试及 doc 也要按本次列举记录，新增目标先核副作用再纳入。

`reference.rs` 的三个差分测试覆盖粉碎机 4 刻、分流器 12 刻和现场 Python 重算；手工黄金按原字节保护。来源篡改与负例属于 §3.3 的 suite 整体，包括库测试及 CLI，不能归到 reference 的三项上。

### 3.4 必须冻结构建与运行时来源

每批从明确的已提交 commit，以 `git archive` 读取 Git 对象，在 `/tmp` 建最小完整冻结树，包含项目根三份正式源及求解器资源。只按清单抽取必需路径，不归档整棵历史仓库；所需未跟踪 fixture 或本批选定的未提交正式源按登记哈希实拷覆盖，A/B 使用同一覆盖集。提交 ID 不是工作区内容的替代标识。采用 archive 不创建 worktree，不改原仓库 Git 元数据；副本不含符号链接或编译产物。

逐个解析输入、配置及证据中的引用，确认实际读取落在冻结树或明确绑定的外部验证依赖内；仍指回活动工作区的绝对引用须先重定位并登记路径映射，不能以“文件已复制”代替读取来源闭合。A/B 的重定位规则和对应资源字节保持一致。

冻结范围必须同时包括：输入和配置、三份正式源、全部消费的规格/轴表/schema/证明来源、正式静态目录及 include 嵌入资源、样例/fixture/黄金、Cargo.toml/lock、features/cfg/构建参数、活动 kernel/topology 编译闭包、`tests/verify_all.py`、`tests/audit_task7.py` 及其导入的样例模块、Python/Node/AJV 实际依赖闭包、外部 `sha256sum`。AJV 使用绝对路径，至少核入口、依赖文件及 package 版本；外部可执行文件保存解析后的路径/版本/哈希，在每次执行前后核其未变。仅保存版本号不等于冻结。

可以在同一 `/tmp` 物理根顺序完成 A 和 B：先放 A 源码，构建、运行整条 A 链并封存其输出和验证回执；之后仅替换清单登记的 crates 源码为 B，再构建并完成 B 链。非源码资源保持相同，测试基础设施改动另列。A 源码被替换后禁止再次运行 A；若需重跑先按完整清单还原 A 上下文并核哈希。持续交替运行则使用两个独立冻结根，各自构建、绑定来源并登记路径映射，不能让旧 A 在 B 源码根“签来源”。两种方式均使用唯一 profile 和同一共享 target。

构建脚本接受 env.sh 和本阶段冻结求解器路径，生成并持久化 `HEALTH_BIN`/`HEALTH_TOPOLOGY_BIN`（每阶段保留 env.sh 副本和构建日志，再开始下一阶段）：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
export HEALTH_SRC="${2:?传入本阶段 /tmp 冻结求解器根}"
cd "$HEALTH_SRC"
case "$PWD/" in /tmp/*/) ;; *) exit 2 ;; esac
export HEALTH_PROFILE="health$(date +%s)$$"
cargo build --locked --offline --workspace --bins -j 2 --profile "$HEALTH_PROFILE" \
  --config "profile.$HEALTH_PROFILE.inherits=\"dev\"" \
  --config "profile.$HEALTH_PROFILE.codegen-units=1" --message-format=json \
  > "$HEALTH_RUN/build-artifacts.jsonl.log" 2> "$HEALTH_RUN/build.stderr.log"
python -B - <<'PY'
import hashlib, json, os, shlex
from pathlib import Path
run, root = Path(os.environ['HEALTH_RUN']), Path(os.environ['HEALTH_SRC']).resolve()
rows = [json.loads(s) for s in (run/'build-artifacts.jsonl.log').read_text().splitlines() if s.startswith('{')]
bound = {}
for name, key in [('kernel','HEALTH_BIN'), ('topology','HEALTH_TOPOLOGY_BIN')]:
    hits = [r for r in rows if r.get('reason') == 'compiler-artifact'
            and r['target']['name'] == name and 'bin' in r['target']['kind']
            and not r['profile']['test']]
    assert len(hits) == 1, hits
    r = hits[0]
    assert r['fresh'] is False and r['executable'], r
    assert Path(r['manifest_path']).resolve().is_relative_to(root), r
    path = Path(r['executable']).resolve()
    bound[key] = str(path)
    (run/(name+'-bin.sha256')).write_text(hashlib.sha256(path.read_bytes()).hexdigest()+'  '+str(path)+'\n')
bound.update(HEALTH_SRC=str(root), HEALTH_PROFILE=os.environ['HEALTH_PROFILE'])
with (run/'env.sh').open('a') as f:
    for k,v in bound.items(): f.write('export '+k+'='+shlex.quote(v)+'\n')
PY
source "$HEALTH_RUN/env.sh"
sha256sum --check "$HEALTH_RUN/kernel-bin.sha256" "$HEALTH_RUN/topology-bin.sha256"
```

每次执行前后核二进制和冻结清单，并检查实际 `producer.path`/嵌入源码根。R2 已观察到同 profile 的 copy2 副本被 Cargo 判为 Fresh 却沿用原目录产物；只核退出码或可执行文件名不能验来源。

第 0 批运行器还必须做**编译闭包核对**：从本次 compiler-artifact 的精确产物标识找到对应 `.d`，解析 Makefile 转义及续行，不通配选一个旧文件。取非 test 构建的 kernel lib 和 bin 的 Rust 源/嵌入资源闭包，分别与记录 checker 中 Rust 路径、catalog/profile 的对应嵌入路径比较；Cargo.toml/lock、语义来源、运行时验证依赖单列核验。kernel lib 的 `.d` 不含 `main.rs`，不能把仅 lib 的集合与所有 checker 条目直接判相等。topology 编译闭包独立绑定到外层清单；按 cfg/编译产物判别测试专用源，不能靠文件名前缀过滤。重复项、漏项和无解释的多项均失败。在 `/tmp` 注入新增编译模块而不登记的负例必须被拒收。正式 build ID 协议见 §4.3；该子批通过前，结构抽取仍限原文件内。

批次中途发生正式源重锁时，既有冻结树不随工作区自动更新。若本批继续评价旧上下文，结论明确绑定旧来源；若须评价新上下文，则用旧内核源码加新正式源/目录/输入/规格构建 A′，先重做 A′/A′，再以同一新上下文构建 B′比较。旧 A 输出只存档，不与新 B 混用；核重锁自身的接受域/语义变化，不能用来源映射掩盖规则改变。

### 3.5 A/A、字节等价及引用闭包

每例 A/A 和 A/B 都在同一固定物理输出根 `$CARGO_TARGET_DIR/health-capture/<run_id>/<case_id>/` 顺序捕获。运行器创建独占标记，只允许清理本次登记的该 case 目录；每次捕获开始前清空，结束后将整条链所有原始产物、stdout/stderr 和退出码复制到专属封存目录并核哈希。封存目录放共享 target 或本次 cargo-test-evidence，索引留运行目录；不因封存改变产物内路径。验证须在捕获根仍完整且对应源码上下文未换版时完成。A/B 不并行写同一根。

从 [R2 的 44 组命令](2026-09-22d/R2-来源与计时/commands.md)提取分支模板生成 `cases.json`；R2 目录只读，其过期 batch、bad-input、contract、checkpoint 不作为新输入直接复用。每个 prerequisite 登记为本次链中的生成步骤：记录/周期/种子由该链同一二进制生成，变异负例及 topology contract 由本次生成物和冻结资源按显式变换生成。每轮 A/A 都重走前置步骤，缺失前置产物硬失败。verify-* 只验证本链对应版本的刚生成产物；跨版本拒收实验单列预期结果，不混入等价测试。

`cases.json` 逐例列 ID、角色、argv、cwd、环境、输入哈希、前置步骤、预期退出码和所有输出；`binaries.json` 绑定实际产物、源码根、构建清单与哈希。覆盖 seed/check/request、full/delta、缓存开关、零 tick、停止/装载失败、周期命中/耗尽、verify-record/cycle、两类 checkpoint、verify-batch、topology 和 CLI 参数错误。

第 0 批运行器须实现 `aa --cases ... --binaries ... --fields ... --out ...`、`capture --cases ... --binaries ... --out ...`、`compare --baseline ... --candidate ... --fields ... --out ...`；baseline 指本批同冻结上下文的 A capture，加固批另接 `--changes <行为变更契约>`。运行器先用缺字段、错类型、数组交换、JSON 重排/改缩进、错来源映射、坏引用哈希、缺被引用文件及未登记差异检验拒收能力。

**结构线逐字节验收落盘 JSON 和文本。** 保留所有原始字节；在比较副本中只把已登记的 B 字段替换为 A 的原始 token 字节，然后与 A 逐字节比较。替换须由保留位置的 JSON 解析器定位到确切 Pointer，不能 parse 后整份重新序列化，不能全局字符串替换；重复键或定位不唯一拒收。键序、空白、转义、换行和数字拼写的未登记变化均失败。解析层比较只用于定位差异，解码 full/delta 相等不能替代文件字节相等。这一要求同样适用于第 4 批等价优化。

初始测量白名单只来自 R2 的 44 组 A/A：

| 用例 | 通道 | Pointer | 字段契约 |
|---|---|---|---|
| `run-no-output` | stdout JSON | `/elapsed_ns` | 十进制非负整数字符串 |
| `run-no-output-no-cache` | stdout JSON | `/elapsed_ns` | 同上 |
| `run-no-output-zero` | stdout JSON | `/elapsed_ns` | 同上 |
| `run-no-output-stop` | stdout JSON | `/elapsed_ns` | 同上，Stop 其他字段全保留 |
| `run-no-output-out` | `--out` JSON | `/elapsed_ns` | 同上，stdout 仍逐字节比较 |

只在实际到达计时分支时应用；装载失败没有该字段，不补造字段。原计时值保留并另入性能表。

`comparison-fields.json` 每批按 `case_id + artifact + JSON Pointer` 登记映射，包含 A/B 原值、依据及验证器。来源路径/哈希先分别核闭包；按 role 和规范路径展开到具体索引，核数组长度、顺序及条目身份。`producer.path` 和因两个冻结根产生的路径差异逐项登记；固定捕获根使 out 路径无需因版本改名。新增/删除来源字段及数组结构改变进入 §4.3 协议变更契约。

**引用闭包验收：**逐条解析 `run_record_ref`、`replay_input_ref`、`normalization.definition`、`mapping_proof`、`proof_sources` 等引用。在各自原上下文先核实际目标原字节 SHA-256，不能先归一化再通过原始验证。对叶子来源先做已登记来源映射；对本次生成产物按依赖顺序证明目标在登记替换后逐字节等于 A，才允许把引用方的 B 哈希映射为 A 哈希。记录“引用方 Pointer → 目标路径/原哈希 → 字节等价证据 → 映射依据”。缺目标、坏原哈希、未核差异或无法闭合的循环引用均拒收；不能整类忽略名为 sha256 的字段。修改只发生于比较副本，原证据和同版本验证回执不变。

状态、事件 ID、模型时间、basis、Stop、账、周期键及证据义务严格相等。出现新差异先定位，确有来源/测量依据才登记并重跑 A/A；行为变化进入加固线。

**第 0 批总验收：**恢复先行并独立提交、隔离入口连续两轮通过、完整文件/目录保护通过、A/A 满足字段和引用闭包契约、编译闭包检查能拒收新增遗漏模块。另在同一路径、同 profile、同工具链和环境下比较测试隔离改码前后的 kernel/topology 生产二进制，须逐字节相同；运行时 Python/AJV/sha256sum 依赖也须相同。正式源并发变化则使此对照失效，重建前后上下文再验，不能将差异过滤。A 的源码—二进制—输入—原始输出—回执完整绑定后，才能冻结基线进入第 1 批。

## 4. 第 1 批：状态/API 边界与来源绑定

本批采用缺陷修复/API 加固线，状态与来源分开实施、分别冻结。改动文件集中于 `engine.rs`、`input.rs`、`cycle.rs`、`seed.rs`、`output.rs`、`cycle_io.rs`、`main.rs` 及对应回归；构建身份可新增 `crates/kernel/build.rs`；§4.3 的来源完整性门槛必须在任何跨文件结构抽取前完成。

### 4.1 可编辑输入与已验证运行实例

[R1](2026-09-22d/R1-状态边界.md)已重现：生产环带成功 `step(false)` 后，仅将 b 格库存数量从 `"1"` 改成 `"0"`，库存验证与独立周期键入口返回 `invalid_input`，Engine 周期键却返回 Ok。其他公开模型/状态与派生索引可分离修改在代码上成立，其运行后果尚待逐类反例。

先给候选和调用方一个稳定入口：可编辑的 raw/seed 副本经 parse、prepare/生产域与种子校验进入运行实例；恢复时重建库存/进度/仓库索引、活动边、pending、轮询镜像及派生缓存。存活 Engine 提供只读模型/状态视图、独立可编辑快照、统计读取和明确的预算设置。周期键的快速内部函数只消费已验证的完整边界；对外导入/独立键入口执行对应合法性校验。

执行清单：

1. 以 [R1 public-fields.json](2026-09-22d/R1-状态边界/public-fields.json) 的 Engine 9 项、Input 15 项及报告 §4 调用方为迁移表，逐项给出读接口、受控写接口或内部归属。Engine 的嵌套 Input 一起收口，输出快照与内部运行对象分离。
2. 适配 `main.rs` 的 raw 移出、geometry/catalog 读取、max_sweeps 设置和统计读取。R1 的最小 `pub(crate)` 对照在这些位置产生 8 处私有访问；bin 与 bin-test 重复成 16 条诊断。`pub(crate)` 只完成 crate 外边界，库内直接写仍需按职责收口。
3. 保留 `cycle::cycle_key`、`checkpoint_input`、`seed::canonicalize_seed` 的重绑定语义，迁到明确准备/恢复流程；`cycle::restore/search`、`cycle_io::verify_cycle_at` 使用预算接口。
4. R1 的 30 个测试/辅助函数、50 个写入或可变借用起点逐项迁移。合法准备通过可编辑种子与重建；非法库存、事件、极端时间和特殊配方使用 `cfg(test)` 的显式构造/注入接口，保留每条拒收断言。历史探针保留原字节，以外部适配器或旧环境运行。

**验收：**把 R1 的 `probe.rs` 最小反例转成活动回归，保留合法→单叶变零→恢复的三个阶段。可变字段原调用在外部 crate 的编译负例中被拒；零库存快照经公开导入/键入口返回 `invalid_input`；合法状态的三个入口均成功，两种周期键完整相等。若保留兼容的可变对象入口，其零库存调用也须明确拒收。测试专用非法对象只进入待验边界，快速编码函数由模块权限和已验证状态类型保护。

来源以外的合法输入接受域、轨迹、独立键及正常 CLI 输出按 A/B 严格等价；按 R1 调用方清单完成 `cargo check --workspace --all-targets` 和安全回归。验收同时检查 API 只读视图无法取得内部可变引用、失败恢复不留下半更新索引、缓存开关下结果一致。完整验证放在装载/恢复边界，正常步进沿已证明的状态维护路径执行，并量化成本。

**对外变化：**直接字段赋值迁为准备/恢复/预算 API；非法状态进入周期键的路径被拒收或在编译期消除。合法 CLI 行为保持；记录的诊断等级保持。

### 4.2 停止状态与可恢复边界

[R5 §3](2026-09-22d/R5-验证与失败态.md)已重现，扫描预算耗尽前可以已经移动库存，`step_with_snapshot` 封存实例并重复返回原 Stop；`step_tick/step_instant` 在包装器之前的锚点错误有独立行为。当前 `run_record` 保留成功 rows，周期搜索保存 `last` 和 `stop.partial_events`。

把已有“最近完整边界”和“停止工作态/部分审计”区分带到公开视图及 checkpoint API。完整边界带有成功闭包凭据和上下文绑定；运行实例的 phase 字符串只作字段值。checkpoint/resume 使用经过校验的完整边界，诊断视图保留停止位置及部分执行。沿用现有最后成功状态的保存机制，按导出需要取快照。

**验收：**环带 `max_sweeps=1` 下覆盖 `step(true)`、`step(false)`、`step_compact()`，核已经发生的库存变化、Stop、再次调用的不变性；覆盖进入 step 包装器之前的锚点拒收。恢复最近成功边界与连续运行的后续 `time/events/state/warehouse_ledger/closure` 相同，累计摘要明确从原运行起点还是恢复起点计数。失败工作态导出可恢复 checkpoint 的请求有明确拒绝结果。

**对外变化：**只读诊断视图与恢复 API 的适用对象显式化；新增拒绝路径按本子批契约登记。既有失败后封存、子动作提交次序及前置锚点错误保持。

### 4.3 构建来源与运行时来源分开

[R2](2026-09-22d/R2-来源与计时.md)已复现旧二进制记录新源码哈希，以及新增模块编译后行为改变而来源数组不变。正式构建身份至少绑定实际编译闭包、构建环境与产物；运行时输入/配置/正式规则来源另行绑定。

实施最小闭合链：

- 生成构建期清单并嵌入 build ID：活动 Rust 模块、`include_str!/include_bytes!` 的输入、生成代码的源及 build 脚本、相关 Cargo.toml/Cargo.lock、features/cfg、rustc 与关键构建参数。由 Cargo 元数据、编译依赖文件和模块/include 关系核对新增、删除、移动文件；为外部生成输入显式登记依赖和重新构建触发。
- 构建完成后在外层产物清单中记二进制 SHA-256，与嵌入 build ID 对应。运行记录引用实现 build ID，输入、配置、正式源及实际消费投影继续记录运行时哈希。验证器分别核这两类来源及实际加载的验证依赖。
- `verify-batch` 的 `crates/kernel/tests/verify_all.py`、`crates/kernel/tests/audit_task7.py` 以及实际加载的 Python/Node/AJV 文件/版本纳入验证依赖清单。R2 对此是代码上成立、尚未篡改实测；通过改动副本中依赖并核拒收补齐反例。
- 在 `output.rs`、`cycle_io.rs`、`main.rs` 的相关读取入口登记 schema/字段兼容矩阵。旧格式按原格式与匹配实现验收，历史文件保持原字节；新格式按新身份契约验收，迁移清单描述新旧字段与拒收结果。

**验收：**在 `/tmp` 副本重做 R2 两组实验：保留旧二进制、修改磁盘源码后，其 build ID 保持旧身份；重新编译后身份变化；仅修改新增已编译模块也改变身份。替换来源清单、build ID、输入来源或验证依赖的篡改逐项拒收；合法记录和周期仍可回放。新增模块/include 文件缺失时构建来源完整性检查失败。保存每阶段源码、二进制哈希、运行命令和原始输出，按独立 profile 排除缓存串用。

**对外变化：**记录与验证协议新增/调整实现身份，验证依赖范围扩大，旧产物的验收入口按兼容矩阵选择。语义轨迹、basis、账与证据等级继续严格相等。来源协议验收通过后冻结新基线。

## 5. 第 2 批：按状态不变量划分内部责任

本批采用结构等价线，只收窄 crate 内部 helper 的参数和写权限，所有公开可达 API 保持冻结。§4.3 未通过前，非测试编译文件集合不变，只在原文件内抽函数；通过后才按已验收的构建身份协议提取文件或结构体。每个 helper 附读集、写集、缓存失效集、失败点及提交前/后位置。

| 内部职责 | 当前涉及文件 | 必须维持的关系 |
|---|---|---|
| 已验证静态模型 | `input.rs`、`catalog.rs`、`engine.rs::prepare`、`interfaces.rs` | 目录/几何/模板/参数与静态索引一致；装载守卫和首错顺序固定 |
| 运行动态状态 | `model.rs`、`transition.rs`、`warehouse.rs`、`polling.rs` | 库存、进度、门、冷却、pending、轮询及其序列化镜像同步 |
| 可重建缓存 | `cache.rs`、`engine.rs`、`polling.rs` | 每类库存、容量、开关、接通及身份变化对应明确失效/重建位置 |
| 本刻审计 | `event_identity.rs`、`ledger.rs`、`transition.rs`、`output.rs` | 身份分配/执行、事件次序、物料账与本刻动作一一对应 |
| 运行状态与边界 | `transition.rs::step_with_snapshot`、`cycle.rs`、`output.rs` | 完整前缀、最后成功边界、停止工作态与部分审计可区分 |

`Progress` 的 `phase: String` 搭配 `recipe/locked_recipe/remaining` 可选字段及候选/冷却集合，由装载期检查组合合法性。只有第 1 批 API 边界收口通过后，才在原校验成功处引入受约束的私有运行阶段类型；该类型不得出现在任何对外类型中，公开 `Progress`/`State` 的字段与 serde 形状不变；外部 JSON、`Time/Quantity` 的数字类别、basis、输入顺序和错误定位保持。R5 的 `idle + remaining` 仍应能反序列化而在同一校验阶段返回 `unsupported @ initialization.other_inventory`。这种类型化按外部接受域、首错和序列化等价验收。

### 5.1 机制 × 不变量 × 独立检查

R5 确认 `verify_record_at → run_record`，`verify_cycle_at → cycle::search/Engine` 使用同一生产实现的新实例；现有重放负责复现一致性，并配有身份和守恒检查。下表作为新增性质检查清单，每行记录已覆盖 fixture、预算、命令及未覆盖部分：

| 机制 | 不变量/观察 | 检查方法和落点 |
|---|---|---|
| 装载与校验顺序 | 接受域、首个 Stop | 在 `src/tests/` 保存跨阶段双错误负例；独立列出预期 status/axis/location/reason |
| 库存/制造/仓库 | 数量、容量、同种、整批与各物种账平衡 | 直接从输入配方和前后状态计算守恒式；保留黄金独立常量 |
| 事件与轮询 | 唯一性、已执行闭合、pending/别名冲突、断边和恢复后游标 | 保留 `event_identity` 校验；枚举小规模合法变更，保存首个失败输入 |
| full/delta 与恢复 | 解码后完整 tick 相等；完整边界续跑一致 | 独立朴素 delta 应用器核每刻状态；连续/恢复对照事件、账和计数起点 |
| 缓存与记录开关 | 逐刻语义一致 | 缓存开/关覆盖断边、恢复、失败尝试及容量变化；capture 开/关对照完整物理状态和可共同观察的统计 |
| 周期投影 | 固定上下文中合法同键状态的后继相容 | 枚举/生成小状态对，先独立校验合法性和同键；实际走一步，核成功/停止结果一致，成功时比较后继键及声明保留的实际入库增量；保存种子、完整键及失败见证 |
| 现有 Python 参考 | 粉碎机 4 刻、分流器 12 刻完整轨迹 | `cargo test -p kernel --test reference` 的三项测试；覆盖事件/状态/账/summary/closure，外层来源封套另验 |
| 候选确实使用的机制 | 成功箱体传输等两场景尚缺的转移 | 在独立参考或不变量检查器中逐项补最小正/负例；每项标明独立到规则实现还是仅独立到编码 |

分流器参考中的 transfer 全为停用分支，该场景的成功传输覆盖保持“未覆盖”。有限生成检查提供反例搜索及指定域内验收，证据等级沿用现有范围。

**命令与验收：**对所触库测试执行过滤后的 `cargo test --locked --offline -p kernel --lib <具体测试名> -j 2 -- --exact --test-threads=1`，再执行 §3.3 suite、A/A 和 A/B。性质检查新增项先在基线运行并保存种子。每次抽取逐一核对失败前后提交点，保持扫描/仲裁次序、缓存失效位置和完整停止前缀。对外行为保持第 1 批已冻结的契约。

## 6. 第 3 批：长函数、测试布局与代码入口清理

### 6.1 按责任选择函数

[2026-09-22/functions.json](2026-09-22/functions.json)记录当时超过 80 行的 30 个函数（28 个运行/接口函数、2 个测试函数），作为导航清单。实施时按新源码重算定位，按维护需求选一组提取。

| 责任 | 文件与函数 | 提取单元及专项验收 |
|---|---|---|
| 装载信任边界 | `engine.rs::prepare/validate_seed`；`input.rs::parse/timeline/validate_runtime_interfaces/validate_branches`；`interfaces.rs::check_input_axes/check_event_owners` | 只读验证阶段、时间线/参数/身份检查；原顺序与多错误首错逐一对照 |
| 动作提交与停止边界 | `transition.rs::move_channel/manufacture/boundary/step_inner`；`warehouse.rs::plan_receipt` | 读写参数收窄、预检/提交/后效分段；失败前移、全收/部分收/全拒、同刻完成与窗口到期保持 |
| 候选与 CLI 接入 | `main.rs::run`；`catalog.rs::load/build`；`topology::check_categories/validate` | 参数解析/handler、来源/目录投影、几何检查/必要条件报告；路径解析、首错和完整报告保持 |
| 恢复、轮询与身份 | `seed.rs::derive_context`；`polling.rs::group_levels/refresh`；`event_identity.rs::seed_executed` | 参数镜像、成员/幸存游标、身份来源闭合；after_closure 续跑、批量断边/恢复保持 |
| 周期与产物 | `cycle.rs::domain_condition/encode_key/search`；`output.rs::run_record`；`cycle_io.rs::verify_cycle_at` | 域条件、规范键、候选确认、重放和封套；D 条件优先级、摘要碰撞、预算和最后有效前缀保持 |
| 独立目录断言 | topology `tests/validation.rs` 的 `catalog_unit_properties_match_formal_reference_values`、`catalog_port_edges_match_formal_reference_layouts` | 按单元类型分表，helper 只比较；保留独立期望常量 |
| 纯映射 | `config.rs::name` | 保留封闭枚举映射；有实际修改需求时逐项核轴名称和序列化名称 |

§4.3 未验收前仅在原非测试文件内抽取，禁止新增、删除、改名非测试编译文件。每步先由原入口调用 helper，对照分支顺序、短路求值、返回值及写入时机；通过后只收窄私有实现权限，公共签名与可见性保持。算法、集合和缓存策略的优化按第 4 批单列。

### 6.2 测试按机制组织

内部单元测试入口选 `crates/kernel/src/tests/mod.rs`，将原 `src/tests.rs` 的入口迁入后，各机制放在同目录的 `inventory.rs`、`manufacturing.rs`、`bridge.rs`、`gates.rs`、`polling.rs`、`warehouse.rs`、`seed_resume.rs`、`cycle_key.rs`、`cycle_replay.rs`、`event_identity.rs`、`stops.rs`，夹具与测试注入放 `support.rs`。现有 `tests_revision*.rs`、`tests_round5.rs`、`tests_round6.rs` 按断言行为映射过去。

公开 CLI/文件协议测试留在 `crates/kernel/tests/`，按 `cli_paths`、`cli_records`、`cli_cycles`、`schema_tamper`、`reference_differential` 组织。共享 helper 放子目录，Python 辅助脚本按同一机制归属并继续使用第 0 批输出接口。

纯搬迁保存旧测试全名→新全名双射、原断言块哈希、fixture 与负例类别清单；通过映射归一化路径/模块名后核断言内容。测试数、ignored 数、断言和负例类别保持，新增性质测试另记增量。先完成文件/mod/path 搬移，再单独整理 helper。

### 6.3 有入口证据的清理

对候选逐项核 Cargo targets、模块可达性、trait/Serde/宏回调、CLI/API 暴露和测试用途。`value.rs` 的 `expecting/visit_*` 是 Serde 回调，`digest.rs::known_vectors` 及 topology 的 `legacy_machine_constants_equal_catalog` 是测试，按各自入口保留。仅 crate 私有且确证无入口的冗余项可附定义/调用/暴露证据后删除；公开项即使仓库内零调用也保留，删除或收窄须另立 API 加固批及调用方迁移表。

**命令与验收：**保存搬迁前后每个 harness 的 `-- --list`，更新 §3.3 suite 的目标名映射，运行 build/check/Clippy、所触机制和完整安全 suite，以及 A/B 五层观察比较。保留桥同种双轴满速、异种不串轴、运输滞留、制造整批、门窗口/累计、仓库接收、暂停恢复、守恒、缓存、摘要碰撞、来源篡改和周期诊断范围等原断言。对外运行行为保持不变，测试全名变化按映射交付。

## 7. 第 4 批：资源预算与有证据的优化

[R4](2026-09-22d/R4-资源.md)的 release 实测提供初始测点；这些是该机器和版本的结果，候选比较使用同机新 A/A 波动：

| 测点 | 实际完成量 | 墙钟与峰值 RSS |
|---|---:|---|
| 环带无输出，三次中位数 | 1,000 / 10,000 / 100,000 tick | 0.04 / 0.39 / 4.01 秒；8,392 / 22,868 / 167,716 KiB |
| `密集制造闭环序2核验.json` 无输出，单次 | 100,000 tick | 92.60 秒；4,486,920 KiB（4.279 GiB） |
| 环带 full / delta 输出，单次 | 1,000 tick | 999,684 / 221,540 KiB |
| 制造闭环周期搜索，K=1 / 32 / 128，单次 | 各 1,000 tick，均 inconclusive | 666,652 / 83,472 / 68,832 KiB |

环带无输出 100,000 tick 的集合探针计得 `allocated=1,000,013`、`executed=1,000,000`、`records` 长度/容量均为 0。身份集合长期保留、`run_record` 积存 rows/取证信息、搜索保留摘要位置和间隔 State 检查点均已核实；各结构占全部 RSS 的精确比例仍待测。

### 7.1 先固定测量矩阵和预算

矩阵覆盖环带与制造闭环、1k/10k/100k tick、缓存开关、无输出/full/delta、扫描预算正常/停止、搜索检查点 K=1/32/128，以及多个实际未命中搜索长度。有输出先按 1k 档测峰值，再依预算扩规模。每个比较点至少三次独立进程，记录工具链/二进制/输入哈希、实际 status、completed_ticks、Stop、扫描轮数、事件/身份数量、输出字节、墙钟、CPU 与峰值 RSS。计时分清 CLI 的循环计时和 GNU time 的进程墙钟。

分流器在第 12 tick 因外部历史覆盖耗尽停止，留作停止用例。环带周期搜索在第 21 次推进命中周期 20，按实际推进量记账；长搜索使用能跑到预算的输入。真实候选可用后追加候选测点并冻结输入。

在本阶段冻结源码根构建独立 release 继承 profile，从本次 Cargo 产物消息选择 `HEALTH_PERF_BIN`，不得覆盖行为基线的 `HEALTH_BIN`：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
cd "$HEALTH_SRC"
HEALTH_PERF_PROFILE="healthperf$(date +%s)$$"
cargo build --locked --offline -p kernel --bin kernel -j 2 --profile "$HEALTH_PERF_PROFILE" \
  --config "profile.$HEALTH_PERF_PROFILE.inherits=\"release\"" \
  --config "profile.$HEALTH_PERF_PROFILE.codegen-units=1" --message-format=json \
  > "$HEALTH_RUN/perf-build.jsonl.log" 2> "$HEALTH_RUN/perf-build.stderr.log"
python -B - <<'PY'
import hashlib, json, os, shlex
from pathlib import Path
run = Path(os.environ['HEALTH_RUN'])
rows = [json.loads(s) for s in (run/'perf-build.jsonl.log').read_text().splitlines() if s.startswith('{')]
hits = [r for r in rows if r.get('reason') == 'compiler-artifact'
        and r['target']['name'] == 'kernel' and 'bin' in r['target']['kind']]
assert len(hits) == 1 and hits[0]['fresh'] is False and hits[0]['executable']
assert Path(hits[0]['manifest_path']).resolve().is_relative_to(Path(os.environ['HEALTH_SRC']).resolve())
p = Path(hits[0]['executable']).resolve()
(run/'perf-bin.sha256').write_text(hashlib.sha256(p.read_bytes()).hexdigest()+'  '+str(p)+'\n')
with (run/'env.sh').open('a') as f: f.write('export HEALTH_PERF_BIN='+shlex.quote(str(p))+'\n')
PY
```

`HEALTH_TIME_BIN` 指向经 `--version` 核实的 GNU time；R4 宿主标准路径缺失，复跑时从可用工具或 `/tmp` 解包工具取得并登记版本/哈希，将其绝对路径以 shell 安全引用写入本次 env.sh。测量命令模板：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
cd "$HEALTH_SRC"
: "${HEALTH_PERF_BIN:?指定本次已核哈希的 release 构建产物}"
: "${HEALTH_TIME_BIN:?指定已核实的 GNU time 可执行文件}"
mkdir -p "$HEALTH_RUN/perf"
sha256sum --check "$HEALTH_RUN/perf-bin.sha256"
HEALTH_PERF_OUT="$CARGO_TARGET_DIR/health-perf/$(basename "$HEALTH_RUN")"
mkdir -p "$HEALTH_PERF_OUT"
LC_ALL=C "$HEALTH_TIME_BIN" -v -o "$HEALTH_RUN/perf/ring-none.time.log" \
  "$HEALTH_PERF_BIN" run 数据/样例/生产循环环带.json --config 规格/内核配置-v1.json \
  --ticks 100000 --max-sweeps 100000 --no-output \
  > "$HEALTH_RUN/perf/ring-none.stdout.log" 2> "$HEALTH_RUN/perf/ring-none.stderr.log"
LC_ALL=C "$HEALTH_TIME_BIN" -v -o "$HEALTH_RUN/perf/dense-cycle-k32.time.log" \
  "$HEALTH_PERF_BIN" cycle 数据/样例/密集制造闭环序2核验.json --config 规格/内核配置-v1.json \
  --max-ticks 1000 --max-sweeps 100000 --no-record --search-checkpoint-interval 32 \
  --out "$HEALTH_PERF_OUT/dense-cycle-k32.json"
```

有输出测点的主记录、附带记录及周期包一律写 `$CARGO_TARGET_DIR/health-perf/<run_id>/`，不落入待提交的日志目录；原始字节保留，运行目录仅保留哈希、字节数、路径索引和摘要。运行器为矩阵、重复次数分配独立文件名，在 `resource-policy.json` 中于候选运行前登记每组时间/内存上限、测量波动和优化目标。结构批次的性能回归按此阈值检查；数值改进同时比较真实完成工作量和语义差分。

### 7.2 优化单元及行为契约

| 单元 | 实施与证明义务 | 对外行为及验收 |
|---|---|---|
| 事件身份保留 | 区分外部历史身份、pending 身份和严格时序生成的内部判断身份；研究结构化 ID/水位或可证明回收边界。逐项覆盖重复执行、别名、恢复及跨边界 pending 冲突 | 等价优化：合法输出与原有拒收相等；身份数量/内存增长目标达到预登记值 |
| 记录内存 | 分析 rows、acceptance/manufacturing、封套和最终序列化的各阶段峰值；先选能保持现有输出协议的局部释放/编码优化 | 等价优化以 §3.5 登记替换后的落盘 JSON 逐字节相等、完整解码记录及原 CLI 契约验收；真正分块/流式协议另列加固子批，明确部分文件、失败封尾与读取器迁移 |
| 周期搜索预算 | 分别预算摘要位置、State 检查点、候选重放和命中后记录；按字节/条目核消耗并在完整边界停止 | 新预算是行为变更：参数、默认值、Stop 字段、最后完整前缀及恢复方式写入契约；耗尽返回 inconclusive 和明确资源原因 |

资源预算采用可重复的条目或字节计费规则，OS RSS 用作测量而非等价比较中的随机截断点。默认值的兼容性在契约中明确；边界测试覆盖刚好足够、少一单位、恢复、缓存开关和有/无记录。优化通过后重跑 §3.3、§5.1 性质检查、A/A、A/B 和资源矩阵。资源结果分别标注“重现了”“代码上成立但没重现”及尚待归因的部分。

## 8. 每次正式源/实现指纹重锁的固定流程

第 0 批完成前按 §8.0；完成后按 §8.1。每次重锁独立分配 HEALTH_RUN，记录当前源字节与允许写入范围。

### 8.0 第 0 批完成前的过渡规则

默认只运行 §3.3 第一块的 build/check/Clippy、kernel/topology lib、reference、topology validation 和 doc；不执行七个 CLI 入口，不使用全工作区测试命令。检查前后读取资源保持稳定，实际命令仍受线程与共享 target 约束。

确需 CLI 时，在本次输出目录实拷 `2026-09-22/run_checks.py` 与 `bin/python` 作为过渡实现素材，原文件只读。将 runner 的 O、日志、PATH 和包装器输出根参数化到本次目录，删除全工作区测试入口，只枚举七个明确目标；设置 KERNEL_TEST_EVIDENCE_DIR。原脚本固定写旧日期目录，不能直接重跑。

包装器以“脚本绝对路径及哈希 → 预期输出常量及次数”映射校验：当前六个 Python 入口各自只命中五种候选常量之一，预期一次；先 assert 对应原串 count==1，再只替换该串为 Path 类型的新根，确认旧输出常量已消失。不得要求每个脚本五种常量全命中，也不得在未命中时静默执行原脚本。包装器末尾的类型补换同样登记次数；更简单的实现直接构造 Path 表达式，避免第二轮宽泛替换。未知脚本只允许已登记的只读子进程，禁止泛化放行历史生成器。

在 /tmp 无链接副本按 §3.2 的 no-run、Fresh=false 和来源验收流程验证过渡包装器；产出绑定本次源/包装器哈希的 `transition-isolation-accepted.json` 才能运行指定 CLI 目标。过渡通过不替代永久隔离验收。运行前后核完整保护清单，至少覆盖四棵历史树 `crates/kernel/evidence`、`crates/kernel/复核`、`数据/复核`、`规格/复核`，并包含 R3 写点表中的其他历史位置、旧维护目录、忽略文件和空目录。发生差异保留现场，不用 HEAD 批量回退。

### 8.1 隔离入口验收后的重锁

按以下顺序执行，所有命令台账归入新的 HEALTH_RUN：

1. **登记变更与保护集。** 保存当前正式源、活动目录/输入、源码、黄金和历史的完整字节清单。`relock-plan.json` 逐路径列出来源变化、依赖关系、预期接受/拒收影响以及允许更新的活动文件。历史路径按用途识别，seed/input schema 的历史产物仍属于历史。
2. **生成当前输入更新。** 从正式源生成/核对当前目录，只在计划列出的活动样例、fixture 和投影中更新来源引用。把 `2026-09-22{,b,c}/relock.py` 作为算法/范围参考，在本次运行目录创建显式参数化的 `relock_current.py`，声明全部读写路径后执行 `python -B "$HEALTH_RUN/relock_current.py"`。新参考基线先在本次目录生成，独立参考与物理轨迹比较通过后按已登记路径发布；手工黄金保持原字节。
3. **重建与绑定。** 更新实际需要的构建来源闭包，使用 §2 环境和 §3.4 的独立 profile 构建。记录构建命令、工具链、嵌入身份、源码根、二进制哈希及运行时来源。旧来源的输入/记录保留为拒收用例，新输入按预期接受。
4. **建立测试前快照。** 执行 `python -B 数据/工具/kernel_file_guard.py snapshot --root .. --out "$HEALTH_RUN/pre-test.json"`，保存本次有意修改完成后的状态；设置新的 `KERNEL_TEST_EVIDENCE_DIR`。执行 `python -B 数据/工具/kernel_regression.py tests --out "$HEALTH_RUN/regression"`，内部逐项运行 §3.3 命令；测试入口的全部子进程共享隔离策略。
5. **跑正向链与反例。** 使用本次二进制，依次 seed→check→run→verify-record、cycle→verify-cycle、checkpoint→恢复续跑、verify-batch 与 topology；产物均给明确 `--out`。执行来源错配、缺项、旧来源、非法状态、预算停止及现有篡改用例。示例正向链如下。
6. **A/A、差分及最终保护审计。** 对固定语料执行 A/A，重锁前后按本次来源映射表比较；语义变更使用单独契约。执行 `python -B 数据/工具/kernel_file_guard.py verify --before "$HEALTH_RUN/pre-test.json" --out "$HEALTH_RUN/protection-diff.json"` 核测试期间的写入，另将最终历史清单与步骤 1 比较。历史“未变”结论来自全部测试和产物生成结束后的字节审计。
7. **封存本次结果。** 保存逐命令退出码、实际测试名/计数、输入/源码/二进制/工具链哈希、字段映射及首个差异、历史保护结果和未完成项。新回归证据保存在本次运行，成为下一次维护的读取资料。

正向命令使用 §3.4 已绑定的 `HEALTH_BIN`，运行器逐条保存退出码和 stdout/stderr；以下以环带为例：

```bash
set -euo pipefail
source "${1:?传入本次 env.sh 的绝对路径}"
cd "$HEALTH_SRC"
sha256sum --check "$HEALTH_RUN/kernel-bin.sha256"
mkdir -p "$HEALTH_RUN/cargo-test-evidence/positive"
"$HEALTH_BIN" seed 数据/样例/生产循环环带.json --config 规格/内核配置-v1.json \
  --out "$HEALTH_RUN/cargo-test-evidence/positive/seed.json"
"$HEALTH_BIN" check "$HEALTH_RUN/cargo-test-evidence/positive/seed.json" --config 规格/内核配置-v1.json
"$HEALTH_BIN" run "$HEALTH_RUN/cargo-test-evidence/positive/seed.json" --config 规格/内核配置-v1.json \
  --ticks 12 --out "$HEALTH_RUN/cargo-test-evidence/positive/run.json"
"$HEALTH_BIN" verify-record "$HEALTH_RUN/cargo-test-evidence/positive/run.json" --config 规格/内核配置-v1.json
"$HEALTH_BIN" cycle "$HEALTH_RUN/cargo-test-evidence/positive/seed.json" --config 规格/内核配置-v1.json \
  --max-ticks 50 --search-checkpoint-interval 3 --out "$HEALTH_RUN/cargo-test-evidence/positive/cycle.json"
"$HEALTH_BIN" verify-cycle "$HEALTH_RUN/cargo-test-evidence/positive/cycle.json" --config 规格/内核配置-v1.json
"$HEALTH_BIN" checkpoint "$HEALTH_RUN/cargo-test-evidence/positive/run.json" --config 规格/内核配置-v1.json \
  --out "$HEALTH_RUN/cargo-test-evidence/positive/checkpoint.json"
"$HEALTH_BIN" run "$HEALTH_RUN/cargo-test-evidence/positive/checkpoint.json" --config 规格/内核配置-v1.json \
  --ticks 4 --out "$HEALTH_RUN/cargo-test-evidence/positive/resumed.json"
```

材料包按活动 CLI 测试的包结构在本次目录构造，再调用 `verify-batch`；恢复结果与连续运行相同后续片段比较。发生保护差异时保留现场字节和命令，按已保存的原哈希恢复并修复写入入口，重新完成本轮验收。

## 9. 候选接入、可选归档与批次交付

### 9.1 候选输入接口

几何候选通过受校验适配器给出单位位置/朝向、配方与设定、完整自动接通关系、显式参数、初态或调试义务和来源引用，进入 `Input::parse`、seed/check 与生产域检查。适配器首先在现有合法样例上完成导出→装载→执行的往返，真实候选到达后增加其最小接入用例。候选运行输出区分必要条件结果、具体工程轨迹、周期诊断和待完成的证明义务；`unsupported/inconclusive` 回传具体原因和适用范围。

接入前按候选实际使用的每种转移建立独立参考或独立不变量检查的最小正例、负例及来源/覆盖清单，不等待第 2 批。箱体无线传输须包含非零实际入库正例、空箱/冷却/拒收负例，以及物品在两次传输判断间经出口离箱、无线入库为零的反例；事件名为 transfer 或返回 success 不能替代正数量入库核验。对依赖箱子无线交货的候选，明确采用不接出箱传送带的结构约束，或另有覆盖其相位/判定次序的留存与交货证明；不把“箱子启用 transfer”直接当成已交货。D.5 箱数上界不代替这些机制义务。

第 1 批的只读/恢复 API 与这条输入路径一起验收。每个接入阻塞记录具体字段、命令和 Stop，以最小修复子批解决。完成第 0 批和候选所需的第 1 批边界后，候选接入即可独立推进。

### 9.2 可选索引与归档

优先为 CodeGraph、rg、IDE/rust-analyzer 分别配置活动路径：`crates/kernel/src`、`crates/kernel/tests`、`crates/topology/src`、`crates/topology/tests` 和明确的数据工具，再裁掉 legacy probes、复核、证据、归档及缓存。每个工具按自身支持的配置验收，保存实际索引文件清单，确保活动 `Engine` 定义唯一且历史资料可另行定位。

确需移动快照时，使用 [archive-inventory.json](2026-09-22/archive-inventory.json)作候选目录资料，先查 Cargo/include/import/fixture 和历史负例引用。`revision_r3_cli.py` 的两个 r3 历史输入须先按 §3.2 固定为必需只读依赖；迁移后核两组负例仍执行，缺失必须失败。逐树登记文件数、字节数、完整哈希、权限、链接、内部布局及引用，目标为 `归档/证据快照/<原相对路径>/`；原创审查材料按原位置保存。`索引.json` 记录原路径→归档路径、来源与清单哈希，原位置使用定位页，活动读取者使用显式路径映射。

每棵树先核目标冲突，再移动并校验全部内容，记录可逆映射；出现冲突保留两边现场。完成后验证 targets、引用、测试清单、安全回归和原哈希。回滚逐树按映射恢复，检查原路径是否出现新文件。归档仅改变资料定位，旧结论的适用范围仍由原来源决定。

### 9.3 每批交付与完成判据

每批交付：改动路径和目的、所属工作线、A/B 来源与原始输出索引、执行命令和退出码、实际测试清单、字段白名单/行为变更契约、调用方或测试迁移表、读写/首错/失败点对照、资源结果及文件保护结果。

入库范围为运行根的报告、脚本、源码探针、cases/映射/来源清单、journal、保护差异和小型文本日志；所有文件逐个核大小。`cargo-test-evidence/`、共享 target 的 `health-capture/`、封存产物和 `health-perf/`、/tmp 源码树及编译产物不入库，保留位置和完整哈希用于复核。历史 b/c 的 `cargo-test-outputs/` 属显式列出的七项归档例外，须先核每项大小、版本与来源再入库，不继承测试输出的可丢弃身份。不使用整目录无审查添加，也不强制添加大型运行产物。

结构批出现接受域、事件、账、首错、周期键、公开接口或证据等级差异时，保留最小反例，定位抽取错误或转为独立修复子批；修复通过后重新冻结基线。保护审计发生异常先完成现场保存和按哈希处置。未执行、未覆盖和恢复待办均留在交付清单。

最低可用完成点是：安全回归与来源基线闭合、可恢复历史已按清单复原且剩余项可追踪、交付结论依赖的状态/来源边界通过验收、候选可经稳定接口接入。继续拆分或整理以明确的维护收益和资源需求为依据。

## 10. 依据与复核入口

| 依据 | 本方案使用的事实与清单 |
|---|---|
| [R1：状态边界](2026-09-22d/R1-状态边界.md) | 三入口零库存反例、24 个公开字段、30 个测试/辅助函数、CLI 可见性对照 |
| [R2：来源与计时](2026-09-22d/R2-来源与计时.md) | 两个来源反例、44 组 A/A 中观测到的唯一不稳定字段、共享 target/profile 来源串用 |
| [R3：测试写历史](2026-09-22d/R3-测试写历史.md) | 七个写历史入口、159/7/14 分类、恢复源与哈希、辅助脚本副作用 |
| [R4：资源](2026-09-22d/R4-资源.md) | release 测点、身份集合计数、记录/搜索保留机制、实际完成量 |
| [R5：验证与失败态](2026-09-22d/R5-验证与失败态.md) | Progress、封存边界、同实现重放、参考覆盖、134/141 计数 |
| [函数盘点](2026-09-22/functions.json)、[快照盘点](2026-09-22/archive-inventory.json) | 结构整理与可选归档的导航资料 |

核实报告中的复现命令、脚本和日志用于读取证据及提取最小用例。新运行从显式输出参数化后的入口执行，保存到新的运行目录。各批的通过结论以其执行时的字节、命令和实际输出为准。
