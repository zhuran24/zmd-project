# 内核维护执行记录

记录日期：2026-09-22。性质：本次执行史料及交付验收记录。状态：三项工作已完成；桥旧读法已退回，正式源已重锁，结构体检方案已交付。验证不构成完整工厂或全称游戏规则认证。

## 1. 工作边界与核对依据

工作目录为`/home/zhuran24/zmd-research-fresh/求解器/`，所有Cargo命令从该处运行，`CARGO_TARGET_DIR`显式设为共享`求解器/target`。没有调用Git，没有访问或修改模拟器，没有改写项目根目录的三份正式文件及候选约束。维护目录只有脚本、日志、JSON与md，没有仓库快照或编译产物。

规则依据只取《明日方舟：终末地》游戏规则.txt、求解任务.txt、求解约束.txt。三审§2.2/§5、任务书7终修记录、两份规格的09-22补记及r13桥例外旧验收仅作定位材料。现行R13列「协议储存箱、缓存格和桥接器除外」，因此桥同种跨轴占格禁令无效；R21包括「可同时打开或关闭多个开关」，本次将该现文及整份正式源重新锁入目录。

执行前记录5906个求解器文件的哈希和大小，排除target、缓存与Git；[before.json](before.json)只有指纹，没有文件内容副本。同期`候选约束轮次/`和`几何/`有其他工作产出，本次未修改或回退那些文件，最终清单将其与维护改动区分。

## 2. 旧桥读法的实际退回

| 文件 | 实际改动及原因 |
|---|---|
| `数据/正式静态目录.json` | 桥接器`inventory_rules.same_item_across_slots`由`at_most_one_slot`改为`exempt`，改写旧同种禁令notes。目录为内核编译时嵌入的真源；不删除普通制造单位的同种唯一守卫。 |
| `数据/工具/formal_units.py`、`test_formal_catalog.py` | 生成器按现行规则给桥和箱例外；反例改为把桥错误设回`at_most_one_slot`，防止后续生成恢复旧禁令。 |
| `数据/样例/runtime_record.py`、`数据/送料契约.md` | Python状态校验从目录读取例外；同种桥双轴合法，普通制造格同种重复仍拒收；送料契约同步。 |
| `crates/kernel/src/tests.rs` | 旧`bridge_same_item_cannot_occupy_both_axes`改为双轴同种满速回归。两轴同时各1件、当tick不能穿桥、重载完整种子、连续四次各送1件及总物料守恒全部断言。异种不串轴测试保留。 |
| `规格/运行语义.md`、`选择点清单.md` | 桥入口和T14撤同种跨轴守卫；仍保留每轴容量/滞留、轴身份和按单位调度。两处09-22补记明确写“已退回”，不再推迟到下一轮。 |
| `规格/受限转移定义.md`、`参数扫描约减.md` | 桥目标不再因另一轴同种拒收；约减读集去掉同种互斥理由，保留按单位调度及Peers依赖，未把桥跨轴事件直接声明可交换。 |
| `会议成果/任务书7执行/密排布局.json` | 恢复六条成品路的`service_capacity_given_receiving_boundary`；`service_contract`长路到箱/到仓18/23退回17/22，逐轴同种守卫改为例外，正确有限预装恢复字段由null恢复106。actual_rate仍为null，整厂认证仍false。 |
| 同目录`成品输出通路.md`、`密排试点.md`、`整厂衔接.md` | 恢复逐轴1件/tick、三类16/21、8/13、17/22条件等待；成品物品格容量恢复76、全图170，与占地72/166分开。空输出在途20保留；106是明确预装与持续接收前提下的源端恢复界。 |
| 同目录`植物运行试点.md` | 满线路径不再要求同桥两轴异种；单物种运输容量不再扣除桥的第二轴。 |
| 同目录`本轮成果.md`、`未决去向.md`、`会议成果回填稿.md`及`会议成果/主会话三审-0921.md` | 现行汇总同步上述界和服务；三审§2.2标为旧裁定，§5注明已退回。历史终修段和原证据仅按当时输入解读。 |

当前桥行为由`Catalog::load`将目录例外映射为`same_item_unique=false`，`Engine::validate_inventory`及`Engine::target`据此允许双轴同种。不需要另在engine.rs加入桥特判。运行内核Rust生产源码保持原字节；本次行为修正在正式目录及其嵌入编译产物中完成。

服务恢复保留必要前件：箱有电、传输开启、仓库持续收得下成品、物种和预装合法。106tick依据现行桥独立链服务重新写明，不将其扩大成全厂恢复界。原基于同种互斥的17.8/22.7反例仅适用于旧规则，未改哈希冒充新证据。

具体脚本：[revert_bridge.py](revert_bridge.py)、[正文同步](revert_bridge_docs.py)。这些是带旧文断言的一次性变更记录，不是对任意版本通用的自动迁移器。

## 3. 正式源重锁

逐一按真实字节计算SHA-256，逐行比较目录`sources[].lines`和正式全文；`lines`是完整行数组，下表另列行数。同步`check_examples.py::SOURCE_HASHES`，更新56份活动输入/参数赋值的目录、轴表或配置引用。旧运行输出和旧周期证书不直接刷新指纹。

| 正式源 | 新SHA-256 | 行数 |
|---|---|---:|
| `《明日方舟：终末地》游戏规则.txt` | `4f04de50b2f743aec1da903f00f0f89f92f1aeba60eb4513320b71d0ee0a57fd` | 114 |
| `求解任务.txt` | `1630ca1febec79b324aa3afb110be2d3330298e266c68b06415c3d1516108bac` | 16 |
| `求解约束.txt` | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` | 126 |

新目录SHA-256：`0690960c37c1ebcd0d89aa57fa86b5ca9f5d83098330d1acc1fbf9b0be4ed60b`。目录版本为`2026-09-22-r13-bridge-r21-switch`。源全文核验见[catalog.log](catalog.log)，重锁脚本及逐输入引用清单见[relock.py](relock.py)、[relock.json](relock.json)。

`cargo build --workspace`和`cargo build --release --workspace`均退出0，目录已分别嵌入debug和release；未创建其他target。工具链为Rust 1.98.1、Clippy 0.1.98，完整版本见[toolchain.json](toolchain.json)。最终正向验收使用release内核，二进制SHA-256为`27d64241c18bf753ba53d52ef9196dbbdbd5c6166a61ef26bf7241123fe4c843`。

正向来源采用已有任务7样例`数据/样例/任务7内核/无线多格全收.json`，派生输入将引用路径绝对化后落在维护目录。`seed`→`check`→运行6tick→`verify-record`全部退出0；反向对照换回旧目录哈希，seed退出2。命令、二进制指纹和返回码见[positive-validation.json](positive-validation.json)，逐次原始输出见对应`kernel-*.log`，输入和结果分别是[positive-input.json](positive-input.json)、[seed-output.json](seed-output.json)、[positive-run.json](positive-run.json)。

## 4. 为完成全量验收处理的旧测试脱节

首次全量测试有97项在装载阶段失败，主因是任务7现有配置已将11个轴固定，而旧样例仍放在`fixedness_unproven`。只改哈希不能使这些输入合法。本次按已有配置迁移活动样例的轴分组、已定值、停止值与StateSeed镜像，并由输入重建两份参数投影；没有放宽内核配置或停止守卫。见[迁移脚本](migrate_active_inputs.py)、[回归同步脚本](sync_regressions.py)。

旧回归逐项对齐已经存在的任务7实现：身份门条件恢复取代永久锁存；空箱传输的JSON详情与冷却同时检查；生产周期维持diagnostic_cycle而非旧counterexample；非运输年龄已投影删除，溢出负例移到实际读取年龄的运输格，并核在装载派生阶段停止；CLI周期v3/记录v4版本和新增周期键审计来源进入严格指纹对照。没有删掉测试或把失败改为ignored。

两个参考场景重新由独立Python执行器计算，分别为4tick与12tick。相对旧记录，只允许当前参数镜像及闭包依据文字按新契约变化；库存、事件、轮询、台账、时刻、制造和手工黄金摘要逐字段不变。新结果写为`数据/样例/*-参考运行记录.json`，当前schema为kernel-output-v4，reference.rs读取新记录；旧v3文件原字节保持。见[reference-refresh.json](reference-refresh.json)及逐字段差异清单。

为保留CLI全链路检查，还修正了只读批量验收器解析相对目录路径时错误依赖cwd的问题：目录引用现在以实际输入文件目录为基准。不同cwd、两种记录编码、相对引用均通过。当前batch只支持kernel生产者，因此旧参考生产者成功断言拆为kernel正向验收与bounded-reference明确拒收；独立参考的正向验证仍由reference.rs和runtime_record验证器完成。

目录回源辅助工具原来仍断言任务条件10项、箱级冷却未定。本次同步到已存在的11项目录/正式任务和当前箱字段，使回源检查真正逐字段闭合；不改箱传输行为。回源6项测试通过，包含1232个单位叶子及154个配方叶子的变异拒收。

补充检查边界：`check_examples.py --self-test`默认把桥运行样例送入只支持两个参考场景的旧通用Decision检查，仍会因桥轴封套不属于Decision而停止。这不是本次必需的cargo或内核装载验收入口；未把该失败记为通过，也未放宽检查来消掉它。两个受支持参考输入检查已通过；桥本身由Rust实际运行回归、完整种子恢复和[Python桥/普通格对照](python-bridge-validation.json)分别验证。此旧自查入口的覆盖分拆应在维护阶段处理。

## 5. 验证结果与记录位置

| 检查 | 本次结果 | 证据 |
|---|---|---|
| 工作区debug / release build | 均退出0；未报告编译告警 | [build.log](build.log)、[build-release.log](build-release.log) |
| `cargo test --workspace` | 141 passed; 0 failed; 0 ignored | [test.log](test.log) |
| 全targets Clippy | 退出0，compiler-message告警0 | [clippy.log](clippy.log)、[clippy-diagnostics.json](clippy-diagnostics.json) |
| 正式目录回源与变异测试 | 目录通过；6 tests OK | [catalog.log](catalog.log)、[catalog-tests.log](catalog-tests.log) |
| 受支持参考输入 | 两份通过 | [supported-sample-check.json](supported-sample-check.json) |
| 桥例外及普通格反例 | 同种桥双轴接受、制造同种重复拒绝 | [python-bridge-validation.json](python-bridge-validation.json)及cargo中的bridge两项 |
| release内核正向链 | seed/check/run/verify-record均0 | [positive-validation.json](positive-validation.json) |
| 旧目录指纹负例 | 非零拒收，退出2 | [stale-catalog.log](stale-catalog.log) |
| 正式文件、候选、黄金及历史证据 | 最终与执行前SHA-256相同 | [final-audit.json](final-audit.json) |

完整命令、cwd、耗时和退出码保存在[commands.jsonl.log](commands.jsonl.log)。早期失败和修订验证保留在`*.attempt-*.log`及定向集成日志；最终结果以本节对应最新日志为准。最早两轮同名日志曾被后续轮次替换，其返回码仍在命令清单，失败原因已在§4记明，不声称保存了每轮的全部输出。

首轮Rust CLI测试的硬编码目录曾覆盖三份`crates/kernel/evidence/round5`旧输出。发现后将新运行产物保存到维护目录，三份旧文件均依据执行前完整SHA-256恢复：一个取现有同哈希副本，一个恢复计时字段的原序列化，一个依据历史v2来源清单重建确定性错误封套；只有完整哈希完全匹配才写回。详见[恢复核对](early-test-output-restoration.json)及恢复脚本。最终不将这些新输出留在历史证据原位。后续Python CLI测试用维护目录启动包装器仅重定向输出常量，每次使用独立attempt目录；Rust round5测试通过`KERNEL_TEST_EVIDENCE_DIR`重定向。测试断言和真实CLI调用照常执行。

## 6. 代码体检方案与交付自审

方案已写入[代码体检方案](../代码体检方案.md)。包含编译/Clippy基线、30个超过80行函数的逐项拆法、死代码识别边界、按行为重排测试、黄金与差分安全网，以及整棵证据快照移入单一归档根、按SHA-256保留出处、从代码索引排除的步骤。每一步均列行为不变的验收和回滚条件。本轮没有执行其中的函数拆分、死代码删除、测试目录重排或快照搬迁。

快照盘点为字节指纹及目录计数：数据/复核815文件、规格/复核1247文件、crates/kernel/复核2093文件；全工作区675组重复哈希只作候选盘点，不作为删除理由。当前`.codegraph`运行状态未修改。

读者视角自审覆盖：来源与旧证据时点分开，等待/恢复前件明确，17/22与106的当前数值唯一，物品格与占地不混记，样例计划率没有冒充实际保证，测试结果对应真实日志，文内本地链接可解析；方案中的后续步骤没有宣称已实施。正式源、候选、黄金、旧v3参考和复核证据在最终字节审计中核验，改动路径的前后哈希在同一审计文件登记。
