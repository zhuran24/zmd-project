# 2026-09-26 第 88—89 轮修正版三审同步记录（r29：改写「存货误料停机与可用台数」，条数仍 77）

执行记录。日期：2026-09-26。开工 HEAD `79bbe50`（第 88—89 轮入档提交；三审改的求解约束、求解充分条件与两份候选文件在工作区，尚未提交）。状态：目录与引用同步完成；指定安全集全部通过；历史证据目录前后全量哈希一致。本记录不构成目标循环或全厂达标认证。

## 起因

主会话三审第 88—89 轮修正版（报告见 [三审报告](../../候选约束轮次/三审-第88-89轮/三审报告.md)），求解约束只改一条：「存货误料停机与可用台数」并进停机态合计三份修正版多出的内容（四类之外，任取一个按时拿取、不再离线的后续，在其中只能再开有限批的机器都一并扣除，举两例；研磨机恰 32 台时另一种主料进空格这一步调试期后不能发生），据行补分流器。条数仍 77，内核写死的条数不改。求解充分条件.txt 同时由 10 条变 11 条，它不是内核来源（formal_catalog 只读三份正式文件），内核不需要为它改动。

## 结论与计数

目录版本 `2026-09-26-r29-constraints-77`，SHA-256 `74deafed83876dcf40fffe55d8f01d45edcfd2840e35725c8006da996c877e02`（旧 r28 为 `55eabb8e…ba49`）。共改 **62 个业务文件**：同步脚本一次写入 60 个，候选 B 报告与验证记录另 2 个。

| 正式来源 | SHA-256 |
|---|---|
| 《明日方舟：终末地》游戏规则.txt | `6e64e3903a65536c530b363c9f3aef8c1bb2a1c1193e866799125dd047159924`（未变） |
| 求解任务.txt | `1630ca1febec79b324aa3afb110be2d3330298e266c68b06415c3d1516108bac`（未变） |
| 求解约束.txt | `dfea46c61f29be5a86659bce3642009d2558ace073d6dbd79f5dd3c6475226db`（旧 `74409eaf…4ce4`） |

| 改动 | 文件 |
|---|---|
| 目录重建：用现行 `formal_catalog.py` 回源投影；只有约束投影、求解约束来源全文与 SHA、版本变化；任务、18 类单位、18 条配方、32 项常量、19 项物料流量逐对象不变；正文或据变化的恰为「存货误料停机与可用台数」一条，没有新增或删除的条目 | 数据/正式静态目录.json |
| 54 份内核输入（45 个样例、9 个 fixture）只换目录 SHA，其余字节不变 | 数据/样例/**、crates/kernel/tests/fixtures/* |
| 候选 B 来源清单：求解约束、目录两项按新值；候选约束.txt 一项按现行字节同步（r28 之后入档第 86—89 轮、第 86—87 轮与本轮三审改状态行与进度段） | 数据/候选B/来源清单.json |
| 正式约束 SHA | 数据/样例/check_examples.py（SOURCE_HASHES）、规格/运行语义.md（同一行的行数 158 改为现行的 168；158 是 72 条时的行数，此后几轮同步都没跟着改） |
| 规格自查的只读快照入口改到本目录 [只读文件指纹.json](只读文件指纹.json) | 规格/check_revision.py |
| 追加 r29 节 | 规格/修订记录.md |
| 候选 B 报告：用本轮构建的 `target/debug/topology` 重新生成（报告写到标准输出，落本目录 [candidate-b-report.log](candidate-b-report.log)，再原样写回），4924 项通过、0 项失败、80 项不能静态检；验证记录首段改指本记录 | 数据/候选B/校验报告.md、数据/候选B/验证记录.md |

两张规则覆盖表（规格/规则覆盖表.md、数据/规则覆盖表.md）按新目录重建后与原文逐字相同（条目行号、据行位置都没变），同步脚本断言了这一点，没有写入；数据/候选B/转换说明.md 的条数文字不变，也没动。所有内容与授权检查先于第一次业务写入；原字节备份在 [before/](before/)，逐文件前后 SHA 见 [sync-plan.json](sync-plan.json)、[sync-changes.json](sync-changes.json)。

## 实际命令与结果

脚本沿用 2026-09-26-第84-85轮三审同步：[guard.py](guard.py)（逻辑未改，只改首行说明）、[work.py](work.py)（常量、条数不变、改写条目名、修订记录一节、运行语义行数、覆盖表只核不写按本轮改）、[run.sh](run.sh)、[safe-suite.sh](safe-suite.sh)。共享 `CARGO_TARGET_DIR=求解器/target`，默认 dev/test profile，**没有新建 profile**，所以没有要删的 `target/<profile>/`。cargo 一律 `-j 4`，Rust 测试 `--test-threads=1`。完整 argv、环境、耗时、返回码见 [commands.jsonl](commands.jsonl)。

| 步骤 | 命令（cwd：求解器） | 退出码 | 结果 |
|---|---|---:|---|
| [cargo-build](cargo-build.log) | `cargo build --workspace -j 4` | 0 | 通过 |
| [candidate-b-report](candidate-b-report.log) | `target/debug/topology 数据/候选B/contract.json` | 0 | 4924 通过、0 失败、80 不能静态检 |
| [cargo-check](cargo-check.log) | `cargo check --workspace -j 4` | 0 | 通过 |
| [cargo-clippy](cargo-clippy.log) | `cargo clippy --workspace -j 4` | 0 | 无警告 |
| [kernel-lib](kernel-lib.log) | `cargo test -p kernel --lib` | 0 | 108 通过 / 0 失败 |
| [topology-lib](topology-lib.log) | `cargo test -p topology --lib` | 0 | 1 通过 / 0 失败 |
| [kernel-reference](kernel-reference.log) | `cargo test -p kernel --test reference` | 0 | 3 通过 / 0 失败 |
| [topology-validation](topology-validation.log) | `cargo test -p topology --test validation` | 0 | 30 通过 / 0 失败 |
| [kernel-doc](kernel-doc.log) | `cargo test -p kernel --doc` | 0 | 0 项 |
| [topology-doc](topology-doc.log) | `cargo test -p topology --doc` | 0 | 0 项 |
| [catalog-verify](catalog-verify.log) | `python3 数据/工具/formal_catalog.py` | 0 | 77 条约束上下文等全部一致 |
| [catalog-regressions](catalog-regressions.log) | `python3 数据/工具/test_formal_catalog.py` | 0 | 8 项通过 |

Rust 安全测试合计 142 通过、0 失败。没有运行 `cargo test --workspace`、七个 CLI 测试目标或任何写历史证据的脚本；没有跑 kernel 的 seed/check/run 正反链。每条命令前后活动源码与输入的哈希都没有变化。源码与测试入口没有改，第 0 批测试输出隔离的验收凭据不受影响（本轮也没有跑 CLI 测试）。

另在守卫之外只读跑了一次 `python3 规格/check_revision.py`（它不写文件），日志 [spec-check-revision.log](spec-check-revision.log)：与本轮有关的「正式约束 77 条覆盖」「三份正式文件与候选约束 4 份只读指纹未变」都通过（在脚本第 37、47 行，排在前面）；它停在第 115 行 T12 断言，与第 81、84—85 轮同步时相同，是选择点清单里的既有问题（2026-09-22g 记录已列），本轮没有改这些文件。

[审计](audit.json)（`run.sh audit`）：用现行 `formal_catalog.py` 回源核目录；56 份输入／参数赋值的 catalog、轴表、配置、声明引用全部与实际文件 SHA 相符；候选 B 16 项来源全部相符；SOURCE_HASHES 与目录来源一致；运行语义含三份来源 SHA；两张覆盖表的规则 114 行、任务 16 行与约束 77 行逐行对得上；报告含全部 77 个正式条目及其据行；只读指纹 4 份与现行文件一致。旧目录 SHA 与旧正式约束 SHA 的全仓检索（[after-old-sha-inventory.json](after-old-sha-inventory.json)）在活动文件中残留 0；余下命中为本轮记录 63 个、归档或并行目录 36 个、规格修订记录的旧节 1 个。

## 历史证据全量哈希

| 历史目录（相对求解器） | 文件数 |
|---|---:|
| crates/kernel/evidence | 572 |
| crates/kernel/复核 | 2094 |
| 数据/复核 | 815 |

共 3481 个文件、1018807436 字节，含 1 个 gitignore 忽略文件（[清单](ignored-history-files.json)）。按路径、字节数和完整 SHA-256 比较，每条命令前后、同步与审计前后以及交付时共 29 次与初始基线比较，新增 0、删除 0、修改 0。基线 [initial-history.json](initial-history.json)，交付 [delivery-history.json](delivery-history.json)、[差异](delivery-history-diff.json)。

## 留下的事

- `规格/运行语义.md` 第 229 行仍说「A=1113 的达标必要台数和无箱按约束 L113 核」（第 81 轮同步记录已列），本轮没有动。
- `求解器/构造/` 有文件引用旧目录或旧正式约束 SHA（上午记录已列），本轮没有动。
- 候选约束.txt 会被流水线继续入档，候选 B 来源清单与规格自查的只读指纹届时又会过期（与以往相同）。
- 「存货误料停机与可用台数」新增的「只能再开有限批的机器一并扣除」要看后续，内核没有为它写专门的检查。
