# 2026-09-26 第 84—85 轮修正版三审同步记录（r28：正式约束 75 条改为 77 条）

执行记录。日期：2026-09-26。开工 HEAD `38b0e30`（第 84—85 轮修正版三审采纳提交）。状态：目录与引用同步完成；指定安全集全部通过；历史证据目录前后全量哈希一致。本记录不构成目标循环或全厂达标认证。

## 起因

主会话三审第 84—85 轮修正版（报告见 [三审报告](../../候选约束轮次/三审-第84-85轮/三审报告.md)），求解约束改动：新增「存货首件死锁」「成品停收的停批范围」两条；「存货误料停机与可用台数」原句后接推广、据行改写。条数由 75 变 77，内核写死的条数与目录投影随之同步。求解充分条件.txt 同时由 4 条变 9 条，它不是内核来源（formal_catalog 只读三份正式文件），内核不需要为它改动。

## 结论与计数

目录版本 `2026-09-26-r28-constraints-77`，SHA-256 `55eabb8e53dc93c16d31e372e1d3ede8fa9e22d17337b14b7c82094ac3dfba49`（旧 r27 为 `f0e18dc1…c352`）。共改 **69 个业务文件**：同步脚本一次写入 67 个，候选 B 报告与验证记录另 2 个。

| 正式来源 | SHA-256 |
|---|---|
| 《明日方舟：终末地》游戏规则.txt | `6e64e3903a65536c530b363c9f3aef8c1bb2a1c1193e866799125dd047159924`（未变） |
| 求解任务.txt | `1630ca1febec79b324aa3afb110be2d3330298e266c68b06415c3d1516108bac`（未变） |
| 求解约束.txt | `74409eaf42e3c9f1671a017491b1f43ea5268b5aab7989b15a448cd447014ce4`（旧 `4813a423…0304`） |

| 改动 | 文件 |
|---|---|
| 目录重建：用改为 77 条的 `formal_catalog.py` 回源投影；只有约束投影、求解约束来源全文与 SHA、版本变化；任务、18 类单位、18 条配方、32 项常量、19 项物料流量逐对象不变；新增的恰为上述两条，正文或据变化的恰为「存货误料停机与可用台数」一条，没有删除的条目 | 数据/正式静态目录.json |
| 写死的条数 75→77 | 数据/工具/formal_catalog.py（断言与输出文字）、数据/工具/test_formal_catalog.py、数据/工具/check_revision.py、规格/check_revision.py、crates/topology/tests/validation.rs |
| 54 份内核输入（45 个样例、9 个 fixture）只换目录 SHA，其余字节不变 | 数据/样例/**、crates/kernel/tests/fixtures/* |
| 候选 B 来源清单：求解约束、目录两项按新值；候选约束.txt 一项按现行字节同步（r27 之后入档第 84—85 轮、本轮三审改状态行与进度段） | 数据/候选B/来源清单.json |
| 正式约束 SHA | 数据/样例/check_examples.py（SOURCE_HASHES）、规格/运行语义.md（并把「75 条约束覆盖」改为 77 条） |
| 规格自查的只读快照入口改到本目录 [只读文件指纹.json](只读文件指纹.json) | 规格/check_revision.py |
| 两张规则覆盖表的正式约束节按新目录重排序号、源行、`constraints[i]` 与据行，原有条目的去向文字按条目名沿用；新增两条的去向按同类必要条件登记（「§6：达标必要条件，保持原文前件；未作运行认证」）；标题条数改 77 | 规格/规则覆盖表.md、数据/规则覆盖表.md |
| 条数文字 | 数据/候选B/转换说明.md |
| 追加 r28 节 | 规格/修订记录.md |
| 候选 B 报告：用本轮构建的 `target/debug/topology` 重新生成（报告写到标准输出，落本目录 [candidate-b-report.log](candidate-b-report.log)，再原样写回）。与旧报告相比只差目录版本行、「不能静态检」78→80 项、改写条目的原文与据行和新增两条的行；4924 项通过、0 项失败 | 数据/候选B/校验报告.md、数据/候选B/验证记录.md |

所有内容与授权检查先于第一次业务写入；原字节备份在 [before/](before/)，逐文件前后 SHA 见 [sync-plan.json](sync-plan.json)、[sync-changes.json](sync-changes.json)。

## 实际命令与结果

脚本沿用 2026-09-26-第81轮三审同步：[guard.py](guard.py)（逻辑未改，只改首行说明）、[work.py](work.py)（常量、条数、新增与改写条目名、修订记录一节按本轮改）、[run.sh](run.sh)、[safe-suite.sh](safe-suite.sh)。共享 `CARGO_TARGET_DIR=求解器/target`，默认 dev/test profile，**没有新建 profile**，所以没有要删的 `target/<profile>/`。cargo 一律 `-j 4`，Rust 测试 `--test-threads=1`。完整 argv、环境、耗时、返回码见 [commands.jsonl](commands.jsonl)。

| 步骤 | 命令（cwd：求解器） | 退出码 | 结果 |
|---|---|---:|---|
| [cargo-build](cargo-build.log) | `cargo build --workspace -j 4` | 0 | 通过 |
| [candidate-b-report](candidate-b-report.log) | `target/debug/topology 数据/候选B/contract.json` | 0 | 4924 通过、0 失败、80 不能静态检 |
| [cargo-check](cargo-check.log) | `cargo check --workspace -j 4` | 0 | 通过 |
| [cargo-clippy](cargo-clippy.log) | `cargo clippy --workspace -j 4` | 0 | 无警告 |
| [kernel-lib](kernel-lib.log) | `cargo test -p kernel --lib` | 0 | 108 通过 / 0 失败 |
| [topology-lib](topology-lib.log) | `cargo test -p topology --lib` | 0 | 1 通过 / 0 失败 |
| [kernel-reference](kernel-reference.log) | `cargo test -p kernel --test reference` | 0 | 3 通过 / 0 失败 |
| [topology-validation](topology-validation.log) | `cargo test -p topology --test validation` | 0 | 30 通过 / 0 失败 |
| [kernel-doc](kernel-doc.log) | `cargo test -p kernel --doc` | 0 | 0 项 |
| [topology-doc](topology-doc.log) | `cargo test -p topology --doc` | 0 | 0 项 |
| [catalog-verify](catalog-verify.log) | `python3 数据/工具/formal_catalog.py` | 0 | 77 条约束上下文等全部一致 |
| [catalog-regressions](catalog-regressions.log) | `python3 数据/工具/test_formal_catalog.py` | 0 | 8 项通过 |

Rust 安全测试合计 142 通过、0 失败。没有运行 `cargo test --workspace`、七个 CLI 测试目标或任何写历史证据的脚本；没有跑 kernel 的 seed/check/run 正反链。每条命令前后活动源码与输入的哈希都没有变化。

另在守卫之外只读跑了一次 `python3 规格/check_revision.py`（它不写文件），日志 [spec-check-revision.log](spec-check-revision.log)：与本轮有关的「正式约束 77 条覆盖」「三份正式文件与候选约束 4 份只读指纹未变」都通过（它们在脚本里排在前面）；它停在 T12 断言，与第 81 轮同步时相同，是选择点清单、参数轴表里的既有问题（2026-09-22g 记录已列），本轮没有改这些文件。

[审计](audit.json)（`run.sh audit`）：用新 `formal_catalog.py` 回源核目录；56 份输入／参数赋值的 catalog、轴表、配置、声明引用全部与实际文件 SHA 相符；候选 B 16 项来源全部相符；SOURCE_HASHES 与目录来源一致；运行语义含三份来源 SHA；两张覆盖表的规则 114 行、任务 16 行与约束 77 行逐行对得上；报告含全部 77 个正式条目；只读指纹 4 份与现行文件一致。旧目录 SHA 与旧正式约束 SHA 的全仓检索（[after-old-sha-inventory.json](after-old-sha-inventory.json)）在活动文件中残留 0；余下命中为本轮记录 63 个、归档或并行目录 34 个、规格修订记录的旧节 1 个。

## 历史证据全量哈希

| 历史目录（相对求解器） | 文件数 |
|---|---:|
| crates/kernel/evidence | 572 |
| crates/kernel/复核 | 2094 |
| 数据/复核 | 815 |

共 3481 个文件、1018807436 字节，含 1 个 gitignore 忽略文件（[清单](ignored-history-files.json)）。按路径、字节数和完整 SHA-256 比较，每条命令前后、同步与审计前后以及交付时共 29 次与初始基线比较，新增 0、删除 0、修改 0。基线 [initial-history.json](initial-history.json)，交付 [delivery-history.json](delivery-history.json)、[差异](delivery-history-diff.json)。

## 留下的事

- `规格/运行语义.md` 第 229 行仍说「A=1113 的达标必要台数和无箱按约束 L113 核」（第 81 轮同步记录已列），本轮没有动。
- `求解器/构造/` 有文件引用旧目录或旧正式约束 SHA（上午记录已列），本轮没有动。
- 候选约束.txt 会被流水线继续入档（第 86、87 轮），候选 B 来源清单与规格自查的只读指纹届时又会过期（与以往相同）。
- 新增两条在 topology 报告里落在「不能静态检」默认一类；内核没有为它们写专门的检查。
