# 内核体检第 0 批输出隔离实施记录

续接状态（2026-09-23T02:46:33.883463-04:00）：**DONE，第 0 批执行验收通过**。13 项原字节仍缺失，已逐项标记；本轮未执行 Git 操作。最新结论及边界见文末“第 0 批最终验收”。以下原席 BLOCKED 段落保留为历史快照。

史料／执行记录。日期：2026-09-23；截止：2026-09-23T00:30:38.124144-04:00。状态：**BLOCKED，未达到第 0 批完成门槛**。工作区及归档基点：`aec2a30d945e21a492d4b05639d67a63abacb5b3`。本轮只实施测试输出管理；生产实现、公开 API、运行时验证脚本未改。本记录不批准运行七个 CLI 目标或进入第 1 批。

## 结论

七个活动 CLI 测试已接入独占输出目录；路径分配器、导入无历史写入、必需 fixture 拒收及保护器基础检查通过，尚未完成真实 harness 隔离验收。构建在三次有完整线程观测的尝试中均达到 11 线程，超过方案 §2 的 6 线程上限，因此停止后续构建和 CLI suite。§3.3 的两轮安全回归、§3.4 的完整来源闭包、§3.5 的二进制字节对照与 A/A 均未验收。受保护历史意外变化 **0 次／0 处**，无需恢复；未暂存、未提交，也未删除历史或其他既有文件。

机器汇总：[summary.json](summary.json)。改动路径与开工前后哈希：[changed-paths.json](changed-paths.json)。

## 阻点及停止位置

| 构建观测 | Cargo 退出码 | 峰值线程 | 判读 |
|---|---:|---:|---|
| [原始限制](production-a-build.command.json) | 0 | 11 | jobs=1、codegen-units=1、单 CPU affinity，未通过资源门槛 |
| [串行前端与后端](production-a-serial-build.command.json) | 0 | 11 | 增加 `RUSTC_BOOTSTRAP=1`、`-Z threads=1 -Z no-parallel-backend`，仍未通过 |
| [全部 rustc 调用显式注入](production-a-observed-build.command.json) | 0 | 11 | wrapper 对 host 构建依赖也补串行参数；仍未通过 |

最后一次峰值：[production-a-observed-build.thread-peak.json](production-a-observed-build.thread-peak.json)。Cargo 4 线程；rustc 在编译 proc-macro2 构建脚本时 7 线程（含 ctrl-c、coordinator、opt cgu.0）；完整 argv 证明串行参数已经传入。观测使用 `/proc` 的线程数，并记录 task 名，未用 CPU affinity 代替线程数。峰值不包括外层审计进程，因此计入外层只会更高。

一次 wrapper 参数试验把无值选项写成 `-Zno-parallel-backend=yes`，Cargo 退出 101；原始报错保存在 [production-a-wrapped-build.stderr.log](production-a-wrapped-build.stderr.log)，随后修正。下一次编译的观察器遭遇进程退出与 `/proc` 读取的竞态，该次未形成完整验收回执；编译最终完成，未继续执行二进制。观察器修复后用新 profile 重做完整观测，得到上表最后一项，不把竞态中断当成资源通过。

继续完成需要先取得符合总线程上限的构建方式，或由主会话明确调整该资源合同；本轮没有把“串行编译／单核”解释为已经满足 6 线程。`env.sh` 中的 bootstrap、编译器 wrapper 和串行参数仅为限线程试验环境，不是已验收基线。

## 测试输出改动

- 新增 `crates/kernel/tests/evidence_paths.py` 与 `support/mod.rs`；Rust 为每次执行分配新目录并显式传给 Python。显式根要求当前运行登记，缺省使用共享 target 的唯一目录；路径及祖先符号链接、相对路径和越界根拒收。失败目录保留并输出位置。
- 六个 Python CLI 入口改为显式 main；round6 的顶层执行移入 main；revision/revision_r2 不再用自动删除的临时目录。变异输入、日志及材料包从各自实例根派生。
- `revision_r3_cli.py` 固定两个历史 fixture 的实际原哈希，运行主体前校验，去掉 `if exists` 跳过覆盖。两组共 5 次调用的预期清单见 [r3-required-fixtures.json](r3-required-fixtures.json)；本轮只验证缺失／错哈希在调用内核前失败，5 次真实负例未执行。
- `benchmark_round6.py` 的生成输入、审计记录和结果改用隔离实例；`--out-dir` 须指该实例。`test_formal_catalog.py` 的正式源临时副本改放 `/tmp`。这些主体本轮未运行。
- 新增 `数据/工具/kernel_file_guard.py`，支持 snapshot/verify，并记录文件哈希、权限、目录及链接；本轮完整根的精确排除项固定在初始清单。该工具的一般性自查通过，后续回归运行器尚未实现。snapshot/verify 的排除项需按本轮清单显式传入，不把未配置的默认调用当作已验收入口。

上述变更仅在测试／工具范围，共 19 个路径记录（含新增 support 目录）。没有改 `src`、`tests/verify_all.py`、`tests/audit_task7.py` 或其运行时导入依赖。两份已跟踪 pyc 未清理，以遵守本次不删除文件的要求。

## 已做检查与未完成门槛

| 项目 | 本轮结果及证据 |
|---|---|
| 路径分配基础检查 | [helper-validation.json](helper-validation.json)：显式根、默认根、两次唯一分配、两个并发分配、中文空格、非法根／祖先链接拒收、子进程失败后目录保留 |
| 不同 cwd／fixture 失败 | [final-static-validation.json](final-static-validation.json)：从 `/tmp` 分配成功；两个必需 fixture 的缺失、错哈希共 4 个拒收通过 |
| 保护器及语法 | [guard-selfcheck.json](guard-selfcheck.json)：空目录、链接、改写、权限、增加、移动／缺失被识别；Python AST 检查通过。Rust 仅对七个改动入口及 support 做 rustfmt，未完成编译验收 |
| 恢复先行 | 已有独立提交 `8ab232419c0e9e1702a14c4881a173f15fdade84`；[restoration-readback.json](restoration-readback.json) 核 160 个目标仍等于恢复清单哈希；13 个原件缺失项原样保留 |
| 来源副本 | [freeze.json](freeze.json)：从 aec2a30 的 Git 对象以显式路径 archive，535 个文件置于 `/tmp/kernel-health-20260923-_5s65pp9/求解器`；无符号链接、无编译产物；JSON 路径重定位登记原／新哈希。尚未完成实际读取引用闭包，不能宣称冻结来源验收通过 |
| 工具环境 | [tools-observed.json](tools-observed.json)：解析路径、版本和哈希；AJV 只记入口及包版本，传递依赖未闭合 |
| 两轮安全回归 | **未执行**；构建返回 0 因线程超标被门禁拒收，不能算通过 |
| 副本隔离验收 | **未执行** harness no-run／dep-info／直接 harness 全集；`isolation_audit.py`、`.sh` 是待续脚本，不是回执，未生成 `isolation-accepted.json` |
| 编译闭包及新增模块负例 | **未执行** |
| 同路径同 profile 生产字节等价 | **未执行** A/B；未封存合格 A 二进制绑定 |
| A/A 与引用闭包 | **未执行**；`kernel_regression.py`、cases、比较字段清单和比较器负例均未实施 |

路径分配的基础测试不等于 §3.2 的真实 CLI 隔离验收。没有运行 `cargo test --workspace`、任何测试 no-run 构建、七个 CLI 目标或内核运行／验证链。生产构建日志均保留 compiler-artifact 原件，但没有把失败阶段的日志转为通过凭据。

## 保护范围、并行变化与文件落点

[initial-protection.json](initial-protection.json) 保存开工清单：40767 项，其中 36835 文件；包含跟踪／未跟踪／忽略文件、空目录、权限及链接。排除仅为已枚举 target／Git 内部／工具缓存和本轮输出根。四棵历史树及旧维护目录、R3 其他写点（包括样例／fixtures／黄金）均在保护范围。四树计数见 [history-coverage.json](history-coverage.json)。

正常完成的写文件任务命令前后均执行全量哈希对照；初始 bootstrap 自身也有前后检查。观察器竞态中断的一次缺少当时自动后检，构建结束后补做全量检查，见 [迟补后检](production-a-wrapped-fixed-build-late-after-protection.json)，结果无意外变化；没有把补检冒称即时回执。观察器现已用 finally 等待子进程退出并执行后检，注入观察器失败的自查也确认后检仍执行。每份 `*-protection.json` 将有意测试编辑、已知并行候选工作及意外变化分开列出；历史意外变化为 0。观察器竞态中断后的编译已经结束，并由后续全量保护检查核其未改写历史。

开工盘点期间 `候选约束轮次/第63-65轮/` 已有其他工作进程新增／修改文件；本任务尚未做任何该目录写入时就观察到变化，见 [bootstrap-check.json](bootstrap-check.json)、[concurrent-initial-diff.json](concurrent-initial-diff.json)。后续只记录该轮并行前后哈希，不回退、不操作其文件，不将其算作本次历史损害。三份正式源、根目录说明文件和其他受保护路径无本任务改动。

构建产物只在共享 `求解器/target` 的四个独立 profile 中；本目录仅有脚本、日志、JSON、Markdown 和 `cargo-test-evidence` 中的小型基础检查输出。忽略规则实核见 [output-ignore-check.log](output-ignore-check.log)。未触碰 Git 索引／提交／worktree；仅只读使用 rev-parse/status/log/archive/check-ignore 读取所需来源信息。副本及编译产物保留供诊断，不作为可提交证据。

## 续接入口

先处理上述资源阻点，再核当前生产／测试／正式源与清单是否一致。`isolation_audit.py` 只是测试前后字节对照和副本 harness 流程的草稿；运行前仍须补齐 §3.2 的精确 dep-info、全部运行时读取边界和完整矩阵，并实现 §3.3–3.5 的运行器，不能直接凭脚本存在宣告验收。后续发生源码、路径或依赖变化，须重建绑定并从相应阶段重新验收。


## 2026-09-23 续接：资源口径与保护检查点

以下为续接早期检查点，当时尚未完成第 0 批；最终状态见末节。开工清单为 [continuation-start.json](continuation-start.json)。当前命令入口为 [continuation_guard.py](continuation_guard.py)：Cargo jobs=2、codegen-units=1、测试及计算池单线程；统一四核 affinity，记录全部任务的 affinity、运行态线程、总线程和子树 CPU 时间/墙钟。等待线程不作为六核预算的拒收依据。首次合规 build 平均约 1.57 核、总线程峰值 23，退出 0。

按任务明确要求补齐六份原目录缺失标记，共列出 13 项原 SHA-256、原大小、现存哈希、保留位置和查找依据。原大小同时核 before.json 与 unresolved_targets.json。160 个可恢复目标、3 个早期 round5、7 个旧路径移出和14个历史版本再次核对通过。六份预期新增见 [continuation-markers-approved.json](continuation-markers-approved.json)，历史既有文件无改写；这六份新增在最终 history_changed 的结构变化口径中计入，不能报成 0。现存替代字节未删、未替换，未操作 Git。

活动工作区首轮 build/check/Clippy 退出 0，kernel lib 108 项通过；后检检测到研究文件 `候选约束.txt` 变化，守卫停止，未凑成一轮安全回归。测试进程退出后该文件继续变化，依据见 [continuation-external-drift.json](continuation-external-drift.json)。候选任务也扩展到第66–68轮，本任务按原任务对整个候选约束轮次目录的禁止写入要求，仅记录该树并发差异。没有回退外部修改。更正早期归类：变化的 `候选约束.txt` 不是三份正式源；三正式源均与冻结副本一致，见 [continuation-formal-sources.json](continuation-formal-sources.json)。

复用原席从 aec2a30 显式 Git archive 生成的 /tmp 冻结树，535 文件逐项重新核 SHA-256，无新 Git 操作。生产 A 新 profile 构建通过；精确产物 .d 与 checker/嵌入资源核对通过，topology 独立绑定。另一个 /tmp 副本实际新增并编译漏登模块，检查器拒收；见 [编译闭包](continuation-compile-closure.json) 和 [新增模块负例](continuation-compile-closure-negative.json)。

A/A 运行器及原 token 比较器位于 [continuation_aa.py](continuation_aa.py)。初次通用引用解析未考虑 static-catalog-v2 的 source_root、输出中保留的原输入 parameter_assignment 上下文，探索运行被拒收并保留。比较器已按读取契约区分基目录，未更改源和输出字节。先前8对探索捕获归 preliminary-aa，不计完整验收。该检查点开始重新执行44例，每例重做前置产物、在固定路径运行并验证，再将整目录移入封存（未删除）。Python、Node、AJV传递依赖及 native 启动文件在新运行前绑定，逐命令前后核哈希，并以 strace 核成功打开的实际文件路径。

### 续接已完成门槛检查点

44 对 A/A 全部通过，五个计时字段的实际 A/B token 值写入 comparison-fields.json，其他字节未作归一化。两轮封存捕获按原物理路径实拷复原，全部 JSON 引用递归核原 SHA-256，完整边表为 continuation-reference-closure-a.json 与 -b.json；验证时仍处于 A 源上下文。产物统一通过 schema 检查，运行记录及周期逐项调用同版本验证器；非 completed 的孤立记录按契约拒收，周期装载失败的正确诊断可由 verify-cycle 成功复现。全部额外验证回执见 continuation-a-validation-receipts.json。验证副本整目录移入 target 封存，未删除。

生产 A 在原根和原 profile、明确绑定的编译器/标准库/锁定 registry 来源下重建，原始字节与执行 A/A 的 A 二进制完全相同；随后覆盖登记的测试基础设施、强制实际重编 B，kernel 与 topology 两个生产二进制均与 A 逐字节相同。见 production-byte-equivalence.json、continuation-production-a-rebound.json、continuation-build-inputs.json。

B 版本在同一冻结根使用独立安全 profile 连续执行两轮 build/check/Clippy、kernel/topology lib、reference、topology validation、doc，18 条命令全部通过；当时回执已封存于 isolation-before-r3-fix/；该检查点尚不等于真实 CLI 隔离矩阵已完成，最终版本的安全回归与隔离凭据见末节。


## 第 0 批最终验收

截止：2026-09-23T02:46:33.883463-04:00。判定 **DONE**，只适用于本记录绑定的第 0 批测试基础设施与冻结来源。生产实现、公开 API、运行时 verify_all.py / audit_task7.py 及其原有导入依赖未修改。未运行 cargo test --workspace 的无选择全测试入口；副本使用方案指定的 --tests --no-run 构建及已绑定 harness 绝对路径执行。

| 完成门槛 | 结果 | 证据 |
|---|---|---|
| 恢复逐项对账 | 160 可恢复项、早期 round5 3 项、7 个旧路径移出、14 历史版本定位再次通过；13 原件缺失有 6 份原目录标记 | [恢复回执](continuation-restoration.json)、[标记清单](continuation-markers-approved.json) |
| 连续两轮安全回归 | 最终 R3 修复版本连续两轮，各 9 命令；Clippy 0 告警 | [安全回归](continuation-safe-passed.json)、[命令结果](continuation-frozen-safe-results.json) |
| 七 CLI 与副本隔离 | 13 个 metadata 目标完整对应；每轮 149 项，7 CLI 各执行两轮；两个 bin harness 为 0 项 | [凭据](isolation-accepted.json)、[两轮结果](continuation-isolation-rounds.json)、[harness](test-harnesses.json) |
| 文件/目录保护 | 既有历史文件字节、权限、结构改写 0；6 份授权新增标记单列 | [最终保护](continuation-final-protection.json) |
| 编译闭包 | kernel lib/bin 与 20 个 checker Rust 路径及嵌入资源对应；topology 单独绑定；实际编译的漏登模块被拒收 | [正例](continuation-compile-closure.json)、[负例](continuation-compile-closure-negative.json) |
| 同路径同 profile 生产字节 | kernel 与 topology 的最终 B 二进制均与用于 A/A 的 A 原字节完全相同；工具链与 registry/标准库绑定 | [字节对照](production-byte-equivalence.json)、[A 重建绑定](continuation-production-a-rebound.json)、[构建输入](continuation-build-inputs.json) |
| A/A 与引用闭包 | 44 对通过；仅五个登记计时 token 可变；所有捕获、递归引用边、schema 及同版本验证回执保留 | [A 最终绑定](continuation-a-final.json)、[A/A](continuation-aa-results.json)、[引用 a](continuation-reference-closure-a.json)、[引用 b](continuation-reference-closure-b.json) |

隔离矩阵包括显式根、无配置默认根、两轮重复、不同 cwd、中文/空格路径、两个并发实例、子进程失败及非法根拒收。六个 Python 导入无写入、祖先链接拒收和两件必需 fixture 的缺失/坏哈希负例在最终副本重新核验。每次实例独占输出目录，失败产物保留。成功写打开调用另经 [写入边界审计](continuation-write-boundary-audit.json) 核查，包括测试结束后已不存在的临时路径；按 §3.2 明确登记 test_formal_catalog 在 /tmp 中对三份正式源副本执行的漂移反例与自身临时目录清理，不把它当作历史写入。新入口 `数据/工具/kernel_regression.py` 提供 tests、check-isolation、aa、capture、compare；tests 接口在凭据验收后另外实际执行 9 条安全命令和七个正式 CLI suite，全部通过。实现与清单通过 runner-binding.json 绑定，源上下文失效时拒绝重新运行 A。接口正向/拒收回执见 [入口核验](continuation-entry-validation.json)。

资源判据为实际计算占用，统一四核 affinity、jobs=2、codegen-units=1、测试/计算池单线程。实际 CPU 时间÷墙钟和运行态/等待线程均进入逐命令记录。Node 或 rustc 的等待线程可使总线程数超过 6，不再误判为计算超限。最终验收的独立 harness 按序执行；双实例探针单独并发两个 harness。

### 六份历史结构变化的准确口径

`history_changed=6`，全为本任务明确要求新增的 `原字节缺失.json`。既有历史文件改写、权限变化与意外结构变化均为 0，因此没有需要从 Git 恢复的历史损害；没有执行 Git 命令、暂存或提交。六个新增路径为：

- `crates/kernel/evidence/revision-r4/cli/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/negative/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/reference-package/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/package/full_state_each_instant/records/原字节缺失.json`
- `crates/kernel/evidence/round6/cli/原字节缺失.json`

研究文件 `候选约束.txt` 和候选约束轮次树的外部变化单独保存前后指纹及归属依据，未回退、未写入，未算作本任务历史损害。三份正式源保持与冻结副本相同。

### R3 读取边界修复与已知覆盖边界

真实 strace 验收发现历史 expired fixture 的绝对 catalog 引用仍读取原仓库。最终测试在本次实例内重定位历史 fixture 的 path 字段。补拷第三份只读输入 tiny-expired-after-closure-input.json，先按旧指纹核其原字节，再仅变换派生输入的路径 token，并把派生 legacy record 的唯一 input 指纹绑定到新输入原字节。三个历史原件、其原始哈希、所有非路径状态数据、用例名、次数和原有断言保持；源 catalog 等历史依赖哈希不重锁。每次执行保存路径映射、原/新目标 SHA-256，之后重建绑定并重跑全部隔离矩阵与两轮安全回归。详见 [最终修复账](continuation-r3-final.json)、[源差分](continuation-r3-final.diff.log) 和 [只读测试资源补充](continuation-cli-fixtures.json)。该补充不在非测试编译闭包或44例 A/A 的消费闭包内，生产对照资源与运行时验证实现均未变化。

该 fixture 的历史引用哈希已经过期，现有用例因来源不匹配提前拒收。[断言与用例对账](continuation-r3-assertion-parity.json) 确认原 12 条断言全部保留，最终为 17 条；59 个用例的名称、退出码和状态在两轮中与修复前一致。第 0 批不重锁这些历史字节、不更改内核运行语义，也不把该命名用例的通过宣称为新增的过期门状态覆盖。13 项原字节缺失同样没有被回归通过核销。

A/A 的探索性拒收、解析器修正和旧版本隔离回执均保留在 preliminary-aa、failed-*、isolation-before-r3-fix 等索引/目录中；不计作最终通过。冻结源实拷位于 /tmp，所有编译产物及大型原始捕获只在共享 target；本目录保存脚本、日志、JSON、Markdown 和规定的隔离测试输出。
