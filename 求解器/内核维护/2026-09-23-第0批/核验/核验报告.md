# 内核体检第 0 批独立核验报告

日期：2026-09-23。状态：**FAIL——未达到第 0 批完成门槛**。保护审计截止：2026-09-23T00:42:17.280188-04:00。

核验对象为《[内核代码体检与增量维护方案](../../代码体检方案.md)》第 1 节第 0 批门槛、§3.2—§3.5 与第 8 节，连同恢复仍成立的前置条件。工作区基点为 `aec2a30d945e21a492d4b05639d67a63abacb5b3`。本报告的 FAIL 是完成验收不通过，不表示已发现生产算法行为改变。

## 1. 结论与门槛

独立核对发现：改动范围、历史保护、恢复目标的字节核对及基础隔离探针通过；恢复缺失标记、真实隔离凭据及后续动态验收未通过。当前不能宣告第 0 批完成或进入第 1 批。

| 完成门槛 | 结论 | 独立证据与边界 |
|---|---|---|
| 恢复逐项对账仍成立 | **部分通过，整体未通过** | 159+1 个目标与原哈希一致；早期 round5 3 项一致；7 个原路径已移出，14 个提交版本均可定位并匹配 blob。13 项缺失原字节仍缺失，相关 6 个原目录均无规定的缺失标记。 |
| 连续两轮安全回归 | **未通过** | 重新执行安全 build，命令树观察到 8 线程即终止，超过 ≤6；完成轮数为 0。 |
| 七 CLI 隔离与受守卫 suite | **未通过** | 6 个 Python import 探针、分配器与 fixture 前置检查通过；没有真实 harness no-run 来源验收及有效 isolation-accepted.json。七 suite 实际执行数为 0。 |
| 完整文件/目录保护 | **通过** | 四棵历史树共 4,722 文件，文件字节、权限、目录、链接新增/删除/改写均为 0；旧维护目录无变化。 |
| 冻结来源与编译闭包 | **未通过** | 535 个已登记冻结文件逐个哈希通过、无额外文件或符号链接；没有精确产物 dep-info 闭包及新增模块漏登拒收，运行时外部依赖闭包也未验收。 |
| 同路径、同 profile 生产二进制字节相同 | **未通过** | 没有合格且绑定完整的 A/B 二进制对照，源码未变不能替代该门槛。 |
| A/A、字段白名单与引用闭包 | **未通过** | 运行器、cases、binaries、比较字段清单及比较器拒收用例未完成，未执行 A/A。 |

机器门槛：[gates.json](gates.json)。最终保护结论：[protection-conclusion.json](protection-conclusion.json)。

## 2. 独立方法与改动范围

没有调用任何 git 命令，没有暂存、提交、切换、回退或删除文件。为满足工作区差分核查，用标准库只读解析现有索引和对象；每个读取的 Git 对象均重算对象 SHA-1。未修改 Git 元数据。索引对当时 HEAD 无暂存差异，HEAD 与实施基点一致。实现见 [read_objects.py](read_objects.py)；结果见 [source-diff.json](source-diff.json) 与 [workspace-changes.diff](workspace-changes.diff)。

独立盘点识别 18 个变更文件与 1 个新增目录，与实施席所列 19 路径相符；不靠其 changed-paths.json 选择差异。每个已有变更文件的基点字节也与开工保护清单一致。改动为七个 Rust CLI 入口、六个 Python CLI 入口、benchmark_round6.py、evidence_paths.py、support/mod.rs、kernel_file_guard.py、test_formal_catalog.py；support 目录单计。

差分限于测试输出、测试入口和保护工具。kernel/topology src、Cargo 配置与锁文件、三份正式源、运行时 verify_all.py/audit_task7.py 及仓内样例 Python 依赖没有变化；定向源码比较覆盖 46 文件，全仓保护对比也未发现这些范围的其他变动。round6 顶层测试主体移入 main 后，去掉新增 global/目录分配两条语句，其 AST 与原主体完全相同。R3 两个必需 fixture 增加哈希检查并去掉存在性跳过，属于 §3.2 明确要求。未发现生产实现或公开 API 越界改动；没有用静态结论冒充运行语义或二进制动态等价验收。

## 3. 恢复核验与缺失标记

[restoration-audit.json](restoration-audit.json) 保存每项原哈希、现时哈希、历史 blob 校验及归档定位。159 项直接同时核 f8f6129、7da52a7 对象原字节；第 160 项核 pre_day_hash_drift.json 原哈希和当前同哈希样例。另核 early-test-output-restoration.json 的 3 项。

独立确认恢复提交 `8ab232419c0e9e1702a14c4881a173f15fdade84` 位于实施基点的首父祖先链，其差异没有生产 src/Cargo 修改。7 个新增历史路径已不在旧轮次目录；14 个历史版本全部与对应提交 blob 相符，其中两组相同版本通过 journal 显式指向同一 b 版归档，没有伪造独立副本。

**新增核验发现：13 项缺失原字节只在集中 journal 中登记，相关原目录没有 `原字节缺失.json`。**这不满足方案 §3.1 第 6 项和恢复验收条款。按缺失文件的直接父目录列出的 6 个缺失标记为：

- `crates/kernel/evidence/revision-r4/cli/原字节缺失.json`
- `crates/kernel/evidence/round6/cli/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/negative/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/package/full_state_each_instant/records/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/reference-package/原字节缺失.json`
- `crates/kernel/evidence/round6/revision-r5/cli/原字节缺失.json`

这些标记在本轮开工清单里也不存在，因此是未完成的恢复要求，不是本次删除。13 项现存替代字节与原恢复 journal 相同，不能视为原件已恢复。未重复进行全机原字节搜索，也没有替补生成或修改历史。

## 4. 实际执行与资源守卫

执行源码见 [safe_commands.py](safe_commands.py)、[rustc_serial.py](rustc_serial.py)。实际 cwd 是登记的稳定工作区 `求解器`；共享 target 不变，profile 为 `healthaudit1790138353409234070`，只在新 profile 写入编译产物。设置 jobs=1、codegen-units=1、单 CPU affinity、测试/计算池单线程，并向每次 rustc 调用注入 `-Zthreads=1 -Zno-parallel-backend`。实际环境、工具路径、argv、日志哈希均留存。

| 命令/探针 | 实际结果 |
|---|---|
| rustc -Vv、cargo -V、python --version、clang --version | 均退出 0，见 version-fixed-*.log 与 [toolchain.json](toolchain.json)。 |
| cargo metadata --locked --offline --no-deps --format-version 1 | 退出 0；枚举 13 个目标，仅作为目标清单，不算测试通过。 |
| cargo build --locked --offline --workspace -j 1（独立 profile） | 守卫终止，退出 -15；命令树峰值至少 8，含观察器至少 9，超过 6。 |
| 六 Python 入口无写入导入、显式 main | 6/6 通过；运行时审计钩子拒绝任何写文件、建目录和启动子进程。 |
| 分配器基础探针 | 两次独占、显式/默认根、中文空格、Rust→Python 风格实例交接通过；重复使用、相对/历史/穿越根、祖先链接共 5 项拒收通过。 |
| R3 fixture 前置探针 | 有效两件到达停止哨兵；缺第一件、坏第一件、缺第二件、坏第二件均在目录分配及内核调用前拒收，共 5/5 通过。 |

完整构建记录：[safe-build-fixed.json](safe-build-fixed.json)、[stderr](safe-build-fixed.stderr.log)。观察到 cargo=4、rustc=4，均有 `/proc` 线程名和 argv；每 5ms 采样，首次超限即对本次独占进程组发送 SIGTERM。该读数是终止时已观测下界，不是允许构建跑完后的峰值；外层 shell 等没有计入，结论不依赖是否计观察器。

实施席三份峰值记录里的线程逐项求和确实各为 11，但那只是对留存材料的算术复核。本次独立实测证明当前配置依然超限，不声称重新观察到了三个 11。

首次自建 wrapper 用 execv 处理 Cargo 传入的裸 `rustc`，在编译前报 FileNotFoundError（build 退出 101）；原始 [日志](safe-build.stderr.log) 保留。仅修正核验目录内 wrapper 为 execvp 后，以新 profile 重跑取得上述资源拒收。初次错误不计资源失败或测试失败。

根据 §3.3 失败即停及 §8.0 过渡规则，未继续 check/Clippy/lib/reference/validation/doc，也未运行任何真实 CLI suite、旧二进制或 A/A 链。未为取得通过结果放宽线程预算。分配器和 fixture 探针是受限前置检查，不是 suite 替代品；探针源码与回执见 [probe_isolation.py](probe_isolation.py)、[isolation-probes.json](isolation-probes.json)。

## 5. 未完成凭据及冻结边界

实际检查确认以下文件不存在：实施运行根的 `isolation-accepted.json`、`test-harnesses.json`、`cases.json`、`binaries.json`、`comparison-fields.json`，以及 `数据/工具/kernel_regression.py`。因此 §3.3 的 check-isolation 接口也不可执行，没有将脚本存在当作凭据。

[freeze-readback.json](freeze-readback.json) 复核 `/tmp/kernel-health-20260923-_5s65pp9` 内 535 文件：全部与 freeze.json 匹配，额外文件 0、链接 0。此结论仅证明留存副本未漂移，不证明输入引用、producer.path、Rust `.d`、嵌入资源或 Python/Node/AJV 传递依赖闭合。没有新增模块拒收回执；没有同路径同 profile A/B 原始二进制字节对照；没有固定捕获根下 A/A、保留 token 字节的比较器负例与引用哈希递归验收。上述均保留为未完成。

## 6. 历史保护及并发变化

核验采用独立 [audit_files.py](audit_files.py) 全仓重算 SHA-256，记录权限、文件类型、大小、链接目标及空目录，不遵循 Git ignore 排除历史。排除沿用已登记的 Git 内部、target、工具缓存，但只排除本次核验输出根，**实施席已有记录仍纳入保护**。

| 历史树（相对求解器） | 条目 | 文件 | 变化 |
|---|---:|---:|---:|
| `crates/kernel/evidence` | 595 | 566 | 0 |
| `crates/kernel/复核` | 2154 | 2094 | 0 |
| `数据/复核` | 1015 | 815 | 0 |
| `规格/复核` | 1370 | 1247 | 0 |

四树合计 5,134 条目、4,722 文件；最终均与实施开工时相同，旧维护目录也相同。保护还覆盖正式源、规格、样例、fixtures、黄金和其他仓内内容。没有缩减为仅检查四树。

最终独立快照共 40,989 条目、37,044 文件，扫描错误 0。相对实施开工清单有 108 项差异：19 项本批测试基础设施编辑，以及 89 项已指明的并发候选任务差异。相对核验自身开工有 18 项差异（16 文件、2 目录），全部位于 `候选约束轮次/第63-65轮/`。这些变化保存前后哈希，归属依据为任务明确说明的并行工作范围及本核验全程限定的写入路径，未回退或碰触。实施席记录变化数 0；其他非候选保护路径变化数 0。

完整证据：[开始快照](start-snapshot.json)、[最终快照](final-snapshot.json)、[相对实施开工差异](final-vs-implementation-start.json)、[相对核验开工差异](final-vs-audit-start.json)、[最终历史审计](final-history.json)。`history_changed=0` 指受保护历史文件相对实施开工的变化数；不把并发候选产物、已有原件缺失或缺失标记误算作本次历史改写。

## 7. 交付与验收边界

报告、核验脚本和日志均在本目录。新建的少量 fixture 沙箱只在 `/tmp`，默认输出分配探针与构建产物只在共享 target 的新专属目录，具体位置见回执；全部保留，未执行清理或删除。没有修改实施席文件，也没有代做尚缺的实现。

完成验收仍需补齐原目录缺失标记、得到合规资源构建、完成真实隔离矩阵与两轮回归，再补编译闭包正负例、生产二进制字节对照、A/A 和引用闭包。这些工作未完成前，判定保持 **FAIL**。
