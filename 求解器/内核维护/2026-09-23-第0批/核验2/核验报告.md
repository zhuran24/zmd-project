# 内核体检第 0 批第二次独立核验报告

截止：2026-09-23T03:23:07.097200-04:00。结论：**PASS，当前工作区的第 0 批执行验收通过**。核验对象为[方案](../../代码体检方案.md)第 1 节完成门槛、§3.2—§3.5 及第 8 节。上次报告保留在[核验/核验报告.md](../核验/核验报告.md)。本结论不代表 13 项缺失原件已恢复，也不增加 R3 历史 fixture 的语义覆盖。

**历史变化的计数口径：`history_changed=6`。**以实施开工清单 `../initial-protection.json` 为基准，六个原目录各新增一份 `原字节缺失.json`；既有历史文件的改写、删除、权限变化均为 **0**。以本次核验开工的 `start-snapshot.json` 为基准，历史文件及实施席文件的变化均为 **0**。六份授权补记不归零，也不算历史字节损坏。

| 完成门槛 | 独立核验结果 | 证据 |
|---|---|---|
| 恢复逐项对账 | 160 个目标、3 个早期 round5 文件哈希符合清单；7 个旧路径缺席，14 个归档版本可定位；6 份标记完整覆盖 13 项缺失原件 | [逐项对账](restoration.json) |
| 连续两轮安全回归 | 每轮按 §3.3 执行 9 条命令，18 条均退出 0；两轮 Clippy 各 0 告警 | [逐命令结果](safe-results.json)、[完成凭据](safe-passed.json) |
| 七 CLI 与隔离 | 13 个 metadata 目标与实际 harness 完整对应；两轮各 149 项通过，七 CLI 各两次；隔离矩阵通过 | [suite 结果](guarded-suite-results.json)、[目标与来源](harness-binding-audit.json)、[矩阵](matrix-results.json) |
| 文件、目录及读写边界 | 四棵历史树 4728 文件、5140 条目受保护；本次历史变化 0；真实读写没有越界 | [最终历史](final-history.json)、[读边界](read-boundary-audit.json)、[写边界](write-boundary-audit.json) |
| 编译闭包 | 精确 compiler-artifact 对应的 .d 与 20 个 checker Rust 路径及嵌入资源吻合；topology 单列；实际编译的漏登模块被拒收 | [正例](own-closure-positive.json)、[精确路径再核](exact-checker-closure-audit.json)、[负例](own-closure-negative.json) |
| 同路径同 profile 生产字节 | 独立副本先构建 A，再覆盖登记的测试基础设施并实际重编 B；kernel、topology 均与封存 A 原字节完全一致 | [独立对照](own-production-equivalence.json)、[原实施 A/B 字节重核](retained-production-bytes.json) |
| A/A 与引用闭包 | 独立重跑 44 对；235 对文件仅允许五个计时 token 替换；其余字节完全相同，5366 条递归引用边通过原字节哈希核验 | [独立比较](fresh-independent-aa.json)、[引用闭包](fresh-independent-references.json)、[同版本回执](own-validation-receipts.json) |

安全回归每轮的实际通过数为 kernel lib 108、topology lib 1、reference 3、topology validation 30，共 142；两个 doc 目标各 0。完整 harness 加上七个 CLI 单项后为 149，两个 bin harness 各 0。149 是 Rust harness 的计数，不能与 CLI 内部子用例数混用；R3 内部两轮各 59 项，名称、退出码和状态一致。

**核验方法。**没有执行 git 命令、访问 Git 对象/索引或修改实施席文件。恢复目标的现时字节和归档字节直接重算 SHA-256，与方案给定的原始恢复清单及历史 journal 逐项核对。恢复提交 `8ab232419c0e9e1702a14c4881a173f15fdade84` 的对象级及祖先关系核验属于上次报告；本次遵守不碰 Git 的边界，没有把该关系冒称为新做的核验。当前改动待提交不影响这里的工作区执行结论，本报告不作新的提交状态认证。

连续回归从已绑定的 B 冻结根运行。每次实际执行前后重核隔离凭据的源文件、脚本、harness、二进制、dep-info 和运行时依赖哈希；执行的是清单中的绝对 harness 路径。另执行 cargo metadata 和每个 harness 的 --list 重新数目，重新解析精确 .d，并核二进制内嵌来源根。没有用无选择的 `cargo test --workspace` 替代安全命令或 CLI 清单。

独立 A 副本从留存冻结文件及封存 A 测试源码逐文件恢复，535 个来源文件先核原登记哈希；仅对新物理根作登记的路径重定位，A/B 共用同一资源集。新根和映射见[独立冻结清单](own-freeze.json)。原 R2 的 44 个分支模板与实施 cases 的 argv、预期退出码逐项对应，见[模板审计](case-template-audit.json)。每次捕获都重新生成前置记录，使用同一固定 case 输出路径，完成后将整目录移入共享 target 的专属封存目录；没有重用 R2 的旧产物。

独立生产对照使用物理根 `/tmp/kernel-audit2-v3r63b5w/求解器`、profile `healthaudit2prod1422118`，两次构建均有真实非 Fresh 的本地编译产物。编译器、标准库、锁定 registry、Python/Node/AJV/sha256sum 依赖按绑定清单在阶段边界重核。A 捕获中的 kernel-output-v4/kernel-cycle-v3 文档通过 schema 检查，并完成 84 条同版本验证/覆盖回执后，才替换成 B 测试上下文。非 completed 独立记录的预期拒收仍保留，未把退出 2 当作成功验证记录。

留存实施 A/A 的原字节也独立重比了一遍：44 对、235 对文件、5 个计时字段、5,366 条引用边均通过，见[留存比较](retained-independent-aa.json)及[留存闭包](retained-independent-references.json)。新捕获又经另一份比较程序重新检查 JSON 顶层 Pointer 的原 token 位置；替换只发生在内存比较副本中，没有 JSON 重新序列化、全局替换或整类忽略 sha256。比较器针对缺字段、错类型、数组交换、键序、缩进、重复键、错来源映射、坏引用哈希、缺目标及未登记差异的拒收探针通过，见[拒收检查](comparator-probes.json)。

**改动范围。**相对实施开工，独立识别 19 个活动测试/工具文件及一个 support 目录的变更；完整差分见[scope.diff](scope.diff)，清单见[scope.json](scope.json)。kernel/topology 生产 src、Cargo 配置及锁文件、三份正式源、verify_all.py、audit_task7.py 和原运行时样例 Python 依赖未变；定向保护核对覆盖 111 个路径。round6 移入 main 的 38 个主体语句及原辅助函数 AST 相同。其余改动为输出分配、测试入口、强制 fixture 校验、测试中的引用路径重定位、保护器和回归运行器；未发现生产实现、公开 API 或运行语义越界改动。生产二进制的独立逐字节等价为这一结论提供动态补证。

方案文件的资源口径变化单列；根研究文件 `候选约束.txt` 也与实施开工不同，并非三份正式源。其并发归属材料为 `../continuation-external-drift.json`，本次只确认该差异在核验开工前已存在、它不在冻结消费闭包内且本次没有改写，未重新证明作者身份。候选约束轮次目录按任务边界排除归责，仅记录差异。本次开工至收尾该目录共观察到 67 个条目变化；其他受保护路径变化 0。

**上次两项问题。**六份缺失标记现已存在，每项的原 SHA-256、原大小与两份原始清单交叉核对；现存 SHA-256、字节数、处置位置及查找依据均核实。13 项仍是原字节缺失，当前替代字节未被冒称恢复，也没有重生成历史。本次没有重复全机搜索，搜索范围局限沿用历史证据，不能声称穷尽全机。

资源不再按总线程数判失败。所有运行子树限制到同一组四个逻辑 CPU，Cargo jobs=2、codegen-units=1，测试/计算池单线程；核每个采样线程的 affinity 及子树 CPU 时间/墙钟。共 251 条有资源回执的命令，最大平均占用 1.980 核，总线程峰值 23，未发现 affinity 越界或超过六核预算。等待及排队线程不作为同时计算核数；双实例探针只并发两个已绑定 harness。详见[资源汇总](resource-summary.json)和逐命令 `.command.json`。

**R3 覆盖边界。**相对原始 A 源码，断言由 10 条增至 17 条；相对续接修复前为 12 条增至 17 条，原断言没有删除。三个历史 fixture 原字节未改；派生输入只作路径重定位，并为该派生输入的原字节重绑一个 input 指纹。实际两轮输出均显示 expired-after-closure 的 run/seed/cycle 以及 legacy record 因“源文件指纹不符”提前拒收；因此这几项不能作为过期门状态校验已覆盖的证据。详见[本次运行边界](r3-runtime-boundary.json)、[断言核对](r3-continuation-assertions.json)。这项旧覆盖限制不由本批回归通过核销。

**核验自身错误与修正。**首个尝试把本报告目录直接用作 CLI 输出根，缺少规定的 run-registration.json，被分配器以退出 101 拒收；日志见[suite-1-3-revision_cli.stderr.log](suite-1-3-revision_cli.stderr.log)。随后在本目录内新建符合结构和登记要求的 `内核维护/独立回归 中文/`，完整重跑两轮 harness 和隔离矩阵。失败尝试保留，未计入任何通过轮次；没有通过修改实施代码绕过守卫。无未解决的执行错误。

完整开工/收尾盘点均重新读取所有受保护文件的 SHA-256，覆盖目录、空目录、链接、权限及忽略文件；安全回归和 CLI 阶段逐命令全量核哈希。后续高频捕获仍逐路径盘点，仅对设备号、inode、权限、大小、mtime、ctime、链接数全部未变的文件复用本核验已算出的哈希，变动项即时重哈希；最终再次做无缓存全量盘点。最新盘点有 46138 条目，扫描错误 0。[最终快照](final-snapshot.json)、[相对实施开工](final-vs-implementation-start.json)、[相对本次开工](final-vs-audit-start.json)保留完整证据。

报告、脚本、日志和索引全部位于本目录；源码副本只在 `/tmp`，编译及大型捕获只在共享 target 的本次专属路径。未手工删除任何文件，未修改或清理历史/实施产物；受守卫测试自身对新实例临时文件的既有生命周期操作仍按方案运行。
