# 规格关联终修差异

执行记录，截止2026-09-21。

## 运行语义.md

```diff
--- /home/zhuran24/zmd-research-fresh/求解器/规格/运行语义.md
+++ /home/zhuran24/zmd-research-fresh/求解器/规格/运行语义.md
@@ -1,6 +1,6 @@
 # 运行语义
 
-日期：2026-09-21。状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；新增条件证明待独立复核，Rust实现与运行验收由任务8承接。
+日期：2026-09-21。状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；各条件证明按自身复核范围使用；任务8工程实现和IE-06定向重放已经交付，一般真实调度和全称认证仍待相应证明。
 
 本次现行依据为规则`d150b86b398f`、任务`1630ca1febec`、约束`f6503e6c1568`，完整指纹见本文§1。任务现行行号：拿取9、循环10、建造11、调试12、精度13、离线14、判定次序15、位置16。旧轮次报告、KQ编号及历史验证引用均作史料，效力按对应版本和覆盖范围读取。
 
@@ -49,7 +49,7 @@
 | 采种机、种植机 | 同上 | 5×5，对边各 5 口；同上运行字段 | 中制造单位、存货/取货物品格、缓存格、制造 |
 | 研磨机、封装机、灌装机 | 两存货格、一取货格，上限 50（存货格计数域见 T7）；一个无限缓存格 | 6×4，长边各 6 口；同上运行字段 | 大制造单位、存货/取货物品格、缓存格、制造 |
 | 传送带 | 一格，上限 1，记录入格时间 | 1×1，存货一边、另任意一边取货；三种旋转轨道按connection.belt_shape输入，调试经重建改形（T11） | 运输单位、传送带、滞留 |
-| 桥接器 | 两对平行边各一个独立格，各自记录滞留；容量 1 为已采纳约束的记账口径，非规则直引，完整容量域见 T14 | 1×1，每对存取方向由先接上的那一端所接的单位决定，已知相邻端口为取则本端为存，反之亦然（互依赖见 T5）；两格不能互换货或共用占用标记；两轴按单位共同存货分级，轮询分存取两侧，见 T14 | 桥接器、运输单位、滞留、轮询、存货优先级；容量记账参照约束·箱体过站、约束·运输下限 |
+| 桥接器 | 两对平行边各一个独立格，各自记录滞留；容量 1 为已采纳约束的记账口径，非规则直引，完整容量域见 T14 | 1×1，每对存取方向由先接上的那一端所接的单位决定，已知相邻端口为取则本端为存，反之亦然（互依赖见 T5）；两格不能互换货或共用占用标记；R13仍约束同种物品同时只占一轴；两轴按单位共同存货分级，轮询分存取两侧，见 T14 | 桥接器、运输单位、滞留、轮询、存货优先级；容量记账参照约束·箱体过站、约束·运输下限 |
 | 物品准入口 | 一格，上限 1，记录滞留 | 1×1，对边存取；身份未设/指定物品、累计上限未设/1…5000、窗口上限未设/1…5；累计已收件数、当前窗口已收件数、窗口起点/未开始、阻断状态 | 物品准入口、运输单位、滞留 |
 | 分流器 | 一格，上限 1，记录滞留 | 1×1，一存三取；两侧轮询权限分别记录 | 分流器、运输单位、轮询、滞留 |
 | 汇流器 | 一格，上限 1，记录滞留 | 1×1，三存一取；两侧轮询权限分别记录 | 汇流器、运输单位、轮询、滞留 |
@@ -133,7 +133,7 @@
 | 输入入缓存（判定分类待 T2） | 输入闸的明文条件为“存货物品格中的物品符合某个配方”且上一批全部进入取货格；累计通过一次制造用量即关闭 | “符合”的逐格/并集论域、存货格混装读法、物种/数量完备度、数量恰等/至少一批及额外物种允许/拒配、匹配多配方时选哪条及锁定时点、归集粒度见 T7 和 §2.3；供电/开关是否管此接口见 T6，不列为已定守卫。据：缓存格、制造、需电功能、开关 |
 | 尝试制造/开始制造 | 一次制造用量已进入缓存格且制造功能有供电、开关运行时立刻开始；所用配方决定耗时及产物 | 停用期间先入料、恢复时开始的衔接及配方锁定见 T6/T7，事件拆分见 T2。据：制造、缓存格、需电功能、开关、配方 |
 | 制造完成及出缓存 | 工作时长按配方；制成的一批进入缓存格；只允许完成的一整批通过至取货格，目标须可容纳且种类兼容，并在出缓存后保持本单位全部非缓存普通格同种单格；冲突则保持完成态且整批留缓存；无额外制造冷却 | 同刻完成、出缓存、再进料与外部通道交织及其事件划分见 T1/T2；停用时输出接口能否通行见 T6。据：制造、缓存格、物品格、配方 |
-| 尝试无线传输 | 箱体有电、功能开、箱级冷却就绪；一次单位判定按各物种可收余量尽量送入仓库，余货留箱；空箱、全拒收及有货送出每次尝试均起5 tick冷却 | 停用恢复的具体回放、同种跨格部分扣减及同刻事件组织继续核，代码迁移待任务8。据：规则L20、L25、L36、L41、L72；T6 |
+| 尝试无线传输 | 箱体有电、功能开、箱级冷却就绪；一次单位判定按各物种可收余量尽量送入仓库，余货留箱；空箱、全拒收及有货送出每次尝试均起5 tick冷却 | 停用恢复的具体回放、同种跨格部分扣减及同刻事件组织继续核，部分接收及每次尝试冷却已有任务8/IE-06定向验收，一般后继对应仍待PC-06。据：规则L20、L25、L36、L41、L72；T6 |
 | 准入口收货 | 按当前身份、累计n与阈值C、窗口余额判断；所有阻断条件消失恢复接收资格；成功收件更新计数，首件开窗、满5 tick后下一件重起；仅改阈值保留n | 恢复事件、接通记账和同刻维护/轮询衔接待任务6。资格恢复后实际收件仍核容量、滞留和权限。据：规则L16、L22—25、L29—30、L59、L64；v46 N-F19 |
 | 时间推进 | 运输滞留按经过时间增加；运行中制造进度推进，停用时进行中的制造进度保留；传输冷却的暂停或继续计时待 T6；同时到时的判定仍逐个 | 何时结束当前时刻、时间域、tick 内采样边界见 T1；离线时间推进见 T10。据：滞留、需电功能、制造、传输、判定 |
 | 离线/上线接续（环境转移） | 更新在线/离线模式及切换历史；离线后允许任意两条通道接通先后改变；判定固定性按 T2 所选辖域核验，按 §3.1 核对派生量 | 重排发生在离线还是上线边界、库存与进度等后效、时间推进及原子性见 T10；不得只检查重排后终态。据：离线、判定次序、目标 |
@@ -226,3 +226,7 @@
 以连续制造代替目标比较时，采用任务6 PA-11的完整D域：完整合法周期、全矿取货、仓库收得下成品、周期非成品零入库、全部普通库存和正常批次复原、六类台数为68/51/6/32/16/3、仅用规则L81—114配方。该域内六类连续先给电池b=3/5；植物平衡给16=25b/3+20c，故胶囊c=11/20。其它台数配置另给反向充分证明。A=1113的达标必要台数和无箱按约束L113核；尚未认证候选的r=0由实际路由或完整反向收支推出。
 
 共同状态路线给S及全部起态包含、正常/离线/恢复保持，并对每个成品给有限值V_i满足d_i−ρ_iΔt≥V_i(s')−V_i(s)。循环相加得到目标；具体S与V_i尚须在候选上实例化。周期键路线另核完整转移、时间、覆盖和有限表示。两种路线的证明范围分别登记（任务6 PA-12；受限转移§6.4）。
+
+## 终修记录（任务书7终修席，2026-09-21）
+
+IE-07关联支持说明同步：已实现的部分接收/当前条件恢复与PC-06未证真实对应分开，引用任务8及IE-06实际验收；精确键以受限转移§6.2为准。AX-03补明R13的桥同种跨轴守卫，轴身份与联合占格约束分别保留。
```

## 选择点清单.md

```diff
--- /home/zhuran24/zmd-research-fresh/求解器/规格/选择点清单.md
+++ /home/zhuran24/zmd-research-fresh/求解器/规格/选择点清单.md
@@ -1,6 +1,6 @@
 # 选择点清单
 
-日期：2026-09-21。状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；新增条件证明待独立复核，Rust实现与运行验收由任务8承接。
+日期：2026-09-21。状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；各条件证明按自身复核范围使用；任务8工程实现和IE-06定向重放已经交付，一般真实调度和全称认证仍待相应证明。
 
 本次现行依据为规则`d150b86b398f`、任务`1630ca1febec`、约束`f6503e6c1568`，完整指纹见[运行语义§1](运行语义.md)。任务现行行号：拿取9、循环10、建造11、调试12、精度13、离线14、判定次序15、位置16。旧轮次报告、KQ编号及历史验证引用均作史料，效力按对应版本和覆盖范围读取。
 
@@ -97,7 +97,7 @@
 
 正常5 tick窗口从首件开始，走完后由下一件重新起算；仅重置该窗口计数，全时段累计数保留。身份阻断也不改变已经起算的5 tick时长。`gate.window_clock=wall_clock`、`gate.window_recovery=on_expiry_if_other_guards`、身份和累计的`current_conditions`均为已定规则，旧锁存备选撤下。（据：规则L64；v46 S-D04、N-F19）
 
-**缺推导**：断边恢复怎样读取当前候选物品、同刻先后怎样安排、恢复后的接通记录与两侧轮询/上游阻尼怎样更新，由任务6根据L16、L24—25、L28—32、L64补全。恢复资格已由上述合读推出，这些事件表示义务以该结论为前件。工程的快照批维护与逐边维护仍是待核组织方式；每次都须按完整前后图核成员、级排序和指针。当前Rust身份锁存实现待任务8替换。
+**缺推导**：断边恢复怎样读取当前候选物品、同刻先后怎样安排、恢复后的接通记录与两侧轮询/上游阻尼怎样更新，由任务6根据L16、L24—25、L28—32、L64补全。恢复资格已由上述合读推出，这些事件表示义务以该结论为前件。工程的快照批维护与逐边维护仍是待核组织方式；每次都须按完整前后图核成员、级排序和指针。当前Rust已从原几何候选更新三原因并恢复通道，见任务8/IE-06；上述一般规则对应仍归PC-06。
 
 ## T9. 同一准入口改阈值与计数寿命
 
@@ -133,7 +133,7 @@
 
 ## T14. 桥接器的库存例外、容量依据与调度辖域
 
-- **已定**：每对端口独立拥有一个物品格，物品按所在格记录滞留；两轴仍属于同一个单位。规则 29 按一个单位的存货/取货端口各自轮询，规则 32 按一个单位的全部存货通道分级，因此两轴存货通道共同分级、取货通道共同轮询；桥接器为运输单位，不适用仅给非运输单位的取货分级。（据：桥接器、滞留、轮询、存货优先级、取货优先级）
+- **已定**：每对端口独立拥有一个物品格，物品按所在格记录滞留；两轴仍属于同一个单位；R13的同种单格守卫继续适用，同一种物品同时只占一轴。规则 29 按一个单位的存货/取货端口各自轮询，规则 32 按一个单位的全部存货通道分级，因此两轴存货通道共同分级、取货通道共同轮询；桥接器为运输单位，不适用仅给非运输单位的取货分级。（据：桥接器、滞留、轮询、存货优先级、取货优先级）
 - **已排除**：按轴另分两套存货/取货轮询辖域，或按轴分别存货分级。规则 63 的“每对独立”修饰拥有物品格，随后规定该对的端口定向，没有例外化规则 29/32 的“一个单位”；把库存独立扩张为调度独立没有条文来源。两轴共用库存亦被排除。辖域已定不等于 T3 的权限协调与续接记忆已定。（据：桥接器、轮询、存货优先级、物品格）
 - **容量取值与证据层级**：规则 59 的“一个物品格（上限1），桥接器例外”未明确例外只限个数；规则 63 补明两格但未重述容量。每格 1 件按已采纳约束·箱体过站/运输下限的运输格记账口径使用，标为约束记账解释，不冒充规则直引。待审方向为仅个数例外（容量 1）或容量也属例外（具体上限未给）；后一读法没有任取容量的授权，更没有无限容量的授权。（据：运输单位、桥接器、物品格、约束·箱体过站、约束·运输下限）
 - **不能反推的内容**：T+b 的两个运输格计数与下界并不在逻辑上唯一确定每格的物理容量：更大格容量配合既有每端口每 tick 1 件、滞留约束也不能仅由此数式裁掉。只实现容量 1 时须标为记账口径下的受限模型；完整状态界仍须证明容量域或给可靠抽象。容量记账同样不证明两轴同时满吞吐。满速独占的“另一对端口不受影响”不能解除规则 29/32 的调度守卫。（据：约束·箱体过站、约束·运输下限、约束·端口速率、滞留、约束·满速独占、轮询、存货优先级；逻辑边界）
@@ -171,3 +171,7 @@
 非运输输出侧按全部自动形成通道分级后，零级无出货，单非空级无需阻尼级间比较。直接对端全非汇流器是充分结构条件，覆盖所有分支持续方式；多级继续按真实阻尼比较。每台检查记录unit、全部channel、实际peer和level_groups，scope=`per_nontransport_output_side`；缺实际几何时为geometry_pending。依据规则L30—31、任务6 PA-01；这是T4的条件消去接口，不新增参数轴。
 
 没有分流器→汇流器直接通道时，约束L16指定构型消失；共享格、存货分级和固定判定顺序仍保留。末端箱按T6的全部前件应用约束L80。两项分别记录结构检查及未覆盖供排服务（任务6 PA-02—03）；每份设计律实例的独立复核状态单列。
+
+## 终修记录（任务书7终修席，2026-09-21）
+
+IE-07关联支持说明同步：已实现的部分接收/当前条件恢复与PC-06未证真实对应分开，引用任务8及IE-06实际验收；精确键以受限转移§6.2为准。AX-03补明R13的桥同种跨轴守卫，轴身份与联合占格约束分别保留。
```

## 四件前置义务对照.md

```diff
--- /home/zhuran24/zmd-research-fresh/求解器/规格/四件前置义务对照.md
+++ /home/zhuran24/zmd-research-fresh/求解器/规格/四件前置义务对照.md
@@ -1,6 +1,6 @@
 # 四件前置义务对照
 
-日期：2026-09-21。状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；新增条件证明待独立复核，Rust实现与运行验收由任务8承接。
+日期：2026-09-21。状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；各条件证明按自身复核范围使用；任务8工程实现和IE-06定向重放已经交付，一般真实调度和全称认证仍待相应证明。
 
 本次现行依据为规则`d150b86b398f`、任务`1630ca1febec`、约束`f6503e6c1568`，完整指纹见[运行语义§1](运行语义.md)。任务现行行号：拿取9、循环10、建造11、调试12、精度13、离线14、判定次序15、位置16。旧轮次报告、KQ编号及历史验证引用均作史料，效力按对应版本和覆盖范围读取。
 
@@ -22,7 +22,7 @@
 
 **已建立**：原子判定串行，容量/种类、运输滞留、端口速率、批次闸门、优先级都是转移约束；逐端口速率已定，同侧统一单件限额 H 与存在任何达标布局不相容（核心六口与 5328 格证明见运行语义 §4.3），在达标论域已排除；若 H 被证为须覆盖的合法机制则全题无达标解，不要求认证同时在 H 下成功，也不以有解假设删除合法坏运行。（据：判定、物品格、滞留、约束·端口速率、缓存格、优先级、约束·取货口配置、约束·出库上限、约束·矿石需求、约束·物料流量）
 
-**缺推导/复核**：规则L29“尝试后立刻传给下一条”已确定成败后都前移，L30确定最高可移动级；双端怎样构成尝试、失败空转何时结束、同刻怎样继续推进仍交任务6。准入口恢复资格已定，观察事件、接通记账、分级/阻尼及指针映射须补后效。传输按可收部分送出已定，箱内残留格、仓库空格竞争和冷却暂停须补完整后态；空箱/全拒收也开始5 tick冷却已定（规则L36、T6）。当前Rust与新规格的差异由任务8实现和复核。受限转移§5给工程失败环边界，§6给现行条件投影；新增条件证明待独立复核。
+**缺推导/复核**：规则L29“尝试后立刻传给下一条”已确定成败后都前移，L30确定最高可移动级；双端怎样构成尝试、失败空转何时结束、同刻怎样继续推进仍交任务6。准入口恢复资格已定，观察事件、接通记账、分级/阻尼及指针映射须补后效。传输按可收部分送出已定，箱内残留格、仓库空格竞争和冷却暂停须补完整后态；空箱/全拒收也开始5 tick冷却已定（规则L36、T6）。任务8和IE-06已定向验证部分接收、门恢复及新键；IE-07精确工程说明已终修汇入受限转移§6.2，一般真实对应仍未完成。受限转移§5给工程失败环边界，§6给现行条件投影；新增条件证明待独立复核。
 
 **结清条件**：给出事件日程及每一原子事件的完整守卫/后效，证明覆盖全部合法判断且每个完整参数、初态和环境输入对应唯一后继；若保留分支模型，就证明该模型覆盖所需合法周期，而不能仍叫确定性运行族。（据：判定、判定次序、循环态、目标）
 
@@ -81,3 +81,7 @@
 无线同种跨格部分扣减和可观察匿名格竞争若缺后态机制，按受限转移§4.3停止确定回放；持续成品全收的结构域可用逐格清空证明。请求完整基地周期时逐项核§6.5.5的实际拿取程序、所有库存/标签和外部过程复原。以上停止项分别记录来源与位置，for_owner为空。
 
 连续制造启用范围和PA-11完整D域见运行语义§7。直接共同状态证明与有限图路线见受限转移§6.4，前者完成所需义务后即可交付，无须先实现通用执行器。
+
+## 终修记录（任务书7终修席，2026-09-21）
+
+IE-07关联支持说明同步：已实现的部分接收/当前条件恢复与PC-06未证真实对应分开，引用任务8及IE-06实际验收；精确键以受限转移§6.2为准。PC-01—09仍按各自构造、推导与复核范围登记。
```
