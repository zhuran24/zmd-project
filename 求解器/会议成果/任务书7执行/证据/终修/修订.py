#!/usr/bin/env python3
"""任务7终修：限指定正文与规格；旧复核和证据原件保持。"""
from pathlib import Path
from hashlib import sha256
import difflib
import json

B = Path('/home/zhuran24/zmd-research-fresh/求解器')
E = B / '会议成果/任务书7执行/证据/终修'
D = B / '会议成果/任务书7执行'
R = sha256((B.parent/'《明日方舟：终末地》游戏规则.txt').read_bytes()).hexdigest()
assert R == 'd150b86b398f8e73e57bee1598020e6bcd895e5a13a254b4b11dd1e4416c325a'
OLD = '31ced2a24fef738c72dfa406351b6bad5df3e353d6b1c2dfd06ab1febaf566ff'
changes = []

def edit(path, transform, record):
    before = path.read_text()
    assert '## 终修记录（任务书7终修席，2026-09-21）' not in before
    after = transform(before)
    after = after.rstrip() + '\n\n## 终修记录（任务书7终修席，2026-09-21）\n\n' + record + '\n'
    path.write_text(after)
    changes.append({'path':str(path),'before':sha256(before.encode()).hexdigest(),
                    'after':sha256(after.encode()).hexdigest(),
                    'diff':''.join(difflib.unified_diff(before.splitlines(True),after.splitlines(True),fromfile=str(path),tofile=str(path)))})

def replace(s, a, b):
    assert a in s, a
    return s.replace(a,b)

def warehouse(s):
    s=s.replace(OLD,R)
    s=replace(s,'状态：任务书7任务2证明稿完成，待独立复核及规格席落地。','状态：任务2条件证明已交付；接口复核IE-08承接接收非干扰及循环方向，终修已同步现行R36。规格§6.5已汇入；本次受影响文字经终修重核。')
    s=replace(s,'完整路径、SHA-256和读取范围见[输入指纹](证据/仓库/输入指纹.json)。','当前版本见[终修输入指纹](证据/终修/输入指纹.json)及[终修记录](复核/终修记录.md)；[原输入指纹](证据/仓库/输入指纹.json)保留任务2首次提交时点。')
    s=s.replace('主会话三审第11—31、49—53行','主会话三审第11—31、49—54行')
    s=replace(s,'到仓库中，能送多少送多少，5 tick 冷却','到仓库中，即使那个物品格中没有物品也一样，能送多少送多少，5 tick 冷却')
    s=replace(s,'储存箱空着时的冷却细节见§9，不能在周期证书中抹成每步随意传输。','R36及三审L54确定空箱、全拒收和有实际发送的每次尝试都重起5tick冷却；持续供电且开关不变时，实际尝试沿初始相位每5tick进行。')
    s=replace(s,'现有 §6.2 第157、165、177行还保守保留非运输格年龄、未设累计上限时的历史收件数和部分审计期限偏移；这些记录可能持续增长，使物理行为已重复的运行没有重复工程键。因此“所有已找到的键循环达标”还缺从所有正式循环到这个具体键循环的覆盖证明。任务6须证明相关字段可删、可归约，或用直接证明覆盖漏检循环；本稿的成品非干扰不会自动消去这些其他坐标。','现行规格§6.2在逐字段未来读取前件内归并非运输同种审计组，删除无用年龄及无限额累计，并用制造remaining保存有效剩余工作量。采用该新键时继续核D域、真实后继与耗时保持；PC-06的一般真实调度对应及PC-07的实数表示仍开放。沿用旧详细键的证据仍要说明单调审计字段不会漏掉所称的正式循环。')
    s=replace(s,'| 仓库-具体键覆盖 | 任务第10行“状态以固定周期重复”；受限转移第157、165、177行保留可能单调增长的工程字段；§6.1定位了物理重复而工程键漏检的方向 | 缺推导：具体生产键覆盖全部目标内循环的表示证明，或覆盖漏检循环的直接证明，任务6 |','| 仓库-具体键覆盖 | 任务第10行“状态以固定周期重复”；现行转移§6.2已条件化删除无效审计坐标，§6.1保留表示前件 | 缺推导：PC-06真实后继对应、PC-07实数表示及全部目标内循环覆盖，任务6 |')
    s=replace(s,'| 仓库-传输迁移 | 规则第36行部分传输，受限转移第87—94行及内核输出第42、49行仍采用全箱事务 | 缺实现与复核：部分后态、实际台账、证书域和旧停止项同步，规格席及任务8 |','| 仓库-传输迁移 | 规则第36行“能送多少送多少”；现行转移§4.3、任务8及IE-06已经同步部分接收、实际账和所有尝试冷却 | 已有定向实现及独立重放；同种多编号格的严格部分扣减仍按下一项停止 |')
    start=s.index('| 仓库-空箱冷却 |');end=s.index('\n证据见[核对说明]',start)
    s=s[:start]+'''| 仓库-部分扣格 | R36“能送多少送多少”确定逐种送出总量；R72“编号最小”只用于端口存取。箱格1/3各有电池、格2有胶囊而电池仓只余1位时，先扣格1或格3会留下不同的后续端口首货 | 缺游戏事实：确定无线严格部分传输的扣格顺序。全送、全拒收或仅一格同种时后态唯一；一般条件证明按同一合法后效匹配，当前执行器对歧义情况提交前停止 |
| 仓库-独立复核 | IE-08核了§3—6的条件非干扰与循环方向，AX-01/IE-09击中旧R36和来源登记 | 本次已按现文重核受影响文字；具体整厂证明仍按以上缺口承担 |

R36现文含“即使那个物品格中没有物品也一样”；三审L54明确一件都送不出去也算尝试。空箱及零发送的5tick冷却已定，全部初始相位继续覆盖。

**for_owner（仅登记）：** R36规定尽量无线送入，R72只规定端口按编号取放；同种分占多个箱格且仅能送一部分时，无线按什么顺序扣格？全收域证明继续成立，依赖唯一残留的运行暂按unsupported处理。
''' + s[end:]
    return s
edit(D/'仓库接收与循环对应.md',warehouse,'采纳AX-01、IE-09：更新R36全文及完整指纹，结清空箱/零发送冷却；保留同种多编号格严格部分扣减的事实缺口。同步现行新键和已完成工程迁移的状态。IE-08的接收非干扰及投影方向按原前件保留；原证据按旧版本封存。')

def warehouse_draft(s):
    s=s.replace(OLD,R)
    s=replace(s,'状态：任务书7任务2提交稿，待独立复核与规格席落地。替换对象为输入指纹所锁定《受限转移定义.md》第209—222行整节；本文未改规格源文件。配套同步清单见文末。','状态：任务2替换稿已由规格席汇入现行§6.5；本稿经AX-01/IE-09终修同步。原替换位置第209—222行和文末清单按首次提交版本定位，当前实际契约见[受限转移定义](../../规格/受限转移定义.md)§6.5。')
    s=replace(s,'传输相位和空箱冷却后效保留在比较中，本证明对每种实际合法取值分别成立。认证一个布局时，生产证据应覆盖声明的全部取值，或附使其不影响结论的充分证明。规则第36行现文未唯一说明空箱无实际发送的尝试是否启动冷却；需要固定该机制的具体运行在明确前保留相应未决，能够覆盖后果的结构证明继续有效。','传输相位保留在比较中，生产证明覆盖声明的全部相位。R36“即使那个物品格中没有物品也一样”和三审L54确定：空箱、全拒收、部分及全部发送，每次有资格尝试均开始5tick冷却。保持供电及开关时，由初始余量与5tick节拍确定尝试时刻。')
    s=replace(s,'现有生产键还保守保留非运输格年龄、部分历史累计和审计期限偏移，物理循环可能因此没有重复工程键。采用该键做全称认证时，任务6须补其循环覆盖证明，或直接覆盖漏检循环。上述投影定理保留此表示前件。','现行生产键§6.2在读取前件成立时删除无效非运输年龄及历史累计，以remaining编码制造待办；完整原状态继续保留。PC-06的一般真实后继对应、PC-07的实数表示及全称覆盖仍须完成。旧详细键可能单调漂移的提醒仅用于旧编码，上述投影定理始终保留表示正确性的前件。')
    s=s.replace('本表是修改请求，不是已完成的实现。','本表保留首次提交的修改请求位置；当前已汇入结果见规格回填的汇入记录与本次终修汇总。')
    s=s.replace('空箱冷却及扣格后效以有据机制或覆盖报告处理','所有尝试后冷却已定；歧义部分扣格后效以有据机制或覆盖报告处理')
    s=s.replace('空箱冷却具体未决保留','空箱及零发送冷却采用已定every_attempt')
    return s
edit(D/'受限转移定义-§6.5替换稿.md',warehouse_draft,'采纳AX-01、IE-09：与现行R36、规格§6.5对齐，撤下空箱冷却未决；原修改清单标明版本与实际落地入口。保留严格部分扣格、真实循环复原和全称覆盖各自义务。')

def loop(s):
    s=s.replace(OLD,R)
    a=s.index('**空箱冷却的条文终点。**');b=s.index('\n\nT11 ',a)
    s=s[:a]+'''**空箱冷却及相位。** R36现文：“在供电状态时会一直尝试把自身物品格内的所有物品立刻无线传输到仓库中，即使那个物品格中没有物品也一样，能送多少送多少，5 tick 冷却”。三审L54明确空箱及零发送也算尝试并重起5tick冷却。持续供电、开关不变时，初始余量与5tick节拍给出尝试时刻；C8的全部初始相位继续覆盖。本版按实际送入件数记账并按周期首尾复原消账，完成收支、K、Z及产能结论保持原前件。''' +s[b:]
    s=s.replace('本次修订文字待独立复核。账面为','本次任务7终修已核R36及来源影响，其余历史增量的复核范围见末尾记录。账面为')
    s=s.replace('正常制造门控使一台机器至多留一批','同种物品在一座桥上同时至多占一轴（R13），两轴同种服务另核；正常制造门控使一台机器至多留一批')
    s=s.replace('日期：2026-09-21。修订输入为 397 行','历史执行记录，截止2026-09-21任务3首次修订；其中来源及空箱登记由文末终修记录更正。修订输入为 397 行')
    return s
edit(B/'规格/推导/回路总数决定论-v2.md',loop,'采纳AX-01、AX-02：R指纹由中间版本31ced2a24fef更新为d150b86b398f，§1.2按R36及三审L54结清空箱/零发送冷却；§8原登记保留为史料。AX-04按实质证据分层：§3.7有r2§2独立推导，旧数学段承接原审范围，U1—U7继续开放。补R13桥同种约束，未改变任何库存、相位或整厂充分性边界。')

def phase(s):
    s=replace(s,'| 规则 | [《明日方舟：终末地》游戏规则.txt](../../../《明日方舟：终末地》游戏规则.txt) | 31ced2a24fef |','| 规则 | [《明日方舟：终末地》游戏规则.txt](../../../《明日方舟：终末地》游戏规则.txt) | d150b86b398f |')
    s=s.replace('SHA-256 前 12 位与任务给定值一致。','SHA-256前12位按磁盘现文核定；完整R为`'+R+'`。')
    s=replace(s,'现行规则第 36 行为“能送多少送多少，5 tick 冷却”。','现行规则第36行含“即使那个物品格中没有物品也一样，能送多少送多少，5 tick 冷却”；三审L54确定空箱及零发送尝试同样重起冷却。')
    s=replace(s,'日期：2026-09-21。修订输入为 300 行','历史执行记录，截止2026-09-21任务3首次修订；P-E01的来源完成状态由文末终修更正。修订输入为 300 行')
    s=replace(s,'§1.1 同步现行规则指纹 `31ced2a24fef`，说明 R36 部分传输下矿守恒和产能结论仍按原范围使用 | 来源已回填；新增说明待独立复核','当时§1.1改到中间规则指纹 `31ced2a24fef`；现由终修同步为 `d150b86b398f`，R36实际搬运仍保持矿守恒 | 旧完成宣告更正；现行来源见§1.1及末尾终修记录')
    s=replace(s,'本次来源与接口修订见 §7，新文字待独立复核','任务3来源与接口修订见§7；任务7终修的来源影响已核，其他新文字的独立复核范围见末尾记录')
    return s
edit(B/'规格/推导/三种相位不改产量-v2.md',phase,'采纳AX-02：当前来源改为d150b86b398f并补完整指纹，P-E01记录明确中间版本与本次更正。R36空尝试改变箱节拍说明；§2.1矿守恒与§2.7的68+r、51+r按配方和真实周期计数，保持原前件。AX-04下旧局部回转/灌装参数算例仍承接旧审；本次没有重放这些旧算例。第3行与§4第7项的历史复核原句保持。')
edit(B/'规格/推导/总纲-流量存量相位.md',lambda s:s.replace(OLD,R).replace('本次重写文字待独立复核','重写全文的整体复核范围保持；本次终修已核现行来源及R36对所列收支/产能结论的影响'),'采纳AX-02：第6节R完整指纹同步为d150b86b398f；31ced2a24fef为本轮中间版本，abc7a5867f64为更早版本。R36每次实际搬运保存矿含量，空尝试没有物料变化；第3节等号链及第4节68+r、51+r均不受其影响。AX-04下结论依据仍是逐项正面推导与正式约束，未将“否证不成立”作为证明。G-U01/G-U02及L/U保持。')

def scope(s):
    s=s.replace(OLD,R)
    s=s.replace('三份指定独立复核的裁定全部采纳，48个编号逐项处理','三份指定报告的48个编号按实质证据分层承接；任务7终修另外处理AX/IE发现')
    s=replace(s,'磁盘三份正式文件及三份正文的输入指纹与委托一致；三份独立裁定全文及三审现文已读取。修订前本汇总文件不存在。','现行正式文件按上表核验；转发中的31ced2a24fef是中间版本。三份独立裁定按各自被审版本读取。')
    s=replace(s,'| 正文绝对路径 | 修订前完整SHA-256 | 修订后完整SHA-256 | 新行数 |','以下为任务3首次修订的历史指纹表；当前终修指纹见[交付清单](证据/终修/交付清单.json)。\n\n| 正文绝对路径 | 首次修订前SHA-256 | 首次修订后SHA-256 | 当时行数 |')
    s=replace(s,'表中“保留”承接各报告给出的前提与推导，新增文字的独立复核范围见第4节。','“否证不成立”仅指该攻击在原命题条件内未成立；“已证窄式”须另有正面推导。表中处理栏说明新核、旧审承接或仅历史事实核对，第4节保留未重核范围。')
    s=s.replace('裁定确认现文正确，保留','保留所引正面论证及明确前件')
    s=s.replace('| L-N01 | 已证窄式／否证不成立 | 保留所引正面论证及明确前件 |','| L-N01 | 已定事实及范围核对／该攻击未成立 | r2§3.1已核失败前移与调度断点；C82应用承接旧审 |')
    s=s.replace('| L-N02 | 已证窄式／否证不成立 | 保留所引正面论证及明确前件 |','| L-N02 | 条件后果核对／该攻击未成立 | r2§3.2重核停流后果，偏向其余旧数学承接旧审 |')
    for i in range(3,8):
        prefix=f'| L-N{i:02} |'
        lines=s.splitlines()
        for n,line in enumerate(lines):
            if line.startswith(prefix):
                cols=line.split('|');cols[3]=' 保留r2§2的独立推导；默认全厂应用仍待证 ';lines[n]='|'.join(cols)
        s='\n'.join(lines)+'\n'
    s=s.replace('相位§1.1、§7：R前12位更新为31ced2a24fef','相位§1.1、§7：R前12位按终修更新为d150b86b398f')
    s=s.replace('| P-N01 | 已证窄式／否证不成立 | 保留所引正面论证及明确前件 |','| P-N01 | 净需求分数复算／该攻击未成立 | 保留执行复核§3的净需求推导；旧几何和轨迹未重放 |')
    s=s.replace('| P-N05 | 已证窄式／否证不成立 | 核实原句正确并保留，另作状态补记 |','| P-N05 | 历史复核存在及版本核对 | 保留文档事实；不据此新增运行证明 |')
    s=s.replace('无编号承接项也已核对：','无编号旧审承接项（本轮未重核其完整推导与旧算例）：')
    s=s.replace('R36环境与空箱断点','R36环境与已定空箱冷却')
    s=s.replace('现行指纹见第1节。','任务3首次修订指纹见第1节历史表，当前终修指纹见证据/终修/交付清单.json。')
    a=s.index('## 6. 不同意的裁定');b=s.index('## 8. 执行与自核证据',a)
    s=s[:a]+'''## 6. 裁定差异及证据边界

AX-04关于“否证未成不等于证明”的批评成立。旧审承接的回路§2.1—2.3、§2.5—2.7、§3.1—3.5及相位旧算例按各报告的原范围使用，本轮没有重放。

AX-04若将L-N03—L-N07一并称作未重核，范围过宽：r2§2直接重建了Z恒等式、三互等边界、前缀充分界和失配停止，执行复核§5另有5202个库存/头件与7550次恒等检查。P-N01净需求在执行复核§3直接复算；P-N05只确认报告存在、版本与状态，属于文档事实。保留这些实际工作，撤下整表“裁定确认现文正确”的统一表述。

## 7. for_owner

空。R36及三审L54已经结清空箱、零发送尝试后的5tick冷却；本席收支及产能结论按各自前件成立。

''' +s[b:]
    s=s.replace('[核验脚本](证据/推导修订/核验.py)复核','首次修订的历史[核验脚本](证据/推导修订/核验.py)曾复核')
    s+='\n旧脚本的31ced2a24fef硬断言只对应其历史输入，现行终修禁止用它作通过门槛；替代入口为`python -B 会议成果/任务书7执行/证据/终修/核验.py`。旧报告、脚本、日志原件按委托“其他文件不动”保持，现行裁定和证据适用范围由本稿及终修汇总覆盖。\n'
    return s
edit(D/'推导复核范围.md',scope,'采纳AX-01/02：更新源指纹、撤空箱for_owner、旧产物哈希表改为历史表，当前清单另锁。采纳AX-04的判定口径批评，逐项分开正面推导、旧审承接及历史文档事实；不同意把r2§2已独立重推的L-N03—07笼统降为未重核。旧审查原件及旧脚本保持，现行替代核验在证据/终修。')

def phase_cert(s):
    a=s.index('磁盘与转发的实质增量有两项。');b=s.index('\n\n本文的“状态删除”',a)
    s=s[:a]+'''任务4容量分为物理种子1150/2150与正常槽位850/1600，F13修订后独立重核按原席登记。任务5的[否证报告](复核/否证-任务5.md)已经存在；其攻击判定不作为条件命题的证明。本稿承接的是任务5§3.2的正势函数和有限动作论证、§2.3的准备/释放截面区分，以及下文逐项列出的前件。DR-01—06继续开放，DR-07记录已有该次审查，不据检查条数提升其结论。''' +s[b:]
    s=replace(s,'任务5§2.3及否证F05—F07已经核过这个条件范围。','依据是任务5§3.2逐配方增权、有限容量及有限路径的论证与§2.3的截面定义；否证F05—F07只记录对应攻击的审查结果。')
    s=s.replace('本席新增结论待独立复核。','已有复核按被审指纹和条目承接；本次终修核AX-04引用及其条件范围。')
    return s
edit(D/'相位离线与认证范围.md',phase_cert,'采纳AX-04：§1删除以否证计数承接结论的表述，PA-05直接引用任务5的正势函数和截面证明。条件成立才能消去准备截面的非零进度；释放后相对进度、关闭intake接口和PC-06继续保留。IE-11仍为缺联合推导与复核。')

def plant(s):
    s=replace(s,'桥只能使用已确定方向的独立轴，无额外入流、门限或分级争用。','桥使用已确定方向的轴，并满足R13：同一座桥同时承载的两轴物种相异，或另有证明保证同种从不同时请求占格且首格服务保持；无额外入流、门限或分级争用。本节满线切面两轴同时非空，适用桥的两轴必须为异种。')
    s=replace(s,'全基地桥物理容量是2，但某植物只有确实连到它的轴才计入该植物可用容量。','桥结构有两个物品格；按R13，同一种种子或植株在同桥同时至多占一格，两种不同物品才能同时占两轴。某植物形态的实际可占量按轴连接与跨轴身份守卫共同核算。')
    return s
edit(D/'植物运行试点.md',plant,'采纳AX-03的容量与接口影响：§6.3把桥的两结构格和单物种同时占格上限1分开；§5.2明确满线切面的桥两轴异种前件。§5.3实际三机结构全为带，23格、277等已有数值保持；未落格的A/B实际l_e仍为null，完整几何和服务仍待证。')

# 精确工程契约：只改文档，不改代码、schema或配置。
def transition(s):
    s=s.replace('状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；新增条件证明待独立复核，Rust实现与运行验收由任务8承接。','状态：任务2、6及任务8 S8-01—04的当前工程契约已汇入；IE-07精确键冲突已终修。当前Rust行为按内核验收/IE-06范围承接，PC-06真实调度对应仍开放。')
    s=replace(s,'同一门只改阈值保持total_received。','同一门只改阈值保持total_received；完整状态允许n大于当前阈值C，n≥C记total_exhausted，n<C解除该原因，真实n保持；运行区间内改阈值动作仍按工具支持域处理。')
    s=replace(s,'3. 没有取货端口指向两成品的非空/历史身份格；','3. 当前Rust逐个核全部核心及仓库取货口指派，只允许对应格为源矿或蓝铁矿。仍取植物的输入可按普通有限执行核验，生产接口返回unsupported(cycle.domain.D3)。没有取货端口指向两成品的非空/历史身份格；')
    s=replace(s,'全部保留，按(item,规范剩余滞留)保存多重集；年龄删除须先满足下一行的读取前件；category不入物理键','运输格按(item,规范剩余滞留)保留；在residence.nontransport=zero、正常单批缓存且无非运输年龄读取的D域内，非运输同一格的同种审计分组数量相加成一行。不同编号箱格和桥轴保持独立；完整分组留原StateSeed；category不入物理键')
    s=replace(s,'仅at_time，值改deadline−t；制造完成由remaining判，旧deadline在无未来触发/去重读取时移审计；活动期限保持精确相对值；窗口deadline必须等start+5','完整StateSeed仅接at_time；制造pending在键中改为remaining_work，value=Time(progress.remaining)。窗口pending在键中仍为at_time，value=Time(deadline−t)，且原deadline=start+5。制造原预计截止及id留完整状态，并核deadline≤t+remaining')
    s=replace(s,r'`C\|deadline\|unit`/`W\|deadline\|unit`规范为(operation,target,deadline−t)，保留同一事件的引用一致性，不以字符串相等阻断时间平移','键中的event编码为三元JSON数组[operation,target,剩余量字符串]；制造取progress.remaining，窗口取deadline−t。完整原id保持，规范引用一致重命名')
    s=replace(s,'无未来读取的非运输entered_at设null；','无未来读取的非运输entered_at设null，并将同格同种分组求和归并；')
    s=replace(s,'精确JSON编码为`{schema,domain,state,product_acceptance}`','制造待办在键中精确编码为`trigger={kind:"remaining_work",value:Time(remaining)}`、`event=[operation,target,remaining字符串]`；窗口用`at_time`及相对deadline。完整种子的制造trigger仍为at_time，校验原deadline≤t+remaining；暂停只推迟完成，旧预计身份不会占用同机未来批次的身份。该投影与按格同种归并均限上述读取前件。\n\n精确JSON编码为`{schema,domain,state,product_acceptance}`')
    s=replace(s,'旧实现只在现存PC读身份、断边后锁存的算法已撤出当前规则口径；任务8须据有效替换稿实现并验证该维护。','当前Rust已从原几何候选重算三原因并实现恢复，定向验收见任务8及IE-06；一般调度与恢复的规则对应继续按PC-06处理。')
    return s
edit(B/'规格/受限转移定义.md',transition,'采纳IE-07，汇入S8-01—04：完整累计允许n>C、D.3逐口全矿、非运输同格同种分组归并，以及制造pending的remaining_work/数组身份精确编码。StateSeed仍保存原at_time与id，并校验deadline≤t+remaining；窗口保留真实相对deadline。文档对齐已有实现，代码、schema、配置原字节保持；PC-06及全称对应未提升。')

def inp(s):
    s=s.replace('状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；新增条件证明待独立复核，Rust实现与运行验收由任务8承接。','状态：任务2、6和任务8 S8-01/04/07输入支持说明已汇入，IE-07关联冲突已终修。实际种子支持与完整起法认证分列。')
    s=replace(s,'有限执行入口仍只接受原来的 before_boundary 全空种子；任意中途种子的直接续跑、in_boundary 回放和可达历史族认证未实现，须报告 unsupported。','第四轮Python参考入口当时只接受before_boundary全空种子，作为史料保留。当前Rust接受已完整校验的before_boundary条件种子及after_closure恢复状态，可含非空库存、正常intake/working/completed、箱冷却和门窗；in_boundary/in_closure直接续跑仍为unsupported，程序全部可达后态的认证另证。')
    s=replace(s,'计数 Quantity，起点 Time/null；','计数Quantity；完整累计n允许超过当前C，n≥C记total_exhausted，n<C解除该原因，真实n保留；起点Time/null；')
    s=replace(s,'日程是机制/历史产物，at_time 不授予玩家定时能力。','日程是机制/历史产物，at_time不授予玩家定时能力。当前Rust制造pending在完整StateSeed中仍保存原at_time及id，装载核deadline≤time+remaining；仅生产键改为remaining_work及[operation,target,remaining字符串]。窗口键保存deadline−time，完整窗口deadline=start+5；详见受限转移§6.2。')
    s=replace(s,'运行检查子集接受本版已建完整几何、全空非仓库且idle的种子','第四轮Python运行检查子集当时接受已建完整几何、全空非仓库且idle的种子')
    s=replace(s,'第五轮循环只接受after_closure并按受限转移§6逐字段规范，I身份维护事件与v3台账见内核输出§2。','当前Rust循环键接受after_closure；before_boundary先实际闭包再规范。运行/周期输出为kernel-output-v4/kernel-cycle-v3，I身份维护事件及当前台账见内核输出§2。')
    return s
edit(B/'规格/内核输入.md',inp,'采纳IE-07关联S8-01/04/07：明确累计n>C合法、完整制造pending与规范键不同编码及截止不变量；将旧全空限制限定为第四轮Python史料，列当前Rust两种锚点的非空支持。关闭intake与默认程序、直接中途恢复和全起法认证保持原断点。')

def out(s):
    s=s.replace('状态：任务书7任务2、6证书契约已汇入，schema升版；Rust生成与语义验收由任务8实现，新增条件证明待独立复核。','状态：任务2、6证书契约与任务8 S8-05/06工程说明已汇入。IE-07已终修；当前Rust生成及独立重放按任务8/IE-06支持域使用，真实前向对应保持未决。')
    s=s.replace('须重算并生成v3','须重算并生成kernel-output-v4')
    s=s.replace('须重新校验来源投影、完整回放并生成v3','须重新校验来源投影、完整回放并生成kernel-output-v4')
    s=replace(s,'低产生产循环缺真实反向对应时status=diagnostic_cycle、kind/direction=diagnostic、level=production_part。','指定工程运行找到正时长重复键，但正式前向对应、真实反向复原或其他必要证据未齐时，status=diagnostic_cycle、kind/direction=diagnostic、level=production_part；实际率lt/eq/gt单独保存。当前Rust在PC-06未结清期间统一生成诊断周期，数值达标也适用。')
    s=replace(s,'## 3. 验证与结果解释','## 3. 验证与结果解释\n\n当前入口为`kernel verify-batch`：AJV结构校验、Rust完整重放和`crates/kernel/tests/audit_task7.py`逐事件部分接收核账。当前格式为kernel-output-v4/kernel-cycle-v3；旧Python参考实现及其记录按历史格式单列，未知版本拒收。下列第四至六轮样例和命令是历史证据定位。')
    s=replace(s,'本规格汇入仅完成契约，实际内核运行结果由任务8提交。','任务8与IE-06已交实际定向运行/重放；本次文字汇入后新生成的相关证据及重锁范围见任务7执行/复核/终修记录.md。')
    return s
edit(B/'规格/内核输出.md',out,'采纳IE-07关联S8-05/06：诊断身份取决于对应义务而非是否低产；列当前v4/v3生成与verify-batch验收入口，旧Python及v3记录归史料。精确键从受限转移§6.2读取，完整StateSeed恢复语法保持。旧证书原件绑定旧文字，当前重算另存终修证据。')

def model(s):
    s=s.replace('状态：任务书7任务2、6规格汇入完成。已定规则、带前件的证明接口和工程停止分别登记；新增条件证明待独立复核，Rust实现与运行验收由任务8承接。','状态：任务2、6规则与任务8工程支持说明已汇入；IE-07关联S8-06已终修。99轴处置保持，已实现机制和未证真实对应分别登记。')
    s=s.replace('Rust实现尚待任务8同步部分接收和准入口条件恢复；配置被装载不构成这些行为已实现的证据。','Rust已实现部分接收、每次尝试冷却和准入口当前条件恢复，定向运行与独立重放见任务8/IE-06；完整真实调度与全部后态仍按PC-06等义务登记。')
    s=s.replace('当前Rust传输函数仍为旧实现，任务8须迁移部分接收、残留格与台账','当前Rust已实现部分接收及台账；严格部分接收且同种多编号格的扣格歧义提交前停止')
    s=s.replace('当前Rust身份锁存实现待任务8替换；恢复后的接通记账、轮询和同刻组织仍待任务6','当前Rust已按原几何候选恢复三原因；恢复后的接通记账、轮询和同刻组织的一般真实对应仍待任务6')
    s=s.replace('部分接收、门条件恢复修改后的完整后继仍缺任务6推导与任务8实现','部分接收及门条件恢复已有任务8实现和IE-06定向重放，完整真实后继仍缺任务6的PC-06推导')
    return s
edit(B/'规格/受限模型声明.md',model,'采纳IE-07关联S8-06：撤下部分接收与门恢复“尚未实现”的过时说明，改为已实现的具体支持域与独立运行范围；歧义扣格、一般调度及全参数证明保持未完成。99轴名字、值和生命周期未改，配置及schema保持原字节。')

for name,record in [
    ('规格修改稿-任务8.md','采纳IE-07：S8-01—07已逐项核当前代码、正式条文与实际输出，写入受限转移/输入/输出/模型声明。schema当前已容纳实际编码，未改schema、配置或代码；修订后依赖重锁及定向重放另存证据/终修，旧证据保留原指纹。'),
    ('内核验收.md','采纳IE-07：七条规格修改稿已落地；精确键、当前版本及种子支持与代码对齐。IE-06原定向验收按旧指纹范围保留；本次关联周期和运行记录另生成并重放，见复核/终修记录.md。没有改源码或追加全域认证。'),
    ('规格回填.md','采纳IE-07：任务8 S8-01—07汇入受限转移、输入、输出和受限模型声明；现行精确键采用remaining_work及同格同种归并。此前任务1/汇入记录均按明确时点保留，当前实现状态取内核验收和终修汇总。AX-01的旧空箱条目仍仅属早期记录，现文every_attempt已定。')]:
    def f(s,name=name):
        if name=='规格修改稿-任务8.md':
            s=s.replace('状态：内核席提交，尚未写入规格目录。','状态：S8-01—07已经任务7终修核对并汇入规格正文；原位置行号绑定首次提交版本。')
            s=s.replace('交规格席落地','当前落点见文末终修记录').replace('规格席落地后须重锁受影响输入/证书的依赖并重新验收；本稿没有直接改规格文件。','当前落地后须按新文字重新生成关联证据，原运行证书按旧指纹封存；本次定向重放见复核/终修记录.md。')
        elif name=='内核验收.md':
            s=s.replace('独立接口与证据席的复核待交','独立接口复核IE-06已交，IE-07契约冲突由本次终修处理')
            s=s.replace('该稿尚未汇入规格目录；当前schema已接受实际输出。','该稿S8-01—07现已由终修席汇入；当前schema接受实际输出的范围保持。')
        else:
            s=s.replace('## 1. 依据与证据层级','前六节为任务1首次执行史料，截止2026-09-21上午恢复R36前；后续当前口径见“汇入记录”及文末“终修记录”。\n\n## 1. 依据与证据层级')
        return s
    edit(D/name,f,record)

(E/'正文修订差异.md').write_text('# 终修正文差异\n\n执行记录，截止2026-09-21；仅含被修订段落差异，当前正文为规则应用入口。\n\n'+ '\n'.join('## '+Path(x['path']).name+'\n\n```diff\n'+x['diff']+'```\n' for x in changes))
(E/'修改清单-第一段.json').write_text(json.dumps([{k:v for k,v in x.items() if k!='diff'} for x in changes],ensure_ascii=False,indent=2)+'\n')
print(json.dumps({'changed':len(changes),'files':[x['path'] for x in changes]},ensure_ascii=False))
