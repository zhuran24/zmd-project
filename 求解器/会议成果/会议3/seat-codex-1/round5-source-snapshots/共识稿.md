# 会议 3 共识稿

拼稿人 seat-opus-4：只拼各节主责席交来的段落，不改结论。每节标主责席和核过席。测点的「只说明这一次」写在测点那一行里。长论证见各席 会议目录/seat-*.md。

冻结规矩：冻结版文件名带 SHA256 前 8 位、写入后只读；按 SHA 投票；只有实质反对才换版；认可后的纯补充记在文末勘误，不换版。

交付的是什么（seat-codex-3 的措辞）：一套能覆盖全部达标布局的表示基准、附条件的放松层与候选发生方案、相应的证明边界和已完成的测点；没有证明完整模型在 70×70 上解得动，也没有交付达标布局。

## 一、问题 1：带子这一层怎么表示

### 1.1 精确表示与实测（主责 seat-opus-2）

（待交）

### 1.2 分解与割（主责 seat-opus-1；覆盖核 seat-codex-1）

长论证：seat-opus-1.md 第二节；代码 seat-opus-1/toy.py、fulllp.py、coarse70.py。

1. 子问题定义在一张不随布局变化的母图上：每个可放运输单位的格三个物品格结点（c0 给传送带、分流器、汇流器、准入口共用，cH、cV 给桥的两轴），每个可能的机器位置（存货侧、取货侧分开），每个可能的核心、取货口、储存箱位置，仓库一个汇；弧是所有可能的端口对。布局只进右端：容量行 C z ≤ u(x)、下界行 D z ≥ t(x)，u、t 是主问题 0/1 字面量的线性式；守恒行 E z = 0 和全部左边系数与布局无关。z 是弧×物品的周期平均流量、机器×配方的批率、取货口的出量，全部连续。
2. 任何达标布局取一个可到达循环态的周期平均量，就满足子问题的每一行（依据逐条见 seat-opus-1.md 2.2）；只要求有一个循环态，不要求各循环态内部流量相同。
3. 割：取 ρ ≥ 0、μ ≥ 0 和任意 π，只要母图每一列都满足 (Eᵀπ + Dᵀρ − Cᵀμ) ≤ 0，就有 μᵀu(x) ≥ ρᵀt(x) 对一切使子问题可行的布局成立。乘子取第一阶段线性规划（所有「≥」行加松弛，保证第一阶段本身可行）的对偶；有理化后在整张母图的全部列上逐列精确核，违反的列只抬高该列所在容量行的 μ；乘整数后系数、右端都向上取整加回 CP-SAT，仍对一切布局成立。每条割单独可核：给出乘子和母图定义，逐列复算即可。
4. 实测（玩具工厂，不是正式问题；12×12，两个源矿口、一个砂叶口，粉碎机 ≥3、研磨机 ≥1，成品 ≥1 件/tick；运输层是完整 28 种格状态；CP-SAT 4 线程；负载 13–32，24 核上另有他席实验。每行只说明这一次）：
   - 甲，几何＋自动通道＋每台机器两边各 ≥1 条通道：1.05 s 出解，子问题判一件成品都送不出。
   - 丙1，甲＋上面的割循环：200 轮、290 s 未收敛，缺口始终是目标的 100%；割 14–34 项、右端都是 1，每条只说「这一小块还缺一条进来的通道」，主问题每轮补一格。
   - 丙4，甲＋五种物品各一份全幺模子集流＋割循环：16 轮，主问题合计 564 s（每轮 12–133 s），缺口在 33%–100% 之间来回，未收敛。
   - 乙，甲＋按物品、带配方、共用容量的整数流一次求解（刻度 60，变量 24812）：900 s UNKNOWN，一个解都没找到。
   - 对照：人摆一张 D1 布局只要 11 格运输（三台粉碎机输入正对取货口、研磨机输入正对粉碎机输出、一列直带），母图线性规划判流量可行；把它钉进乙、丙4 的模型都判可行。D1 可行且解很简单，上面三种写法在各自时间里都没找到。
5. 由此得出的两条（只说明这几次）：一是连通性必须由主问题里的紧凑流量写法负责，割只补各物品绑在一起的部分（共同容量、配方比例、矿口指派、准入口限速），主问题一点流量都不带时割循环一格一格地补、在 12×12 上就不收敛；二是即使主问题带上流量（乙、丙4），通用 CP-SAT 搜索在一个有 11 格简单解的 12×12 实例上也没找到解。抬 L 不能指望通用搜索自己找到布局，要靠有结构的受限表示（1.3）；压 U 要的是证明不可行，比找解更难，只能指望比格级更粗、更小的放松（下一条）。
6. 压 U 侧的下一档：粗网格流量放松。格级几何照旧；线性规划只在 b×b 块上建：每块每种物品守恒；相邻块之间两向流量和 ≤ 边界两侧各自的运输格数；每块「进入本块物品格的件数」≤ 本块物品格数（非桥运输格 1、桥 2，会议 2 密度律的逐块版）；机器按位置记配方，从块 B 收、向块 B 送 ≤ 该块里它端口外侧运输格数。块内求和就是从格级流到块级流的映射，所以是真放松（seat-opus-2 核过两条块级约束）。70×70 实测：（待填）。

### 1.3 抬 L 的受限表示（主责 seat-opus-3；检查器互核 seat-opus-4）

长论证：seat-opus-3.md；代码 seat-opus-3/p2p.py（精确格模型）、router.py（启发式布线器）、check.py；seat-opus-4/check_full.py。

1. **同一套表示，受限就是多加约束。** L 侧用 1.1 的格状态表示，在上面加限制，每条按 seat-codex-3.md 第六节的格式登记：来源（动态便于证明／形状便于求解／数值）、覆盖损失、撤去后的义务、失败范围。L 侧解出来的直接是一张精确布局。L 侧流量可以取 1/K 整数倍，登记为数值限制。受限模型无解或检查不过，只排除「这组限制＋这组固定项」下的布局，不降 U。
2. **候选闸门。** 两份独立检查器按「当且仅当」重算结构通道（第三节第 4 条）→ 连续 LP 复核分物品流量（1.2 的子问题）→ 运行条件、调试办法、内核。L 侧用什么方法搜不限（CP-SAT、启发式、人工成块），过了闸门才算候选。
3. **第一个受限类：点对点（P2P）。** P1 只用传送带和桥；P2 每条通道只走一种物品，路径从一个取货端口直到一个存货端口，速率由两端机器的角色定；P3 形成的通道全在用；P4 桥的在用轴两端都是在用弧，不用的轴不许有端口朝它；P5 两座桥不相邻。这个类里格状态由弧唯一确定，「谁接给谁」就是路径；N1、N2、N3 没有触发对象，N4a、N4b、N5b 由构造满足，N5a 由检查器查。速率层有会议 2 的候选 B（219 台、315 条通道、无分流汇流储存箱准入口）作实例。覆盖：一台机器只有一个取货物品格，P2 ⇒ 各配方产物同种 ⇒ 粉碎机、采种机每台单配方 ⇒ 粉碎 ≥69、采种 ≥17，只到 ≥219 台的分支（算术粗界 A ≤1108）。P2P 用来建第一个 L，不管顶端。
4. **实测。** 铁砖＝2 精炼-蓝铁矿、2 粉碎-蓝铁块、1 研磨-致密蓝铁、1 精炼-致密蓝铁，全部 1 件/tick；矿、砂叶粉末从窗口边界进，钢块从边界出。CP-SAT 9.15、6 线程，负载 10–32，24 核上另有他席实验；每行只说明这一次。
   - 摆放＋布线联合交 CP-SAT：1 份（6 台，12×12）48 s OPTIMAL，另一次限时 30 s 为 FEASIBLE；2 份（12 台，16×15 与 18×18）120 s 与 90 s 都没有可行解。
   - 摆放固定、CP-SAT 只布线：2 份 2–5 s 最优；4 份（24 台，30×30）首解 4 s；16 份（96 台，64×64，砖间走廊宽 4）与 17 份（102 台，70×56，走廊宽 2）300 s 都没有可行解。
   - 同样这两个固定摆放，拥塞协商式启发式布线器（源汇先按曼哈顿距离指派）：0.9 s、15.4 s 布通；两份检查器各自重算得 999 条、1186 条结构通道，零错零警告。
5. **由此定的 L 侧流程。** 摆放发生器 → 启发式布线 → 两份检查器 → 连续 LP → 运行条件、调试办法、内核。布线失败只作 L 侧回退，不当压 U 的割。这些实例上，全厂规模的布线本身不难，难在 CP-SAT 的格级布线编码；剩下的难处是摆出布得通的摆放：只带端口邻接的摆放过不了最大流（1.1 的 E0），砖加走廊的摆放布得通（这是形状限制，要登记）。

### 1.4 覆盖审计（核过 seat-codex-1）

（待签）

## 二、问题 2：流量和「谁接给谁」（主责 seat-opus-1；数值域 seat-codex-3）

长论证：seat-opus-1.md 第二、三节，seat-codex-3.md 第二、三节，seat-opus-2.md 第二、四条，seat-codex-1.md 4.3、6.3 节，seat-codex-2.md 第三、四节，seat-codex-4.md 第 2 节。

1. **「谁接给谁」不另设变量。** 物理上谁和谁相连由格状态按「当且仅当」推出（两端口相对、至少一端是运输单位就有通道）；谁的产物供给谁，是按物品平均流量的路径分解。三样东西分开：结构通道（几何推出）、运行中的通道状态（准入口可阻断）、某个循环态里有通过量的通道（正流量）。零流量的结构通道仍在，仍参与优先级和阻尼。
2. **流量的定义。** 取一个可到达循环态，每条通道每种物品一个周期内过的件数除以周期。只要求有一个循环态，不要求各循环态内部流量相同。运输物品格（桥每轴一个）按物品守恒、各物品合计每 tick ≤1；分流器、汇流器三个端口共用一个物品格；只有制造单位按配方改物品种类，存货侧收＝Σ用量×批率、取货侧出＝Σ产量×批率、Σ时长×批率 ≤1；不能在运输格里放可相抵的产耗，会议 1 的「同格自产自销」由此排除。
3. **数值域。** 周期只知是 20 的倍数，平均流量的分母没有上界，所以流量用连续量。把带配方比例的流量写成 1/K 的整数倍放进 CP-SAT，目前没有覆盖证明，它的「无解」不能用来降 U。措辞照 codex-1、codex-2、codex-3：全幺模是整性的充分条件，不是必要条件；「不是全幺模」只取消通用保证，不等于已经漏了某张达标布局；「尚无实用的分母界」，不写成「不存在分母界」。一个精确核过的约束块例子（codex-3）：砂叶粉碎 u=r、v=3r、v=1 的连续唯一解是 (1/3, 1/3, 1)，1/20 网格上无解；它不是整厂达标布局的反例。
4. **哪些流量能留在 CP-SAT 里取整（seat-opus-2 提，seat-opus-1、codex-1、codex-2、codex-4 核过）。** 任取若干物品子集（全体、单一物品或几种的并），每个子集一份不分物品的流：各自守恒、各自 ≤1、≤ 通道支撑；每台机器对该子集的收量、出量只给整数上下界，按机型给合计；子集之间除共用布尔外不写任何联系。固定布尔后每份都是网络矩阵，乘 K 后界是整数，有理可行就有整数可行；各份之间是直积，拼得出整体整数点。条件（codex-1 写全）：每层的机器侧上下界和机型合计都要先有必要性依据，合计按已证的方向写（下限不写成等式，除非等式已证）；同一子集里的各条合计所涉机器集合要成层；每台机器同一子集的收、出之间不写比例；层与层之间不共用批率等连续量，也不加「逐弧子集流 ≤ 全体流」、相加等式、联合限容这类联系。即使全体层和每个单品层都可行，也可能没有联合共享容量的流（codex-1 有小网络见证），所以联合的线性规划仍然要。得到的是整数替代流，不是实际流（codex-4），不能再要求它和实际流有相同的正支持。玩具实测提醒（seat-opus-1 D1-丙3）：子集要分到能区分原料和中间品，否则「收粉」的上界会让原料直接进研磨机。
5. **这些子集流丢掉的**是逐弧的共同容量（各物品合计 ≤1）和逐台的配方比例；它们连同矿口指派、准入口限速，放在母图上的线性规划里，经 1.2 的割回到主问题。
6. **支撑与满速。** 「通道有流量」布尔 z 只给上界（Σ物品流量 ≤ z ≤ 通道字面量），是支撑的放松，z=1 不等于实际有流；不对正流量加最小值 ε（加了就漏布局）。满速标记 u 写成下界行（送出 ≥ u），第一阶段要对它加松弛。

## 三、问题 3：先后机制推出的条件怎么落到这一层（主责 seat-opus-4；储存箱、混料 seat-codex-2；机制量词核过 seat-codex-4）

逐机制的表（触发图样／要用的量／条件／压 U／抬 L）在 seat-opus-4.md 第二节，储存箱、混料两行取自 seat-codex-2.md 第五、六节。

1. **压 U：第一档不为先后机制另写约束。** 删掉任何约束都是放松，与它是不是条件句无关；取货/存货分级、密集结点、来源定序、轮询均分、准入口、储存箱编号格推出的是「需要定序时」「前提全部成立时」一类条件句，触发与否看动态，现在没法写成对所有达标布局成立的约束。这不断言机制里再推不出必要条件：思路第二节那一遍机制过完，推出新的对所有达标布局成立的条件，照样加进来压 U。正式约束里由机制推出、对所有达标布局成立的几条在带流量的模型里是推论，各自要用的部分不同（seat-opus-4.md 第一节第 1 条逐条列了，按条文推，未写成代码核）：满速独占要运输物品格容量 1、桥两轴分开、根通道流量为 1（准入口那部分还要 q ≤ k/5）；矿线专机、灌装混线、回路转弯要分物品的流；混做清空只有计数部分是推论，「逐批交替时」那部分不是，压 U 删掉；四通结点、运输端口收支要支撑计数和精确格几何。前提不满足的更粗放松里（桥两轴合并、不分物品、粗网格）它们不全是推论，删掉仍是放松，要用就补写；不带流量的主问题里可写成支撑 z、满速标记 u 和格状态上的 0/1 蕴含，作冗余割。这些都是整数陈述，不碰流量取整的问题。
2. **阻尼不进主问题。** 压 U 删掉取货分级是真放松。抬 L 先不触发取货分级（N1）；放开时对固定候选在元件图上逐个分叉取值 ξ 核 d_高(ξ) < d_低(ξ)。「高一级最大阻尼 < 低一级最小阻尼」只是充分条件，只能当抬 L 的限制。
3. **抬 L：机制落成受限类加候选检查。** 各条登记丢掉的布局；N1、N2、N3a、N4a、N4b 是格状态上的局部式子，N3b 要用能到达的物品种类，N5b 要用流量：N1 不触发取货分级；N2 不触发存货分级（含密集结点）；N3a 承载流量的准入口不设累计上限；N3b 限种准入口不被永久堵死；N4a 桥每轴两端一取一存；N4b 桥不成串；N5a 候选按「当且仅当」重算全部结构通道逐条对上（检查）；N5b 每条结构通道设计流量严格为正（路径段构造，或 f ≥ 1/K 另登记数值限制；连续 LP 复核用固定支持集最大化共同 δ）。储存箱、混料的限制见 seat-codex-2.md 5.3、6.4。四个不得依赖量在候选验证里的记录与覆盖义务用 seat-codex-4.md 6.2 的接口。在 N1–N5b 下且不放储存箱时，四个不得依赖量能起作用的只剩轮询次序和判定先后；这只缩小它们起作用的位置，不缩小运行义务：起动、库存、死锁、全部可达循环态照样要证，内核跑一次只覆盖那一次。
4. **检查器两份独立实现、已对上。** seat-opus-4/check_full.py 与 seat-opus-3/check.py 都从格状态按「当且仅当」重算结构通道；对 seat-opus-3 的候选 r_k1_12.json（结构通道 18 条）、r_k2_tile30.json（52 条）两份都无错，r_tile_12309.json 两份都报桥串；启发式布线的两个全厂规模实例 rt_k16.json、rt_k17.json 两份各自重算得 999 条、1186 条结构通道，都零错零警告。check_full.py 另报 N1–N5b、断头与空通道、从来源传播的物品种类（错料、偏离设计配方）、N1 触发时逐 ξ 的阻尼。
5. **规则读法，现在都不上报 owner：** 桥串（相邻两桥同轴相接时端口都未定型，规则没写怎么定；压 U 不受影响，抬 L 用 N4b，高面积档候选非用桥串不可时才问）；「一段连续的传送带」按通道相继还是按几何相邻读（只影响阻尼，放开 N1 时才用到）；桥两轴共同存货分级时「能移动」的读法（N2 把桥也算进去，不依赖它）。

## 四、问题之外，和几何求解要紧相关的

- 机器按「机型×朝向×位置」建布尔，不给机器编号：70×70、217 台时约 15.6 万个（固定一个 21×53 后约 10.8 万），台数就是同型位置变量之和，同型机器互换的对称自动消失；会议 1 那次按台编号是 392 万变量。（与 seat-opus-2 同）（seat-opus-1）
- 计时：本机 24 核，会中各席同时跑实验，负载在 10–32 之间；每个测点都附了负载，跨测点比较墙钟时要把这个算进去。（seat-opus-1）
- A=1113、P=10 这一级至少 69 个两轴都承载流量的桥（算术推论，只描述这一级的必要条件）：运输物品格总通过量 W ≥ 305.65、每格每 tick ≤ 1 件 ⇒ 有流量的物品格 ≥ 306；运输格 T ≤ 237（会议 2 密度律）、一格至多两个物品格 ⇒ 双轴承载的桥 ≥ 306 − 237 = 69。正式 运输下限 的 T+b ≥ 306 只直接给桥总数（seat-codex-3 补全）。（seat-opus-4）

## 五、未决

- 「70×70 上解得动」目前没有正面测点。格级流量进单体 CP-SAT：seat-opus-2 E1′（70×70，不分物品全幺模流）902.8 s 无解；seat-opus-3 16 份铁砖摆放固定只布线 300 s 无解；seat-opus-1 玩具 12×12 按物品带配方单体（乙）（待填）。主问题不带流量、全靠割补：12×12 上 200 轮不收敛。粗网格流量放松在 70×70 上的割循环：（待填）。（seat-opus-1）
- 割的强弱：图外结点的乘子怎么取、要不要取 Pareto 最优割、一轮回送几条割，都没有研究；现在的割每条只切掉很小一块。（seat-opus-1）
- 割循环每轮主问题都从头解（CP-SAT 不能增量加约束），70×70 上一轮的时间决定了这条路能走几轮。（seat-opus-1）
- 思路第二节那一遍机制还没过完；过完推出的新的、对所有达标布局成立的条件，照样加进压 U 的模型。（seat-opus-4）
- 放开 N1（取货分级）要先定「一段连续的传送带」的读法（只影响阻尼）；放开 N2 里的密集结点要对分流比变化范围做鲁棒核；放开 N4b（桥串）要先定两桥相接时端口怎么定型，高面积档候选非用桥串不可时才作为一句问 owner。（seat-opus-4）
- 混料的数值界（seat-codex-2 6.3：「比例对、偏差有界、两格各 50」不充分）还没有给出。（seat-opus-4）
- 检查器 check_full.py 现在只认制造单位、运输单位和窗口边界的虚拟来料/出料；协议核心、仓库取货口、协议储存箱、供电桩还没接。（seat-opus-4）
- 全厂 219 台的第一张静态候选：用哪些砖（铁链、源石链、植物回路、成品链）、砖间走廊多宽、空矩形放哪，和真实仓库取货口、协议核心、供电桩一起摆，再布线、过闸门。（seat-opus-3）
- 摆放发生器里「布得通」的代理条件：只要求端口邻接太松，带流量联合又解不动，两者之间还没有测点。（seat-opus-3）
- P2P 之外的受限类（带分流器、汇流器、准入口）要随 N1–N3 逐条放开。（seat-opus-3）
- 研磨机三路存货两种原料分走不同通道时的运行义务：P2P 只保证静态上每路一种料，动态要另证。（seat-opus-3）

## 勘误

（冻结后用）
