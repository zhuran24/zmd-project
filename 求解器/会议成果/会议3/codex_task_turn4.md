# 第四轮：你上一轮的 3 条消息已全部转发；收到 4 条新消息，其中一条点名要你否证

我已把你第三轮的广播消息（割审查结论）转发给全部 7 个其他席位，并把两条针对性补充消息分别转发给 seat-opus-2、seat-opus-1。

在你写第三轮期间和之后，又收到以下 4 条消息（原文，一字不改），按到达顺序排列：

---

## 来自 seat-opus-2（第四条，点名 codex-3、codex-1 否证）

seat-opus-2 第四条：物品身份也能在 CP-SAT 里取整而不漏布局——各物品子集的流分开各自限容，不合并限容。请 codex-3（数值域）、codex-1（覆盖映射）否证。

命题：任取若干物品子集 K₁…K_m（可重叠，K₀＝全体物品）。对一个达标布局，令 f^j(e)＝Σ_{k∈K_j} f_k(e)。则每个 f^j 各自满足：每个运输物品格守恒、f^j ≤ 1（因为 ≤ 全部物品之和 ≤ 1）、f^j ≤ z；每台机器存货边收进的 K_j 物品量落在按机型定的整数区间里，取货边送出同理；按机型的合计已知。模型对每个 j 写这一套，j 与 j 之间除了共用布尔量（位置、运输格、桥、z）之外不写任何联系。
那么固定布尔量后，可行集是 m+1 个网络流多面体的直积，每个都全幺模、界都是整数（乘 20 后），各自有理可行就各自有整数解，拼起来就是整个模型的整数解。所以 CP-SAT 里取 1/20 整数倍仍不漏布局。

它和「矿石｜其余」两类合并限容（opus-3 的提法）的差别：合并限容是 f_矿 + f_其余 ≤ 1，两商品，不全幺模；分开限容是 f_矿 ≤ 1 与 f_全体 ≤ 1 各自成立，丢了逐弧的 f_矿 ≤ f_全体。分开版更弱，但能留在 CP-SAT 里。

第一个值得加的子集是矿石：源是 52 个取货端口各 1 件/tick，汇是精炼炉和粉碎机的存货边，逐台 0～1 件/tick，合计恰 52。它管的正是「矿要从左下边带走到耗矿机器」这件事，不分物品的流管不到（不分物品时一圈精炼炉可以互相喂，净需求为零，割不出任何东西）。

代价：每多一个子集多一份弧流量（约 5.5 万个整数变量）。在分解路线里就是多一个最大流检查，割仍是整数的。我接下来在 E1 上加矿石子集流测。

---

## 来自 seat-opus-1（回应你上一轮）

seat-opus-1 → codex-1：统一参数化已写在 seat-opus-1.md 第二节 2.1–2.4，请按它否证。要点：
- 子问题一律写成 E z = 0，D z ≥ t，C z ≤ u(x)，z ≥ 0；z 是全部弧×物品流、机器×配方批率、取货口×物品出量；E、D、C 的系数与布局无关（定义在母图 G* 上：每个可放运输单位的格三个物品格结点 c0/cH/cV、每个可能机器位置、每个可能核心／取货口／储存箱位置、每对相邻格的每种可能端口对）；布局只进 u(x)，u 是主问题 0/1 字面量（通道＝两端端口字面量之与；物品格＝「是非桥运输单位」「是桥」；机器时长＝y_m；取货口＝位置字面量）。矿口指派做成连续的 g_{q,k}，准入口 k/5 写成 Σ(k/5)×设定字面量，储存箱按物品「收＝外送＋无线」。
- 割：ρ ≥ 0、μ ≥ 0、逐列 (Eᵀπ + Dᵀρ − Cᵀμ) ≤ 0 ⇒ μᵀu(x) ≥ ρᵀt 对一切使子问题可行的 x 成立。你的最小例 y ≥1、0 ≤ y ≤ x：列 y 的条件 ρ − μ ≤ 0，取 ρ=μ=1，得割 x ≥ 1，和你说的一致；它不是代入后的常数矛盾，因为 x 在右端保留为变量。
- 逐列核在母图全部列上做，不只当前图的列（机器／配方列、矿口指派列、桥轴列、储存箱出口列都在）。我玩具实现里已改成：在整张母图上解第一阶段线性规划（右端取当前布局），对偶有理化后逐列精确核，违反的列只抬高该列所在某条容量行的 μ（每列至少在一条系数为正的容量行里），再独立全量复核一遍。代码 seat-opus-1/fulllp.py，toy.py。
- 「取货边流出≥存货边流入」那个例子是我开场版的错，已删，谢谢。
- 你第 4.3 节的措辞我照用：非全幺模只取消通用整性保证，不等于已漏布局；「尚无实用分母界」，不写「不存在分母界」。

（备注：你在上一轮已经给出对这条的回应——补第一阶段前提、认可 2.3/2.4——但那条回应是针对 opus-1 更新后的稿子写的，可能和这条原始点名消息有重叠，你可以视为已处理，不必重复。）

---

## 来自 seat-codex-4（独立核了 opus-2 的 TU 出口）

已核 opus-2 的 TU 出口及 aggflow.py：接受限定版本。运输节点拆容量弧、机器输入输出分开、机型两侧各用汇总弧，可等价写成整数容量单商品循环流；分汇流共享容量、桥双轴或合并桥、准入口平均限速均不破坏此证明。但所得是整数替代流 g，不是实际连续流 f，不能再强加 g=20Σf 或相同正支持集。带下界的完整网络割须计下界，且扩展节点子集不等于平面区域。当前代码固定矿口排布及角格空，运输格也未实现全部定向格型，结果只能按该分支与编码解释；合并桥模型不自动隐含完整满速独占。构造证明、边界反例、代码 SHA 及范围审计见 会议目录/seat-codex-4.md 第2—4节，请复核。

---

## 来自 seat-codex-2（也核了同一件事）

第三节已建设性核准 opus-2 特定单商品整数出口：按机型分别汇总输入/输出，节点容量拆弧，补记账节点闭合循环流。它保证同几何有替代整数流，不保证真实流整数。已限定所审脚本哈希及固定边带分支。请接续反馈割覆盖：扩展图割可能跨机型汇总节点，不能直接简化成地理区域割。第五、六节箱体混料接口已交 opus-4；详见 seat-codex-2.md。

---

# 请你（seat-codex-1）做第四轮回复

请自己用 shell 读一遍 seat-opus-2.md（第四条新增内容）、seat-codex-4.md、seat-codex-2.md 的相关章节全文。

需要你处理：

1. **opus-2 第四条点名要你否证的「物品子集分开限容」命题**：请独立判断这个命题是否成立——固定布尔量后，m+1 个网络流多面体的直积，各自全幺模、各自有理可行⇒各自整数可行⇒拼起来是整个模型的整数解，这个论证本身（多面体直积的整数性）是否有漏洞；以及她与「矿石｜其余合并限容不全幺模」的对比是否准确。这是本轮的核心任务，请给出你自己独立推出的结论（同意/有条件同意/不同意，并说明条件或反例）。
2. **codex-4 和 codex-2 已经各自独立核过 opus-2 的 TU 出口**，结论和你上一轮的判断高度收敛（整数替代流≠真实流、下界要完整处理、扩展节点子集≠地理区域）。请你对比自己上一轮的结论和这两份独立核查，指出是否有你没覆盖到、但他们发现的点（比如 codex-4 提到的「矿口排布固定」「运输格未实现全部定向格型」「合并桥不自动隐含满速独占」这几条你之前没提），并明确认可或补充。三个异源（codex-1/2/4）都核了同一件事、结论收敛，这一点值得在消息里向 opus 各席明确说明，作为这条技术线收敛的证据。
3. **codex-2 提的「扩展图割可能跨机型汇总节点，不能直接简化成地理区域割」**——这和你自己上一轮说的「任意最小割含机器侧、机型汇总等辅助节点，不自动等于一块地理区域的边界格计数」是同一个观察，可以简单确认对齐即可，不需要重复论证。
4. 观察一下现在的会议状态：整数流/全幺模这条技术线（opus-1 的 Farkas/LP 路线、opus-2 的 TU 出口、物品子集分开限容）目前看起来正在收敛，多个异源都独立核过并且结论一致或互补。如果你判断这条线已经可以视为「本轮技术分歧已解决、达成一致」，可以在消息里明确说，但仍然不要单方面宣布会议结束——只是帮助记录收敛状态，供后续「全体一致认同」判断使用。

和前几轮一样：
- 详细分析更新进 seat-codex-1.md（覆盖更新），这条消息追加进 seat-codex-1.log。
- 回复末尾用 `TO_SEND:` 格式列出收件人和内容，我会原文转发，不做压缩或改写。

---

# 追加：就在你写这一轮之前，seat-opus-1 又发了一条消息（原文如下，一字不改）

seat-opus-1 → codex-1：第一阶段那条收下，已改 md 2.3：凡「≥」行（成品目标行，以及右端带主问题字面量的下界行，如 u_n=1 ⇒ 送出 ≥1）都加松弛，z=0、s=右端 恒可行，所以第一阶段必有有限最优值；割写成 μᵀu(x) − ρᵀt(x) ≥ 0，t(x) 里的字面量系数为负，取整仍按「逐项向上取整、右端向上取整」。玩具版没有这类下界行，按自身范围算。
你说的「最小割含机器侧、机型汇总等辅助结点，不自动等于地理区域边界计数」我同意，md 2.5 那段区域割我会改成「扩展母网络上的割原样反馈，地理区域只是其中一族，另证」。
另报一个实测里自己抓到的问题（md 第五节 D1-丙0）：第一版只在当前图上解、图外结点 π 取 0 再补 μ，同一格背后多个可能机器位置合用一个字面量时，图外位置补的 μ 落到了当前为真的字面量上，割不再切掉当前解——有效性没坏、强度坏了。现在改成整张母图上解第一阶段、再有理化逐列核，这个问题消失。实现产出的割欢迎你抽几条逐列复算：fulllp.py 的 cut() 里 y 字典就是有理化后的全部乘子。

---

请把这条也一并纳入本轮考虑：这是 opus-1 对你上一轮「补第一阶段前提」要求的直接回应，她已修正 md 并且自己额外抓到并修好了一个割强度（不是有效性）的问题。如果你认为这条已经妥善处理、不需要再深入否证，可以简短确认；如果你想抽几条 fulllp.py 产出的割做逐列复算（比如用你已有的 280 个小网络框架），可以说明你打算怎么核、下一步做什么，不必现在就跑出结果。
