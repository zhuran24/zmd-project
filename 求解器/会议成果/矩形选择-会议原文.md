# 会议 2 里「空矩形怎么选」的原文清单

查的问题只有一个：在构造法（抬 L 那一头）里，空矩形的尺寸、位置、面积档是怎么选的，或者会上认为该怎么选。

**材料范围**：`/home/zhuran24/文档/会议2全套/` 下全部文件——八份席位转录、八份 `seat-*.log`（发出消息原文）、八份 `seat-*.md`（终态立场）、142 份 `seat-codex-N_task_turnK.md`（转发层交给 codex 的任务书，内含收到的消息原文）、四份 codex 侧原始日志 jsonl、`纪要.md`、`初始材料.md`、`msg1.txt`、共识稿 v29～v45 十七版及各席复审快照。

**标注**：
- 【核过】＝该句所依据的原文已用 `grep -nF` 回原文件逐字核对，路径与行号是核对后的实际值。
- 【转述】＝我对原文意思的概括，原文引用跟在后面或在 §5 对应条目里。

**一条读时的提醒**【核过】：会上有两套互不相同的 M 编号。seat-opus-1 的 M0–M8 是全席采用、进了共识稿 E 表的那一套；seat-opus-3 在自己的立场文件里另用了一套 M1–M7，其中「M4」指「从 1113/21×53 倒推」、「M5」指局部搜索/紧凑化（`/home/zhuran24/文档/会议2全套/会议目录/seat-opus-3.md:337`、`:338`）。引用 opus-3 立场文件里的 M 编号时不要与共识稿 E 表对齐。

---

## §1 会上定下来的

1. **M4 的做法是「把最大空矩形的位置与尺寸做成会议 1 那个 R1 装箱＋端口邻接模型的决策变量，直接最大化 A」，提出人 seat-opus-1，seat-codex-1 复评，这一条从开场广播一直保留到终态立场文件。**【核过】
   出处：`会议目录/seat-opus-1.md:78`、`会议目录/seat-opus-1.md:202`、`会议目录/seat-opus-1.log:10`、`会议目录/msg1.txt:9`、`会议目录/seat-codex-1.log:141`。

2. **但这个带矩形变量的 M4 没有任何测点，会上把这一点写死进了共识稿。**【核过】共识稿 E 表 M4 那一格的「离资产」栏与 D1 推论 2～4 都写着会议 1 那个 278 秒 OPTIMAL 的测点不带矩形变量、不带流，带上之后无测点。
   出处：`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:363`、`:197`。

3. **M5 的做法是「先选空矩形 R（位置＋尺寸），再问 70×70\R 能否装下全部单位并连通」，提出人 seat-opus-1；opus-1 原稿给的扫描顺序是「按面积降序扫」。**【核过】
   出处：`会议目录/seat-opus-1.md:84`–`:85`，`会议目录/msg1.txt:9`。

4. **M5 的主评是 seat-codex-1，它给出的定版形态是「可回退分解」：固定矩形内允许搬机、旋转、改拓扑与重布线；必要时回退矩形位置／尺寸和设备数量；失败证据随原固定条件保存，换矩形不继承无依据的设备冻结。**【核过】这段话是 codex-1 第 5 轮发给 seat-codex-3 与 seat-opus-1 的消息原文，随后被 codex-3 明确接受并并入，也被 opus-1 写进共识稿 E 表与 F 节。
   出处：`会议目录/seat-codex-1.log:75`（同文另见 `:79`）；接受方 `会议目录/seat-codex-3.log:111`；共识稿 `共识草案-v45-5c9e556a.md:364`、`:379`。

5. **「单位置失败不排整档」是会上多席同口径的结论，并且被上升成一般纪律写进共识稿 C 节。**【核过】codex-1 在终态立场表里写「单位置失败不排整档」，codex-2 的方法表连写三版「部分位置失败不能排除整档」，codex-3 写「若模型固定了……某个矩形位置，就只能排除这个分支」，共识稿 C 节写「一个摆放的不可行性永远不足以降低全局 U」。
   出处：`会议目录/seat-codex-1.md:28`、`会议目录/seat-codex-1.log:45`、`席位转录/codex原始日志/seat-codex-2-codex侧原始日志.jsonl:109`、`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`、`共识草案-v45-5c9e556a.md:158`。

6. **M5 的失败记录形态定下来了：保存矩形／台数范围及未决域、已否域、静态候选、已认证布局；有限搜索失败或超时不排候选域、不排方法族；只有完整覆盖证明才能关闭对应域。**【核过】
   出处：`会议目录/seat-codex-1.md:33`、`会议目录/seat-codex-1.log:75`；共识稿 E 表 M5 的「失败时留下」栏写作「未决域记录」（`共识草案-v45-5c9e556a.md:364`）。

7. **M6（顶端直攻 21×53）与 M5 的关系定下来了：M6 是 M5 的顶端档位（codex-1 的原话），codex-2 写作「M6 是 M5 的顶端任务」。定这条的目的是「避免重复计分」。**【核过】
   出处：`会议目录/seat-codex-1.log:45`、`:49`、`席位转录/codex原始日志/seat-codex-2-codex侧原始日志.jsonl:109`。

8. **M6 不是入口，只是约定预算内的一次可证伪尝试；八席里 opus-3、opus-4、codex-1、codex-2 开场就反对以 1113 的必要条件作全局入口。**【核过】
   出处：`会议目录/纪要.md:46`；各席原话见 `会议目录/seat-opus-3.log:17`、`会议目录/seat-opus-4.log:31`、`会议目录/seat-opus-4.log:56`、`会议目录/seat-codex-1.log:2`、`会议目录/seat-codex-2.log:44`、`会议目录/seat-opus-2.md:3`。

9. **M5 的扫描顺序上，会上唯一定下来的是一条否定句：不限定必须从顶端往下扫。提出人 codex-1，opus-1 接受，进了共识稿 F 节第 6 条。**【核过】
   出处：`共识草案-v45-5c9e556a.md:379`、`会议目录/seat-codex-1.md:28`、`会议目录/seat-codex-1_task_turn11.md:19`、`会议目录/纪要.md:40`。

10. **「矩形先行」这件事本身被明确判为不违反 owner 的原则——违反的只是「把顶端条件当所有构造入口」。这是 codex-1 发给 opus-4 的原话。**【核过】
    出处：`会议目录/seat-codex-1.log:70`。

11. **M8（局部/大邻域搜索）在会上被定位成「扩大空矩形」的工具，并被降为 M5 的内层修复、不淘汰。**【核过】
    出处：`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:157`、`:215`；`会议目录/seat-codex-1.log:141`；`共识草案-v45-5c9e556a.md:367`、`:379`。

12. **交付物里必须带「最大空矩形的面积与短边」；建立保守 L 只需要一个合格矩形见证，只有在声称该布局是最大值时才需要最大性检查（codex-4 的原话）。**【核过】
    出处：`共识草案-v45-5c9e556a.md:385`、`会议目录/seat-codex-4.log:26`、`:23`。

13. **第一张布局上定下来的是一条边界条件而不是选法：不优化空矩形的布局只是运行试点／里程碑，不构成构造下界 L；真实的 L 必须带一个短边 ≥6 的空矩形。**【核过】这条由 codex-3 先提，codex-2、codex-4 同口径，提出「先求可行、不管矩形」的 opus-3 本人接受并写进自己的终态文件。
    出处：`会议目录/seat-codex-3.log:37`、`会议目录/seat-codex-2.log:44`、`:117`、`会议目录/seat-codex-4.log:23`、`会议目录/seat-opus-3.md:76`。

14. **「A=0」不是一个可选的目标档。codex-3 写死：A=0 只是方便讨论容量的放宽点，不是符合短边至少 6 要求的目标矩形。**【核过】共识稿 D1 的面积档表里 A=0 那一行同样写作「完全不要矩形，只求可行」，与 A=36「短边 6 的最小合格矩形」分列。
    出处：`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:202`、`共识草案-v45-5c9e556a.md:190`–`:191`。

---

## §2 会上明确留开的、或根本没人谈到的

1. **「矩形按什么顺序试、先试哪一个」明确留给 M5 主评自定——而且这句话在收口期被编掉了。**【核过】共识稿 v13／v19／v24 那一档的 F 节第 6 条原文是：「M5 可回退分解（codex-1 主评）——**不限定必须从顶端往下扫**（codex-1 指出，接受），矩形的选取顺序由主评自定」。到编辑收口基线 `consensus-editorial-base-fff4688b220b.md:197` 时「矩形的选取顺序由主评自定」这半句已不在，v45 F 节只剩下否定句。
   出处（留开的原话）：`会议目录/seat-codex-1/consensus-unreviewed-de4c01afba56.md:185`、`会议目录/seat-codex-2/consensus-reviewed-round8-v13.md:200`、`会议目录/seat-codex-2/consensus-reviewed-round9-v19.md:204`、`会议目录/seat-codex-2/consensus-reviewed-round10-v24.md:216`；
   出处（已被编掉）：`会议目录/seat-codex-1/consensus-editorial-base-fff4688b220b.md:197`、`共识草案-v45-5c9e556a.md:379`。
   **这一句不在 v45 的任何一处**【核过】：v45 全文 `grep -nF "矩形的选取顺序"` 无命中。

2. **codex-3 从第三轮起就把 M5 的读法标成「待主评确认」，直到会议结束也没有出现过一份「矩形怎么选」的定稿。**【核过】原话：「对 M5 暂按『保留目标矩形、在其补域内安排设备与物流』理解，待主评确认」。
   出处：`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`。

3. **转发层把「矩形怎么选、失败后怎么回退」当成一个技术问题正面抛给过 codex-1，并给了「或者说明这是需要 M5 主评自己决定」的出口。**【转述】转发层是会议机制的一部分、不是席位，这一行的措辞是转发层写的，不是任何一席的发言；codex-1 的回答就是 §1.4 那段「可回退分解」。
   出处：`会议目录/seat-codex-1_task_turn5.md:37`。

4. **矩形的具体位置（贴哪条边、贴不贴角）没有任何一席在会上论证过——但正式约束里早就有一批限制矩形位置的条文，四个 codex 席在第一轮把它们逐字读进了会话，然后没有任何人往构造方向用。**【核过】详见 §4 与 §5 第 5 条的原文。
   - 全部 `会议目录/` 与 `席位转录/` 文件里没有出现过「不失一般性」「等价类」这类措辞（逐词 grep 零命中）。
   - 「左下角」「右上角」「贴角」只出现在四份 codex 侧原始日志里，且**全部落在 `cat 求解约束.txt` / `rg … 求解约束.txt` 的工具输出中**（`矩形离带`、`角区`、`核心离带`、`内带缺口` 等条文都以「以左下角格为 (0,0)」开头）。逐条核过事件类型：`seat-codex-1-codex侧原始日志.jsonl:23,25,229,232`、`seat-codex-2-codex侧原始日志.jsonl:23,25,188,191`、`seat-codex-3-codex侧原始日志.jsonl:21,23,25,30,32,44,46,88,90,173,176`、`seat-codex-4-codex侧原始日志.jsonl:20,23,130,144,147,466,468` 全是 `CommandExecution` 或它的 `custom_tool_call_output`；`seat-codex-4-codex侧原始日志.jsonl:653` 是 `compacted` 事件（把上述工具输出原样带进压缩历史）。**没有一条是席位自己写的正文。**
   - 唯一出现的「支配」是 codex-4 写的「可以将密排、接头格模型作为优先试验的启发式，**不可作为已证支配关系**」（`席位转录/codex原始日志/seat-codex-4-codex侧原始日志.jsonl:230`），以及 codex-3 写的「不接受其无条件支配其他构造法」（同目录 `seat-codex-3-codex侧原始日志.jsonl:131`），两处都不是关于矩形位置的。
   - 唯一出现的「对称性」是 codex-2、codex-4 在评整场联合求解时说的「变量与对称性成本可能高」（`seat-codex-2-codex侧原始日志.jsonl:68`、`seat-codex-4-codex侧原始日志.jsonl:29`），指的是求解器里的对称破缺成本，不是矩形位置的支配论证。
   - opus 四席的转录里这一段工具输出被截断了（例如 `席位转录/seat-opus-2-全程转录.md:163` 写着「…[截断 9024 字]…」），完整条文只存在于 codex 侧原始日志。

5. **「矩形不能贴左边和下边」这句话在会上是有原文的，但它是正式约束 `边带排布` 的结论句，不是哪一席的论证。**【核过】原文见 §5 第 5 条：「因此空矩形不含左、下两边最外的两列与两行」。
   - **没有任何一席在会上引用过这句**【核过】：「空矩形不含左、下两边最外的两列与两行」在全库的 17 处命中全部是 codex 侧的工具输出行。
   - 席位自己引用「取货口配置：左、下边界放满仓库取货口」时（`席位转录/seat-opus-3-全程转录.md:1037`、`会议目录/seat-opus-2.md:15`、`会议目录/seat-opus-3.md:319`），每一次都是用它来数 138 格占地、或用它推「52 个取货端口一件成品也不放行」（`会议目录/seat-codex-4_task_turn20.md:122`），**没有一处从它推出矩形的位置限制**。

6. **「候选矩形集合就是没被排除的那些位置」这句话没有任何人说过。**【核过】全库 `grep -n "候选矩形"` 只有两处命中，都是转发层在任务书里替 codex-1 拟的一个提问（「矩形分层搜索是否要按 opus-2 给的『A 上限价签』重新排序候选矩形/结构分支」，`会议目录/seat-codex-1_task_turn4.md:48`、`席位转录/codex原始日志/seat-codex-1-codex侧原始日志.jsonl:111`），**codex-1 那一轮的回复没有正面回答这个问题**（该轮四条出站消息见 `会议目录/seat-codex-1.log:58`–`:71`）。

7. **压 U 那一头已经算出来的矩形位置枚举，会上只被引用过一次，而且是当作 M5 的「漏掉」栏写的，不是当作候选集合。**【核过】见 §4。

8. **共识稿 H 节（未决与不承诺）里一条与矩形选择有关的条目都没有。**【核过】`共识草案-v45-5c9e556a.md:419`–`:466` 逐条读过：回路存量上界、M068 残余扇出点、「同一时刻不是同一 tick」、离线承接量、ρ 形、粗密度界、注入机制三条降级理由、S6′、M7 无测点——没有一条是矩形。

9. **几何这一层整场一格都没碰，这是会上自己写的。**【核过】纪要的会后工作清单末句写「315 条通道能否嵌进 70×70 完全没碰」；opus-4 终态文件写「315 条专用直连能否在 70×70 里无冲突走通，一格都没碰。这是下一步唯一该做的事」。
   出处：`会议目录/纪要.md:122`、`会议目录/seat-opus-4.md:220`。

10. **会上认领的分工在会后全部失效，开工时要重派——这是主席在纪要里按「自己推」标注的一句，不是席位的话。**【核过】
    出处：`会议目录/纪要.md:119`。

---

## §3 各席提案与反对清单

| 席位 | 提案要点（关于矩形怎么选） | 谁反对 / 谁补正 | 结果 | 出处（绝对路径见文末简写表） |
|---|---|---|---|---|
| seat-opus-1 | M4：把最大空矩形的位置与尺寸做成 R1 装箱＋端口邻接模型的决策变量，直接最大化 A | 无人反对提法本身；codex-1 只对它的**证据地位**设限（固定 217 台测点不能直接认证全局松弛；否定单个矩形位置不一定压全局 U） | 保留，进 E 表与 F 节第 5 条，定为「低成本跑一次、结果不 gate 任何人」；且标明**无测点** | `seat-opus-1.md:77`–`:82`、`:202`；`seat-codex-1.log:45`；`v45:363`、`:378`、`:197` |
| seat-opus-1 | M5：先选空矩形 R（位置＋尺寸），再问余下能否装下并连通，**按面积降序扫**；「与 M4 天然合体——R1 模型里带 R 变量就等于一次扫完所有 R」 | codex-1 反对把顺序限定成从顶端往下扫；codex-3 要求说清「如何允许改变矩形和设备布局」 | 提法保留，**降序扫这条顺序被去掉**，改成「不限定必须从顶端往下扫」＋（中间版本）「选取顺序由主评自定」 | `seat-opus-1.md:84`–`:89`；`seat-codex-3.log:48`；`v45:379`；`consensus-unreviewed-de4c01afba56.md:185` |
| seat-opus-1 | 排序：M2/M3 出第一个 L>0 → M8 改进 → **M5/M6 从顶端往下压**，两端相向而行 | codex-1（顺序不限定）；codex-3（不先压 U，U 已走 56 轮到 1113、边际收益递减，L 还是 0） | opus-1 接受；F 节改成 M7 → M1 → 植物试点 → M4 → 之后 M5/M3/M6 | `seat-opus-1.md:125`、`:118`；`纪要.md:44`、`:40` |
| seat-codex-1 | 开场即把「矩形分层搜索」列为自己主评的比较路线之一，同时写「不把 1113 的必要条件当作全局搜索入口」 | 无反对 | 采纳，成为它的主评范围 | `seat-codex-1.log:2`；`seat-codex-1.md:16`、`:20` |
| seat-codex-1 | M5 定版：可回退分解（固定矩形内可搬机/旋转/改拓扑/重布线，必要时回退矩形位置尺寸与设备数量；失败证据随原固定条件保存；换矩形不继承无依据的设备冻结） | codex-3 接受并并入；codex-2 记入方法表「可改数量与矩形；单次失败不禁整个方法」 | 采纳，成为 E 表 M5 与 F 节第 6 条的底稿 | `seat-codex-1.log:75`；`seat-codex-3.log:111`；`c3jsonl:202`；`c2jsonl:211`；`v45:364` |
| seat-codex-1 | M6 是 M5 的顶端档位，按此避免重复计分 | codex-2 同口径（「M6 是 M5 的顶端任务」） | 采纳为分工口径；但这句话**没有进共识稿正文**，E 表里 M5 与 M6 仍是并列两行 | `seat-codex-1.log:45`、`:49`；`c2jsonl:109`；`v45:364`–`:365` |
| seat-codex-1 | 矩形先行本身不违反 owner 原则，只有把顶端条件当所有构造入口才违反 | 无反对 | 保留（在 log 与转录里，未进共识稿正文） | `seat-codex-1.log:70` |
| seat-codex-2 | M5/M6 合成一格评：直接对齐目标；部分位置失败不能排除整档；不独押 1113，保留低档首个构造；**外层策略可用多种内层算法** | 无反对 | 「部分位置失败不排整档」进了 codex-1 终态表与共识稿 C 节；「外层多内层」这句只留在 codex-2 自己的方法表里 | `c2jsonl:109`、`:153`、`:211` |
| seat-codex-3 | M5 读作「保留目标矩形、在其补域内安排设备与物流」，**待主评确认**；遗漏栏写「固定形状、位置或先摆后连可能排除可行结构」 | 无反对；codex-1 的可回退分解正是对这条的回应 | 提法被 codex-1 的可回退分解吸收；「待主评确认」这个状态一直没被取消 | `c3jsonl:144`、`:157` |
| seat-codex-3 | 初期可以暂不优化空矩形面积，但没有短边 ≥6 的空矩形就只能算运行试验产物，不能建立构造下界 | 无反对，opus-3 接受 | 采纳，成 §1.13 | `seat-codex-3.log:37`；`c3jsonl:144`；`seat-opus-3.md:76` |
| seat-codex-3 | A=0 只是方便讨论容量的放宽点，不是符合短边 ≥6 的目标矩形 | 无反对 | 采纳，共识稿 D1 面积档表按此分列 A=36 与 A=0 | `c3jsonl:202`；`v45:190`–`:191` |
| seat-codex-4 | 开场把「目标矩形搜索」列进要比较的六类做法；补「全厂运行通过仍须有短边 ≥6 的空矩形见证才可抬完整任务 L」；「建立保守 L 可用合格矩形见证，声称布局最大值时才需最大性检查」 | 无反对，codex-3 接受 | 采纳 | `seat-codex-4.log:2`、`:23`、`:26` |
| seat-opus-3 | 先要一个「完全不管空矩形、只求可行」的布局把 L 从 0 抬起来；不从 1113/21×53 倒推 | codex-2 指出前半句写松了：不管矩形的布局只是里程碑，不是 L | 前半句被收窄为「里程碑」，后半句（不倒推）全席采纳 | `seat-opus-3.log:17`；`seat-opus-3.md:76`、`:356` |
| seat-opus-3 | 排序第 3 步：密排装箱＋接头格模型，**先不要求大矩形，但要求密排** | 「第一版必须密排」被驳，收窄为启发式判断，共识稿明写「不写成『第一版必须密排』」 | 密排作为启发式偏好保留，不作硬要求 | `seat-opus-3.md:352`、`:75`；`v45:197` |
| seat-opus-4 | **不做「先钉 21×53 再装填」**（在自己的做法表里单列一行，「失败时给不给信息」栏写「不给信息」） | 无反对 | 保留为该席立场；全席口径落在「M6 不是入口、但保留为预算内的可证伪尝试」 | `seat-opus-4.md:173`、`:175`；`seat-opus-4.log:31`、`:56`；`v45:365`、`:379` |
| seat-opus-2 | 按 A 参数化，不从 1113 倒推；D5「面积阶梯：第一版就按密排做（这是启发式判断，不是证明）」 | 无反对 | 保留 | `seat-opus-2.md:3`、`:160` |
| seat-opus-1（撤回） | 「顶端余量只有 29 格……可行布局占地被钉在一条宽 29 格的窄带里」曾被当作「难处在把空格拢成一个矩形」的依据 | codex-4 先限定，后整句删除 | **「29 格窄带」的说法在 v45 里已删除**；能说的只剩「若要摸到 U 附近，占地必须几乎打到下界、且空格几乎全部落进同一个矩形」 | `v45:221`；`seat-opus-1.md:24`；残句仍留在 `seat-opus-2.md:188` |

---

## §4 正式约束里限制矩形位置的条目，跟构造法选矩形的关系：会上谁说过什么

**只记会上说过的。**

1. **会前主席在初始材料里点了名：**「面积预算、内带缺口、矩形离带、矿石走廊、核心离带、核心邻格、边带排布（左下两带恰 47 种排布）等几何条目见 求解约束.txt 几何节」。【核过】`会议目录/初始材料.md:36`（同文进了每个 codex 席的第 1 轮任务书，例如 `会议目录/seat-codex-1_task_turn1.md:58`）。

1b. **这些条文里有五条直接限制矩形的位置与贴边，四个 codex 席在第一轮就把它们逐字读进了会话（`cat 求解约束.txt` 的工具输出），原文见 §5 第 5 条。**【核过】它们分别给出：矩形不含左、下两边最外的两列两行（`边带排布`）；不含格 (2,2)（`角区`）；a∈{2,3} 时的高度上限与「贴上边界」分支，面积 >1005 时 a ≥4 且 b ≥4、两边都 ≤66（`矩形离带`）；a=4 时 H ≤21（`矿石走廊`）；矩形不能沿协议核心的取货边贴靠（`核心邻格`）。
   **但会上没有任何一席引用、讨论或据它们推过构造侧的矩形该放哪。**【核过】这五条条文名在席位自己写的文件里只出现于两个用途：codex-3 的 1089 推导（`内带缺口`＋`矩形离带`，压 U，见本节第 4 条）与 opus-4／codex-2 的矿石侧骨架（`边带排布`，见本节第 6 条）。

2. **会上唯一一处把压 U 侧的矩形位置枚举接到构造法上的，是 opus-1 写在 M5「漏掉」栏里的一句**【核过】：
   > 漏掉：对单个 R 的判定本身就是整题难度；1113 级位置数少（候选文件第 54 轮：每朝向 P=10/11/12 各 12/8/1 个位置），往下掉一级位置数迅速爆炸。
   `会议目录/seat-opus-1.md:88`。
   **它是用来说 M5 的难处的，不是用来定候选矩形集合的。**【转述】这一段的上下文（`:87`–`:89`）讲的是 M5 的覆盖、漏掉与代价，结论落在「与 M4 天然合体」。

3. **那些位置数的来源，会上各席是看到过的**【核过】：codex-3 在第一轮通读 `候选约束.txt` 时把进度段整段读进了会话，其中写着「1113（第 54 轮矩形周边缺口：贴最大空矩形一圈的格与贴基地边界同构，4(T+F)+2J ≥921+X+Y；1116 级 2580 个位置全排，只剩 21×53）。1113 级在第 54 轮的放宽模型下每朝向仍留 P=10/11/12 各 12/8/1 个位置（允许量 29/19/7），再降要新的计数或把 X、Y 两套与侧旁供电合并算」。
   出处：`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:23`（工具输出，`sed -n '1,220p' 候选约束.txt`）。
   **但会上没有任何一席对这段位置枚举做过进一步处理**【核过】：全库检索「边段」「侧旁」只命中这一处工具输出；「2580」「12/8/1」在席位自己写的文件里只出现于 `seat-opus-1.md:88` 那一句（且未写 2580）。

4. **矩形离带被会上实际用过一次，是在压 U 方向上**【核过】：codex-3 的 219 台 A ≤1089 候选，推导里用了「若 A>1005，正式矩形离带给两边均 ≤66；1090 含素因子 109，无法分解为两条均 ≤66 的整数边，故 A ≤1089」。opus-1、codex-1、codex-2 三轮复核未否证。
   出处：`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:215`、`会议目录/seat-codex-3_task_turn7.md:21`、`会议目录/seat-codex-1/candidate-review.md:28`、`共识草案-v45-5c9e556a.md:301`。
   **这条是压 U，没有任何一席把它转成构造侧的矩形选法。**【核过】该候选在共识稿里只出现在 D2 分支账本与 D5 候选清单里。

5. **会上就「固定某个矩形位置」的证据方向立了两条纪律，方向都是限制压 U，不是提供构造侧的选法**【核过】：
   - codex-1：「否定单个矩形位置不一定压全局 U」（`会议目录/seat-codex-1.log:45`）。
   - codex-3：「若模型固定了 217 台、无箱、某种拓扑或某个矩形位置，就只能排除这个分支，除非另证这些限制在目标档内无损」（`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`）。
   - 共识稿 C 节据此写死：「即便证出某个固定摆放不可行，也只否掉那个摆放，不直接压全局 U——要压全局 U 必须覆盖该档的全部合法摆放，一个摆放的不可行性永远不足以降低全局 U」（`共识草案-v45-5c9e556a.md:158`）。

6. **边带排布（左下两带恰 47 种排布）在会上被用过，但用途是矿石侧骨架，不是矩形位置**【核过】：opus-4 的做法表把它写成「骨架先行（矿线/边带）：左下两带被 边带排布 压到 47 种排布，近乎钉死」，codex-2 据此主张「植物回路走标准件线，矿石侧走骨架线，并行」。
   出处：`会议目录/seat-opus-4.md:168`、`会议目录/seat-codex-2_task_turn3.md:72`、`会议目录/seat-opus-4.log:31`。

7. **总结这一节**【转述】：正式约束里限制矩形位置的五条条文（`边带排布`、`角区`、`矩形离带`、`矿石走廊`、`核心邻格`）在会上被完整读进过会话，但席位只在压 U 一侧用过它们（codex-3 的 1089 推导用了 `内带缺口`＋`矩形离带`；C 节立了「固定某个矩形位置只排除该分支」的证据方向纪律），构造一侧只引用过一次位置计数、且是当成 M5 的难处写的。**没有任何一席提出过「把这些条目排除剩下的位置当成构造侧的候选矩形集合」，也没有任何一席讨论过这些条文对「第一张布局的矩形放哪」意味着什么。**

---

## §5 全部原文引用（按问题清单八条分组）

路径一律相对 `/home/zhuran24/文档/会议2全套/`。jsonl 引用的是 codex 侧原始日志里某一行 JSON 事件内的回复正文；同一段正文会在 `item_completed` 与 `response_item` 两类事件里各出现一次、并在后续 rollout 回放里再出现一次，这里给的是**首次出现的行号**。

### 第 1 条：M4 把矩形位置和尺寸做成变量

**`会议目录/seat-opus-1.md:77–82`**（提出人 seat-opus-1，终态立场文件）
```
### M4 松弛阶梯压 U（兼候选发生器）
R0 纯计数/面积（已给 1113）→ R1 装箱＋端口邻接（已知可跑）→ R2 粗粒度静态流 → R3 tick 级。**把最大空矩形的位置与尺寸作为 R1 的决策变量直接最大化 A**。

- 覆盖：R1 最优值是程序证明的上界，覆盖范围＝R1 这一层；同时它的解可以当候选摆放喂给 M3/M5。唯一已知跑得动的模型。
- 漏掉：任何动态；反向风险是写过紧给出假上界（见 §3）。
- 代价：低（有测点），审查代价中。
```

**`会议目录/seat-opus-1.log:10`**（第 1 轮开场广播，发往 opus-2 与 opus-4；同文见 `会议目录/msg1.txt:9`）
```
八法（覆盖/漏掉/代价见 seat-opus-1.md §4）：M1 非确定执行语义／M2 饱和纪律构造／M3 模块库＋组合条件／M4 松弛阶梯压 U（矩形位置尺寸做成 R1 的决策变量直接最大化 A）／M5 补域装填／M6 顶端直攻 21×53（当可证伪尝试，不是当答案结构）／M7 拓扑先行／M8 局部搜索／M0 格子级一把梭（30×30 UNKNOWN 且模型有漏洞，失败零产出，不该占先手）。
```

**`会议目录/seat-opus-1.md:202`**（认领）
```
- **M4 主评**（codex-1 复评）：把最大空矩形的位置与尺寸做成会议 1 那个 R1 装箱＋端口邻接模型的决策变量，最大化 A。按 §9 第 3 条，它同时是高档形态的候选发生器。
```

**`会议目录/seat-codex-1.log:45`**（复评认领，第 3 轮，发往 seat-opus-1；节录）
```
接受主评分解搜索族，认领你提出的 M4/M5/M6 异源复评，请邀请一位 opus 席复评我的分解族。建议你承接 M4 主评，我复评；M7 邀请 codex-2 主评。清单已对齐：M1 是共用验证能力，M4 是上界用途，M6 是 M5 的顶端档位，M7 补充网络先行，避免重复计分。复评要点：固定217台测点不能直接认证全局松弛；否定单个矩形位置不一定压全局U；静态装填成功不能直接抬L；顶端真实达标则可证最优。
```

**`会议目录/seat-codex-1.log:141`**（第 7 轮，确认分工）
```
确认M4你主评、我复评。四分支粗界1142/1133/1117/1108及h上界8.833633/1.550793均正确。请将“占地无上界”改为“没有普遍4000上界”：基地和合格矩形仍给占地≤4900−A≤4864。有效粗界可用于超过界的剪枝，并非只能排序。不同上界之差不是实际可证明性代价。平均跳数不证明两种布局无法由局部或大邻域操作连通，故不同意据此淘汰M8；支持M5回退，同时保留M8作内层修复。存货无分级仍未获必要性证明，不能进入M4硬剪枝。
```

**测点（共识稿 v45 E 表 M4 行）`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:363`**
```
| **M4 松弛阶梯压 U ＋候选发生器** | 压 U ＋发生器 | 对偶界 | 静态几何＋端口邻接 | 低 | 278 秒 OPTIMAL 那个测点**不带矩形变量、不带流**；带上之后**无测点** | 每个台数分支各跑一次 | 加约束后 UNKNOWN **只说明该编码这次没解出来，不等于放弃整条路线** |
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:197`**（D1 推论 2～4，节录）
```
M4/R1 在高面积档**可能**比原先估计的更有用（但「装箱＋端口邻接几乎就是全部问题」已撤回，且 M4 的低成本主张**无测点**——278 秒那个测点不带矩形变量、不带流）
```

**`会议目录/纪要.md:22`**（主席纪要 E 表 M4 行，标【转述】）
```
| M4 松弛阶梯压 U 兼候选发生器（装箱＋端口邻接的 CP-SAT，把矩形位置尺寸做成变量） | 压 U＋发生器 | 对偶界 | 静态几何＋端口邻接 | 低 | 会议 1 那个 278 秒 OPTIMAL 测点不带矩形变量、不带流；带上之后无测点 | 每个台数分支各跑一次 | 加约束后 UNKNOWN 只说明该编码这次没解出来 |
```

---

### 第 2 条：M5 补域装填「先选矩形再装余下」

**`会议目录/seat-opus-1.md:84–89`**（原始提法）
```
### M5 补域装填（矩形优先）
先选空矩形 R（位置＋尺寸），再问 70×70\R 能否装下全部单位并连通；按面积降序扫。

- 覆盖：直接对着目标函数；每个被**证明**否掉的 R 压 U，每个被解且认证的 R 给 L=area(R)；天然 anytime。
- 漏掉：对单个 R 的判定本身就是整题难度；1113 级位置数少（候选文件第 54 轮：每朝向 P=10/11/12 各 12/8/1 个位置），往下掉一级位置数迅速爆炸。
- 代价：中。与 M4 天然合体——R1 模型里带 R 变量就等于一次扫完所有 R。
```

**`会议目录/seat-codex-3.log:48`**（codex-3 第 3 轮，把问题正式抛给 M5 主评方）
```
本席接受opus-1的口径合并分工，建议你继续主评分解搜索。第三轮已给七项判据和M0—M8比较；请重点审查覆盖与失败反馈范围，及M5补域装填如何允许改变矩形和设备布局。见meeting2/seat-codex-3.md。
```

**`会议目录/seat-codex-1_task_turn5.md:37`**（转发层写给 codex-1 的任务书；**这是转发层的措辞，不是席位发言**）
```
1. 消息10（codex-3）：它已接受口径合并、继续按你主评分解搜索族，并请你重点审查覆盖/失败反馈范围、以及M5补域装填如何允许改变矩形和设备布局。这需要你正面回应M5的这个技术问题（补域装填是先定矩形再装补域，如果补域装不满/冲突，是否允许回头改矩形位置/尺寸或已摆机器位置，还是纯粹的「失败→换下一个矩形候选」单向流程），这直接关系到M5作为anytime算法的搜索空间定义，请给出你的判断或说明这是需要M5主评（opus-1）自己决定、你只能提供审查意见。
```

**`会议目录/seat-codex-1.log:75`**（codex-1 第 5 轮的正面回答；同文另发 seat-opus-1，见 `:79`）
```
已审阅七项判据，支持口径合并。M5建议采用可回退分解：固定矩形内允许搬机、旋转、改拓扑与重布线；必要时回退矩形位置/尺寸和设备数量。失败证据随原固定条件保存，换矩形不继承无依据的设备冻结。启发式失败或超时只保留未决；完整覆盖证明才能关闭对应域。保存最佳认证布局可支持有解后随时返回，L=0时不承诺任意时限都有解。完整方案已写入文件。
```

**`会议目录/seat-codex-3.log:111`**（codex-3 接受）
```
接受你M5可回退方案并已并入。跳数界只限制流量加权平均，不能排除所有长干线，也不能证明h约5到低h结构无法通过改造实现。需区分小移动、大邻域和重做拓扑，不据该界降级全部分解与局部改造方法。见meeting2/seat-codex-3.md。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:202`**（codex-3 立场文件该版正文，节录三行）
```
| M5可回退分解 | 固定矩形内允许搬机、旋转、改拓扑、重布线，必要时回退矩形和台数 | 失败仅随原条件保存，不能跨分支继承 |
接受seat-codex-1的M5方案。它不等于冻结机器后一次性接线，也不要求第一版采用松散长干线。跳数预算强化其运输成本检查，不构成否定分解法的理由。
A=1113时1.50是错误收紧，不能使用。A=0只是方便讨论容量的放宽点，不是符合短边至少6要求的目标矩形。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:215`**（codex-3 后一版方法表）
```
| M5可回退空间分解 | 搬机、旋转、改拓扑、重布线，并可回退矩形与台数 | 失败范围不能超出原固定条件 | 构造主干之一 |
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`**（codex-3 第 3 轮方法表与读法说明）
```
以下M编号对应seat-opus-1本轮提出的名称；对M5暂按“保留目标矩形、在其补域内安排设备与物流”理解，待主评确认。
| M5 目标矩形补域装填 | 直接围绕任务几何目标组织空间搜索 | 固定形状、位置或先摆后连可能排除可行结构 | 易接现有空间工具；允许改变矩形和反馈重排 |
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:157`**（同席后一版）
```
| M5 目标矩形补域装填 | 直接组织任务空间目标 | 固定形状、位置或摆放可能使后续无法布线 | 与拓扑及运行反馈联动 |
```

**`席位转录/codex原始日志/seat-codex-2-codex侧原始日志.jsonl:109`**（codex-2 方法关系表第一版）
```
| M5/M6 矩形补域/顶端 | 直接对齐目标；部分位置失败不能排除整档 | M6是M5的顶端任务；不独押1113，保留低档首个构造 |
```

**`席位转录/codex原始日志/seat-codex-2-codex侧原始日志.jsonl:153`**（第二版）
```
| M5/M6矩形补域/顶端 | 对齐目标；只覆盖部分位置或模板不排除整档 | 保留合格矩形，首个构造不独押1113 |
```

**`席位转录/codex原始日志/seat-codex-2-codex侧原始日志.jsonl:211`**（第三版，拆成两行）
```
| 可回退分解 | 连接平均网络、静态候选与认证；反馈仅限覆盖域 | 不冻结摆放、图或模块 | codex-1主评，接口反馈需审查 | 可改数量与矩形；单次失败不禁整个方法 |
| 矩形补域/顶端 | 空间候选或覆盖完整的排除证据 | 部分位置失败不排整档 | 外层策略可用多种内层算法 | 顶端是特定任务，不独押1113；保留合格低档 |
```

**`会议目录/seat-codex-1.md:28`**（codex-1 终态方法表该行）
```
| 矩形补域 | 外层直接对齐面积及短边目标 | 内层可改机器与拓扑，不限定顶端下扫；单位置失败不排整档 |
```

**`会议目录/seat-codex-1.md:33`**（codex-1 终态，失败记录与回退）
```
M5保存矩形/台数范围及未决、已否域、静态候选、已认证布局。允许搬机、换图、重布线、放松模块边界；改变条件后旧失败不自动沿用。有限搜索失败或超时不排候选域，不排方法族。已有认证布局始终保留；L=0时不承诺任意时限都有解。
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:364`**（E 表 M5 行）
```
| **M5 补域装填（可回退分解）** | 两向 | 未决域记录 | 静态几何 | 中 | 中 | 可回退，不冻结设备数 | 单矩形长期 UNKNOWN |
```

**`会议目录/纪要.md:23`**（主席纪要 E 表 M5 行，标【转述】）
```
| M5 补域装填（先选矩形、再问余下能否装下并连通，可回退分解） | 两向 | 未决域记录 | 静态几何 | 中 | 中 | 可回退，不冻结设备数 | 单矩形长期 UNKNOWN |
```

---

### 第 3 条：M6 顶端 21×53 直攻，与「M6 是 M5 的顶端档位」

**`会议目录/seat-opus-1.md:91–96`**
```
### M6 顶端直攻（A=1113、21×53）
不是把顶端钉死的结构当答案结构来推方法（owner 已指出那是错的），而是当一次**可证伪的构造尝试**。

- 覆盖：成功即最优；失败若是不可行性证明就压 U。1113 级结构钉得很死（九机型恰下限、无协议储存箱、P ≤12、只能 21×53），搜索空间小。
- 漏掉：**超时零产出**——必须组织成「证明或反驳」，不能组织成「搜一搜看」。
- 代价：中。
```

**`会议目录/seat-codex-1.log:49`**（codex-1 → codex-2，第 3 轮）
```
我接受分解搜索族主评，与你汇总全表互补；codex-4负责接口拼接及失败范围。邀请你主评M7拓扑先行，并由opus席复评：这补足先网络后嵌入与先摆放后布线的对照，须比较保留多拓扑和回退成本，不能只嵌入一个最少运输图。M1/M4应分别按验证设施、上界用途记账，M6作为M5顶端档位，避免重复评分。请确认是否接受M7。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`**（codex-3 M6 行）
```
| M6 顶端21×53直攻 | 检验当前上界级，成功收益很高 | 一次候选失败不否定整个1113档；整档排除也不能给出达标布局 | 可作有限对照试验，不作为唯一主线 |
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:215`**（后一版）
```
| M6顶端21×53直攻 | 检验当前上界级 | 候选失败不等于整档失败 | 有限对照，不作唯一主线 |
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:365`**（E 表 M6 行）
```
| **M6 顶端直攻 21×53** | 两向 | 做成证明才压 U；搜索未成证明时仍留候选、反例与测点 | 视编码 | 中 | 中 | 钉死 A=1113 | **没有「立即停」这种转向条件**；转向条件是投入到约定额度仍既没做成不可行性证明、也没产出可用候选或反例时收手 |
```

**`会议目录/seat-opus-4.md:173`**（opus-4 做法表里把它单列成一行，投反对）
```
| 先钉 21×53 再装填 | 直接对目标 | owner 已点名的错误：顶级被钉死的结构不是答案的结构 | 中 | — | 不给信息 |
```

---

### 第 4 条：搜索顺序（不限定从顶端往下扫／矩形先行不违反 owner 原则／不把 1113 的必要条件当入口）

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:379`**（F 节第 6 条，终稿）
```
6. 之后：M5 可回退分解（codex-1 主评，**不限定必须从顶端往下扫**）、M3 拼装、M6 在**约定的预算内做搜索或证明**（与 E 表一致：没有「只能做证明」这种限制，也没有「立即停」这种转向条件；额度耗尽仍既没做成不可行性证明、也没产出可用候选或反例时收手）；**M8 不淘汰，保留作 M5 的内层修复**。
```

**`会议目录/seat-codex-1/consensus-unreviewed-de4c01afba56.md:185`**（v13 一档的同一条，带着后来被编掉的半句）
```
6. 之后：M5 可回退分解（codex-1 主评）——**不限定必须从顶端往下扫**（codex-1 指出，接受），矩形的选取顺序由主评自定；M3 拼装，M6 只在能组织成证明时做。**M8 不淘汰，保留作 M5 的内层修复**（codex-1、codex-4 的保留已采纳：平均跳数界不证明两种形态无法由局部或大邻域操作连通）。
```

**`会议目录/seat-codex-1.log:70`**（codex-1 → opus-4，第 4 轮；节录）
```
矩形先行本身不违反owner原则，只有把顶端条件当所有构造入口才违反。建议逻辑图允许几何与运行反馈修改，不预先定死；无桥无箱可作比较分支，不排除其他分支。
```

**`会议目录/seat-codex-1.log:2`**（codex-1 开场广播，同文发往七席，见 `:2,5,8,11,14,17,20`；节录）
```
初步倾向可分析局部结构结合允许放松模块边界的分解搜索；保留联合模型、模块组合、局部修复和矩形分层搜索作为比较路线，不把 1113 的必要条件当作全局搜索入口。
```

**`会议目录/seat-codex-1.md:20`**（codex-1 终态「方法选择」）
```
优先以少量可分析局部结构和多种逻辑拓扑生成候选，尽早接续全场几何，允许反馈修改台数、位置、端口和线路。首个真实达标布局优先，但必须满足完整产率、零干预和短边至少6的空矩形条件。保留增机、有箱、混做、共享运输等合法结构域；不把1113档必要条件变成全部搜索入口。
```

**`会议目录/seat-opus-3.log:17`**（opus-3 开场，同文发往七席）
```
先做哪个：我主张先要一个「完全不管空矩形、只求可行」的布局，把构造下界从 0 抬起来；不要从 1113 / 21×53 倒推（owner 已指出顶级被钉死的结构不是答案的结构，且 1113 很可能不可达，失败还不给信息）。
```

**`会议目录/seat-opus-4.log:31`**（opus-4 开场 → opus-1）
```
我的先后：先把逻辑层（配置表＋每台通道数＋每条通道件/tick）做完，它是所有路线的共同输入且基本手算；同时定 L1/L2/L3 的边界与上面那处冲突；然后骨架(47 种边带排布)与标准件库并行；整场单体求解只在局部小块用；不做「先钉 21×53 再装填」。
```

**`会议目录/seat-opus-4.log:56`**（opus-4 → opus-3）
```
同意你「先求可行、不管矩形、不从 1113 倒推」。
```

**`会议目录/seat-opus-2.md:3`**（opus-2 终态定位，节录）
```
我做的是：在放第一个格子之前把「流网络与几何之间的换算率」算清楚，并**按 A 参数化**——不从 1113 倒推。
```

**`席位转录/seat-codex-4-全程转录.md:148`**（codex-4 早期立场文件正文）
```
当前没有真实达标布局，1113 只是空矩形面积上界。不能把面积 1113 才成立的九机型恰下限、无箱等必要条件变成所有构造的入口条件。第一份达标布局即使面积较小，也能提供真实下界和后续改造起点。
```

**`会议目录/纪要.md:46`**（主席纪要，标【转述】）
```
为什么不从 21×53 倒推【转述】：opus-3、opus-4、codex-1、codex-2 开场都反对以 1113 的必要条件作全局入口；最终 M6 保留为「约定预算内的一次可证伪尝试」，不是入口。
```

**`会议目录/seat-opus-1.md:125`**（opus-1 原稿里被换掉的那条顺序）
```
之后：M2/M3 出第一个 L>0 → M8 改进 → M5/M6 从顶端往下压，两端相向而行。
```

---

### 第 5 条：矩形位置的支配或交换论证

**席位做出的支配／交换论证：一条都没有。**【核过】

**但正式约束里限制矩形位置的条文被完整读进了会议记录**，出处 `席位转录/codex原始日志/seat-codex-1-codex侧原始日志.jsonl:23`（`item.type=CommandExecution`，命令 `/usr/bin/bash -lc "cat 求解约束.txt"` 的 stdout；同一份 stdout 另见 `seat-codex-2-codex侧原始日志.jsonl:23`、`seat-codex-3-codex侧原始日志.jsonl:21`、`seat-codex-4-codex侧原始日志.jsonl:20`）。逐字节录五条：

```
边带排布：左、下两边各 23 个仓库取货口恰留 1 个空格，空格只能在从角起第 3k+1 格（k=0…23），且两边的空格至少有一个在角格；46 个取货端口正对的格子都是运输单位，沿边相邻两个相距 3 格（跨空格处 4 格）；因此空矩形不含左、下两边最外的两列与两行
    据：取货口配置、仓库取货口、仓库取货口位置、基地、端口对接、目标
```
```
角区：以左下角格为 (0,0)，短边 ≥6 的空矩形不含格 (2,2)；46 个矿石格（仓库取货口取货端口正对的运输单位格）与非运输单位形成的通道数合计至多 93
    据：边带排布、取货口配置、仓库取货口、端口对接、满速独占、桥接器、汇流器、物品准入口、接通先后、目标
```
```
矩形离带：以左下角格为 (0,0)，设短边 ≥6 的空矩形占列 a…a+W−1、行 b…b+H−1。a∈{2,3} 时，记左带取货端口落在行 b…b+H−1 内的个数为 m，矩形贴上边界时 e=1、否则 e=2，则 m ≤ e(a−1)；故 a=2 时 H ≤9 且不贴上边界，a=3 时 H ≤15、贴上边界时 H ≤8；对下带与 b 同理；面积大于 1005 的空矩形有 a ≥4 且 b ≥4，两边都 ≤66
    据：基地、目标、边带排布、角区、取货口配置、仓库取货口、仓库取货口位置、端口对接、端口速率、运输单位、桥接器、供电桩、小制造单位、协议储存箱、循环态
```
```
矿石走廊：矩形离带 的记号下，a=4 时也有 m ≤6，故 H ≤21
    据：矩形离带、边带排布、角区、取货口配置、仓库取货口、端口对接、端口速率、桥接器、物品准入口、准入累计、小制造单位、协议储存箱、配方、制造、矿系不入库、循环态、空矩形上界、基地、目标
```
```
核心邻格：协议核心两条取货边上偏移 1、4、7 的 6 个端口对面的格都是运输单位，成品有直接进核心的存货通道时存货边外至少再有 1 个，故核心邻格中的运输单位 ≥6（成品全走储存箱传输）或 ≥7；短边 ≥6 的空矩形与核心的取货边至多只在该边端头 1 格相邻，不能沿取货边贴靠；核心不能占 x0 ≤3 且 y0 ≤3 的位置；核心贴带的那条边外侧，只在 x0 ≤7（下带 y0 ≤7）或该边是取货边时不能作空矩形的一边
    据：协议核心、端口对接、取货口配置、端口速率、入库途径、矿系不入库、边带排布、核心离带、基地、目标
```

**这五条在会上没有被任何一席引用或讨论过。**【核过】它们的结论句（例如「空矩形不含左、下两边最外的两列与两行」「短边 ≥6 的空矩形不含格 (2,2)」「面积大于 1005 的空矩形有 a ≥4 且 b ≥4」「矩形贴上边界时 e=1」）在全库的每一处命中都落在 `CommandExecution` 事件里，席位自己写的 `.md`、`.log`、任务书、共识稿各版里一处都没有。

会上最接近「支配」「对称」两个词的三句，都不是关于矩形位置的：

**`席位转录/codex原始日志/seat-codex-4-codex侧原始日志.jsonl:230`**
```
可以将密排、接头格模型作为优先试验的启发式，不可作为已证支配关系。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:131`**
```
因此本席把“满速专线加少量复杂模块”列为优先候选家族，不接受其无条件支配其他构造法，也不接受“非满速难点只有列出的几类”的穷尽判断。
```

**`席位转录/codex原始日志/seat-codex-4-codex侧原始日志.jsonl:29`**（「对称性」在会上的唯一含义：求解器里的对称破缺成本）
```
| 全场联合约束求解 | 在编码范围内同时决定机器、位置、端口、运输、供电和空矩形，有机会发现人工未想到的紧密结构 | 静态流量与几何编码不自动覆盖运行行为；固定台数、周期或拓扑均会收窄覆盖范围 | 变量规模、对称性与动态编码成本高；优先用于局部精确问题及必要条件模型，暂不独押全场 |
```

「矩形贴基地边界」这个措辞在会上只出现在各席通读正式条文的工具输出里，例如 **`席位转录/seat-opus-2-全程转录.md:164`**（内带缺口条文节录）：
```
（不含四个对角格；矩形贴基地边界的一侧没有这圈格）中不属于制造单位、协议核心、供电桩的格数，则没有协议储存箱时还有 4(T+F)+2J ≥921+X+Y（J 见 供电下限）
```
**没有任何一席据这句往下推过矩形该贴哪条边。**【核过】

「左、下边界放满仓库取货口」在会上被引用过，用途是数占地与推成品出口，不是推矩形位置：
**`会议目录/seat-opus-2.md:15`**
```
任何布局占地 ≥ 机器格 + 81（协议核心 9×9）+ 138（46 个仓库取货口，取货口配置 要求左下边界放满）+ 4P（供电下限 P ≥10）+ T + 9B。基地 4900，A ≤ 全部空格数，故
```
**`会议目录/seat-codex-4_task_turn20.md:122`**（节录）
```
正确的一步是引 **取货口配置**（已采纳）：「左、下边界放满仓库取货口，它们与协议核心的全部取货端口只设为源矿或蓝铁矿，且循环态中全部满速」⇒ 对任何满足目标的布局，52 个取货端口一件成品也不放行 ⇒ 循环态中 **入库 ＝ 玩家拿取**。
```

---

### 第 6 条：压 U 侧的矩形位置枚举，跟构造法选矩形的关系

**`会议目录/seat-opus-1.md:88`**（会上唯一一次把两者接起来）
```
- 漏掉：对单个 R 的判定本身就是整题难度；1113 级位置数少（候选文件第 54 轮：每朝向 P=10/11/12 各 12/8/1 个位置），往下掉一级位置数迅速爆炸。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:23`**（工具输出：codex-3 通读 `候选约束.txt` 的进度段，节录）
```
当天的面积阶梯：1149→1148（整数边长）→1140（第 39 轮存货边流量加权，T ≥207）→1134（第 44 轮外边界缺口，边段 DP）→1131（第 48 轮内带缺口，4(T+F)+2P ≥918/921）→1116（第 50 轮：一般分支界 4A+14P ≤4639/4608、占边桩 9J ≤23P−217、内外带 4(T+F)+2J ≥921+X）→1113（第 54 轮矩形周边缺口：贴最大空矩形一圈的格与贴基地边界同构，4(T+F)+2J ≥921+X+Y；1116 级 2580 个位置全排，只剩 21×53）。1113 级在第 54 轮的放宽模型下每朝向仍留 P=10/11/12 各 12/8/1 个位置（允许量 29/19/7），再降要新的计数或把 X、Y 两套与侧旁供电合并算。
```

**`会议目录/初始材料.md:36`**（主席会前点名的几何条目，节录）
```
面积预算、内带缺口、矩形离带、矿石走廊、核心离带、核心邻格、边带排布（左下两带恰 47 种排布）等几何条目见 求解约束.txt 几何节。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:215`**（矩形离带在会上的实际用法：压 U 的 1089）
```
**条文：** 粉碎69台、采种17台，其余七机型恰为机型下限且没有协议储存箱时，最大空矩形面积至多1089。
**据：** 基地、各单位尺寸、机型下限、内带缺口、供电下限、矩形离带、目标。
由P≥10，A≤1090.75，整数面积A≤1090。若A>1005，正式矩形离带给两边均≤66。1090含素因子109，无法分解为两条均≤66的整数边，故A≤1089；A≤1005时结论自然成立。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`**（固定矩形位置的证据方向）
```
模型无解才可排除该域。若模型固定了217台、无箱、某种拓扑或某个矩形位置，就只能排除这个分支，除非另证这些限制在目标档内无损。
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:158`**（升成 C 节纪律）
```
- **即便证出某个固定摆放不可行，也只否掉那个摆放，不直接压全局 U**——要压全局 U 必须覆盖该档的全部合法摆放，一个摆放的不可行性**永远不足以**降低全局 U。
```

---

### 第 7 条：明确说「这件事没定」「留待开工时定」

**`会议目录/seat-codex-1/consensus-unreviewed-de4c01afba56.md:185`**（节录）
```
矩形的选取顺序由主评自定
```
同句在会议记录里的全部落点（`grep -rn` 穷举）：`会议目录/seat-codex-1/consensus-unreviewed-de4c01afba56.md:185`、`会议目录/seat-codex-1/consensus-reviewed-2b6451c042dd.md:200`、`会议目录/seat-codex-1/consensus-reviewed-b3bf654033c8.md:204`、`会议目录/seat-codex-1/consensus-reviewed-5ba21fb6d4b7.md:176`、`会议目录/seat-codex-1/consensus-reviewed-b11e327579ca.md:204` 与 `:341`、`会议目录/seat-codex-2/consensus-reviewed-round8-v13.md:200`、`会议目录/seat-codex-2/consensus-reviewed-round9-v19.md:204`、`会议目录/seat-codex-2/consensus-reviewed-round10-v24.md:216` 与 `:359`、`席位转录/seat-opus-1-全程转录.md:1182`（起草人 opus-1 写这一版时的转录），以及四份 codex 侧原始日志里各席读它的工具输出。
**编掉它的那一版**：`会议目录/seat-codex-1/consensus-editorial-base-fff4688b220b.md:197`
```
6. 之后：M5 可回退分解（codex-1 主评，**不限定必须从顶端往下扫**）、M3 拼装、M6 只在能组织成证明时做；**M8 不淘汰，保留作 M5 的内层修复**。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`**
```
以下M编号对应seat-opus-1本轮提出的名称；对M5暂按“保留目标矩形、在其补域内安排设备与物流”理解，待主评确认。
```

**`会议目录/seat-codex-1_task_turn5.md:37`**（转发层留的出口；节录）
```
请给出你的判断或说明这是需要M5主评（opus-1）自己决定、你只能提供审查意见。
```

**`会议目录/纪要.md:119`**（主席，标【自己推】；节录）
```
席位在会上认领的分工（opus-2 与 codex-2 做 M7、opus-3 做砖尺度执行器、opus-4 承接起动与逻辑层、opus-1 与 codex-1 做 M4、codex-1 做 M5）是子代理的会中分工，会后席位已退出，开工时要重派。
```

**`会议目录/纪要.md:122`**（会后工作清单末句，标【转述】；节录）
```
315 条通道能否嵌进 70×70 完全没碰。
```

**`会议目录/seat-opus-4.md:220`**（opus-4 终态）
```
1. **逻辑层自洽不蕴含几何可行。** 315 条专用直连能否在 70×70 里无冲突走通，一格都没碰。这是下一步唯一该做的事。
```

---

### 第 8 条：第一张布局（L 从 0 起）的矩形该怎么定

**`会议目录/seat-opus-3.log:17`**（最早的提法，见第 4 条引文）—— 后被收窄。

**`会议目录/seat-opus-3.md:76`**（提出人自己接受的收窄）
```
- **补记**（seat-codex-2 指出，我上一版漏写）：「不管空矩形的可行布局」只是里程碑，**不是构造下界 L**；真实的 L 必须带一个短边 ≥6 的空矩形。
```

**`会议目录/seat-codex-3.log:37`**（codex-3 先提）
```
与你的证据边界立场一致。本席补充：不优化空矩形可作运行试点，但没有短边至少6的空矩形时不能直接提高完整任务的构造下界；满速必要条件也不能循环用于证明整厂达标。第二轮全文见meeting2/seat-codex-3.md。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:144`**（同席立场文件正文）
```
初期可以暂不优化空矩形面积；但若布局没有短边至少6格的空矩形，就只能作为运行试验产物，不能直接建立符合全部任务条件的构造下界。
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:157`**（同席后一版）
```
初期可以不优化矩形面积，但完整任务的构造证书仍须具有短边至少6格的空矩形。
局部占地较小不等于整厂最大空矩形较大。最终空间收益须在整厂评价，面积相等时仍比较短边。
```

**`会议目录/seat-codex-4.log:23`**（codex-4）
```
确认分工无冲突。本席补充了可引用证据格式：对象/状态、条件→保证、依据与覆盖、拼接义务、失败范围、实测或未知成本。通用判读表不是具体认证；未证明的接口仍记待证。codex-2 审查具体组合，codex-1 可据具体证据条目比较认证成本及合法反馈。另补：全厂运行通过仍须有短边≥6的空矩形见证才可抬完整任务 L。新版见 `meeting2/seat-codex-4.md`。
```

**`会议目录/seat-codex-4.log:26`**（会上关于第一张布局最接近「做法」的一句）
```
接受空矩形补充，已加入结果表：运行试点通过不自动抬完整任务 L，须另核对短边≥6的空矩形见证；面积相等时短边长者优另行比较。建立保守 L 可用合格矩形见证，声称布局最大值时才需最大性检查。满速必要条件不能循环证明达标已合并保留。见 `meeting2/seat-codex-4.md`。
```

**`会议目录/seat-codex-2.log:44`、`:117`**（codex-2 两次同口径；节录）
```
赞成先求达标，但真实下界仍须有短边至少 6 的空矩形。
```
```
矿石0余量不等于没有物理缓冲；首个完整下界仍需短边至少6的空矩形。
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:190–191`**（D1 面积档表的两行，会上唯一成文的「面积档」阶梯）
```
| 36（短边 6 的最小合格矩形） | 1314 | 8.60 | 4.30 |
| 0（完全不要矩形，只求可行） | 1350 | **305.65·h ≤ 2700**（≈8.8336，**剪枝一律用精确式，不用截断小数**） | 305.65·h ≤ 1350 |
```

**`席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl:202`**（A=0 不是目标档）
```
A=1113时1.50是错误收紧，不能使用。A=0只是方便讨论容量的放宽点，不是符合短边至少6要求的目标矩形。
```

**`会议目录/seat-opus-3.md:352–353`**（opus-3 的排序第 3、4 步）
```
3. **密排装箱＋接头格模型**，先不要求大矩形，但要求密排。
4. **第一个完整候选布局**（含短边 ≥6 的空矩形）→ 构造下界从 0 抬起。
```

**`会议目录/seat-opus-2.md:160`**（opus-2 的「面积阶梯」一行）
```
- **D5 面积阶梯**：第一版就按密排做（**这是启发式判断，不是证明**）。
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:197`**（把「第一版密排」限定成启发式的终稿措辞；节录）
```
「先求松散可行再局部紧凑化」的期望收益**可能**低于一开始就往密排做（**不写成「第一版必须密排」**）。
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:385`**（交付物定义，含矩形；节录）
```
> 交付物 = 布局（全部单位的位置与朝向）＋ 全部可设定项（物品准入口限量、核心取货端口的物品指派、储存箱与传输开关）＋ **在 操作精度 下可执行的调试期办法及其可能后置状态** ＋ 覆盖四个不得依赖量与**所有合法可达循环态**的达标论证 ＋ 最大空矩形的面积与短边。
```

**`会议目录/seat-opus-1/共识草案-v45-5c9e556a.md:221`**（原「29 格窄带」说法的终稿状态；节录）
```
**「29 格窄带」的说法删除**——它不是对一般可行布局的描述（一般可行布局的占地可高到 4900−A）；能说的只是：**若要摸到 U 附近，占地必须几乎打到下界、且空格几乎全部落进同一个矩形**。由此的定性判断（不是定理）：难处更可能在「剩下的空格能不能拢成一个矩形」，而 L=0 说明我们连这个几何问题的一个可行点都还没有。
```

---

## 路径简写对照

| 本文简写 | 绝对路径 |
|---|---|
| `会议目录/…` | `/home/zhuran24/文档/会议2全套/会议目录/…` |
| `席位转录/…` | `/home/zhuran24/文档/会议2全套/席位转录/…` |
| `共识草案-v45-5c9e556a.md`、`v45:N` | `/home/zhuran24/文档/会议2全套/会议目录/seat-opus-1/共识草案-v45-5c9e556a.md` 第 N 行 |
| `c3jsonl:N` | `/home/zhuran24/文档/会议2全套/席位转录/codex原始日志/seat-codex-3-codex侧原始日志.jsonl` 第 N 行 |
| `c2jsonl:N` | `/home/zhuran24/文档/会议2全套/席位转录/codex原始日志/seat-codex-2-codex侧原始日志.jsonl` 第 N 行 |
| `c4jsonl:N` | `/home/zhuran24/文档/会议2全套/席位转录/codex原始日志/seat-codex-4-codex侧原始日志.jsonl` 第 N 行 |
