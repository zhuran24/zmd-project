# 契约修订与校验器第二轮否证记录（第 2 席）

性质：判读存档。日期：2026-09-19。对象：本轮给定的 20 条发现及读取时的文件版本。结论：1 条已否证，19 条未能否证。未能否证不表示接受发现的全部措辞、严重度或假设反例的可实现性；以下逐项限定成立范围。

## 来源与方法

已完整读取工作流目录 `/tmp/claude-1000/-home-zhuran24-zmd-research-fresh/393193c2-617f-4fbe-a1b0-a5c39943863c/scratchpad/step1/` 的 `任务书.md`、`任务书2.md`。规则依据为仓库根三份正式文件；完整性批评、契约说明、修订记录和转换说明只用于核对交付范围及既有声明，不提升为游戏规则。

下文路径均相对于 `/home/zhuran24/zmd-research-fresh`。代码行号对应读取版本；同时给出字段或检查项名称以便定位。回读了 `求解器/crates/topology/src/lib.rs` 的相关判据、`tests/validation.rs` 的测试、转换脚本及单位目录、候选 JSON。另用 Python 标准库只读解析 JSON，以 Fraction 独立核对植物机器面积、来源映射和扇出数值。没有执行转换器、修改草稿或正式文件；没有用第三方模拟器判断规则。未重跑 cargo；本次结论不依赖宣称测试全绿。

## C2-r2-L1-1

结论：refuted=false，未能否证。

原文据：第一轮任务书 §3.B.1 指定 38 台多料机三栏；正式约束“满载配置”包含粉碎机、采种机混做，“混做清空”以逐 tick 完成一批且产物不止一种为前件，并非以配方输入种类数为前件。`lib.rs:591、646–658` 确实按 `rec.inputs.len()>1` 决定三栏必须存在，否则必须为空。`送料契约.md:51` 允许同机多配方，:66 则把三栏限制在多料配方机。

尝试否证：首轮任务书只明确要求这 38 台三栏；`lib.rs:1097` 的全称目标说明也明确不免除单料机或环外机的活性义务。因此不能说当前代码已经宣称单料机无需运行认证。但这些文字没有给混做或同物品多输入机器提供逐机登记接口，也没有登记这一接口限制。候选 B 的设计限制不能替代格式限制；缺口仍在。不能从“混做清空”单独推出所有机器必须采用同名三栏，只能确认发现指出的表达及登记缺口。

## C2-r2-L1-2

结论：refuted=false，未能否证。

原文据：规则“物品准入口”明写累计上限 1–5000、每 5 tick 上限 1–5，并规定第一件起算、窗口走完由下一件重起；“传输”明写 5 tick 冷却。第二轮任务书 §3.C.3 要表示准入口设定和储存箱传输开关。目录 `units[id=物品准入口]` 无这些设定域、窗口字段，`units[id=协议储存箱]` 只有 `powered_functions:["transfer"]` 而无冷却数值。

尝试否证：B.1 的逐项清单主要是尺寸、端口、库存和需电，未单列设定域；但 `送料契约.md:153–159` 又把该目录作为共享单位属性输入，并表示不再另建单位常量表。约束全文里的 k/5 和 basis 的规则名不能代替缺失的 1–5000 及窗口起算定义。既没有目录字段也没有缺口登记，无法否证。

## C2-r2-L1-3

结论：refuted=false，未能否证。

原文据：规则“物品格”同时规定逐格单物品和单位内同种物品跨格唯一，例外为协议储存箱、缓存格；“存货/取货物品格”明写存货物品格不限制物品种类。目录只给逐格 `item_policy`，未见跨格唯一性字段。协议核心的无限格数、每格 80000 不能独自编码每种只有一格。大机输入的 `item_policy=unresolved` 和 note 只记混装未定，没有保留“不限制物品种类”的已知限制。

限定：桥接器“两轴格独立”直接可回源到规则“每对独立拥有一个物品格”，并非该短语自身必然违反跨格唯一性。独立有格与共享一种物品的跨格禁令可以同时成立。因此发现把按语判作反方向稍强，但未编码跨格条款且未说明两轴独立的边界这一核心缺口仍无法否证。不能用某一子句措辞过强否掉整条。

## C2-r2-L1-4

结论：refuted=false，未能否证。

原文据：正式约束“出库上限”“研磨进料”“封装进料”“单位矿耗”“物料流量”“运输端口收支”“箱体接口”有对应数字；`lib.rs` 在运行判据中直接写 23、52、31、5、20/30/40、19 项流量以及 312/307/624 等。目录虽有约束文本，运行判据未由文本或结构化阈值生成。`legacy_machine_constants_equal_catalog` 仅比机型，目录属性测试仅比单位，覆盖行测试仅验证每个条目存在报告行。转换器的全文相等断言不会检查 Rust 字面量。

限定：`送料契约.md:155` 的前半句明确说“从目录取机型与核心容量”，可以把“旧硬编码”狭义理解为此次迁移的机型常量；B.1 也未逐字要求自动解析全部条文公式。因此不将本发现解释为所有数字已经算错，或断言任意正式改动都必定全绿。但正式阈值存在第二份运行源、没有同步断言，且“第一轮硬编码只保留在测试中”的总括措辞未列例外，事实仍成立。

## C2-r2-L1-5

结论：refuted=false，未能否证。

原文据：“出库上限”逐字给出每边 23；契约 :26 把正式已采纳约束中的数字定义为条文直引。`contract.json.source_domain.left/bottom.category` 为算术推论，契约 :121 同样如此。可由 70÷3 另行推导不消除当前文件已直写该值的事实。按本交付自己采用的分类，发现成立。

## C2-r2-L1-6

结论：refuted=false，未能否证。

原文据：规则“元件”仅指特别运输单位或连续传送带，协议储存箱在“仓储单位”节；契约顶层字段表 :42 把含 storage 的 `planned_absent` 叫“元件类型”。:102 已明确箱体不是运输单位，只能证明正文另一处正确，不能让字段表的集合分类同时正确。应保留为局部术语矛盾，不等于储存箱分段修订完全未做。

## C2-r2-L1-7

结论：refuted=false，未能否证。

原文据：所谓完整性批评“§5.5”实际是 §五.2 的第 5 项，明确批评待验字段没有 T3/T7/T12 交叉编号。契约全文与修订记录仅泛指规格参数轴；未给 `fanouts.certification`、三栏和选择点的映射。读取的 `选择点参数轴.md` 已有 T 编号及具名轴，但其目录接口也不能替代契约义务映射。

限定：第二轮 B 的三项明确动作没有单列该交叉编号，修订记录“§3.B.1–3 无待实现项”不能简单等同于宣称全体完整性批评已解决；也不能把来源物理身份待求直接等同于 T3/T7/T12。不过发现的核心——已指出的交接缺口既未补齐也未列为该侧未决——未被现有文字覆盖。

## C2-r2-L2-1

结论：refuted=false，未能否证。

独立对照规则 :36、:64 与目录两个单位对象，累计域、窗口域和起算规则、传输冷却确实均未编码。目录供电桩已经编码 12×12，并将覆盖格集合另标未定，说明已知常数与未定语义可以分开。主席只把冷却粒度留为未定，没有撤销 5 tick 常数。即使本契约候选排除准入口和箱体，目录还要被 C 线引用，不能据此取消共享目录缺项。与 L1-2 同一根因，分别判定。

## C2-r2-L2-2

结论：refuted=false，未能否证。

原文据：规则制造单位写 3×3、5×5、6×4 和整条边为端口，“端口”又规定一口一格。制造单位端口数 3/5/6 需结合两条规则得到；协议储存箱 3/3 同理。目录把这些数量标作条文直引，契约定义却把据正式数字运算所得归算术推论。反方向的 23 已由“出库上限”“边带排布”直写，却标算术推论。

限定：不能仅因端口格位标算术推论就推定所有单位的端口数也必须是算术推论；核心 14/6 在条文明写，格位还涉及零起点换码。发现所指制造单位和箱体已足以成立，不扩大到所有同名字段。

## C2-r2-L2-3

结论：refuted=false，未能否证。

原文据：转换器 :82 只读机型 `input_count`，:88 用同一 cap 填两侧。正式当前九种制造机两边确实等长，所以现候选数值不错误，且 Rust spec 分别取两侧并能发现不一致。这些都不能证明转换器自身已分别使用两侧目录字段。

未来非对称制造单位不属于当前正式规则，本席不拿它充当当前合法反例；本发现保留为单一数据源接口和声明精度问题，不表述为本轮取货端口数错值。

## C2-r2-L2-4

结论：refuted=false，未能否证。

原文据：首轮任务书 §3.B.3 要用有理数不用浮点；转换器 :87 从整数批数构造 `Fraction(20,batches)`，:128 却用 `quantity(str(row['批间隔tick']))`；源 design.py:108 用 `/` 生成该字段。独立读取源 fanout.json 为 32 个 float 1.0、1 个 float 2.0。本轮全部精确。

独立算术反例：`Fraction(str(20/3))=6666666666666667/1000000000000000`，不等于 `Fraction(20,3)`。这证明两条数值管道不普遍等价；不证明随意换成 3 批即可构成完整合法候选。Rust :1045–1048 的比对会发现两值不等，而不能恢复已舍入值。契约加载器拒绝小数字符串不覆盖转换前的浮点来源。

## C2-r2-L2-5

结论：refuted=false，未能否证。

原文据：“回路转弯”末句规定相关制造单位加植物运输单位占格至少 1378。代码 :1090 写“物料图回路不提供运输转弯与占格”，确实没有给这一末句的算术投影。独立按 JSON 的机器配方和 area 去重计算：种植 32×25=800，采种 17×25=425，粉碎过植株的 17×9=153，总计 1378。

限定：“运输转弯与占格”可理解为运输部分，两者确实未提供，故不是全部理由字面皆假；计划配方也不能证明真实循环态采用过这些配方。但报告其他静态项均以计划实现为条件，按同样前提可给 `运输占格≥max(0,1378−相关制造占格)`，当前右端为 0。缺少几何坐标不妨碍这个条件算术投影。本条仍不能否证。发现中的另一配置不视为已验证可实现布局。

## C2-r2-L2-6

结论：refuted=false，未能否证。

原文据：“端口速率”量化所有端口；契约 :75 明说沿途运输单位数量、几何未知。`lib.rs:1072` 给端口速率的正式条目理由只列实际循环态流量未认证；逐记录及按源端、目标端聚合的检查未覆盖运输网络内部端口。

尝试否证：全文已有网络缺失说明，因此没有证据说读者被保证了完整布局认证。但是 :20 承诺逐条列未覆盖部分，通用几何说明不能替代该正式条目自己的论域缺失说明。保留为报告覆盖理由不完整，非当前端点速率误算。

## C2-r2-L2-7

结论：refuted=false，未能否证。

原文据：契约 :114 位于 `sources[].id` 行，却末尾写“本契约使用 LF id”；转换器 :107 为来源分配 ORE 顺序号，送料 :114 左右才分配 LF。只读核对得 ORE000 被 LF018 引用、ORE051 被 LF248 引用。

限定：“保持与源 CSV 的 C 行对应”可指保留关联，不一定声称数字相同；这半句本身可以读对。但在来源 id 的单元格写 LF 仍混淆来源变量与送料记录。转换说明已有正确映射，不消除此处歧义。没有发现实际数据对应丢失。

## C2-r2-L3-1

结论：refuted=true，已否证。

直接反证是发现所引契约 :88 同一段的后半句：“全局 `planned_absent` 是明确的计划限制，它与 null 的局部几何占位同时存在。已填 true 不得违反全局排除。”候选 JSON 的 `planned_absent` 确为 splitter、merger、gate、storage 四项，`model` 也直写无这四类单位。`lib.rs:422–428` 核全局集合恰为四项；:529–535 才检查局部占位没有与排除条件相反的 true。

因此局部 null 和 false 均被允许，不是从 null 推导不存在，而是全局计划已经独立声明不存在；数据承载位置是顶层 `planned_absent`，不在每条 via。正文 :20 又声明报告以输入计划成立为条件。正式规则允许分流器、汇流器、准入口、箱体，不妨碍任务书纪律允许明确声明并登记覆盖损失的受限计划。

本否证不宣称实际布局已经排除了这些单位，也不使用发现给出的“四式即使有元件也充分”的附加推论。这里只反驳“数据没有承载”“口径自相矛盾”；局部尚未布线与全局限制允许的未来实现不矛盾。

## C2-r2-L3-2

结论：refuted=false，未能否证。

原文据：“矿系不入库”先允许成品或植物系进入，再在种植机恰 32 台时收紧为只有成品。`lib.rs:934–941` 不查台数，直接要求全部进核心记录的物品为两成品。契约 :51 的受限模型只排除运输类型，未普遍排除植物入库；:147 又说条件式先算台数前件。

候选 B 恰 32 台使本次结论正确，CLI 的候选 B 回归也会拒收别的配置；这防止把发现称作当前候选被误判，不能补齐该正式条目投影的缺失前件。尚未构造完整可行的多种植机布局，本判定只确认条件式被无条件收紧且未声明。

## C2-r2-L3-3

结论：refuted=false，未能否证。

原文据：“矿石分流与专机”定义 C矿 要求只做对应矿配方且每 tick 一批。“箱体过站”沿用该量。`lib.rs:853–872` 每遇矿石记录都把目标插入 dedicated，valid 只累计进 ore_valid；:879–883 和 :976–980 没引用 ore_valid，却将 dedicated 长度称为 C矿。

上一行“矿线专机/计划”失败会使整个报告失败，并非畸形输入会整体通过。然而 Check 列表独立输出每项，且正式条目逐项覆盖是明确交付要求；其他行失败不能让这两行对 C矿 的陈述变真。本轮 B 的专机均满足 valid，所以当前两行真值不受影响。保留逐条报告正确性问题，不宣称成功骗过总验收。

## C2-r2-L3-4

结论：refuted=false，未能否证。

原文据：“混做清空”“灌装混线”均有数据相关前件；`lib.rs:1076–1085` 根据正式条目名选择常量理由，没有根据本次配方、台数或先前检查真值生成“本候选无混做”“本计划前提不触发”等语句。:833–838 的 else 也可能由灌装机台数不等于 3 触发，而不一定是四输入机数量条件。

尝试否证：契约 :18 已声明 CLI 混有候选 B 回归，当前 JSON 无混做、三台灌装都四输入，故当前生成报告文字确实为真。可是 validate 接受并报告输入计划，格式又允许多配方；失败报告也不能无条件断言输入无混做或专机已核。因此这是输入变动后的报告诊断缺陷，不能据当前 B 恰满足而否证。

## C2-r2-L3-5

结论：refuted=false，未能否证。

独立回源核对：物料流量、单位矿耗、目标、出库上限、运输端口收支、箱体接口、矿石分流与专机、成品汇入的运行阈值仍是 Rust 字面量。目录 `constraints` 是 name/text 集合；测试只核单位属性和正式条目报告存在，没有逐一把这些运行值对回正式文本。转换器 :43–44 只比较 Python 重建的文本/配方与目录。

契约 :155 可狭义读为此次迁移的机型常量不再运行硬编码；转换说明还提示正式文件变动须人工审查。因此不能断言交付承诺了任意规则改动自动支持。但“旧硬编码仅留一致性测试”的表述没有排除其余正式阈值，实际同步风险依旧，未能否证。与 L1-4 重复，不另制造一个数值错例。

## C2-r2-L3-6

结论：refuted=false，未能否证。

独立回读 quantity 用 `Fraction(value)`；源 fanout 用 `P / it.batches`；转换器 :128 先 str 再 Fraction，无法验证 float 之前的精确比值。33 项源值都是浮点，32 项 1.0、1 项 2.0，现值没有误差。Rust 加载时拿到的是已变为整数/分数格式的字符串，所以拒绝 JSON float 的测试对此路径不构成保护。

限定：契约 :24 的拒收也可以狭义指最终 Quantity 接口，最终 JSON 没有违反加载语法；但首轮要求有理数不用浮点，且转换说明自述精确有理数换算。跨管道精度问题未被说明覆盖，不能用最终字符串合法否证。与 L2-4 为同一转换点。

## 独立只读核算及自审

核算使用 `json.loads`、`Fraction`、`Counter`，没有运行原设计导出代码。结果：相关制造单位占格 1378；源扇出 33 项均为 float，其中 1.0 共 32 项、2.0 共 1 项；20/3 的浮点十进制路径不等于精确有理数；当前多配方机器数为 0；315 条 via 的 splitter/merger/gate 全为空，而顶层明确排除这三项及 storage；两端 ORE→LF 映射为 ORE000→LF018、ORE051→LF248。

已做读者视角自审：20 个发现 id 分别给出结论与回源依据；重复发现保留独立结论；当前候选错值、潜在接口风险、逐条报告失真与总体验收失败分开陈述。未把假设改动证明成真实合法布局；未把只读算术核算称为运行认证。

## 读取对象指纹

以下 SHA-256 固定本判读引用的主要文件版本。

| 文件 | SHA-256 |
|---|---|
| `《明日方舟：终末地》游戏规则.txt` | `abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670` |
| `求解任务.txt` | `10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864` |
| `求解约束.txt` | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` |
| `求解器/数据/送料契约.md` | `138ba7f15059de5bbe49615fa466880db3a0a415f7c054c024e7347d08635922` |
| `求解器/数据/正式静态目录.json` | `63acbb92067fd9ba4229f7b83de8b5054023b6b4e520f9e7077d48b562896230` |
| `求解器/数据/修订记录.md` | `996c707fba81b97585865441847963373250bc1c0a39293c75f50a95b77ec223` |
| `求解器/数据/候选B/contract.json` | `d0c887c73674a58424d05257f002953a2ee4d0b888614d52cc0b2b60ebd58ff2` |
| `求解器/数据/候选B/转换说明.md` | `861382b5f5a7d33adfb5601d23cb14dd6e705a4e62ebd3e8aa2574f6316b62c6` |
| `求解器/数据/工具/convert_candidate_b.py` | `01f0b75c44f7ecf1b21a180271870b6bfcfa3a4da6bc9bb0087f4d8be786703e` |
| `求解器/crates/topology/src/lib.rs` | `593b3e62c88629c82e22816b74e75b2e1e57eb2e690d077f7664d0a0c8ac1286` |
| `求解器/crates/topology/tests/validation.rs` | `3ca31c3c16455a461a86d2b02ac98cfc6ce8fb4b6d25b193d0c3e43451b280c3` |
| `求解器/规格/复核/完整性批评.md` | `eb1cdd83cbe471c69fae7cd4f82cdd45c89180c1497318c137e7676de8b13daf` |
