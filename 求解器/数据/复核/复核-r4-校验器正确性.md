# 契约线第 4 轮复核：校验器正确性

日期：2026-09-19。复核席：GPT 源，Codex 转发。状态：复核完成，发现 1 条重要问题；未发现当前候选 B 的已检查静态数值算错。静态通过不构成几何、起动或运行认证。

## 复核对象与判读边界

先完整阅读工作流目录 `2a1af1b1-ede2-4b4a-8f95-41e78df48af0/scratchpad/step1/` 下的 `任务书3.md`，再完整阅读 `任务书.md`、`任务书2.md`。第三轮任务书第 0 节已经确定：分工调整延后不取消任何任务，本轮应执行复核；前轮修订记录中关于停派解释的历史未决不作为本轮跳过任务或请求裁定的理由。

规则判断仅依据仓库 `/home/zhuran24/zmd-research-fresh` 根目录三份正式文件：[游戏规则](../../../《明日方舟：终末地》游戏规则.txt)、[求解任务](../../../求解任务.txt)、[求解约束](../../../求解约束.txt)。已完整回读三份正式文件。会议纪要、共识稿有关认证边界与契约交接的章节、评审摘录、完整性批评和前轮复核／修订产物用于确定工作范围与核查承接，不作为新增游戏规则。没有引用候选约束证明合法性，没有使用面积档位条件选取做法，没有复议主席已定事项。

本次阅读 `crates/topology` 全部代码：`src/lib.rs` 1365 行、`src/main.rs` 24 行、`tests/validation.rs` 757 行，以及两级 Cargo 清单。完整检查 `formal_catalog.py` 和 `convert_candidate_b.py`，阅读契约、共享目录、覆盖表、修订记录、候选输入、校验报告、验证记录；JSON 数据和 4984 项报告结果由独立脚本完整解析。被复核输入的 SHA-256 固定于[输入指纹](r4-校验器证据/输入指纹.json)。

所有本席写入均在 `求解器/数据/复核/`。原始被复核文件、三份正式文件与 `候选约束.txt` 未改；变异只发生在本席的内存对象或隔离副本中。未操作模拟器，未提交或推送。

## 实际运行与独立复算

下列测试结果是工具运行记录；候选件数、计划流量与派生下界是以候选为前提的算术结果，不是游戏运行实测。

| 核验 | 结果 | 证据 |
|---|---|---|
| 原版 `cargo test --offline --locked` | 1 单元＋28 集成，29 通过、0 失败 | [原版日志](r4-校验器证据/cargo-test.log) |
| 原版候选 B CLI | 退出 0；4925 通过、0 失败、59 不能静态检 | [本次完整输出](r4-校验器证据/候选B校验.stdout)；与原 `候选B/校验报告.md` 逐字节相同 |
| 原 Python 目录负例 | 3 通过 | [日志](r4-校验器证据/原工具重跑.stderr)；只将临时文件目录重定向到本席目录，未改变断言 |
| 原转换器复跑 | `contract.json` 与 `来源清单.json` 均逐字节复现 | [结果](r4-校验器证据/转换复现结果.json)；仅将 AST 中输出目录 `OUT` 改为本席目录，正式目录与候选源资产仍只读 |
| 独立配方回源 | 当前 18 条配方的机型、输入、输出、耗时与正式配方节逐项相同 | [核验脚本](r4-校验器证据/audit.py)、[结果](r4-校验器证据/独立核验.json) |
| 独立下限回源 | 九机型的台数、存货通道数、取货通道数共 27 个值与正式约束相同 | 同上；从正式条文按机型名提取，不使用 Rust 的旧常量对照表 |
| 候选逐机配方守恒、负荷和端口 | 全部通过，按 `Fraction` 精确计算 | 同上；315 条 LF 的端点、物品、配方、速率与满速标记对照原 CSV |
| 目录变异测试 | 配方耗时、供电覆盖宽度、机型下限的变异均被 `verify` 接受；运输阈值变异被拒 | [变异结果](r4-校验器证据/目录变异结果.json) |
| 隔离配方耗时变异后的原版测试和 CLI | 29 测试仍通过；CLI 仍为 4925／0／59，退出 0 | [运行结果](r4-校验器证据/隔离变异运行.json)；详见发现 |
| 隔离供电宽度变异后的原版测试和 CLI | 29 测试仍通过；CLI 仍为 4925／0／59，退出 0 | [运行结果](r4-校验器证据/供电宽度变异运行.json)；详见发现 |

原版运行目录仍为 `求解器/`；`CARGO_HOME` 使用本席复制的依赖缓存，`CARGO_TARGET_DIR` 和 `TMPDIR` 均指向本席目录，没有向原构建或修订验证目录写入。复跑命令为：

```bash
cd /home/zhuran24/zmd-research-fresh/求解器
CARGO_HOME="$PWD/数据/复核/r4-校验器证据/cargo-home" \
CARGO_TARGET_DIR="$PWD/数据/复核/r4-校验器证据/target" \
TMPDIR="$PWD/数据/复核/r4-校验器证据" \
cargo test --offline --locked
cd /home/zhuran24/zmd-research-fresh
PYTHONDONTWRITEBYTECODE=1 python 求解器/数据/复核/r4-校验器证据/audit.py
PYTHONDONTWRITEBYTECODE=1 python 求解器/数据/复核/r4-校验器证据/check_tools.py
```

独立复算得到制造单位 219 台、占格 3325、逻辑送料 315 条、S=R=315、计划满速 LF 300 条（矿石 52，非矿石 248）。机型计数依次为粉碎 69、精炼 51、研磨 32、塑形 6、配件 6、种植 32、采种 17、封装 3、灌装 3。目标交付为电池 3/5、胶囊 11/20 件/tick。转换复现还核对了全部逐机端口列表、扇出 30／2／1 和三栏到达义务 43 台。没有用测试通过反推这些数值正确，而是另行从输入和正式条文计算。

## 发现

### C3-r4-L3-1

- id：C3-r4-L3-1
- where：/home/zhuran24/zmd-research-fresh/求解器/数据/工具/formal_catalog.py:104-132；/home/zhuran24/zmd-research-fresh/求解器/crates/topology/tests/validation.rs:425-474、740-756；/home/zhuran24/zmd-research-fresh/求解器/数据/送料契约.md:156、165
- claim：独立回源核验遗漏配方数值及供电覆盖尺寸，违背正式原文的目录副本仍可通过全部 cargo test 和候选 B 校验。
- evidence：《明日方舟：终末地》游戏规则.txt L92：「2 蓝铁粉末 ＋ 1 砂叶粉末 → 1 致密蓝铁粉末，1 tick」；L76：「供电桩：2x2,以自己中心为原点12x12的范围内若有某个需电单位的一部分则为该单位赋予供电状态」。任务书2.md §3.B.1：「作为规格、契约、Rust 三方唯一真源」。
- severity：重要

#### 1. 当前正确值和反例的判错依据

当前目录 `recipes[id=研磨-致密蓝铁].duration.value` 是 `"1"`（[目录](../正式静态目录.json) L144–164），与正式规则 L92 一致。当前 `units[id=供电桩].coverage.width/height` 均为 `"12"`（同文件 L3484–3495），与正式规则 L76 一致。本条没有声称原目录已经录错。

两份反例分别仅将上述耗时改成 `"1/2"`，或将覆盖宽度改成 `"13"`。它们直接违反已写明的 1 tick、12×12；无需依赖任何运行轨迹或未定读法。供电格边界究竟如何计入覆盖的参数不会改变条文明写的宽度；这里不复议 `positive_area` 与 `closed_touch` 的主席裁定。

任务书2 §3.B.1 要求共享目录作为规格、契约、Rust 三方唯一真源。契约 L165 进一步提供任一目录消费者可独立执行的 `formal_catalog.py` 回源入口，且声明 Cargo 一致性测试调用同一检查。因此原文快照一致与结构化规则数值一致是两个必须分开的命题。

#### 2. 漏检发生在哪里

`formal_catalog.py` 的完整流程是：

1. L108–109 获取三份正式文件当前字节指纹与行文本，并与 `catalog['sources']` 比较。这证明快照未漂移，不能证明目录的另一组结构化字段由快照正确转录。
2. L110–111 只比较 `formal_projection(current)` 返回的键。其 L104 返回集合恰为 `constraints`、`task`、`static_checks`，没有 `recipes` 或完整的 `units` 投影。
3. L112–131 另查单位中的准入口上限和起算、储存箱冷却和判定域、跨格唯一性、部分端口数字类别。这里没有任何配方数值检查，也没有供电 `coverage.width/height` 检查。
4. L132 正常返回；命令行 L137 给出“一致”消息。

这不是仅凭函数名推测。内存中的四组独立变异结果如下；未修改正式来源，也未重写 `sources` 的任何指纹或正文。

| 单一变异 | `formal_catalog.verify` 实际结果 | 解释 |
|---|---|---|
| 研磨-致密蓝铁耗时 1 → 1/2 | 正常返回 | 配方表不在回源比较集合 |
| 供电覆盖宽度 12 → 13 | 正常返回 | 单位补充检查不覆盖该数值 |
| 粉碎机台数下限 68 → 1 | 正常返回 | `units[].static_lower_bounds` 也不在此入口的比较集合；但 Rust 旧常量测试另有守卫，不据此声称整套 Cargo 会放过该变异 |
| `static_checks.constants.transport_s` 312 → 1 | 抛出断言 | 对照组证明回源函数确实执行，且已覆盖的结构化阈值能够拦截错误 |

输入全文与结果分别见[耗时变异](r4-校验器证据/变异-研磨耗时.json)、[宽度变异](r4-校验器证据/变异-供电宽度.json)、[下限变异](r4-校验器证据/变异-机型下限.json)、[对照变异](r4-校验器证据/对照-运输阈值.json)、[结果](r4-校验器证据/目录变异结果.json)。

#### 3. Cargo 测试没有补上这两处缺口

现有 `catalog_unit_properties_match_formal_reference_values` 的 L425–434 对照的是单位占地尺寸、面积、端口数和需电标记；L435–468 检查物品格和端口布局，未比较供电范围。该测试最后 L470–474 对配方仅检查其 `kind` 引用某个制造单位，不核配方用量、产量和耗时。后面的端口布局测试同样不涉及这两类数值。

`catalog_is_checked_against_live_formal_sources`（L740–756）只运行上述 `formal_catalog.py` 并要求退出成功，没有独立的完整配方或覆盖尺寸回源检查。

本席在 `r4-校验器证据/隔离副本/` 复制三份正式文件、Cargo 清单和锁文件、完整原 Rust 代码和测试、原 `formal_catalog.py`、原候选输入。三份正式文件字节与原件相同。每次仅替换隔离目录的 `正式静态目录.json`，并更新时间戳以防 Cargo 复用旧的内置 JSON；两次均确实重编译 topology。未删除测试、未变更断言、未放宽代码。

耗时变异中，`cargo test --offline --locked` 仍为 1＋28 项通过；新编译 CLI 也退出 0。更直接的证据是同一报告行已读到错误值：

| 报告行 `制造能力/M120` | 原版 | 仅耗时变异版 |
|---|---|---|
| 各配方批次率×耗时之和 | `1 ≤1` | `1/2 ≤1` |
| 该行状态 | 能检且通过 | 能检且通过 |
| 整份报告分组 | 4925／0／59 | 4925／0／59 |

对照见[原版报告 L3906](r4-校验器证据/候选B校验.stdout)、[耗时变异报告 L3906](r4-校验器证据/变异-研磨耗时-候选B校验.stdout)、[耗时变异测试日志](r4-校验器证据/变异-研磨耗时-cargo-test.stdout)、[编译日志](r4-校验器证据/变异-研磨耗时-cargo-test.stderr)。`lib.rs` L754、795–799 正在使用目录耗时计算制造负荷，因此这处缺口能够进入实际检查结果，不只是未使用的展示字段。

宽度变异同样通过全部测试并使 CLI 退出 0，见[宽度变异测试日志](r4-校验器证据/变异-供电宽度-cargo-test.stdout)、[编译日志](r4-校验器证据/变异-供电宽度-cargo-test.stderr)、[报告](r4-校验器证据/变异-供电宽度-候选B校验.stdout)。这里的 CLI 不检查几何，故不能把它的成功说成错误几何被认证；错误在于供共享消费者使用的回源核验和一致性测试没有发现明确的目录数值错误。

#### 4. 已有保护不能否证本发现

`convert_candidate_b.py` L24–41 确实从正式文件解析全部配方，并在 L41 比较 `recipes == catalog['recipes']`。如果重新运行转换器，耗时反例会被这一断言拒绝。本席已重跑其原版，转换也精确复现。因此本条不指称转换器缺少配方回源，更不指称原候选已被错误生成。

问题是这个检查只存在于转换候选 B 的入口，未进入对共享目录公布的独立 `verify`，也未进入现有 Cargo 回源测试。任务书要求的另外两条线和已有候选的再次编译，并不必须重新转换候选 B；测试中也确实没有调用转换器。供电宽度甚至不在转换器的这项配方断言中。

同理，当前原文 SHA-256 正确、目录版本字符串不变、Quantity 类别仍是“条文直引”，均不能排除目录正文与结构化值自相矛盾。两份反例全部保留这些字段，仍被接受。

#### 5. 影响、严重度和最小修订方向

这是可重现的规则数值回源覆盖缺口，记“重要”：它会漏掉共享目录中的明确错误，并可误导后续对制造能力或单位属性的使用。没有将其升级为当前候选 B 已错误达标，也没有构造通过全部正式运行约束的反例布局，故不据此记“阻断”。

最小修订是把转换器中完整配方解析／比对迁入共享回源函数，由转换器和 Cargo 测试共同调用；补齐供电覆盖尺寸等正式已定单位数值的回源比较，并保留上述单字段变异作为拒绝测试。保留合法的未定语义字段，不将未定当作替换已定数值的许可。若独立核验有意只覆盖部分字段，必须明确列出范围，不能将部分核验当成完整共享目录一致性的证据。本席未修改被复核代码。

## 其余检查的结论与未报发现的理由

| 检查范围及代码位置 | 对回正式条文的结果与证据边界 |
|---|---|
| `lib.rs` L12–49、233–283：有理数与加载 | 使用 BigInt／BigRational，分母严格为正；JSON 浮点和小数字符串不接受，可空字段仍须出现。运行算术未发现 f32/f64 或浮点中转。测试中用于拒绝的 `0.6` 是负例，不是正式计算。转换器 `parse_float=str`、`Fraction` 路径未发现浮点数值运算。 |
| L380–465：数字类别 | 本轮已按字段角色检查合法枚举内的错标；来源域 23 对回“出库上限”，机型端口数继承目录算术类别，目标对回任务。候选回归、归一化负荷、无箱代入和形状算法已与正式阈值分开说明。该结果不替代本次发现中的目录数值回源。 |
| L609–699：端点、端口和速率 | 对回“端口速率：每个端口每 tick 至多通过 1 个物品”。同一端口多记录合计，端点关系双射；不支持的分流、汇流、准入口、箱体模型拒绝，并明示只是格式覆盖限制。运输内部端口未提供，正式条目保留未知，没有被算成已检。 |
| L705–851：逐机／全局守恒、制造负荷和成品目标 | 配方输入输出按批次率逐项相等；负荷按耗时归一化。“周期倍数”原文为成品产率“恰为 0.6 个/tick 与 0.55 个/tick”，所以目标用等号正确，不是误将任务的“至少”缩窄。当前目录配方逐项正确；来源守卫的缺口另见唯一发现。 |
| L857–918：机型、通道下限、满载 | 九机型下限与正式原文逐项一致。满载前件的机型集合恰为“粉碎机、精炼炉、配件机、种植机、采种机、封装机”；候选粉碎 69、采种 17，两个混做子句未触发，不能要求它们按恰下限配置混做。 |
| L920–1009：研磨、封装、灌装条件 | 研磨恰32时31台至少3输入、塑形恰6时5台至少2输入、采种恰16时每台2输出、封装恰3时每台5输入及恰5时满速，均按前件判定。灌装三台都是4输入，“恰2台”前件不成立；前件成立时专用无汇流／无箱模型确实不能满足“灌装混线”的结构结论。没有将单纯前件不触发记成运行已认证。 |
| L1010–1068、1157–1180：矿口、矿线专机与矿耗 | 对回“取货口配置”“矿线专机”“单位矿耗”“矿石分流与专机”。专机集合现在实际要求对应矿配方、满批及唯一输入；C矿不再只按下游存在计数。单位矿耗系数和外部流量分别读目录，对候选为蓝铁矿34、源矿18。 |
| L1069–1111：植物回路和入库限制 | 两个方程对应“采种=粉碎+植株及种子入库”“种植=2×采种−种子入库”；“种植机恰32台时植物系也不入库”有台数守卫。种植32、两类植物物料下限及满载共同给出荞花种植11、砂叶种植21，再由守恒给采种5.5、10.5；这不是漏掉一个必须另写的静态条件。 |
| L1112–1156：成品汇入、S/R、箱体接口 | K只数成品入核心端口，来源只数成品源；无箱无汇流时条件代入 K≥6 正确。S、R按非运输端点端口去重；S=R单列为契约冗余自查。无箱无分汇流代入 H=2、η=max(0,5,−5)=5，S+R≥624，候选630通过；未知实体运输数量没有伪造为零。 |
| L1147–1149、1178–1192：派生下界 | “运输端口收支”给 T+b≥315+E≥315，比无箱“箱体过站”的 ceil(305.65)=306 更强。相关植物制造占格为1378，面积差下界为0；两个实际实现比较仍列未知。它们是条件下界，不是存在运输布局的证明。 |
| L362–377、1193–1226：首次物料流、满速集合、回路图 | 箱体转发不重复计基本物料需求，Q与S/R另定义；315条LF里300条计划满速已逐条登记。植物图仅确认有向回路，未外推起动、活性或物理转弯。满速独占仍要求真实逐tick满速，不由平均候选值直接认证。 |
| L1228–1290：扇出与候选回归 | 单配方扇出分类与认证分开；平均间隔不当成确定间隔，全部认证栏强制待验。219、3325、315、38、43和30／2／1明标候选回归；这些裸数字不冒充正式一般阈值。 |
| L1291–1328：完整正式条目余项 | 56条均列未覆盖说明。几何、接通／判定参数、准入口、库存、循环态、起动、轮询均分的实际前件仍待证。无箱不能自动关闭一般箱体约束；现已输出可知代入部分。混做清空的平均输出通道数必要条件由逐机输出守恒与端口容量共同保证，逐批清空仍属动态义务，没有据候选无混做擅自删去。 |

上述核验未发现新的条文阈值错误或计划／运行认证混淆。当前目录中已独立核实的正式值正确，不代表目录核验程序已完整；“目前数据正确”和“错误数据必被拒绝”正是本次唯一发现区分的两件事。

## 交付核验

复核后再次重读全文，核对发现字段、代码行号、正式原文引文、测试范围和证据链接；反例目录明确标成隔离测试数据，未将它们描述为合法规则取值。发现的五个字段与[结构化摘要](r4-校验器证据/findings.json)逐项一致。原件前后指纹核验见[保护核验](r4-校验器证据/保护核验.json)。

本席没有需要 owner 再裁定的事项。待修订项就是 C3-r4-L3-1；物理来源指派、运输、几何、起动和全称运行认证是契约已声明的后续范围，不作为本轮新增问题凑数。
