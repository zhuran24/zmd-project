# 契约修订与校验器：第二轮异源否证第 1 席

日期：2026-09-19。性质：本轮读取快照的判读存档。20 条发现逐条处理，1 条已否证，19 条未能否证。未能否证不表示接受发现中的全部推演或严重性判断；各节列明适用边界。

## 来源、判据与核验方法

仓库根为 `/home/zhuran24/zmd-research-fresh`。以下相对路径均以仓库根为基准，代码行号对应本报告末尾指纹的读取版本。

完整读取第一轮及第二轮任务书：`/tmp/claude-1000/-home-zhuran24-zmd-research-fresh/393193c2-617f-4fbe-a1b0-a5c39943863c/scratchpad/step1/任务书.md`、同目录 `任务书2.md`。正式依据为 `《明日方舟：终末地》游戏规则.txt`、`求解任务.txt`、`求解约束.txt`；现行回路守恒原文为“仓库外（含缓存格）”。历史完整性批评所引旧哈希不替代现行正式文件。

对照产物包括 `求解器/数据/` 下的 `送料契约.md`、`修订记录.md`、`正式静态目录.json`、`候选B/contract.json`、`候选B/转换说明.md`、`工具/convert_candidate_b.py`、`工具/check_revision.py`；`求解器/crates/topology/` 下的 `src/lib.rs`、`src/main.rs`、`tests/validation.rs`；以及 `求解器/规格/复核/完整性批评.md`、`求解器/规格/选择点参数轴.md` 与 `求解器/规格/内核输入.md` 的相关对接原文。数值依据还回读会议原资产目录 `/home/zhuran24/文档/会议2全套/会议目录/seat-opus-4/` 的 `channels.csv`、`fanout.json` 与 `design.py` 的批间隔生成式。

本席用只读 Python 解析完整目录、契约及原表，独立重算字段统计、映射、植物机器面积、浮点有理化差值和矿机局部谓词；未运行会覆盖产物的转换器或自查脚本。局部谓词探针是代码判据的独立复算，不是执行 Rust 的测试记录，也不是构造合法达标布局。未修改草稿、正式文件或候选约束，未操作模拟器，未提交或推送。

第一轮 §3.B.1 明定三栏覆盖 38 台多料机；§3.B.3 要求有理数及逐条静态检查；第二轮 §3.B.1 明定共享单位目录与机型常量一致性测试。任务范围不会自动扩张为通用布局认证器，但已经给出的字段语义和逐项通过结论仍须准确。

## C2-r2-L1-1

**refuted=false；未能否证。**

规则“配方”“缓存格”与约束“满载配置”“混做清空”允许并约束同机多配方；混做清空的前件是每 tick 完成一批且产出不止一种物品，并非某个配方投入种类大于一。`lib.rs:591` 的 `multi |= rec.inputs.len() > 1` 与 646–658 的双向检查确实使全部单投入配方的混做机不能填写三栏。同物品多通路也不会令 multi 成立。当前 M152–M156 五台塑形机就有多个输入端口而三栏为 null。

契约第 51 行明说能表达同机多配方，第 66 行仅描述“多料配方机／单料机”；候选设计覆盖损失及末尾一般活性待验，并不提供其它机器逐机登记这些义务的字段。转换说明虽提醒“不允许将运行义务缩至这些机器”，仍未补齐表达或明确登记该格式损失。

边界：第一轮只强制给 38 台多料机加三栏，不能据此声称原任务逐字要求所有机器都填三栏，也不能以混做清空直接推出“同源标记”这一个工程字段。存活的是表达范围与义务登记缺口，不是已经发现候选 B 的运行错误。

## C2-r2-L1-2

**refuted=false；未能否证。**

规则“物品准入口”明写累计上限 1–5000、每 5 tick 上限 1–5，并写首件起算、窗口走完由下一件重新起算；“传输”明写 5 tick 冷却。完整读取两个 units 条目，均没有这些域或起算规则，conventions 也不补充。powered_functions 中的 transfer 只是名称，不承载冷却值。

第二轮 §3.C.3 要求这些输入设定，契约第 153–159 行又承诺共享单位属性输入。内核输入第 97 行已另写累计及窗口限额，不能倒过来证明目录已有它们。目录对供电覆盖另存 12×12，亦不足以支持“所有功能数值都故意不收入目录”的解释。冷却粒度未定不影响 5 tick 数值已定。

## C2-r2-L1-3

**refuted=false；未能否证，但桥接器措辞的指控须收窄。**

规则“物品格”明确有单位内同种单格约束，例外列协议储存箱和缓存格。目录的 single_item / single_item_per_slot 只描述逐格物品种类，conventions 与 inventory 没有跨格约束。协议核心的格数不限与每格 80000，结合这一条应保留“同种仅一格”的信息；目录未显式表达。制造输入格的 item_policy 全为 unresolved，未保存“存货物品格不限制物品种类”这个已写前提及其与混装未定的区别。A 线参数轴第 57、63、92 行保存了部分这些信息，但不等于共享目录已经保存。

不能接受发现中“两轴格独立必然与条文相反”的强读法：桥接器原文明写“每对独立拥有一个物品格”，第一轮状态任务也叫两个独立物品格。独立拥有两个格不逻辑蕴涵可以把同一种物品分放两格。目录真正缺少的是跨格条款以及独立性的边界；不用先裁定桥接器例外的全部辖域，就足以保留这一缺口。

## C2-r2-L1-4

**refuted=false；未能否证。**

回源核对“出库上限”“单位矿耗”“矿石需求”“物料流量”“研磨进料”“封装进料”“运输端口收支”“箱体接口”等条文，所指运行阈值确实仍在 Rust 中以 integer/ratio 的字面参数及直接比较出现。目录 constraints 是名字和整段文本，Catalog/Rule 的反序列化及 validate 没有把这些文本解析为比较值。机型 spec 和机型下限从 units 读取，这部分确实已修正。

`legacy_machine_constants_equal_catalog`、两个 catalog_unit/catalog_port 测试检查机型、单位或端口；`every_formal_constraint_has_coverage_row` 只核名字覆盖，未把这些运行字面阈值与条文数字相等联系起来。转换器的 constraints 全文比较也不触及 Rust 阈值。因而规则文字与目录一起更新后，运行值仍可能留旧。修订记录第 11 行的“第一轮硬编码只保留在测试中”没有把此例外写出。

边界：第二轮 B.1 的明示改动对象首先是单位和机型常量，不能把它改写为逐字要求本轮结构化全部 56 条约束。契约第 155 行前句也限定“机型与核心容量”；但范围限定不足以证明剩余字面值不存在，或已有变更联动测试。风险是同步防线缺口，未发现现有阈值数值错误。

## C2-r2-L1-5

**refuted=false；未能否证。**

正式“出库上限”“边带排布”都明写每边 23。契约第 26 行明确已采纳正式约束中的数字属条文直引，contract.source_domain.left/bottom 及契约第 121 行却标算术推论。23 也能由尺寸计算，不取消其在该自定分类中的正式直引来源。第 125 行并列两类不能修正已经具体标错的两项。

## C2-r2-L1-6

**refuted=false；未能否证。**

规则“元件”限特别运输单位或连续传送带段；协议储存箱位于“仓储单位”。契约第 42 行仍把包含 storage 的 planned_absent 一概称作“元件类型”。第 102 行已明确储存箱不是运输单位，却没有修正字段表中的形式术语。当前 storage 分段和拒收实现正确，不能消除这一局部文档分类冲突。

## C2-r2-L1-7

**refuted=false；未能否证。**

完整性批评的准确定位是 §5.2 第 5 项（发现简称 §5.5），指出待验字段与 T3/T7/T12 缺交叉编号。契约结构及文字中没有这些映射，修订记录的未决段也没有明列。复算得到 52 个来源待求、33 个扇出待验、38 组三栏待验。A 线参数轴末节连接的是单位目录的 unresolved 字段，不是这些逐候选待求／待验字段，不能充作已完成的对应表。

第一轮所要求的来源身份、同步和扇出认证义务都仍未结清。不能说所有待求都必然属于同一语义轴；来源几何指派还含几何任务，交叉编号也不等于运行认证。第二轮 B 的显式三项未单列此事，所以不据此断言违反了 B.1–3 的逐字清单；但完整性缺口及未决未登记仍未能否证。

## C2-r2-L2-1

**refuted=false；未能否证。**

独立按本条所指 units 原文核查：准入口条目无 limits/window 等设定定义，箱体条目无 cooldown，二者 basis 中的规则名仅为指引。规则“物品准入口”的两个整数域及完整起算句、“传输”的 5 tick 冷却都明确存在。第二轮 B.1 的共享属性目标与 C.3 的输入需求共同说明这些不是候选 B 不用箱体、准入口就可以从共享目录排除的内容。与 L1-2 重复，但独立结论相同。

## C2-r2-L2-2

**refuted=false；未能否证。**

小／中／大制造单位只给 3×3、5×5、6×4 和整条边为端口；结合“端口”每个一格宽才得到 3/5/6。储存箱的 3 个端口同理。按契约第 26–27 行分类，这些端口计数属于推算，而目录标条文直引。反方向的每边 23 已在“出库上限”“边带排布”明写，实际却标算术推论。

边界：不能说全部 18 类单位所有 input_count 都错。核心 14/6、分流器一进三出等有明文直接数量；零起点位置另有换码也会使位置类别不同于计数类别。因此单凭两字段类别不同不构成证明；制造与箱体的边宽推算、23 的原文直引才构成此项证据。

## C2-r2-L2-3

**refuted=false；未能否证。**

转换器第 82 行只取 unit.ports.input_count，第 88 行给两侧写同一个 cap。全脚本其它地方会读核心 output_count，但制造机写入路径没有读本机的 output_count；发现“全脚本从未读 output_count”若作无范围限定的字面理解不准确，制造侧的核心指控仍成立。

规则规定的现有九种制造机两侧对称，本席核目录全部相等，因此当前值没有错，发现举出的非对称新机型不是现行规则对象。它揭示的是制造取货侧数据依赖与“机型两侧容量以目录为输入”的共享来源口径不一致，而不是本轮候选误报。上述反证只能压缩影响范围，不能证明转换器已独立消费取货侧字段。

## C2-r2-L2-4

**refuted=false；未能否证。**

原 design.py 第 108 行为 interval=P/it.batches，fanout.json 全部 33 项为 JSON float：32 项 1.0、1 项 2.0。转换器第 128 行走 Fraction(str(float))；第 87 行机器表走 Fraction(20,整数批数)。本席直接复算：Fraction(str(20/3))=6666666666666667/1000000000000000，与 20/3 的差为 1/3000000000000000，不为零。Rust 第 1045–1048 行是精确相等比较，故该数字来源路径确实不能普遍保真。

第一轮 §3.B.3 的有理数要求与契约第 24 行的精确表示目标不能由“输出已包装为分数字符串”来满足。当前 33 项无数值误差；本席没有构造全约束合法的三批候选，发现中的未来合法候选断言不在本席证明范围。存活结论是可复现的转换精度缺口。

## C2-r2-L2-5

**refuted=false；未能否证，但不能把整句理由读成否认机器占格字段。**

正式“回路转弯”末句的单位集合包含全部种植机、采种机、粉碎过植株的粉碎机及植物运输单位。独立对 contract 按机器 id 去重，得到种植 32 台×25=800、采种 17 台×25=425、植物粉碎 17 台×9=153，制造部分共 1378 格。在“计划配方有正批次率并得到实现”的条件下，运输面积非负已足以使总占格下限成立；不需要先知道几何坐标。

`lib.rs:981–1005` 仅做流图含环，1090 行“物料图回路不提供运输转弯与占格”可合理读成未知运输占格，这一部分并不虚假。但完整“回路转弯”条款的机器面积投影确实未计算，而契约第 20 行和第一轮 B.3 要求报告可计算投影及剩余覆盖。故不足以整条否证。

发现称换成采种 16、粉碎 68 就必得 1353，信息不足：68 台粉碎中多少台粉碎过植物仍须读配方集；只有植物粉碎台数仍为 17 才得到 1353。且由同条 T植≥34 已给至少 34 格，≥25 的残差不一定增加约束力。不能把该举例当作新候选完整下界证明。

## C2-r2-L2-6

**refuted=false；未能否证。**

约束“端口速率”辖域是每个端口；规则“端口”涵盖运输单位。代码 in_ports/out_ports 只由逻辑记录端点构造，未构造路径内部运输单位端口。契约第 75 行也明确这些单位数量和几何未知。第 1072 行的说明仅谈实际循环态流量尚无证书，没有写论域还缺运输端口。

整份契约读者能从其它章节推知这一限制，所以不把它说成隐蔽地认证了运输网络。但第 20 行承诺每条约束列未覆盖部分，对本条仍遗漏独立于运行证书的模型覆盖缺口。

## C2-r2-L2-7

**refuted=false；未能否证，按文档歧义保留而非数据映射错误。**

规则中矿石来源的物理身份是仓库取货口或核心端口，ORE/LF/C 都是工程编码。逐条核 315 行 C→LF，均保留数字部分；ORE000 对应 C018/LF018，ORE051 对应 C248/LF248。源变量 id 是 ORE，送料 id 才是 LF。转换说明第 44 行和转换器第 107、112 行正确。

契约第 114 行开头“独立的 ORE 变量”已经明确其类型，“保持与 C 行对应”也不必表示数字相等；这些是可用于反驳严重误读的证据。但该 sources[].id 字段格末尾紧接“本契约使用 LF id”，没有明确主语是送料记录，仍有歧义。未能证明发现所指的说明误导已经被该格完整覆盖；不据此指控实际 ORE 映射损坏。

## C2-r2-L3-1

**refuted=true；已否证。**

发现省略了同一契约第 88 行后半句：“全局 planned_absent 是明确的计划限制，它与 null 的局部几何占位同时存在。已填 true 不得违反全局排除。”第 42、51 行及第 102 行也说明排除范围。contract 的 planned_absent 实际是 splitter、merger、gate、storage。

代码第 422–428 行单独检查该集合与这四个名称完全相等；第 529–535 行再排除局部与之冲突的 true。于是数据明确承载全局设计排除，null 只表示没有局部落实证据；两个字段合用没有推出“null=false”。第 20 行还限定报告均以输入计划成立为条件，不将计划排除宣称为真实物理布局认证。

规则的“运输端口收支”“箱体接口”等可在显式受限计划 D=M=B=0 下作条件投影，第一轮纪律也许可有覆盖损失声明的受限模型。本发现的“数据没有承载排除／口径自相矛盾”已被全局字段、检查和原文说明反证。不能用实际运输未布线否定其存在明确设计限制。

## C2-r2-L3-2

**refuted=false；未能否证。**

“矿系不入库”先排含矿物品，再规定成品或植物系，最后才以种植机恰 32 台为前件禁止植物系入库。第 934–941 行确实无条件仅许两种成品；第 898–904 行回路守恒反而已经接入植物与种子入库量，不能以“本格式根本不能写植物入库”消解。

当前种植机恰 32 台，所以当前行结论正确。契约第 18 行和版本检查明确本程序包含候选 B 回归，因此本席不声称已证明一个其它候选能通过其余全部检查；也不以多于 32 台就断言其整个布局合法。存活问题是该正式条文投影无条件加强、并非真实前件的实现；候选专用总入口不能把局部判据解释成正式一般条文。

## C2-r2-L3-3

**refuted=false；未能否证。**

正式 C矿 定义为只做该矿石配方且每 tick 一批的专机。第 870–872 行分别累计 ore_valid 和无条件插入 target；第 879–883、976–980 行仅用 targets 数量判通过。Report.check 的逐项结果独立存放，没有自动传播前项失败。

基线独立复算为目标 52、满足专机谓词 52。只在内存把这些机器计划批次率改为 1/2、倒数改为 2 后，目标仍 52、专机谓词合格数变为 0、ore_valid=false，而两行计数谓词仍 true。这是所述变量错用的直接反例，不是合法候选；矿线专机和守恒等其它项会失败。故发现的局部错误通过成立，不能扩大成整份输入被错误验收。

## C2-r2-L3-4

**refuted=false；未能否证。**

约束“混做清空”“灌装混线”的前件取决于输入配方与机器通道配置。第 1071–1092 行 match 只由条目名选字符串，不读对应前件或已算检查结果。“本候选无混做”“本计划前提不触发”“专机批次率已核”因此不由该处代码保证。第 833–838 行 else 还包括灌装台数不等于三的情形，却只解释为不是恰两台至少四输入。

本席复算当前 219 台均单配方、三台灌装均四输入，当前句子为真。入口限制候选 B 且总报告可能已经有其它失败，限制了未来使用场景；但 validate 对失败输入仍生成整张报告，固定数据断言不会自动随错误输入失效。存活的是报告必须准确说明本次输入及已核结果，不是宣称当前候选报告这一行已为假。

## C2-r2-L3-5

**refuted=false；未能否证。**

逐一读取本条所指运行分支，目标、矿耗、物料流量、来源数、边域、S/R、S+R、核心入库计数等仍直接使用正式阈值字面量。目录内 Rule.text 只作报告条文展示；现有一致性测试没有把这些字面值对应到 text。第 155 行“从目录取机型与核心容量”属实，但后一句“旧硬编码仅留在一致性测试”若概括整个运行检查则不属实。

与 L1-4 重复，但各指向代码均已复核。没有证据表明本轮数字错误；现存问题是单一真源及变更检测未覆盖全部正式比较值，说明中也未区分已迁移机型常量与未迁移阈值。B.1 的范围限定不能证明这些同步防线已经存在。

## C2-r2-L3-6

**refuted=false；未能否证。**

原表全 33 个批间隔确为 float，转换器第 128 行确实先 str 再 Fraction；机器表则从整数批数精确求倒数。第一轮有理数要求不能只靠最终 JSON 表面类型满足。Rust 加载器拒绝 JSON 浮点，无法追溯已经被转成分数字符串的浮点来源。

当前所有值均为 1.0/2.0，精确相等检查也会拦截未来与机器表不符的近似值，所以没有当前数值失真或静默错误通过。机器整数路径已经提供替代的精确来源，仍不足以否证现有转换路径含浮点这一事实及口径缺口。

## 独立复算摘要与交付自审

只读解析得：315 条逻辑记录、945 个 splitter/merger/gate 局部 null、全局四类排除；52 个来源待求、33 个扇出待验、38 组三栏待验。原矿接收机 52 台当前全部满足专机谓词。植物相关制造面积 1378 格。源扇出间隔 32 个 1.0、1 个 2.0；三批示例有理化差值为 1/3000000000000000。

逐条结论均区分正式规则、输入计划和未实现几何／运行；没有从局部探针宣称合法达标布局，也未把当前 CLI 的候选 B 专用边界删掉。20 个发现 id 均单列；重复发现保留独立裁决。回读自审检查了来源定位、原文主语、候选前提及局部失败与全报告失败的区别。

## 读取快照指纹

以下 SHA-256 固定本报告针对的版本；交付前再次读取核对。

| 文件 | SHA-256 |
|---|---|
| `/home/zhuran24/zmd-research-fresh/《明日方舟：终末地》游戏规则.txt` | `abc7a5867f6477eedb619c63a21c4a77144c57ad4567fdcfb696b3049d66a670` |
| `/home/zhuran24/zmd-research-fresh/求解任务.txt` | `10abf80fe6eefe647471eabe78da4053f77b489d3806722c501c6acb405a2864` |
| `/home/zhuran24/zmd-research-fresh/求解约束.txt` | `f6503e6c1568ae5ef05bb2dfac249df0a6a371bb24342e23ba77fafc813648b6` |
| `/home/zhuran24/zmd-research-fresh/候选约束.txt` | `a0ac3f9c82f371132b7de29fe47e4c6c587904680bc2786523de0c825fd5efd9` |
| `/home/zhuran24/zmd-research-fresh/求解器/数据/送料契约.md` | `138ba7f15059de5bbe49615fa466880db3a0a415f7c054c024e7347d08635922` |
| `/home/zhuran24/zmd-research-fresh/求解器/数据/修订记录.md` | `996c707fba81b97585865441847963373250bc1c0a39293c75f50a95b77ec223` |
| `/home/zhuran24/zmd-research-fresh/求解器/数据/正式静态目录.json` | `63acbb92067fd9ba4229f7b83de8b5054023b6b4e520f9e7077d48b562896230` |
| `/home/zhuran24/zmd-research-fresh/求解器/数据/工具/convert_candidate_b.py` | `01f0b75c44f7ecf1b21a180271870b6bfcfa3a4da6bc9bb0087f4d8be786703e` |
| `/home/zhuran24/zmd-research-fresh/求解器/数据/候选B/contract.json` | `d0c887c73674a58424d05257f002953a2ee4d0b888614d52cc0b2b60ebd58ff2` |
| `/home/zhuran24/zmd-research-fresh/求解器/crates/topology/src/lib.rs` | `593b3e62c88629c82e22816b74e75b2e1e57eb2e690d077f7664d0a0c8ac1286` |
| `/home/zhuran24/zmd-research-fresh/求解器/crates/topology/tests/validation.rs` | `3ca31c3c16455a461a86d2b02ac98cfc6ce8fb4b6d25b193d0c3e43451b280c3` |
