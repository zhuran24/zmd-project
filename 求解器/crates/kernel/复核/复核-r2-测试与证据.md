# 内核实现第二轮复核：测试与证据

日期：2026-09-19。状态：复核完成，发现 **1 条重要问题、0 条阻断问题、0 条次要问题**。现有黄金、完整轨迹差分、确定性与独立手推均通过；Python 落盘验收仍会接受伪造的逐轴覆盖证据和部分身份元数据。此结论限于本报告列明的输入、检查和实现快照，不构成全部运行分支、解释组合或目标认证。

## 1. 依据、对象和复现边界

执行依据是第四轮任务书 §4.7、§5，并按要求依次读取任务书4、任务书3、任务书、任务书2。任务书3 §0 的分工延期不取消任务。游戏判据仅取三份正式文件及当前规格；候选约束、实现摘要、旧审查结论均不充当正确性公理。

主要判据为《游戏规则》的物品格、缓存格、需电功能、滞留、判定、轮询、制造、传输和配方，《求解任务》的外部过程、零干预、判定次序，以及《求解约束》的端口速率与物料收支；对应当前《运行语义》§2–§6、《受限转移定义》§1–§5、《内核输入》§1–§6、《内核输出》§1–§3及99轴配置。

复核对象包括任务列出的源文件、测试、说明、第一轮修订证据、四份 `-kernel.json` 和 KQ 疑问登记。原始产物只读。既有 `revision_cli.py:13–14,38,96` 会把测试结果写回实现席的 evidence 目录，因此完整 Cargo 测试在本复核目录的字节一致副本中执行，没有直接在原目录运行这个有写入副作用的集成测试。

- [源码及依赖副本指纹](r2-测试与证据/snapshot-manifest.json)：192 个文件，收尾再次比对，无字节漂移。
- [原文件开工保护指纹](r2-测试与证据/protected-before.json)：272 个原文件，包含正式源、候选、规格、被审源和既有证据。
- [收尾范围核验](r2-测试与证据/final-scope-audit.json)：上述原文件无变化。编译输出、测试临时文件、探针和新记录均定向到 `复核/r2-测试与证据/`。未操作模拟器，未 commit/push。
- [独立审计脚本](r2-测试与证据/audit.py)、[停止及守恒脚本](r2-测试与证据/check_stops_and_invariants.py)、[Rust 验收对照](r2-测试与证据/probe/src/main.rs)保留复现入口。

从仓库根复现独立检查：

```bash
python -B 求解器/crates/kernel/复核/r2-测试与证据/audit.py
python -B 求解器/crates/kernel/复核/r2-测试与证据/check_stops_and_invariants.py
```

Cargo 测试工作目录为 `求解器/crates/kernel/复核/r2-测试与证据/snapshot/求解器`，命令为 `cargo test --locked --offline`；`CARGO_TARGET_DIR` 指向 `复核/r2-测试与证据/target`，`TMPDIR` 指向同级 `tmp`，并设 `PYTHONDONTWRITEBYTECODE=1`。Clippy 在同一环境执行 `cargo clippy --locked --offline -p kernel --all-targets -- -D warnings`。

## 2. 测试结果及其实际含义

| 检查 | 本轮结果 | 证据与限制 |
|---|---|---|
| workspace Cargo 测试 | 93 通过、0 失败 | [cargo-test.txt](r2-测试与证据/cargo-test.txt)：kernel 单元58、参考集成3、CLI/Python集成1；topology 单元1、集成30。不是“kernel有93个单元测试”。 |
| Clippy 全目标 | 通过，`-D warnings` 无警告 | [clippy.txt](r2-测试与证据/clippy.txt)。未重新宣称历史release性能数据。 |
| 现有 CLI/Python 变异回归 | 4个有效对照、40项篡改拒收、6个CLI负例通过 | 实际重跑结果位于[副本 regression-results.json](r2-测试与证据/snapshot/求解器/crates/kernel/evidence/revision-r1/regression-results.json)。这40项没有覆盖本报告§3的新反例。 |
| 四份原始交付记录 | 原 Python 验收入口均通过；粉碎机每份4刻/156事件，分流器每份12刻/794事件 | [audit-results.json](r2-测试与证据/audit-results.json)的 `records.controls`。事件数包含 `no_request`，不能当成实际判定数。 |
| 无语义数组乱序 | 2个样例各3种随机排列，共6组；完整trace相同 | 粉碎机4刻、分流器12刻；种子19/29/39，记录每个数组是否实际改变。 |
| 同一输入重复 | 两个样例各两次，整份输出文件字节相同 | 同一输入路径、同一参数与时段；换输入路径的排列试验只比完整trace，因为输入指纹应当不同。 |
| 独立逐物种账及端口预算 | 16个观察时刻全部通过 | [stops-and-invariants.json](r2-测试与证据/stops-and-invariants.json)：独立加总仓库、普通格、缓存，不调用 Rust `inventory_totals`。 |
| 停止轴公开请求 | 18项均退出2，含准确轴名 | 同上 `request_stops`；这是请求入口覆盖，不等同18个真实游戏触发场景。 |
| 原否证席14个探针 | 输入指纹相同，状态、退出码、已完成前缀长度与修订记录相同 | 同上 `old_probes`；只开工对照仍为 `output_blocked=not_exercised`。 |
| 新手推变体 | 4刻通过 | 见§6，比较全部19个库存格、矿量、进度、待事件、成功移动顺序、内部通道事件和闭包轮数。 |

修订交付清单31个文件的当前哈希均与登记相同；第一轮修订基线中本次选取的55个正式源/规格/样例保护文件均未变，`reference.rs`也与该基线相同。黄金JSON的SHA-256为 `bf0e705f9a92712cd495ae9fabebe95d910eac38ff825b6dc52da04f5b4b3959`。这些比较证实相对已登记基线没有改黄金或参考比较器；哈希本身不证明黄金推演正确，也不证明基线之前的全部修改历史。

## 3. 发现 KR-r2-L2-01

**severity：重要。**

**where：** `/home/zhuran24/zmd-research-fresh/求解器/crates/kernel/tests/verify_outputs.py:63–91`，尤其68、86–90行；回归遗漏位置为 `tests/revision_cli.py:50–63`。

**claim：** Python 落盘验收未核逐轴集合、处置与触发证据及部分顶层身份关联，能够把伪造覆盖报告的记录验收为通过。

**规则依据：**《内核输出》§1规定 `profile_id`“必须等于输入与 parameter_assignment.profile_id”，producer“工具路径也入指纹”，`uncovered_axes`“恰覆盖本次全部轴”，reason/disposition来自配置，`exercised`代表实际执行，evidence为事件id或结构/未触发原因；§3要求先核关联义务，再核整份记录，不能用另算出正确轨迹代替读取并核对交付记录。故正确轨迹及来源指纹不足以接受一个轴身份或执行证据被伪造的报告。

**代码原因：** `verify` 确实核schema、完整输入/历史、来源闭合、全部ticks、end_time及validation_scope；但对逐轴部分仅统计状态数量，再断言总数和不重复数量都是99，没有比较轴集合是否等于实际配置，没有核处置/损失/状态/证据。producer仅核kind，顶层profile_id及producer.path与来源的关联没有核。JSON Schema中的字符串或合法枚举只保证形状，不能补足这些关联。

**可复现证据：** 对两样例×两种编码的四份原记录分别执行下列独立变异，保留轨迹、输入、参数、指纹和validation_scope不变；随后调用未修改的 `verify_outputs.verify(path, data, expected)`。结果见[28项变异结果](r2-测试与证据/record-mutations.json)，亦汇入[audit-results.json](r2-测试与证据/audit-results.json)。

| 单独变异 | Python结果 | 为何违反契约 |
|---|---|---|
| 把第一个axis改为 `fabricated.axis`，仍保留99个互异名字 | 四份均通过 | 真实轴被漏掉，伪轴不属于配置 |
| 将 `transfer.cooldown_scope` 改为 `exercised`，证据为 `never_executed_event` | 四份均通过 | 粉碎机无箱；分流器箱体传输全关闭，两者均没有该执行事件 |
| 将 `warehouse.periodic_lift.disposition` 改为 `已定` | 四份均通过 | 当前配置是“超出覆盖即停”，并非已定机制 |
| 两个warehouse接收域轴的evidence都改为单条“无逐边界报告” | 四份均通过 | 原本4/12个观察边界的报告消失，仍获通过 |
| 顶层 `profile_id` 改为 `fabricated_profile` | 四份均通过 | 与输入及parameter_assignment不相等 |
| `producer.path` 改为 `/nonexistent/fabricated.rs` | 四份均通过 | 与实现来源指纹没有关联 |
| 对照：仅将首刻summary的warehouse_ore改为123 | 四份均拒收 | 表明验收函数实际执行，已有轨迹比较仍生效 |

合计 **24份语义非法的元数据变异被接受，4份摘要变异被拒绝**。每份篡改记录原件及对应有效对照均在本复核目录，不覆盖交付记录。

**边界及反证检查：** [Rust对照结果](r2-测试与证据/rust-verifier-controls.json)表明，`output::verify_record`对有效对照通过，对同样六类变异全部拒绝，理由为“交付记录与完整重算逐字段不同”。因此本发现只针对Python独立验收/其自动回归覆盖，并不声称Rust的整份重算验收也有这个漏洞。当前四份原记录未据此判为已含伪造内容；问题是验收器无法排除这类错误产物。

**影响与修订方向：** 现有“40项篡改均拒绝”不能作为逐轴报告已完整验收的证据，后续修订可能漏轴或冒领机制覆盖而仍绿灯。建议从配置及轨迹独立重建轴集合和关联字段，验证事件证据及仓库逐边界报告，并将以上六类负例加入双样例双格式回归；profile和producer关联也应验证。此席未改实现或测试。

## 4. 黄金、差分与确定性论证

`tests/reference.rs:13–32`的compare逐层比较对象键集合、数组长度、数组顺序和叶值；没有删字段、排序数组、忽略basis/detail或进行数值容差。`45–66`先把Rust每一步的完整tick和参考记录相比，再把粉碎机四个summary与黄金的全部ticks字段相比。黄金本来就是摘要格式，不能将这项称为“黄金保存了每个完整StateSeed”；完整状态义务由旁边的参考差分承担。

`70–103`确实现场启动Python，核“Python重算ticks＝原落盘ticks”，然后核“Rust完整tick＝Python完整tick”。本轮三个测试均通过。Python的 `runtime_record.same`以排序对象键的JSON序列化比较，数组和JSON类型仍保留，`true`不会与`1`混同。原始四份记录的schema、全状态/事件/闭包、两格式连续重建及末段仍严格比较。§3指出的是完整记录中尚未比较的元数据部分，不否定上述tick比较。

`tests.rs:124–145`实际对 `raw.layout.units/physical_channels/buffer_channels`调用reverse，再从修改后的raw重新解析。不是只创建了一个未使用的排列变量。两份样例单位和PC各有多个，粉碎机BC有8条，都会发生真实变化；分流器BC本来为空，反转空数组不算BC置换证据。

既有确定性测试两例都只走4刻。本轮额外把分流器延长到完整12刻，覆盖t=6窗口恢复后的区间；随机置换只改无语义的布局枚举，不改模板全序、接通仲裁、配方序等真正的输入参数。6组输出完整trace一致，分别记录改变标志；分流器的空BC明确标false，不冒领扰动。没有据此声称所有排列及所有输入均获穷尽证明。

## 5. 不变量、停止测试与未覆盖边界

### 5.1 不变量

**仓库E单调与O稳定子序。** `warehouse_identity_monotone`不是在空E上作恒真断言：它先加两个匿名格，显式O为`[spare_b,spare_a]`，首次入库后检查只余`spare_a`，并检查被选格实际持有电池；清空后历史身份保留、不回E，同种再入复用原格。`warehouse_competition_atomic_stop`比对整个判前后state及交付账；未指派双新种、被指派单新种也各有正对照。其依据为转移§4.1–§4.3。这是有效的代表性机制回归，不是所有输入的归纳证明。两个主样例的匿名E为空，单靠它们不能证实删除行为。

**闭包与终止。** `closure_and_resources`核扫描至少两轮，给1轮上限时返回 `inconclusive(resource.sweeps)`；`stopped_engine_cannot_continue`核停止后即使提高资源界限也不能复用该实例。代码 `transition.rs:549–565,621–676`把初始K加入seen，按完整序列化内容比较，包含库存/进度/仓库身份/O/轮询/图/门/待事件/预算，没有只比哈希或只比库存。两参考轨迹逐事件及scan_rounds比较、本轮手推的2/3/3/3轮共同提供具体检验。一般支持域终止仍由转移§5.2有限非轮询变化＋有限轮询状态的论证承担；有限测试不能代替该证明，也未证明资源上限永不触发。

**物料及预算。** 既有 `warehouse_80000_and_recipe_conservation`从80000起核账，在制造完成时才据配方做净转换；缓存计入库存。`transfer_conservation`核无线入库不改变全基地逐物种总量、另计交付。独立脚本从JSON直接加总完整仓库和全部inventory，对制造完成读取前态已锁配方，从正式目录取输入/产出量，对两个样例16刻逐物种相等；同时从成功PC事件重新统计端口，每端每刻不超过1。它没有从被验tick.summary抄期望，也不调用Rust计数方法。

### 5.2 停止覆盖矩阵

“18轴都有拒收测试”成立；“全部运行停止分支已逐条有语义触发测试”不能由此推出。两种覆盖分开如下。

| 停止类别 | 实际到达的测试/证据 | 范围 |
|---|---|---|
| 18个Stop轴取未支持值 | `config_axes_and_all_stop_values`；本轮18次公开CLI request | 每个轴名和位置均核；request统一返回unsupported |
| 阻尼无终点、缺分支 | `damping_no_terminal`、`damping_paths_and_missing_branch` | 真实求值为unresolved；不同于上行请求入口 |
| 仓库空格可观察竞争 | `warehouse_competition_atomic_stop` | 核判前状态与交付账不变 |
| 运行期拿取/离线/调试/重建/建造 | `external_stop_positions`；`revision_external_action_references`及旧offline-control探针 | 核具体时间/事件位置；部分单元直接改内部输入，公开解析对照由新增测试/探针补充 |
| periodic_lift | `external_stop_positions`与本轮CLI request | 只支持明确停止，没有周期提升算法或证明 |
| 空/迟到/充分补矿、覆盖上界 | `revision_ore_history_sufficiency`、`revision_ore_depletion_is_not_a_movement_guard`、`ore_history_horizon_stop`，CLI旧探针 | 有合法补给对照，耗尽不会伪装成物理不可动后继续completed |
| 中途种子/不支持排序 | `seed_and_parameter_negatives` | 目前覆盖in_closure及v2排序；不据此声称所有语法变体均测遍 |
| after_closure记录 | `revision_reports_and_after_closure_stop`、CLI负例、旧after-closure探针 | 输出为unresolved(output.start_anchor)；引擎续跑另有成功对照 |
| 资源截断及停止实例封存 | 上述闭包测试 | 不把资源边界当合法闭包 |
| 输入别名/缺字段/非法类型等 | tests_revision、CLI6负例、原否证探针 | 已列代表性回归通过，不宣称所有invalid分支有分支覆盖率证明 |

桥先接平局、两项准入口设定编辑停止轴、离线各细分后效轴没有各自独立游戏场景的完整覆盖证据：当前实现可以在更前面的结构/玩家操作/离线总入口停止，不能把18次枚举请求误写成这些机制都实际执行或逐一触发。本轮没有把这一覆盖限制另外凑作发现；第四轮明确要求的改轴拒收、玩家拿取与periodic_lift均已有测试。

### 5.3 第一轮修订及KQ未决

第一轮14项的对应测试与公开入口复跑总体通过；补矿短缺、历史顺序相容/冲突、事件历史别名、外部动作引用、无关分支、输入类型及初始仓库标签的旧否证输入均保持预期状态。原身份独立验收测试的5种变异在Cargo中通过，不单靠重新计算整份记录才拒绝。制造证据不再把只开工的两刻误记为输出分支，完成成功和completed受阻分别有正例；仓库两轴已生成逐边界报告。不过Python对这些报告的验收仍有§3的遗漏。

KQ-02是现行转移§1与输出§2的起点冲突。当前安全停止明确，不能称after_closure记录功能完整；同时 `after_closure_restart`和`explicit_step_anchors`证明合法引擎续跑没有被取消。KQ-01、KQ-03–KQ-05仍为非空补矿编码、新标签冲突接口、单级阻尼调用前件及身份维护日志接口的已登记问题；本轮不擅自裁定或修改规格，不把它们重复编号成新发现。

## 6. 独立手推：关闭粉碎机的制造开关

### 6.1 输入和期望来源

以正式样例“混做粉碎机两下游”为底，仅将 `settings.switches`中crusher的manufacture.enabled从true改为false。布局、99轴、事件全序、仓库初值、输入种子、两台研磨机开关和补矿史不变。复制到复核目录时catalog和axis_registry路径改为对应原文件的绝对路径，仅为维持引用。

输入见[手推输入](r2-测试与证据/手推-关闭粉碎机-input.json)，先写出的物理期望见[expected.json](r2-测试与证据/手推-关闭粉碎机-expected.json)，真实CLI输出见[record.json](r2-测试与证据/手推-关闭粉碎机-record.json)，比较投影见[actual-projection.json](r2-测试与证据/手推-关闭粉碎机-actual-projection.json)。期望不是复制Python或Rust结果：它由下述四步递推产生，脚本中先落盘期望，再调用内核。

### 6.2 逐刻推演

当前模板先访问源口→feed_belt（rank0），再访问feed_belt→crusher（rank1），最后访问crusher制造（rank11）；其余输送及下游制造因无货不产生成功物流。两矿初值均80000且无补给，四刻消耗源矿4件不会触及供给边界。

| t | 仓库源矿 | feed_belt | crusher存货格 | crusher缓存 | crusher阶段 | 成功PC事件，按真实顺序 | BC事件 | 闭包扫描轮数 |
|---|---:|---|---|---|---|---|---|---:|
| 0 | 79999 | 源矿1，入格0 | 空 | 空 | idle | J\|0\|0\|0 | 无 | 2 |
| 1 | 79998 | 源矿1，入格1 | 空 | 源矿1 | intake | J\|1\|0\|1；J\|1\|1\|0 | J\|1\|0\|11 | 3 |
| 2 | 79997 | 源矿1，入格2 | 源矿1 | 源矿1 | intake | J\|2\|0\|1；J\|2\|1\|0 | 无 | 3 |
| 3 | 79996 | 源矿1，入格3 | 源矿2 | 源矿1 | intake | J\|3\|0\|1；J\|3\|1\|0 | 无 | 3 |

1. **t=0。** 源口到空带满足两端权限，源矿出库1件；新入运输格在此刻年龄0，不能同刻进crusher。该扫描改变库存/预算，下一完整扫描边界首次重复，故2轮。依据：规则滞留、转移§3.1/§3.3/§5.1。
2. **t=1。** 首个模板先看到带满，不能再送；rank1把已滞留1 tick的旧矿送入crusher。rank11虽制造关闭，仍按§4.2第2步把一批原料搬入缓存、锁定粉碎-源矿并置intake；第3步因开关关闭不能置working，也没有完成日程。下一扫描rank0向腾空带补一件，第三扫描边界首次重复。依据：规则缓存格/需电功能，转移§4.2关于内部搬运不额外需电及intake开工条件。
3. **t=2。** 旧带矿进入crusher存货格；缓存仍处于intake，不能重新归集下一批。下一扫描补带，最后重复，共3轮。停用状态不计工作量，remaining为null，待事件仍空。依据：转移§2.1/§4.2。
4. **t=3。** 同理存货格由1增到2，未超50；缓存1不变，下一扫描补带，3轮闭包。除feed_belt、crusher存货格和缓存格外，其余16格均空，两台下游progress仍等于初始idle。依据：转移§4.1/§4.2和容量50。

每刻全基地源矿账分别为 `79999+1`、`79998+1+1`、`79997+1+1+1`、`79996+1+2+1`，均80000；无完成转换、无交付。其余五种初始仓库物品均80000，运输成功各使用其端口至多一次。四刻均没有制造完成事件、没有pending。

### 6.3 比较范围与一次投影纠正

最终比较全部19格的物种和数量、运输格entered_at、仓库源矿及其余初始物种计数、crusher阶段/remaining、下游完整progress、pending、全部成功PC的实例id和顺序、BC实例及scan_rounds，逐项通过。

最初脚本误把非运输格的entered_at也按null作强制相等，首次比较因此失败；物理库存、事件和轮数没有差异。回源《内核输入》§6库存字段明确允许Time或null，强制可核年龄只针对运输；本配置 `residence.nontransport=zero`也无额外滞留前件。因此只在比较投影中去除非运输到达时间约束，保留全部运输年龄检查，**物理手算期望及原黄金均未改变**。首次原始到达时间投影和失败日志分别保存在[with-nontransport-times.json](r2-测试与证据/手推-关闭粉碎机-actual-with-nontransport-times.json)、[首次投影检查](r2-测试与证据/手推首次投影检查.txt)，不把这次复核脚本错误列为内核缺陷。

该变体证明了这条具体输入下“停用仍可归集、不开工、后续库存累积及同刻补带”的行为；不证明玩家能精确构造此起点，不证明完整生产、循环态或目标。

## 7. 交付结论

本轮保留发现仅 **KR-r2-L2-01（重要）**：Python记录验收的逐轴及顶层身份关联缺口。93项测试、Clippy、黄金/完整差分、真实排列试验、18轴请求、14旧探针、独立16刻物料账和4刻手推的通过结果均有原始文件。KQ-02等规格未决继续保留；不存在凭当前通过结果宣布全部接口或全称认证完成的结论。

机器汇总见[findings.json](r2-测试与证据/findings.json)。
