[pytest]
addopts = --basetemp=.pytest_tmp
markers =
    slow: 重型/慢集成测试(端到端 run_outer_search、并行 wave、campaign resume、隔离 replay 等,单条可达分钟级)。快 gate 用 `-m "not slow"` 跳过保持秒级;慢测试由 preflight_gate.py --slow-tests 的专用慢 lane 用长超时真跑到完成,堵住「超时吞掉慢 soundness 测试」的盲区。
