ZMD RESEARCH WORKTREE STATE
===========================

Updated: 2026-08-25
Branch: research/main
Mode: RESEARCH
Active campaign: zero_condition
Campaign file: research_lab/campaigns/zero_condition/CAMPAIGN.txt

Program versus campaign
-----------------------
The research worktree owns the complete program in PROGRAM.txt. The active
campaign is only the current point of attack.

Current project position
------------------------
The lower side is still empty: there is no complete whole-layout witness. The
current zero-condition placement-plus-power master can produce a full mandatory
placement, but downstream evidence shows that the old placement / binding /
routing partition transports too little of the actual topological cause of death.

The decisive research update is no longer merely "routing is hard". Fixed-layout
probes and the first checked feedback replay now show a causal progression:

  * complete binding selections can be rejected immediately by blocked fronts;
  * after front filtering, coarse free-space component support can make binding
    itself infeasible before exact routing starts;
  * for A1, blue_iron_block alone is an inclusion-minimal commodity core, and a
    three-cell closed pocket yields a sound four-pose judgment;
  * E001 transported that judgment into group-semantic pose-bool literals and
    changed the master candidate with one constraint: only the implicated power
    pole moved one cell right;
  * the replacement passes static port domains and terminal uniqueness, then
    becomes INFEASIBLE at commodity component support before exact routing;
  * E002 shows this is not one exceptional commodity: every one of the 19 routed
    commodities is individually component-infeasible, with all 19 statuses
    reproduced by fresh complete model rebuilds;
  * E002 also reproduces stale repeated-solve CP-SAT sufficient-core literals, so
    that diagnostic API is excluded from future proof-bearing use in this runtime;
  * E004 removes hard component equalities, minimizes component mismatch in 19
    fresh models, and still proves a positive optimum for every commodity (4 to
    32 mismatches), with every optimum witness agreeing with production precheck;
  * E005 shows 95 of A1's 98 components survive E001 unchanged, component shapes
    compress strongly but exact boundary causes do not, and the historical F4
    path has mathematics without live state, generator, compiler, or consumer;
  * E006 turns permeability into a positive construction signal: a complete-hint
    LNS raises free adjacency from 1081 to 1166, reduces components from 97 to 91,
    and removes ten unpriced residual power poles, producing a new master candidate;
  * E007 sends that candidate to a fresh all-rules-hard component binding model,
    which proves INFEASIBLE in milliseconds before exact routing;
  * E008 shows the proxy still moved the real interface: total exact mismatch
    falls 281 to 214 and 13/19 commodities improve, but 602 more binding patterns
    are front-pruned, exposing a connectivity-versus-terminal-exposure tradeoff;
  * E009 rejects the raw global front_clear sum, derives selection-conditioned
    pattern-survival coefficients from E006, and predicts +1085 meaningful
    patterns; the fixed candidate measures +1101 while components fall 91 to 83
    and the largest grows 405 to 414, though component binding remains INFEASIBLE;
  * E010 proves a second broad mismatch reduction: 281 -> 214 -> 196 across
    E001/E006/E009, with 11 commodities improved from E006 and none yet at zero.

This is enough to reopen the binding-to-routing seam as a derived research
object. The old point-nogood package is no longer the default feedback language,
and the current layer count and cut families are not presumed correct.

Live question
-------------
Can a radius-limited permeability step around E009 improve free adjacency while
preserving E009's contextual selected-pattern score, and does the jointly guarded
candidate reduce exact component mismatch enough to approach a component-compatible
binding?

Current campaign posture
------------------------
No detailed multi-step plan is frozen. The current replacement has already
located the first interface wall at component support, not front occupancy or
duplicate terminal keys. Commodity minimization then produced the stronger 19/19
result, so commodity-by-commodity cut mining is no longer the default move.

The differently shaped mismatch objective has now confirmed that all 19
commodities remain fragmented even when binding optimizes for one commodity at a
time. The observed optimum boundaries are mixed: many have only 3 to 10 owners,
while components touching the main 405-cell region can have 154.

E005 has selected the representation fork. The one-cut repair changed only three
old components into two new ones; 95 exact components and their boundary semantics
survived. Geometric symptoms repeat, but full causal motifs remain a long tail.
The existing F4 cannot consume the actual object.

E006 has now produced the candidate rather than merely proposing the proxy. The
first monolithic objective solve spent its budget in presolve; the same semantic
objective, carried by a complete-hint LNS container, found a geometry with 85 more
free adjacencies, six fewer components, and ten fewer power poles. This separates
where the knowledge belongs from which solver container can consume it.

The real-interface test and comparative atlas are now complete. E006 improves
component topology broadly but worsens ordinary port-domain survival. This is not
a reason to discard permeability; it proves the two resources share placement
variables and must eventually be optimized together.

The raw global front_clear sum has now been rejected without burning a solve: it
counts fronts of unselected poses and moves opposite real domain survival. E009's
selected-pose pattern linearization repairs the object space and is locally
faithful to within 16 patterns. Its trust-region candidate improves both actual
port-domain survival and component morphology. E010 then confirms a second broad
reduction in exact mismatch, but every commodity remains positive.

The next experiment combines the two earned resources rather than alternating
blindly. Around E009, it maximizes exact free adjacency while imposing a contextual
lower bound that preserves E009's selected-pattern linearization score, retains
the current pole cap, and limits pose churn. A candidate is immediately measured
by actual pattern survival, component-aware binding, and the mismatch atlas. If
this jointly guarded step cannot improve the interface, the residual component
condition moves into a smaller joint placement-binding model instead of another
scalar objective.

The experiment must continue to serve the zero-condition witness directly.
General outer-loop lessons are welcome outputs, but method validation must not
displace the whole-layout campaign.

Architecture status
-------------------
The research tree has its own front door, program, live state, campaign surface,
ignored runtime area, role-specific Claude Code memory, mode checker, and
packet-only promotion boundary. The old combined-tree memory was extracted into
stable research habits; dynamic campaign state remains here and in the campaign
instead of being copied into auto-memory. The old main tree is history/material
only. The independent certification tree now exists at
/home/zhuran24/.devspace/worktrees/zmd-pj-89d26f57 on certification/main; it is
read-only from research sessions and receives only frozen candidate packets.

Orientation guards
------------------
* Do not redefine the research tree as the zero-condition project.
* Do not let future generality displace the current lower-side bottleneck.
* Do record whether a representation is instance-specific, premise-transferable,
  or likely to obstruct the future expected target.
* Do not send research writes into the history/material tree.
* Do not promote research conclusions from this tree into certified claims.
* When a result matures, freeze a compact packet for a fresh
  certification/main session.
