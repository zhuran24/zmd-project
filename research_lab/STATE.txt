ZMD RESEARCH WORKTREE STATE
===========================

Updated: 2026-08-26
Branch: research/main
Mode: RESEARCH
Active campaign: zero_condition
Campaign file: research_lab/campaigns/zero_condition/CAMPAIGN.txt

Program versus campaign
-----------------------
The research worktree owns the complete program in PROGRAM.txt. The active
campaign is only the current point of attack.

Current project position
------------------------
The lower side is still empty: there is no complete whole-layout witness. The
current zero-condition placement-plus-power master can produce a full mandatory
placement, but downstream evidence shows that the old placement / binding /
routing partition transports too little of the actual topological cause of death.

The decisive research update is no longer merely "routing is hard". Fixed-layout
probes and the first checked feedback replay now show a causal progression:

  * complete binding selections can be rejected immediately by blocked fronts;
  * after front filtering, coarse free-space component support can make binding
    itself infeasible before exact routing starts;
  * for A1, blue_iron_block alone is an inclusion-minimal commodity core, and a
    three-cell closed pocket yields a sound four-pose judgment;
  * E001 transported that judgment into group-semantic pose-bool literals and
    changed the master candidate with one constraint: only the implicated power
    pole moved one cell right;
  * the replacement passes static port domains and terminal uniqueness, then
    becomes INFEASIBLE at commodity component support before exact routing;
  * E002 shows this is not one exceptional commodity: every one of the 19 routed
    commodities is individually component-infeasible, with all 19 statuses
    reproduced by fresh complete model rebuilds;
  * E002 also reproduces stale repeated-solve CP-SAT sufficient-core literals, so
    that diagnostic API is excluded from future proof-bearing use in this runtime;
  * E004 removes hard component equalities, minimizes component mismatch in 19
    fresh models, and still proves a positive optimum for every commodity (4 to
    32 mismatches), with every optimum witness agreeing with production precheck;
  * E005 shows 95 of A1's 98 components survive E001 unchanged, component shapes
    compress strongly but exact boundary causes do not, and the historical F4
    path has mathematics without live state, generator, compiler, or consumer;
  * E006 turns permeability into a positive construction signal: a complete-hint
    LNS raises free adjacency from 1081 to 1166, reduces components from 97 to 91,
    and removes ten unpriced residual power poles, producing a new master candidate;
  * E007 sends that candidate to a fresh all-rules-hard component binding model,
    which proves INFEASIBLE in milliseconds before exact routing;
  * E008 shows the proxy still moved the real interface: total exact mismatch
    falls 281 to 214 and 13/19 commodities improve, but 602 more binding patterns
    are front-pruned, exposing a connectivity-versus-terminal-exposure tradeoff;
  * E009 rejects the raw global front_clear sum, derives selection-conditioned
    pattern-survival coefficients from E006, and predicts +1085 meaningful
    patterns; the fixed candidate measures +1101 while components fall 91 to 83
    and the largest grows 405 to 414, though component binding remains INFEASIBLE;
  * E010 proves a second broad mismatch reduction: 281 -> 214 -> 196 across
    E001/E006/E009, with 11 commodities improved from E006 and none yet at zero;
  * E011 preserves E009's contextual exposure score while buying +29 free
    adjacencies, +30 actual meaningful patterns, +20 free cells, and -5 poles;
  * E012 shows the residual rule is no longer explained by those scalars: exact
    mismatch worsens 196 to 198, leaving all commodities positive despite both
    proxy gains;
  * E013 transports the 196 E009 mismatch boundaries into 313 real consumer
    literals and 151 coverage classes. Sixteen dispersed classes touch 87.8% of
    observations, but 15 are manufacturing group poses; a pole-only or single-
    window repair is therefore the wrong next object;
  * E014 exhausts 425 exact one-literal fixed-outside alternatives. Static owner
    domains reject 287; E015 reclassifies three protocol-core moves as aggregate
    base-binding capacity failures and confirms 135 later component failures. The
    selected pole is power-locked and no candidate reaches routing;
  * E015 proves E009's one-binding optimum is 200, then finds a unique unary move,
    crusher_sandleaf pose 2186 -> 15830, with exact shared optimum 186. Its
    residual boundary and independent unary gradient both select crusher_source
    pose 6191 as the next partner;
  * E015's optimum-face audit shows only blue_iron_ore and source_ore trade four
    units; their fixed sum falls 23 -> 19 after the best move. It also exposes the
    missing aggregate generic-output capacity gate between owner domains and
    component support;
  * E016 fixes the unary move and exhausts 55 crusher_source partner poses. The
    unique best pair uses pose 6102 and proves 186 -> 178; all 19 commodities
    remain positive and the ore-pair sum falls again, 19 -> 18;
  * E017 evaluates four third-member families and proves 178 -> 176. Twelve
    candidates tie across crusher-source and refinery groups, while the residual-
    coverage leader grinder pose 7754 worsens to 180. Coverage proposes; shared
    binding must rank;
  * E018 exhausts 152 fourth-member alternatives on one representative triple.
    Thirty-six shared bindings are OPTIMAL and refinery pose 5343 -> 3338/3339
    lowers 176 -> 174, but every commodity remains positive. Its exact face keeps
    the ore-pair sum at 18, and the predeclared stop rule moves the outer loop to
    the twelve-state E017 beam;
  * E019 applies grinder pose 7754 across all twelve beam seeds. Every branch has
    exactly two owner-domain failures and one OPTIMAL child at 178, so all parents
    worsen by two. The action is branch-invariant despite twelve placement and
    binding identities; causal coverage and continuation value are now separate
    outer-loop ledgers;
  * E020 applies the known-composing refinery pose 5343 across eight eligible
    seeds. Every branch reaches best objective 174, but alternatives range 51-63,
    base-feasible children 9-23, and the child-objective histograms and best
    geometries differ. Beam state now needs score, action-domain, and witness
    identities as separate layers;
  * E021 applies crusher-source pose 6125 to four objective-174 free-space class
    representatives. All four improve exactly one unit to 173 through pose 6123,
    but feasible child counts remain 11, 13, 23, and 11. The optimum face keeps
    only the ore-pair exchange and lowers its fixed sum 18 -> 17;
  * E022 reconstructs each retained endpoint without re-solving away its witness.
    Full residual rankings differ, yet 312 literals are common and four appear in
    every top ten. Grinder pose 7754 touches exactly 16 observations in all four
    states and ranks 1, 1, 1, and 2; class 4 instead ranks crusher-blue-iron pose
    2053 first;
  * E023 replays grinder pose 7754 on all four parents. Each has two owner-domain
    failures and one OPTIMAL pose-7755 child at 175, uniformly worsening 173 ->
    175. This exactly repeats E019's earlier +2 response, so the common coverage
    leader is retired from the current unary ledger;
  * E024 exhausts the class-4-specific crusher-blue-iron pose 2053. Forty-two of
    55 alternatives empty an owner domain; 13 are OPTIMAL and pose 5278 proves
    173 -> 168. The gain repairs four non-ore fixed commodities while the ore-pair
    optimum-face sum remains 18;
  * E025 recovers the exact E024 binding endpoint after same-seed parallel replay
    first returns another optimum. This exposes digest-only state persistence as
    insufficient. The 168 surface still ranks the retired grinder action first;
    after applying the action ledger, packaging-battery pose 6189 is the strongest
    common action and crusher-sandleaf pose 2155 is the leading 168-specific one;
  * E026 exhausts all 12 packaging-battery alternatives across the live beam. Every
    candidate empties a front-filtered owner domain with the same three rejection
    patterns, so the action is retired before component support rather than being
    mislabeled as another topology failure;
  * E027 exhausts 51 crusher-sandleaf alternatives on the objective-168 branch.
    Forty-seven empty an owner domain and four shared bindings are OPTIMAL; the
    best child reaches only 167. The predeclared two-unit material-gain threshold
    therefore stops serial unary motion, and the exact endpoint is fully persisted;
  * E028 releases packaging_battery pose 6189 and grinder_dense_blue_iron pose
    6049 together. Only two of 47 pairs survive static domains, but both reach
    objective 166. The winner swaps the two operations across their existing 6x4
    footprints, improving the shared interface without changing any free cell;
  * E029 reconstructs the fully materialized 166 endpoint and enumerates 8,486
    same-type, cross-operation swaps that preserve occupied geometry. A diversified
    twelve-swap portfolio touches 26-29 residual observations per action, led by
    crusher_sandleaf pose 2155 <-> refinery_blue_iron pose 5327;
  * E030 proves the assignment layer is reusable: all twelve swaps pass static
    domains and reach OPTIMAL shared bindings with objectives 163-169. The best
    swaps crusher_blue_iron pose 2105 with refinery_steel pose 2947, lowering
    166 -> 163 while the free-cell set remains identical;
  * E031 replaces pair-by-pair guidance with three exact five-footprint assignment
    neighborhoods. Ninety-seven non-identity permutations yield 77 OPTIMAL shared
    bindings. The 3x3 arm reaches 161 through two distinct assignments, while 5x5
    and 6x4 bottom out at 165 and 164. The best face lowers the ore-pair sum to 15
    and all 19 commodities remain positive;
  * E032 fully materializes the second 161 endpoint. The two residual surfaces
    share 9/10 top literals and the same leader; their operation-swap top tens
    overlap 8/10 and their twelve-action portfolios overlap 10/12. They share a
    common leading swap but retain two branch-specific pose-2963 actions;
  * E033 gives the exact stop signal for external assignment actions. The common
    swap improves both 161 parents only to 160, while both branch-specific swaps
    worsen to 163. The scalar responses match, but all child placement, binding,
    and port-spec identities remain distinct. All 19 commodities stay positive.

This is enough to reopen the binding-to-routing seam as a derived research
object. The old point-nogood package is no longer the default feedback language,
and the current layer count and cut families are not presumed correct.

Live question
-------------
E064 successor: the four pole near misses retain their exact objective-one status,
but their source-isolated representative is source-only component 4 rather than
component 1. Under the smallest pole move 3725 -> 3794, which complete-face-
derived second object can remove the final fine-powder mismatch while preserving
qiaoyu compatibility, and does that signature candidate survive the full
conditional-binding consumer?

Under the representative E062 pole near miss, which smallest second-object move
removes the sole source-only fine component 1 and reaches the corrected two-zero
signature condition, without expanding into an unconstrained all-pairs portfolio?

Current campaign posture
------------------------
No detailed multi-step plan is frozen. The current replacement has already
located the first interface wall at component support, not front occupancy or
duplicate terminal keys. Commodity minimization then produced the stronger 19/19
result, so commodity-by-commodity cut mining is no longer the default move.

The differently shaped mismatch objective has now confirmed that all 19
commodities remain fragmented even when binding optimizes for one commodity at a
time. The observed optimum boundaries are mixed: many have only 3 to 10 owners,
while components touching the main 405-cell region can have 154.

E005 has selected the representation fork. The one-cut repair changed only three
old components into two new ones; 95 exact components and their boundary semantics
survived. Geometric symptoms repeat, but full causal motifs remain a long tail.
The existing F4 cannot consume the actual object.

E006 has now produced the candidate rather than merely proposing the proxy. The
first monolithic objective solve spent its budget in presolve; the same semantic
objective, carried by a complete-hint LNS container, found a geometry with 85 more
free adjacencies, six fewer components, and ten fewer power poles. This separates
where the knowledge belongs from which solver container can consume it.

The real-interface test and comparative atlas are now complete. E006 improves
component topology broadly but worsens ordinary port-domain survival. This is not
a reason to discard permeability; it proves the two resources share placement
variables and must eventually be optimized together.

The raw global front_clear sum has now been rejected without burning a solve: it
counts fronts of unselected poses and moves opposite real domain survival. E009's
selected-pose pattern linearization repairs the object space and is locally
faithful to within 16 patterns. Its trust-region candidate improves both actual
port-domain survival and component morphology. E010 then confirms a second broad
reduction in exact mismatch, but every commodity remains positive.

The jointly guarded scalar step is now complete. It proves that permeability,
actual pattern survival, pole economy, and free-cell quantity can improve together,
but E012 supplies the stop signal: exact component mismatch does not follow those
proxies beyond the E009 point.

E013 has now selected the neighborhood without presuming its geometry. The
corrected exact coverage curve is 19, 36, 67, 115, 172, and 196 observations for
budgets 1, 2, 4, 8, 16, and 32. The minimum counts for 50%, 75%, 90%, and 100%
coverage are 7, 13, 18, and 26. Small-budget optima are manufacturing-heavy, and
a single 24x24 window reaches only 43.4%.

E014 has now separated nominal coverage from actual mobility. Fifteen
manufacturing targets can move, but most footprint changes destroy a complete
binding domain and every domain-valid candidate still fails component support.
Same-footprint port-mode changes also fail, so neither geometry-only nor binding-
only unary repair is sufficient. The selected pole cannot move alone while exact
power and the outside factory remain fixed.

E015 has now supplied the joint gradient. The sum of independent commodity minima
understates E009's attainable one-binding optimum by four. That optimum has a
one-dimensional generic-output tradeoff: blue_iron_ore and source_ore exchange
four units while the other 17 values remain fixed. The best unary placement move
reduces the exact shared objective by 14 to 186, but every commodity remains
positive.

The interface ladder is also revised. Three protocol-core alternatives have
nonempty owner domains but only 48/49 generic-output slots for exact demand 52 and
are infeasible with every component guard disabled. Aggregate binding resource
feasibility therefore belongs before component support; those candidates must
not be attributed to F4-like topology.

E016 confirms that the first two gradients compose rather than cancel. The best
crusher-source alternative lowers the shared optimum by eight more units to 178.
Its optimum face still has only the four-unit generic-output exchange; every
other commodity value is fixed, so the pair improvement is not an arbitrary
binding endpoint effect.

E017 confirms a third move can compose, but only by two units and with a twelve-
way tie across three operation families. The residual-coverage leader itself does
not compose. This is the first strong warning that one greedy candidate path is
no longer a faithful outer-loop state: equally scoring geometries may expose
very different next neighborhoods.

E018 has completed the fourth-member stop test. The best representative branch
reaches 174 through refinery pose 5343 -> 3338/3339, while the grinder coverage
leader again fails to compose. This is a real gain, but it does not rescue the
single-line state representation: E017's twelve objective-176 triples remain
live, and they differ in placement, free-space, port-mode, and binding-selection
identity.

E019 now calibrates what that diversity means. The same grinder action is
completely invariant across all twelve states: two domain failures and one exact
178 child per seed. The beam keeps the twelve parents and discards all grinder
descendants. Representational difference is therefore necessary metadata, not
sufficient evidence of continuation value. Boundary coverage proposes actions;
only cross-state exact replay tells whether a branch deserves retention.

E020 has tested the first known-composing beam action. All eight eligible seeds
reach 174, so they are equivalent at the best one-step value. They are not
equivalent as search states: candidate counts, domain-survival counts, objective
histograms, and best replacement geometries differ substantially. The outer loop
therefore records three separate identities: score/optimum-face,
action-response domain, and placement/binding witness.

E021 now shows that the next crusher-source action is also exact-value invariant:
all four class representatives reach 173 through pose 6123. The shared optimum
face remains one-dimensional and its ore-pair sum falls to 17, so the gain is
structural rather than an arbitrary binding endpoint. Yet the four action domains
remain unequal and all 19 commodities stay positive.

E022 preserves the retained binding endpoints and shows that the beam shares a
portfolio rather than one complete ranking. The robust common leader is grinder
pose 7754 with equal coverage 16 in every state, but class 4's local leader is
crusher-blue-iron pose 2053. The pairwise top-10 overlap ranges from 0.25 to
0.818, so the common probe cannot erase branch-specific alternatives.

E023 now gives the second exact rejection. The action worsens every objective-173
parent by two, exactly as it worsened every objective-176 parent in E019. Its
boundary coverage is causal-touch information, not continuation value, and the
four original objective-173 parents remain the live beam.

E024 validates the branch split. Class 4's unique leader improves the exact
shared objective by five, 173 -> 168, while the common leader had worsened every
branch by two. The beam therefore cannot be represented by one score-class
representative or one global residual ranking.

The live beam contains one objective-168 child and three objective-173 parents.
E025 recomputed the 168 residual surface without replacing its frozen binding
endpoint. E026 then showed that the strongest non-retired common proposer has an
empty exact action domain in every state: all three alternatives destroy one or
two front-filtered owner domains before component support. This response can be
stored once as an action-domain signature shared by the four parents.

E027 has now closed the serial-unary branch. Only four of 51 alternatives survive
static domains, and the best exact shared objective is 167, a one-unit gain below
the predeclared material threshold. The 167 child is diagnostic, not retained as
the next greedy lineage.

E028 validates the first simultaneous placement layer, but not by the most literal
repair. Packaging pose 6186 remains statically dead even when the implicated
grinder is released. The two surviving pairs instead exchange packaging and
grinder operation/port semantics across the two current 6x4 bodies. Free space is
identical, while the exact shared objective falls 168 -> 166.

The objective-166 child is now represented by geometry, operation assignment,
and a fully materialized binding endpoint. E029 shows that operation assignment
is not a one-off pair trick: 8,486 same-type cross-operation exchanges preserve
occupancy, and a compact twelve-action portfolio touches a substantial residual
surface. The E028 reverse is excluded because its exact value is already known.

E030 validates operation-to-footprint assignment as a standing constructive
layer. Every proposed swap is base-feasible, but exact objectives range 163-169;
the top-coverage action worsens to 169 and the portfolio's lowest-coverage member
wins at 163. Boundary incidence can generate a finite portfolio but cannot rank
it. The objective-163 optimum face still varies only the ore pair at fixed sum 16;
blue_iron_block -1 and steel_block -2 are the structural gain.

E031 now validates a bounded multi-footprint assignment neighborhood. The selected
3x3, 5x5, and 6x4 neighborhoods contain 59, 19, and 19 non-identity assignments.
Only the 3x3 arm improves the parent, reaching 161 twice. One optimum is a pair
exchange; the other is a three-way reassignment with a different binding endpoint.
The best audited face keeps only the ore-pair exchange and lowers its fixed sum to
15. Equal score is therefore another live state split, not permission to retain
one arbitrary witness.

E032 now shows that the split is narrow but real. The second endpoint is fully
materialized. The two states share the same residual and operation-swap leaders,
yet their full rankings and two portfolio entries differ because pose 2963 carries
different operations. They may share a contextual action ledger, but they cannot
be merged before exact continuation is measured.

E033 supplies the final external-action measurement. The common swap gives the
same marginal 161 -> 160 response on both states, and the analogous branch actions
both worsen to 163. Those contextual responses can be shared, but the resulting
endpoints remain distinct witnesses. External assignment enumeration therefore
stops.

E034 implements the replacement rather than merely proposing it. Twenty
footprint-operation variables and 123 conditional binding-pattern variables share
one CP-SAT model with the unchanged outside binding system. Both objective-160
warm states independently recalibrate to OPTIMAL 160, and the free solve also
proves OPTIMAL 160. The five 3x3 footprints are exactly saturated in the joint
language; returning to another swap or permutation inside that context is closed.

E035 expands that exact context by a residual-selected five-footprint 6x4 block.
The selector raises boundary coverage from 51 to 97 and introduces four operation
families. Both regression assignments still calibrate to 160. The free model then
proves OPTIMAL 159 by exchanging grinder_dense_source and
grinder_fine_buckwheat between poses 7017 and 7895 while leaving the 3x3 assignment
unchanged. The audited ore-pair sum remains 15; dense_source_powder 7 -> 6 is the
fixed gain. All 19 commodities remain positive.

E036 adds the exact-selected five-footprint 5x5 block, which raises touched
observations from 96 to 125. The parent recalibrates to 159 and the alternate
regression state to 160. Releasing all three blocks still proves OPTIMAL 159. The
5x5 assignment changes buckwheat, buckwheat_seed, and sandleaf_seed but the net
objective stays fixed; the audited ore-pair sum remains 15.

E037 then removes an artificial boundary inside the 6x4 assignment layer. Five
new residual footprints are merged with the live five into one ten-footprint
block. Both objective-159 states recalibrate exactly, while the free model proves
OPTIMAL 157 and also switches to the alternate 3x3 branch. The audited ore-pair
sum remains 15; the two-unit gain is fixed outside that face.

The current twenty-footprint assignment context touches 137 of 157 observations.
Remaining five-footprint marginal coverage is +9 for 3x3, +6 for 5x5, and zero
for 6x4, so E038 opens the merged ten-footprint 3x3 discriminator.

E038 correctly stops at an extension-seam defect rather than accepting a lower
prototype value. E039 repairs that seam. Conditional owners are registered before
native domain construction and always retain an inactive pattern; seven
operation/footprint pairs with no active pattern become inactive-only choices
instead of disappearing or killing the model. The frozen E037 and E036
assignments recalibrate exactly at 157 and 159.

The free repaired model is OPTIMAL 157 and ordinary fixed-layout replay agrees.
Its exact face varies only the ore pair at fixed sum 15; every other commodity is
fixed and all 19 remain positive. The merged 3x3, 5x5, and merged 6x4 assignment
context is therefore faithfully saturated on the current frozen poses. E040 shows
that calling this "fixed geometry" was too coarse because pose identity also fixes
port mode.

E040 reconstructs all 157 residual mismatch observations, selects sixteen current
literals covering 136 of them, and exhausts 328 fixed-outside pose alternatives.
The best child reaches 154 by changing planter_sandleaf pose 9653 to same-footprint
pose 9650. Every occupied and free cell is unchanged; the audited ore-pair sum
remains 15, while sandleaf falls by one and sandleaf_seed by two. The best actual
body relocation reaches only 156.

E041 completes that fixed-body extension. It creates native conditional owners
for 195 (body destination, mode, operation) choices, exactly recalibrates the E039
157 and E040 154 endpoints, and proves the free joint model OPTIMAL 152 with best
bound 152. Seven destinations change same-footprint mode while operation
assignment and binding change jointly; occupied geometry remains byte-identical.
The exact ore-pair face falls from fixed sum 15 to 14, and all 19 commodities
remain positive.

E042 rebuilds the 152 residual and classifies all 268 admissible current literals.
The exact six-target unadmitted-mode portfolio reaches only 151. The exact
six-target body portfolio reaches 150 by shifting grinder_dense_blue_iron pose
6228 to 6098, changing the free partition from 79 to 77 components. Both audited
faces keep the ore-pair sum at 14; the body's fixed gain is buckwheat -1 and
sandleaf_powder -1.

E043 carries the faithful joint middle layer across two distinct E042 body
geometries. Seed A, the in-block 6x4 move, calibrates at 150 and reoptimizes to
OPTIMAL 147. Seed B, the outside 3x3 move, calibrates at 151 and reoptimizes to
OPTIMAL 150. Ordinary replay agrees. Seed A's exact face has ore-pair fixed sum 15;
its joint middle layer accepts one extra ore mismatch while reducing the fixed
ledger by four.

The fixed-assignment geometry ranking survives, but the three-unit and one-unit
joint gains prove that geometry cannot be carried with a stale middle-layer value.
Seed A's fully materialized 147 endpoint replaces the E042 atlas.

E044 rebuilds the 147 residual and finds 261 current literals with actual body
mobility. An exact budget-six portfolio covers 70/147 observations and produces
44 OPTIMAL ordinary body children. After removing geometries that already carry
joint values, two distinct new states are materialized at ordinary objective 146:
a one-cell 5x5 planter move with 76 components and largest component 479, and the
composition of the retained 6x4 move with an outside 3x3 relocation, also with 76
components but largest component 520.

E045 reconstructs the E043 Seed A context and verifies each E044 seed differs from
the immediate 147 parent by exactly one body move. Both ordinary 146 endpoints
calibrate exactly. Seed A's 5x5 planter geometry remains OPTIMAL 146 after freeing
the middle layer. Seed B's outside 3x3 geometry reaches OPTIMAL 145 and is the
strict winner; its singleton body selects same-footprint pose 5343. The audited
ore-pair sum remains 14, so the one-unit reoptimization gain lies in the fixed
commodity ledger.

Seed B's fully materialized 145 endpoint replaces the E044 proposer tie. E046
then integrates singleton body proposal and joint revaluation in one loop. Its
budget-six body portfolio covers 70/145 observations and finds nine novel free
geometries, but the best planter-buckwheat move reaches only joint OPTIMAL 144;
the second selected geometry remains 148. The objective-144 face keeps the ore
pair at fixed sum 14 and all 19 commodities remain positive.

The singleton-body axis therefore stops at its predeclared threshold. E047 rebuilds
the 144 residual and exact single-body response atlas, then forms a bounded pair
portfolio from domain-valid singleton actions and direct empty-owner rescue edges.
The six target set contains no internal direct-rescue edge. Nineteen pair specs
produce 16 ordinary OPTIMAL states, but every value is worse than the parent: the
best is 146 and almost every measured response is exactly additive. Three distinct
pair geometries are materialized at 146, 151, and 153.

E048 carries the faithful joint middle layer onto all three pair geometries. Their
ordinary endpoints calibrate exactly at 146, 151, and 153. Free joint values are
OPTIMAL 146, 149, and 150, so none equals or improves the objective-144 parent.
All 19 commodities remain positive.

The bounded complete-pair language is therefore retired, but E049 repairs the
missing rescue relation before building a larger native model. E047's empty-owner
mappings had been stringified and then compared directly with instance IDs; the
actual internal edge count is zero, yet that transport could never discover an
edge. Structured parsing exposes eight clean external edges.

Exhausting 212 owner alternatives under those source moves revives 55 OPTIMAL
states. The strongest packaging_battery_001 -> grinder_dense_source_003 relation
reaches ordinary 143 through two body-relocation mode variants; a third same-owner-
body variant reaches 144. All 19 commodities remain positive.

E050 rebuilds that faithful middle layer. All three ordinary endpoints calibrate
exactly at 143, 143, and 144. Free joint values are OPTIMAL 141, 141, and 139. The
ordinary-144 state, consisting of one packaging-battery body move and one grinder-
dense-source same-body mode change, becomes the strict continuation winner.

All three audited ore-pair sums remain 14, matching the objective-144 parent. The
five-unit improvement is therefore fixed commodity structure, not an optimum-face
exchange. All 19 commodities remain positive and routing is not reached.

The objective-139 Seed C now replaces the old 144 state. E051 first audits the
progress metric rather than continuing the scalar descent. The fixed state and
free joint context both reproduce OPTIMAL 139. Over the entire total-139 joint
face, the minimum positive-commodity count is exactly 19; requiring at most 18
positive commodities in the same context is INFEASIBLE. No internal mode,
operation-assignment, or binding choice can create even one zero.

The nearest obstruction is `fine_buckwheat_powder = 1`. Its three source grinders
all feed a source-only component on the right boundary. E052 exhausts all six
same-body mode surfaces and all 44 placement-/power-valid fixed-outside pose
alternatives of the three sources and three capsule sinks; none reaches target
mismatch zero. Source minima remain one, while capsule minima are four, three, and
one.

Four relevant 6x4 bodies were still outside the live assignment block:
`filling_capsule_002` and all three right-edge fine-powder grinders. E053 merges
them into one fourteen-body block and exactly recalibrates 139, but unrestricted
sum and target-zero solves remain UNKNOWN without incumbents or search branches.
The representation is faithful; the free symmetric assignment container is not
yet a usable proposer.

E054 supplies a causal symmetry breaker rather than broadening geometry. It tests
all nine swaps between three right-edge grinder bodies and three capsule bodies.
Six swaps are exact first-zero states. The selected grinder-004/capsule-003 swap
keeps occupied geometry unchanged and proves total mismatch 139 with 18 positive
commodities and `fine_buckwheat_powder = 0`. Fixed replay and production component
precheck agree exactly.

The active progress order is now positive commodity count first and total mismatch
second. E055 retains the selected causal relation, fixes every current port mode,
and releases the other twelve operation assignments in the fourteen-body 6x4
block. It proves the exact lexicographic optimum is still (18,139). The first
launch also catches another instance-name transport error: after operation
reassignment, the causal bodies must be addressed by block/destination, not by the
old symmetric instance names.

Operation assignment is therefore closed at this geometry and causal relation.
E056 then releases all admitted same-body modes in the merged 6x4 block while
keeping the causal relation, occupied geometry, and non-6x4 choices fixed. The
primary positive-count solve proves OPTIMAL 18, so this mode layer also cannot
create a second zero.

The secondary total solve and a direct total<=138 discriminator both remain
UNKNOWN. The E055 18/139 state is still a valid witness in the wider E056 context,
but 139 is not claimed secondary-optimal there. Because positive count is the
primary progress measure, the next step does not continue burning budget on that
secondary bound.

E057 selects qiaoyu_capsule because it is the smallest downstream obstruction and
shares the first-zero filling-capsule chain. Appending the sole outside 6x4 body
with a component-15 qiaoyu-output mode recalibrates 18/139 exactly, but its
operation-count-preserving three-operation cycle is INFEASIBLE. The body exposes a
direct terminal tradeoff: one mode places qiaoyu output in component 15 and fine
input in 54, while the reverse mode swaps those roles.

E058 then removes the residual block/operation-split doubt. It compiles every
native active pattern from all 38 current 6x4 bodies into 338 exact terminal-
signature options. Fine-only and qiaoyu-only are each feasible, but the two-zero
condition is INFEASIBLE even after all unrelated 6x4 operations are collapsed into
one `other` class. The fixed geometry therefore lacks a compatible fine-input /
fine-output / qiaoyu-output signature allocation.

E059 compresses that relaxed infeasibility into an exact tradeoff. With qiaoyu
zero hard, minimum fine mismatch is two; with fine zero hard, minimum qiaoyu
mismatch is one. The complete qiaoyu-zero optimum face has common fine components
{6,15}, one source-only component from {1,4,8,12,22,26}, and one sink-only
component from {21,54}; exact and other-collapsed pattern sets are identical.

One synthetic filling-capsule signature with qiaoyu output in component 15 and
fine input in any source-only component repairs the two-zero signature model. One
synthetic fine-grinder output in either sink-only component does not.

E060 then catches a proof-surface defect before geometry search: qiaoyu sink
component 15 was the materialized generic-input slot, not a fixed rule. The E055
protocol core has seven usable slots across components 15 and 39. Restoring exact
sink-slot choice leaves both corrected joint models INFEASIBLE, reproduces the 2/1
tradeoff, and reproduces the same twelve-pattern face; every optimum chooses
component 15. E058/E059's original fixed-sink surfaces no longer carry standalone
scientific authority, but their conclusion and filling target are independently
revalidated by E060.

E061 performs that scan exhaustively. It evaluates 10,016 placement-/power-valid
body-changing alternatives, quotiented into 4,714 distinct occupancy changes,
with candidate-specific components, target signatures, and generic sink slots.
No candidate is feasible and none is nonterminal in the corrected relaxation. It
also closes 538 non-6x4 same-body modes: 537 are signature-invariant and the one
protocol-core orientation remains INFEASIBLE; all 6x4 modes were already joint in
E060.

The one-object pose language is therefore closed for this first-zero state. E062
then asks a directional question rather than repeating the flat negative scan:
with qiaoyu zero hard, can one object reduce fine mismatch below the corrected
baseline two? Exactly five states reach one and none reaches zero. Four are the
same lower-right power-pole move family and leave only source-only component 1;
the fifth is a distinct 6x4 relocation leaving source-only component 40.

The pole family and 6x4 near miss are not automatically complementary: all four
direct combinations remain INFEASIBLE, and no already occupied 6x4 footprint in
the pole states supplies the missing filling signature. The next proposer is
therefore conditioned on one representative pole parent and the boundary/terminal
causes of component 1. It enumerates only second objects that can change that
relation, not the full one-object Cartesian square. A corrected-signature feasible
pair is sent to the full conditional binding consumer; a fully zero binding enters
routing immediately.

E064 then audits the executable identity of E057-E062. Default-cache imports
contain 83 module-level functions compiled from neighboring runner files, while a
fresh `PYTHONPYCACHEPREFIX` yields zero. Source-isolated reruns preserve every
scientific field of E057-E061. E062 preserves its exact distribution and all five
physical near misses, but the four pole states' selected presence witness changes
from source-only component 1 to component 4. The old component-1 interpretation is
retired; E063 now derives its causal set from the complete source-isolated
objective-one face.

The experiment must continue to serve the zero-condition witness directly.
General outer-loop lessons are welcome outputs, but method validation must not
displace the whole-layout campaign.

Architecture status
-------------------
The research tree has its own front door, program, live state, campaign surface,
ignored runtime area, role-specific Claude Code memory, mode checker, and
packet-only promotion boundary. The old combined-tree memory was extracted into
stable research habits; dynamic campaign state remains here and in the campaign
instead of being copied into auto-memory. The old main tree is history/material
only. The independent certification tree now exists at
/home/zhuran24/.devspace/worktrees/zmd-pj-89d26f57 on certification/main; it is
read-only from research sessions and receives only frozen candidate packets.

Orientation guards
------------------
* Do not redefine the research tree as the zero-condition project.
* Do not let future generality displace the current lower-side bottleneck.
* Do record whether a representation is instance-specific, premise-transferable,
  or likely to obstruct the future expected target.
* Do not send research writes into the history/material tree.
* Do not promote research conclusions from this tree into certified claims.
* When a result matures, freeze a compact packet for a fresh
  certification/main session.
