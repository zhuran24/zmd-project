E079 RESULT: K=47 BOUNDARY PACKING COMPILED TO AN EXACT 47-STATE MACRO
============================================================================

Status
------
Completed as research-only boundary-packing evidence. No full-layout, binding,
routing, throughput, certification, U/L, owner, production, pruning, or
publication effect.

Frozen evidence
---------------
Main result:
  research_lab/local/zero_condition/E079_k47_boundary_macro/run-001/RESULT.json

SHA-256:
  06576856727f16eb1a52ced690a865f50453c39cf7a5a478c26e481777d5f718

Macro artifact:
  research_lab/local/zero_condition/E079_k47_boundary_macro/run-001/BOUNDARY_MACRO_V1.json

SHA-256:
  4300cc3fbd4423e6f7ea21cf49e32c9264dc4177b97e1fb891bc4d6883087ef1

Encoding benchmark:
  research_lab/local/zero_condition/E079_k47_boundary_macro/run-001/ENCODING_BENCHMARK.json

SHA-256:
  5ad9f09b8c73b23bb3f9ce377be2cf990721c54194f5b36b998f90154bcb96eb

Verdict:
  K47_BOUNDARY_PACKING_EXACTLY_COMPILED_TO_47_STATE_MACRO

Decision:
  USE_MACRO_IN_PARTITION_SEAM_PROTOTYPE_BEFORE_FULL_MASTER_INTEGRATION

Machine result
--------------
The frozen boundary-only body-packing model has:

  legal boundary poses       136
  mandatory boundary bodies   46
  complete packings            47
  left bodies per packing      23
  bottom bodies per packing    23
  occupied allowed-L cells    138
  omitted allowed-L cells       1

The baseline 136-pose CP-SAT model exhaustively enumerates exactly 47 packings.
Their omitted cells are all distinct and exhaust the 47 legal omitted-L cells.
Every state carries the complete selected pose set, body/front cells and
digests, side anchors, omitted cell, and canonical sorted-instance to
sorted-pose label recovery.

The compiled one-hot macro uses 47 state variables with ExactlyOne. A separate
state-to-pose channel model, built with linear incidence equalities and no
AddElement long table, also enumerates exactly 47 states and roundtrips every
state to the identical 46-pose set.

Independent comparison
----------------------
The main-tree heterogeneous recheck agrees on K=47 and on the canonical rank-1
packing. The blind-tree artifact exposes the complete packing list; all 47
packings agree exactly after comparison in the same boundary-pose index space.

Encoding note
-------------
The direct pose model has 136 decision variables. The minimal one-hot macro has
47, a raw decision-variable ratio of approximately 2.894 to 1. Materializing all
136 pose-channel variables remains optional and can erase much of that size gain;
consumers should read state fields directly when possible.

Consumption license
-------------------
The macro is usable only while all five conditions travel with it:

1. canonical rules, the 46-instance count, and frozen candidate-pool identity
   remain unchanged;
2. all 46 boundary instances remain homogeneous except for stable instance ID;
3. the consumer is instance-permutation equivariant or uses the canonical label
   recovery;
4. all 47 states remain in the disjunction unless another proof removes states;
5. every omitted full-layout body, front, binding, routing, throughput, export,
   and game constraint is reintroduced by the consumer.

Rank 1 is a deterministic construction choice, never WLOG.

Truth boundary
--------------
E079 is exact only for legal boundary_storage_port pose selection plus mutual
boundary-body exclusivity. A macro state need not extend to a full layout. The
result neither fixes one boundary state nor authorizes rejecting the other 46.
