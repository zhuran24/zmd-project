# P1.2 闭合证据台账 (living, CC 维护)

> 目的: 给 owner 的手动闭合决策 (`phase_1_2_spike_close` gate, 计数权在 owner) 提供**系统性、可核查**的证据汇总。本文件只汇总与指路, 不重复论证细节 (逐项有归档指针)。闭合判据 = 连续独立全审零 finding (owner 计数) + 验证加固阶梯交叉确认。
> 最后更新: 2026-06-13 晚。**当前诚实结论: P1.2 尚不可闭合; 六面 (1/2/3/6/7/8) 达饱和下沿 (face 2 r10 零轮新晋); face 2 r8 终饱和轮抓 HIGH false-INFEASIBLE (第六次确认轮实证, 连零归零)** — face 1/3/6/7/8 连零 2 ✅; **face 2 r8 抓 F-GM-R8-SYM-01 (双标尺对称破缺删整个等价类 = false-INFEASIBLE, max_lex 下漏真矩形, 实数据有反向排列; 已修清零, 连零归零)**; preprocess r12 (F-PRE-R12-01) 已修清零; **face 4 r8 终饱和轮抓 CUT-R8-H1 (D2 raw-core over-cut, PCR-R5-H3 constant-support 义务复发; 已修清零, 连零归零 — 第七次确认轮实证)**; **face 5 r13 确认轮判 R12-01 修复 sound 但自由攻击角抓 F-PRE-R13-01 (cycle recipe I/O 闭包 fail-open; 已修清零, 连零归零 — 第八次确认轮实证)**; **face 4 r9 确认轮判 R8-H1 修复 sound 但自由攻击角抓 CUT-R9-H1 (D2 非 production relaxation 根前提击穿; 已修清零, 连零归零 — 第九次确认轮实证)**; **face 2 r9 零 soundness (SYM-01 修复四向确认 + LOW hint parser 共享化加固已落, 连零 1)**; **face 4 r10 零 soundness (CUT-R9-H1 修复四点确认 + LOW owner 校验加固已落, 连零 1)**; **preprocess r14 确认轮判 R13-01 修复 sound 但自由攻击角又抓 2 HIGH (F-PRE-R14-01 多输出 recipe co-product 重复计数 + F-PRE-R14-02 cycle-internal commodity 组外 producer 漏算, 均 fail-closed 已修 — 第十次确认轮实证; 本轮并发会话 e8a750c5 验收被 owner 停会话打断, SessionEnd 兜底 WIP commit 27967ec, 本会话 20690dc4 接管收尾)**; **face 2 r10 终饱和轮零 soundness (几何 master 4 深角落 + 自由攻击角全干净 + LOW persisted-hint 入口加固已落, 连零 2 → 达饱和下沿; 本会话 20690dc4 在并发事故处置后接管验收, e8a750c5 未碰此单)**; 交互缝未做。全量基线 **3037 绿** (r15 +3, r12 +1)。**⚠️cuts r11 + preprocess r15 的 GPT reply model 降级为 `gpt-5-5-thinking` (非 Pro·进阶, dispatch collect WARN, 13min 非时长降级 = 真模型差异)**: F-PRE-R15-01 真 finding 无争议已修; 但 cuts r11 零 round 在降级模型下可信度打折 → **face 4 streak 2 饱和资格 + 是否暂停发新轮待 owner 裁决** (详见 handoff stamp #67)。**model 真相 (2026-06-13 晚, 经 r12 实锤纠正): r11/r15 = `gpt-5-5-thinking`, r12 = `gpt-5-5-pro` (owner 复查时把 Project 切到真 Pro), thinking≠pro 是不同模型 (dispatch 的 model_slug "含 pro" 检测有效, 之前当误报的判断作废; 生成 <5min 仍是另一有效降级判据)。真 Pro r12 抓出 thinking 轮 r11 漏的 cuts HIGH CUT-R12-H1 (power cut 丢 unpowered occupancy support, CUT-R8-H1 在 power 通道复发, gated/exploratory; 已修+验收 3037+lock+归档)。结论: cuts r11 (thinking) 零 round 作废 → cuts streak 0, r12=修复轮; preprocess r15 抓的 F-PRE-R15-01 仍真 (已修); owner"再审保险"硬实证 (真 Pro 抓 thinking 漏的真 HIGH)。下一步 cuts r13 + preprocess r16 都真 Pro。详见 handoff stamp #69。** owner goal (2026-06-13): 完成 P1.2 的 GPT 外审闭环。

## 一、按面滚动续审状态 (tier ③, 饱和判据 = 每面连续 2-3 轮独立零 finding)

| # | 面 | 轮次史 | 零 finding 连击 | 状态 |
|---|---|---|---|---|
| 1 | Benders/LBBD 主循环 | 算法审 1 轮 (A-1/A-2 → 已修) + 修复确认 2 轮零; **涟漪确认轮 (2026-06-12, 包 6867b7ce) 抓 2 finding 已修**: **F-BL-R3-01 (HIGH, guarded)** = `EXACT_B1_BINDING_ALT_CAP` 命中时把「迭代预算耗尽」误写成「binding alternatives 穷尽证明」→ 铸 whole-layout routing_exhausted_nogood (false-INFEASIBLE 方向); env 在 certified 被 blocker 拦 = guarded latent, 但直接调用/测试 harness/未来 allowlist 漂移可达; 修 = cap 命中 + 仍有替代 → UNKNOWN 不铸 nogood (穷尽证明唯一来源 = binding CP-SAT 重解 INFEASIBLE)。**F-BL-R3-02 (Medium hardening)** = routing unexpected status 落默认 INFEASIBLE 分支 (当前 producer 只回三态故不可达, consumer 无 contract check); 修 = 显式 guard 非三态一律 UNKNOWN 无 cut。**本轮强干净证据**: Q1 完整 16 行状态消费矩阵 (binding×precheck×routing×guard 全组合, TIMEOUT/UNKNOWN 全不变 INFEASIBLE/CERTIFIED) + Q2 五类 cut 时机/作用域全覆盖 (binding nogood 不跨 placement / whole-layout 两形态前提 / front_blocked ladder 全失败=UNKNOWN / persisted cuts 仍 telemetry-only) + Q3 三交互对验证 (wireless×A-2 端口集同步 / loader loud-fail 不被吞 / lazy cut×guard 无绕过) + Q4 max_lex 排序键与 UNKNOWN-frontier 阻挡链逐行验证。验收: 2 回归 stash 判别翻红 → ruff 清 → 全量 **2946 绿** (+2) → preflight 21/21 → lock 新增 F-BL-R3 双条款。归档 `algoaudit_benders_loop_r3_REVIEW_20260612.md` + `algofix_FBL_r3.patch`; **r4 (F-BL-R3 修复攻击面 + 重入/重试语义 + 终止保真角度, 包 b377a2a7, 会话 6a2c24e3) 判 r3 修复 sound 但抓同型残留 1 HIGH — F-BL-R4-01**: binding solve 的状态消费存在与 F-BL-R3-02 同型裂缝 — 初始 solve 只显式处理 TIMEOUT/INFEASIBLE, 非契约状态 (MODEL_INVALID/UNKNOWN/ABORT) 继续建 routing core、跳过 `while binding_status=="FEASIBLE"`、落到 `binding_status="EXHAUSTED"/routing_status="ALL_INFEASIBLE"` whole-layout nogood 分支 (probe 实测: MODEL_INVALID → `master_cut_added_continue` + `cut_type=routing_exhausted_nogood` = 非证明状态铸穷尽 cut, false-INFEASIBLE 方向); 同型共五处 (初始 solve / overload fallback retry / precheck safe-reject 重解 / relaxed_disconnected 重解 / routing INFEASIBLE 后重解)。修 = `_record_unexpected_binding_status()` 统一 fail-closed (`subproblem_status_contract_violation="unexpected_binding_status"` + UNKNOWN 无 cut) 五处全堵 + overload retry INFEASIBLE 时切换 proof source 到 retry model。**r4 还核了**: Q1 cap→UNKNOWN 路径与下游消费链 (UNKNOWN 不再加 cut, outer 写回 campaign 停为 UNKNOWN) + binding model 生命周期 (每次重构、穷尽证明=同一 model 生命周期内重解 INFEASIBLE) + flow=telemetry-only; Q2 重入/重试语义清单 (重入入口 reset metadata / persisted exact_safe_cuts 置空不 replay / hint 非 proof-bearing / campaign resume fail-closed 状态机 / 全部预算耗尽路径返 UNKNOWN 无强终态); Q3 终止与 frontier 保真 (max_lex 排序键 / 三类派生剪枝条件逐条 / UNKNOWN 不在 skip 集 + skip-unknown 实验门在 certified 直接 BLOCKED / admissibility 无 off-by-one); Q4 A-1 guard/A-2 枚举优先/F-BL-R3-01 回归在场维持。验收: 测试半边先行红复现 (trace 实证 `_add_exact_whole_layout_nogood(cut_type=routing_exhausted_nogood)` 被非契约状态触达) → src 半边贴上绿 → ruff 清 → 全量 **2957 绿** (+2 回归) → preflight 21/21 → lock F-BL-R3 条款扩写 R4 (binding 状态契约五消费点)。归档 `algoaudit_benders_loop_r4_REVIEW_20260612.md` + `algofix_FBL_r4.patch`; **r5 确认轮 (F-BL-R4-01 修复攻击面 + 跨 candidate 会话隔离 + proof summary 完整性角度, 包 278e4d67, 会话 6a2c3d80) 零 soundness finding ✅** — 本面首个干净轮: Q1 五消费点核对表 (逐处 file:line, return 后 cut/exhaustion 全不可达; rg 追无第六处; overload retry 全切换非半切换 [retry INFEASIBLE 的 summary/nogood/exhaustion 证明源全指 retry model]; `master_status:"FEASIBLE"` 硬编码符合调用前置且下游无强消费; cap×非契约组合无 false-INFEASIBLE 序); Q2 **跨 candidate 共享态清单 13 行** (static core 无 mutation 点 [overlay 全克隆: proto/build_stats/groups/coordinate 绑定]; CutManager/generated_exact_safe_cuts/binding cache/routing shrink counters 全 controller 局部; persisted cuts replay 置空维持; campaign state 强状态只认 CERTIFIED/INFEASIBLE; serial vs parallel 主隔离边界等价, 差异仅 seed/调度轨迹非 proof 语义; 保守备注: facility_pools pose dict 浅拷贝共享, 当前无 mutation 路径, 未来 overlay 原地写须先深拷贝 — 非缝挂账); Q3 单槽 summary 时序 (五分支写后立即返回不被覆写; summary 键下游全 telemetry/审计无强消费; campaign heartbeat merge 只在 RUNNING 时写不压 terminal; campaign.save 异常不吞 = availability 非 fail-open)。**附带 1 LOW 已修 — F-BL-R5-PS-01**: env 门控 power placement forensic 分支 (`EXACT_POWER_PLACEMENT_SUBPROBLEM`, 公开 certified 入口被 `power_placement_subproblem_not_certified` blocker 拦) 非契约 power status 直接 UNKNOWN 返回不写对齐 summary → 直接调用 controller 的 forensic 路径读到 stale/空 summary = status×summary 错配 (完整性非 soundness); 修 = 返回前写 fail-closed summary (stage/power_placement_status/master_follow_up + 审计字段) + 1 回归。验收: 测试半边先行红 → 贴绿 → test_exact_contract 94 绿 → ruff 清 → 全量 **2964 绿** (+1) → preflight 21/21 (LOW 完整性不动 lock, 按 DOC-LOW 先例台账承载)。归档 `algoaudit_benders_loop_r5_REVIEW_20260613.md` + `algofix_FBL_r5_ps.patch`; **r6 确认轮 (R5-PS 修复确认 + cut/缓存生命周期 + 时间预算终验, 包 70457b5e, 会话 6a2c6597) 零 soundness finding ✅ — 连零 2**: Q1 R5-PS 修复确认 (fail-closed summary 字段集逐项 + `_publish_last_run_metadata` 全 `.get(default)` 读取无 KeyError 面 + 公开 certified env guard BLOCKED 拦截 + 其余 power 分支无错配 [INFEASIBLE 须 ghost 条件化成功才铸 cut, 缺件全 ABORT→UNKNOWN]); Q2 **7 行 cut×有效性矩阵** (binding domain empty/RAB certificate / binding infeasible / front_blocked ladder / routing exhausted [仅穷尽后] / lazy connectivity [subproblem-local 不持久化] / power conditioned [必须 ghost-conditioned, 解析失败 skip 不降级无条件] / persisted replay 置空) + condition lits fail-closed 全失败模式清单 + dedup signature 含 condition 不吞 ghost A/B + master apply all-or-nothing (失败 False 不当有效削减, 成功清 `_last_solution` 排除同解循环) + **7 行缓存 proof-bearing/telemetry 判读** (pose domain cache=immutable pattern 新 materialize / RAB filter 不污染 raw cache / solve status proof 但 hit/miss telemetry / domain analysis 同 attempt 内复用 / shrink counters/replay count 全 telemetry); Q3 **15 行时间预算出口清单** (master/binding 五解点/routing/guard/alt cap/power forensic/coordinate precheck/mandatory precheck/flow 全部 TIMEOUT→UNKNOWN 或排除强剪枝; 单调时钟; 无无界自旋路径)。**附带 1 LOW 已修 — F-BL-R6-01**: certified 主循环 `max_iterations` 耗尽返回 `RUN_STATUS_UNPROVEN` 而非 `UNKNOWN` — 迭代预算耗尽应与 r3 预算口径一致 fail-closed; CC 核实 UNPROVEN 在 outer 侧与 UNKNOWN 同为 frontier 阻挡弱状态 (outer_search:1665 "candidate_returned_unproven") = 纯状态语义一致性非 soundness; 修 = 该出口改 UNKNOWN + `master_follow_up="fail_closed_unknown"` + `_master_search_summary()` 对齐其它 unknown 出口 + 1 回归。验收: 测试半边先行红 → 贴绿 → test_exact_contract 95 绿 → 行号抽查 2 处命中 (condition lits fail-closed 契约 / build 域绑定) → ruff 清 → 全量 **2974 绿** (+1) → preflight 21/21 (LOW 不动 lock, 按 R5-PS DOC-LOW 先例台账承载)。归档 `algoaudit_benders_loop_r6_REVIEW_20260613.md` + `algofix_FBL_r6_cap_unknown.patch` | **2** | **饱和判据下沿达成 (连零 2/2-3); 可选 r7 终轮或转横切** |
| 2 | 几何 master | 算法审 1 轮 (B-01 → 已修) + 再审 1 轮 (2 finding → 已修) + 确认 1 轮零; **双向保真轮 (2026-06-12, 包 6867b7ce) 抓 1 HIGH — F-GM-Q3-01**: coordinate delegate 的 protocol storage 下界约束只数 residual optional 槽、忽略 fixed required 槽 → 「fixed 槽已满足下界 + 零 residual」的合法配置被编码成 `0 >= 1` = false-INFEASIBLE。**定性核实 (CC 独立验证)**: 默认 certified 路径 latent (`build_exact_core` 不传 counts 恒空 dict, 仅 env 拦截的 pose-bool 路径与显式 API 用法传) — latent 加固 + API 路径修复。修 = `residual_active >= max(0, lower_bound - fixed_required_count)`。**本轮强干净证据**: Q1 全姿态矩形性独立重验 (66403/66403, bbox 当前池=精确非近似) + 半开区间 off-by-one probe (贴边合法/重叠非法) + mode-channel 审计 (6x4 双向独立 bbox); Q2 ghost 编码无暗藏连通约束 + admissibility 全链溯源; **Q3 13 约束族×规则溯源矩阵** (无 stricter-than-rule 工程近似); Q4 电力覆盖全量重验 (54780 powered poses 矩形 + 4761/4761 杆覆盖=canonical 裁剪矩形)。验收: stash 判别 (unpatched 新回归 INFEASIBLE 复现/patched OPTIMAL) → ruff 清 → 全量 **2942 绿** (+1 回归) → preflight 21/21 → lock 新增 F-GM-Q3-01 条款。归档 `algoaudit_master_geometry_r2_REVIEW_20260612.md` + `algofix_FGM_q3.patch`; **r3 (F-GM-Q3-01 修复攻击面 + 对称破除保真 + ghost 锚点域完备性角度, 包 b377a2a7, 会话 6a2c2549) 判 r2 修复 sound 但抓对偶残缝 1 HIGH — F-GM-Q3-01-R3-A**: r2 修复只盖 `fixed >= lower`; `0 < fixed < lower` 时 residual protocol storage 池仍被「存在 fixed required count 就整体跳过」的旧逻辑砍掉 (signature bucket 准备点 + slot 创建点两处) → shortfall 无 literal 可补, 编码成 `0 >= shortfall` = 合法 fixed+residual 混合配置 false-INFEASIBLE (probe 实测: 3×3 网格 demand=4/slots=3→lower=2, fixed=1 → 未修 INFEASIBLE / 修后 OPTIMAL); 同源第三处 = master_model powered residual 统计同口径排除 → 只修 delegate 会旁路分裂。修 = `_needs_residual_optional_slots_after_fixed_required()` 单一谓词供两处共用 (protocol: lower>fixed 才留 residual 池; 其它模板维持 fixed 全代表语义) + residual upper bound 扣减 fixed (防双花) + powered residual 统计同步口径。与 r2 同属 latent+API 路径 (默认 certified `build_exact_core` 不传 counts 恒空 → 行为全等)。**r3 还核了**: Q1 修复无第二套 fixed 计数来源 + lower 来源规范化 (负数 fail-closed/0 不加约束) + core/overlay 同快照传递 + 同型 lower-bound 编码全扫无未修兄弟; Q2 对称破除保真 (mandatory 分组粒度 `(facility_type, operation_type)` 不误并 / order-key 只裁匿名可交换槽标签 / residual 前缀激活不切高编号-only 配置 [共享 domain 交换论证]; 1 非 soundness 观察: 部分 canonicalization 不受 enable_symmetry_breaking 控制, 配置语义议题非缝); Q3 ghost 锚点域 (半开坐标域含贴边 anchor, 5 矩形 probe 4692/70/1 全等 expected + oriented (2,3)/(3,2) 分别枚举 + AddNoOverlap2D footprint bbox 维持 B-01); Q4 B-01/mode-channel/r2 回归三处抽查在场。验收: 测试半边先行红复现 (INFEASIBLE 实测) → src 半边贴绿 → 专项+test_master 228 绿 → ruff 清 → 全量 **2958 绿** (+1 回归) → preflight 21/21 → lock F-GM-Q3-01 条款扩写 R3-A (对偶义务: shortfall 必须有 residual 池 + 防双花 + 单一谓词)。归档 `algoaudit_master_geometry_r3_REVIEW_20260612.md` + `algofix_FGM_r3.patch`; **r4 (R3-A 修复攻击面 + optional 基数不等式族双向保真, 包 278e4d67, 会话 6a2c4481) 判 R3-A 闭合但同族 API 缝再爆 1 HIGH — F-GM-Q3-01-R4-A (latent+API false-INFEASIBLE)**: 显式传 `exact_required_pose_optional_counts={"power_pole": N}` 时, fixed pole slot 只作为几何 slot 占格**不承担电杆语义** — 不入 family membership/count 通道、不入 table/geometric 覆盖 witness 枚举, 而 residual pole 池又按「fixed 全代表」语义被跳过 → 合法「固定 1 杆供 1 机」probe 实测 INFEASIBLE (修后 OPTIMAL, pole_slots 1)。修 = `_all_power_pole_slots()` 统一 fixed+residual / fixed pole 接入 family tuple+membership (active=常量1) / family count 上界与 coverage witness 改用全部 materialized slots / lazy power stats 同步; 不动 R3-A protocol 判定。**r4 强证据**: Q1 谓词调用点全扫 (两处直接调用无旧逻辑残留 + master powered 统计三区间口径对照表全等) + `upper<fixed` 边界判读 (真 INFEASIBLE 非 false, 来源 min(pose_count, area_bound) 是必要上界; 建议未来加诊断 ValueError 非缝) + required optional 模板枚举 (无第二个 protocol 同型 lower 模板); Q2 **bound 族×方向×推导依据 6 行全表** (全部规则蕴含的有效不等式无启发式: area 必要条件/ceil lower/pole witness 重标号论证/family capacity) + wireless slots 注入链 5 步单源 + family 瀑布激活重标号论证 (不切高编号-only 配置) + pool=0 退化矩阵 7 行 (各 cell 判读真/假 INFEASIBLE); Q3 canonicalization 不受 flag 控制复核 (匿名槽标签排列, 配置语义非 soundness) + B-01/mode-channel/ghost 半开域抽查在场。验收: 测试半边先行红 (INFEASIBLE 复现) → src 贴绿 → 专项+test_master 229 绿 → ruff 清 → 全量 **2967 绿** (+1 回归) → preflight 21/21 → lock F-GM 条款扩写 R4-A (fixed slot 必须承担模板完整角色语义)。归档 `algoaudit_master_geometry_r4_REVIEW_20260613.md` + `algofix_FGM_r4.patch`; **r5 (R4-A 修复确认 + slot域×pool 对接 + specs 文本独立对照, 包 70457b5e, 会话 6a2c65bd) 抓 R4-A 自身退化边界 1 Medium — F-GM-Q3-01-R5-A (false-INFEASIBLE, API)**: R4-A 把 fixed pole 接入 family channel 后, **family 映射为空时** (skip_power_coverage 或全模型无 powered demand) `_prepare_power_pole_families` 提前返回 → `_attach_required_power_pole_family_channels` 拿到 tuple_rows=[] → 对每个 fixed pole `Add(0==1)` = 合法纯几何固定杆配置被判死 (probe: instances=[] + required_counts={power_pole:1} → INFEASIBLE, 修后 OPTIMAL)。修 = family 映射为空时跳过 attach (fixed pole 仍是几何 slot, 经 `_all_power_pole_slots()` 被存在的通道读取); 有 family 映射但 tuple 表异常空保留 fail-closed。**确认轮即抓上轮修复回归边界 = 确认轮价值实证**。**r5 还核了**: Q1 R4-A 主路径确认 (`_all_power_pole_slots` 全文件读取点扫描 [witness/count/lazy stats 全走合并函数, residual-only 引用只在 residual 创建/对称破除/guidance 合法] + 常量 active 经 CP-SAT probe 验证 AddElement/OnlyEnforceIf/线性约束等价 + R3-A×R4-A 混合 probe [fixed pole+fixed protocol+residual protocol 共存 OPTIMAL] + family count 上界混合有效); **Q2 slot域×pool 对接 8 行判读表** (mandatory/required 空域正确判死 vs residual 空域不 materialize 非全局 infeasible / pose tuple 重复 fail-closed / powered pose 过滤=必要条件 / signature bucket 压缩表示不可表示即 fail-closed / 瀑布激活只作用 residual 不混 fixed / pose_idx 下游 master→binding→extract 同一 pool 顺序); **Q3 9 行 specs 文本独立对照** (70×70 域 / max_lex 与 admissibility 分工干净 [coordinate master 是固定 (w,h) feasibility 模型无内部目标] / mandatory exactly-one+required 必放+residual 由 active / no-overlap 只盖刚体+ghost 不含 belt / **ghost「空」口径 = 设施 body+pole body, 不含 connector/belt/coverage, 与 specs 一致** / 电力覆盖双 witness 枚举全部杆 / footprint 从 occupied_cells 推导不 under-approximate / protocol 下界 fixed 计入)。验收: 测试半边先行红复现 (INFEASIBLE) → src 贴绿 → 专项 230 绿 → ruff 清 → 全量 **2973 绿** (+1 回归) → preflight 21/21 → lock F-GM 条款追加 R5-A 退化边界。归档 `algoaudit_master_geometry_r5_REVIEW_20260613.md` + `algofix_FGM_r5.patch`; **r6 (R5-A 修复确认 + 解回读保真 + master 侧 cut apply, 包 3f4ceebb, 会话 6a2c6ee8) 抓 1 HIGH — F-GM-R6-01 (stale solver witness, API soundness)**: cut 成功加入后只清 `_last_solution`, 旧 `_solver/_status` 仍在 — `extract_solution()` 入口只查 `_solver is not None and _status in {OPTIMAL,FEASIBLE}`, `_last_solution` 空时会从旧 CpSolver assignment **重建刚被 cut 禁止的解** (probe: 2x1 网格 cut pose0 后未重 solve, extract 仍回 pose0; `extract_bound_state()` 同理读旧 objective/bound)。LBBD 主循环 cut 后立刻重 solve 不踩 = API 面 fail-closed 义务; **与 face 1 r6 互补审查的价值实证** (benders 侧从 caller 视角判「清 _last_solution 排除同解循环」足够, master 侧从实现深挖发现清得不彻底)。修 = exact + legacy 双路径 cut 成功后同清 `_solver=None/_status=None` (cut 后重 solve 前 extract 返回 {}/无 incumbent) + 新回归文件 2 测试。**r6 还核了**: Q1 R5-A 确认 (空映射两条成因路径与「无需 family 语义」严格等价 [有 demand 有 pool 必建 family 即使系数全 0; demand 在 pool 空 → slot 空域 `0==1` fail-closed 是无候选不可满足非逃逸] + 空映射路径几何约束全保留 [skip_power probe 实证 extract 回读唯一杆] + 非空映射空表 fail-closed monkeypatch probe INFEASIBLE); Q2 回读链 5 环判读 (tuple 反查单值 [重复 key 构造期 raise] + inactive residual 不入 solution [active==1 才读] + ghost 回读同源 u_var + downstream 不把 ghost marker 当设施 [campaign/benders 专门分支] + bound state 不依赖 placement cache); Q3 cut apply 通道 (alias = fail-closed 返 False 非去重继续 / OnlyEnforceIf 极性正确 [condition 真才生效, persisted 侧解析失败 skip 互补] / N == len(present_lits) 全解析成功才加 / all-or-nothing 无半 cut 残留 [helper equality 是定义性通道])。**CC 验收注**: 第一次全量 1 红 `test_ghost_signature_bucket_mandatory_region_counting_falls_back...` — 单跑/整文件/复跑全量均绿, 测试不触及 cut apply 路径 (纯 env-gated build 对比), 判与本批修复无因果的既有偶发环境敏感, **挂账待复发深挖**。验收: 测试半边先行双红复现 → src 贴绿 → 专项 14+226 绿 → ruff 清 → 全量复跑 **2976 绿** (+2 回归) → preflight 21/21 → lock 新增 F-GM-R6-01 条款。归档 `algoaudit_master_geometry_r6_REVIEW_20260613.md` + `algofix_FGM_r6.patch`; **r7 (R6-01 修复确认 + ghost 编码本体首审 + hint 通道首审, 包 38b57070, 会话 6a2c827d) = 零 soundness finding (本面首个干净轮)** + LOW F-GM-R7-HINT-01 已修 (malformed hint [非 int/越界 pose/不存在 anchor] 在 exact/legacy 应用层可 solve 前抛异常中断 — hint 只写 `solution_hint` proto 永不约束故非 soundness, 坏方向 = 可用性; 修 = 统一降级 skip + ghost anchor 不再写全 0 矛盾 hint)。**强干净证据**: Q1 **solve 派生字段处置 10 行全表** (cut 成功清 `_solver/_status/_last_solution` 双路径一致 / 失败路径保留 witness 不误清 / `extract_solution`/`extract_bound_state`/`extract_master_hints` 全有 `_solver is None` gate 无静默重建 / build_stats 是 telemetry 非 witness 通道) + Q2 **ghost 编码本体** (anchor 枚举 `range(grid-w+1)` 完整无裁/越界 fail-closed; **互斥 = body-only 实证**: 全 7 模板族 footprint 扫描 port cells in occupied body = 0, AddNoOverlap2D 只收 body interval + ghost; **max_lex 无 CP-SAT 加权目标** — frontier tuple `(area, min_side)` 比较 + master 只判固定 (w,h) 可行性 = 无权重溢出; min_side≥6 在 candidate 生成/final validation 为 admissibility 非 tie-break; 禁区确认: ghost enforcement 无任何 exterior path/connectivity 约束) + Q3 **hint 永不约束实证** (故意坏 hint [提示与 body 重叠的 anchor] → 结论不变同 OPTIMAL 同 anchor; merge 是 instance 粒度非半变量; 每次 solve 前清旧 hint proto = cut 后旧 hint 不粘)。验收: 3 红复现 → 贴绿 → ruff 清 → hint/cut 专项 36 + master 230 绿 → 全量 **2988 绿** (+3) → preflight 21/21 → lock 新增 F-GM-R7-HINT-01 条款 (hint 纯 guidance + malformed 降级 skip)。归档 `algoaudit_master_geometry_r7_REVIEW_20260613.md` + `algofix_FGM_r7_hint.patch`; **r8 (终饱和轮: 对称破缺保代表性 + mandatory 装配本体 + 自由攻击角, 包 37b84be0, 会话 6a2c90f0) 抓 1 HIGH 已修 — F-GM-R8-SYM-01 (双标尺对称破缺删整个等价类, false-INFEASIBLE)**: 同构 slot 已按 `order_key` 单调排序后又加 `signature` 单调 — 两把不同 total order; pose 对在一个 key 上升另一个 key 下降时, 两种排列各违反一条 = 整个可行等价类被删空 (max_lex 下漏真最大矩形)。**非理论边角**: 实数据 crusher_blue_iron pose 254/257 (order 543<561 但 bucket 1>0) 等多个 mandatory group 存在反向排列; 最小复现回归 = 2 slot 2 pose 模型, symmetry on→INFEASIBLE / off→OPTIMAL (CC 红复现)。修 = signature 单调加同序门卫 (仅当全候选集 signature 按 order_key 非降时才加 = 冗余剪枝不删解; 否则跳过 + telemetry 计数 [实跑 mandatory 45 加/202 跳]); order_key 单调本身保留 (单标尺安全)。**影响判读**: false-INF 方向 = 历史上 symmetry 开启的 master INFEASIBLE 结论存疑, 但项目尚无生产 certified campaign 跑完, 影响前瞻。附带 LOW F-GM-R8-HINT-02 已修 (telemetry 返回阶段二次 int() 仍可抛 + float/bool 截断穿过 = 不符 r7 统一 skip 语义; 修 = strict-int parser 全链)。**r8 其余干净**: Q2 对称破缺逐条保代表性 (order_key 单调/active 前置/power family 三元排序全安全, 唯 signature 双标尺中招) + mandatory 装配 19 group 266 对照精确 + optional/residual active 挂接双向无遗漏 (inactive 不入 NoOverlap 无幽灵占地); Q3 自由攻击角双选 (sparse AddAllowedAssignments 域表完整 + build 顺序依赖无半挂)。验收: 补丁 LF 空白差异需 --ignore-whitespace (上下文逐行核对后贴) → 13+1 红复现 → 贴绿 → master 全专项 243 绿 → ruff 清 → 全量 **3004 绿** (+14: 13 hint 参数化扩容 + 1 对称回归) → preflight 21/21 → lock 新增 F-GM-R8-SYM-01 (单标尺义务) + F-GM-R7-HINT 条款补 strict-int。归档 `algoaudit_master_geometry_r8_REVIEW_20260613.md` + `algofix_FGM_r8_sym.patch`; **r9 (确认轮: SYM-01/HINT-02 修复确认 + 自由攻击角, 包 095a0b6d, 会话 6a2c9a52) = 零 soundness finding + 1 LOW 非 soundness 加固已落**: Q1 SYM-01 四向确认 (**同序门卫量化范围逐族** = `_slot_signature_order_pose_indices` 优先 allowed_tuples 反查 = slot 实际域, mandatory/required_optional/residual 三族门卫输入与约束作用域同集合, bucket 覆盖缺 pose 即 ValueError fail-closed 防"缺块子集误判同序" / **order_key 注入式 total order** = scale 构造可唯一反解 (x,y,mode), 实数据 probe collision=0 + 保代表性论证 [同族 slots 共享 allowed_tuples/domains, 无 slot-index 特化约束; residual active 前缀 + inactive sentinel signature 不删代表解] / **543 条 residual storage_box 独立抽查** = probe 544 slots/4624 poses 全集同序 543=544-1 非误判 / **power family 复合 key 复核** = active→family→同 family 内 order_key 是单一复合尺非双标尺, inactive sentinel 排尾) + Q2 HINT-02 确认中抓 **LOW 残留: community blueprint merge/legacy solve fallback/pose-bool delegate 三入口仍裸 int() (3.14→3, True→1, "0"→0 绕过下游 strict parser; hint-only 不碰可行域非 soundness, 但违 HINT-02 全链口径 + 污染 telemetry)**, 修 = 共享 `src/models/solution_hint_parser.py` (type(value) is int) 全入口复用 + Q3 自由攻击角二缝无 finding (signature payload cache key=(tpl,pose_indices) 无 bucket_defs 漂移前提 / inactive sentinel 三态判读 real≤sentinel 安全)。验收 (零轮规程 + LOW 补丁): probe 本地复现数字全同 (collision 0/mismatch 空/45 加 202 跳/543) + 行号抽查 2 处命中 (:2522-2533 allowed_tuples 反查 / :3035-3038 active 前缀) → LOW 补丁分层红复现 (先贴 parser 新文件再跑旧入口 = 6 红) → src 贴绿 33 → ruff 清 → master 专项 227 绿 → 全量 **3018 绿** (+8 hint 回归) → preflight 21/21 → lock HINT 条款补「单一共享 parser 入口」句。归档 `algoaudit_master_geometry_r9_REVIEW_20260613.md` + `algofix_FGM_r9_hint_parser.patch` + probe; **r10 (终饱和轮: r9 LOW 修复确认 + 几何 master 4 深角落 + 自由攻击角, 包 2cd169b4, out_dir 20260613_gm_r10) = 零 soundness finding (本面达饱和下沿连零 2 ✅)** + LOW-HINT-R10-01 已修: 几何 master 主体零 false-FEASIBLE/INFEASIBLE; Q2 深挖 ghost 交互约束 / power coverage witness 链 (probe coverer index 全量一致 10385840 edges, powered_nonpole_rect_bad=0) / mandatory exactly-one→slot 装配 (probe unguarded_bad=0; naked-global box 会多收非法 tuple [boundary 9114/9248 等] 证明 signature/table guard 真实有效) / delta-interval footprint bbox; Q3 自由攻击角 (全局域裸用 + exact-core ghost overlay 绑定链) 全无 finding。LOW = persisted master hint 入口 (`master_hint_persistence.py` write/load + legacy `apply_master_hints`) 仍裸 int() 接受 "1"/True/1.0 (r9 共享 parser 的第四个残留入口, hint-only 非 soundness); 修 = 三处换 `parse_strict_int_hint_value` (write 非 int 抛 TypeError, load/apply skip)。**接管说明: 本轮由本会话 20690dc4 在并发会话事故处置后验收 (e8a750c5 未碰此单)**。验收: 测试半边先行红复现 (7 failed: "1"/True/1.0 被裸 int 接受, None 本就抛) → src 贴绿 9 → hint 专项 64 绿 → ruff 清 → 全量 **3033 绿** (+9 hint 回归) → preflight 21/21 → lock HINT 条款补 R10 persisted-hint 入口。归档 `algoaudit_master_geometry_r10_REVIEW_20260613.md` + `algofix_GM_r10_persisted_hint.patch` | **2 (零 soundness, 连零 2 ✅ 达饱和下沿)** | 已达饱和下沿暂停 |
| 3 | routing + guard/lazy cut | P0-1 双层修复 + lazy cut 双独立零 finding + guard 完整性 1 轮 (审出对偶条件类缺陷已修*); **编码本体双向保真轮 (2026-06-12, 包 a02b7aed) 首次独立攻击编码本体即爆 2 HIGH 已修**: **F-RT-R2-01** = sink front 方向极性反向 (front 应朝 connector 送料 `Opp(dir)`, 编码要求朝外) → 紧凑 corridor false-INFEASIBLE (3 格直线 probe 实测 INFEASIBLE→修后 FEASIBLE) + 宽敞布局可绕路满足幻影状态而料不进 connector; **guard 用同一反向索引 = 同源漂移一起错; fuzz oracle 的 sink front 键也同源反向 → 历史 900 实例对极性类盲 (独立验证器盲区实证: 验证器复制实现的键方向 = 不构成独立性)**, oracle 已同步修正 + self-test fixture 重写 + 修后 200 新实例零不一致。**F-RT-R2-02** = L0/L1 合法重叠时局部 `>=1` 支撑允许单条有向边喂两层/两层并一边 = 免费隐形 splitter/merger, 且连通性 guard 看不见 (probe: 强制非法双层态 FEASIBLE + guard failure_count 0); 修 = 每 commodity 每条非 terminal 有向边「送=收」守恒约束。**本轮还核了**: Q1 网格/层语义 9 项规则对照矩阵 (solid/容量/bridge 直行/互斥重叠/方向/splitter/endpoint/port-front) + Q3 precheck 保守性 (front_blocked/relaxed_disconnected 均不比完整 solve 严, 无超时 fail-open) + Q4 guard 同源更新确认。验收: 2 回归 stash 判别翻红 → ruff 清 → 全量 **2949 绿** (+2) → preflight 21/21 → fuzz oracle 修极性后 50+150 实例零不一致 → lock 新增 F-RT-R2 双条款。归档 `algoaudit_routing_face_r2_REVIEW_20260612.md` + `algofix_FRT_r2.patch`; **r3 (F-RT-R2 修复攻击面 + 容量/多商品纵深 + 终端语义角度, 包 b377a2a7, 会话 6a2c30c1) 判 F-RT-R2 双修复 sound 但终端语义×域收缩交界爆 1 HIGH — F-RT-R3-01 (false-FEASIBLE, live 路径)**: routing 域从未剔除物理 port connector cell — `_resolve_routing_domain_context` 原样返回 free_cells (`routable_cells = free_cells | port_cells` 更是「port cell 可走」语义), successor/predecessor 连续性把 terminal 侧以外邻接全当普通 cell, guard 只证 reachability 不证「节点占没占 connector」→ 任意商品可把别的端口 connector 当普通 belt 格穿过 / 同商品可复用 source/sink terminal 侧, CP-SAT 与 guard 双双放行 (probe 实测: ore 线穿 water_src 的 connector (2,1), solve FEASIBLE + guard failure_count 0; 规则依据 specs/06:72-75 + specs/08:29-34 connector 非 routing cell)。修 = ① `_port_connector_cells()` 收集 in-grid connector → 域解析时从 free_cells 扣除 + placement-core 复用路径重算连通组件; ② `_bind_domain_analysis` 对外置 domain_analysis fail-closed 二次扣除; ③ continuity 加 terminal-side 禁止 (普通态不得向 source front 的 connector 侧送/从 sink front 的 connector 侧收, belt-and-suspenders); ④ diff-fuzz oracle 独立新增 connector 占用检查 + self-test 场景 + README 旧极性文案修正。**r3 还核了**: Q1 极性消费点全清单 (域分析/索引/局部剪枝/adherence/guard/oracle 六处全 `DIR_OPP`, 无残留原 dir; source/sink 对称成立) + 边守恒形状不过强 (合法分/汇流在同 cell 多出入边上, 单条有向边物理通道数必须左右相等; terminal 豁免边界在 connector 剔除后恰好) + L0-straight×L1-bridge 跨越 probe FEASIBLE 不误剪; Q2 双层×商品状态矩阵 7 行 (AtMostOne 恰好互斥/L1 只直桥/bridge 与 L0 non-straight 互斥/不同层不同商品合法/边守恒/adherence exact-one/connector 本轮新增); Q3 终端抽查 (front=port+dir 全链一致; 多 port 共享 front 合法性维持; 备注: 完全相同 terminal key 会折叠 multiplicity, canonical 非重叠 pose 构造不出, port_specs 入口未来开放时建议 fail-closed 重复检查 — 挂账非缝); Q4 guard 判读 (reachability guard 对 connector 污染天然盲, 修后非法态不被创建 + oracle 独立检查补位 = 可接受)。验收: 测试半边先行红复现 (precheck feasible→应 relaxed_disconnected) → src+fuzz 半边贴绿 → routing+guard 34 绿 → oracle self-test 4 场景过 → **新跑 fuzz 200 实例 (seeds 400/500) 零不一致** → ruff 清 → 全量 **2963 绿** (+1 回归) → preflight 21/21 → lock 新增 F-RT-R3-01 条款 (specs 规则文本本就正确, 错在实现, 无需 spec 改动)。归档 `algoaudit_routing_face_r3_REVIEW_20260613.md` + `algofix_FRT_r3.patch`; **r4 (F-RT-R3-01 修复攻击面 + 域收缩/precheck 纵深 + 终端 key 挂账复核, 包 278e4d67, 会话 6a2c3d4c) 判 connector 修复闭合但 Q2/Q3 角度爆 2 finding 已修**: **F-RT-R4-01 (HIGH, false-INFEASIBLE)** = `analyze_exact_routing_domain()` 把同 commodity 全部 terminal fronts 强制压单连通分量 (`len(component_ids)>1` 即 relaxed_disconnected) — 「双孤岛各自 source+sink 闭合」的合法布局被误拒 (probe: 双岛 analyze=relaxed_disconnected + solve INFEASIBLE; 外置 feasible domain 绕过 precheck 则 CP-SAT FEASIBLE + guard 0 failure = 误拒在 producer 不在编码), 与 guard 语义 (每 source 达某 sink/每 sink 被某 source 达) 和 specs/08 pool 模型直接矛盾; 消费侧经 binding-local safe-reject 放大 = 丢合法 binding/layout。修 = terminal fronts 按 component 分组, 双侧 commodity 要求每个含 terminal 的 component 有 ≥1 source + ≥1 sink, domain 取满足 component 的并集 + 逐 component peel; 单侧 commodity 保留旧保守行为 (兼容 wireless 局部语义)。**F-RT-R4-02 (MEDIUM, 外置入口 hardening)** = r3 挂账「重复 terminal key 折叠」复核结论双层: canonical 构造不出 (几何论证: 同 front+dir ⇒ 同 connector ⇒ 设施 body overlap 被 master no-overlap 禁 + 599382 端口实扫 0 重复) 但外置 port_specs 可构造 (双 port 折叠到同一 exact-one key = multiplicity 丢失); 修 = `_duplicate_terminal_front_keys()` 在 analyze 返回 front_blocked fail-closed + build 对外置 domain_analysis 二次 fail-closed。**r4 还核了**: Q1 connector 修复 10 行确认清单 (域两入口/precheck front/外置 analysis/state 创建/双向 terminal-side ban 几何唯一性论证/oracle 独立性; 残留备注: `routable_cells = free|port` 属性现无消费者 = stale API 非洞, 未来启用须同步扣); Q2 shrink summary = 纯 telemetry 非域生产者 + precheck 三态×benders 消费表 (feasible 不当证明/front_blocked 走 binding-local ladder/relaxed_disconnected fail-closed 消费 — 缝在 producer 过严已修); Q4 极性/边守恒/guard/oracle 独立性维持。验收: 测试半边先行双红复现 → src 贴绿 → routing+guard+wireless 43 绿 → oracle self-test → fuzz 100 实例 (seed 600) 零不一致 → ruff 清 → 全量 **2966 绿** (+2 回归) → preflight 21/21 → lock 新增 F-RT-R4-01/02 双条款。归档 `algoaudit_routing_face_r4_REVIEW_20260613.md` + `algofix_FRT_r4.patch`; **r5 (R4 修复确认 + specs 文本独立对照 + 三修复叠加组合, 包 70457b5e, 会话 6a2c5af9) 判 R4-01/02 修复 sound 但外置入口族再爆 1 HIGH — F-RT-R5-01 (false-FEASIBLE, 外置 hardening)**: `_bind_domain_analysis()` 对外置 domain 只减 connector 不与 `grid.free_cells` 求交, 而 CP-SAT 障碍排斥实现为「只在 active domain 建 route-state」(`_add_obstacle_exclusion` 是 no-op) → 陈旧/外部分析含 occupied 格时在实体格上建变量, solve FEASIBLE + guard 接受 = 穿墙 (probe 实测: analyze 正确 relaxed_disconnected, 但外置 stale domain 时 FEASIBLE 且 route 用 (2,0) solid 上的 L1 bridge; 修后该格零变量 + INFEASIBLE)。主链先 analyze 同一 grid 故不直接触发, 属 F-RT-R4-02 同族外置入口缝。修 = active/component cells 改与 `free_cells - connector` 求交 (connector/occupied/出界三类同口径挡回; front 被裁出 domain 走 `0==1` adherence fail-closed)。**r5 还核了**: Q1 R4-01 四向确认 (component 划分与 CP-SAT 域同口径 [扣 connector 后的 resolved_free_cells] / 「source 空置流量 0」不构成误拒 [port_specs 已是 binding 选后必接线端口, 时序在前] / peel 无共享格污染 [components 互不相交+独立 peel+union] / 单侧分支切换判据 + guard 双侧非空兜底 = 收敛风险非 false-FEASIBLE) + R4-02 双层同 helper 无复制漂移 + fail-closed 方向 (front_blocked → binding-local ladder 非直接 INFEASIBLE); Q2 **10 行 specs 文本→实现对照表** (先读 specs/06/08/09/03 + canonical 推导再对照: connector terminal/solid 两层锁死 [即本轮 finding 行]/cell-layer AtMostOne/L1 只直桥+共存约束 [桥与 L0 直带方向不要求同向, 文本未限定]/方向连续+边守恒/12 belt 形态/splitter-merger 容量节点抽象 [1→2 已接受口径]/零长度硬连由 placement port-clearance 层执行/同边同质+空置由 binding 裁/guard 非认证边界); Q3 三修复叠加组合 (R2 边守恒×R3 connector 扣除×R4 分组两两不冲突, 论证逐点) + **4 个端到端中型 probe** (双孤岛 FEASIBLE / connector 屏障 INFEASIBLE / 多商品 bridge 十字 FEASIBLE [实测 (3,4) ore L1 桥 + water L0 直带共存] / 陈旧域穿墙修后 INFEASIBLE) + guard 同步性 (selected graph 建图与 successor/predecessor terminal 例外一致)。验收: 测试半边先行红复现 (occupied 格上有 route-state) → src 贴绿 → routing 25 绿 → ruff 清 → 全量 **2969 绿** (+1 回归) → preflight 21/21 → lock 新增 F-RT-R5-01 条款 (specs 文本本就正确无需 spec 改动)。归档 `algoaudit_routing_face_r5_REVIEW_20260613.md` + `algofix_FRT_r5.patch`; **r6 (R5 修复确认 + guard 本体深审 + 状态枚举完备性, 包 3f4ceebb, 会话 6a2c7808) = 零 soundness finding (本面首个干净轮)**: Q1 R5 三向确认 (求交两集合无 bind 后 mutation 路径 / occupied·出界·connector 三类逐类判读 [出界由 range(GRID_W/H) 构造性排除] / 第四类格排查无遗漏 [其它商品 terminal front 走 cell-layer AtMostOne 容量裁非预剔除; ghost 不入 occupied 属 master 层契约; wireless routing-free 不进 port 集] / adherence `0==1` 对 source/sink 双向生效无 patch 早退); Q2 **guard 本体首次独立深审** (guard 直接读 `_selected_route_keys(solver)` 非 extract_routes 产物 [extract 在 guard 接受之后, 无字段裁剪弱化]; 邻接 layer-agnostic 与 CP-SAT successor/predecessor 的 `_vars_by_cell_dir_*_commodity` 同构, 跨层 receiver/sender 两侧一致; terminal 例外一致 [source 为 BFS 起点无 predecessor / sink 不展开 successor]; 多源多汇 = existence/pooling 与 specs/09:100-106 + 08:88-92 一致非完美匹配; fail 处理 fail-closed [拒收→lazy cut self-check→nogood, 耗尽→TIMEOUT 清 witness]); Q3 **从 specs/03:306-344 + 09:43-64 独立推导 pattern 封闭集合 48** (12 belt + 16 splitter [4×(C(3,2)+C(3,3))] + 16 merger + 4 直桥) probe set-equality 与实现精确相等, component_type 下游消费一致 (连通性只由 directed sides 决定)。advisory 挂账: 若未来把 `_route_state_adjacency` 当纯 potential-graph oracle 用应显式传 source_fronts 过滤 source-entry arcs (当前仅影响 lazy-cut 保守度非 soundness)。验收 (零轮规程): probe 本地复跑通过 (pattern_counts + domain clip + guard fail-closed 三段全 ok) + 3 处行号抽查一致 (:841-859 求交 / :867-905 枚举 / :1604-1657 pooling) + probe ruff 清 + 全量 **2980 绿** (零代码改动)。归档 `algoaudit_routing_face_r6_REVIEW_20260613.md` + `algoaudit_routing_face_r6_probe.py`; **r7 (终饱和轮: 约束本体直审 + precheck 生产者 + 自由攻击角, 包 37b84be0, 会话 6a2c8adc) = 零 soundness finding — 连零 2, 本面达饱和下沿**: Q1 **从 specs 独立推导 8 约束族清单逐族对照** (pattern 48 封闭集 / obstacle 域排除三层 / capacity AtMostOne 全 var 入桶 probe 反查 / bridge 共存 straight 判据不含 splitter / continuity successor-predecessor 的 OnlyEnforceIf 形态与「必须接续」等价 + 空邻接走 var==0 非 vacuous + terminal 例外只在 ground 不外溢 L1 / **edge balance 量化覆盖全 layer 全 state side, terminal connector side 由 adherence 单独精确处理** / adherence exact-one 含 splitter-merger 履行口径 + 重复 key fail-closed / gap 规则由 front-cell 模型+placement clearance 承担 + guard 终门); Q2 **precheck 生产者本体** (生产口径与 build `free_cells - connector` 同源 / front_blocked 两方向后果链验证 [误报→build 端同判据也 fail-closed + 消费侧走 ladder 不直接 INFEASIBLE; 漏报→full CP-SAT 慢证纯性能] / relaxed_disconnected 是必要条件证明非启发式 [更宽图断开⇒任何 selected path 不存在] / **消费点全扫**: heuristic_feasible_finder 直判 INFEASIBLE 但注释明示 best_effort 不产 nogood 非 proof producer, campaign_triage/telemetry 仅分类, 无裸用 precheck 当 certified 证明点 / peel 只剥非 terminal leaf 不删必需走廊 / 时序 = binding 选后 port_specs); Q3 自由攻击角三连 (solve loop guard 门闩无 stale 放行 / extract_routes 只读 solver keys 无重建失真 / 外置窄域 = API 信任边界非生产链缝)。**接受行为挂账 (非 soundness)**: CP-SAT 可选与 terminal 无关的多余闭环 state, 删之只释放容量不破 guard 已证 source→sink, 影响蓝图简洁度不影响存在性认证。验收 (零轮规程): 行号抽查 3 处命中 (:1068-1093 edge balance 跳 terminal / :363 同口径求交 / heuristic_finder :152-184 best_effort 注释) + GPT 专项 11+8 绿 + 零代码改动基线 **2988 维持**。归档 `algoaudit_routing_face_r7_REVIEW_20260613.md` (无补丁) | **2 (饱和下沿 ✅)** | 饱和暂停; 交互缝轮待全面达标 |
| 4 | cuts 机制 (F1-F9/PCR) | 算法审 1 轮 (C-3/C-4 latent, 非公开路径); **r2 live exact-safe 轮 (2026-06-12, 包 db740254, 脚本自动外发) 抓 1 P2 hardening — CUT-R2-H1**: env 门控 pose-bool cell-pattern cut (`add_routing_port_blocking_cell_cut`) 把「binding 可选端口」当「必然激活端口」量化 — visible demand 不覆盖全部物理端口的 side 也被登记进 raw per-cell routing-visible 索引 → blocker 只挡 inactive binding slot 时合法 placement 被 over-cut (最小反例: 双输入口 demand=1 的机器 + 占第一口 front 的 blocker, binding 走第二口本可行); 同源第二处: `_build_port_lookup_cache` 对 global 坐标 pose data double-anchor → candidate alias 到幻影格 (与隔壁 `_build_global_pose_cache` 注释直接矛盾, 轻则漏 cut 重则错切)。公开 certified 默认零影响 (`pose_bool_master_not_certified` blocker 拦在 `run_benders_for_ghost_rect` 入口), 升级条件 = 未来 promote pose-bool/cell cut 而不带本前提。修 = `_mandatory_port_side_is_cell_pattern_exact()` (input 侧 demand>=端口数 / output 侧 visible 非零=total 且>=端口数才登记; RO pose 无 binding identity 不登记; 混合侧留给更弱但 exact 的 lazy-demand) + cache 直用 global 坐标。**本轮强干净证据**: Q1 六 cut 族 exact-safe 对照矩阵 (binding nogood / master placement nogood / deletion-core / lazy-demand / cell-cut / lazy connectivity — 各自有效性定理+前提检查+fail-closed 逐个判读, 公开 certified 默认路径零 soundness) + Q2 作用域/生命周期/`exact_safe_cuts` resume 消费点穷举 (V82 telemetry-only 边界为代码强制实证: certified 下 `raw_candidate_cuts=[]`, 无任何 certified path 把 persisted cuts 加回 master) + Q3 cut 间交互 (公开 certified 实际可组合 4 类各自独立 proof 前提不互相依赖; CUT-R2-H1 为唯一交互风险已修) + Q4 `src/cuts/` F1-F9 框架边界 (step_2/step_8 仍 NotImplementedError, 全仓无 certified runtime 导入 CutStore/replay_cut/F1-F9 apply)。验收: 2 回归 stash 判别翻红 (over-cut+幻影格双复现) → ruff 清 → 全量 **2951 绿** (+2 回归) → preflight 21/21 → lock 新增 F-CUT-R2-01 条款 + specs/10 §10.8 addendum。归档 `algoaudit_cuts_face_r2_REVIEW_20260612.md` + `algofix_FCUT_r2.patch`; **r3 (CUT-R2-H1 修复攻击面 + nogood 作用域/condition_lits 角度, 包 b377a2a7, 会话 6a2c3123) 判 R2 修复 sound 但抓同族残缝 1 P2 hardening — CUT-R3-H1**: generic 槽是 binding 容量不是逐口强制需求 — `_profile_port_demands()` 无条件把 `generic_output_slots` 计入 visible output demand / `generic_input_slots` 计入 input demand, 但 binding 给每个 generic output 物理槽的 domain 含 `__unused__` (required 只约束真实赋值数), 2 物理口+1 required 时被挡的第一口可留 unused 走第二口 → cell-pattern cut 误剪可行 placement (probe 实测: binding FEASIBLE 走第二口 / 旧 cut 加上后 master INFEASIBLE); generic input 槽在 canonical 实现里是虚拟无线容量 (无物理 front) 同向 overcount。修 = input 侧只计 concrete demand + generic output 只在「required 总数 == mandatory 容量总数」全局饱和证明成立时计入 (饱和把 `__unused__` 压成 0 = 必然激活, 镜像 F-BIND-R1 52=52 逻辑), 容量不可知 fail-closed 不登记。**CC 验收加固**: GPT 容量统计对「组实例数不可知」走 skip/假定-1 = 少算容量可伪造饱和 (与 finding 本体同族 over-cut 方向) → 改两路径均 fail-closed return None + 1 加固回归 (`test_pose_bool_cell_pattern_cut_refuses_unknowable_generic_output_capacity`)。**r3 还核了**: Q1 修复确认四问 (concrete input 前提 sound / 输出三条件补饱和证明 / `_profile_port_demands` 异常方向 fail-closed 弱化非 over-cut / double-anchor 全文件重扫无第三处); Q2 nogood literal 集边界 (binding nogood: extract_selection 含 `__unused__` 槽选择有对应 literal, 两个只差 unused 槽位的 binding 不会塌成一个 nogood; fixed choice 无 CP 变量省略非 under-scope [实例内不变]; literal 集偏大只弱化) + master conditioned cut 全登记点 (power 条件 nogood ghost_anchor 键 fail-closed resolver / dedup 含 condition_set 不 alias / whole-layout conflict 排除 ghost_pick / F1-F9 不在 certified runtime); Q3 维持 (V82 telemetry-only / power witness skip / C-3/C-4 挂账在文档)。验收: 测试半边先行红复现 (2 over-cut 场景 cut 真被加) → src+specs 半边贴绿 → lock 手工合并 (本地条款已扩写, GPT hunk 必冲突) → 专项+cuts+binding 498 绿 → ruff 清 → 全量 **2962 绿** (+4 回归) → preflight 21/21 → lock F-CUT-R2-01 条款扩写 CUT-R3-H1 (generic 槽容量语义 + 饱和证明 + 容量 fail-closed) + specs/10 §10.8 扩写。归档 `algoaudit_cuts_face_r3_REVIEW_20260613.md` + `algofix_FCUT_r3.patch`; **r4 (CUT-R3-H1 修复攻击面 + lazy-demand/count cut 族 exactness 角度, 包 278e4d67, 会话 6a2c4999) 判 R3 修复 sound 但抓前提残缝 1 finding (env-gated, 未来 promote 即 HIGH) — CUT-R4-H1**: 饱和只证「非 `__unused__`」不证「routing-visible」— 若某正数 required generic-output commodity 同时是 routing-free generic-input sink (即也在 `required_generic_inputs` 正数集), binding 可把物理输出槽赋给该无线商品, `extract_port_specs()` 按 routing-free 丢弃它 (binding_subproblem:1060) = routing 不要求该 front 可达; 老 cut 在饱和下仍把该口当必然 routed → 误剪合法 placement (probe 实测: binding FEASIBLE + port_specs=[] + 旧 cut 加上后 master INFEASIBLE / 修后 cut_added=False + OPTIMAL)。当前 canonical 数据无此重叠 (generic outputs=blue_iron_ore/source_ore vs generic inputs=qiaoyu_capsule/valley_battery) 且 validator 不强制两集 disjoint = 前提未编码。修 = `_required_generic_outputs_are_all_routing_visible()` (正数 required generic-output 集 ∩ routing-free sink 集 == ∅, 异常 fail-closed False) 作为 `generic_output_visible` 第二合取条件; 混合 routed+routing-free 需求 fail-closed 计 0 (弱化非 over-cut, 待 binding-aware/global count proof 恢复强度)。**r4 还核了**: Q1 饱和证明链独立重证 (binding AddExactlyOne + per-commodity sum==required → required_total==slot_total 时 `__unused__` 必为 0, 不依赖每商品分布) + 容量统计双路径核对 (production `_build_mandatory_groups` 的 count/instance_ids 同源排除错配; fallback 缺 instance list fail-closed None) + 严格等式三分支判读 (required<capacity 只弱化 / > 不饱和 / == 须加 disjointness 才 sound) + 消费者传播 4 行表 (PORT_ACTIVE front-clear / cell-pattern cache / lazy-demand / legacy helper 共享 `_profile_port_demands` 一致); Q2 lazy-demand/count cut 族 exactness (`sum(blockers) <= K - demand` OnlyEnforceIf 定理 + port_binding 组合枚举使 count cut 不挑具体口 = 精确 / 现 canonical 下 boundary_io K=1, K<=demand 多跳过落到 cell-pattern / 触发链与 fail-closed 行为逐项 [env 门 + KeyError 零需求 + 异常 False + 无 slack 侧不发 cut] + cell-pattern×lazy-demand 组合 = 同一 pose 语义两必要条件交集安全); deletion-core 消费 spot-check (port_specs 已滤 `__unused__`/虚拟/routing-free → oracle 按过滤端口集判 front, r4 回归在场); Q3 维持 (V82 persisted cuts telemetry-only / F1-F9 step_2/step_8 stub + phase gate 锚定 / C-3/C-4 留挂账)。验收: 测试半边先行红复现 (visible=(0,1) 应 (0,0)) → src 贴绿 → r4 专项 12 绿 → ruff 清 → 全量 **2968 绿** (+1 回归) → preflight 21/21 → lock F-CUT-R2-01 条款追加 CUT-R4-H1 addendum + specs/10 §10.8 扩写。归档 `algoaudit_cuts_face_r4_REVIEW_20260613.md` + `algofix_FCUT_r4.patch`; **r5 (CUT-R4-H1 修复确认 + PCR-CUT 通道首审 + deletion-core 算法本体, 包 3f4ceebb, 会话 6a2c6ec2) 判 R4-H1 修复 sound 但 PCR-CUT 首审爆 4 HIGH 已修 (全 over-cut 方向, env-gated `EXACT_B1_PATCH_ROUTING_CORE`)**: **PCR-R5-H1** = patch boundary relaxation 只为 ground layer 建虚拟边界变量, elevated state 跨 patch 人工边界被强制 0 = patch 严于 full = 违反 `full FEASIBLE ⇒ patch FEASIBLE` 核心前提 (probe: 单 cell 强制 elevated 桥 INFEASIBLE→修后 FEASIBLE); 修 = 全 LAYERS 建 boundary vars。**PCR-R5-H2** = patch sink port 极性用 `ps.direction` 而 full routing 用 `DIR_OPP` — **F-RT-R2-01 极性缺陷类在重实现中复发** (probe: 直线 corridor patch INFEASIBLE→修后 FEASIBLE); 修 = sink front index/adherence 改 `DIR_OPP`。**PCR-R5-H3** = blocker footprint 编成常量但 UNSAT core/master terms 只盖 port owners → cut 变「victim pose 无条件禁」漏真 blocker; 修 = patch cells+一圈 cardinal 邻域的 occupancy owners 全建 assumption + master cut 前 augment core (多余 term 只弱化)。**PCR-R5-H4** = signature lifting 同 mandatory group 的重叠 lifted var set 重复计入线性 nogood (`2x<=1` 把共现禁强化成单 pose 禁); 修 = lifted var 集相交即 fail-closed 返 added=False (caller 按 reject 回落)。**r5 还核了**: Q1 CUT-R4-H1 确认 (disjointness 两集与 binding extract_port_specs 同口径 + certified 路径单源快照传递链逐 file:line + 无第三 saturation consumer + broad except 方向 fail-closed); Q2 PCR 前提编码位置全判读 (free cells over-approx / patch 外 terminal 丢弃 over-approx / 修后 boundary 全层 over-approx / QuickXplain cap 命中=非最小核=更弱方向 / replay validate=patch 模型内 replay proof 不替代 encoding 级义务); Q3 deletion-core 干净 (oracle 弱于真 precheck [漏判类型逐列] = 核偏大方向安全 / deletion-minimal 非 minimum 且调用方不消费 minimum 语义 / pose_idx_by_id 只来自保留集 S = 量化范围==支撑集 / binding alternatives 未穷尽不进 deletion-core)。验收: 测试半边先行 4 红复现 → src 贴绿 → 专项 496 绿 → ruff 清 → 全量 **2980 绿** (+4 回归) → preflight 21/21 → lock 新增 PCR-R5 四义务条款。归档 `algoaudit_cuts_face_r5_REVIEW_20260613.md` + `algofix_FCUT_r5_pcr.patch`; **r6 (PCR-R5 修复确认 + ladder 交互 + cut 生命周期, 包 38b57070, 会话 6a2c82da) 抓 1 HIGH 已修 — PCR-CUT-R6-H1 (patch 端口成员判据用 connector 而非 terminal front)**: connector 在 patch 外但 front cell 在 patch 内的端口被 `_index_port_fronts`/`_add_port_adherence`/separator/local signature 四处整体丢弃, 而 connector 是 occupied 格不入 active cells → boundary relaxation 不会为它开变量 = 端口注入/吸收能力直接消失, patch 比 full 更严 (严格反例 probe: full FEASIBLE / patch INFEASIBLE / source front 空, CC 本地双向复现 [修前 INFEASIBLE→修后 FEASIBLE+front 正确]); 修 = 四处全改为「connector **或** front 与 patch 相交」即纳入 (front-in-patch 外部端口入 signature 防 lifting 误并)。恰为 r6 brief Q1-③「connector 伸入」预言点位 = 确认轮第四次实证。**r6 强干净证据**: Q1 其余四义务复核全过 (H1 全层无 residual 缝 + 边界变量形态 over-approx / H2 极性从规则文本独立推导对照 patch=full 一致 / H3 cardinal ring 对 occupancy 充分 [本轮缝是 terminal 语义非 occupancy] / H4 added=False 回落无状态污染) + Q2 **ladder 5 rung 交互表** (D2→PCR→deletion→lazy→cell 次序互斥 [成功即 skip 后续, 无同 conflict 叠加]; 全失败 = cut_stall + UNKNOWN 不假装剪枝; rung 间无状态泄漏) + Q3 **cut 生命周期 6 场景表** (同实例累积单调无删除 / 重建不带旧 cut = 纯收敛性 / persisted exact_safe 在 certified 主路径被 `raw_candidate_cuts=[]` 强制清空 + metadata 标 replay disabled / within-instance lifting 修后仍强制)。验收: 测试半边先行 3 红复现 → src 贴绿 17 → 专项 497 绿 → ruff 清 → 全量 **2985 绿** (+3 回归) → preflight 21/21 → lock PCR 第五义务条款。归档 `algoaudit_cuts_face_r6_REVIEW_20260613.md` + `algofix_FCUT_r6_front_port.patch`; **r7 (R6-H1 修复确认 + QuickXplain/replay 本体 + patch 构造对接, 包 37b84be0, 会话 6a2c8a7a) = 零 soundness finding (本面首个干净轮)**: Q1 R6-H1 五向确认 (**端口×patch 相交 5 形态全表** [双内/外 connector 内 front/内 connector 外 front=boundary 吸收/双外 transit=无端口语义/front occupied=forbid owner 或 fail-closed] + front-in-patch 外部端口走 exact terminal link 且 owner 必入 assumptions [patch_ports instance_id 显式并入] + signature 新维度只细分不合并等价类 + support augmentation 把 blocker owner 加回 core 不再 blame victim); Q2 **QX/replay 本体首审** (oracle 单调性方向验证 [assumption 越多越约束, probe 四子集状态序列实证] + 递归对照 monotone 结构 + **cap 命中由 replay 兜住** [probe cap=0: QX 返回 invalid core → fallback raw validated, reason 字段明示] + replay 同 model 新 CpSolver、assumptions proto 清空重建无状态泄漏 + augment 是 assumption superset 无需二次 replay [单调性已证]); Q3 **8 行对接保真表** (patch cells 选择与 port/active/support 收集同口径跟最终 candidate 走、无截断错位; precheck summary 不参与 port 重建 [直接用 binding 后 port_specs 原始字段, 无二次极性翻译点]; active cells 与 placement_solution 同快照; 多 patch 逐个新建零污染; master add 全 fail-closed)。验收 (零轮规程): 行号抽查 3 处命中 (:584-601 boundary 吸收 / :757-784 assumption 清重建 / :948-987 QX 回落) + GPT 专项 17+34 绿 + 零代码改动基线 **2988 维持** (b044799 验证)。归档 `algoaudit_cuts_face_r7_REVIEW_20260613.md`; **r8 (终饱和轮: 跨通道 cut 语义族谱 + BendersCut/condition 本体 + 自由攻击角, 包 37b84be0, 会话 6a2c9665) 抓 1 HIGH 已修 — CUT-R8-H1 (D2 commodity-flow cut 丢失 occupancy proof context)**: env-gated `EXACT_B1_D2_COMMODITY_FLOW` rung (front_blocked ladder 首位, 成功即 skip 其余通道) 的 master cut 只用 raw terminal core, 但 D2 模型把当前**全 layout footprint 编译成常量 occupied grid**、非 core terminal helper 也以当前位置参与约束 → CP-SAT assumption core 只覆盖 assumption literals, 常量 context 不受保护 — "当前障碍下该 terminal 子集不可行"被升级成"含这些 pose 的任意 layout 不可行" = over-cut (**PCR-R5-H3 constant-support 义务在 D2 通道的复发**; toy 反例: 单行走廊 + 墙挡中点 → INFEASIBLE core 只含 src, 移墙后同一对 src/sink pose FEASIBLE); 修 = support-augmented conflict set (`_build_d2_supported_conflict_set` = 全部 port owners + 全部 occupancy contributors + raw core, `ghost_pick` 除外; 并入只弱化, 被禁集合回落到 proof context 内) + metadata `support_conflict_size`/`support_owners`。**r8 强干净证据**: Q1 跨通道 cut 语义族谱 8 行表 (D2/PCR/deletion-core/lazy-demand/cell-pattern/fallback selected/binding-local/whole-layout — 各通道「实禁集合 ≤ 证明义务」逐项判读, 唯一违反者即 D2 已修; **弱序审计**: 各 rung 失败均 fail-closed 或落更弱通道, 不向更强 cut 升级) + Q2 BendersCut/condition 本体链路 10 点表 (字段保真 to_dict/from_dict roundtrip 含 condition_set / certified 空 condition raise / dedup key = (conflict_signature, condition_signature) 不互吞 / condition resolver 全 fail-closed ([], False) / 三条 master apply 路径全量解析才加 + OnlyEnforceIf / persisted replay 强制 raw_candidate_cuts=[] / power conditioned 取不到 anchor 即 ABORT — 未发现 condition 丢失致无条件 over-cut) + Q3 自由攻击角二缝 (D2 = 本 finding; cell-pattern self-front/duplicate-literal seam 实证 probe 66,403 poses / 599,382 ports → self_front=0 outgrid_front=0 当前工件无缝, duplicate guard 留作未来防尘建议)。验收: 测试半边先行红复现 (unpatched conflict 实测 `{'src': 0}` 裸 core) → src 贴绿 → ruff 清 → 专项 cuts 463 + condition/PCR/D2 37 绿 → 全量 **3006 绿** (+2 回归) → preflight 21/21 → lock 新增 CUT-R8-H1 条款 (separator cut 不得窄于其模型编译进的 layout context, 泛化到一切 separator 通道) + specs/10 新增 §10.9。归档 `algoaudit_cuts_face_r8_REVIEW_20260613.md` + `algofix_CUT_r8_d2_support.patch`。审包注: r8 审的 37b84be0 在其在途期间被 095a0b6d 替换 (Sources 清理), 沙盒开工时已解包未受影响 (sha 校验过); **r9 (确认轮: CUT-R8-H1 修复确认 + 自由攻击角, 包 13fbe643, 会话 6a2ca720) 判 R8-H1 修复 sound 但自由攻击角抓 1 HIGH 已修 — CUT-R9-H1 (D2 模型不是 production routing 的 relaxation, 根前提击穿)**: D2 至少两处编码比 production **更严** — per-cell `AddAtMostOne` 是 2D 表达不了跨层 bridge crossing; 单位流守恒表达不了 splitter/merger 一源多汇 → production 可行 layout 可被 D2 判 INFEASIBLE, 即使 support 全并入 cut 仍禁真可行解 (独立于 R8-H1 的第二种 over-cut; **双 probe 实证**: 两层 crossing + splitter 拓扑, 双双 precheck=feasible + RoutingSubproblem=FEASIBLE + raw D2=INFEASIBLE + 原 separator cut_added=True)。修 = D2 INFEASIBLE 不再独立作 proof source — separator 入口对同一 occupied+port_specs **重跑 production precheck**, 仅 `front_blocked`/`relaxed_disconnected` 放行 (其余 deny-unknown 返 MODEL_INVALID 不建模不写 cut); 配套 `_placement_to_occupied` 显式跳过 ghost_pick (separator occupied ≤ production 口径, blocked/disconnected 判定沿障碍单调安全) + 无主 port spec fail-closed + metadata 记 precheck status/domain_stats。**CC 独立核了修法 soundness 前提**: benders_loop :5325-5381 分支结构 = front_blocked master-cut ladder 仅在 `binding_selection_safe_reject=False` 或 binding alternatives 穷尽后到达 → D2 cut 与 fallback selected nogood 同位置同保护, cut 范围 (support 全集) ⊇ fallback tuple 只弱化。**R8-H1 修复四点全确认**: ① support 与 `_placement_to_occupied` 逐条件同口径 (pool/pose_idx 边界/空 footprint/缺 facility_type; ghost 口径分叉已焊) ② 非 core terminal 并入必要 (helper literals 在当前模型参与 terminal context) 且补丁把无主 port 第三形式 fail-closed ③ dict 合并三来源同 instance pose 恒一致, 异常 pose_idx 由 master 侧拒绝, presence-nogood 语义对接正确 ④ fail-closed 路径全维持 + 新增 precheck gate 一层。**rung wiring 复核**: 上层调用条件 front_blocked 已部分遮挡 runtime 风险, gate 下沉使 standalone separator 也 fail-closed; `_b1_d2_skip_other_cuts` 只跳过更慢 fallback 不扩 proof 范围。验收: 测试半边先行 2 红复现 (production-feasible 双拓扑下原 separator 真写 cut) → src 贴绿 4 → ruff 清 → 专项 cuts+condition+PCR+routing 510 绿 → 全量 **3010 绿** (+2) → preflight 21/21 → lock 新增 CUT-R9-H1 条款 (非证明 relaxation 的 separator 模型不是 proof source) + specs/10 新增 §10.10。归档 `algoaudit_cuts_face_r9_REVIEW_20260613.md` + `algofix_CUT_r9_d2_relaxation.patch`; **r10 (确认轮: CUT-R9-H1 修复确认 + 自由攻击角, 包 2cd169b4, 会话 6a2cb114) = 零 soundness finding + 1 LOW fail-closed 合同加固已落**: Q1 四点全过 — ① **precheck gate 语义对齐** (separator 简化调用与 production 四种调用形态 [placement_core/owner-map/RoutingGrid/from_placement_core] 4×4 probe 矩阵 status 完全一致 [feasible/front_blocked/duplicate_front_key/relaxed_disconnected]; status 全集枚举 = 三值, deny-unknown 无遗漏且对未来新值闭合; owner-map 缺省只影响诊断字段不参与可达性判定, D2 cut 并入全 occupancy support 故诊断差异非 proof 差异) ② **单调性三问独立复核** (front cell 判定 :433-435 = in-grid + resolved_free_cells, 障碍单调; relaxed_disconnected 在 free-cell 图上, 加障碍只切分不合并 component; production :6025-6055 与 separator 同字段同 ghost-skip 口径, separator 宽容方向 = 少算障碍 = 安全) ③ **ladder 前提复核** (safe-reject ladder :5328-5382 先接管 binding alternatives; D2 rung :5384-5448 仅在固定 binding 情形到达; relaxed_disconnected branch 不调 D2; support tuple ⊇ fallback placement_level_conflict_set = 只弱化) ④ **MODEL_INVALID 消费** (caller 只看 cut_added, false 即 fall through 到 PCR/deletion/lazy/cell/fallback, 无证明性误读)。**LOW CUT-R10-L1 已修**: 原 owner 校验 `str(None)→"None"` 当有主端口 + 不校验 owner∈placement → synthetic owner `{"None": -1}` 可进 conflict (production 不可达: extract_port_specs 产 string owner + 真 delegate 拒 -1, 但违 r9 宣称的 fail-closed 边界); 修 = `_d2_port_owner_validation_error()` 前置拒 None/空白/ghost/不在 placement + 2 回归。**r8+r9 叠加链路复查**: crossing/splitter 双反例在叠加补丁下 = precheck feasible → separator MODEL_INVALID 不写 cut ✅。验收: 2 红复现 → 贴绿 6 → ruff 清 → 专项 580 绿 → 全量 **3020 绿** (+2) → preflight 21/21 (lock/spec 语义已覆盖不需新条款)。归档 `algoaudit_cuts_face_r10_REVIEW_20260613.md` + `algofix_CUT_r10_owner_validation.patch`; **r11 (终饱和轮: CUT-R10-L1 修复确认 + 全通道自由攻击, 包 eca69648, ⚠️model gpt-5-5-thinking 非 Pro) = 零 soundness finding** (REVIEW 质量高: Q1 CUT-R10-L1 D2 owner gate 逐项确认 + 自跑 None/blank/ghost/missing probe 全 fail-closed; Q2 深挖 lazy connectivity [自检 vertex-cut 证书] / deletion-core [oracle 弱于 precheck 核偏大方向安全, 调用方不消费 minimum 语义] / 跨通道 ladder 弱序 [强 separator 成功即停, 失败只回落或 UNKNOWN] 全 sound; 抽查 PCR/cell-pattern/conditioned cuts; 独立判 cuts 面达饱和下沿)。无代码改动。归档 `algoaudit_cuts_face_r11_REVIEW_20260613.md`; **r12 (再确认轮: owner 复查后用真 Pro `gpt-5-5-pro` 重审, 包 c9315ba2) 抓 1 HIGH — CUT-R12-H1 (power-conditioned infeasible cut 丢 unpowered fixed-occupancy support: `PowerPlacementSubproblem` 把所有非 pole 设施 [含不需电的 boundary_storage_port×46/protocol_core×1] 纳入固定障碍挡 pole, 但 INFEASIBLE cut 只收 powered poses 进 conflict_set, 丢真正挡 pole 的 unpowered blocker → blocker 移走后误禁合法 layout; = CUT-R8-H1 constant-support 义务在 power 通道复发; gated EXACT_POWER_PLACEMENT_SUBPROBLEM, 默认 certified 不暴露 forensic-bypass 才 unsound)。修 = conflict_set 收所有非 pole/非 ghost selected occupancy + pose_idx 解析失败 ABORT。验收: 测试半边红 (conflict_set 丢 boundary_001) → src 绿 → ruff → 全量 **3037** → preflight 8 → lock 新增 CUT-R12-H1 条款 → 归档。**真 Pro 抓出 r11 (thinking) 漏的 finding = cuts r11 零 round 作废, streak 归 0**。归档 `algoaudit_cuts_face_r12_REVIEW_20260613.md` + `algofix_CUT_r12_power_support.patch` + probe | 0 (r11 thinking 零 round 作废 + r12 真 Pro 抓 CUT-R12-H1, streak 0) | r12=修复轮; **r13 (CUT-R12-H1 确认轮, 真 Pro gpt-5-5-pro) 又抓 1 finding (CUT_R13_power_ghost_context, 真 Pro 在 power 通道连环抓第二个, 待验收)**; cuts streak 仍 0; C-3/C-4 latent 待办仍挂账 |
| 5 | preprocess 链 | r1: F-01/F-02 → fbb0466; r2: F-03 → b7d2115; r3: F03-R3-01 (RAB build-time 侧门, env 潜伏) + H03-R3-02 (dual-role 语义守卫) → 51c5f90; **r4 (front 消费点全仓穷举轮, owner 手动发) 又出 4 组 residual 已修复落地**: F04-R4-01 (preprocess_context 直构路径绕过 dual-role 守卫 → 双层 fail-closed) + F04-R4-02 (deletion-core raw oracle, env `EXACT_B1_DELETION_CORE_CUT`) + F04-R4-03 (pose-bool master 四处 raw port/front: PORT_ACTIVE/CLEARANCE_HARD/blocking-cell/lazy-demand, env 门控) + F04-R4-04 (SAC/L2/dynamic separator 把 routing-free 终品当 routed source)。验收: 2 probe unpatched 复现 + patched 翻转, 行为级判别 (separator 分类 probe + pose-bool :303/:926 现场), 全量 **2908 绿** (+5 回归), preflight 20/20, lock/spec 消费点清单扩列; **r5 确认轮 (包 e676c94d, owner 手动发) 零 soundness finding** — 4 组修复逐处 file:line 复核 + r4 穷举清单独立复核同意 (port_exposure_oracle 零生产调用 / flow diagnostic 不作 proof, CC grep 抽查二者属实) + r2/r3/r4 交互复核 + 文档清单与代码一致; 归档 `algoaudit_preprocess_face_r5_REVIEW_20260612.md`; **r6 (非 wireless 角度, 包复用 e676c94d) 又出 1 HIGH — R6-F-01**: `preprocess_plan.json` 可静默同名覆盖 canonical recipe/target/commodity (probe: packaging_battery input_slots 3→1 静默接受 = 欠约束/false-CERTIFIED 方向) 且 plan 完全不在 exact campaign hash 闭包与 preflight 冻结登记 (同一 campaign hash 两种运行时端口语义)。**修复已验收落地**: builder 对三键 fail-closed (additive-only) + schema 收紧 + `OPTIONAL_EXACT_HASH_FILES` 纳 hash 闭包 (missing sentinel 保 synthetic) + CC 补 FROZEN_ARTIFACTS 登记与 lock/specs04/18/19/20 同步 (含纠正 specs18 "regeneration-only" 过时论断); probe 双向翻转, 全量 **2912 绿** (+4), preflight **21/21**; r6 其余 Q1-Q5 零 finding (demand 数学/池计数 66403 闭式吻合/profile 投影/确定性/三件一致性, 复核详尽)。归档 `algoaudit_preprocess_face_r6_REVIEW_20260612.md` + `algofix_R6_plan_hash_overlay.patch`; **r7 确认轮 (包 e8c7dac3, 干净 worktree 唯一名包) 零 soundness finding**: R6-F-01 修复逐项确认 (builder gate 全装载路径覆盖 / sentinel 形状不撞 sha256 / resume dict 全等 probe 验证 / binding 直读 plan 不消费三键且 bytes 已 hash 绑定) + **Q2 泛化穷举 16 类 runtime 输入面对照 hash 闭包无同类缝** (commodity_demands=diagnostic-only 留再审触发条件: 若未来 certified 分支在它上面; CC grep 抽查属实) + r6 数字抽查 (266/52 槽/池 hash) 全验证 + 2 条 DOC-LOW (01=plan metadata 措辞 stale, 动冻结工件不值挂账; 02=specs19 措辞, CC 已落地 spec-only 修复); 归档 `algoaudit_preprocess_face_r7_REVIEW_20260612.md`; **r8 (枚举完备性+解析纵深角度, 包 50360c1d) 抓 1 HIGH — F-PRE-R8-01**: preprocess 再生成链 4 个装载点 (preprocess_context 的 canonical/plan 读 / placement_generator 的 canonical 读 / instance_builder 的 machine_counts 读 / build_current_preprocess_context 的 parity 消费) 仍默认 `json.loads` — 与 F-BIND-R2-02 同根: hash 闭包钉字节不钉解析, 重复 key last-write-wins 首次构建可静默改写 (probe: canonical 重复 value key → valley_battery target 3.0→999 静默接受)。修 = 共享 strict loader `src/io/strict_json.py` (拒重复 key + NaN/Infinity) 四处换用 + 生成侧 dump `allow_nan=False`。**r8 强证据**: Q1 枚举完备性**独立重建** expected pose 池逐项比对 (不调用被测 generate_all_pools, 从 canonical 模板维度自构) — 7 池 66403/66403 零 missing 零 extra, anchor 域/orientation 等价类/边界裁剪逐池判读; Q3 ceil 数学 17 operation 全表核对 (理论→ceil→冻结双工件一致, 219+1+46=266); Q4 生成契约与 F-BIND 新消费契约一致 (角色枚举先在 context validation 报错, 不会静默产出被拒工件)。验收: probe unpatched 复现→patched 翻转→ruff 清→**candidate_placements 再生 hash 不变** (adcc2a6e/45773799, 写出侧改动零内容漂移)→全量 **2933 绿** (+6 回归)→preflight 21/21→lock 新增 F-PRE-R8-01 条款+specs/18 同步。归档 `algoaudit_preprocess_face_r8_REVIEW_20260612.md` + `algofix_FPRE_r8_strict_json.patch`; **r9 (r8 修复攻击面 + 交叉一致性/再生确定性角度, 包 ec504afe) 抓 1 Medium — F-PRE-R9-01**: strict JSON 未拒数字上溢 — `1e309` 字面量经 `json.loads` 解析成 `inf` (`parse_constant` 只拦拼写常量, 没设 `parse_float`), 且 `build_current_preprocess_context.py` 复用的 writer 无 `allow_nan=False` → 上溢值可流入 context/parity 工件并写出非标准 `Infinity` (probe: canonical 注入 1e309 → context 工件含 `Infinity`, 实测复现)。当前冻结工件干净 + hash 闭包仍挡已登记工件漂移 = 再生链 strictness 完整性/可用性缺陷, 非 soundness 破坏。修 = `parse_float` 拒非有限 (正常 float 行为不变) + 本地 strict atomic writer (保留 fsync 语义 + `allow_nan=False`)。**r9 强干净证据**: Q1 装载点穷举无第五处默认 `json.loads` + 写出点四处全表 + 嵌套/数组内重复 key probe 过; Q2 三件套×canonical 交叉一致性 12 行矩阵全过 (模板集 7/7 相等 / 266 实例 0 unknown / 角色闭合 / parity 6/6, 无「生成能过消费静默吞」构造); Q3 再生确定性 (无随机/并行/时间戳入 JSON, 固定枚举序+紧凑分隔符, 修复后再生 hash 字节级不变 adcc2a6e); Q4 当前合法工件上零行为差 (不存在 r8 前静默接受 r9 后报错的工件)。验收: probe 复现 1e309→inf → apply → stash 判别 4 回归翻红 → ruff 清 → 全量 **2955 绿** (+4) → preflight 21/21 → lock/specs18 条款扩写 R9。归档 `algoaudit_preprocess_face_r9_REVIEW_20260612.md` + `algofix_FPRE_r9_overflow.patch`; **r10 (r9 修复确认 + pose 几何变换数学本体 + schema/消费对齐, 包 70457b5e, 会话 6a2c54c5) 判 r9 修复 sound 但 fail-open 族再爆 2 Medium + 1 Low 已修**: **F-PRE-R10-01 (Medium)** = path loader (`load_default_preprocess_context`/`load_preprocess_context_from_paths`) strict-load 后直接构造 context, 从不跑 schema 校验 → schema-required 字段 (tick_interval_seconds / utility 槽数) 缺失被代码默认值吞 = 「schema 锁了, 消费侧绕过」fail-open (probe: 删 tick 字段照样 default 2.0 通过); 修 = 文件入口 strict-load 后、defaults 前跑双 schema 校验 (dict-level builder 保持纯构造供测试变体)。**F-PRE-R10-02 (Medium, latent)** = generator 闭式几何族硬编码 (core 9x9+6/14 / omni 3x3 / pole 2x2+radius5 / boundary 1x3 / long_sides w>h / square w==h) 从不对照 canonical dimensions → schema-valid 的 canonical 漂移会「canonical 说一套 pose 发另一套」(probe 6 case 全静默通过); 当前 canonical 干净 = 未来 owner-gate 扩展缝; 修 = `_validate_template_geometry_contract()` 分派前 fail-closed 锁 contract。**F-PRE-R10-03 (Low)** = `current_preprocess_context.json` (POSTPROCESS_ONLY audit 工件, 不在冻结登记) metadata 3 字段过时 (r6 改名 canonical_rules_plus_overlay→canonical_rules 后未同步); 修 = 再生同步。**r10 还核了**: Q1 r9 修复 11 边界 probe (正常 float/科学计数/负零/subnormal/长精度/巨 int 全同 std; ±1e309/嵌套上溢全拒; 字符串 "1e309" 合法保留) + parse_int 无对偶缝 + writer fsync/replace 原子链 + 11 个冻结/审计 JSON 全 strict-load 可过; **Q2 pose 几何变换数学独立重推** (端口=bbox 外一格 + front=port+DIR_DELTA + 90° 旋转边/极性映射 top/N→right/E 全表) + **9 行手算对照** (6x4 四 mode 坐标逐口 / core 9x9 sparse-dense 双 mode / 方形折叠安全性 [body 对称但 4 port_mode 保留] / 无端口模板折叠 / boundary 两基座) + 7 池计数复核 66403 + bad_physical_ports=0; **Q3 15 行 schema×消费对齐矩阵** (R10-01/02 修复行 + recipes/targets/commodity 双层校验对齐 + plan additive-only 双防护)。**CC 验收连带**: 生成脚本 Windows 上写 CRLF (writer 没钉 newline, Linux 沙盒盖不到) → `newline="\n"` 双写出点修复 + 本地再生实证幂等 (diff_report 字节级不变)。验收: 测试半边先行 3 红复现 → src+data 贴绿 → 专项 58 绿 → **candidate_placements 再生 hash 不变 (adcc2a6e/45773799, 无需推进登记)** → ruff 清 → 全量 **2972 绿** (+3 回归) → preflight 21/21 → lock 新增 F-PRE-R10-01/02 双条款 + specs/18 同步。归档 `algoaudit_preprocess_face_r10_REVIEW_20260613.md` + `algofix_FPRE_r10.patch`; **r11 (R10 修复确认 + demand solver 数学本体 + canonical 投影保真, 包 3f4ceebb, 会话 6a2c7833) 抓 3 HIGH 已修**: **F-PRE-R11-01** = `load_templates()` 即 Q1 预言的「第三个 canonical 文件入口」(只 strict-load 不 schema 校验, schema-required 缺字段可绕守卫进候选再生链; 修 = strict-load 后立即跑 canonical schema); **F-PRE-R11-02** = geometry contract 漏锁 `rotatable`/`is_solid_z` (schema-valid 把 6x4/core/boundary 的 rotatable 翻 false 仍发旋转 pose, is_solid_z 翻 false 仍无条件发 occupied_cells = canonical 说一套 pose 发另一套; 修 = per-family 锁双字段 + bool 类型守卫; 交付含 6 模板族字段覆盖矩阵, needs_power 判读为非 generator 几何假设不入 contract); **F-PRE-R11-03** = cycle group 只验 square/singular 不验非负 (canonical 漂移可解出负机器率, 下游 `_sort_fraction_mapping` 的 `>0` 过滤静默吞 = 冻结需求工件缺机器; probe: seed 产出 2→0.5 时 P=-D/C=-2D 仍成功构造 context; 修 = net_export∈internal 校验 + 每 net-export 单位需求非负基解证明 + solve 时负 run rate fail-closed)。**r11 强干净证据**: Q2 **17 操作完整 demand 链手算对照冻结工件全对** (52/34/18 口预算 + 农业循环 det=1 解 P=2D,C=D + ceil 单层位置与 specs/04 一致, 无链式过早 ceil); Q3 **6 recipe 抽样 vs vendored 上游逐字段忠实** (craftingTime/2=ticks 换算一致, semantic_mapping 注释痕迹齐, 无痕迹漂移零)。验收: 测试半边先行 3 红复现 (2 新函数 + 1 既有测试 4 新 case, 计数差额查清) → src 贴绿 44 → 专项 65 绿 → **candidate_placements 再生 hash 不变 (adcc2a6e/45773799, 本地实测)** → ruff 清 → 全量 **2982 绿** (+2) → preflight 21/21 → lock 新增 F-PRE-R11-01/02/03 三条款 + specs/18 同步。归档 `algoaudit_preprocess_face_r11_REVIEW_20260613.md` + `algofix_FPRE_r11.patch`; **r12 (R11 修复确认 + 实例展开器本体 + 工件交叉一致性, 包 37b84be0, 会话 6a2c8ec0) 抓 1 HIGH 已修 — F-PRE-R12-01 (cycle RHS 成员闭包 fail-open)**: RHS 按 `internal_commodities` 列表组装 → 列表外 key 的正需求被静默丢弃; `cycle_internal` commodity 只查 group 存在不查反向成员 → canonical 加新 cycle 商品漏列 internal 列表时, 下游正流量 3/5 配零支撑机器进冻结工件 (probe: ghost_spore 攻击, CC 本地红复现 + 修后四向翻转 [正非 export 拒/未知 internal 拒/负拒/零保留]); 另堵「正需求打非 net-export internal」绕过 R11 非负证明前提的入口。修 = 反向索引校验 + solver 入口 RHS 规范化 (正 key 必须 internal∩net-export) + 保留逐项负值兜底。**r12 强干净证据**: Q1 R11 三修复确认 (schema 缺失/损坏 fail-closed 实测 / **7 模板族 rotatable·is_solid_z 期望值表独立对照 canonical 真值全对** + truthy 非 bool 实测全拒 / **多 net-export 线性论证验证**: Fraction Gauss-Jordan 无 ceil 无过滤, 解对 RHS 线性 ⇒ 单位基非负 + 非负组合 = 非负, R11 证明框架前提成立 [本轮 finding 即该前提未在入口强制]); Q2 **实例展开器首审**: 266 重算精确对上 (219 制造 [17 操作 ceil 表逐项] + 1 core + 46 boundary; 分布 3x3=132/5x5=49/6x4=38), profile 从 context 派生无第二张手写表, id 排序+三位序号确定性稳定, wireless 标注与 generic_io 同源 (52 slot 对 34+18); Q3 **工件交叉一致性矩阵 9 行** (hash 闭包 = mandatory/candidate/canonical/generic_io/plan 五件; machine_counts/port_budget/commodity_demands 闭包外但 certified runtime 不消费 [load_project_data certified 只读 mandatory, port_budget 实现里显式 del]; 部分再生撕裂由 golden/parity tests 抓 = 设计边界非缝)。验收: 2 红复现 → 贴绿 21 → ruff 清 → 宽专项 40 绿 (含 golden 再生) → candidate hash 不变 → 全量 **2990 绿** (+2) → preflight 21/21 → lock 新增 F-PRE-R12-01 条款 + specs/18 同步。归档 `algoaudit_preprocess_face_r12_REVIEW_20260613.md` + `algofix_FPRE_r12.patch`; **r13 (确认轮: R12-01 修复确认 + R11 维持 + 自由攻击角, 包 095a0b6d, 会话 6a2c9f6a) 判 R12-01 修复 sound 但自由攻击角抓 1 HIGH 已修 — F-PRE-R13-01 (cycle group recipe I/O 闭包未校验, fail-open)**: cycle 矩阵与解后 machine-run 聚合只看 `internal_commodities`, cycle recipe 的 inputs/outputs 引用列表外 commodity (canonical drift 场景, 如 planter 加 source_ore 输入) 时该外部消耗/产出**静默漏出** `commodity_demands`/`port_budget`/`generic_io_requirements` — 冻结工件少算外部供料却仍报 52-port budget 可行 (probe 实测: 突变 rules 后 context 照常接受, source_ore flow 18 漏 planter 额外 11/tick, port total 仍 52; **R12-01 成员闭包类从 demand keys 延伸到 recipe I/O 的同族缝**)。修 = 双端 I/O 闭包校验 (context validation + `_solve_cycle_group_exact()` 入口, 覆盖未验证 context 直调; `inputs ∪ outputs ⊆ internal_commodities` 否则 ValueError 列明 recipe:commodities)。**R12-01 修复四点全确认**: ① 反向索引覆盖完备 (builder 后立即 validate, 两文件入口同 builder; 全仓语义消费点穷举 = demand_solver 转发 + public solver 入口自带规范化, 绕过 builder 直调也兜住; 六向 probe 全 REJECTED/正确) ② RHS 三分支边界 (internal/net-export 两集合同一 group 对象派生与 R11-03 证明同源无第二套重算; 零需求归一不入 normalized_external_demands 无松弛旁路) ③ 实现边界 (空 group schema minItems 拒 + 直调 builder 反向成员拒; 单 commodity 奇异矩阵拒; 跨 group 共享 commodity 角色一致性拒 — 三向 probe 实测) ④ 冻结工件侧 (再生对比 6/6 matches_frozen, candidate hash/size 不变)。**R11 三修复维持确认** (schema 入口/几何契约钉死/非负证明完好, R12 改动未削弱)。验收: 测试半边先行 2 红复现 (unpatched 不 raise) → src 贴绿 23 → ruff 清 → 宽专项 79 绿 → 全量 **3008 绿** (+2) → preflight 21/21 → lock 新增 F-PRE-R13-01 条款 (cycle group = recipe I/O 闭包双端校验) + specs/18 R13 段。归档 `algoaudit_preprocess_face_r13_REVIEW_20260613.md` + `algofix_FPRE_r13.patch`; **r14 (确认轮: R13-01 修复确认 + 自由攻击角, 包 2cd169b4, out_dir 20260613_pre_r14) 判 R13-01 sound 但自由攻击角抓 2 HIGH 已修 — F-PRE-R14-01 (多输出 recipe co-product 重复计数: demand backprop 按输出逐项 charge run, schema-valid 双输出会重复 charge 同一 operation, probe packaging_battery 3→4 虚增 mandatory → 潜在 false-INFEASIBLE; 修 = 三处 fail-closed: schema outputs maxProperties:1 + validate_canonical_document + validate_preprocess_context len(outputs)!=1) + F-PRE-R14-02 (cycle-internal commodity 被组外 recipe 产出: role.cycle_group 非空商品的正需求直转 cycle solver, 在非 cycle producer index 查询前 → outsider 机器/输入静默漏算 fail-open, probe synthetic_orb 机器缺失 + source_ore 不计; 修 = context validation 拒任何非组内 recipe 输出 cycle-internal commodity, 非 cycle 消费仍是合法外部需求边)**, 两修均 fail-closed 不改冻结 canonical 产物。**接管说明: 本轮由并发会话 e8a750c5 应用补丁+lock+spec (质量合规), 被 owner 停会话打断 → SessionEnd 兜底成 WIP commit 27967ec → 本会话 20690dc4 接管收尾验证**。验收: worktree 隔离修复前红复现 (4 R14 回归在 91a4865 源码全 DID-NOT-RAISE = fail-open) → 修复后全量 **3024 绿** (+4 回归, 0 failed) → ruff 清 → preflight 8 obligations/21 → 27967ec 零 `data/` 冻结产物改动 (frozen parity 自动保证) → lock 已含 F-PRE-R14-01/02 双条款 + specs/18 R14 两段。归档 `algoaudit_preprocess_face_r14_REVIEW_20260613.md` + `algofix_FPRE_r14.patch`; **r15 (确认轮: R14-01/02 修复确认 + 自由攻击角, 包 eca69648, ⚠️model gpt-5-5-thinking 非 Pro) 抓 1 HIGH 已修 — F-PRE-R15-01 (public solver 入口 `solve_demands_exact(context=...)` 直用 caller 传入 context 不重验 → deep-copy/overlay 篡改绕过 R14 三道 fail-closed 门, 两 probe: co-product 重复计数 + 组外 producer 漏算; 修 = 加 `validate_preprocess_context` + R14 校验抽 `_validate_single_output_recipes`/`_validate_cycle_internal_output_ownership` 共享 + raw cycle solver group-local 兜底)。验收: 测试半边红 (3 DID-NOT-RAISE) → src 贴绿 → ruff → 全量 **3036** → preflight 8 obligations → lock/spec 同步。归档 `algoaudit_preprocess_face_r15_REVIEW_20260613.md` + `algofix_FPRE_r15.patch` | 0 (又抓 HIGH, 连零归零) | r15 抓 F-PRE-R15-01 已修; ⚠️model 降级待 owner 裁决 |
| 6 | binding 建模忠实度 | **首轮 (2026-06-12, 包复用 13dc4e59, 脚本全自动外发) 爆 2 HIGH 已修**: **F-BIND-R1-01** = generic output 槽 domain 无 `__unused__` 哨兵, 把 specs/04 §4.5「52=52 满额」数值巧合硬编码成结构假设 → 需求<槽数时合法空置被判 INFEASIBLE (false-INFEASIBLE → objective 级 false-CERTIFIED 方向)。**定性诚实**: 当前 active scope (R=S=52 满额) 下 latent, 不是 active bug; 与 C-1 refute **不冲突** (C-1 补丁改坏精确计数被禁是对的; 本修保留精确计数只加哨兵, 52=52 下计数逼满哨兵恒 0 行为零变化) — specs/03 多处「多余端口允许空置」证实规则层面空置合法。**F-BIND-R1-02** = generic IO/wireless 槽数 loader fail-open: 缺 section 静默空需求 (真实需求消失=false-FEASIBLE 方向) + int() 接受 bool/float 截断/字符串 + 不校验 canonical 商品角色 (中间品可冒充无线终品被吞, routing 不兜底)。修复: loader fail-closed (双 section 必须在场+严格非负整数+哨兵保留名+默认装载路径校验 source_kind/sink_kind) — 生产三调用点 (benders 主链:4909/retry:5813/heuristic finder) 全走 loader 路径实证受保护, 显式传参为 test-fixture-only。验收: 3 probe unpatched 复现 (F-01 INFEASIBLE / 缺 key 静默 / steel_block 被吞) → patched 翻转 2 个 + F-02-B 经 loader 路径篡改工件 probe 补证 ValueError → ruff 清 → 全量 **2923 绿** (+6 回归) → preflight 21/21 → lock 新增 F-BIND-R1-01/02 两条款 + specs/04 §4.5 实现注记 + specs/05 §5.4.3 generic output 对称段。挂账非 soundness: master_model.py 自有宽松 loader (后被 r2 推翻升级为 F-BIND-R2-01); RAB 证书 union 非最小 (保守方向, 非缝)。归档 `algoaudit_binding_face_r1_REVIEW_20260612.md` + `algofix_FBIND_r1.patch`; **r2 确认轮 (包 6a9c241a) 判 r1 修复 sound (含 52=52 哨兵恒 0 代数证明 + 哨兵不漏 routing/flow 的消费链穷举) 但泛化又爆 2 HIGH 已修**: **F-BIND-R2-01** = master 侧 `load_generic_io_requirements_artifact` 仍 fail-open **且 r1 挂账判断被正当推翻** — certified session 在 binding 之前就把 loader 结果喂进 `build_exact_core` (probe: 字符串 '100' 被 int() 吞 → protocol_storage_box optional 下界 1→34 注入 master 硬约束 = false-INFEASIBLE 方向; CC 抽查 benders_loop:1568-1577 链属实); 修 = master artifact loader 委托 binding fail-closed loader (proof-surface 单一入口) + master in-memory normalizer 同步收紧。**F-BIND-R2-02** = 全部 proof 输入 JSON 装载用默认 `json.loads` — 接受重复 key (last-write-wins: 重复 section 可静默清空需求 / 重复 generic_input_slots 可把无线槽数改 0) 与 NaN/Infinity; 修 = strict JSON helper (拒重复 key + 常量), generic IO/无线槽数/canonical 角色读三处换用。验收: 3 probe unpatched 复现 → patched 全翻转 raise → ruff 清 → 专项 330 绿 → 全量 **2927 绿** (+4 回归) → preflight 21/21 → lock 新增 F-BIND-R2-01/02 两条款 + specs/05 同步。r2 还核了 Q2 结构假设 7 项 (fixed pattern 枚举端口数充足 / generic output+input utility roster 硬编码与当前 profile 一致但有 owner-gate 扩展 guard 建议 / 槽数不再硬编码 / pose-optional map / 排序仅 hint / overload env 默认关被 env guard 拦) + Q3 入口 7 项 + Q4 抽查 3 项 (search guidance 无硬约束 / TIMEOUT→UNKNOWN 消费链 / RAB+overload env guard)。挂账 (非 soundness): generic utility roster 扩展时无 profile-driven guard (owner-gate 扩展时需同步, 已在 lock 外建议)。归档 `algoaudit_binding_face_r2_REVIEW_20260612.md` + `algofix_FBIND_r2.patch`; **r3 (包 50360c1d) 判 r2 修复 sound 但「单解析/单快照」泛化又爆 5 HIGH 已修**: **F-BIND-R3-01** (certified binding 仍传 project_root 重读磁盘 → master/binding 同一工件两个时间点两快照可分裂; 修 = binding 显式接收 master normalized 快照, certified 模式 fail-closed) / **R3-02** (master `_load_json` [mandatory/candidates/canonical] 仍默认 JSON; 修 = strict) / **R3-03** (preprocess_context utility 槽数 `int()` 吞 bool/float/str; strict JSON 部分与 F-PRE-R8-01 撞车已由 r8 先修, CC 手工移植槽数 strict int 部分) / **R3-04** (wireless 槽数用 import-time 默认 profile 不进 master core 快照 → optional 下界与 project-root 规则可漂移; 修 = 槽数参数化流入 master/outer/campaign/coordinate 四消费点) / **R3-05** (campaign proof helpers 私有解析绕过 canonical 角色校验; 修 = 委托共享 loader)。Q2 给出 5 工件×loader 矩阵 (全链单一入口验证) + Q3 解析层四项 (嵌套重复 key probe 过/大数精度无损/同形异码 fail-closed/BOM 拒收) + Q4 抽查维持。**CC 验收修连带 ×2**: ① R3 新 fail-closed 契约打爆 16 个 synthetic 工程测试 (fixture 写空 `{}` 需求工件/缺 commodity_metadata/缺 plan 文件/mock master 缺快照属性/stale 测试被角色校验提前拦) — 全部按"fixture 跟上契约"方向升级 (6 文件), 非放松实现; ② mypy 2 错 (kwargs helper 返回类型过窄 → Dict[str, Any])。stash 判别: 旧 src + 新回归 = 7 翻红。全量 **2941 绿** (+8 net) → preflight 21/21 → lock 新增 F-BIND-R3 单快照综合条款 + specs/05 同步。归档 `algoaudit_binding_face_r3_REVIEW_20260612.md` + `algofix_FBIND_r3.patch`; **r4 (包 6867b7ce; 首发 GPT 生成事故只吐 1 字重发成功) 判 r3 修复 sound 但抓同型残留 1 HIGH — F-BIND-R4-01**: r3 只注入了 generic maps, **wireless 槽数没注入** — binding 建模时仍从磁盘重读 plan = master 快照槽数 vs binding 磁盘槽数双时间点 (probe: master 按 3 槽证下界 1, binding 按磁盘 1 槽拒 placement = false nogood/prune)。修 = PortBindingModel 新增槽数显式参数 + kwargs helper 注入 (certified 有 generic inputs 时 fail-closed 必须有快照) + lock/specs05 条款扩为 R3/R4。**r4 还核了**: R3-01 等价推理补证 (AST mutation scan: 生产代码对 generic_io_requirements 唯一赋值点, 无 update/clear/pop; 显式传参仍过 normalize 哨兵校验) + R3-04 消费点重扫 (5 hard consumers 全覆盖 + 4 非 proof 引用判读) + CC 连带三测试意图复核 (stale/inspector 防线实跑有效) + Q2 终遍 10 行矩阵 (含 campaign checkpoint/terminal 面/commodity_demands=diagnostic/overload=env 拦/hint=非约束) + Q3 三批修复交互 (异常类型不统一但 fail-closed 行为一致) + Q4 BOM/嵌套重复 key 三 helper 实测。验收: probe 复现 → 4 新回归 stash 判别翻红 → ruff 清 → 全量 **2944 绿** → preflight 21/21。归档 `algoaudit_binding_face_r4_REVIEW_20260612.md` + `algofix_FBIND_r4.patch`; **r5 (包 54ffa047; 交付经历单字符读取事故后由 --resume 救回, 见工具线) 判 r4 修复 sound 但单快照族终验再挖 1 HIGH — F-BIND-R5-01**: outer_search 的 certified frontier domain 在 `ExactSearchSession.create` **之外**直接读 wireless 槽数算 safe_area_upper_bound (实测 slot 3↔1 漂移会让候选域上界差 9 面积单位 = frontier 完备性主张可混用两 domain), 且 parallel worker 自建 session 无 artifact hash 一致性回报 → 候选域与 solver 证明可来自两个工件宇宙。修 = outer domain snapshot 封印 (hashes+generic_io+槽数) + 每次 session 创建后全等校验 fail-closed RuntimeError + worker 启动接收 expected hashes 不一致 STARTUP_ERROR。**r5 还核了**: Q1 R4 修复四向 (空需求带箱不复活读盘 [probe] / normalizer 边界值 7 组全等 [含 bool 不偷渡 int 子类] / 0 槽语义 fail-closed 双路径一致 / heuristic fallback 非 certified 消费) + Q2 134 行引用重扫 (五 hard consumer 注入链成立, outer/worker 即本轮 finding) + Q3 三交互组合 + Q4 更宽 AST scan (setattr/__dict__/mutating calls 全扫) 维持 r4 结论。验收: 判别翻红 → ruff 清 → 全量 **2947 绿** (+1) → preflight 21/21 → lock 新增 F-BIND-R5-01 worker-seal 条款。归档 `algoaudit_binding_face_r5_REVIEW_20260612.md` + `algofix_FBIND_r5.patch`; **r6 确认轮 (R5 修复攻击面 + 单快照族终验 + 枚举/对称数学角度, 包 ec504afe) 零 soundness finding** — 单快照族 R3→R4→R5 三轮连挖后**首个干净轮**: Q1 R5 修复四向确认 (snapshot 字段集无第四类 proof 输入遗漏 [canonical/mandatory/candidates 经 hash 封, admissibility 从 canonical 读] / 生产无 bypass session 构造 [三 ensure 点全带 validator, factory TypeError 只兼容旧签名] / worker expected-hash 链 fail-closed [mismatch probe 实证 STARTUP_ERROR raise, 唯一缺省 = checkpoint_free_evaluator 自标 proof_source=False] / 失败不被吞 [RuntimeError 不在 telemetry except 内]); Q2 **50+ 行消费点终验矩阵** (proof loader/owner/seal/consumer vs 非 proof diagnostic/adapter/generator/postprocess 全仓二分判读, 单快照族判收口); Q3 binding 枚举/对称 (nogood 恰排当前 selection 不漏邻近 / binding 内无自有 cap 与 F-BL-R3-01 一致 [回归已存在] / 置换对称 = slot-indexed alternatives 枚举成本非正确性问题, probe 实证 3 槽 1 商品枚举 3 置换、2 商品 6 置换后 INFEASIBLE 穷尽 / `__unused__`+ExactlyOne+精确计数对任意 R<=S 非满额配置语义成立 + R>S infeasible, 不依赖 52=52 巧合); Q4 r1-r5 修复交互四组 (strict loader×封印 / slot 漂移×ensure [回归在] / worker mismatch×STARTUP_ERROR / env guard×overload separation) 无误吞组合。CC 验收: 行号抽查精确命中, 零 finding 无代码改动。归档 `algoaudit_binding_face_r6_REVIEW_20260612.md`; **r7 确认轮 (规则文本独立对照 + routing-free/generic IO 边界 binding 侧纵深 + selection/nogood 完备性, 包 70457b5e, 会话 6a2c54f7) 零 soundness finding ✅ — 连零 2**: Q1 **11 行规则↔实现对照表** (方法论 = 先从 specs/03/04/05 + canonical 推导预期语义再对照实现, 不从实现学语义 [F-RT-R2-01 教训的正确姿势]: 端口空置合法/侧别几何边界判读 [binding 不二次编码几何, pose 生成阶段 materialize, 职责切分正确]/精确计数=pooling 不绑产线/`__unused__` 哨兵/wireless 虚拟槽无坐标不进 port_specs/routing-free 只滤 output terminal 保 input/端口极性 in=sink out=source 与 routing 同向/overload=env-gated heuristic 默认关 + controller env-off retry 不当证明) + 静默缺失检查无发现; Q2 routing-free 生产/消费链路全点名 (binding 三处 + Benders separator 入口 + separator/abstract classification + deletion-core oracle 只消费 filtered specs) + **9 行三类判读表** (当前 sound 4 项 / 「重叠出现才错」3 个具体点位 [固定 output 商品级过滤 :1021-1025 / generic output extraction :1055-1061 / RAB build-time 删槽 vs extraction 语义分叉 :728-731 — owner-gate 扩展守卫最具体钉点] / 「永远不错」4 项论证 [双变量族不互污/物理 vs 虚拟槽 family 隔离/selection key space 分族/虚拟槽双保险]) + owner-gate 扩展两语义路线建议 (禁止重叠 fail-closed vs role-aware terminal policy); Q3 4 自由度清单 + extract_selection 全覆盖 (:982-1003) + nogood 固定常量省略安全论证 (:1090-1106, 空 literals 不加空 nogood) + overload retry 后 selection 属 retry model (:5121-5170) 自洽。**CC 验收**: probe 本地复跑全过 (当前角色集零重叠实证 + 18 operation domain 计数对照 + nogood 重解判别 + 人工 dual-role 边界分类) + 行号抽查 3 处精确命中 + probe ruff 干净入库; 零 finding 无代码改动, 基线 2968 维持; GPT 诚实声明沙盒全量超时未完成 (专项 45 绿 + proof obligations 过)。归档 `algoaudit_binding_face_r7_REVIEW_20260613.md` + `algoaudit_binding_face_r7_probe.py` | **2** | **饱和判据下沿达成 (连零 2/2-3); 可选 r8 终轮或转横切** |
| 7 | campaign/resume 状态机 | **首轮 (2026-06-12, 包 1ebcc03b) 爆 F78-F-01 HIGH**: 陈旧 candidate `solution` 穿越状态改写存活 (started 拷入 RUNNING + CERTIFIED(None) 继承旧 witness + 弱状态带 solution 不被校验拒) → 陈旧 witness 可过 resume 边界撑起 terminal 证据 = false-CERTIFIED 方向。**修复落地**: solution 仅 CERTIFIED 可携 + CERTIFIED 必须带新 witness + 强状态单调 (rerun 不降 RUNNING / 弱覆盖审计阻断 / 强冲突 raise) + resume 校验拒弱状态带 solution。probe 复现+四轴翻转, lock 新增 F78-F-01 条款; **r2 确认轮 (包 13dc4e59, 脚本全自动外发) 零 soundness finding**: F-01 修复逐处 file:line 复核 (入口/resume 栅栏 + started 强状态 no-op + result 单调语义) + 强→弱阻断跳过 cut/counter 更新判读为 completeness-only (counters 非证据判据, CC 同意) + **Q2 泛化穷举 campaign 全 writer 清单** (`exact_campaign.py` 7 writer + `outer_search.py` 6 直写点, 逐个判读无弱证据穿强缝) + 字段族 deny-unknown 边界复核; 非 finding 备注: 同 hash 旧版坏强记录无法自证 provenance 属固有限制 (新 writer 已不能生成)。归档 `algoaudit_campaign_scheduler_r2_REVIEW_20260612.md`; **r3 零 finding** (崩溃时序/原子性角度, 详见面 8 行同轮记录) | **2** | **饱和判据下沿达成 (连零 2/2-3)** |
| 8 | parallel scheduler 合并 | **首轮同包爆 F78-F-02 HIGH**: `results_by_seq` 只认 dispatch_seq 不校验候选身份 (setdefault 先到先得), outer_search 对未匹配结果走 prune_fill 兜底照写 campaign → 队列边界可注入"从未派发的候选"的结果。**修复落地**: 调度/消费双侧身份校验 (seq/attempt/candidate/key 全匹配), 重复/错配/errored 结果全弃, 畸形波次 → worker_process_failed/UNKNOWN 停机。probe 复现+翻转 (修后比 REVIEW 描述更保守: 错配波次同伴结果一并丢弃, 只伤完整性), lock 新增 F78-F-02 条款。验收: CC 修 1 个连带 (test_exact_contract 的 SimpleNamespace mock 缺 error 字段), 全量 **2917 绿** (+5), preflight 21/21; **r2 确认轮 (同包同轮) 零 soundness finding**: F-02 修复双侧复核 (scheduler `_worker_result_identity_violation` 四元组 + consumer `_parallel_wave_result_identity_failure` 独立二审, 普通收割/crash drain/尾部 drain 三路径无旁路) + **Q3 队列/序列化边界穷举 8 类** (task queue/READY/heartbeat/result queue/drain/wave 对象/telemetry/pickle round-trip, 逐个判读) + r1 抽查 (resume hash fail-closed / terminal export 守卫 / partial failure fail-closed 三声明复验成立); 非 finding 挂账: **task_fingerprint 纵深防御建议** (WorkerResult 身份四元组不含 epsilon/solve_mode/profile, 当前 queue 生命周期下无漂移路径, 属可选加固非缝)。CC 验收: 行号抽查 4 处精确命中 + 基线 e5e6d49→HEAD 源码零漂移 + 无降级 (21min 生成); 本轮无代码改动, 全量基线维持 2917 绿; **r3 (崩溃时序/原子性角度, 包 32a25b71) 零 soundness finding**: Q1 文件×损坏形态 13 行矩阵 (空/截断/旧完整/temp 残留/replace 前后崩溃/三 public artifact 部分写, 全 fail-closed) + Q2 五时刻崩溃推演 (started 后/result 未消费/候选 CERTIFIED 无 terminal/terminal commit 夹缝 [确认 validator-before-save 正确顺序]/stop 与 save 间) + **12 个落盘形态 probe 实证** (resume/reset/best_certified 行为全表) + Q3 多进程独占 (worker 无 campaign path 是机制; **双 coordinator 无 lockfile 判运维风险非 soundness** — atomic write 排除交错 JSON + resume 全校验 + last-writer 最多回退进度, 挂账运维硬化建议) + Q4 隔离复核 (强→弱阻断不依赖 audit 持久化 / telemetry/frontier_probe 不进 proof)。CC 验收: 行号抽查 2 处对包基线 814498c 精确命中; 零 finding 无代码改动 | **2** | **饱和判据下沿达成 (连零 2/2-3); 可选 r4 终轮或转横切** |

\* 面 3 注: 2026-06-12 凌晨的 guard 完整性/电力见证轮所发现项的处置与判读细节在 CC 工作区台账 (刻意不入仓库), 对应盲区回归已落仓 (`test_p0_certified_soundness_fixes.py`, commit c2e7394)。

横切: 路 A (伪造交付面) 已由 V81-V98 deny-unknown 19 轮横扫至封闭契约, 不在上表;「跨子系统交互缝」专项轮排在面 5-8 各自首轮干净之后。

## 二、差分对拍 fuzz (tier ②, 机械验证, 不受 reviewer 能力上限约束)

| 切片 | 覆盖 | 结果 | 工具 |
|---|---|---|---|
| 1 | routing 全局连通 + cell-layer capacity + port exact-one (A-1 类及端口履行/容量类) | 累计 **900** 随机实例 0 不一致 (含 F04-R4 落地后复跑 150, seeds 50-52)。**诚实标注 (F-RT-R2-01 教训)**: 该 900 实例对 sink front 极性类**盲** — oracle 的 sink front 方向键与实现同源反向, 两边一致地错 = 对拍不出; 验证器从实现复制键方向就不再独立。oracle 极性已修 (2026-06-12) + self-test fixture 重写, 修后编码×oracle 新跑 **200 实例零不一致** (seeds 200/300); **F-RT-R3-01 connector 修复后 (2026-06-13) oracle 独立新增 connector 占用检查, 编码×oracle 再跑 200 实例 (seeds 400/500) 零不一致** | `cc_context/verification/diff_fuzz/routing_connectivity_diff.py` |
| 2 | master no-overlap/bounds/电力 (B-01 类) 正向 + false-INFEASIBLE 反向; **生成器已加无线箱形态** (方形无端口单朝向, 镜像 post-F-01 协议箱几何) | 累计 **~1760** 实例 0 不一致 (反向 pinned 二次裁决累计滤 28 假阳性; 无线箱形态 168 实例, seeds 100-105) | `cc_context/verification/diff_fuzz/master_geometry_diff.py` |
| 待做 | binding oracle (难) | — | — |

方法论要点 (审计可复核): 独立验证器零共享被测代码路径; reverse 方向因 ghost 矩形可行性 oracle 不可独立重写, 用「嫌疑 witness pinned 重喂 master」自裁, 只有 pinned-FEASIBLE 才计真 over-cut。

## 三、审查能力校准 (tier ①)

已完成一轮, 结果支持「此前零 finding 轮可信」(对已校准的子系统与缺陷形态)。细节/台账在 CC 工作区与 harness memory, **刻意不入仓库不入包**。边界: 跨子系统多步交互形态未校准 (排期: 面 5-8 首轮干净后做交互专项)。

## 四、当前阻塞闭合的事项 (按序)

1. ~~wireless 修复链 (F-01/F-02 → F-03 → F03-R3 → F04-R4 → r5 零 finding 确认)~~ ✅ **收口** (fbb0466 / b7d2115 / 51c5f90 / c7f3bb5 + r5 干净轮)。r5 留 3 条 non-blocking 备注挂账 (非 soundness): ① 根目录裸 pytest 会误收集 `补丁包/` 归档重复测试 (日常用 `src/tests` 限定); ② PCR candidate scoring 仍用 raw front cluster + 未传 routing-free set 的 SAC pressure (仅影响候选排序效率, patch proof 走 filtered port_specs + replay); ③ flow_subproblem / heuristic_feasible_finder 内残留 raw port 逻辑 (diagnostic / 非 certified 主链)。
2. ~~preprocess 面续审~~ — r5/r7 零 finding 确认轮夹 r6/r8/r9/r10 各轮 finding (已修), **当前接 r11 确认轮**。挂账非 soundness: DOC-LOW-01 (plan metadata 描述仍写 "optional future overrides", 修复需动冻结工件 hash 完整仪式, 为措辞不值——下次真有理由动 plan 时顺路改); commodity_demands.json 不在 hash 闭包 (当前 diagnostic-only 无需; **再审触发条件 = 未来任何 certified 分支依赖它**)。
3. **当前队列 (2026-06-13 下午, 并发上限 2)**: face 4 r10 已验收 (零 soundness + LOW owner 校验, 连零 1); **face 5 r14 已验收 (确认轮判 R13-01 sound + 自由攻击角抓 2 HIGH F-PRE-R14-01/02 已修; ⚠️ 并发会话 e8a750c5 验收到一半被 owner 停会话 → SessionEnd 兜底 WIP commit 27967ec → 20690dc4 接管收尾, 基线 3024, 连零仍 0)**; **face 2 r10 (gm_r10) 已验收 (零 soundness + LOW persisted-hint 修复, 基线 3033, 连零 2 → 达饱和下沿; 本会话接管 e8a750c5 遗留单)** → 剩 **face 4 r11 终饱和轮** (需含 r10 LOW owner 修复的新包) + **face 5 preprocess r15 确认轮** (R14 修复确认, preprocess 连零仍 0 是最顽固面) → 这两面达连零 2 后做交互缝专项轮收官。面 1/2/3/6/7/8 (六面) 已达饱和下沿暂停。**⚠️ 并发会话事故备注 (2026-06-13): 本日一度两个 CC 会话 (20690dc4 + e8a750c5) 并发操作同一仓库 (owner '线程分支'设置遗留的孤儿会话), 表现为工作树/暂存区在诊断期间反复变动; 已由 owner 停掉 e8a750c5, 仓库现归 20690dc4 单一操作。**
4. 面 4 latent 待办 (C-3 F2 容量 / C-4 readiness gate blocker) 处置或显式划出 P1.2 范围 (owner 决策)。
5. 交互缝专项轮。
6. owner 按手动计数 gate 做闭合宣布。

## 五、已收口的大项 (证据指针)

- 3 真 P0 (A-1/B-01/A-2) 修复 + 两轮外审收口: commits 415c0c0/eb016ef/863f6d2, 归档 `cc_context/review/algofix_p0_*`。
- P0-1 lazy connectivity cut: commit 1876a6e, 双独立零 finding (`algofix_p0_1_lazycut_review_r1a/r1b_20260612.md`)。
- preprocess 面审查交付链: r1 `cc_context/review/algoaudit_preprocess_face_r1_*` (commit a716173); r2 `algoaudit_preprocess_face_r2_REVIEW_20260612.md` + `algofix_preprocess_F03_routing_free_leak.patch`; r3 `algoaudit_preprocess_face_r3_REVIEW_20260612.md` + `algofix_F03_r3_residual.patch`; r4 `algoaudit_preprocess_face_r4_REVIEW_20260612.md` + `algofix_F04_r4_residual.patch`; r5 (零 finding 确认轮) `algoaudit_preprocess_face_r5_REVIEW_20260612.md`; r6 (R6-F-01) + r7 (零) 见面 5 行; r8 `algoaudit_preprocess_face_r8_REVIEW_20260612.md` + `algofix_FPRE_r8_strict_json.patch` (F-PRE-R8-01 strict JSON 纵深)。
- wireless 修复链 (F-01..F04-R4) 全弧线收口: 5 轮收敛 (2 finding → 1 → 2 → 4 → 0), 每轮 brief 点名上轮修复为攻击面; 终轮独立穷举复核全部 raw port/front 消费点 + 交互 + 文档一致。
- face 7/8 审查交付链: r1 `algoaudit_campaign_scheduler_r1_REVIEW_20260612.md` + `algofix_F78_campaign_scheduler.patch` (2 HIGH); r2 (零) `algoaudit_campaign_scheduler_r2_REVIEW_20260612.md`; r3 (零, 崩溃时序角度) `algoaudit_campaign_scheduler_r3_REVIEW_20260612.md` → **连零 2, 饱和下沿**。
- face 6 审查交付链: r1 `algoaudit_binding_face_r1_REVIEW_20260612.md` + `algofix_FBIND_r1.patch` (2 HIGH: 哨兵缺失 latent + loader fail-open); r2 `algoaudit_binding_face_r2_REVIEW_20260612.md` + `algofix_FBIND_r2.patch` (r1 修复 sound; 新 2 HIGH: master loader 分叉 + strict JSON)。
- 单一 living 现状源: `_cc_live_memory/handoff_windows_ninth_review_pending.md` (stamp 滚动递增, 以文件内最大编号为当前)。
