# 终末地 IndustrialPlanner 精确求解器 — campaign/resume 状态机面 round 7 (真 Pro 第四次全面 soundness 重审·攻多文件提交时序 + 内存改写后未落盘窗口 + torn-value 残面)

## 任务性质 (新会话零历史, 独立对抗审查)

项目快照包在本 Project **文件区 (来源/Sources)**: `zmd_snapshot_0590f9ca.zip`, sha256 `0590f9ca30aac5bb7afe18945eb36d347ea8b0c5b467fd6baff4679eff8c5234`。**只认这个文件名, 文件区其它快照包一律无视; 开工前先校验 sha256, 对不上停下来报告**。zip 内 `project/` 为仓库根 (ZIP_LZMA, `python -m zipfile -e <zip> .` 解包), 干净 git 树快照, HEAD `7fec29a` —— **这是带 round-1 + round-2 全部修复合入的新树** (上一轮 r6 的树是 `eb5c012`)。依赖 wheels 同在文件区 (`zmd_py313_linux_x86_64.zip`), 沙盒 Python 3.13, 离线安装。

`data/preprocessed/candidate_placements.json` (45,773,799 bytes, sha256 `adcc2a6e8a1daaa9dea6cae68883301ad07ce123fa286b55dcbe79ca2f34bec0`) **已随包**, 已校验, 不准伪造/重生覆盖。

## 项目一句话

70×70 网格 certified-exact 最大空矩形求解器 (目标 `max_lex(area, min_side)`, 266 强制设施, OR-Tools CP-SAT 9.15 + Benders/LBBD)。宪法 `PROJECT_LOCK.md`; fail-closed 默认姿态。本面 = **campaign 持久化 / resume 状态机** (`src/search/exact_campaign.py` 为核, 配 `src/search/certified_frontier.py` 终局证据 + `src/search/outer_search.py` 的 frontier 重建/终局提交)。**并行 scheduler 合并是 face 8 单独审, 本轮不审。**

## 本面定义 + 本轮性质 (关键, 必读)

本面 = **状态机 soundness**, 不重新证明 LBBD 子问题正确性 (那是 master/routing/cuts/preprocess/binding 各面的事)。它把单个候选的 CERTIFIED/INFEASIBLE 判定当作「在同一冻结 artifact hash 下已正确」来信任, 只校验: ① **持久化落盘原子性 + 崩溃时序**; ② **resume 一致性** (候选不重复消费 / 不丢已证候选 / 陈旧 witness 不穿越状态改写); ③ **强状态单调 mark_candidate_result/mark_candidate_started 不变量**; ④ 终局证据 (full-frontier potential_domain 空 + frontier 空) 不被错误升格成更强主张。

**历史轨迹** (本面 round 编号; 早期与 face 8 同包, 后独立成面):
- round-1 (= 旧台账 r6 prompt) = 真 Pro 第三次全面重审, M1-M6 六块 (落盘原子性 / save-vs-validate 时序 / mark_candidate_result 内存改写顺序 / attempts 单调与重复消费 / resume 字段级容错 / 遥测非证据性) 逐块判 sound, **并抓出一个 LOW/availability finding F-CAM-R6-01**: `campaign_hours: null` 能通过 `_validate_resume_state` 然后在 `load_or_create` 预算扩展热路径 `float(state.get("campaign_hours", 0))` 崩 `TypeError`。
- round-2 = **F-CAM-R6-01 落地 + 入 LOCK** (lock:155): 新增 `_strict_resume_nonnegative_float()` (`exact_campaign.py:296-302`), 在 `_validate_resume_state` 里对 `campaign_hours` 做 bool / 非数值 / NaN / Inf / 负数 fail-closed (`:1511-1514`, 返回 `campaign_hours_invalid`); 回归测试 `test_exact_campaign_state_soundness.py:112-118` 锚住 `campaign_hours=None` → 干净 reset。这是 conditional availability hardening, 非 false-CERTIFIED reset。

**本轮 round-3 (= r7) = 真 Pro 第四次独立全面 soundness 重审。姿态铁律:**
- round-1 是真 Pro M1-M6 全面零 soundness finding (只一个 LOW availability), round-2 把那个 LOW 修了。**前轮 clean 不构成任何先验**。同期真 Pro 一切到其它面屡次抓出 thinking 漏了多轮、甚至自己上一轮漏掉的真 finding (cuts CUT-R12/R13/R14/R15、preprocess R15/R16/R17/R18、binding R10/R11、几何 master R11/R12/R13)。请当作从未深审重走。
- **不要复读 round-1 的 M1-M6 判读路径**, 更不要重报已修的 F-CAM-R6-01。round-1 已把「`atomic_write_json` 单文件原子」「save 是 writer / validate 在 resume+export 兜底」「mark_candidate_result 内存改写顺序」「attempts 单调」「resume 字段级容错」「遥测非证据」这六条主路各跑过探针并判 sound; round-2 又补了 `campaign_hours` 这一个 torn-value 字段。本轮要求**换全新角度下沉到「**多文件之间的提交顺序 + 内存改写后到落盘前的中间窗口 + round-2 只修了一个 torn-value 字段而其余 required 字段的 torn-value 残面**」这三层** —— 这些正是 round-1/round-2 没正面深挖、或刚被 round-2 局部触碰却没穷举的缝。若你重走 M1-M6 原路得到同结论, 那只是复制 round-1, 没有增量。
- 本轮 attack surface = **跨文件提交时序 (checkpoint vs final_solution/blueprint/manifest 的写顺序与崩溃落点) + 内存态 mutate 后未 save 的中间窗口 (尤其 resume 预算扩展、`mark_campaign_stopped` → 重建 evidence → save 的三步之间) + round-2 留下的 torn-value 残面 (campaign_hours 已堵, 其余 14 个 required 字段是否同等堵)**。核心问题: 一次崩溃 (kill -9 / 断电 / 磁盘满 / 进程被抢占在两次 write 或两个文件之间) 能否留下一个**自洽得能通过 resume validation / 能通过 public CERTIFIED 出口、但语义上已 false-CERTIFIED / 已丢已证候选 / 已让弱结果实际覆盖了强结果**的盘上状态? round-2 只把 `campaign_hours` 一个字段从「key 在但值被截断成 null」堵成 fail-closed, 本轮要确认**这是个孤例堵漏还是一个完整的 torn-value 防线**。

注意行号: 下面给的 `file:line` 基于 HEAD `7fec29a` 包内源码 (与上轮 `eb5c012` 相比, round-2 的 `_strict_resume_nonnegative_float` 插入使几个核心函数整体下移约 6-12 行), **以你解包后实读为准**, 函数名稳定 (`atomic_write_json` / `_fsync_directory` / `save` / `mark_candidate_result` / `mark_candidate_started` / `mark_campaign_stopped` / `_validate_resume_state` / `load_or_create` / `_validate_candidate_record` / `_strict_resume_int` / `_strict_resume_nonnegative_float` / `_commit_terminal_full_frontier_certified_result` / `best_certified_result` / `_persist_best_certified_result_if_any` / `_refresh_certified_delivery_manifest_if_any`); 若行号微漂以函数体为准, 别因行号差几位就当代码变了。

## 本轮新攻击面 (6 块, 全部是 round-1/round-2 当前提信任、或刚被局部触碰却没穷举的「跨文件 / 中间窗口 / torn-value 残面」层)

### M1 [多文件提交顺序的崩溃落点矩阵] checkpoint 已落但 final_solution/blueprint/manifest 未落之间崩
round-1 的 M1 已确认单次 `save()` 只写一个 checkpoint 文件 (`exact_campaign.py:2217-2220`)、单文件 tmp-swap 原子。**但终局公开交付是多文件**: `_commit_terminal_full_frontier_certified_result()` (`outer_search.py:868-894`) 在内存里 set `final_result`/`final_status=CERTIFIED` → `mark_campaign_stopped(...)` → 重建 `terminal_frontier_evidence` → 校验 → **`save()` (写 checkpoint)**; 然后**它的调用方**才另写 `final_solution.json` / `optimal_blueprint.json` (`_save_final_result` / `_persist_best_certified_result_if_any:910-927`) 和 `certified_delivery_manifest.json` (`_refresh_certified_delivery_manifest_if_any:930-`)。请逐一推演崩溃落点: ① **checkpoint 已 `os.replace` 成功但 final_solution/blueprint 还没写** —— resume 读到 final_status=CERTIFIED + 完整 terminal_frontier_evidence 的 checkpoint, 但磁盘上 final_solution/blueprint 缺失或还是上一轮的旧值。这条 resume 后, public CERTIFIED 出口 (`best_certified_result` / delivery manifest / certified_surface) 是**从 checkpoint 内存态直接出证**, 还是**强制要求磁盘 final_solution/blueprint/manifest 当前且与 checkpoint 同源**? 若任一公开面信任 checkpoint 而不校验磁盘交付物 currentness, 那「checkpoint-CERTIFIED + 旧/缺 final_solution」就是一个 false-CERTIFIED 交付。② **checkpoint 已落 + final_solution 已落 + manifest 还没落** —— 同理追 manifest 是否 fail-closed。③ 写顺序本身: checkpoint 在交付物之前写, 是否意味着「checkpoint 比交付物新」是不变量? 若反过来 (manifest 先 / final_solution 先, checkpoint 后), 崩在中间会不会留下 manifest 指向一个 checkpoint 还没记的 final_result? **重点: 找一个跨文件崩溃落点, 使盘上 checkpoint 自洽 CERTIFIED 但配套交付物 stale/缺失, 而某个公开面据此出证。实证不出来 (所有公开面都强制磁盘交付物 currentness 同源校验) 就明说跨文件这层是 sound。**

### M2 [内存 mutate 后到 save 前的中间窗口] resume 预算扩展 / stop-clear 改了内存却没立刻落盘
round-1 把「save 是 writer」当前提, 但没正面查「**内存改了但还没 save**」这个窗口里崩会留下什么。请深查: ① `load_or_create` resume 成功分支 (`exact_campaign.py:1855-1876`): 它 `state["updated_at"]=now_iso()`、`state["reset_reason"]=None`、若 `campaign_hours > 旧值` 则 `state["campaign_hours"]=新值`、清 `campaign_time_budget_exhausted` 的 `last_stop_reason`/`final_status` —— **然后直接 `return cls(...)`, 没 save**。也就是说 resume 后内存态已是「预算扩展 + stop 清空」, 但**磁盘还是旧 checkpoint** (旧预算 + 旧 stop), 直到下一次 `save()`。这个窗口里崩: 盘上仍是旧态 (保守, 看似安全)。但请确认: (a) 这个「内存新 / 磁盘旧」分叉**绝不**让任何 soundness 判定读到内存的乐观值而磁盘的悲观值 (或反之) 造成不一致 —— 比如 `final_status` 在内存被清成 None 但磁盘仍是某旧 status, 若崩后另一进程/另一次 resume 读磁盘旧 status 会怎样。(b) 清 `final_status=None` (`:1869`) 只在 stop reason 恰是 `campaign_time_budget_exhausted` 时发生 —— 若磁盘旧态的 `final_status` 是 `CERTIFIED` 而 stop reason 不是 budget-exhausted, 这步不清, resume 后内存仍挂着旧 CERTIFIED final_status + 旧 final_result。这是不是会让一个**旧的 terminal CERTIFIED** 在预算扩展 resume 后被当作仍然有效的终局而继续挂着 (本应在新候选跑起来后让 frontier 非空 → 失效)? 追这条 mixed 态在 resume 后第一次 `mark_candidate_started`/`save` 之间是否有窗口让公开面误读。② `_commit_terminal_full_frontier_certified_result` 的三步 (`outer_search.py:875-886`): set final_result+status → `mark_campaign_stopped` (它在 `:881` 把 `terminal_frontier_evidence` 设 None, 因为此刻 reason 已是 terminal-certified reason 所以**不**清? 请实读 `mark_campaign_stopped:2187-2202` 的 `terminal_frontier_evidence=None` 条件分支, 确认 commit 路径下它到底清不清) → 重建 evidence → 校验 → save。**这五步全在 save 之前, 任何中间崩都不落盘**对吗? 确认**没有任何一步在这五步中间偷偷调了 `save()`/`atomic_write_json`** 把半成品 (final_status=CERTIFIED 但 evidence=None, 或 evidence 旧) 落盘。**重点: 确认所有「内存先改、磁盘后写」的窗口里, 崩溃要么落盘旧完整态 (保守) 要么根本没落盘, 绝无半改写态落盘成自洽坏 CERTIFIED。**

### M3 [torn-value 残面穷举] round-2 只堵了 campaign_hours, 其余 14 个 required 字段呢
round-2 的 F-CAM-R6-01 把「key 在但值被 torn-write 截成 null/错类型」这一类**只对 `campaign_hours` 一个字段**堵成了 fail-closed (`:1511-1514`)。但 `REQUIRED_STATE_FIELDS` (`:127-143`) 有 15 个字段。请逐一过**其余 14 个**, 看每个被 torn-write 截成 `null` 或错类型时, `_validate_resume_state` (`:1482-1588`) 是 fail-closed 还是 slide-to-default: ① `schema_version`/`proof_summary_schema_version` 走 `_strict_resume_int` (`:1489`/`:1503`), 缺/非 int reason-fail —— 确认无默认。② `solve_mode` torn 成 null → `str(None)!="certified_exact"` → `solve_mode_mismatch` (堵)。③ `created_at`/`updated_at` 被截成 null: validator **完全不校验它们的值** (只 `REQUIRED_STATE_FIELDS` 查 key 在不在), 它们的下游消费者是 `remaining_seconds()`/`elapsed_seconds()` 里的 `iso_to_ts(str(state.get("created_at", now_iso())))` —— `str(None)="None"` 喂给 `iso_to_ts` 会怎样? 崩 / 返回 0 / 返回错时间? 一个被 torn 成 null 的 `created_at` 会不会让 `remaining_seconds` 算出**无穷大剩余时间** (campaign 永不停) 或**负 / 0** (campaign 立刻判时间耗尽)? 这是 availability 还是能间接影响终局完备性判定? ④ `reset_reason` torn: validator 不校验它, 下游 `reset_reason` property 只 `str()`。⑤ `master_domain_contract` torn 成 null: 走 `_validate_master_domain_contract(state)` (`:1499`) —— 实读这个函数, null 是 fail-closed 还是放行 (这个 contract 是「whole-candidate 证明 vs domain-slice 证明」的关键, 放行可能让 slice 证明被当 whole)。⑥ `final_result`/`final_status`/`last_stop_reason`/`declare_mode`/`candidates`/`terminal_frontier_evidence`/`artifact_hashes` 的 null/错类型分支 round-1 的 M5 已覆盖部分, 但请专门确认 `terminal_frontier_evidence` 被 torn 成 null 而 `final_status` 仍是 CERTIFIED 时, terminal evidence 校验 (`certified_terminal_evidence_violation` / `:1552-1559`) 是否 fail-closed (CERTIFIED 但 evidence 缺应当拒)。**重点: round-2 是「孤例堵一个字段」还是「torn-value 防线已完整」? 构造一个『某 required 字段被 torn 成 null 但 campaign_hours 完好』的 checkpoint 喂 `_validate_resume_state`, 看哪个字段是 slide-to-default 放行而非 fail-closed。尤其 created_at/updated_at/master_domain_contract 这三个 validator 不直接校值的字段。**

### M4 [时间戳作为排序/选择键的隐性证据性] now_iso 非单调 / torn 时间戳能否换掉 best
round-1 的 M6 判了「terminal digest 不读时间戳」, 但只查了 digest。请换角度查**所有用时间戳做比较/排序/选 best 的地方**, 确认没有一处让一个被改/被 torn 的时间戳反向影响 soundness 判定: ① 全仓 grep `updated_at`/`started_at`/`finished_at`/`created_at` 的**读取消费者** (不是写入), 逐个判: 有没有「按 updated_at 选最新 / 按 finished_at 排序选 best candidate / 按 created_at 算预算从而决定是否还继续搜 (影响 frontier 是否被判完整)」。`remaining_seconds()` (`:1906-1909`) 用 `created_at` 算剩余预算 —— 一个被 torn 或时钟回拨产生的 `created_at` 让 `remaining_seconds` 返回正数 (本应 0) → campaign 继续搜 → 这是 availability (多搜) 还是会让一个本应「budget-exhausted 非终局」的 stop 被误读成可继续? 反向: `created_at` 让 remaining 提前归零 → campaign 提前 stop → 是 `campaign_time_budget_exhausted` 非终局 stop (保守, availability) 还是会误触发某终局? ② `now_iso()` 在崩溃/时钟回拨下非单调: 两条 candidate record 的 `updated_at` 可能逆序。确认**没有任何 best-candidate / frontier 选择逻辑依赖 updated_at 单调**。③ `mark_campaign_stopped` 写的 `last_stop_reason.updated_at` 与 candidate record 的时间戳: 确认它们纯遥测, 不进 terminal evidence digest、不进 `best_certified_result` 选择、不进 `certified_surface` publishable 判定。**这块判 sound 的标准: 任何时间戳对『是否 CERTIFIED 完备』『选哪个 final_result』零影响; 时间戳只影响 campaign 跑多久 (availability)。若发现某处用时间戳选 best / 判完整, 那是 soundness。**

### M5 [resume 后强状态 + 新 RUNNING 混合态的二次确认] round-1 M4 查过, 换 torn-write 角度再钉
round-1 的 M4 判了「RUNNING 候选 resume 后恰好重新消费一次」「旧 final_result + 新 RUNNING 不能 short-circuit 放行」。本轮换**崩溃在 mark_candidate_started / mark_candidate_result 之间留下的盘上 RUNNING 态 + 旧 final_result 仍挂**这个具体盘上态来钉: ① 造一个 checkpoint: `final_status=CERTIFIED` + `final_result` 完整 + `terminal_frontier_evidence` 完整 (上一轮真的完成过终局), 然后**某个 candidate record 被改成 RUNNING** (模拟「终局后又被错误重启了一个候选并崩在 started 后」)。喂 `_validate_resume_state`: 它是 fail-closed (终局已 sealed, 不该再有 RUNNING) 还是放行? 若放行, resume 后这个 RUNNING 候选会被 outer search 重新 dispatch, 而旧的 terminal_frontier_evidence 仍在 —— 此时若 outer search 在重跑前就有公开面读 `best_certified_result`, 会不会拿旧 evidence 出证 (而 frontier 实际已不完整, 因为有个 RUNNING 在跑)? 追 `_compute_exact_frontier_state` (`outer_search.py:624`) 重建 frontier 时, RUNNING 候选算不算「未完成 → frontier 非空 → 不该终局」。② `mark_candidate_started` 对 strong record early-return 不动 (`:2026-2034`): 确认一个 torn-write 把某 strong record 的 `status` 字段截没了 (status 缺) 但 record 还在 —— resume 时 `_validate_candidate_record` 是不是拒 (status 缺应 fail)? 若 status 缺的 record 漏过 validator, 后续 `mark_candidate_started` 读 `existing.get("status","")` 得空串 → 不在 STRONG → 当新候选重跑, 这是 availability (重跑) 还是会让一个本是 strong 的候选被降级重跑从而污染 frontier? ③ `mark_candidate_result` 的 existing-strong-遇-weak early-return (`:2088-2104`): round-1 确认它只 append audit-log + 改 top-level updated_at 不碰 record。本轮确认: 若 existing strong record 本身是个**torn 的 strong** (status=CERTIFIED 但 solution 字段被 torn 没了), 这条 early-return 路径会把它原样留盘 —— 但这种 torn strong 应该早在 resume 的 `_validate_candidate_record` (`:1564`) 被拒 (CERTIFIED 缺 solution 应 fail), 所以根本进不到内存。确认这条「resume validator 先拒 torn strong → 内存里不会有 torn strong」的因果链没有缺口 (即不存在「不经 resume validator 直接把 torn strong 灌进内存」的入口)。**重点: 终局已 sealed 的 checkpoint 上再出现 RUNNING / torn candidate 是不是 fail-closed; torn strong record 是不是在 resume validator 就被拦死、绝不进内存供 early-return 放行。**

### M6 [save 失败 / 磁盘满 / fsync 失败的错误传播] 写盘报错时状态机是 fail-closed 还是 fail-open
round-1/round-2 都假设 `save()` 要么成功要么留旧文件。请正面查**写盘失败时**的传播: ① `atomic_write_json` (`:1314-1335`): `json.dump` 写到一半磁盘满 → `OSError` 从 `os.fsync(handle.fileno())` 或 `json.dump` 抛出 → `finally` 删 tmp → 异常往上传。确认这个异常**不被任何上层 `except: pass` 吞掉**导致「以为 save 成功实则没写」—— 全仓 grep `save()` 的调用点, 看有没有哪个调用方 try/except 包了 save 然后继续往下走出证 (内存态 CERTIFIED, 但磁盘没更新, 后续公开面若信任内存 → false-CERTIFIED)。② `_fsync_directory` (`:1301-1311`) 故意吞掉 `os.open`/`os.fsync` 的所有异常 (`except: return` / `except: pass`) —— 这在 fsync 不支持的 fs 上是合理的, 但确认它吞异常**只影响持久性保证 (断电可能丢), 不影响逻辑正确性** (即吞掉 dir-fsync 失败不会让一个没真正落盘的 state 被当成已落盘而推进证据)。③ `os.replace` 失败 (跨 mount / 权限 / symlink 指向只读): 异常传播路径同 ①。lock:V98 登记了 symlink campaign path authority —— 确认 symlink 路径下 `os.replace` 失败是 fail-closed (异常上抛, 不出证) 而非被吞。**重点: 写盘失败 → 状态机必须 fail-closed (异常上抛, campaign 不推进出证), 绝不能「磁盘没写成但内存态被当真出了 CERTIFIED」。找一个 save 调用点把写盘异常吞掉后继续出证的路径; 找不到就明说写盘错误传播是 sound。**

## 明确不要报的 (重复报不算 finding)

- **已修条款 (含 round-1/round-2 已 lock 的全部 finding)**: **F-CAM-R6-01** (lock:155, campaign resume 拒 torn/partial 盘上态 + `campaign_hours` torn-value fail-closed) —— 本轮 M3 可把它**钉成攻击面找同型 torn-value 残面**, 但**绝不重报 `campaign_hours` 字段本身**。**F78-F-01** (lock:92, 候选 solution 卫生 + 强状态单调) —— 本轮 M5 可钉同型时序缝, 不重报条款本身。Accepted invariants: lock:87/88/91 (best certified 跨持久化单调 + final_solution/manifest 同源 + coordinator-only writer 不相交波次)、**F-BIND-R5-01** (lock:103, worker/domain artifact-hash 单快照封印)、**lock:118** (终局证据 closed project-bound 契约)、**lock:119** (deny-unknown `EXACT_*`)、**F-PS-R6-01** (lock:145, precheck-elimination 写前重验完整 INFEASIBLE 契约)、**F-PS-R7-01** (lock:156, 每个 precheck-elimination strong-write 走共享 validator)、**V97/V98** (canonical/symlink campaign path authority) 已登记, 别重报机制本身; 可质询其覆盖边界但要给新路径。
- **round-1/round-2 已审结论别当新 finding**: 「同 hash 旧坏强 record 无法自证 provenance」属固有信任边界 (人为篡改 checkpoint / 旧 bug 产物) —— 除非你能给出**不依赖人为篡改、纯崩溃时序 + 状态机自身逻辑**就能产生 false-CERTIFIED 的新路径, 否则别报。直接 API misuse 存盘 (save 是 writer 非 validator) 在 resume/export 被 fail-closed, 不算 finding —— 但 M1/M2/M6 若能给出**一个真实崩溃时刻 / 写盘失败传播缝**让坏态恰好通过公开面, 那是新 finding。round-1 的 M1-M6 主路结论 (单文件原子 / save-vs-validate / 内存改写顺序 / attempts 单调 / 字段级容错主路 / 遥测非证据) 不要原样复述当新结论。
- **跨面边界 (别误判为本面缝)**: ① **F78-F-02 / F-PS-R4-01 / F-PS-R5-01 worker 身份绑定 + 并行波次合并/discard latch 是 face 8 (parallel scheduler) 单独审, 本轮不审** (lock:93)。② worker 进程内 Benders/cuts/binding/几何 master 正确性属各自面, 本面只假设 worker 返回 status 语义与 sequential 同源。③ M1/M5 若怀疑落到「某个被信任的子问题判定其实是 false-INFEASIBLE/false-CERTIFIED」 —— 那是 routing/binding/cuts/preprocess/master 面的事, 本面状态机会忠实持久化并据此剪枝, 这是**跨面信任不是本面缝**; 交叉引述 PROJECT_LOCK 相应条款指明归属面, 别在本面重证子问题。
- **设计决策 (owner 已定, 别报)**: canonical 口径 / 266 强制设施口径 / `min_side>=6` 是 admissibility 录取门非 tie-break / 全封闭合法空矩形不要求外部连通 (lock:117) / max_lex 目标定义。
- **env-gated 行为不属 P1.2 certified soundness**: `EXACT_USE_POSE_BOOL_MASTER` / `EXACT_POWER_PLACEMENT_SUBPROBLEM` / `EXACT_B1_BYPASS_*` 全 env-gated 且被 `pose_bool_master_not_certified` 等 guard 挡在 certified 公开面外 (lock:132/143/144/148/149/150), **非默认 certified 路径**, 不在本面 soundness 范围。
- preflight `phase_1_2_spike_close` BLOCKED 是 owner gate (别报); P1.3B `step_8_apply_to_master` 是禁区 (别动别报); exploratory 行为/性能不审; persisted `exact_safe_cuts` 是 telemetry **非 proof object** (别当证据缝报)。

## 自验环境与已知基线

- candidate 已随包, 全量应跑得动: `python -m pytest -q src/tests` 期望 **0 failed** (passed ≈3092, 具体数目以实跑为准; **硬不变量 = 0 failed**)。沙盒 pytest-randomly 报 seed 错就加 `-p no:randomly`。跑不完全量就跑 campaign 专项 (`test_exact_campaign*` / `test_v62*` / `test_v63*` / `test_v97*` / `test_v98*` / `test_p0_certified_soundness_fixes*` / `test_exact_campaign_state_soundness*` 等) + 如实声明跑了哪些。
- `python scripts/check_p1_2_proof_obligations.py` 应 pass (**8 obligations**)。
- finding **必须**带可复现 probe (最好是能跑的 .py / pytest) **或** 严谨 file:line 论证; **实证推翻你的怀疑就不要报**。把怀疑写成探针先自我证伪, 证不伪再报。本轮 M1/M3/M6 尤其鼓励**模拟崩溃时序 / torn-write / 写盘失败的盘上态构造探针**: ① 手工造一个『checkpoint-CERTIFIED 但磁盘 final_solution/manifest 缺失或旧』的多文件态, 喂给公开面 (`best_certified_result` / delivery manifest export / certified_surface), 看是 fail-closed 还是放行 (M1); ② 造一个『某 required 字段被 torn 成 null 但 campaign_hours 完好』的 checkpoint, 喂 `_validate_resume_state` 看哪个字段 slide-to-default (M3); ③ monkeypatch `atomic_write_json` 抛 `OSError` 模拟磁盘满, 看 save 调用点是 fail-closed 上抛还是被吞 (M6)。
- 契约锚点: `PROJECT_LOCK.md:85-119`/`:145`/`:155`/`:156` (Accepted Invariants 区, 含 F78-F-01/F-02、F-BIND-R5-01、终局证据 closed 契约、deny-unknown EXACT_*、F-PS-R6-01/R7-01、F-CAM-R6-01)。终局证据/状态机契约测试: `test_v62_candidate_frontier_contract.py` / `test_v63_terminal_evidence_contract.py` / `test_v97_canonical_campaign_state_authority.py` / `test_v98_b5a_symlink_campaign_path_authority.py` / `test_exact_campaign_state_soundness.py` (含 round-2 的 `campaign_hours=None` 回归 `:112-118`)。

## 交付物

- `REVIEW.md`: 逐条 finding (severity / file:line / 可复现 probe 或严谨论证 / 修法), 有把握附 unified diff + regression 测试 (**LF 行尾**)。
- **若确认 sound, 明确写「本轮零 soundness finding」** + 附分段判读, 按本轮 M1-M6 六块各给真 Pro 复核结论 (不是复制 round-1 的 M1-M6, 要体现你在 M1-M6 这些**跨文件提交时序 / 内存改写后未落盘窗口 / torn-value 残面 / 时间戳排序性 / 写盘失败传播新角度**上各自查了什么、构造了什么崩溃/torn-write/写盘失败探针、为什么判 sound)。
- 真 Pro 第四次独立重审, 前三轮 (含 round-1 真 Pro M1-M6 + round-2 落地一个 LOW) 零 soundness finding **不代表本轮默认干净**; 按你自己的独立判断下结论。

## 严重度纪律

- **false-CERTIFIED on canonical + 默认 env = soundness reset** (P1.2 闭环只认这个 HIGH/critical): 终局把不完备搜索/失真证据/误剪/崩溃坏态/跨文件 stale 交付当成 CERTIFIED 完备主张通过公开面。这是本轮唯一的 HIGH/critical 类。
- **env-gated / conditional / false-INFEASIBLE / 保守失败 = hardening/availability** (漏真矩形/回退进度/拒绝合法态/重跑一个候选/崩后保守 reset, 但不谎称完备): 标 **LOW** 加固建议并**明确标注是 env-gated 还是 conditional 还是 availability**, 不是闭环阻断项 (F-CAM-R6-01 就是这一档的标杆)。
- 区分清楚: 一个缝若只会让搜索**多保留候选 / 回退进度 / 拒绝合法态 / 重跑一个候选 / 崩后保守 reset 整轮** = availability (LOW); 只有会让**不该 CERTIFIED 的态通过公开面**才是 soundness (HIGH)。跨文件崩溃缝若只导致「resume 后保守重跑 / 重新生成交付物」是 availability; 若导致「checkpoint-CERTIFIED 配 stale final_solution 被公开面当真出证」或「弱结果实际覆盖了强结果而 resume 没拦」才是 soundness。

## 范围边界

重点 = campaign 持久化 / resume 状态机 / 终局证据 soundness 的真 Pro 第四次复核, **本轮专攻多文件提交时序 + 内存改写后未落盘窗口 + round-2 留下的 torn-value 残面 + 时间戳排序性 + 写盘失败传播 (M1-M6 新角度)**; **parallel scheduler 合并 (face 8) 与其余面 (master/routing/cuts/preprocess/binding/benders) 不审**, 怀疑跨面时交叉引述 PROJECT_LOCK 契约指明归属面而非在本面重证。
