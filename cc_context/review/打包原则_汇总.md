# 终末地项目 · external-review 打包原则 穷尽汇总

> 2026-05-31 由工作流三源(memory 44 条 + build脚本 32 条 + docs 34 条)去重合成。
> 源：`feedback_review_pkg_*` / `feedback_external_review_*` / `feedback_gpt_review_*` /
> `reference_review_pkg_7z_strategy` / `feedback_big_milestone_gpt_pro_review` /
> `feedback_adversarial_soundness_audit` / `feedback_audit_verify_before_archive` /
> `feedback_keep_review_process_light` 等 memory + `build_phase1_*_review_v*.py` 版本注释 +
> `docs/项目说明/16_workflow_review.md` / `12_go_criteria.md` / `15_workflow_testing.md` / HANDOFF / PROJECT_LOCK。

---

## 1. 内容选择（什么入包）

- **默认全项目 root copy**（src+docs+rules+data+scripts+main.py+spec+audit archive），不再用精选 file list。（v7 87-file 精选包被反馈太小、reviewer 缺 context；v8 起转全项目 scope）
- **全 src self-contained 可跑 + 真数据 inline**（canonical_rules / preprocessed / candidate_placements / mandatory_exact_instances / generic_io_requirements）。缺真数据 reviewer 不可能 catch spec↔data gap。
- **spec 全文入包 + git diff 以 format-patch 入 + testing infra(requirements/pytest.ini)入**。
- **in-flight 未完成 design 不入包**（8 路 parallel 没 merger 完前），merger 稳定后才放——raw 草稿会 anchor reviewer。
- **spike/throw-away 分支只入 verdict + 实测数据 artifact**，实施 code 走 off-limits（PR #1 verdict-only style，MERGER §5.1 rollback-safety）。
- **区分 master commit 改动 vs spike-only code**：master 真改动(如 F3 generator)正常入包，只有 spike-only 走 off-limits（防把"spike 不入包"误扩到 master 真改动）。
- **code 不该进 master 树但 reviewer 要源码核 → 镜像到独立 `code_context/<phase>/`**（刻意不放 scripts/ 下）+ README 标 review-only NOT-merge-target + SHA256 manifest。（v17 四审 Reviewer B 指出删 spike code 是 evidence gap，v18 反转"完全不放"策略）
- **选实际存在且承载证据的 artifact**，不照搬 task/spec 里假想的文件名。

## 2. 无主动性内容（no-priming）

- **包内不放任何主动性内容**：verdict claim（Step X 已 close / 数学 sound / 已闭环）、Close 分类列表、审查指引（请审查 X / 期望反馈）、"verify reproducibility cite file:line" 锚点、包目的写"请整 phase audit"。（reviewer 该独立判断不被 priming；v2 包 README 写了被用户立刻删）
- **判别准则**：factual evidence（data/code/spec/transcript/diff/command/git log）入；verdict-priming（结论/期望/指引）不入。"含 X/Y/Z + 实测 414 pass" = factual 入；"5 commit 修复了 X 期望 reviewer verify 已 close" = priming 不入。
- **plan/roadmap doc**（`docs/项目说明/*.md` 这类战略 plan）算主动性 framing，应 EXCLUDE。⚠️ 见 §矛盾点①——v22 实际把它们打进去了。
- **事实层 source snapshot（code_context/spike/）算 factual 不算 priming**，与 no-prompt 互补不矛盾。
- **source 注释归因/断言降级到可证实范围**：无 upstream issue/bug ref 就标"observed locally / spike-local workaround"，不写"verified regression / 已证实 stdlib regression"。（v21 八审 V21-8F2 NIT）
- **prompt 里给 axis/选项 force reviewer evaluate = input-side framing 可以**；但包内一律不重复这些 priming（职责分层）。

## 3. 数据完整性

- **default 完整**：包含 reviewer 源码级复核所需全部 factual 材料，即使是 throw-away spike code / 子代理 cross-check transcript / N=8 design intermediate。不 preempt 删怕 anchor。（删 evidence 省的 effort < reviewer evidence gap 代价）
- **入不入包 shorthand**：(1) factual evidence 入；(2) verdict-priming 不入；(3) **不确定则入**（default 完整）。不该自问"reviewer 会不会 anchor 在这上面"——那是 reviewer 的判断。
- **factual evidence 6 类全入**：① spike code → code_context/ mirror + SHA256SUMS；② Gemini/子代理 cross-check archive prompt+raw transcript（不只 commit log claim）；③ reproducer scripts 入口可跑；④ telemetry raw events + aggregate summaries **两者都入**；⑤ 跨 branch overlay 从 git show 取关键 src+data 标 branch HEAD；⑥ 历次 external audit report+patches 全入 `docs/research/<phase>_<audit_id>/`。
- **reviewer 引用到的 raw telemetry 必须随包**，不能只在 verdict cite 路径却漏打包。（v14 二审 finding 4）
- **数字口径拆清并对齐**（build-time RSS vs after-solve RSS；raw sample vs aggregate snapshot），不同来源不混报。（v14/15/16/20 反复 catch RSS 口径）
- **commit claim / verdict 措辞不 overclaim，与代码实际行为逐字对得上**。（v19 六审 MED："two-literal" overclaim）
- **doclag 是反复失分点**：每次重测后包内所有 README/verdict.md/内嵌表的实测数字（pytest 数/radon/RSS/commit hash/"vN package"自指）必须同步。（四/五/六/七审几乎每轮 catch）
- **样本口径**：默认 ship 真数据全集(53MB)；单测/反例 reproduce 可用 viewer sample(<1MB)，但反例 cite 具体 pose_id 必声明 sample-only 且与 audit archive 数字一致。

## 4. 配套 prompt（分离 + 结构 + armor）

- **prompt 不进包**：chat message 单独给用户，用户自己 paste；包内不放 PROMPT.md / 任何 task 说明。（prompt 是临时 directive 不该跟长期复用包绑定）
- **external review prompt 走固定 7/8-section**：§1 标题(项目+phase+审次) §2 真瓶颈现状(背景+本次含/不含+rationale+上次verdict+README路径) §3 已 NOT_GO lever 历史(死因+reproducer，欢迎挑战) §4 审查重点 axis 化(关键决策点 explicit 列 a/b/c) §5 优先方向(不限于此) §6 不可达论证 armor §7 deliverable+sha256。
- **排版固定**：§6 → §7 deliverable → sha256 单独最末 1 行。不要 §7 展开 rationale / sha 夹中间 / 加解包步骤段 / §6§7 颠倒。
- **除 §6 armor 外不规定 reviewer 回答方式**——不写"按 severity/file:line 输出"/"末尾必给 GO/NOT_GO"/"控制 X 字"等 schema 锁。（reviewer 不是模板填表机；§6 是唯一例外因 vague math claim 代价高）
- **§6 不可达论证 armor**：finding 若暗示"做不到/必须X/不能defer"必须形式化（complexity reduction / proof system lower bound / resource inequality / cite literature）；不接受"I believe / intuitively"，允许诚实出口"I cannot provide formal lower bound, this is intuition only"。（GPT v3/v8/v10 三连错估后加料，L14 应验）
- **死路 inventory 措辞硬度** = "已 verdict NOT_GO + 欢迎挑战 + 不是禁止讨论"；不写"黑名单/禁止/不准"，应明说"看到漏的角度/新算法跨门槛/reproducer 有问题直接 push back"。
- **推理模型不写 role-priming**（"你是 X 专家/请扮演"）；直接讲任务+format+约束。（对推理模型反作用）
- **给外部 reader 的长 narrative 默认 Gemini fat-context 写、Claude review 细节修**（Claude 默认 register 偏端着/工程化）。
- **Gemini 数学 consultant 走 API 看不到本地文件**：prompt 必 self-contained ≥~1500 字（两层结构 + 死路 inventory + 关键数据点 + PROJECT_LOCK 约束 + file path + 用户偏好 + prior verdict）；API key 不 commit/share。

## 5. 打包机制与体积

- **大包(全项目 scope, unzipped >50MB / >100 文件)用 7z -mx=9 + zip 壳(project.7z + tools/7za binary + README + COMMIT_LOG) + ship 解压工具**；cut/lib-only(<100 文件)用 zip 即可。⚠️ 见 §矛盾点②——本次 Windows 无 7z，用了单层 zipfile。
- **排除清单(顶层 dir + 目录名)**：.venv / .git / .pytest_cache / .mypy_cache / .ruff_cache / .artifacts / .codex_test_logs / .upstream_clones / .claude / _codex_archive / node_modules / __pycache__。
- **排除运行期产物 pattern**：telemetry_samples.jsonl / exact_campaign_state.json.* / cache.*.db / tree.txt / *.tar.xz / .DS_Store / Thumbs.db；但 spike 专用 telemetry 显式入包。
- **build 脚本本身从包排除**(前缀 `scripts/build_phase1_` 覆盖整系列) + 自防递归打包。
- **依赖不进包**，依赖包(zmd_deps_v3.zip)单独上传，README 给 pip install --find-links 步骤。
- **off-limits 硬 enforce**：spike 实施 code 即使误 cherry-pick 也由 build `should_skip()` 拦 + build 末尾 forbidden-path sanity check(泄漏即 FATAL return 1)。
- **历史 review 包归档外盘**(`/mnt/wd_external/zmd_review_archive/`)，不进 master/交接包。
- **打包脚本含 ~75 处硬编码 /home/zhuran24/ 路径，机器专属不可直接复用**；核心运行路径(main.py+src/)无硬编码绝对路径。

## 6. 复现性

- **README 给一套确定复现命令**：pytest 普通 + `python -O` + ruff + mypy --strict + vulture + bandit + radon，附期望计数。（`python -O` 专防 assert-only schema 校验在生产 strip 后失守）
- **完整交付含可复现 metadata**：package_build_info.json(commit+测试摘要) + audit_logs/ gate 输出全套 + SHA256SUMS.txt + MANIFEST.txt。（让 reviewer byte-equal 复现，如 radon Average 精确到小数点后）
- **复现脚本不 hard-code 包路径层级**，walk-up 找项目根双标记(PROJECT_LOCK.md + src/cuts)，Python+shell 两侧都改。（v16 _find_project_root()）
- **source snapshot SHA256 manifest 校验命令包内实际可跑**（manifest 路径相对 code_context/，命令写 `(cd code_context && sha256sum -c ...)`）。（v18 五审 catch，v19 修）
- **external review 报告非确定性**：同包+同 prompt 跑两次可能不同 finding；可跑 2-3 次合并，冲突信保守那条(cut 过切/exactness/data loss 方向)。
- **README 必写"报告 markdown 同时下载本地，不光靠 sandbox 链接"**；sandbox temp file 几小时过期。
- **RAM 测量必走 psutil RSS**，禁逻辑大小计算(getsizeof/JSON len)。（碎片化致逻辑 3GB→RSS 8GB 假 PASS）

## 7. 触发时机（何时打包 / 走哪层审查）

- **大节点(Phase 完成/ramp 关键点/paradigm shift)结束才打包交 GPT pro 整 phase 审查**；大节点 ≠ per commit/单 family/单 module(那层 Gemini cover)。
- **两层审查分工**：Gemini per-commit(schema↔src↔data gap，高频，每 commit 后立刻不堆) + GPT pro 大节点 batch(adversarial soundness，低频 1-3 round/phase)；纯 implementation(refactor/rename/IO/test fixture)不算数学层不跑。
- **Gemini multi-round close 只是 Layer 1 工程 close**；任何 family validator 仍必经 GPT pro 一次 adversarial pass(Layer 2 假 cert 能 pass 吗)才算 audit close。（Phase 1.2 20 round Gemini 全 GO，GPT pro catch 2 BLOCKER+1 HIGH）
- **流程要轻**：不给每个小 patch 套重型循环；默认 写 patch → pytest 核心门禁 → preflight → commit。上传 review 包前先问"真需要外部审查吗"。
- **只有重型场景才外部审查**：改 frozen artifacts(4 canonical 数据) / 改证明源头(master 核心 constraint) / 引入或 swap 求解器架构 / lock 名单变更 / 每天 ultrareview；一般 hint 调优/调度/AI sidecar/tuning 不需。
- **GPT pro batch 触发于 phase boundary**；phase boundary 是审查体系盲区(8 路 sibling + Gemini + GPT pro 都看不见 leak)，**user 是唯一可信 phase boundary auditor**。
- **review 包反复 build / memory audit fix 这种独立闭环(≥3 step + read/edit/verify + 不需中途决策)直接 spawn sub-agent(opus background)**，不在主对话做也不问 user。

## 8. 审查后验证与归档

- **任何 external audit finding 在 archive/write memory/commit/propose fix 前必先逐条 specific reproduce verify**(数字用 script / 行为用 reproducer / spec drift 用 grep)，全 pass 才 archive，失败标 reviewer 误报。（~5-15 min/finding cheap）
- **reproduce-before-archive 双向适用**：GO 章 ritual 跟反 GO 章 ritual 都该拒，不管 verdict GO/NOT GO/partial。
- **audit verdict 立即归档进 git**：Gemini→`cross_check/gemini_round_NN_*.md`(连号)，GPT pro→`external_review/gpt_pro_phase{N}_v{V}_audit_round{R}_{VERDICT}.md`；review pkg 给下个 reviewer 时带 archive。
- **调研/cross-check transcript ≥3 agent 跑完立刻 cp Temp → docs/research/ + 同步 INDEX.md 按 Round 分段 + 保原 <agent_id>.output 名**；session 结束前 final sync 兜底。
- **包随 audit round 迭代**：每轮 NOT_GO → 修 + 加上轮 GPT archive → 重 build 递增版本号。
- **包 self-reference 当前版本**，不留指向旧版本包的 stale 推荐("recommend v20 package")。（v21 八审 V21-8F3）
- **fail-closed 契约的 micro-probe 自测随包入**，锁住"malformed → 不 fallback synthesize"边界。（v22 V21-8F1，test_toy_translator_f3_malformed.py 9-case）
- **src 数学层改动即使为修 reviewer finding 也必须另走 Gemini cross-check**，打包 sub-agent 不自行盖章。
- **build 脚本 docstring 充当版本考古**(逐版记 vX→vX+1 改了什么+对应 finding+修复 commit)；但早期 v2-v7 docstring 有 stale copy-paste，考古要交叉看 CHANGELOG。

## 9. 门禁与 GO 判据（审查上下文）

- **GO 准则**：0 P0 + ≤3 P1(各有 mitigation) + P2/P3 不卡 GO 进 followup。NOT GO：≥1 P0 必 close；>3 P1 排序 close top-3；同 round 重复 catch 同 finding = spec drift 必同步 spec/src/test 三层。
- **P 分级**：P0=假 cert 能 pass / 生产 crash / data corruption / soundness 根据被否；P1=未验 sub-invariant 当前数据不触 / 静态 strict 不过 / spec drift 不破 soundness；P2=dead code/注释错；P3=cosmetic。
- **adversarial soundness 5 验(GPT pro 主战场)**：cert 内 sound / cert↔literals / cert↔真数据 / cert↔state / cert↔不变量；GPT pro 主 catch 3+4+5，Gemini 倾向 1+2。
- **family 级 GO**：validator+evaluator+oracle + ≥10 unit test(sound + ≥3 attack 反例 + schema_err + adversarial scope) + spec↔src↔真数据三层 align + Gemini cross-check + 大节点 GPT pro batch；每加 1 family 至少 land 1 新 red fixture + test_family_*.py。
- **GPT review 4 类错估指导下一步**：算法错估 + 前提错估 → push 加料 prompt；数学能力错估 → 接受 verdict 考虑 paradigm 升级。拿新方案先看 GPT 自列 caveat 数(0=算法/前提错估，3+=数学能力错估)。
- **形式化 proof(Coq/Lean)当前 defer**；数学 sound 用工程 verify(validator 重算 cert + adversarial audit + telemetry 反推)。proof object 须 6 步 lifecycle 全 wire 才可信。

## 10. 沟通与新窗口零历史

- **每次给外部 reviewer 都是新窗口零 history**；prompt + 包内文档不准引用上次 reviewer 内容("参考 v11 计划书"/"跟 L14 一样")。要 reference 必须打包进 zip 或 prompt 里 inline 完整重写。区分"GPT 输出过 X"(历史事实 OK) vs "参考 GPT 上次给的 X"(要 GPT 回忆，不 OK)。
- **新窗口 review 包 README 不带历史轮次 carry-forward**("跟 v3/v4 不一样这次只一个问题")；README 极简到 问题/已排除方向/包内容指引/开放式问，详细让 reviewer 自查 zip。⚠️ 见 §矛盾点③。
- **第一句给结论+真问题定位**；不先列 A/B/C 让用户选；不术语+表格堆数据回避"问题在哪"；不确定直接说"倾向 Z"。

---

## 三源矛盾点 / 缺口（工作流 catch，最值得注意）

1. **plan/roadmap doc 入包矛盾**：`reference_review_pkg_7z_strategy` + build v8 EXCLUDE_FILES 明确排除 `docs/项目说明/*.md`(plan 算主动性)，但 v22 全项目 root copy 把 21 sub-doc(含 08/09/10 phase plan)全打进了。需判定：是 v8 EXCLUDE 已废止(全项目 scope 后全收)，还是 v22 漏排。**三源未明确，真实缺口**。
2. **7z 机制矛盾**：三源一致规定大包用"7z -mx=9 + ship 7za"，但本次 Windows 无 7z，实际 ship 单层 zipfile DEFLATED(14.41MB)。务实适配可接受(standalone 仍可 unzip 直跑)，但与标准机制不一致——README 解包步骤已相应改对(已核实 ✓ 无 7za token)。应在 handoff/runbook 注明本次 Windows 单层 zip 变体 + 把变体打包脚本(build_v22_win.py)记入口。
3. **README carry-forward vs 新窗口零历史冲突**：原则禁 carry-forward，但 README_V22 实含 v17→…→v22 五段历史轮次叙述(已核实 shipped 包内 8 处 vN→vM 标记)。把"版本考古"误漏进给 reviewer 的 README。新窗口九审零 history reviewer 读这些会困惑。**原则与实践系统性张力**。
4. **no-priming vs README finding-close 表**：README_V22 含 "V21-8F1 — CLOSED"(已核实 12 处 CLOSED) + GO_WITH_MINOR(6 处)，处灰区——"实测 9/9 PASS"是 factual，但"CLOSED/已修复"label 是 verdict-priming。历史 v2/v12/v15 正因 README 写 close/verdict 被用户要求删。**建议 README 只留 factual(改了哪些文件+实测数字)，删 CLOSED 结论 label**。
5. **过时条款**：build v2-v7 docstring "Diff vs v1" 是 stale copy-paste，考古不可只信早期 docstring。

---

## 刚建 v22 包合规核对（已加主代理实地核实修正）

| 原则 | 满足 | 备注 |
|---|---|---|
| 包内不放 prompt | ✅ | prompt 成独立文件 GPT九审_prompt.md，zip 内无 prompt |
| 全项目 root copy + 真数据 + cross-check archive 全入 | ✅ | candidate_placements 53.6MB 字节完整 + Gemini archive + raw telemetry |
| spike code → code_context/ mirror + SHA256SUMS + NOT-merge 标注 | ✅ | 11 文件 + README + manifest |
| off-limits 硬 enforce + build 0 泄漏 | ✅ | 独立验证 build脚本/​.git/​.venv 0 泄漏 |
| README 复现命令全套 | ✅ | pytest/-O/ruff/mypy/radon + 期望计数 |
| 数字口径拆清(RSS sample vs after-solve) | ✅ | G8 行三口径拆清(v20 已修) |
| commit claim 不 overclaim | ✅ | v20 修 two-literal |
| self-ref 当前版本 | ✅ | v22 修 v20-package stale ref |
| fail-closed micro-probe 随包 | ✅ | 9-case 实测 9/9 |
| **README 解包步骤(7za→单 zip)** | ✅ | **主代理实地核实：shipped README 0 个 7za/project.7z token，解包段是单层 unzip(工作流原报 NO 是误报)** |
| **7z -mx=9 + ship 7za** | ⚠️ 务实偏离 | 本机无 7z 用单层 zipfile；standalone 仍可跑，但偏离标准(见矛盾点②) |
| **README 不带 carry-forward 历史** | ❌ | shipped README 8 处 vN→vM(见矛盾点③) |
| **不放 verdict-priming(CLOSED/已闭环)** | ⚠️ | shipped README 12 CLOSED + 6 GO_WITH_MINOR(见矛盾点④) |
| **排除 plan/roadmap doc** | ❌ | docs/项目说明/ 21 篇随全项目入包(见矛盾点①) |
