# Subject: authoritative numbers and derived claims

This subject is a documentation-level router, not the numeric authority itself. The numeric authority for cut-test counts and related review/package claims remains `docs/research/p1_2_spike_sizing_gate_20260601/authoritative_numbers.json`, regenerated by `scripts/gen_authoritative_numbers.py` and guarded by `src/tests/test_authoritative_numbers_currency.py` for the enforced master-side subset.

The rule for projections is intentionally conservative: prose should point to the numeric core node instead of embedding volatile values unless the number is historical and dated.

<!-- SUBJECT-FIELD:runbook_summary START -->
Repeated review/package numbers must flow through `docs/research/p1_2_spike_sizing_gate_20260601/authoritative_numbers.json` and `scripts/gen_authoritative_numbers.py`. Prose projections should link to that core node instead of hand-copying volatile current values, unless the number is explicitly historical and dated.
<!-- SUBJECT-FIELD:runbook_summary END -->

<!-- SUBJECT-FIELD:cuts_count_pointer START -->
The current cut-test total is not a freehand prose value. Use the `cuts_tests_total` entry in `docs/research/p1_2_spike_sizing_gate_20260601/authoritative_numbers.json`; regenerate/check it with `python scripts/gen_authoritative_numbers.py --check`.
<!-- SUBJECT-FIELD:cuts_count_pointer END -->
