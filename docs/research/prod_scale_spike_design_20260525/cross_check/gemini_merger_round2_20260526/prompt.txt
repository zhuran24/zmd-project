你是一个独立的算法/数学/paradigm 层 cross-check reviewer. 任务: 这是 **round 2** — 验 round 1 verdict (NOT_GO) 的 finding 是否真被 fix effective. 你看不到本地文件, 所有 context 在本 prompt 内.

严格要求 (同 round 1, 不可商榷):
1. 不接受 "I believe / intuitively / experience suggests / common sense" 这类 vague claim
2. 每条 finding (含 verify verdict) 必含: severity (BLOCKER / HIGH / MEDIUM / LOW), 问题陈述 (≤3 句), 数学或 paradigm 论证 (复杂度估算 / 文献 / 实测数据 / 反例构造 任一), suggested fix
3. push back 某 fix 必须引用具体数据 / 文献 / 复杂度论证 / 反例构造, 不能只 "intuition"
4. 末尾 overall verdict: GO / GO_WITH_MINOR / NOT_GO + 1 句话理由
5. audit 模式: 找问题, 不要被夸傻不要给 ritual GO
6. **本 round 2 重点 = fix verification**, 不重审 round 1 已 close 的决策 (C1 81K 全量 / C2 stub 用 / C5 2 PR 流程 paradigm). 但若发现 fix 引入新 risk 必 flag.

================================================================================
SECTION A. Round 1 verdict 完整摘要 (你审过, 这里复述确认上下文)
================================================================================

A.1 Round 1 overall verdict: **NOT_GO**

理由 (你原话): "Spike 选取的 LBBD 收敛指标 (G15: search tree node 单调减) 在 OR-Tools CP-SAT 的 portfolio search 与 presolve 机制下数学上不成立, 且抛弃 100K cut scale 测试将直接在 168h prod run 中触发 OOM/超时盲区, 必须修正这两大基石才能启动."

A.2 Round 1 findings 全列 (6 finding + 1 missing risk + 3 residual)

| ID | Severity | 决策 | 问题 |
|---|---|---|---|
| F1 | BLOCKER | C2/C4 | G15 "search tree node 单调减 ≥30%" 在 CP-SAT presolve 重排下数学不成立; D3 stub 返 fixed verdict 不能模拟 Benders 真动力学 |
| F2 | HIGH | C3 | 砍 100K cut 是 168h prod 致命盲区 (168h / 60s/iter × 10 cut/iter ≈ 100K 累积, 50K→100K 越 L3 cache boundary 易触发超线性 RSS 撞 L24 30 GB) |
| F3 | HIGH | C1 | 50 inst probe 5-10 min wall cap 过松 (mini Step 8 baseline 推 probe ≤ 5s 合理, 给 5-10 min 多 60-120× margin) |
| F4 | MEDIUM | C4 | G3 build wall ≤ 60s @ 10K cut 过松 (10K cut × 100 boolean term = 10^6 term × OR-Tools C++ vectorized 1-2s, 60s 容忍 Python for 循环逐条 `model.Add()` 反模式; 建议 ≤ 6s @ 10K) |
| F5 | MEDIUM | C5 | 2 PR 重写 fidelity gap 无机制保证 (spike validated 性能特征在 PR #2 重写后可能不复存, 撞 L13 hidden assumption 死法) |
| F6 | HIGH | C2 | 5 iter LBBD 不足以触 phase transition (iter 7+ 才进 marginal cut presolver 失效区, L16 + B1 path-2 死在 10 iter UNPROVEN, 建议 ≥15 iter) |
| C6.3 | missing | cross-cutting | 跨 candidate source_digest invalidation (outer search 切 candidate 时 ghost_id 变, store cache 跟 by_ghost watcher 必清, source_digest 必重新 emit, 8 路 sibling 0 cover) |
| C7 | residual | future | 3 项 P1.3A risk: sub-problem stub vs real binding/routing fidelity gap / inner subprob timeout 策略 / "optimal but unprovable" trap |

A.3 Round 1 你没 push back 的决策 (本 round 不重审, 除非 fix 引入新 risk)

- C1 81K BoolVar 全量主路径 (你 implicit GO)
- C2 sub-problem 接 stub 思路 (你 implicit GO, 但 stub design 必改 = F1)
- C5 2 PR 流程 paradigm 立场 (你 implicit GO, 但 fidelity 机制必加 = F5)
- C7 spike GO ≠ paradigm GO 的边界

================================================================================
SECTION B. Round 1 fix list (commit `52cd014` landed)
================================================================================

下列每条 fix = "round 1 finding ID → MERGER §X 改动具体内容". 你的 round 2 任务是 verify 每条 fix verdict (CORRECT / NEW_GAP / PARTIAL / INCORRECT).

B.1 F1 fix (BLOCKER) — G15 metric 改 + G15b stub redesign (MERGER §5.4)

**原 G15**: "5 iter × 3 candidate = 15 master.solve, search tree node count 单调减 ≥30% per iter OR iter 5 prune ratio vs iter 1 ≥ 2x"

**新 G15** (replace): "≥15 iter LBBD single candidate, wall-time 收敛:
- iter N+1 wall ≤ iter N wall × 1.5 (允许 noise)
- 最后 5 iter avg wall ≤ first 5 iter avg wall × 0.7 (真收敛 signal)
**不用 search tree node count** (per round 1 论证 CP-SAT presolve 重排让 node count 不单调甚至激增)."

**新 G15b** (新增): "stub 必返 targeted no-good (读 master `SolutionInfo()` 当前 selected_pose set 产 `sum(x[g,p] for selected) ≤ len-1` 切当前解), 不能返 fixed verdict."

**配套 N8 update** (replace):
- 原 N8: "G15 search tree node 单调增 OR prune ratio 不收敛"
- 新 N8: "G15 wall-time 不收敛 (iter 15 wall > iter 1 wall × 0.7) OR stub 没产 targeted no-good"

B.2 F2 fix (HIGH) — cut count ramp 恢复 100K + 加 G4b + G9 100K 阈值 + N3 100K trigger (MERGER §5.2/§5.4)

**§5.2 cut count ramp** (replace): `1K / 10K / 50K / **100K**` (加 100K 挡; 注释加 "Gemini round 1 F2 fix — 168h / 60s/iter × 10 cut/iter ≈ 100K accumulated, 必测 50K→100K L3 cache boundary 防超线性 RSS")

**§5.4 加 G4b** (新增): "G4b: 81K + 100K cut build wall ≤ 600s"

**§5.4 G9 update** (replace 原 "≤ 500 MB"): "G9: proto.ByteSize() ≤ 500 MB @ 50K, ≤ 1 GB @ 100K"

**§5.5 N3 update** (replace): "N3: G8 RSS > 30 GB (撞 L24 augmented master 死法 reference); 100K 挡位 RSS 超线性 (e.g. 50K→100K 涨 >2x) trigger Gemini F2 警告"

B.3 F3 fix (HIGH) — 加 G17 probe wall ≤ 15s + N13 abort (MERGER §5.4/§5.5)

**§5.4 加 G17** (新增): "Failfast probe (Gemini F3 HIGH fix): G17: 50 inst subset probe wall ≤ **15s**. 超时 abort spike (probe 自己慢 = harness bug, 不进 81K 主测)."

**§5.5 加 N13** (新增): "N13: G17 probe wall > 15s (harness 自身 bug, 不进主测)"

B.4 F4 fix (MEDIUM) — G3 build wall 60s → 30s (折中) (MERGER §5.4)

**G3 update** (replace 原 "≤ 60s"): "G3: 81K + 10K cut build wall ≤ **30s** (折中: Gemini round 1 推 ≤ 6s 没考虑 9 family translator dispatch overhead, 收紧到 30s 卡 SWIG vectorized 反模式)"

注: round 1 你推 ≤ 6s, merger 取 30s 折中. 理由是 9 family translator (F1-F9) 各 oracle 的 Python-side dispatch overhead 不可忽略 — 每 family translator 接 cut tuple 调 `model.Add()` 时有 Python attribute lookup / dispatch / type check overhead. 实测 OR-Tools 9.15 单 `model.Add(sum([...]) <= K)` 在 Python wrapper 走 SWIG 边界 ~50-200 μs 量级, 10K cut × 200 μs = 2s, 9 family dispatch dispatch overhead 又 ×3-5 → 10-30s 是预估范围.

B.5 F5 fix (MEDIUM) — 加 PR #2 protobuf hash compare 验 fidelity (MERGER §5.1)

**§5.1 update** (add): "GO → 2 PR (PR #1 verdict doc only / PR #2 重写 P1.3A 实施, 不 cherry-pick spike code); **PR #2 必须 emit 同结构 `cp_model.Proto()`, hash compare spike verdict 的 baseline protobuf hash 验 fidelity** (Gemini round 1 F5 fix — 防重写后性能特征跟 spike 验过的不一致, 撞 L13 hidden assumption 死法)"

B.6 F6 fix (HIGH) — multi-iter LBBD 5 iter × 3 candidate → ≥15 iter × 3 candidate (MERGER §5.2)

**§5.2 multi-iter LBBD update** (replace 原 "5 iter × 3 candidate"): "**Multi-iter LBBD**: **≥15 iter** single candidate × 3 candidate (Gemini round 1 F6 fix — L16/B1 path-2 死在 iter 10 UNPROVEN, iter 7+ 才进 marginal cut presolver 失效区, 5 iter 触不到 phase transition), benders_loop 接 **targeted no-good sub-problem stub** (per G15b)"

B.7 C6.3 fix (missing risk) — 加 G16 跨 candidate state isolation verify + N8b leak trigger (MERGER §5.4/§5.5)

**§5.4 加 G16** (新增): "**跨 candidate state isolation** (Gemini C6.3 missing risk fix): G16: 3 candidate 切换时, store.snapshot() after candidate N vs N+1 必 verify:
- by_ghost watcher entries cleared
- source_digest 重新 emit (hash 应 equal 但 emit timestamp 必 update)
- cross-candidate cache 无 leak"

**§5.4 §5.2 3 ghost transition update** (add): "3 ghost transition: 测 held/active/quarantined 状态机 + **跨 candidate source_digest invalidation**"

**§5.5 加 N8b** (新增): "N8b: G16 跨 candidate 切换 store cache leak (by_ghost watcher 残留 OR source_digest 没重新 emit)"

B.8 C7 fix (residual) — 入 P1.3A risk register (待 plan doc update)

3 项 P1.3A residual risk (sub-problem stub vs real fidelity / inner subprob timeout / "optimal but unprovable" trap) 入 P1.3A risk register, **spike scope 不 cover**. 待 `docs/项目说明/09_phase_1_3_plan.md` 后续 update.

B.9 工时 update (MERGER §5.6)

- 原: 15-22h Claude / 3-6h wall
- 新: **20-29h Claude / 5-9h wall** (Gemini round 1 fix 加 +3-4h Claude / +2-3h wall, 主因 15 iter + 100K cut + stub redesign + 跨 candidate verify + probe timeout 加)

================================================================================
SECTION C. 当前 MERGER §5 / §6 / §7 final 全文 (round 1 fix landed)
================================================================================

C.1 §5.1 Branch + commit 策略

- Branch: `spike/prod_scale_master_integration_20260526` (off master `20f1f22`)
- 7-10 commit checkpoint, each `[SPIKE]` 前缀 + interim GO/NOT-GO signal
- 失败 → `git branch -D` (zero touch master)
- GO → 2 PR (PR #1 verdict doc only / PR #2 重写 P1.3A 实施, **不 cherry-pick spike code**); **PR #2 必须 emit 同结构 `cp_model.Proto()`, hash compare spike verdict 的 baseline protobuf hash 验 fidelity** (Gemini round 1 F5 fix)
- 7-day wall clock 硬上限
- State sandbox: `EXACT_SPIKE_OUTPUT_DIR=data/cuts/spike/`, 8 项 off-limits PR rebase 时 zero diff enforce

C.2 §5.2 Scope (要测的)

- Master scale: 81,795 BoolVar 真 prod (env `EXACT_USE_POSE_BOOL_MASTER=1` 用 PoseBoolExactMaster, B1)
- Failfast probe: 50 inst subset fixture, spike 启动前 5-10 min run (per D1)
- Lifecycle: 9 step 全真 invoke (step 1 oracle real emit / step 3 serialize / step 4 deserialize / step 5 validate dispatch / step 6 attach scope check / step 7 evaluate / **step 8 apply_to_master = 主交付** / step 9 regression sweep)
- Real oracle real emit: 9 family × ≥5 cert = ≥45 cert 真 step 5 validate
- Cut count ramp: 1K / 10K / 50K / **100K** (Gemini F2 fix)
- Feasible smoke: IP v2 blueprint hint case — branches≥1K / conflicts≥100 / wall≥1s / status≠INFEASIBLE
- **Multi-iter LBBD**: **≥15 iter** single candidate × 3 candidate (Gemini F6 fix), benders_loop 接 **targeted no-good sub-problem stub** (Gemini F1 fix — stub 必 read master `SolutionInfo()` 当前 selected_pose set 产 `sum(x[g,p] for selected) ≤ len-1` 切当前解)
- 6-dim watcher: 全 dimension (by_cell / by_group / by_pose / by_commodity / by_region / by_ghost) 各 ≥1 注册
- 3 ghost transition: 测 held/active/quarantined 状态机 + **跨 candidate source_digest invalidation** (Gemini C6.3 fix)
- Adversarial inject: 50 bad cert + 9950 good cert 混 + 3 forged-cert case (F1/F2/F3 each)
- Sub-route: 主测 sub-route 1 (solve-rebuild), sub-route 3 1-iter trial 验接口, sub-route 2 defer P1.3B
- Active filter: 跑推荐的 Hybrid (score = activity_count - 0.1 × age_decay), ablation defer

C.3 §5.3 NOT-scope

- 真跑 binding subproblem (用 targeted no-good stub instead)
- 真跑 routing subproblem
- Multi-process / multi-worker (single worker; CP-SAT threading non-determinism 文档化 per Gemini C6.1)
- 168h ramp (spike ≤ 2 hour run; 100K cut 是 168h 等价累积上限)
- Sub-route 2 (C++ propagator) — defer P1.3B
- Active filter ablation 全跑 — 只测推荐 Hybrid
- Observability 全 12 event class — 只 4 必
- Cut purge 物理删机制 — Gemini C6.2 derived risk, defer P1.3A 主体

C.4 §5.4 量化 GO criteria (17 项, 全 hold 才 GO)

**Build cost** (Gemini F4 收紧 G3):
- G1: 81K BoolVar + 0 cut build wall ≤ 10s
- G2: 81K + 1K cut build wall ≤ 20s
- G3: 81K + 10K cut build wall ≤ **30s** (折中)
- G4: 81K + 50K cut build wall ≤ 300s
- **G4b**: 81K + 100K cut build wall ≤ 600s (Gemini F2 加)

**Solve cost (feasibility only)**:
- G5: 0 cut feasibility wall ≤ 30s
- G6: 1K cut feasibility wall ≤ 60s
- G7: 10K cut feasibility wall ≤ 180s, status OPTIMAL or FEASIBLE (不能 INFEASIBLE 早停)

**Resource**:
- G8: RSS peak ≤ 20 GB single worker; 100K cut 挡位 RSS 必 measure (Gemini F2)
- G9: proto.ByteSize() ≤ 500 MB @ 50K, ≤ 1 GB @ 100K (Gemini F2)

**Lifecycle correctness**:
- G10: 9 step 全 invoke, 0 NotImplementedError, 0 fail-closed assert violation
- G11: ≥45 cert 真 step 5 validate, 全 sound
- G12: 6-dim watcher 全注册, store.stats() 6 dim 各 ≥1

**Adversarial**:
- G13: 50 bad cert 100% quarantine, store active count = 9950
- G14: 3 forged-cert case (F1/F2/F3 each 1) 必 quarantine, 0 漏

**Multi-iter convergence** (Gemini F1 + F6 fix):
- G15: **≥15 iter LBBD single candidate**, wall-time 收敛: iter N+1 wall ≤ iter N wall × 1.5 AND 最后 5 iter avg wall ≤ first 5 iter avg wall × 0.7. **不用 search tree node count**.
- G15b: stub 必返 targeted no-good (读 master `SolutionInfo()` 产 `sum(x[g,p] for selected) ≤ len-1`), 不能返 fixed verdict.

**跨 candidate state isolation** (Gemini C6.3 fix):
- G16: 3 candidate 切换时, store.snapshot() after candidate N vs N+1 必 verify:
  - by_ghost watcher entries cleared
  - source_digest 重新 emit (hash 应 equal 但 emit timestamp 必 update)
  - cross-candidate cache 无 leak

**Failfast probe** (Gemini F3 fix):
- G17: 50 inst subset probe wall ≤ **15s**. 超时 abort.

C.5 §5.5 量化 NOT GO criteria (13 项)

- N1: G1-G4b 任一 build wall 超阈值 ×2
- N2: G7 INFEASIBLE 早停, 即便加 blueprint hint
- N3: G8 RSS > 30 GB; 100K 挡位 RSS 超线性 (>2x) trigger
- N4: G9 proto > 2 GB
- N5: G10 9 step 任一 raise / assert violation
- N6: G11 oracle real-emit cert unsound
- N7: G13/G14 adversarial inject 漏 ≥1
- N8: G15 wall-time 不收敛 OR stub 没产 targeted no-good
- N8b: G16 跨 candidate 切换 store cache leak
- N9: Reproducibility variance > 30%
- N10: 7-day wall clock 用完仍未 cover 主路径
- N11: spike 跑后 jsonl 4 必 event 任一 = 0
- N12: Off-limits file 8 项任一 diff non-zero
- N13: G17 probe wall > 15s

C.6 §6 / §7 Merger 自评 (round 1 后新加 3 条)

6. D5 cut count ramp 折取错 — Gemini F2 catch 100K cut blind spot, 已恢复 (G4b)
7. D3 LBBD iter 数我估错 — Gemini F6 catch 5 iter 不触 phase transition, 已 5→15 iter
8. 跨 candidate state isolation 完全漏了 — Gemini C6.3, 已加 G16

================================================================================
SECTION D. Round 2 审查问题列 (你需回答的)
================================================================================

请逐条 verify 每个 fix, 给 verdict (CORRECT / NEW_GAP / PARTIAL / INCORRECT) + 论证.

**Q1 (F1 fix verify)**: G15 wall-time 收敛 metric (iter N+1 wall ≤ iter N × 1.5 AND 最后 5 iter avg ≤ 前 5 iter avg × 0.7) 在 CP-SAT 加 cut 后真 sound 吗?
- 数学/CP-SAT internals 角度: wall-time 是不是更 sound 的收敛信号? OR 仍有 corner case (e.g. cut 触发更快 infeasibility 让 wall 急减但其实是 spike fixture 有 bug)?
- 是否需要补 baseline (无 cut iter wall 作 reference, 防 wall 收敛是 spike harness 自己优化的 artifact)?
- "iter N+1 ≤ iter N × 1.5" 的 1.5 factor 是 noise tolerance, "0.7" 是 30% wall 收敛要求. 这 2 个常数来源是 vague 还是 OR-Tools / Benders 文献支撑?

**Q2 (F1b fix verify — stub redesign)**: targeted no-good stub `sum(x[g,p] for selected) ≤ len-1` 真模拟 Benders 动力学吗?
- 数学层: 这条 no-good 跟真 binding/routing subproblem 切的 no-good 是同结构吗? 还是 over-simplified (真 sub-problem 切的 no-good 通常涉及多 instance × 多 pose 组合, 不只 sum<=K-1)?
- 反例: 设计一个真 binding subproblem 行为, 让它产生的 no-good 跟 stub 的 sum<=K-1 行为不同, 导致 spike GO 但 P1.3A 真接进去 LBBD 收敛性差.
- 是否需要让 stub 多种 no-good (sum, weighted sum, pairwise, etc.) 模式中随机选一种, 而不是固定 sum<=K-1?

**Q3 (F2 fix verify — 100K cut 恢复)**: 100K cut build ≤ 600s + proto ≤ 1 GB @ 100K 阈值合理?
- 100K cut build ≤ 600s 数学论证: 假设线性, 50K cut build ≤ 300s × 2 = 600s. 实际超线性 (presolver / SAT preprocessing 在 cut 数大时 cost 涨更快) — 600s 是否过松?
- 168h prod 真的累积 ≥ 100K cuts 吗? 你之前用 168h / 60s/iter × 10 cut/iter ≈ 100K 推. 但 inner LBBD avg iter wall 可能远超 60s (B1 path-2 实测 77.8s/iter, L16 81.8s 单 iter), 真累积可能 50-80K range. 100K 是否 overshoot?
- proto ≤ 1 GB @ 100K: 单 cut sum constraint ~10 KB protobuf, 100K = 1 GB 量级 ✓ 但若 cut 平均 size 涨 (real binding/routing cut > stub no-good) 实际可能 2-3 GB. 阈值是否需 ≤ 2 GB?

**Q4 (F6 fix verify — 15 iter)**: 15 iter LBBD 在 spike scale 足够触 phase transition 吗?
- L16 死在单 iter 81.8s OPTIMAL 但 cut 不收敛, B1 path-2 死在 10 iter 778s UNPROVEN. 这俩 reference 是 wall-time 不是 iter count, 你 round 1 推 ≥ 15 iter 数据来源是?
- 反例: 设计一个 cut family 行为, 让前 15 iter wall 都 monotone 减 (假收敛) 但 iter 16 wall 突然炸 (撞 phase transition 后区) — 15 iter 不够.
- 是否需要 ≥ 20 iter (额外 +33% wall 死时间) 或加一个 "iter wall 突变 > 2× → 警告" criterion?

**Q5 (C6.3 fix verify — G16 跨 candidate)**: G16 verify 方式 (snapshot diff + watcher entries cleared check + source_digest re-emit verify) 是否充分?
- 反例: store cache 表面 cleared 但深层有 dangling reference (e.g. ghost rect proto object 被 candidate 1 ↔ candidate 2 共享, 一边改一边崩). 仅 snapshot diff 抓不到 in-memory aliasing 问题.
- 需要补什么? (e.g. `id()` 对象 identity check / gc.get_referrers() 验 lifetime / weakref leak check)
- by_ghost watcher entries cleared 的 "cleared" 定义: 物理 dict 清空, 还是逻辑 invalidate flag? 这 2 个 spike 必须验哪个?

**Q6 (F4 fix verify — G3 30s)**: G3 30s 折中 vs round 1 你推 ≤ 6s. 30s 是否仍过松?
- 你原 6s 论证: 10K cut × 100 term = 10^6 term × C++ vectorized 1-2s. 现 merger 加 9 family translator dispatch overhead (200 μs/cut × 10K × 5 family ≈ 10s). 这个 dispatch overhead 你认可吗?
- 即便认 dispatch overhead, 6s + 10s = 16s 仍 < 30s. 30s 留 ~2× margin 是合理 noise 还是过松?
- 是否应该 ≤ 20s, 30s 留 N1 (×2 = 60s) 才是过松? 或 ≤ 15s, 30s 留 abort?

**Q7 (F5 fix verify — protobuf hash compare)**: PR #2 protobuf hash compare 真能 catch 重写 fidelity gap?
- cp_model.Proto() 跟 cp_model SerializeToString() 是 deterministic 吗 (OR-Tools 9.15 SWIG bindings 是否保证 field order / repeated field order 稳定)? 若 non-deterministic, hash 比对会 false-positive abort.
- hash 不等 → abort PR 还是 warn? merger 现 §5.1 没明确. 若 abort, 任何 refactor (variable renaming / constraint re-grouping) 都 break hash. 若 warn, 又失去 fidelity 保证.
- 替代方案: 用 model semantic invariant (var count / constraint count / coefficient histogram) 而不是 raw byte hash, 是否更 robust?

**Q8 (residual — round 2 新 finding)**: 还有 round 1 你没 catch 但 round 2 该 catch 的 finding 吗? 特别注意:
- **paradigm 层**: cut framework 内部新死法预测 (e.g. 跨 family interaction — F1 region_capacity cut 跟 F7 power_hitting_set cut 在 master 中加和后是否会产生 redundant constraint, 让 presolver 时间炸)
- **跨 family interaction**: 9 family 间 var structure conflict (e.g. F3 port_exposure 用 pose pair var, F8 power_grid_reach 用 cell adjacency var, 两者在 master 中是否会产生 disjoint var subgraph 让 search portfolio 失效)
- **CP-SAT 9.15 SWIG / propagator hot-path 新风险**: spike 跑 81K BoolVar + 100K cut 的组合是否撞 OR-Tools 9.15 已知 SWIG memory leak (e.g. AddBoolOr 在 long-lived model 上重复调用 RSS 涨 不释放) 或 propagator 在 high cut density 下 priority queue cost 变 O(n²)
- **stub 跟 main solver 的 callback 接口**: targeted no-good stub 怎么 hook 进 PoseBoolExactMaster? master.solve() OR-Tools 调用是 blocking, stub 是 between-iter 还是 within-iter callback?

**Q9 (overall)**: round 1 verdict NOT_GO, round 2 是否升 GO_WITH_MINOR 或 GO?
- 若 round 2 仍 NOT_GO, blocker 是 fix 不彻底 (existing finding 残余) 还是有新 finding (Q8)?
- 若升 GO_WITH_MINOR, 哪些 MINOR 必 spike 内 cover 不能 defer?
- 若升 GO, 你对哪个 finding 的 epistemic 信心最低 (即 spike 跑完最可能反过来打你脸的 prediction)?

================================================================================
SECTION E. 输出格式要求
================================================================================

请按以下 markdown 格式输出:

```
## Overall verdict
[GO | GO_WITH_MINOR | NOT_GO]
理由: [1 句话]

## Round 1 fix verification table

| Finding ID | Fix 内容简述 | Round 2 verdict | 论证 (≤2 句) |
|---|---|---|---|
| F1 (G15 wall-time + stub) | ... | CORRECT / NEW_GAP / PARTIAL / INCORRECT | ... |
| F2 (100K cut 恢复) | ... | ... | ... |
| F3 (G17 probe 15s) | ... | ... | ... |
| F4 (G3 30s 折中) | ... | ... | ... |
| F5 (proto hash compare) | ... | ... | ... |
| F6 (15 iter LBBD) | ... | ... | ... |
| C6.3 (G16 跨 candidate) | ... | ... | ... |
| C7 (residual P1.3A risk) | ... | ... | ... |

## Findings (round 2 新 finding, 含 fix verify push-back)

### Finding 1: [简短标题]
- **Severity**: BLOCKER / HIGH / MEDIUM / LOW
- **针对**: Q1 / Q2 / ... / Q9 或 "cross-cutting"
- **问题**: [≤3 句]
- **论证**: [数学 / 复杂度 / 文献 cite / 反例构造 / 实测数据 — 必须 specific]
- **建议 fix**: [具体, actionable]

### Finding 2: ...

## Q8 missing-risk inventory (round 2 新 catch)

1. [risk 描述 + 8 路 + round 1 没 cover 的原因 + spike scope 内必加 or defer 合理]
2. ...

## Closing

[整体 1 段话评价 round 1 fix 的 epistemic posture (是否彻底解决 / 仍 hand-wave / over-engineered), 并给主对话 next-step 推荐: spike 实施 / round 3 / 重设计]
```

请开始 round 2 审查.
