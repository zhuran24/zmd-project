# W0 外脑回件合并裁决 + 当晚落地读数（2026-08-04）

> **⚠ 勘误（2026-08-05，空地语义裁决）**：本文书对 27/28 号 band22 见证的复核背书
> （「孔 H 与机身交集 0、合规」「`H` 就是该布局的全图最大合法空矩形」「(42,6)【实测】」
> 及「08-04 夜升级两笔」的孔洞相关表述）全部是在**裁决前的宽松 ghost 语义**下做出的
> （V88：空矩形只排设施、允许路由穿孔）。owner 2026-08-05 裁定空矩形=什么都不能有
> （`docs/research/rules_audit_20260718/02_empty_rectangle_semantics_adjudication_20260805.md`）；
> 该见证孔内实含 **22 格路由**（见证自报 `hole.route_overlap_cells: 22`——本文书两轮
> 复核均未把该字段读进表述，检讨已记档）**+ 4 个端口 front 落孔**。严格语义下
> (42,6) 登记资格已暂停，V0-A 骨架随后被 strict-hole 探针判死（定理 A + 5×5 列账
> 48<49，两枪独立；见 `.artifacts/band22_strict_hole_probe_20260805/PROBE_REPORT.md`）。
> 本文书其余部分（25/26 号裁决、G1/G2 读数、L 入账条件的 strict checker 前置）不受影响。
>
> **性质**：research-only；`authority` 恒 false；本文书不登记任何界，
> `U=(1188,18)`（conditional）与 `L=absent` 一字未动，不触碰 cut / production /
> certified 状态，也不触碰 `PROJECT_LOCK.md` 的任何 `F-*`/`PCR-*`/`CUT-*` 条款。
>
> **被裁对象** = 2026-08-04 同日到齐的**四份** GPT Pro 交付包，逐字存档在
> `docs/research/cleanroom_rederivation_20260718/` 下：
> `25_rstar_pricetag_delivery_20260804/`（九条 `R-*` 限制的价签）、
> `26_pricing_bound_exp_delivery_20260804/`（Lagrangian 上界证书的判定实验包）、
> `27_band22_witness_delivery_20260804/`（band22 条带路线的坐标级见证）、
> `28_band22_dup_answer_20260804/`（**同一份 v1 送包被意外发出第二次**、由另一个独立
> GPT Pro 会话作答的第二份答卷——与 27 号同题、**同一份 v1 基础包**、会话互不可见，
> 是独立样本不是修订版。**但订正侧信道未受控**：v1 之后 owner 另贴的口头订正便条，
> 两个会话收到的条目不相同，现有记录本身也自相矛盾（§5.8），所以这不是一次严格的
> 同材料复现实验）。
> 四份各自的来源、送包 hash、owner 转述摘要与边界见同目录 `25_/26_/27_/28_PROVENANCE.md`。
>
> **另含两份我方当晚的落地读数**（这两份不是回件，是我方机器跑出来的数）：
> §3.5 = 26 号判定协议在我方机器上的实跑结果（`.artifacts/w0_fixrerun_20260804/pricing_exp_run/`）；
> §6 = band22 见证到**官方仓库语义**的对齐探路（`.artifacts/w0_fixrerun_20260804/band22_alignment/`）。
> 它们把 §8 处置清单里的四条阻塞项直接做掉了，所以并进同一份文书。
>
> **复核方式** = 四场验收工作流、十一个席位（25/26/27 每场 1 个存档席 + 2 个独立复核席；
> 28 号 1 个存档席 + 1 个撞车复核席）。
> 每场先由**存档席**完成入库（复制入仓、写 provenance、staging、preflight、commit——这一段
> 是写操作，不是只读）；随后两个**复核席对仓库只读**。复核席之间**实现独立、核心谓词交叉重叠**
> （不是方法互不重叠）。承重数字一律不以回件自带代码自证，均由复核席从冻结权威数据从零重算；
> 但 harness 席会**另外复跑回件自带代码**（`test_deliverable.py`、`pricing_probe.py`、
> `make_tables.py`、`apply_price_tag_patches.py`）作功能验证——那部分是「跑得起来」的证据，
> 不是数字正确性的证据。席位原始裁定见 §9 证据索引。
>
> **证据等级**：本文书的**承重数字与结论逐项标等级**（非承重的行文数字可能只受段尾聚合标签
> 覆盖，遇到复合标签以下面这套基本等级拆开读）——【实测】= 我方机器跑出来落盘的数、
> 【复现】= 我方独立重算命中对方自述值、【对方自述】= 未经我方核实、
> 【算术】= 由已核实数字推算、【强论据】= 有证据但不构成证明、【猜测】= 无数据支撑。

---

## 一、总裁定

| 包 | 内容 | 复核席 | 裁定 | 必须随引用带走的尾巴 |
|---|---|---|---|---|
| **25 号** | 九条 `R-*` 充分限制各一份价签 + 补丁 A/B/C/D + 九份判定实验规格 | 对抗席、复算席 | **PASS_WITH_NOTES** | 5 笔数字修正账（§2.2）+ 1 条溯源缺陷（§2.3） |
| **26 号** | Lagrangian 上界证书机器的数学 + 可跑 harness + GO/NO-GO 协议 | 数学席、harness 席 | **PASS_WITH_NOTES** | 消费读数前三道门（§3.3、§3.4）**只做掉了 ①③，② 未做**（§3.5）；3324 是 cap-3 条件阈值不是现成证书 |
| **27 号** | band22 条带路线的坐标级完整布局见证（266 设施 + 25 杆 + 1143 路由格） | 几何席、纪律席 | **PASS_WITH_NOTES**（几何席零 BLOCK） | 见证是 research witness（pre-candidate）；**§6 的对齐探路把 placement / 机身-front / 计数 / 当前供电重算这几个子结论抬到了官方冻结件口径**，但整份见证仍是 research/consultation-aligned，官方两道 CP-SAT 门与终端固定见证复验都未跑；全部定理数字**只对该固定几何**成立 |
| **28 号** | 同题第二份独立答卷：洞①/洞③ 的坐标级局部补丁 + 洞② 的「整族不可行定理」 | 存档席、撞车复核席 | **局部补丁 PASS ／ 核心定理 REFUTED** | **那条不可行定理不得引用**，它指挥的「砍 D0 变体、别再枚举」也一并作废（§5） |

**四包 + 两份落地读数合起来最重要的三件事**：

1. **band22 见证站住了，而且比昨天硬得多。** 27 号那份布局给了一个 G1 **表达不出来**的完整摆法；
   28 号那份反方向的「整族不可行定理」被这同一份见证**当场证伪**（§5）。**注意这轮复核的范围**：
   撞车复核席从零重算的只有**机身、电杆与供电覆盖**这一层，不含 1,143 个路由格、628 个激活端口、
   19 种商品与端口绑定。所以准确说法是「机身与供电子范围的第二轮独立复核」，不是整份见证被复核了两遍。
2. **它的口径折扣被 §6 拆掉了一部分——是子结论级，不是整份。** 昨天说「只在咨询合同口径下成立」；
   现在实测：291 个位姿**全部**能在**官方冻结 pool** 里唯一解析，`matching_map_boundary` 那个
   未测点的前提**根本不存在**（§6）。所以 **placement / 机身与 front 几何 / 端口精确计数 /
   当前供电重算**这几个子结论可以按官方冻结件口径说；但**端口那项只重算了精确计数、没跑 binding
   CP-SAT，路由那项只过了必要条件**，`VERDICT.json` 自己首行就写 `no official checker was run`。
   **整份见证的等级仍是 research / consultation-aligned。**
3. **③ 那条上界证书路当晚跑出的主输出字面值是 `INVALID_CALIBRATION_FAILED`**（§3.5）。
   补入一次无 cap 的单变量校准之后重判才得到 `NO_GO`，且那是**旧 g1 连通语义 + cap-3 口径下**的
   **二次读数**，不是主协议输出的字面裁定，也不是三道门全过后的终局结果。九支界一格没动、
   统一界停在 3388、距证书线还差 64。

---

## 二、25 号：九条 `R-*` 限制的价签

### 2.1 裁定

**PASS_WITH_NOTES。无 CRITICAL / HIGH。** 两席各自独立复算全部量化断言，结论一致：
**除一处 0.001 个百分点的舍入笔误外，未发现算术错误**（`RSTAR_PRICETAG_REPORT.md:178`
把 `265/3,888` 写成 6.817%，Decimal 精确值 6.815843621…%，三位小数应为 **6.816%**；
本文书 §2.1 采用的六位值 6.815844% 是对的，所以是原报告的排印级笔误、不影响任何下游数字。
已补进 §2.2 修正账第五笔）。

复算席用与回件零代码共享的暴力枚举重写了全部八个头条数字，逐位命中【复现】：

- HOLE 房内保留 3,600 / 4,601,025；边界双臂 47 种（递归枚举，并与 `24²−23²` 交叉验证）；
  core 位姿 7,688 → 1；R-BODY 删 1,169,408 / 3,839,768 = 30.455173%；
  R-FRONT 增量 386,560 / 2,670,360 = 14.475951%；联合 1,555,968 / 3,839,768 = 40.522448%；
  R-PORTAL 176,088 / 2,076,768 = 8.478944% 与杆锚点 265 / 3,888 = 6.815844%；
  R-POWER 跨区 165,600 / 562,500 = 29.44%；CORE-FRONT 54 被迫 + 12 额外、杆锚点 28→20、
  恢复的 8 个坐标逐格对上。

比纸面重算更强的一层：**三个最承重的 post-mask 数字直接由我方生产代码跑出、与回件字节级
相同**【实测】——`g1_pattern_generator.enumerate_body_poses` 给 M3/M5/M6 = 10,256 / 6,176 / 6,428，
`enumerate_pole_poses` 合计 3,623，CORE 区 0 台机器位姿 / 20 个杆锚点。这说明对方对固定布局、
front 语义、桩集合与 core 坐标的转录是忠实于代码的，不只是自洽。

`H0` 基线分母也不是转录产物：`candidate_placements.json`（54 MB 冻结件）的 `facility_pools`
长度实测为 17,952 / 16,896 / 16,900 / 7,688 / 4,761 / 136，与回件所用分母逐条相同【实测】。

**口径纪律自称属实**：报告每条散文行都带证据等级；六个不同 proxy 域各自在使用点具名；
§5.4 明写「没有共同分母」并拒绝建立全局全序；防重复计价三条规则（FRONT 报增量 + 联合总数、
PORTAL 与 PAT 连通性合并计价、PAT 与 HOLE 塌进 J-CONN）都正确。
`R-POLE-CAP` 一条尤其干净：四处口径一致，只给表示域尺度、当场声明不是 pattern 数，
并在 §5.4 里是唯一被拒绝入序的一条——顶住了拿硬编数字充数的诱惑。

### 2.2 修正账（五笔，引用这些数字时按修正后的写）

| 原数（回件） | 正确数 | 依据 | 影响 |
|---|---|---|---|
| HOLE 房内保留 **0.07824%**（3,600 / 4,601,025） | **0.07183%**（3,305 / 4,601,025） | 3,600 是**名义词汇量**；实现的孔洞词汇由 `enumerate_hole_poses` 给出，十族合计 `144×16 + 127×7 + 112 + 0 = 3,305`【实测】。两条独立路径同值：生产代码枚举，与「3,600 名义位置减固定家具碰撞」的手算 | 方向保守（真实价格略高于报价）。报告确实把 3,600 标了「名义词汇」，但 §5.4 头条与补丁 B 定量行读起来像实际保留率 |
| R-PORTAL 删 **8.48%** | 同一分母下（`H0` 全量 3,839,768）是 **4.59%** | 8.48% 的分母是 2,076,768（已扣 BODY+FRONT+固定+全部 66 front 之后的域），比 R-BODY 的分母小 46% | 数字本身没错、使用点也具名了；风险是读者把单元格拎出表格与 BODY 的 30.46% 直接比大小 |
| CORE「排除 7,687」个位姿 | 真正买回来的 **≤ 7,441** | 任何合法边界布局每臂必须铺满 23 个长 3 区间，`x=0` 列与 `y=0` 行除一个角格外全被占；落在其上的 123 个 core 锚点（246 个位姿）在**任何**合法布局下都是死的。按当前边界布局实算：不冲突的 core 锚点 3,721 → 7,442 个位姿【实测】 | 报告点了 `47 × 7,688` 不可相乘，但没点这条单独的高报 |
| 杆锚点删除率 **6.817%**（`RSTAR_PRICETAG_REPORT.md:178`） | **6.816%** | `265/3,888` 的 Decimal 精确值 = 6.815843621…%，三位小数舍入为 6.816%【实测】 | 纯排印笔误，0.001 个百分点，不影响任何下游数字；本文书正文用的六位值 6.815844% 本来就是对的 |
| 隐藏前提审计表**九行** | 缺一条同类条款 | 实现的杆预算是 `min(3, 区内机身台数)`（`g1_pattern_generator.py:905`），另加「有机身 ⇒ 至少 1 根区内杆」与「无机身 ⇒ 零杆」（`g1_pattern_evaluator.py:625-633`），三条一起挂在 `R-POWER-LOCAL` 这一个标签下。而 `H0` 根本没有杆预算 | 回件只给 `cap 3`（0..49 → 0..3）标价。这正是 08-04 促成补登记 `R-POLE-CAP` 的同一种「未登记的充分限制」，属于价签审计的漏项。源包 `06 §5` 已披露 `min(3, body_total)` 形态，所以是审计漏看、不是数据缺失 |

另有三条证据等级通胀，改标签即可、不动数字：

- §5.4 的跨域压缩量排序（`HOLE > BOUNDARY > BODY > …`）标【已证明】——它把矩形见证域、
  无标签几何域、实例-位姿关联域、覆盖关系域、旧 catalog 列域串进同一个 `>` 链，
  而同一节 §5.4 自己说「没有共同分母」。应降为【强论据】，或改写成「各自 proxy 内的
  压缩量分别为…（不构成跨域序）」。同一处还混用了两种方向：前两项是「保留比」、
  后面是「删除比」，整句被拎出去引用会读反。
- 「九条中没有一条整体属于纯①算术被迫」标【已证明】——支撑只有「九行里七行零余量账为空」，
  而报告自己反复承认缺完备 pattern 计数，所以只能证「无已登记零余量账支撑」。应降【强论据】。
- 九个「④ 判定实验」段的首句一律标【已证明】，但九条里只有 D8 真跑过（stdlib 脚本产出 8 个锚点）。
  设计句应改标【规格】。

**边界拆分判读正确**（这是最容易出错的一处，特此记账）：回件没把 `R-BOUNDARY-LAYOUT`
整条判成非①，而是照我方过堂表的同一条缝拆开——「必须近饱和铺满左下两边」是①算术被迫、
「边界口的具体相位」是③选择。它给的 47 更坐实了这条缝：`47 = 24² − 23²` 恰是**全部**
近饱和铺法的数目，所以被排除的 46 种也全是近饱和的，排除它们的是相位选择而非近饱和本身。
同理 `R-CORE-FRONT-RESERVE` 拆成 54（①，对应 output 52/52 零余量账）+ 12（③），也对。

### 2.3 一条 MEDIUM：随包成品不是随包脚本的产物

`03_slack_audit_table.patched.md` 被报告与 README 双双称为「补丁 C 的完整应用结果」并标
【已证明】。实测**它不是脚本输出**：在原包副本上跑随包 `apply_price_tag_patches.py` 得到
sha256 `92a12f35…`（17,913 字节），随包件是 `575cdbd8…`（17,457 字节）【实测】。
diff 显示 D 节整体是手写的另一版（标题、表头、数字格式全不同），A/B 两部分与 JSON 块一致、
**数字无出入**。随包 `validate_delivery.py` 抓不到，因为它只数 md 里的 JSON 行数、从不与脚本
输出比对。

**处置**：这是溯源问题不是数字问题。引用该文件时只能引到「人读镜像」这一级，机器可执行
结果以脚本输出为准。若要入库为我方文书，先用脚本重生成，并在 validator 里补一条
「首次输出 == 随包件」的字节断言。

补丁 B 侧则**完全过关**：脚本输出与随包 `04_derived_theorems.patched.json` 字节相同；
23 条 theorem 全保留、顶层键与顺序不变、`authority` 块逐字段相同、原有字段值零改动，
只有 9 条 `R-*` 各新增 7 个字段；幂等性经三向验证（同源二次应用、喂自产物、喂随包成品）
全部收敛到不动点，对已存在且值不同的字段硬 raise【实测】。

`authority=false` / `ledger_effect=none` 全程保持，applicator 有硬守卫。唯一缺口：
`patch_A_total_table.md` 与 `patch_D_experiment_specs.md` 两个文件内零 authority 标记，
而这两份恰恰设计成可脱包粘贴的独立件——脱包后就没有标签随行。抬头各补一行即可。

### 2.4 补丁 D 的九份实验规格：可执行，无越权施工

九份齐全（命题 / 伪代码 / 输入常量表 / 规模预算 / 三分判读五段全有），预算算术逐条验过
【复现】：D1 `56,509 = 17,952+16,896+16,900+4,761`；D2 800 s；D3 576 ≤ 600 s；D4 648 ≤ 650 s；
D9 `47 × 56,509 = 2,655,923 < 2.66M`。三分判读普遍写对了「超时未证不能推出免费」。
交付包三个 `.py` 无 `ortools` / `cp_model` / 网络 / 删除调用，只写自己目录下的两个 json，
与 README 自述「D 里的 CP-SAT 项是纸面规格、未在本批施工」一致【实测】。

---

## 三、26 号：Lagrangian 上界证书的判定实验包

### 3.1 裁定

**PASS_WITH_NOTES。数学全立，harness 在我方目标机跑通。消费任何 GO/NO-GO 读数之前有三道门**
——① 补 status 守卫（§3.3a）、② 在 HEAD/strict 语义上重打 bundle **并重跑**（§3.3b）、
③ 判 NO-GO 前断言 S1 的 15 个 branch 记录齐全（§3.4 第三条）。**三道缺一不可。**

> **08-04 夜实跑后状态**（详见 §3.5）：**①③ 已做，② 没做**。所以这一跑的 NO-GO 只能写成
> 「**08-04 修复前 g1 连通语义 + cap-3 口径下**的 NO-GO」；3388 这个上界本身仍然合法
> （旧语义更弱 ⇒ 其上界对 HEAD 依然成立），但「界压不下去」这个判据可能是假阴性。

### 3.2 数学复核到了什么程度

数学席从 §3.1 的 primal **亲手重推对偶**，与回件 §3.2 逐字一致【复现】：
`z_fp` 列给出 `π_f + Σ_b γ_b a_fpb + λ h_fp ≥ 0`，`y_bc` 列给出 `−γ_b + μ_c ≥ A_c`，
对偶目标 `Σ m_f π_f + Σ d_c μ_c + λ`。令 `w_b = −γ_b`，约束变
`w_b ≥ max_{c∈S_b}(A_c − μ_c)`；因 `γ` 不进目标且 `a_fpb ≥ 0`，**取最小合法 `w_b` 是精确消元**
——回件这条关键说法成立。`ε_f < 0` 不截 0 也正确（`π' = π + ε` 仍满足全部 pattern 对偶约束）。
CLEAN×16 塌缩成立（16 个 region 的 mask 由 `g1_region_model._build_region_classes` 硬断言同构，
C4 为零条约束，无 per-region 残余耦合）。

**阈值算术逐支重算完全吻合**【复现，独立脚本，不复用交付件模块】：`B(CLEAN, no-hole) = 142` 时
九支为 3315 / 3323×5 / **3324×2** / 3295，`max = 3324 < 3325` ✓；`143` 时
3330 / 3339×5 / **3340×2** / 3311，`max = 3340`，距 3324 差 16，确实不过线。
整张杠杆表 3388 / 3372 / 3356 / 3340 / 3324 / 3308 逐行复现，§2.2 四条充要线性条件的
常数 51 / 63 / 64 / 35 全部正确。

**cap-3 scope 纪律全程守住，找不到一处静默外推**【实测】。全文 129 / 85 的每一处会计用法都带
cap-3 标签；`analyze_protocol._best_legal_by_key` 显式排除 loose 与 `max_poles is not None` 的跑，
no-cap 主实验不可能吃到 cap-3 读数。更硬的是数据侧：`duals.json` 的 D0 分支界是 cap 无关的
算术界 144/129/130/116，我方用冻结 mask 独立重算逐格相同。**实质性检验**：把 129/85 换成
算术界 144/116 后，同样的 142 得 unified = 3330（>3324），要压到 141 才得 3315——
说明 cap-3 依赖是真的，单列它是必要而非过度谨慎。

**harness 在我方目标机实跑**【实测】：`test_deliverable.py` 10 passed（Python 3.13.13 +
ortools 9.15.6755）；非空洞性对照通过（把 bundle 目录改名后该测试真的 FAIL）；
`sha256sum -c MANIFEST.sha256` 17/17 OK。D1 CLEAN relaxed 的 pilot 断言
（cap 135 → 证明 bound 132）在我方机器上**逐位复现，连 `model_signature_sha256` 都字节相同**
（`37a5e463…`），22.0 s wall、峰值子进程 RSS 256 MB。strict 分支也端到端跑通，
`evaluator_ok` / `independent_audit_ok` / `actual_ge_credited` 三项全 true。

**无越权**：全部 `.py` 的写入都限于交付件自己目录与调用方指定的 `--out` 路径；
`subprocess` 只出现在 `run_protocol.py:92` 用显式 argv 重调自身探针（无 `shell=True`）；
跑完 `find` 全仓 30 分钟内新文件为空；bundle 携带的冻结输入与仓库字节相同【实测】。

### 3.3 两条 MEDIUM 的处置

**(a) `INFEASIBLE` 被当成 `bound = 0` 吃进分支记账——可凭空造出 GO 证书。**
`pricing_probe.py:619-628` 只判 `status != MODEL_INVALID` 就取 `best_objective_bound`。
实测 ortools 9.15.6755 对不可行的 maximize 模型返回 `best_objective_bound = 0.0`（不是 `-inf`）
【实测】。`analyze_protocol.py:184-193` 与 `_hybrid_dual_bound:150-153` 全程不读 status，
直接把这个 0.0 当分支上界吃进去。真不可行的分支吃 0 是安全的（真值 `-inf`），但**任何伪不可行**
（cap 写错、mask/模型 bug、级别过滤写错）都会把某支压到 0，unified 瞬间跌破 3324，
analyzer 直接输出 `verdict = GO_CERTIFICATE_ALREADY_REACHED`。

> **处置：跑协议之前先补一行守卫**——消费 `certified_objective_bound_scaled` 前拒绝
> `status ∈ {INFEASIBLE, MODEL_INVALID}` 的记录（只有 `UNKNOWN`/`FEASIBLE`/`OPTIMAL` 的
> best bound 才是合法上界）。这是工程 fail-open，不是数学错误；当前交付件里没有产生错误数字。

**(b) 可跑 bundle 建在被取代的旧语义上——这是我方的打包错误，不是对方的。**
我方 08-04 上午发出的 `11_runnable` bundle 用的是 **2026-08-04 修复前**的 g1 front / 连通语义；
仓库 HEAD 已在同日 `4497e05`（05:49 -0700）收敛到 strict 语义，而 bundle 归档时间戳是当日 11:28
【实测】。差异量：`g1_pattern_generator.py` 787 行、`g1_pattern_evaluator.py` 74 行、
`g1_port_semantics.py` 2 行。旧 evaluator 「从所有 live 桩同时泛洪」是严格更弱的读法
（2,593 份入册 pattern 里 855 份含多个带桩分量）。

> **处置：任何 GO/NO-GO 结论落地前，二选一**——在 HEAD 上重打 bundle 并重跑，
> 或把结论显式限定到旧语义 scope。relaxed packing 分支暴露最小（它本来就丢掉连通性），
> strict / hole 分支用 bundle evaluator 当门，暴露最大。
> 26 号 PROVENANCE 应回填这条 caveat（存档件继承同一问题）。

**跑协议前还要知道的两件小事**：`run_protocol.py` 的 1200 s 硬墙只在阶段之间检查，
已启动的阶段会跑完（最坏单阶段 `ceil(15/5) × 240 = 720 s`）；默认 `max_parallel=5 × workers=4`
= 五个 CP-SAT 同时跑，要守「一次只跑一个 solve」必须传 `--max-parallel 1`【实测】。
另：复现需要单独的提问包 bundle，它不在回件 zip 里，README 的相对路径开箱即失效；
`--bundle` 若指向仓库内路径会往 `docs/research` 树里落 `__pycache__`。

### 3.4 三条 note 级前提

- **`CORE` 不能容孔是 3324 结论的承重前提，回件没点明**。九分支记账里没有 `hole@CORE` 支；
  若 CORE 能容孔，该支为 `16×142 + 7×134 + 118 = 3328 > 3324`，142 门槛立刻失效。
  数学席从冻结 mask 独立枚举**验证该前提为真**【实测】：CORE 的 fixed = 95，
  14×14 内任何 6×7 / 7×6 矩形要么被 `x=0` 列切、要么必然压到核心块，`hole_poses = 0`；
  同法算出 CLEAN 144 / 边界 127 / CORNER 112，与回件表逐格相同。
- **「141 vs 142」两个门槛自洽但从未并排调和**。两者是不同量：§2.1 的 ε 是
  `B_CLEAN = max(无孔支, 有孔支)` 在 λ=0 记账下的下降，§2.2 的 `dC0` 只是无孔支的下降、
  且吃了九分支拆分 + cap-3。各节都带了限定语，但 `generated_tables.md` 用一个表头同时索引
  pure 3392 与 hole-aware 3388 两列，**按表头字面读是错的**。外引风险点，建议表头拆开。
- **NO-GO 五条可测、可证伪、已在代码里实现**，反证条件（任一格 drop / 任一 closure ≥ 0.10 /
  校准失败）确实都会翻掉 NO-GO，校准失败落 `INVALID_CALIBRATION_FAILED` 而非 NO_GO，方向正确。
  缺一条完整性门：`max_closure` 对 `best.values()` 取 max，若某个非 critical 任务崩了或没写进
  manifest，样本池会悄悄变小而仍判 NO_GO。建议补「S1 的 15 个 branch 记录必须全在」。

**一条口径提醒**：`01_problem_spec §5` 里「每区杆数上限 = 3」本身就是当前待判定限制档位的
四条限制之一。也就是说**要证的命题本来就是 cap-3 命题，一张 cap-3 scope 的 3324 证书正好落在
靶心上，不是降级品**。回件出于谨慎把主臂设成 no-cap 并因此给出更保守的 141 口径，
但它自己没说破这一点。

### 3.5 我方实跑（08-04 夜）：主输出 = `INVALID_CALIBRATION_FAILED`，补校准后二次重判 = `NO_GO`

工件全在 `.artifacts/w0_fixrerun_20260804/pricing_exp_run/`。**两件事必须摆在最前面**，
因为它们一起决定这个 `NO_GO` 能承多重的重：

- **主协议输出 `pricing_protocol_out/decision.json` 的字面 `verdict` 是
  `INVALID_CALIBRATION_FAILED`**（`calibration.best_bound = 91`，要求 `≤ 90`）。`NO_GO` 出现在
  **另一份**文件 `decision_with_solo_calibration.json` 里——那是我方补跑一次**无 cap 单变量校准**
  （得 85）之后合并生成的**补充二次 manifest**，`merge_supplementary_calibration.py` 明确把它标成
  补充产物并**保留主输出不动**。带 cap 的对照被 91 钉死，无 cap 的对照才是 85。
- **三道门做掉两道、第②道没做**（bundle 未在 HEAD 重打）。

所以这份 `NO_GO` 的完整写法是：**「旧 g1 语义 + cap-3 口径下、经补充校准重判得到的 scoped 二次读数」**。

| 门 | 状态 | 依据 |
|---|---|---|
| ① status 守卫（§3.3a） | **已做** | 补丁在 `patch_failopen.diff`，同时打在 `pricing_probe.py` 与 `analyze_protocol.py`：`bound_admissible = status ∈ {OPTIMAL, FEASIBLE, UNKNOWN}`，不可采时**不回落到模型 cap**（cap 本身是写进模型的约束，INFEASIBLE 可能正是 cap 造成的）【实测】 |
| ③ 15-branch 完整性（§3.4 第三条） | **已做，且实测满足** | `manifest.json` 的 `stage1_15s` 有 **15 条记录 / 15 个不同 (dual, family, hole) 组合**，一条不缺【实测】 |
| ② 在 HEAD 上重打 bundle 并重跑（§3.3b） | **没做** | 运行记录里 `runtime.bundle = .artifacts/w0_consult_packs_20260804/pricing_exp/11_runnable`，该 bundle 的三个 g1 模块与仓库 HEAD 仍差 **787 / 74 / 2 行**（与 §3.3b 记的差异量逐数相同）【实测】。**本次全部读数仍建在 2026-08-04 修复前的 g1 连通语义上** |

**②没做的后果要分两半说，别混**：

- **3388 这个数不会因此变假**。旧 evaluator 的连通读法严格更弱 ⇒ 旧语义的可行集 ⊇ HEAD
  strict 的可行集 ⇒ 在旧语义上证出来的**上界**对 HEAD 依然是合法上界。方向是保守的那一侧。
- **但 NO-GO 这个判据可能是假阴性**。NO-GO 的实质是「界压不下去」，而更松的模型本来就更难把
  界压下去。HEAD 那个更紧的模型有可能让 CP-SAT 走得更远。所以本次读数的正确写法是
  **「在 08-04 修复前 g1 连通语义 + cap-3 口径下，主输出为 `INVALID_CALIBRATION_FAILED`；
  补入无 cap 单变量校准后二次重判为 `NO_GO`」**——要把它当作**终局**关门理由，
  还欠一次 HEAD 重打 + 重跑。

**红-before-绿收据（先证补丁真的在防事）**【实测】：`failopen_regression_check.py` 造一份
「每个 D0 分支都 INFEASIBLE 且 `best_objective_bound = 0.0`」的合成 manifest，
分别喂**未打补丁的存档版**与**打过补丁的工作副本**分析器——

| 分析器 | verdict | cap-3 统一界 | `certificate_either` |
|---|---|---|---|
| 未打补丁（存档 26 号） | `GO_CERTIFICATE_ALREADY_REACHED` | **0.0** | **true** |
| 打过补丁（工作副本） | `INCOMPLETE_NO_CALIBRATION` | 3388.0 | false |

也就是说 §3.3a 那条不是纸面担忧：**未打补丁的分析器在一份全不可行的合成输入上真的发了一张
GO 证书**。`regression_check_ok = true`。

**本次读数的补丁不变式**【实测，本文书作者当场复跑】：把**真实** manifest
（`pricing_protocol_out/manifest.json`，341,924 字节，sha256 `e6ba325b…`）同时喂两个分析器，
`decision` 的 JSON **逐字节相同**（两边都是 `INVALID_CALIBRATION_FAILED`、统一界 3388.0、
`certificate_either = false`）。原因是这一跑的 `infeasible_or_model_invalid_records = 0`。
**所以补丁没有改动本次任何数字，只是把那条阴沟盖上了。**

**读数本体**（`readings_summary.json`、`pricing_protocol_out/decision*.json`）：

| 量 | 值 | 含义 |
|---|---|---|
| strict 跑总数 / 其中界发生过任何移动的 | **29 / 0** | 界一格没动【实测】 |
| `CLEAN` 无孔最好界（D0_AREA） | **146** | 阈值是 **142**，差 4 |
| cap-3 统一界 | **3388** | 证书线 3324，**距离 64**【实测】 |
| 四个 critical 任务的 closure | 全 **0.0**（门槛 < 0.10）；`critical_paired_flat = true` | NO-GO 的正条件 |
| D0 hybrid reduction | **0.0**（门槛 < 16） | 同上 |
| 全部 GO 门 | 六项全 false，`weighted_shape_hits = 0 / 需 2` | 没有一条 GO 通路 |
| 协议 wall | 680 s | 未撞 1200 s 硬墙 |

**主输出的字面值是 `INVALID_CALIBRATION_FAILED`，不是 NO_GO——重判为 `NO_GO` 的推理链如下，
逐步都要看得见（这条链的产物是补充二次 manifest，主输出保持不变）**：

1. `decision.json` 的 `calibration` 是 `best_bound = 91`、要求 `≤ 90`、`valid = false`
   ⇒ 协议按设计吐 `INVALID_CALIBRATION_FAILED`（26 号原设计里校准失败**不**判 NO_GO，
   §3.4 第三条说这个方向是对的）。
2. 我方查出这个 91 是**校准伪影**，不是模型真界：`run_protocol.py` 会把 Stage-0 的松弛界
   写回后续模型当 `objective ≤ cap`；该校准支被注入的 cap 恰好就是 **91**，于是它报的
   `best_objective_bound` 被自己的 cap 钉死在 91。
3. **单变量对照**（除 cap 外一切相同）：`cap=91` 跑满 240 s 得 `FEASIBLE`、界 91；
   把 cap 换成该支的解析界 116 后，**129.6 s 就 `OPTIMAL`、界 85**【实测，
   `readings_summary.supplementary` 两条】。85 ≤ 90 ⇒ 校准其实是**过**的。
4. 用这条补跑的校准替换后重判 ⇒ `decision_with_solo_calibration.json` 给
   `calibration.valid = true`、`verdict = **NO_GO**`。所有 GO/NO-GO 门的数字两份**完全相同**，
   变的只有校准那一格。**但这是补充二次 manifest 上的读数**：`merge_supplementary_calibration.py`
   自述「produces a clearly labeled secondary manifest … the primary manifest is left unchanged」，
   引用 `NO_GO` 时必须连「补充校准 + 旧 g1 语义 + cap-3」三个限定一起带走。

**这条 cap 写回的混淆有多大范围，已经量过**【实测】：15 个分支里 **7 个**的注入 cap 严于其
解析界；但四个 critical 任务里 **3 个**的 cap **等于**解析界（不受影响），只有
`D1 CLEAN 无孔` 是 132 vs 134，轻微。所以混淆确实存在、且**没有**吃掉 NO-GO 的主证据。

**两条操作纪律偏差，一并记账**：① 实跑用的是 `--max-parallel 5 × workers 4`，
不是 §3.3 建议的 `--max-parallel 1`（同期右线 M5 仍在跑）。这不影响任何界的**合法性**
（每个模型自己的界与并发无关），但「29 跑零界移动」这个读数在争抢下天然偏保守；
反过来说，29 跑里界从 `t=0` 起就一直贴着 cap、一格不动，纯争抢解释不了这个形状。
② 上一条已说的 bundle 未重打。

---

## 四、27 号：band22 坐标级见证

### 4.1 裁定

**PASS_WITH_NOTES；几何席零 BLOCK。** 这是迄今收到的外部回件里质量最高的一份：
三处承重洞不是话术闭合而是实证闭合，它自己新提的三个定理级数字被我方逐个复现。

### 4.2 独立复核到了什么程度

两个复核席都**不看对方自述**，直接拿包内权威 `06_problem_instance.json`
（sha256 `e08a1633…`，与我方副本字节相同）+ 见证 JSON，各写各的验证器从零复算。
两席在方法上互不重叠（一席重编译 route / 端口，一席做六谓词 + 上下界证书 + 最大空矩形），
结论一致，**零反例**：

| 检查项 | 结果 | 等级 |
|---|---|---|
| 266 个 required instance 与见证 `instance_id` 双射，template / operation 逐条相等 | 无缺、无多、无重 | 【实测】 |
| 机身 3,544 格 + 杆 100 格 = 3,644 格，两两重叠 0 | 通过 | 【实测】 |
| 1,143 个 route 格：互不重叠、全在格内、与任何机身格交集 0 | 通过 | 【实测】 |
| 1,143 个组件全部满足 §5.0 arity / 方向律（straight / turn / splitter / merger，`inputs ∩ outputs = ∅`，未用 `cross`） | 0 违例 | 【实测】 |
| 组件 kind 计数 straight 596 / turn 16 / merger 274 / splitter 257，合计 1,143 | 与自述逐项相等 | 【复现】 |
| 628 个激活端口**逐个**（非抽样）重算 front = 锚点 + 模板 body_cell + 外法向 | 628/628 命中 | 【实测】 |
| 端子-组件合同双向核对；1,143 个组件的每个申报方向都有真实对家 | 0 悬空、0 幻影源汇 | 【实测】 |
| 19 个商品连通性，两套独立算法（聚合 BFS；逐 628 端子单独 BFS） | 双双 0 失败 | 【实测】 |
| 219 台受电（合同规则 `at_least_one_body_cell_covered`），覆盖重数 174/44/1，未覆盖 0 | 219/219 | 【实测】 |
| 46 个边界口全部满足 `matching_map_boundary` 的可查读法（机身贴板边） | 通过 | 【实测】 |
| 孔 `H = [1,6]×[51,57]` = 6×7 = 42 格，与机身交集 0，短边 6 ≥ `minimum_side` | 合规 | 【实测】 |
| **`H` 就是该布局的全图最大合法空矩形**（对 3,644 机身格取补集跑最大矩形扫描） | 最优恰为 (1,51) 的 42 / min_side 6 | 【实测】 |
| 三个新定理数：56 个合法杆锚点、最大不相交装填 32、最小覆盖 25 | 三个全复现，且 56 是**集合**相等不只是计数相等 | 【实测】 |
| 25 根的最小性证书：25 台机器的合法覆盖锚点集两两不交、并集恰为全部 56 个 anchor ⇒ 任何覆盖 ≥ 25；上界 25 达到 ⇒ **该固定几何下精确最小** | 真证书，非修辞 | 【实测】 |

**三处承重洞逐一闭合**：

1. **5×5 零余量**：新带序 `(4,3,3,3,3,3,3,4,4,4,5,5,5,5)` + core `y0=36` 使核心不碰任何 5 高带；
   `13+12+11+13 = 49 = 需求 49`，余量仍为 0，但四条带的 `x` 起点逐台展开无重叠无越界——
   **零余量在有坐标见证时不再是承重假设**。
2. **杆容量**：我方原先给的是「原带序下 ≤34/36 的条件上界」，它给的 32 是**新固定几何下的
   精确上界**，二者不同构造、无冲突。
3. **core feeder**：core 改 `(60,36)` + `inputs_east_west`，两个 final 输入取 `input_W_1/W_2`，
   front `(59,37)/(59,38)` 是 band9 `x=59` riser 上两个串联 splitter，环未断；
   这 20 个 front 逐个重算命中。

**三处 owner 口头订正全部消化，而且是把条件设计掉、不是勉强满足**：band14 slit 落 `x=69`
（订正里显式的安全端点），四条 5 高带的 riser/slit 全在工作区端点，5×5 列段从不被切成两段，
`mod 5` 坏位规则**根本不进场**；净耗 20 被采用并重新推导（band12 body 行 `y=52..56` 里
`H` 只新禁 `x=3..6` → 4×5 = 20）。已撤回的旧数字（40/38/9 种/30 格/一律 25）在 §6 对照表里
一处都没复用。

### 4.3 这份见证的精确定位

这是本文书最容易被误引的一节，逐条钉死。

**它是什么**：一份 **research witness（pre-candidate 材料，research-grade）**，在**咨询合同
口径**下（`06_problem_instance.json`
的语义 + `03_rules_excerpt.md §5.0` 的合同文本）被我方两个席位从零独立复核通过，零反例。
它的目标值是 **(area 42, min_side 6)**【实测】，且这个 42 经我方实测确认就是该布局的最大空矩形
——既没虚报也没漏报。

> **08-04 夜升级两笔，两笔都有明确的适用范围，别外推**：
> ① 「咨询合同口径」这个限定词**只能对下列子结论收窄掉**——**placement（池成员 / 锚点 / 朝向）、
> 机身与 front 几何、端口精确计数、当前判据下的供电覆盖重算**：§6 的对齐探路在**官方冻结件**
> 这一侧把 291 个位姿逐个验回了池里。**整份见证的定位不变，仍是 research /
> consultation-aligned**：`band22_alignment/VERDICT.json` 的 `mode` 首行就是
> `no official checker was run`，其中端口项**只重算了精确计数、没跑 binding CP-SAT**，
> 路由项是 `NECESSARY CONDITION ONLY -- PASS`；`band22_delivery_manifest.json` 的 `scope`
> 也仍是 `Design consultation only`、`official_checker_ran = false`。
> ② §5 那次撞车（28 号一条方向相反的不可行定理撞上来碎掉）构成**机身与供电子范围的
> 第二轮独立复核**——复核席为此从零重算的是**机身、电杆与供电覆盖**，**没有**独立复核
> 1,143 个路由格、628 个激活端口、19 种商品、端口绑定或终端固定见证复验。
> **证据等级：research-grade；机身/供电子范围两轮独立复核，零反例**——不是官方 checker 结果。

**它不是什么**：

- **不是 `L`**。`L=absent` 不变。本文书不登记任何界。
- **不是官方证书**。对方从未拿到我方 official strict checker 的 CLI、序列化 schema 或 build；
  那张 JSON 是它自定义的 witness 格式（文件名里的 `not_checker_schema` 是它自标的），
  **不能直接喂我方 checker**。`band22_delivery_manifest.json` 自带 `official_checker_ran: false`。
  我方两席也没跑官方 checker（不在复核 scope 内）。
- **不是最优性声明**。对方 §9 明写不声称 42 格是全局最优、25 根是整族最小；
  25 根的最小性只对**这个固定几何**（固定机身 + 固定 1,143 格路由 + 固定孔）成立。

> **⚠ 08-04 夜起，本节下面这两个「未测点」都不再是未测点了**（§6 的对齐探路做掉了）。
> 原文保留在这里是为了留下推理轨迹，**当前有效版本以 §6.4 那张缺口表为准**：
> 差距已从「官方 checker 语义对齐 + 登记链」两道，重排成「两道官方 CP-SAT 门 +
> 终端固定见证复验 + `ghost_pick` 官方索引 + **一个还不存在的摄入 driver** + `00_charter` G3
> 的独立 strict checker 与目标值复算」。
> 「咨询合同口径」这个限定词**只对 placement 那一层**可以去掉——291 个位姿全部在**官方冻结池**里
> 唯一解析；整份见证仍是 research / consultation-aligned。

**距离「可正式登记」还差什么**（以下为 08-04 白天版本，两个未测点均已在 §6 关闭）：

1. **官方 checker 语义对齐**，其中有两个**未测点**：
   - **inactive port front 被机身占用**。**【已关闭，§6.1 C：官方语义确认合法】**
     该布局共有 **11 个未激活端口的 front 落在杆机身上**
     【实测】（core `input_W_7` 在 `(59,43)` 被 pole#18 覆盖；M186 的 `input_S_1/S_2` 在
     `(43,53)/(44,53)` 被 pole#21 覆盖；另 8 个）。`06_problem_instance.json` 对「未激活端口的
     front 可否被别的机身占据」**完全沉默**——这条既推不出也证不了，只能是假设。
     风险不是纸面的：pole#18 与 pole#21 **都在那 25 根最小覆盖里**，若官方 checker 禁止任何端口
     front 压机身，这 11 处连带 25 根**可行上界**一起要重排（下界 25 是由 56 个 anchor 的划分
     给出的，杆位变少时它不会自动变小）。
     **但要说清楚：我方这一侧不是「未定义」，而是已经定了。** `PROJECT_LOCK.md:33-39`
     的 `F-PORT-ACTIVE-BOUNDARY-01` 明写「只有实际被 operation profile / generic binding
     选中的口才必须拥有图内、未被设施本体占用且方向兼容的可布带格」，
     `specs/12_output_blueprint_schema.md:69-74` 与当前终验也只导出/复核**激活** port_specs
     （「未绑定的 pose 槽（包括贴边 pose 的出界槽）不导出」）。
     所以按当前冻结权威，「未激活口的 front 被机身占用」**是允许的**，这份见证在这一点上合规。
     沉默的是**咨询包**（`06_problem_instance.json`），不是我方权威——真正的未测点是
     「咨询包语义 = 官方语义」这一层映射。若有人主张「所有实体口的 front 都必须空」，
     那是**改动已冻结的边界，要走 freeze-ritual**，不是补一条尚未定义的规则。
   - **`matching_map_boundary` 的真实读法 + 到当前 canonical 的标签映射**。
     **【已注销，§6.1 B：模型里根本没有「资源格」概念，约束已全部烤进冻结池的 136 个位姿】**
     咨询包
     `06_problem_instance.json:76` 给 `boundary_storage_port` 的 `placement_rule` 是
     `matching_map_boundary`，而**当前冻结的 `rules/canonical_rules.json:104-113` 写的是
     `left_or_bottom_boundary`**——两个标签名不同，外发包用的是哪一版语义、它到当前
     canonical/checker 的映射是什么，**未验证**【实测：两处字面不同】。咨询包又没带边界资源图，
     可查的最强读法只是「机身贴板边」，46 个口全过；若真 checker 还要把口与**特定资源格**匹配，
     那一层完全未测。这条必须补一次「版本/资源映射核对 + checker 实跑」才算过门。
2. **W0 自己那道 lower-bound 摄入门**。这条线走的是 W0 研究线，它的登记权威写在
   `docs/research/w0_front_aware_20260803/00_charter.md:65-68`（G3 · strict witness 门：
   补齐 required instance ID、commodity binding 与 route component JSON，**直接运行独立 strict
   checker**）与 `:91-92`（双 ledger 纪律：`L=absent` 在 **G3 独立 checker 零 issue 且复算
   ≥(42,6)** 之前不登记任何值）。**这里要的是 research lower ledger 的 feasible witness，
   不是 production `CERTIFIED`**（两者的分野见 `docs/项目说明/21_glossary.md:89-105`）。

**一条必须一起说的口径**：`PROJECT_LOCK.md:329-340` 的 producer → `supervisor_seal()` →
公开发布器那条链，是**终端 `CERTIFIED` 的铸造/发布链**，管的是六谓词 **加 lex 最优性**的
production 命题；它**不是**一份普通可行见证进入 research lower ledger 的既定链路。
`PROJECT_LOCK.md:177-203` 记的 P1.2 owner gate 也**已由 owner 于 2026-07-07 手动关闭**，
不是每份见证还要再过一次的新闸。所以本文书**不把 CERTIFIED 权威链移植过来当 `L` 的入账条件**：
按现行 `00_charter` G3，`L` 的入账条件就是「独立 strict checker 零 issue + 复算 ≥ (42,6)」。

> **若日后希望给 lower bound 另设一套 seal / publish / owner 复核机制**，那是一条**新的
> 方法论候选，须单列并经 owner 认可、且要定义它自己的专用机制**，不能直接套用 CERTIFIED 链，
> 也不能把已关闭的 P1.2 写成待过的闸。本文书不代拍。

即便全部走完，(42, 6) 与在案 `U=(1188,18)`（conditional）也差着数量级，**钳口不会因此合上**
——但 `L` 会从「没有」变成「有一个数」，这是性质变化不是数量变化。

**顺带统一措辞**：为免与认证链里的正式 `candidate` 状态混用，本文书把 checker 之前的这份
材料称为 **research witness / pre-candidate 材料**；只有过了官方 checker、进入认证链之后，
才叫 candidate 材料。

### 4.4 两条 MEDIUM 的处置

**(a) `source_hashes` 块标【已证明】，4 个里只有 1 个能复现。**
声称 `problem_instance = e08a163…`（实测相等 ✓，坐实它消费的是真验收权威）、
`archive = 730d38c…`、`ask = cd7e025…`、`snapshot = 4fa2d35…`。我方对 `~/下载`、包目录、
7z 解包树的全部文件按 raw / CRLF / LF 三种形态穷举 sha256，**后三个一个都没命中**【实测】
（实测 `w0_band22_holes_20260804.7z = d0e01f6…`、`00_ASK.md = 7830195a…`）。
7z 非确定性可解释 archive，但 ask 是纯文本文件、无法这样解释。

> **处置**：转发或引用这份材料时，把这三个 hash 删掉或降级为【对方自述】。
> 决定正确性的那一个（`problem_instance`）字节相符，所以这是溯源瑕疵不是内容瑕疵。

**(b) 「inactive port front 可被机身占用」标【已证明】且披露不全。**
报告 §4.2 只对 core `input_W_7` 一处写「允许被 pole body 阻挡」并标【已证明】，
实际有 11 处。**这条是假设，不是定理**（合同沉默）。

> **处置**：在**回件里**降为【未言明语义假设】（它的合同确实沉默，它无权称已证明）。
>
> **但我方这一侧 08-04 夜已经把它验实了**（§6.1 C）：官方权威明确允许非激活口的 front 被机身
> 占用，暴露面精确到 **11 处 / 1,176 个非激活物理端口格、0 处出界、挡住者全是电杆**；
> 反过来那条**真正强制**的条件——628 个激活口 front 全部图内且无机身——**0 违例**。
> 所以它**不再是官方 checker 回合的第一顺位反例候选**，这条风险出清。

另有两条 LOW：`§0` 的「本文没有发现材料 02/05 的第五处算术错误」标【已证明】——负存在命题
不可证，应为「未发现」；`§8` 的变体 V0-B 只给聚合计数 597/15/274/257、无随包 route witness，
引用只能到「算术自洽」这一级。历史数字 991/29/84/39 全包中一次都没出现（引的是对方自己上一轮
工作记录、未随包），所以「先前是稀疏原型」只能采信其自述——但纠正后的 596/16/274/257
我方已逐格复现，**纠正的结果是硬的**。

**一条给读者的降温**：这份布局的 1,143 个 route 格构成一个巨大 SCC（1,044 格，91.3%）——
单一共享有向环。合同允许商品混流（`multi_commodity_sharing = true`，吞吐 OUT-OF-SCOPE），
所以这**合法**；但代价是谓词 5 几乎免费——实测有 5,562 对**商品不匹配**的源汇也互相可达。
「19/19 商品连通已验证」这句话在这里的物理含义比它听起来少得多，别读成「这个工厂在游戏里能跑」。

**一条已被 owner 定谳、不该再当未决点的规则层问题（记在这里防止它被误读）**：几何席额外跑了
「每台机身**全格**被某杆 stencil 覆盖」这个更严的读法，**219 台里有 109 台不通过**
（6×4 机器长 6 格，经常横跨 stencil 边界）【实测】。这个 109/219 是**反事实敏感性**，不是缺陷：
冻结合同两处明写只要求 `at_least_one_body_cell_covered`，而我方权威侧
`PROJECT_LOCK.md:91-99` 已记 **owner 2026-07-07 裁定：供电覆盖谓词 = intersection——
受电设施 footprint 与被选塔覆盖矩形 ≥1 格重叠即算覆盖，非 containment/全包含**。
所以按当前权威 219/219 全过，几何席**没有 BLOCK** 是对的
（BLOCK 等于执行一条验收合同里没有的规则）。若日后要改成全机身覆盖，那是**改冻结边界、
走 freeze-ritual**，会同时推翻这份见证与我方咨询包 §4——不是一句话可以拍板的普通待定项。

### 4.5 方法论收获：闭洞手法是改骨架，不是硬塞

三处洞它一处都没有「在原构造里挤出空间」，而是**换骨架把条件设计掉**：

- 不塞满 6 条 3 高带（**132 → 124**，把 8 台搬进 4/5 高带与 3×5 复合宏），
  硬造出我方**已证明在原构造里不可能存在**的杆位——洞②因此不是被满足，是不再存在。
- 核心搬离所有 5 高带、孔搬到偶数号 band12 的天然 riser 上，把两条 `mod 5` 必要条件从
  「必须满足」变成「不进场」。

我方前两轮审计给的是「这些洞在原构造下堵不上」的条件上界，全部成立；但它们只约束**那个骨架**。
**本案的写作纪律**：对这类构造线，「在固定骨架下证明某容量不可达」和「该路线走不通」是两件事，
文书里必须分开写。

> **本节的 authority 边界**：以上只是**本案的写作纪律**，不是已生效的跨任务通则。
> 若要把它推广成 SOP，须与 §5.7 那条一样、经 owner 认可后另行写回方法论文档
> （`02_methodology_0b.md`）；在那之前不得当成已生效规则引用。

---

## 五、28 号：同题第二份独立答卷——那条「不可行定理」被 27 号当场证伪

### 5.1 这份是什么、不是什么

**不是 27 号的修订版，也不是见证。** 同一份 v1 送包（sha256 `730d38c7…`）被意外发出第二次，
由**另一个独立 GPT Pro 会话**作答；两个会话同题、同材料、互不可见，是**独立样本**。
它的形态是「部分方案 + 一条不可行定理」：洞①/洞③ 给坐标级局部补丁，洞② 走「交不可修证明」
那条路。**全包没有一根电杆坐标、没有 266 实例逐台清单、没有 route component 导出、
没有端口绑定、没有 19 商品连通检查**；它自己的 README 与 §0 都明说「本包不是完整可行 witness」，
自带校验器也自己打印 `NOT CHECKED: power coverage, operation binding, full strict connectivity`【实测】。

### 5.2 裁定：局部补丁 PASS，核心定理 **REFUTED**

**通过的部分**【实测，撞车复核席从零重算，无一处偏差】：带高序
`(5,3,4,3,5,3,4,3,5,3,5,3,4,4)` 与 15 条走廊 `y=(1,7,11,16,20,26,30,35,39,45,49,55,59,64,69)`
自洽（`54+15=69`）；台数 `3×3=132 / 5×5=49 / 6×4=38` 精确命中；全部机身两两不重叠、
不进 core、不进孔、不压 riser；14 条自由列区间与 38 个乐观 2×2 槽逐条对上；
20 个 core front 用权威模板重算逐个吻合（E 侧 7 个在 `x=70` 出界，按权威规则未绑定口可出界，
合法）；`4900−3544−1157=199 → P≤49` 与带内账 `3672−54−72−25−3325=196 → P≤49` 都复算一致。
随包两个脚本实跑退出码 0。

**被推翻的部分**：回件 §5.2 那条「判死全部**相邻 4+4 直连**宏族（模型 A 与 B 均含）」的
不可行定理**是错的**。**推翻它的是直接坐标反例，不是不等式账**：

> **27 号完整落在 28 号自己申明的适用范围内，却真的存在，而且它那 25 根杆实测覆盖 219/219 台**
> 【实测，复核席从零重算 27 号原始 JSON、不采信它自带的 validation 块】。
> 28 号的结论是「在上述范围内不存在满足 219 台供电覆盖的电杆布局」，这份坐标见证直接把它推翻。

**这里必须记一笔反向订正（原文的证伪收据不能承重，已删）**：本文书上一版引用的
`Q=20 / R=62 / 3Q=60 / 边界 59`、以及「反证脚本退出码 0、末行 `REFUTATION ESTABLISHED`」这条收据
**不成立，不得再引用**。原因是脚本与 28 号的定义不一致：

- 28 号原报告（`w0_band22_holes_consultation_response.md:245-295`）把 `f_i` 定义为**整条带的
  全高自由列总数**、`q_i = floor(f_i/2)`，并明写「这把不连续自由段合并了，所以只会高估」；
  而反证脚本 `refute_28_power_theorem.py:119` 是**对每个连续自由段分别取 floor 再求和**，
  必然 ≤ 原定义值。
- 脚本的 `r_i` 对混合宽度带做「逐宽度 `ceil` 再相加」，这不是 28 号的 `r_i := ceil(m_i/K_w)`；
  而且逐宽度 `ceil` 还可能把同一根能服务多种宽度的杆重复计价。

**按 28 号原定义、从 27 号原始见证重算**【实测，本文书作者当场复跑】：
`q = [0,5,2,2,2,2,2,2,0,3,1,0,0,1]`、**`Q=22`、`R=62`、`3Q=66`、边界修正版 65**——
`R ≤ 3Q` **成立**。也就是说这条全局不等式**没有**被 27 号击穿，用它当证伪收据是错的。

**能保留的干净局部反例只有一条**：**band14 是纯 5×5 带**（13 台宽 5 机器，`r₁₄ = ceil(13/4) = 4`），
而 `q₁₃ + q₁₄ = 0 + 1 = 1 < 4`——两种 `q` 口径下都成立，且 `r` 与 28 号定义一致。
band8 / band12 / band13 都是**混合宽度带**，它们的 `r_i` 在 28 号的定义里没有定理支撑，
**不能算作局部反例**（上一版写的「三个局部带均 `lhs=1` / `rhs=4`」已删）。

复核席逐项核过 27 号确实满足 28 号申明的全部前提：高度多重集 `6×3 / 4×4 / 4×5`；
9×9 核心 `(60,36)` 横跨 band9(4 高) + c9 + band10(4 高)；孔 `[1,6]×[51,57]` 落在 5 高带 band12、
`a=1` 且 riser 在孔内（= 28 号脚本里的 `B_a1` 分支，净耗 20）；每带一条全高 riser；
走廊行全留 transport；**25 根杆全部完整落在制造带内**（逐根验过 host band 唯一）。

### 5.3 两处暗桩：定理申明的范围比它实际证明的范围宽

| # | 暗桩 | 在哪 | 27 号怎么绕过去的 |
|---|---|---|---|
| 1 | **模板—带高纯度**：证明把「132 台 3×3 全部装在六条 3 高带里、每条恰 22 台」当成推论，实际是**未申报的前提** | `verify_w0_direct4_power_obstruction.py:59` 硬编码 `Q3, R3 = 0, 6 * ceil(22 / K[3])`，即六条 3 高带的 `q` 恒为 0 | 27 号把 8 台 3×3 挪进 4 高带（band8/9/10 各 1）与 5 高带（band12 1、band13 4），六条 3 高带只装 `19 + 21×5 = 124` 台，于是出现大量自由列：按 28 号原定义实测 `q₂=5`、`q₃=q₄=q₅=q₆=q₇=q₈=2`【实测】——**不是 0** |
| 2 | **`f_i` 只数「全高自由列」**：3×3 装进 5 高带会在同列留下 2 行空隙，2×2 电杆装得进去，但这类杆位在 28 号的 `f_i` 口径里**完全不可见** | 同一个 `f_i` 定义 | 27 号有 5 根杆（`pole_21@(43,52)` 在 band12；`pole_22@(8,61)`、`pole_23@(21,61)`、`pole_24@(53,61)`、`pole_25@(61,61)` 在 band13）就坐在 3×3 复合宏上下留出的两行里。这两条带按 28 号口径 `q=0`，实际却是覆盖 band12/13/14 的主力——正是 **band14（纯 5×5 带，`q₁₃+q₁₄ = 1` vs `r₁₄ = 4`）** 的局部必要条件被击穿却依然全覆盖的机制【实测】（band12/13 是混合宽度带，其 `r_i` 在 28 号定义下无定理支撑，不列为反例） |

**顺带一条 HIGH**：28 号报告自己的 §5.1「判死原首选参数」挂在同一个纯度前提上（链条是「band2 是 3 高、
装满 22 台只剩 1 列 → 放不下杆 → band3 的杆够不到 band1 → band1 至少 9 台无电」），
一旦允许把 3×3 挪出 3 高带，第一环就断——27 号的 band2 只装 19 台、**实测自由列 10 列、
按 28 号原定义 `q₂ = 5`**，band2 里的 `pole_01..04` 正是覆盖 band1 的杆。
**证据等级应从【已证明】降到【条件成立】。**

**最小修法（定理不必整条丢掉）**：① 把 §5.2 的范围行从「各带具体装机分配均任意」改成
「每台设施只装进与其高度相等的带」，结论降级为「**纯装家族**不可行」——这样与 27 号不冲突；
或 ② 把 `q_i` 从 `floor(f_i/2)`（全高自由列）改成「该带内所有合法 2×2 anchor 的横向覆盖并集
能碰到的机身数」并重推 `M_w` 引理。按 27 号实测，改完后 3 高带的 `q` 会从 0 变成 2~5、
band12/13 会从 0 变成正数，不等式大概率不再普遍击穿。

### 5.4 判定分两级（这条边界必须随结论一起走）

- **被确定性证伪的是 28 号 §5.2 的结论本身**（「这个宏族里不存在能覆盖 219 台的杆布局」），
  证伪物是 27 号那份坐标见证的 25 根杆实测覆盖 219/219，且**不依赖** 27 号的路由层是否成立——
  复核席只复核了「机身 + 电杆 + 供电覆盖」这一层，而这一层已经把结论打穿。
  **注意区分**：被打穿的是**结论**；那条全局必要不等式 `R ≤ 3Q` 按 28 号自己的 `q_i` 定义
  在 27 号身上**仍然成立**（`Q=22`、`R=62`），**不能**说它被证伪。定义一致的局部反例只有 band14 一条。
- **「该宏族不存在完整见证」这个更大的结论现在是「失去支撑但未被反证」**——要彻底反证它，
  还需要 27 号的路由/端口/连通层被独立复核通过（§6 的对齐探路做掉了其中一部分，
  但两道官方 CP-SAT 门仍未跑）。

### 5.5 这构成对 27 号几何**机身与供电子范围**的第二轮独立复核

值得单独记一笔：28 号不是「再看一遍」，是一份**独立产出、方向相反**的定理撞上 27 号见证后碎掉。
**但要把范围写准**：这一轮复核席从零重算的是 **219 台机身、100 格杆、供电覆盖关系**，
**没有**独立复核 1,143 个路由格、628 个激活端口、19 种商品、端口绑定或终端固定见证复验。
第一轮（§4.2）是我方两个复核席从零重算六谓词（咨询合同口径）。
**证据等级：research-grade；机身/供电子范围两轮独立复核，零反例。** 仍然不是官方 checker 结果。

### 5.6 撞车分析：重合八项（多数是共同输入继承）、显式几何分歧七项 + 一项隐藏前提分歧

现行读法「重合 → 被迫结构候选（弱证据）；分歧 → 自由区**（但有例外，见 §5.7）**」有一个前提：
重合必须是**独立会聚**的。本次**这个前提大面积不成立**——共同输入
（`.artifacts/w0_consult_packs_20260804/band22_holes/00_ASK.md:4-7,19-24`
与 `01_construction_verbatim.md:50-90,110-126,143-159`）已经把大半个骨架直接写死了。
所以下面**逐项标出重合的来源**，**只有「独立推导」那一类才可以计入会聚证据**：

**重合（8 项）**【实测】：

| # | 重合项 | 来源分类 | 依据 |
|---|---|---|---|
| 1 | 高度多重集 `6×3 / 4×4 / 4×5` | **ASK 强制** | `00_ASK.md` 明列 |
| 2 | 14 带 + 15 条单格走廊、`54+15=69`、`y=0` 整行让给边界口 | **ASK 强制 / 材料诱导** | `00_ASK.md:19-24`「14 条带 + 15 条单格走廊…这四条保留」；`01:50-58,66-90` 给出边界布局与 `54+15=69` |
| 3 | 奇偶交替极性 + 蛇形单有向环 + `x=1` 西侧返回干线 + 环在 `(1,1)` 闭合 | **ASK 强制 / 材料诱导** | `00_ASK.md:19-24`（交替极性、单一有向环保留）；`01:88-90,110-126` 明写奇偶极性、蛇形环、`x=1` 返回干线 |
| 4 | riser 奇带 `x=69` / 偶带 `x=2` 的交替钉法 | **材料诱导（待再分辨）** | `01:98-108` 已规定每带一条 1 格宽 riser、工作区 `x=2…69`；具体交替相位未明写，需逐份比对才能确认是否独立 |
| 5 | 核心一律 `inputs_east_west`、一律贴东侧、一律选 `4+1+4` 跨带 | **待分辨（材料给的是 `inputs_north_south`）** | `01:143-153` 的首选核心宏是 `(60,60)` + `inputs_north_south`；两份都改成东西向，这项**可能**是独立推导，但需排除是否由 ASK 的走廊极性账逼出 |
| 6 | 孔一律选 `a=1` 西端、6 宽 7 高 42 格、复用 `x=1` 干线 + `x=2` riser 压净耗 | **材料诱导** | `00_ASK.md:39-49` 显式突出「`a=1` 合法」「`a=1` 是 20 格（比 25 便宜）」以及 `x=1`/`x=2` 机制 |
| 7 | 6×4 排 4 高带、5×5 排 5 高带、`x` 起点从 `x=2` 起按模板宽度等距铺 | **大部分材料诱导** | `01:98-108` 已给工作区 `x=2…69` 与 `floor(67/w)` 的等距铺法 |
| 8 | `c14` 不进主环、当顶部 source feeder 汇回环里 | **材料诱导** | `01:128-141`「顶层 `c14` 是 source-only 馈线，不进入环本体」 |

**结论**：项 1 是 ASK 强制，项 2、3、6、8 与项 7 的大部分是**共同输入继承**，
**都不得计入独立会聚证据**；项 4、5 要先做「材料诱导 vs 独立推导」的分辨才能定性。
**上一版据此得出的「`a=1` 是本次撞车里最强的被迫结构候选」这条推断已删**——`a=1` 正是
`00_ASK.md` 亲手推荐的那一个。

**显式几何分歧（7 项）**【实测】：带序（`(4,3,3,3,3,3,3,4,4,4,5,5,5,5)` vs
`(5,3,4,3,5,3,4,3,5,3,5,3,4,4)`）；走廊 `y`（带序的导出量）；核心落位（中部 band9+band10
`(60,36)` vs 顶部 band13+band14 `(61,60)`）；孔位（band12 `[1,6]×[51,57]` vs band9
`[1,6]×[39,45]`）；孔模型（riser 在孔内 = 模型 B 净耗 20 vs 模型 A 净耗 25）；
核心邻带竖连手法（band9 riser 绕到 `x=59` vs b13 riser `x=60` + b14 slit `x=58`）；
杆层（25 根全落地 vs 宣告无解）。

**外加一项隐藏前提分歧**：**模板纯度**（28 号把「3×3 只能进 3 高带」内化成前提，
27 号把它当自由度用掉）——见 §5.7。所以本次撞车的分歧维度合计是
**七项显式几何分歧 + 一项隐藏前提分歧 = 八个分歧维度**；上一版只写「七维分歧」却又在
§5.7 另列模板纯度，两处对不上，已订正。

**三条条件榫眼的预言行为如 ASK 所述**【实测】：「带组成唯一」两份都遵守（但 ASK 已给出，
撞车不构成独立佐证）；「原带序下 `y0∈{58,59,60,61}`」——两份都换了带序、前提失效，
27 号照 ASK 纪律对新带序重新枚举得 `y0∈[30,61]` 并申明 36 在其中，28 号没做这次重枚举
（其 `y0=60` 恰好也在旧区间里，纯属巧合，复核席替它验过事实上合规，属**程序瑕疵**）；
`mod 5` 榫眼两份都靠选 `a=1`（端点值，天然免 residue）绕开——**但 `a=1` 是 `00_ASK.md:39-49`
显式推荐的值，所以这属材料诱导，不构成被迫结构证据。**

### 5.7 方法论修正候选（**标 owner 待认可，尚未写回 SOP**）

撞车诊断现行读法是「重合 → 被迫结构候选；分歧 → 自由区」。本次暴露一条例外：

> **当一方在某个分歧维度上宣告不可行时，该分歧不是自由区，而是前提冲突——
> 其中一方把自己的选择当成了被迫结构。**

模板纯度这一维就是活例：28 号把「3×3 只能进 3 高带」内化成前提并据此推出不可行定理，
27 号则把它当自由度用掉、直接产出可行解。按现行默认读法，这一维会被归进「自由区」，
而它恰恰是可行/不可行的分水岭。

**建议的 SOP 改法**（写回 `.artifacts/w0_consult_packs_20260804/rstar_pricetag/02_methodology_0b.md`
的撞车诊断段）：撞车 → 被迫结构候选（弱证据，还要**逐项排除「ASK 强制」与「材料诱导」两类
伪撞车**——本次八项重合里至少五项属这两类，见 §5.6）；
分歧 → 先分类为「真自由度」还是「一方的暗桩」，**判据 = 有没有一方在该维度上给出不可行断言**。

**状态：候选，待 owner 认可后才写进方法论。** 本文书不代拍。

### 5.8 一条溯源事实：订正侧信道未受控，两边收到的条目不一样

**这里先记一条记录冲突，别再把它写成已定事实**：`27_PROVENANCE.md` 内部自相矛盾——
第 10 行写「owner 在送出 v1 后**另贴便条给对方**：①band14 slit ≤11 ②模型 B 中 `a=1`
③孔洞净耗 20」，第 40 行却写「对方看到的是 **v1 材料**，不含上面列出的三处 owner 订正」；
而本文书 §4.2 又实测 27 号的成品把这三处订正**全部消化掉了**。
**所以「27 号未见 owner 订正」现有记录不足以确认**，上一版那句断言已改。
可以确定的只有：**订正是走口头侧信道发的、未受控，两个会话拿到的条目不相同**
（28 号 journal 的撞车复核记录也明写两边条目不同）。

28 号这份明确**看到了**订正——报告 §1 标题就是「三处勘误的吸收结果」，逐条列出采用值【实测】。
两边第三条的内容不同：

| # | 28 号吸收的订正 | 27_PROVENANCE 记的第三条 |
|---|---|---|
| ① | band14 的 5×5 slit 容量 `≤11` | 同 |
| ② | 模型 B 孔洞净耗：`a=1` 为 20，`2≤a≤64` 为 25 | 记作「`a=1`」与「孔洞净耗 20 不是 35」两条 |
| ③ | 四条 4 高带分配数 = **35 个标号解、归并 11 类，不是 9 种** | **27_PROVENANCE 未记这一条**（它记的是另外两条；且该文件对「27 号有没有收到便条」自相矛盾，见上） |

**处置**：引用任何一份的前提数字之前，先核对它实际吸收的是哪一版订正。三处订正 28 号
一个都没踩（复核席逐条验过：`cap14(s)` 枚举 36 个位置 =11、22 个 =10，坏位正是
`s mod 5 ∈ {0,1}`，与回件逐字一致；模型 B 分段宽度形态正确、没把 20 外推；
35 标号解那条回件明说「本文的覆盖判死不依赖这项枚举」并对自己的新带宽重新枚举，口径分得清）。

### 5.9 一条 HIGH：证据等级病的外脑版 again

被证伪的那条结论在 28 号报告里顶着**【已证明】**标签，出现在
`w0_band22_holes_consultation_response.md` 第 9 行（§0 结论先行）、339 行（§7 风险表
「杆位与 219 台覆盖同时满足 / 该直连宏族不可行 / 【已证明】」）、343、347
（「D0 口径下这八项都会被 §5 判死，继续枚举是无谓消耗」）、399 行。

它是全文的**承重结论**，而且**直接在指挥下一轮方向**——若照单全收，会把一条已经拿到坐标级
见证的方向当死路砍掉。这与记忆卡 `referee-authored-docs-blind-spot` 是同一模式的又一次复现，
只是这次发生在外脑侧。

> **处置**：**28 号 §5/§8/§10 的全部方向性指挥作废**——别按它去砍 D0 变体，也别急着上
> 「corridor 绕杆」放松，那条指挥建立在被证伪的定理上。若要给对方回信，最有价值的一句是：
> 请把「每台设施只装进与其高度相等的带」显式写进定理范围，并把 `f_i` 从全高自由列改成真实
> 2×2 anchor 可行集；改完之后这条不等式还剩多少杀伤力，是个值得算的问题。

---

## 六、对齐探路：band22 见证 → **官方仓库语义**（我方实跑，08-04 夜）

工件全在 `.artifacts/w0_fixrerun_20260804/band22_alignment/`。**只读探路，没跑官方 checker，
没做任何 git 操作。** 下面每个 PASS 都是「从冻结工件 + 源码重新推导」，**不是门执行**。

### 6.1 最重要的一件事：口径折扣被拆掉了大半

昨天这份见证的定位是「**咨询合同口径**下被复核通过」，§4.3 与 §7.1 的结论都挂着
「咨询合同语义 = 官方语义」这个条件。这次探路直接从**官方冻结件**这一侧验：

**A. 池成员：291 / 291 唯一解析**【实测】。方法不是猜名字——每台设施先按 `(template, anchor)`
在 `candidate_placements.json`（54,467,709 字节，pinned sha 命中）里取候选位姿，
再用「该位姿申报的物理端口格是否包含见证的激活口 `(front, dir, kind)` 三元组」筛，
**291 台每台都收敛到恰好一个池位姿，`ambiguous_matches = 0`**。分项：
边界口 46/46、core 1/1、制造机 219/219、电杆 25/25。交叉校验：由池位姿反推的机身格
**3,644** = 见证自报的 3,644；`instance_id` 集合 == `mandatory_exact_instances.json` 的 266 台。

> 这条的分量：见证里的每一个摆法**本来就在官方冻结候选池里**。锚点口径（两侧都用 SW 最小角）
> 与 front 口径（两侧都是 identity——存的坐标**就是**机身外第一格，07-18 事故修复后的语义）
> 逐项相同，**零偏移换算**。

**B. `matching_map_boundary` 那个未测点：前提根本不存在**【实测】。
`canonical_rules.json` 里 `boundary_storage_port.placement_rule = left_or_bottom_boundary`
是**唯一**的摆放约束；`canonical_rules.json`、`preprocess_plan.json`、generator 与任何
certified 路径源码里**都没有「资源格 / 矿点」这个概念**。而且这条约束**已经被烤进冻结池**：
`boundary_storage_port` 恰好 136 个位姿——68 个 `left_base`（锚点 `x==0`）+ 68 个 `bottom_base`
（锚点 `y==0`），其余位置**不可表示**。生成器 `src/placement/placement_generator.py:412-438`
是纯几何。**所以 A 过 ⇒ B 过**：46 个边界口既然都在池里，它们按构造就在左/下基线上。

> 附带口径：边界口**能出什么货**不是格子属性——`boundary_io` 是 generic operation
> （`preprocess_plan.utility_operations`，0 进 1 出），商品由 binding 模型从
> `generic_io_requirements.json` 选；只有 `source_ore` 与 `blue_iron_ore` 带
> `source_kind='external_boundary'`，而见证用的正是这两个。
>
> **这条直接把旧 §8 处置清单 #12 与拍板页 §6.1 第 1 条注销掉了**：不是「映射核对通过」，
> 是「要核对的那个东西不存在」。

**C. 非激活口 front 被机身占用：官方语义下合法，27 号那条假设是对的**【实测】。
证据链九条：`PROJECT_LOCK.md:156-167`（B-4：`machine_min_clearance_cells` **不是** certified
谓词，它管的是「**使用中**端口的 front 须可放带」，机身之间没有间隙要求）、
`PROJECT_LOCK.md:33-39`（`F-PORT-ACTIVE-BOUNDARY-01`：只有被 operation profile / generic
binding 实际选中的口才必须有图内、未被本体占用、方向兼容的可布带格；core 与 box 的未激活
实体口**甚至可以越界**）、`canonical_rules.json` 的两条 semantics（`machine_min_clearance`
只约束 in-use 口；`protocol_storage_box_wireless` 明写未连接的输出口不占其 front 格），
外加**五处源码位、三个模块**：`src/models/port_binding.py:212-266`（绑定模式只选位姿物理口的
**子集**，未选中的格永不进 `active_ports`）、`src/models/binding_subproblem.py:962-984`
（front-free 过滤只遍历 pattern 的 input/output 口）、`:1131-1154` 与 `:1201-1225`
（front 被挡的 generic slot 是**静默 skip**、不是错误）、`:1453-1494`（`extract_port_specs`
只导出已绑定口）、`src/search/pr2_l0_fixed_witness_core.py:1785-1830`
（终端发布边界的 backstop `_connector_body_exclusion_violation` 只遍历那些激活 `port_specs`，
`:1815-1816` 对出界格甚至是 `continue` 而不是拒绝）。

**暴露面量化**：见证共 **1,176 个非激活物理端口格**，其中 **11 个** front 被机身挡住、
**0 个**出界，**挡住者全是电杆**（`protocol_core_001@(59,43)`←pole_18；`refinery_steel_013`
的 `(43,53)/(44,53)`←pole_21；另 8 个分属 `refinery_steel_014..017`←pole_22..25）。
**这不是修复清单，是这条假设的暴露面，全部合法。**
反过来那条**真正强制**的条件：**628 个激活口 front 全部图内且无机身，0 违例**。

### 6.2 顺手过掉的官方谓词（五个直接过，第五个过了必要条件）

| 谓词 | 结果 | 口径 |
|---|---|---|
| ① ghost 内无设施 | 孔 `x1..6 × y51..57` = 6×7，机身格 0，短边 6 = admissibility 门槛 6 | **PASS** |
| ② 两两不重叠 | 重叠格 0、出界格 0 | **PASS** |
| ③ placement_rule | 由 A 蕴含——每个位姿都出自冻结池，而池由 `_validate_template_geometry_contract` 把关 | **PASS** |
| ④ 端口精确计数 | generic 输出 `blue_iron_ore 34 / source_ore 18` 需求与见证逐项相等；generic 输入 `qiaoyu_capsule 1 / valley_battery 1` 相等；**219/219 台逐台配方 profile 精确吻合**（槽数 = `ceil(amount / ticks_per_cycle / belt_capacity_per_tick)`） | **计数恒等式 PASS；CP-SAT binding 门本身没跑** |
| ⑤ 路由连通 | 自由格 1,256、自由分量 **1**、19 商品 0 失败、route 格压机身 0 | **只做了无向自由格松弛这个必要条件** |
| ⑥ 供电覆盖 | 219 受电、25 杆、未覆盖 0；判据取自 `exact_coordinate_master._add_power_coverage_axis_delta_constraints:6206-6227`，是**区间相交**测试、**不是**全机身包含 | **PASS** |

**⑤ 那条的边界要写清**：certified 门是 `routing_subproblem` 的 CP-SAT **加上**
`_validate_selected_route_connectivity`（有向分量、splitter/merger 度数、cross 交叉规则），
本次探路**替代不了**它。

**ghost 与带子的关系顺带钉死了一条**【实测】：谓词①是
`all_cells(π) ∩ R = ∅`，`all_cells` **只含设施占用格**；`benders_loop.py:8086-8089` 明写
「`ghost_pick` 是空矩形标记，不是占用者」，`routing_subproblem.RoutingPlacementCore.from_occupied_cells:54-88`
构造 `free_cells` 时是「网格减机身」、**从不减 ghost 格**。所以见证那个 6×7 孔里穿过 22 个带格
**合法**。据此重算：在这份摆法的 body-free 网格上，满足 `min_side ≥ 6` 的最大空矩形
**恰好就是 `x1..6 × y51..57`，面积 42 / 短边 6**——与见证自报一致，**这份见证对它自己的摆法是紧的**。
（不管 admissibility 的最大矩形是 1×69 条带，面积 69、短边 1，不可采。）

### 6.3 半成品在手：`registration_placement_solution.json`

探路顺手吐了一份**官方形状**的 `{instance_id: {facility_type, pose_idx, anchor}}`，
覆盖全部 291 台，**`ghost_pick` 故意留空**（它的 `pose_idx` 必须由
`_expected_unfiltered_ghost_anchor_index(grid, ghost_w, ghost_h, anchor_x, anchor_y)` 产出，
不能自己编）。

> **警告随件**：里面的 `pose_idx` 是**当前**这份冻结 `candidate_placements.json` 的列表下标，
> 对任何其他 pool build **无意义**。

### 6.4 剩余差距（这是 §8 处置清单的新版本）

| # | 缺口 | 为什么 | 在哪做 |
|---|---|---|---|
| 1 | **`binding_subproblem` CP-SAT 门** | 计数恒等式是手算过的，真门（每实例在其完整枚举域上 `AddExactlyOne`、core 14 槽的 `__unused__` 哨兵处理、storage-box dominance）没跑 | `src/models/binding_subproblem.py` 的 `PortBindingModel`，喂固定 placement_solution |
| 2 | **`routing_subproblem` CP-SAT 门 + `_validate_selected_route_connectivity`** | 只验了无向必要条件。**注意**：见证自带的 1,143 个组件是它的私有 schema，**不是**官方门吃的东西——官方门是**自己解**路由的，所以那套设计不需要翻译，但可行性必须由求解器证出来 | `src/models/routing_subproblem.py` |
| 3 | **终端固定见证复验** | `pr2_l0_fixed_witness_core.run_fixed_witness`（connector body exclusion、routing precheck、隔离 `-I -S -B` 子进程）是发布边界权威，从未在这份见证上跑过 | `src/search/pr2_l0_fixed_witness_core.py` |
| 4 | **`ghost_pick` 的官方索引** | 见 §6.3，必须由官方函数产出 | `pr2_l0_fixed_witness_core.py:1739-1748` |
| 5 | **摄入 driver 根本不存在——要新写** | `scripts/` 与 `main.py` 里**没有任何东西**能吃一份外部 witness JSON 再对它跑门。固定见证复验器是从 search 链内部的 campaign 状态驱动的；`main.py` 没有 `--solution-hint`（`solution_hint_parser` 是内部 helper，而且 hint 是搜索引导、**永远不能当证据**）。**登记这份见证需要新写一个 driver 脚本** | 新工程 |
| 6 | **结构性天花板** | production `CERTIFIED` 证的是六谓词**加 lex 最优性**。一份可行见证**无论过多少道门都不可能自己铸出 CERTIFIED**。它能变成的最好东西是一个 **research lower-ledger 的 feasible witness，即 `L = (42, 6)`**（入账条件见 `00_charter` G3）。对着 `U=(1188,18)`（conditional），钳口照样敞着 | 不在任何「见证登记」工作的范围内 |

### 6.5 诚实声明（探路自带，逐条保留）

- **没跑官方 checker。** 上面每个 PASS 都是从冻结工件 + 读源码重新推导出来的，不是门执行。
- `registration_placement_solution.json` 的 `pose_idx` 只对当前池 build 有意义。
- 探路席当时判定 `zmd-pj/.venv` **已坏**（解释器 symlink 指向已被删除的
  `~/interpreter_ab/py31313-pgo/bin/python3.13`），系统 `python3` 无 `jsonschema` / `ortools`，
  所以它没 import `operation_profiles.py`、改为内联重推槽公式。
  **本文书作者当场复核并订正这条**【实测】：`.venv` 确实坏了，但**本机有可用解释器**——
  `/home/zhuran24/zmd-pj-codex/.venv-uvbolt-backup/bin/python3`（Python 3.13.13 +
  ortools 9.15.6755）能 import `cp_model`，也能在 `zmd-pj` 根目录下 import
  `src.preprocess.operation_profiles`。同一晚的 pricing 协议就是用这个解释器跑的 CP-SAT。
  **所以「跑不了官方 CP-SAT 门」不是环境问题，是 §6.4 第 5 条那个 driver 不存在。**
- 探路没有判断这份设计**好不好**，只判断它在官方口径下**表达得出来、合不合法**。

---

## 七、四包 + 两份读数合起来改变了什么

### 7.1 「G1 拼不满」被从「问题拼不满」里剥离出来了

G1 在 08-04 的止损读数是：catalog 机身面积供给的**上界** **2,544**【实测】，需求 **3,325**【算术】，
因此相对该上界的短缺**至少 781** 格【算术】（见 `STOPLOSS_CHECKPOINT_20260804.md` §2）。

> **这个 2,544 必须写成上界、不能写成「能提供的面积」**：它的原始字段是
> `.artifacts/w0_fixrerun_20260804/regen/gate_union/master/pre_gate.json` 的
> `catalog_class_supply.classes[class=__body_area__].supply_ceiling`，同文件的 `reading` 字段
> 自己写明「takes the best pattern per region class **independently, so it over-counts**」——
> 各 region class 独立取最佳 pattern 后相加，**会重复计价**。`g1_exact_cover_master.py:820-865,918-955`
> 产出的也是这个算术 supply ceiling，不是能同时实现的精确供给。所以真实供给 ≤ 2,544、
> 短缺 ≥ 781；**2,544 不是精确供给，781 也不是精确缺口**。

当时能说的只有「本 catalog 在本次预算下拼不满」，不能说「几何上拼不满」。

band22 见证给出的是一份**把 3,325 格全部摆下**的坐标级布局：266 个机身共 3,544 格，
减去 46 个边界口的 138 格与 core 的 81 格，恰好 `3,544 − 138 − 81 = 3,325`【算术，两端都是实测数】。

**band22 见证摆得下这 3,325 格，而它同时违反 G1 登记的多条 `R-*`。** 我方对这份见证跑了一次
**单向必要条件排除检查**【实测，脚本与收据见 §9】：把 band22 见证按 G1 的房间划分
（`T[i,j] = [14i,14i+13] × [14j,14j+13]`，`g1_region_model.py:5`）逐格重算——

| 项 | 数 | 命中哪条登记限制 |
|---|---|---|
| 跨房间边界的**决策机身**（219 台制造机） | **78 / 219**（6×4 十八台、3×3 三十八台、5×5 二十二台） | `R-BODY-IN-REGION` |
| 跨房间边界的 **fixed furniture**（46 边界口 + 1 core） | 7 / 47（边界口 6、core 1） | 属 `R-BOUNDARY-LAYOUT`，**不是** `R-BODY` 的反例 |
| 激活 front 落在其机身锚点房间之外 | 84 / 628（决策 78、fixed 6） | `R-FRONT-IN-REGION` |
| 其中**机身整体在单房、只有 front 出房**的 | **19 个 front / 涉及 18 台制造机** | `R-FRONT-IN-REGION` 的**独立**反例（不依赖 R-BODY） |
| 孔 `H = [1,6]×[51,57]` 是否跨房间 | **是**（`y` 跨 room 3/4 分界） | `R-HOLE-IN-REGION` |
| 见证边界口锚点与 G1 pinned fixed furniture 重合 | 23 / 46；core 在 `(60,36)` 而 G1 钉死 `(3,59)` | `R-BOUNDARY-LAYOUT` |
| 跨房间边界的电杆 | 2 / 25（`pole_15@(41,31)`、`pole_16@(23,41)`） | （杆不受 `R-BODY` 管，单列） |

G1 把 `R-BODY-IN-REGION`、`R-FRONT-IN-REGION`、`R-HOLE-IN-REGION`、`R-BOUNDARY-LAYOUT`
都**明列在九条充分限制之内**（`g1_region_model.py:24-32`；`00_charter.md` §4 表 223-230 行）。
所以准确的因果是：**这份布局同时违反九条中的多条**——`R-BODY` 由 78 台决策机身坐实，并连带
`R-FRONT`（含 19 个不依赖 R-BODY 的独立反例）、`R-HOLE`、`R-BOUNDARY`。**单松其中任何一条
仍不足以把它纳入 G1 搜索空间。** 房间分解不是「九条之外、先于全部 `R-*`」的东西——它就是
这几条限制的载体，前一版文书那句「被房间分解本身（先于全部 `R-*`）排除」是错的，已删。

**能推出什么、不能推出什么**（这条边界很重要）：

- 能推出：**G1 的搜索空间不完备**，因此 G1 的 `INFEASIBLE` **不能证明原问题不可行**。
  【强论据】**08-04 夜升级**：这条昨天挂着「咨询合同语义 = 官方语义」这个条件，
  现在条件基本被 §6 拆掉了——291 个位姿全部在**官方冻结池**里唯一解析，边界口那个未测点的
  前提根本不存在，非激活口 front 假设经官方权威确认合法。**残余条件只剩两道官方 CP-SAT 门
  （binding / routing）没跑**；那两道门管的是「这份摆法能不能通过官方求解器」，
  而本条结论只需要「这份摆法在官方口径下表达得出来且违反 G1 的多条 `R-*`」，
  后者已是纯实测。
- **不能**推出：G1 的 `INFEASIBLE` 不承载任何信息。它仍然是关于**当前 G1 catalog 的供给、
  本次预算的删失量、以及 G1 家族内部可行性**的读数——前一版写的「不承载关于问题实例的信息」
  是过头话，已删。
- **不能**推出：那 781 格的差可以整个归到「房间表达方式」头上。781 是
  `3,325 − 2,544` 的算术差、且 2,544 只是**供给上界**（所以 781 只是短缺下界），
  而这个上界同时受**预算删失**（193 个 CLEAN 目标超时未判）和
  **多条 `R-*` 共同作用**；这两个来源本批没有分离。前一版那句「781 格的差不在板上、在 G1 的
  表达方式里」把一个算术差精确归因给了单一原因，是错的，已删。
- 能推出：`G1 家族病 vs 问题病`这个二分**在「G1 之外存在完整布局」这一侧被确认**——
  但这不等于回答了「G1 家族内部有没有解」。
- **不能**推出：G1 家族是空的。房间内是否存在另一份合法布局，仍然未知（预算删失依旧）。
- **不能**推出：band22 见证正确到可登记。§4.3 的两道门都还没过；它是一份**固定的
  research witness**，不是 G1 的一个解。

### 7.2 其余几条跨包结论

- **`cap-3` 就是目标档位**（§3.4）。这句话把 26 号那张「cap-3 scope 的 3324 证书」从
  「有条件的次品」重新定位成「正中靶心」。但 §3.5 的实测已经把这条路的价值判掉了大半：
  cap-3 统一界实测停在 **3388**，距 3324 差 **64**，29 跑零界移动（主输出字面值仍是
  `INVALID_CALIBRATION_FAILED`，`NO_GO` 是补校准后的二次读数）。
- **25 号的九份判定实验规格现在多了一条经验线索**（不是排序）。原本九条 `R-*` 的松绑优先级
  只能按纸面价签排；§7.1 那张表告诉你：`R-BODY`、`R-FRONT`、`R-HOLE`、`R-BOUNDARY` 这**四条
  同时**排除了那份已知可摆下的布局。它给不出「先松哪一条」的答案——因为单松一条不够，
  这四条得一起动。真要排序仍需跑 25 号的 D 系列判定实验。
- **27 号那份几何的机身与供电子范围有两轮独立复核**（§5.5）。第一轮是我方两个复核席从零重算
  六谓词；第二轮是 28 号那条反向定理撞上去碎掉的过程——为裁这条定理，复核席把 219 台机身、
  100 格杆、覆盖关系又从零重算了一遍。**第二轮不含路由、端口绑定与商品层。**
  **证据等级：research-grade；机身/供电子范围两轮独立复核，零反例。**
- **band22 见证的天花板已经量清楚了**（§6.4 第 6 条）：它无论过多少道门都不可能自己铸出
  production `CERTIFIED`（那要六谓词 **加 lex 最优性**），它能变成的最好东西是 research
  lower ledger 里的一个 feasible witness，即 `L = (42, 6)`（入账条件见 `00_charter` G3）。
  **这不是这条路的缺陷，是它的定义域**——引用时必须一起说。

---

## 八、处置清单

引用这四包里的任何一条之前，先做完对应的这一格。**08-04 夜有五格状态变了，已在表内标出。**

| # | 事项 | 属于 | 性质 |
|---|---|---|---|
| 1 | HOLE 保留率写 **0.07183%**（不是 0.07824%）；PORTAL 若与 BODY 并排比较必须换算到 `H0` 共同分母的 **4.59%**；CORE 买回写 **≤7,441**（不是 7,687）；杆锚点删除率写 **6.816%**（不是 6.817%） | 25 号 | 数字修正，改文字 |
| 2 | 把 `min(3, 区内机身台数)` + 「有机身 ⇒ ≥1 杆」+ 「无机身 ⇒ 0 杆」补进隐藏前提审计表 | 25 号 | 审计补录 |
| 3 | `03_slack_audit_table.patched.md` 只引到「人读镜像」级；要入库先用脚本重生成 | 25 号 | 溯源 |
| 4 | 三处证据等级降格（跨域排序、非①判读、九条实验设计句） | 25 号 | 标签 |
| 5 | ~~跑 pricing 协议前先补 status 守卫~~ → **已做**（`patch_failopen.diff`，附红-before-绿收据：未打补丁的分析器在全 INFEASIBLE 合成输入上真发了 GO 证书） | 26 号 | **已关闭（§3.5）** |
| 6 | **仍未做**：`11_runnable` bundle **没有**在 HEAD 上重打，08-04 夜那一跑的 `runtime.bundle` 仍是旧包（g1 三模块与 HEAD 差 787/74/2 行）。**所以引用本次读数必须写成「主输出 `INVALID_CALIBRATION_FAILED`；补入无 cap 单变量校准后二次重判为 `NO_GO`，作用域 = 08-04 修复前 g1 连通语义 + cap-3」**；3388 这个上界本身仍合法（旧语义更弱 ⇒ 其上界对 HEAD 依然成立），但「界压不下去」可能是假阴性 | 26 号 | **仍阻塞「终局关门」，不阻塞引用 3388** |
| 6b | ~~判 NO-GO 之前先断言 S1 的 15 个 branch 记录全在~~ → **已做且实测满足**（`stage1_15s` = 15 条 / 15 个不同组合） | 26 号 | **已关闭（§3.5）** |
| 6c | 引用 **3324** 时写成「cap-3 scope 下的**条件**会计阈值 / 候选证书目标」，不是现成 formal certificate；**256 MB 是 D1 pilot 实测**，完整协议峰值未测。**补**：实测统一界是 **3388**、距 3324 差 **64**，引用两个数时别混 | 26 号 | 措辞纪律 |
| 7 | 跑协议传 `--max-parallel 1`；知道 1200 s 硬墙是软的（最坏溢出一个阶段 ≈720 s）。**08-04 夜那一跑没守这条**（用了 `--max-parallel 5 × workers 4`，同期右线 M5 在跑）——不影响界的合法性，但「29 跑零界移动」这个读数在争抢下天然偏保守 | 26 号 | 操作纪律，**本次有偏差，已记账** |
| 8 | 引用 band22 时删掉或降级 `archive`/`ask`/`snapshot` 三个 hash | 27 号 | 溯源 |
| 9 | 「inactive front 可被机身占用」在**回件里**仍降为【未言明语义假设】；**我方侧已升级为实测确认**：官方权威（`PROJECT_LOCK` B-4 + `F-PORT-ACTIVE-BOUNDARY-01` + `canonical_rules` 两条 semantics + 五处源码位）明确允许，暴露面 11 处全部合法（§6.1 C） | 27 号 | **已关闭（§6.1）**，要改是 freeze-ritual |
| 10 | 「杆覆盖是全机身还是一格」**已由 owner 2026-07-07 定谳 = intersection（≥1 格）**（`PROJECT_LOCK.md:91-99`）；109/219 只是反事实敏感性，不再列为待定项。**§6.2 补一条源码侧坐实**：`exact_coordinate_master:6206-6227` 就是区间相交测试 | 27 号 | 权威已定，要改走 freeze-ritual |
| 11 | 引用 `(42,6)` 时同时写明：research witness（pre-candidate）、`L=absent` 不变、官方两道 CP-SAT 门与终端固定见证复验都未跑；入账条件按 `00_charter` G3（独立 strict checker 零 issue + 复算 ≥(42,6)），**不套用 production CERTIFIED 的 seal/publish/P1.2 链**。**「咨询合同口径」这个限定词只对 placement / 机身-front / 计数 / 当前供电重算这几个子结论收窄**，整份见证仍写 research / consultation-aligned | 27 号 | 定位纪律，**限定词按子结论收窄** |
| 12 | ~~补 `matching_map_boundary` 的版本/资源映射核对~~ → **注销**。不是「核对通过」，是**要核对的东西不存在**：模型里根本没有「资源格」概念，`left_or_bottom_boundary` 已完全烤进冻结池的 136 个位姿（68 左 + 68 下），A 过 ⇒ B 过（§6.1 B） | 27 号 | **已注销（§6.1）** |
| 13 | 25 根杆的「精确最小」永远带作用域写：**仅对该固定机身 + 固定 1,143 格路由 + 固定孔的 V0 几何**；合法杆位若因附加规则收缩，**先失效的是 25 根这个可行上界**（由 56 anchor 划分给出的下界不会自动变小） | 27 号 | 作用域纪律 |
| 14 | **28 号那条不可行定理不得引用**，它的方向性指挥（砍 D0 变体、上 corridor 绕杆）一并作废；要复用只能按 §5.3 的最小修法降级成「**纯装家族**不可行」。**证伪收据换掉**：以「27 号 25 根杆实测覆盖 219/219」这条直接坐标反例为准，可附纯 5×5 的 band14 局部反例（`q₁₃+q₁₄=1 < r₁₄=4`）；**`Q=20 / R=62 / 3Q=60 / 边界 59` 与那个脚本 exit 0 都不得再当证明收据**（按 28 号原 `q_i` 定义重算是 `Q=22`、`3Q=66`，不等式反而成立） | 28 号 | **结论 REFUTED，硬禁引；旧证伪收据作废** |
| 15 | 引用 27 号或 28 号的前提数字前，先核对它吸收的是**哪一版**订正便条（两个会话的第三条不同，§5.8） | 27/28 号 | 溯源 |
| 16 | **「分歧项 = 自由区」的例外**（§5.7）与 **§4.5 的写作纪律**在写回方法论 SOP 之前一律标**候选**，不得当成已生效规则 | 方法论 | **owner 待认可** |
| 19 | **`2,544` 一律写成「机身面积供给上界」、`781` 写成「相对该上界的算术差 / 短缺下界」**（`pre_gate.json` 的 `reading` 自述 over-count） | 我方读数 | 数字口径 |
| 20 | 引用「27/28 号撞车」时**逐项标出重合的来源**（ASK 强制 / 材料诱导 / 独立推导），前两类不计入独立会聚证据；**别再写「同材料互盲复现实验」**——订正侧信道未受控（§5.6、§5.8） | 27/28 号 | 溯源与证据等级 |
| 17 | **登记这份见证要新写一个摄入 driver**——`scripts/` 与 `main.py` 里没有任何东西能吃外部 witness JSON 再跑门（§6.4 第 5 条）。别把 `--solution-hint` 当替代品：那不存在，而且 hint 永远不能当证据 | 27 号 | **新工程，阻塞正式登记** |
| 18 | `registration_placement_solution.json` 的 `pose_idx` 只对**当前**冻结池 build 有意义，换 pool 即失效；`ghost_pick` 必须由 `_expected_unfiltered_ghost_anchor_index(...)` 产出，不能自己编 | 我方探路 | 作用域纪律 |

---

## 九、证据索引

| 断言 | 出处 |
|---|---|
| 四包逐字存档与来源 | `docs/research/cleanroom_rederivation_20260718/{25,26,27,28}_PROVENANCE.md` 与同名交付目录 |
| 25 号原始裁定（存档席 + 对抗席 + 复算席，三条 result 记录） | `~/.claude/projects/-home-zhuran24-zmd-pj/0e0796c6-a439-4e55-90af-d9b5b3e50a3d/subagents/workflows/wf_09cd0b4d-d09/journal.jsonl` |
| 26 号原始裁定（存档席 + harness 席 + 数学席） | 同上 `wf_f2d038e0-714/journal.jsonl` |
| 27 号原始裁定（存档席 + 纪律席 + 几何席） | 同上 `wf_9e670990-de6/journal.jsonl` |
| **28 号原始裁定（存档席 + 撞车复核席）：结论证伪、两处暗桩、重合/分歧枚举、方法论例外、订正便条差异** | 同上 `wf_72bf7d2c-ba9/journal.jsonl` |
| **28 号结论的直接坐标反例（承重收据）** | 27 号原始见证 `band22_repaired_design_witness_not_checker_schema.json`：25 根杆实测覆盖 219/219 台，机身重叠 0、出界 0、每根杆 host band 唯一【实测，复核席从零重算】 |
| ~~28 号定理的反证脚本（`Q=20 / R=62`，末行 `REFUTATION ESTABLISHED`）~~ | **作废，不得引用**：`/tmp/.../scratchpad/band22_dup/refute_28_power_theorem.py:119` 的 `q_i` 是逐连续段取 floor 后求和，与 28 号原报告 `:245-295` 的「整带自由列总数取 floor」定义不一致；`r_i` 对混合宽度带逐宽度取 ceil 相加也不是 28 号定义。按原定义重算得 `Q=22 / R=62 / 3Q=66`，不等式**成立**（§5.2） |
| **28 号原 `q_i` / `f_i` 定义出处** | `docs/research/cleanroom_rederivation_20260718/28_band22_dup_answer_20260804/w0_band22_holes_consultation_response.md:245-295`（「定义每条带的全高自由列总数为 `f_i`，令 `q_i=floor(f_i/2)`。这把不连续自由段合并了…」） |
| **28 号暗桩所在** | `docs/research/cleanroom_rederivation_20260718/28_band22_dup_answer_20260804/verify_w0_direct4_power_obstruction.py:59`（硬编码 `Q3, R3 = 0, 6*ceil(22/K[3])`） |
| **pricing 协议实跑全部工件（主输出 `INVALID_CALIBRATION_FAILED`；补校准二次重判 `NO_GO`）** | `.artifacts/w0_fixrerun_20260804/pricing_exp_run/`：`readings_summary.json`、**主输出** `pricing_protocol_out/decision.json`（`verdict = INVALID_CALIBRATION_FAILED`，`calibration.best_bound = 91` / 要求 `≤90`）、**补充二次 manifest** `pricing_protocol_out/decision_with_solo_calibration.json`（`verdict = NO_GO`，`best_bound = 85`）、`merge_supplementary_calibration.py`（自述「secondary manifest … primary manifest is left unchanged」）、`failopen_regression_check.json`（红-before-绿）、`patch_failopen.diff`、`protocol_run.log` |
| **补丁不变式：真实 manifest 上打补丁/未打补丁分析器同 decision** | 本文书作者当场复跑；manifest = `pricing_protocol_out/manifest.json`，341,924 字节，sha256 `e6ba325bec5a959ddf9adb71c42a76e5361c9ec0874d69e32f595c3b6863a8a2`；两侧 decision JSON 逐字节相同 |
| **bundle 未在 HEAD 重打的实证** | 运行记录 `pricing_protocol_out/stage1_15s/*.json` 的 `runtime.bundle` = `.artifacts/w0_consult_packs_20260804/pricing_exp/11_runnable`；该目录三个 g1 模块与仓库 HEAD `diff` 行数 787 / 74 / 2 |
| **band22 → 官方语义对齐探路全部工件** | `.artifacts/w0_fixrerun_20260804/band22_alignment/`：`VERDICT.json`（总裁）、`alignment_A_pool_membership.json`（291 条逐台池成员记录）、`alignment_BC_semantics.json`、`alignment_D_connectivity_necessary.json`、`registration_placement_solution.json`（291 台官方形状半成品）、`max_empty_rect_for_this_placement.json`，及四个 `check_*.py` / `emit_*.py` 源 |
| 非激活口 front 合法性的九条证据链 | `PROJECT_LOCK.md:156-167`、`:33-39`；`rules/canonical_rules.json` 的 `semantics.machine_min_clearance` 与 `semantics.protocol_storage_box_wireless`；`src/models/port_binding.py:212-266`；`src/models/binding_subproblem.py:962-984,1131-1154,1201-1225,1453-1494`；`src/search/pr2_l0_fixed_witness_core.py:1785-1830` |
| 供电覆盖 = 区间相交（非全包含）的源码位 | `src/models/exact_coordinate_master.py` `_add_power_coverage_axis_delta_constraints:6206-6227` |
| ghost 不吃带子（带可穿空矩形） | `src/search/benders_loop.py:8086-8089`；`src/models/routing_subproblem.py` `RoutingPlacementCore.from_occupied_cells:54-88` |
| 可用 CP-SAT 解释器（订正探路的「venv 坏了」诚实注） | `/home/zhuran24/zmd-pj-codex/.venv-uvbolt-backup/bin/python3`（Python 3.13.13 + ortools 9.15.6755，实测能 import `cp_model` 与 `src.preprocess.operation_profiles`）；`zmd-pj/.venv` 的解释器 symlink 确已断（指向已删的 `~/interpreter_ab/py31313-pgo/`） |
| G1 机身面积供给**上界** 2,544（`supply_ceiling`，同文件 `reading` 自述 over-count）/ 需求 3,325 / 短缺**至少** 781、四条路原始价签 | `.artifacts/w0_fixrerun_20260804/regen/gate_union/master/pre_gate.json` 的 `catalog_class_supply.classes[__body_area__]`；生成侧 `g1_exact_cover_master.py:820-865,918-955`；`STOPLOSS_CHECKPOINT_20260804.md` §2、§4 |
| **W0 lower-bound 入账权威（不是 CERTIFIED 链）** | `docs/research/w0_front_aware_20260803/00_charter.md:65-68`（G3 独立 strict checker）、`:91-92`（双 ledger 纪律：零 issue + 复算 ≥(42,6) 才登记）；research lower ledger 与 production CERTIFIED 的分野见 `docs/项目说明/21_glossary.md:89-105`；`PROJECT_LOCK.md:329-340` 是终端 CERTIFIED 铸造/发布链、`:177-203` 记 P1.2 已由 owner 于 2026-07-07 关闭 |
| **27/28 撞车的共同输入（判定重合是否独立会聚的依据）** | `.artifacts/w0_consult_packs_20260804/band22_holes/00_ASK.md:4-7,19-24,39-49`；`01_construction_verbatim.md:50-90,110-126,143-159` |
| G1 房间划分与四条 `R-*` 登记为充分限制 | `docs/research/w0_front_aware_20260803/g1_region_model.py:5,24-32`；`00_charter.md` §4 表 223-230 行 |
| **band22 × G1 单向必要条件排除检查（78/219 决策机身、7/47 fixed furniture、19 front 独立反例等）** | 脚本 `docs/research/w0_front_aware_20260803/band22_vs_g1_region_check.py`，收据同名 `.json`。**脚本默认只打印、传 `--write` 才覆写收据**；它只做单向排除，否定分支是 `UNKNOWN_NOT_REJECTED_BY_CHECKED_CONDITIONS` 不是「可表达」 |
| 前一轮（22/23 号）裁决与四条路的来源 | `docs/research/w0_front_aware_20260803/CONSULT_VERDICT_20260804.md` |
| 四包各自的入库 preflight 收据 | `.artifacts/w0_fixrerun_20260804/preflight_c{12,13,14,16}_*.log`（四份均 exit 0、20 passed、pytest 1544 passed） |

> **家规遵从声明**：本文书是编排者自产的综合推理文书，按
> `referee-authored-docs-blind-spot` 家规须过 codex refute 席后才能承重外发；
> 每个数字前提已标证据等级。四份回件本身「入库 ≠ 采信」，采信范围以本文书
> §2–§6 的逐条裁定为准。**28 号的核心定理是本文书唯一一条判 REFUTED 的外脑结论，
> 它连同它指挥的下一轮方向一起作废。**
