# 勘误二轮验收清单（2026-08-07，外审份4 + 核签）

**为什么这份清单存在**：勘误**一轮**在设计稿 `:4` 写了「本文已按该表就地订正」，这句**完成度声明不成立**——外审份4 的 D-01 查出 6 处残留矛盾（设计稿 2 处 + README 4 处），核签 `ADJUDICATION_fen4.md` §0 判 **6/6 属实、全部 ACCEPT**。所以二轮**不许用自述关闭**，每一项都必须附机器收据。

**收据生成器**：`errata_round2_scan.py`（本目录，零依赖，任何 Python 3 可跑）。

```bash
./.venv/bin/python docs/research/p2_0_specialized_20260807/errata_round2_scan.py
```

**本次结果**：

```
扫描文件 11 个
[1] 旧误文扫描   短语 43 条   BARE=0  TRACED=124  PROBE=12  OTHER=1
[2] 新文断言     断言 66 条   缺失 0 条
[3] 外部件复跑   5/5 exit=0，与归档 stdout 逐字节相同
总判：裸命中=0  新文缺失=0  外部件复跑失败=0  ⇒ PASS（三段全过，可标完成）
```

**判据**：`BARE`（裸命中）= 旧说法仍出现在**正文主叙述**里。**必须为 0**，非 0 即该项不得标完成。`TRACED` 是家规要求的留痕（引用块 / 带撤销标记的行 / 勘误一轮 errata 表的「现文」列），数目越多说明留痕越全。

**三个排除面全部显式声明，逐条明细见文末 §4**：`PROBE`（探针输出面，12 处，刻意不改）、`OTHER`（他席文档，1 处，避免并发撞车）、以及两个**整体不参与 `[1]` 扫描**的文件——扫描器自身与本清单（台账件，按设计逐字引用被撤旧说法）。**排除面不是「扫不到」，是「扫到了但有登记的理由」**——这条本身就是二轮教训：一轮的问题不是没扫，是根本没扫。

---

## 0. 改动文件清单

| 文件 | 性质 | 改动处数 | 净行数（+/−） |
|---|---|---:|---|
| `README.md` | **整份重写**（核签 A1：全稿最落后的一份） | 整份 + 1 处补订 | +40 / −13 |
| `P2_0_SPECIALIZED_DESIGN_V1.md` | 就地订正 | **34** | +254 / −57 |
| `OWNER_DECISION_SUMMARY.md` | 就地订正 | **8** | +55 / −11 |
| `refute_round1/REJUDGE_REPORT.md` | 就地订正 | **27** | +267 / −49 |
| `refute_round1/split_free_probe_v2.py` | 注释/docstring（行号订正 + 术语注） | **2** | +16 / −5 |
| `split_free_probe.py` | docstring 勘误注（v1 历史件） | **1** | +9 / −0 |
| `maxmin_segment_probe.py` | docstring 勘误注 | **1** | +20 / −3 |
| `.artifacts/gpt_pro_review_batch_20260807/DISPATCH_README.md` | 口径订正（C3） | **1** | +16 / −2（未跟踪目录，`git diff` 不显示） |
| `refute_round1/external_round2/` | **新建**：5 脚本 + 5 stdout + README | 11 个新文件 | 新增 |
| `errata_round2_scan.py` | **新建**：收据生成器 | 1 个新文件 | 新增 |
| `ERRATA_ROUND2_CHECKLIST.md` | **新建**：本文件 | 1 个新文件 | 新增 |

**未动的面**：`rules/canonical_rules.json`（canonical 本体一个字未改，挂下一批 freeze-ritual）、`PROJECT_LOCK.md`、`src/` 全部生产代码、全部归档 `*_stdout.log` 与 `*_receipt.json`。

---

## 1. A1–A6 逐项验收

### A1 —— `README.md` 整份重写 ✅

核签 A1 列了五点，逐点收据（新文断言，`[2]` 段）：

| 核签要求 | 落点 | 机器收据（断言短语） |
|---|---|---|
| `:17` 删「6 种商品、37%、15 对」，改成「均摊硬编码越界 / 连续计数只证 buckwheat+sandleaf（10.5%）/ 另四例游戏稳态·几何·可表达性未验」 | 一句话结论 | `均摊硬编码制造出来的` ✅ · `10.5%` ✅ · `warehouse-bridge 排除` ✅ |
| `:19` 删无条件分解语气，加前件 | 一句话结论末 | `master 显式固定了每商品的容量预留` ✅ |
| `:38` 欠账第 1 条改成「refute 一轮已完成 + 该报告本身的独立复审已由 GPT Pro 完成 + 核签路径」 | 已知欠账 1 | `该报告本身的独立复审也已完成` ✅ |
| `:45` 删「混流只可能发生在分流细流段」 | 对外部线净输入 | `速率算术恒能找到一对不同中间品的段` ✅ |
| `:21` `A ≤ 1167` 后加「面积坐标；min_side 上界未立」 | 推荐路线 | `` `min_side` 上界从未立过 `` ✅ |

旧文消失收据（`[1]` 段裸命中 = 0）：`6 种商品` / `占 37%` / `未过独立 refute 席` / `混流只可能发生在分流细流段` 四条短语在 README 正文**零裸命中**；被撤的旧文以「原文（已撤）」引用块留痕。

补订一处：目录表里 `maxmin_segment_probe.py` 一行的「混流窗口」改成「速率兼容对」（D-05 术语统一）。

### A2 —— `P2_0_SPECIALIZED_DESIGN_V1.md`（34 处）✅

| # | 位置（原行号） | 依据 | 改动 | 机器收据 |
|---|---|---|---|---|
| 1 | 头部 `:4` | D-01 | 「已按该表就地订正」撤，加勘误二轮总述 | `勘误二轮（20260807，外审份4 + 核签）` ✅ |
| 2 | `§0` 前 | D-13 | 新增「复合命题必须拆标签」规则 + CP-SAT 格点标签纪律 | `复合命题必须拆标签` ✅ |
| 3 | `§0` 三条结论 | D-13 | 「三条**实测**结论」撤，逐条拆【实测】/【推导】/【假设】 | 见 4、5 |
| 4 | `:25`（§0 结论 2） | **D-05** | 「网络级纯流恒不成立」「混流窗口恒存在」撤 → NS/RC/P1 三谓词 + 定理 1 四条前件 | `网络级纯流恒不成立` 裸命中 **0** ✅ |
| 5 | `:29`（§0 结论 3） | **D-09** | 「不依赖任何前件」撤 → P1 或容量预留后**可条件分解**；无 P1 上界侧不能分解 | `不依赖任何前件` 裸命中 **0** ✅ |
| 6 | `:45` 命题 S2 | **D-04** | 整体替换为「聚合占空约束与算术自由域」，三层标签 + `U_rate` 单向包含 + M2/M3/M4/M5/M7 + binding 审计表 | `命题 S2（聚合占空约束与算术自由域）` ✅ `U_rate` ✅ `速率算术点` ✅ |
| 7 | `:119` | **A4b** | fill-first 定位 `:42-44` → **`:42-43`** | `` rate_lemma_recompute.py:42-43 `` ✅ ／ `:42-44` 裸命中 **0** ✅ |
| 8 | `§2.4` 六例表后 | D-04 | 翻案四例加保守读法（原必然性证明已失效 ≠ 游戏合法反例已存在） | 断言 `速率算术点` ✅ |
| 9 | `§2.4` 末（`:177-181`） | **D-05** | 整体替换为结论 A / 结论 B + 三谓词表 + 删除句式对照 | `结论 A：无分支路由对两种作物不可能` ✅ `结论 B：速率分离不能无条件推出物理纯度` ✅；`没有第三条路` 裸命中 **0** ✅ |
| 10 | `§2.5`（`:187`/`:191`） | **D-06 核签版** | 前件 (ii) = **反稀释条款**；均摊与阶梯**都在族内**；完备二分两分支都杀引理；**616 换 622**；「前件与结论互斥」撤 | `反稀释条款` ✅ `可达最小值是 **622**` ✅；`616` / `前件与结论互斥` / `理论下确界` 裸命中全 **0** ✅ |
| 11 | `§2.5` | **A4b** | `rate_lemma_recompute.py:36` → **`:34` + `:37`** | `` rate_lemma_recompute.py:34 `` ✅ ／ `rate_lemma_recompute.py:36` 全仓裸命中 **0** ✅ |
| 12 | `:204`（§3 引言） | **D-08** | 「不需要新变量结构」撤 → 拓扑层可复用、**数值层必须新增** | `不需要新变量结构` 裸命中 **0** ✅ |
| 13 | `§3` 行族清单 | **D-08** | 新增 D1/D2/T1/T2/T3/C1/C2/P1 八族表 + 与 §6 T1–T6 对齐 + 「谓词(5)连通不能替代数值守恒」 | `P7-S 的最小完整变量与行族` ✅ |
| 14 | `:227`（§3 P2） | **D-07** | 660 加限定（均摊有限表 + Part D/F/G 网格），连续层用 exact LP/Fraction，网格 INFEASIBLE 不得升格 | `exact LP / Fraction` ✅；`公分母 = **660**` / `乘 660 后全部系数为整数` 裸命中 **0** ✅ |
| 15 | `:255-256`（行数量级表） | D-07 | P2/P3 两行系数栏改写 | `公分母 660` / `整数（乘 660）` 裸命中 **0** ✅ |
| 16 | §3 规模小结 | D-08 | 补注「上表只覆盖 P1–P4，不覆盖新补五族」 | — |
| 17 | §4 甲案「欠两条」 | **D-10** | 改「欠一到三条、视冻结了什么」 | `欠两条` 裸命中 **0** ✅ |
| 18 | `:283-290` 义务段 | **D-10** | 整体替换为甲-A0 / 甲-A1 / 甲-A2 三档；**OB-D 更名 OB-P1**、义务对象从「稀释/分流」改为「不同商品共用同一 hardware key」 | `甲-A0` `甲-A1` `甲-A2` `OB-P1（物理纯度支配，未证）` 全 ✅ |
| 19 | §4 乙案附带代价 | D-10 | 限定为「甲-A1 以上才带这条代价」 | — |
| 20 | `:311`/`:314` 闭合判据 | **D-11** | 「上界=下界 ⇒ 全局 lex」撤 → **两阶段**（阶段 A 面积 / 阶段 S 最短边）+ **语义一致性前提** | `阶段 A（面积）` `阶段 S（最短边）` `语义一致性` 全 ✅；`上界 = 下界 ⇒ 全局 lex` 裸命中 **0** ✅ |
| 21 | §4 上界现役件 | D-11 / D-12 | 标明只是 `U_A`，`U_S` 从未开工；D-12 按核签 **DOWNGRADED-IN-REPO**，**不改 `A≤1167` 的等级标签**，改的是外发纪律 | — |
| 22 | §4 推荐理由 | D-08/D-11 | 「零前件」→「零**支配引理**」；新增丙案两条工程欠账 | `零前件` 裸命中 **0** ✅ |
| 23 | §6 净收益 2 | **D-09** | 最小割加限定：无条件上界侧仍需完整 LP/对偶证书 | — |
| 24 | `:403`（§7.1 结论） | **D-05** | 「混流窗口的段速率永远 ≤ 1/2」撤 → 速率兼容对，不构造物理共道、不限定在细流段 | `混流窗口` 裸命中 **0** ✅ |
| 25 | §7.1 两 bullet | D-05 | 「混流窗口直接开在端口上」「15 对窗口」→ 速率兼容对 | 同上 |
| 26 | `:464`（§9.1 工程量表） | **D-01** | 「常数（本稿 receipt 可直接当输入表）」→「170 钉死 / 49 变量 / 净 42 维；**降维，不是常数化**」 | `常数（本稿 receipt` 裸命中 **0** ✅ |
| 27 | §9.1 有理 Farkas 行 | D-09 | 加「无条件上界侧不能用最小割」限定 | — |
| 28 | §9.1 表后 | D-01 | 两行原文留痕 + 点明这是完成度声明不实的证据之一 | — |
| 29 | `:476`（§9.2 R2） | **D-01** | 「P1 族已证为空」→「尚未证明任一受限族…；P1 族空性未测」+ 缓解措施改「由不含 P1 的完整 verifier 复核」 | `已证为空` 裸命中 **0** ✅ |
| 30 | §9.2 表后 | D-01 | R2 原文留痕 + 点明是第二个证据 | — |
| 31 | §9.3 第 3 条 | D-05 | 「混流窗口存在 ≠ …」→「速率兼容对存在 ≠ …」，并标注这是全稿唯一写对边界的地方 | — |
| 32 | §9.3 第 6 条 + 新增 0' | D-01/D-08/D-11 | 二轮外审已完成；补录**三个新命中向量**（勘误声明可信度 / 模型完整性 / 目标第二坐标） | — |
| 33 | §10 台账 | 多条 | Q1 扩成三模型×14 指标；Q3 按档分记；Q10 二轮闭合；Q11 按核签版；**新增 Q12/Q13/Q14** | `Q1 实验规格` `model_proto_bytes` `**Q12**` `**Q13**` `**Q14**` 全 ✅ |
| 34 | 复跑锚点 + 页脚 | A5/A6 | 收编 external_round2 五脚本；页脚记二轮总判与四个主要缺口 | `ERRATA_ROUND2_CHECKLIST.md` ✅ |

### A3 —— `OWNER_DECISION_SUMMARY.md`（8 处）✅

| # | 位置 | 依据 | 改动 | 机器收据 |
|---|---|---|---|---|
| 1 | 勘误头 `:5` | — | 加勘误二轮三点说明 | `勘误二轮（20260807，外审份4 + 核签）` ✅ |
| 2 | `:21` | **D-05** | 「『一格只准走一种中间货』这条规则在任何分配下都不能当模型约束用」+「两头都堵，没有第三条路」**撤**；补定理 1 四条前件 | `再往下推就越界了` ✅；`一格只准走一种中间货` / `没有第三条路` 裸命中 **0** ✅ |
| 3 | `:23` | **D-06 核签版** | 「前件和结论互斥」+「理论下限 616」**撤** → 前件是**防稀释**用的、两份都在族里、真正够得到的最小值是 **622**；并说明外审在这条上判错 | `防稀释` ✅ `真正够得到的最小值是 622` ✅；`前件和结论互斥` / `理论下限` 裸命中 **0** ✅ |
| 4 | 「真正的好消息」段 | **D-09** | 「这个好处不依赖任何前件」**撤** → 带前件的条件分解 | `这个好处**不依赖` 裸命中 **0** ✅ |
| 5 | 「我推荐的路线」段 | **D-11** | 新增 ⚠ 框：**最短边那一半的上界从来没有人立过**，闭合要走两步，别按「上界=下界就闭合」判断进度 | `最短边那一半的上界从来没有人立过` ✅ `min_side 维度本文未触碰` ✅ |
| 6 | 同段（甲案） | **D-10** | 「欠**两条**支配引理」**撤** → 按冻结程度分四种情况（含「只作启发式则一条都不欠」） | `欠**两条**` 裸命中 **0** ✅ |
| 7 | 「顺带两条小事」1 | D-06 | canonical 改法主修 = 声明占空前件；外审的逐端口读法只能当附加 | — |
| 8 | 「已知欠账」 | 多条 | 二轮已过 + 四个主要缺口（模型不完整 / 第二坐标零进度 / 完成度声明不实 / §5.2 被自证伪）+ 攻击面两轮都没预判到 | — |

### A4 —— `refute_round1/REJUDGE_REPORT.md`（27 处）✅

| # | 位置（原行号） | 依据 | 改动 | 机器收据 |
|---|---|---|---|---|
| 1 | 头部 | — | 加勘误二轮总述 + 外审自产件收编说明 | `勘误二轮（20260807，外审份4 + 核签）` ✅ |
| 2 | §0 定理 2 摘要 | D-05 | 「网络级纯流双体制无条件失效」+「没有第三条路」撤 | `无条件失效` 裸命中 **0** ✅ |
| 3 | §0 后新增 §0.1 | **R-10** | 证据等级规则：`legal allocation` → `rate-balance-admissible`；「机器验证」必须注明口径；几何/稳态/可表达性不得并入【实测】+ 12 行等级对照表 | `本报告的证据等级规则` ✅ `rate-balance-admissible` ✅ |
| 4 | `:36`（§1） | **R-02** | 「v1 还有**第二个问题**：逐商品独立判」撤 → 固定均摊前件内无额外耦合缺陷；放开 duty 后**正见证**才须联立 | — |
| 5 | §2 (c) Part D | R-05 | 加【实测·1/660 格点】标签 + 「格点 INFEASIBLE 不构成连续域 INFEASIBLE」 | — |
| 6 | `:112-114` 定理 1 | **R-03 + 核签加强** | 补 4 条前件（frozen targets+counts / 经当前 route graph / warehouse-bridge 排除 / 端口容量与精确计数）；**删多余的「任何最小车道路由」前件**（净加强）；标题「无条件」→「对占空分配与车道约定均无关（当前实例与当前直连路由语义下）」 | `定理 1（作物回流必然分支` ✅ `warehouse-bridge routing 继续被当前模型排除` ✅ `净加强` ✅ |
| 7 | `:133-147` 定理 2 | **D-05** | 标题改「速率分离不能推出物理纯度」；加 NS/RC/P1 三谓词表；「堵死两个方向 / 没有第三条路 / 恒破」撤，改成数字对照 | `定理 2（速率分离不能推出物理纯度）` ✅ |
| 8 | §4 标题 + 引言 | D-06 | 标题「欠一条前件」→「欠的那条前件是**占空约定**」；逐字引入 canonical 反稀释补充句（已回源 `rules/canonical_rules.json` 核对） | — |
| 9 | `:155` | **A4b** | `第 36 行` → **`:34` + `:37`**，附回源实测与兄弟线引法 | `` `:34` 的 `util = runs / machines` `` ✅ `行号订正（勘误二轮，核签 §1.4）` ✅ `` rate_lemma_recompute.py:37 `` ✅ |
| 10 | `:159` 反例段 | D-06 | 「阶梯占空同样满足 (i)(ii)」**声明读法** | — |
| 11 | `:168-180` | **D-06 核签版** | 小节整体替换：读法二分 + **完备二分两分支都杀引理** + 车道数 **628/622/622**（不是 616）+ 逐项下界 `max(n_op, ⌈c_p·x_op⌉)` + true_min_lanes 输出 | `完备二分` ✅ `628 / 622 / **622**` ✅ `max(n_op, ⌈c_p · x_op⌉)` ✅；`理论下确界` / `更满足前件 (ii)` 裸命中 **0** ✅ |
| 12 | `:184` Part F | **R-05** | 加【实测·1/660 格点】+【推导·连续域】双标签，连续结论改引 exact interval proof | `exact interval proof` ✅ |
| 13 | `:196` 共同理由段 | R-05 | 证据来源从格点 CP-SAT 改为连续域证明 | — |
| 14 | `:216` 方向 b | **D-06 + D-05 + R-03** | canonical 替换文本重写：新增 **(iii) 占空前件（主修）** + 逐端口量化澄清（附加）；删 `NETWORK-LEVEL PURE FLOW IS UNCONDITIONALLY FALSE` 与 `shared-lane window always exists`；无条件部分补四条前件 | `A STATED PER-MACHINE DUTY VECTOR` ✅ `LOCAL TO EACH ACTIVE MACHINE PORT` ✅；两条英文旧句裸命中 **0** ✅ |
| 15 | §5.1 标题 | D-05 | 「网络级纯流被证伪」→「靠速率分离推不出纯度」 | — |
| 16 | §5.1 表 | D-05 | 「混流窗口」→「速率兼容对」 | `混流窗口` 正文裸命中 **0** ✅ |
| 17 | §5.1 给 mixflow | D-05 | 无条件收窄重写 | — |
| 18 | `:263-281` §5.2 | **R-06（BLOCK）** | **整体替换**：固定 duty 只固定端口总速率；阶梯真实速率集 **6 元** `{3/20,1/5,1/2,11/20,3/5,1}`（我方 Part C 自证）；三层级（只固定 duty / +lane-merge / +有限 rate-state）；「零有理变量已复活」不成立 | `固定 duty 后，端口速率固定，但内部段速率仍未固定` ✅ `3/20, 1/5, 1/2, 11/20, 3/5, 1` ✅；`从 {1} 扩到 {1, 1/2}` / `全部段速率是单一常数` / `零有理变量` 裸命中全 **0** ✅ |
| 19 | §5.2 记账 | **D-10** | 「未闭合项从 1 条变成 2 条」撤 → 三档分记 | `从 1 条变成 2 条` 裸命中 **0** ✅ |
| 20 | §5.3 推荐理由 | D-08/D-10/D-11 | 「零前件」→「零支配引理」；「欠两条（OB-D、OB-D2）」→ 一到三条；新增丙案两条工程欠账 | `零前件` / `欠两条` 裸命中 **0** ✅ |
| 21 | §5.4 第 2 条 | D-05 | 「恒存在速率 ≤ 1/2 的共道窗口」撤 | `共道窗口` 正文裸命中 **0** ✅ |
| 22 | `:323`（§6） | **R-08** | 「几何上更容易而非更难」**撤**（车道数不是几何可行性的单调充分统计量）；「甲案族在速率算术层非空」**撤** →「速率算术没有给出 P1 的矛盾」；举证责任方向订正 | `更容易而非更难` / `甲案族在速率算术层非空` 裸命中全 **0** ✅ |
| 23 | §8 errata 表 | 多条 | 加**取代说明**：五行被二轮取代 + 二轮新增四项；「§3 行族定义」从「不需改动」清单移出 | — |
| 24 | §9 攻击面 | 多条 | 补录二轮三个新命中向量 + 一条净收入（外审自带 Part F 补丁） | — |
| 25 | `:390` §9 欠账 2 | **R-05** | 「只有 Part D 有格点限制」→ **Part D、Part F、Part G 都是 1/660 网格** + 三处承担者分工 | `Part D、Part F 以及 Part G` ✅ |
| 26 | `:392` §9 欠账 4 | **R-09** | 整体替换为 `hardware_key = H(route_or_rate_state_key)` 稳定投影要求 + 6 项连带重判清单 | `hardware_key = H(route_or_rate_state_key)` ✅ |
| 27 | 复跑附录 | A5/A6 | 收编 external_round2 五脚本 | `external_round2` ✅ |

### A4b —— 行号订正（四处）✅

| 位置 | 原引 | 订正为 | 回源实测 | 收据 |
|---|---|---|---|---|
| `P2_0_SPECIALIZED_DESIGN_V1.md` §2.5 | `rate_lemma_recompute.py:36` | **`:34` + `:37`** | `:34` = `util = runs / machines`；`:36` = `full_rate += 1`（**与均摊无关**）；`:37` = `per_machine_runs = runs / machines`（被引的字面） | 全仓 `rate_lemma_recompute.py:36` 裸命中 **0** ✅ |
| `refute_round1/REJUDGE_REPORT.md:155` | 「第 36 行」 | **`:34` + `:37`** | 同上 | 同上 |
| `refute_round1/split_free_probe_v2.py:25` | `rate_lemma_recompute.py:36` | **`:34` + `:37`** | 同上 | `` split_free_probe_v2.py 含 rate_lemma_recompute.py:34 `` ✅ |
| `P2_0_SPECIALIZED_DESIGN_V1.md` §2.2（`:119`） | `:42-44` | **`:42-43`** | `:42` = `lanes = ceil(rate)`；`:43` = `residual = rate - (lanes - 1)`；`:44` 是 `if residual != 1:`，不属 fill-first | `:42-44` 裸命中 **0** ✅ |

订正依据 = 核签 §1.4；兄弟线 `docs/research/rule_system_redesign_20260807/FINAL_DESIGN.md:186` 已经引对为 `:34/:37`，本批按兄弟线对齐。

### A5 —— 收编外审三个独立脚本 ✅

新建 `refute_round1/external_round2/`，从 `verdict/fen4/files/` 拷入：

```
independent_handcheck.py                  + independent_handcheck_stdout.log
independent_staircase_check.py            + independent_staircase_check_stdout.log
independent_part_f_continuous_proof.py    + independent_part_f_continuous_proof_stdout.log
```

**复跑收据**（`[3]` 段，本席在收编后重跑一次）：

```
independent_handcheck                 exit=0  逐字节相同
independent_staircase_check           exit=0  逐字节相同
independent_part_f_continuous_proof   exit=0  逐字节相同
```

`independent_part_f_continuous_proof.py` 已收作 **Part F 连续域证明的正式附录**（`REJUDGE_REPORT.md` §4「均摊是残道最优」段 + §9 欠账 2）。目录 README 短注已写明来源、复跑状态、五个脚本各自承担什么、以及本目录**不得修改**（改了就失去独立性）。

### A6 —— 收编核签两个验算 ✅

从 `verdict/fen4/adjudication_checks/` 拷入同一目录：

```
aggregate_reading_check.py   + aggregate_reading_check_stdout.log
true_min_lanes.py            + true_min_lanes_stdout.log
```

**复跑收据**：两者 `exit=0`、与归档 stdout **逐字节相同**。

它们是 **D-06 反转与 622 定理的承重件**——`aggregate_reading_check.py` 证明 616 不可达（`filling_capsule` / 谷电池各 3 台不可闲置机器、各自独立出口 ⇒ 产侧 ≥3 条道，聚合下界只给 1 条）；`true_min_lanes.py` 证明 622 是可达最小值且**阶梯处处达到逐项下界**（`阶梯是否处处达到逐项下界: True`）。README 已注明它们是核签席自产、以及为什么是承重件。

### R-03 加强 ✅ · D-11 ✅ · C3 ✅

- **R-03 加强**：定理 1 补 4 条前件 + **删「任何最小车道路由」多余前件** + 标题改「对占空分配与车道约定均无关（当前实例与当前直连路由语义下）」。删除理由已写进正文：铺开产道本身就是一次分支，不铺开则鸽巢矛盾，**两条路都得出分支**，故对任何车道约定成立。收据见 A4 第 6 行。
- **D-11**：设计稿 `:311`/`:314` 两阶段化 + 语义一致性前提；`OWNER_DECISION_SUMMARY.md` 补 `min_side` 未闭合的 ⚠ 框。收据见 A2 第 20 行、A3 第 5 行。
- **C3**：`DISPATCH_README.md:13` 口径订正。**先核实再改**——`unzip -l` 实测七个 zip **全部零个 `zz_optional_`**，份4 zip 共 16 个文件；`4_p2_0_specialized/` 暂存目录里只有 2 个 `zz_optional_*`。收据：`发出去的 zip 一个都没装` ✅ `必须随包` ✅ ／ `为了让包自包含才放进来的` 裸命中 **0** ✅。

---

## 2. 按外审条目的覆盖对照

| 条 | 核签判定 | 本批处置 | 裸命中 |
|---|---|---|---|
| D-01 勘误残留 6 处 | ACCEPT | 6 处全改 + 完成度声明改用机器收据关闭 | 0 |
| D-02 聚合速率/42 维 | ACCEPT（OK） | 无需改，仅标签拆层 | — |
| D-03 两条计数定理 | ACCEPT（OK） | 无需改，补前件（见 R-03） | — |
| D-04 多胞形任意点 | ACCEPT（标签级） | 命题 S2 整体替换 + 实验索引表（**全部指向已在案的 AUDIT 台账，不新增台账**） | 0 |
| D-05 四概念混同 | ACCEPT | 三稿 + 报告共 8 处措辞收缩 | 0 |
| D-06 前件 (ii) 双读法 | **PARTIAL：诊断 ACCEPT，关键论据 REFUTE** | **按核签版改，不按外审版**；616→622；诊断（未声明读法）已订正 | 0 |
| D-07 660 非公分母 | ACCEPT | §3 P2 + 行数量级表 | 0 |
| D-08 §3 无完整模型 | ACCEPT | 补齐八族行清单 + Q1 三模型×14 指标 + 新增 Q12 | 0 |
| D-09 分解有前件 | ACCEPT | §0 结论 3 + §6 净收益 2 + README | 0 |
| D-10 甲案义务三档 | ACCEPT | A0/A1/A2 + OB-D→OB-P1 改名改对象 | 0 |
| D-11 lex 第二坐标 | ACCEPT | 两阶段闭合 + 语义一致性 + 新增 Q13 | 0 |
| D-12 `A≤1167` | **DOWNGRADED-IN-REPO** | **不改等级标签**（仓内是承重档）；改外发纪律（C3） | — |
| D-13 证据等级 | ACCEPT | §0 后新增拆标签规则 + 逐处重标 | — |
| D-14 包不自包含 | **DOWNGRADED-IN-REPO** | 真缺陷（清单口径与 zip 不符）已修（C3） | 0 |
| R-01 | ACCEPT（OK） | — | — |
| R-02 逐商品独立判 | ACCEPT | §1 表述订正 | — |
| R-03 定理 1 前件 | **ACCEPT 并加强** | 补 4 条 + 删 1 条 + 改标题 | — |
| R-04 | ACCEPT（OK） | — | — |
| R-05 Part F 格点 | ACCEPT（外审自带补丁） | 双标签 + 收编连续证明 + §9 欠账 2 扩到 D/F/G | 0 |
| R-06 「两档常数」 | **ACCEPT（BLOCK，我方见证自证）** | §5.2 整体替换 | 0 |
| R-07 | ACCEPT（OK） | 一致性已贯彻到 D-05 措辞 | — |
| R-08 三处越界 | ACCEPT | §6 三处全撤 | 0 |
| R-09 state 粒度 | ACCEPT | §9 欠账 4 整体替换 + 新增 Q14 | — |
| R-10 报告等级 | ACCEPT | §0.1 新增规则 + 对照表 | — |

**24 条全覆盖，零遗漏。**

---

## 3. 结论

**全收据，无缺项。** 三段检查全过（`BARE=0` / 新文缺失 `0` / 外部件复跑 `5/5` 逐字节相同），A1–A6 + R-03 加强 + D-11 + C3 逐项有机器收据。

**注意本清单不声称的东西**（这是二轮教训的直接应用）：机器收据只证明「**旧误文已从正文主叙述消失、新文已写入、外部件可复跑**」。它**不证明**新写进去的判断是对的——本批新增的承重判断（尤其 §2.5 与 §4 的 D-06 核签版、§5.2 的 R-06 整体替换）**本身也是待审对象**，按仓库家规应过下一道独立审查。

---

## 4. 三个显式排除面（逐条登记）

### 4.1 PROBE —— 三个研究探针的输出面（12 处命中，刻意不改）

**理由**：这些是 printed string / receipt 字段 / 内部注释。**一改，归档的 `*_stdout.log` 与 `*_receipt.json` 就与脚本对不上**，而它们是本批的历史证据件（`maxmin_segment_stdout.log`、`split_free_probe_v2_stdout.log`、`split_free_probe_v2_receipt.json` 等，多处被三份文稿引用）。

**代偿措施**：三个探针的 **docstring 顶部已加勘误注**（docstring 不打印、不进 receipt，已实测确认），写明该怎么读它们的输出：

- `split_free_probe.py`：标为 v1 历史件，点名 `:97` 均摊硬编码，四例翻案、37%→10.5%。
- `maxmin_segment_probe.py`：点名同染均摊硬编码，「已证 6 种商品必然分流」已被撤销；输出里的「混流窗口」一律读作「速率兼容对」。
- `split_free_probe_v2.py`：R-02 的表述订正 + 输出术语注 + `:25` 行号订正。

逐条明细：

| 文件:行 | 面 | 内容 |
|---|---|---|
| `maxmin_segment_probe.py:10` | docstring 勘误注自身 | 「已证 6 种商品必然分流」这句（引用被撤内容） |
| `maxmin_segment_probe.py:142` | 内部注释 | `m_j + m_k ≤ cap 即存在混流窗口` |
| `maxmin_segment_probe.py:155` | **receipt 字段** | `question` 字段 |
| `maxmin_segment_probe.py:168` | **printed** | `中间品混流窗口（…）：N 对` |
| `split_free_probe_v2.py:31` | docstring | 「对 v1 判死的 6 种商品」（正确用法） |
| `split_free_probe_v2.py:564` | 内部注释 | Part G 标题 |
| `split_free_probe_v2.py:642` | 内部 docstring | 共道窗口 |
| `split_free_probe_v2.py:770/797/799/802` | **printed** | Part G 输出四行 |
| `split_free_probe_v2.py:843` | **receipt 字段** | 结论字段 |

**若将来要清掉这一面**：必须「改脚本 + 重跑 + 重新归档 stdout/receipt」整批做，并同步三份文稿里引用这些日志行号的地方——**不能只改字面**。建议挂在 P7 实现批或探针 v3 批。

### 4.2 OTHER —— `GAME_RULE_IMPACT_AUDIT.md:93`（1 处，本席不改）

该行 S-4 条的 ⚠ 反向注里写「阶梯下**混流窗口**开在端口残道上」，属同一类术语越级（D-05）。

**本席不改的两个理由**：①核签 §3.A 的施工清单没有列这份文件；②**本会话有活跃的 `rule-impact-audit` 席**，该文件是它的产出面，并发改动有撞车风险。

**已登记为待办**（见下节新发现 ③）。

### 4.3 LEDGER —— 整体不参与 `[1]` 的两个文件

| 文件 | 为什么整体排除 |
|---|---|
| `errata_round2_scan.py` | 它的 `OLD_PHRASES` 表**就是**那 43 条旧短语的字面量清单；不排除的话每条都会命中自己。 |
| `ERRATA_ROUND2_CHECKLIST.md`（本文件） | 台账件。它按设计要写成「原文 X → 改为 Y」，逐字引用被撤的旧说法是它的**功能**，不是残留。 |

**这条排除的风险与代偿**：排除意味着「本清单里写的旧文引用不受机器检查」。代偿是——本清单不承载任何**结论**，它只承载**指针**（哪条改了、收据是什么）；凡它引用的结论性文字，都能在被订正的文稿里找到对应的留痕框。**要验它有没有夸大，看 `[2]` 新文断言那 66 条**——那 66 条是从被订正的文稿里正向断言的，与本清单的自述相互独立。

---

## 5. 施工中发现的三个新问题（核签清单之外）

### ① 核签 §2 的 D-12/D-14 对照表有一处偏差（不影响其判决）

核签表列了 **4 个** `zz_optional_*` 文件在打包暂存目录 `4_p2_0_specialized/` 里「**在**」。实测只有 **2 个**：

```
4_p2_0_specialized/zz_optional_AREA_BOUND_THEOREM_REPORT.md
4_p2_0_specialized/zz_optional_p2_0_paradigm_design_v2.md
```

另外两个（`zz_optional_split_free_probe_v2_receipt.json`、`zz_optional_rate_table_receipt.json`）**在暂存目录里根本不存在**，只在仓内。

**影响**：核签的判决方向不变（仍是包装欠账），但「暂存目录里有、只是没进 zip」这个说法对那两个 receipt **不成立**——它们是**从来没被打包过**。这条已写进 `DISPATCH_README.md` 的订正注：**暂存目录也不等于全**，下批的材料清单必须按 `unzip -l` 的实际输出核，不能凭暂存目录写。

### ② `maxmin_segment_probe.py` 的 docstring 里有一句已被撤销的承重断言

原文：「`split_free_probe.py` **已证 6 种商品在任何最小车道分配下都必然出现分流**。」

这句在勘误一轮就已被 `REJUDGE_REPORT.md` §2 撤销（四例翻案），**但一轮和核签清单都没扫到探针的 docstring**——它和 D-01 那 6 处是同一类残留。本席已在该 docstring 顶部加勘误注留痕（未删原句，因为下文的推理链引用它）。

**这条本身就是 D-01 的教训延伸**：勘误扫描面不能只覆盖 `.md`，代码里的 docstring 同样承载判断。`errata_round2_scan.py` 已把 `.py` 纳入扫描面。

### ③ `GAME_RULE_IMPACT_AUDIT.md:93` 还有一处 D-05 类术语越级

见 §4.2。**建议**：由 `rule-impact-audit` 席在它自己的批次里把「混流窗口」改成「速率兼容对」，或主线程统一排一个术语批。本席未动，避免并发撞车。

---

*勘误二轮完。施工席不 commit，由主线程验后统一提交。*
