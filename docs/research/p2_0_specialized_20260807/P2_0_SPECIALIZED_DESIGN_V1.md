# P2.0 吞吐认证**特化**设计稿 v1（2026-08-07）

**Status**：研究层设计稿。不改生产代码、不改锁面、不改 canonical。
**勘误**：2026-08-07 经 `refute_round1/REJUDGE_REPORT.md` 重判勘误，勘误表见该报告 §8。本文已按该表就地订正；订正涉及命题 S2、§2.4 六例判决、§6 T3/T4/T5、§4 三案对比与 §10 开放问题台账。
**Scope authority**：在本稿落地并走完 freeze-ritual 之前，`PROJECT_LOCK.md` §1A B-1/B-2（吞吐 / belt 带宽 / 离散容量流 OUT-OF-SCOPE）**继续有效**。本稿存在 ≠ scope 已改。

**证据等级**：【实测】= 本稿脚本复跑或源码逐字回源；【推导】= 从已核事实推出、未实测；【假设】= 未核证的前提，引用必须带条件。

**与既有稿的关系**：本稿不取代 `p2_0_throughput_certification_paradigm_design_v2.md`（下称 v2）。v2 是**一般吞吐**的验证范式（两层 TP7-S/TP7-D + 公理组 + 证书格式）；本稿是把 v2 **实例化到当前钉死的问题实例**后重算的可行性评估。第 6 节逐条给出 v2 的 T1–T6 在特化下变成什么。

---

## 0. 总判（先读这段）

立项动因是这样一条命题：`production_targets` 钉死 ⇒ 每种商品的稳态速率是常数 ⇒ 流量谓词退化成「use 变量挂常数系数的线性容量账」，一般吞吐认证的难点（流量与路径皆变量）随之塌缩。

**本稿的结论是：命题的前半段成立，后半段不成立；但塌缩的方向是对的，只是塌缩到的位置和预想的不同。**

三条实测结论：

1. **聚合速率确实是常数，但每台机器的占空不是**【实测】。19 种商品的稳态总流量唯一确定（表 A，`rate_table_receipt.json`）。但目标只钉死每个 operation 的**聚合**活动 `x_op`，台间怎么分是布局的自由度：219 台 recipe-backed 机器里 170 台被 `Σu = n` 钉死为满速，其余 49 台构成一个 **42 维满维多胞形**。表 A/B 里的逐台占空与逐口车道分解是**均摊约定下**的取值，不是唯一解（勘误来源：`refute_round1/REJUDGE_REPORT.md` §1）。F_route = 9,135 件/分钟与 flowbound 线的 OB1 收据逐数字吻合，这条与占空分配无关。

2. **「常数系数挂 use 变量」这个建模形态不成立**，死因有两层【实测】：
   - **第一层（无条件）**：端口速率 = 满速速率 × `u[i]`，而 `u[i]` 是 42 维多胞形里的自由变量。不先把占空分配固定成约定，「某格上某商品的速率」连**在端口处**都没有定义。
   - **第二层（固定占空后才轮到）**：它需要的不是逐口纯流而是**网络级**纯流，而网络级纯流恒不成立。**buckwheat 与 sandleaf 在任何合法占空分配下都必然分流**（种子回流环的奇偶性：N 条速率 1 的产道要喂两支各需 `⌈N/2⌉` 台的下游，N=11、21 皆奇数 ⇒ 耗道 N+1 > N），各逼出一段速率 ≤ 1/2；二者是不同中间品，两段之和 ≤ 1 ⇒ 速率合法的混流窗口恒存在。
   
   canonical `rate_lemma_scope` 的逐口纯流强制在**均摊约定下**成立（本稿独立复现，反例 0），但该约定不在它声明的前件里——换一份同样合法（且车道更省）的分配，反例 40 对。详见 `refute_round1/REJUDGE_REPORT.md` §3 定理 1/定理 2 与 §4。

3. **真正塌缩的位置是求解结构，不是约束形态**【推导】。混流窗口的位置随占空分配而变（均摊下开在被迫切细的细流段，阶梯下开在端口残道），但**恒不为零**。所以特化的收益不在纯度，而在：多商品流问题**分解**成「逐商品单商品流 + 格位打包」两层，而单商品流是整数的、其不可行证书是**最小割**（组合对象），不需要 v2 §4.3 那套有理 Farkas 基建。这个收益是真的，而且比原命题更稳——它不依赖任何前件。

**推荐**：走**丙案（双侧夹逼）**——上界/cut 侧用**与车道分配无关**的松弛，见证侧用受限族。理由与两个替代方案的对比见 §4。

---

## 1. 特化命题的精确陈述

### 1.1 什么塌缩了

设 `production_targets` 钉死（前提：canonical `production_targets`，valley_battery 3.0 条满速线 + qiaoyu_capsule 2.75 条）。则：

> **命题 S1（聚合速率唯一）**【实测】。17 条商品平衡方程对 17 个操作的聚合活动（机器当量 `x_op`）非奇异 ⇒ 每种商品的稳态总吞吐 `F_k` 唯一确定，与布局无关。

复算见 `rate_table.py`（Fraction 精确、高斯消元、零残差断言）。这条与 flowbound 线 OB1 的 [B] 步是同一条，本稿独立复算并与其收据互证通过。

> **命题 S2（聚合占空唯一，逐台分配自由）**【实测】。每个 operation 的**聚合**占空 `x_op` 唯一（`n_op` = mandatory 实例数 = `ceil(x_op)`，与 266 实例普查逐 op 相等）。**逐台占空不唯一**：任何满足 `u[i] ∈ [0,1]`、`Σ_i u[i] = x_op` 的分配都合法（含 `u = 0` 闲置，但本实例里 `n_op = ceil(x_op)` 使闲置无空间——少跑一台就够不到产量）。均摊 `u[i] = x_op / n_op` 只是其中一份分配，它的特殊性在于**最大化最小残道**，不在于唯一性。
>
> 自由度维数：219 台 recipe-backed 机器（266 减去 46 个 `boundary_io` 与 1 个 `protocol_core`）中，10 个 `x_op = n_op` 的 operation 共 170 台被钉死为满速；其余 7 个 operation 共 49 台仍是变量，受 7 条聚合等式约束，**净剩 42 维**（5+10+2+5+5+5+10）。端口 slot 速率随之**不是常数**，而是这 42 维的线性像。

19 商品的 `F_k`、219 台机器的占空、574 个端口 slot 的车道分解全部落在 `rate_table_receipt.json`——**其中逐台占空与车道分解是均摊约定下的取值**，换一份合法分配就变（见 `refute_round1/REJUDGE_REPORT.md` §1）。表 A 摘要（件/分钟，总流量与占空分配无关）：

| 商品 | 角色 | 流量 | 最小车道 | | 商品 | 角色 | 流量 | 最小车道 |
|---|---|---:|---:|---|---|---|---:|---:|
| blue_iron_ore | 外部源 | 1020 | 34 | | sandleaf_powder | 中间 | 945 | 32 |
| blue_iron_block | 中间 | 1020 | 34 | | sandleaf_seed | 中间 | 630 | 21 |
| blue_iron_powder | 中间 | 1020 | 34 | | source_ore | 外部源 | 540 | 18 |
| dense_blue_iron_powder | 中间 | 510 | 17 | | source_powder | 中间 | 540 | 18 |
| steel_block | 中间 | 510 | 17 | | dense_source_powder | 中间 | 270 | 9 |
| steel_part | 中间 | 180 | 6 | | buckwheat | 中间 | 330 | 11 |
| steel_bottle | 中间 | 165 | 6 | | buckwheat_powder | 中间 | 330 | 11 |
| fine_buckwheat_powder | 中间 | 165 | 6 | | buckwheat_seed | 中间 | 330 | 11 |
| sandleaf | 中间 | 630 | 21 | | qiaoyu_capsule | 终品 | 16.5 | 1 |
| | | | | | valley_battery | 终品 | 18 | 1 |

`F_target` = 9,169.5 件/分钟；`F_route` = 9,135（扣两个终品的交付流量，与 OB1 选定口径一致，**互证通过**）。

占空分布（合计 219 台 recipe-backed 机器）：

| 分配 | 分布 | 性质 |
|---|---|---|
| **被钉死的部分** | 170 台满速 | 与分配无关，`Σu = n` 逼出来的 |
| 均摊约定 | 另 49 台 = 27 台 11/12 + 22 台 21/22 | 42 维多胞形里的一个点；**最大化最小残道**的那个点 |
| 阶梯约定 | 另 49 台 = 42 台满速 + 6 台 1/2 + 1 台 3/4 | 另一个点；**车道总数最省**的那个点（全网 622 条 vs 均摊 628 条） |

本稿以下各节凡出现逐台占空或逐口车道数字，若无特别说明均取**均摊约定**；凡该数字承重之处已在勘误中标注约定依赖。

### 1.2 什么没塌缩

速率常数说的是**每种商品的总量**和**每个端口的量**。它**没有**说：某一格上某种商品的流量是常数。后者取决于流怎么分配——而流分配是变量。这正是一般吞吐问题的难点所在，钉死目标并没有消除它。

具体地，「use 变量挂常数系数」这个形态要成立，需要：

> **前件 L（网络级纯流）**：商品 k 的流在整条网络上只沿其**最小车道**走，任何一段的速率都等于某条最小车道的速率。

有了前件 L，「格 c 被 k 使用」就对应一个确定的速率 ρ_k，行才写得成 `Σ_k ρ_k·u[c,ℓ,k] ≤ cap`。**§2.4 证明前件 L 对本实例不可满足。**

### 1.3 与 canonical 的关系（先说清，避免误读）

canonical `semantics.rate_lemma_scope` 的断言是**逐口车道**层面的，本稿独立复现并确认成立（§2.1）。该条目自己声明 `predicate_status: "non_predicate"`、且 `usage_rule` 明确「rate arithmetic never enters a certificate」——它管的是**叙述层**怎么引用速率算术，不是模型约束。

**本稿不推翻 canonical 的任何条目。**本稿否定的是一个**外推**：从「逐口车道两两不可共道」推到「每格至多一种中间品」。这个外推没有写在 canonical 里，但它是「常数系数挂 use 变量」建模形态的隐含前提，所以必须被检验。§2.5 给出建议的措辞加固（可选，非必须）。

---

## 2. 速率引理的作用域：本稿的实测边界

### 2.1 逐口纯流强制【实测，复现】

`rate_table.py` 独立复算逐口车道分解（口速率 r、车道数 `ceil(r/cap)`、前 `L−1` 条满载、末条残余），得到 8 个不同的车道速率。

**口径说明**：下表覆盖的是 **574 个制造机端口 slot**（从 `recipes` 逐条展开），与 `flow_account.json` 的「574 制造端口」、MEMO §2.1 的 `ceil(rate)` 展开三方吻合。**外部边界源的 52 个 slot**（34 blue_iron_ore + 18 source_ore，来自 `boundary_io` 与 `protocol_core` 的 generic output）不在 `recipes` 里、不在下表内；它们各为 1 件/tick 满带宽，需要时另加。

| 车道速率 | 条数 | 商品 |
|---|---:|---|
| 1 | 464 | 17 种（全部中间品与外部源） |
| 21/22 | 22 | sandleaf |
| 11/12 | 30 | buckwheat, fine_buckwheat_powder, sandleaf_powder, steel_bottle |
| 10/11 | 11 | sandleaf_seed |
| 19/22 | 11 | sandleaf_powder |
| 5/6 | 30 | buckwheat_powder, buckwheat_seed, fine_buckwheat_powder, steel_block, steel_bottle |
| 1/5 | 3 | valley_battery |
| 11/60 | 3 | qiaoyu_capsule |

两两共道合法（和 ≤ 1 件/tick）的对**只有 3 对**，且两侧商品全部是终端成品：`11/60+11/60`、`11/60+1/5`、`1/5+1/5`。**涉及中间品的合法共道对 = 0**，与 canonical `rate_lemma_scope` 记的机器复算结论一致。

### 2.2 车道约定无关性（对 canonical 的一条加固）【推导】

canonical 的前件 (ii) 只规定车道**条数**最小，没有规定这些车道之间怎么分配速率。仓库里实际存在两种约定，给出不同的数字：

- **fill-first**（`docs/research/canonical_batch_20260807/rate_lemma_recompute.py:42-44`）：前 `L−1` 条满载、末条取残余 ⇒ 满带宽车道 **464** 条；
- **均分**（`.artifacts/p2_0_refresh_20260805/area_bound_work/ob5_slot_census.py:65`，MEMO §2.1 与其 401/453 口径的来源）：每条取 `r/L` ⇒ 满带宽 slot **401** 个。

两个数字建在同一个基数上（均分口径报的 568 = 574 − 6 个终品输出 slot，而那 6 条道速率是 11/60 与 1/5、在两种约定下都不满带宽，所以 464 与 401 可直接比）。**两个数字都对，只是约定不同。**

**引理不受影响**，理由是两行的算术：

> 设某口速率 r，最小车道数 `L = ceil(r/cap)`，车道速率 `a_1..a_L` 满足 `Σa_i = r`、`a_i ≤ cap`。则
> `min_i a_i = r − Σ_{j≠i} a_j ≥ r − (L−1)·cap` = fill-first 的残余值。
> 即 **fill-first 恰好取到任何最小车道分配下单条车道速率的下确界**，是引理的对抗最坏情形。

所以逐口纯流强制在**任何**最小车道分配下都成立，不只是在 fill-first 下。canonical 收据用 fill-first 是正确的选择（虽然条目本身没写出这个理由）。

### 2.3 「终端成品段可混流」需要一处精度修正【实测】

canonical `rate_lemma_scope` 写「the only lanes on which commodity mixing remains rate-legal are the final-product terminal segments (qiaoyu_capsule + valley_battery, 2-4 lanes into wired warehouse-line inputs)」。

实测：两条终品的**全流**车道之和 = 11/20 + 3/5 = **23/20 > 1**，**一格装不下**。可混流的是**未完全汇聚**的子段：产口侧 3 条 11/60 + 3 条 1/5，任取速率和 ≤ 1 的子集可以共道（最多 5 条：3×11/60 + 2×1/5 = 57/60）。

所以准确措辞是「终端成品段中**所载子速率之和 ≤ 带容量**的那些段」，不是整条终端段。这不影响 canonical 的任何下游结论（该条目本身是 non_predicate），只是引用时别读成「终品段随便混」。

### 2.4 网络级纯流**不成立**（本节是本稿最承重的一条）【实测】

前件 L 要求商品的流在中段不被切细。问题是：**切细是不是可以避免？**

**探针一（`split_free_probe.py`）**：逐商品判定「只合流、不分流」的车道路由是否存在。建模为每条产道**整条**指派给某条耗道（允许多条并入一条），车道速率自由但口内守恒、每条 ≤ cap；CP-SAT 整数精确。

**该探针把每台机器的占空写死成均摊（`split_free_probe.py:97`），这是未经辩护的约定**；放开台间分配后重判（`refute_round1/split_free_probe_v2.py`），六例里**四例翻案**：

| 商品 | 均摊约定下 | 放开占空后 |
|---|---|---|
| **buckwheat** | 鸽巢 11 产道 < 12 耗道 | **仍必然分流**，对任意分配成立 |
| **sandleaf** | 鸽巢 21 < 22 | **仍必然分流**，对任意分配成立 |
| steel_block | 鸽巢 17 < 18 | **翻案**：耗道降到 17，split-free 存在 |
| buckwheat_seed | CP-SAT INFEASIBLE（12 / 11） | **翻案**：产道降到 11，split-free 存在 |
| sandleaf_seed | CP-SAT INFEASIBLE（22 / 21） | **翻案**：产道降到 21，split-free 存在 |
| sandleaf_powder | CP-SAT INFEASIBLE（33 / 32） | **翻案**：产道降到 32，split-free 存在 |

必然分流的商品占路由流量因此从 3,375 / 9,135 = 37% 降到 **960 / 9,135 = 10.5%**。存在一份整网一致的占空分配（阶梯式：能满速的满速、余数落最后一台），使 19 种商品里 **17 种同时 split-free**；计数下界与该见证吻合，故 17 是上确界。

**站住的两例有不依赖求解器的计数证明**（`REJUDGE_REPORT.md` §3 定理 1）：buckwheat 的 planter 侧 `x_op = n_op = 11` ⇒ 占空被唯一钉死为全 1 ⇒ 恰 11 条速率 1 的产道；耗侧 crusher 与 seed_collector 各需 `Σu = 11/2` ⇒ 各至少 `⌈11/2⌉ = 6` 台在跑、每台恰 1 条耗道 ⇒ 12 条。11 < 12。根因是**种子回流环的奇偶性**：采种机 1 进 2 出、种植机 1 进 1 出 ⇒ 采种支恰占产量一半，奇数 N 劈半后两支各需 `⌈N/2⌉` 台，合计 N+1 > N。sandleaf 是 21 vs 22，同构。这条与占空分配、与布局都无关。

**翻案的四例同样可手算**：例如 steel_block，若 6 台 molding_bottle 取「5 台满速 + 1 台半速」而非均摊的 6 台各 11/12，则满速台每台吃 2 件/tick = 2 条满道、半速台吃 1 件/tick = 1 条满道，共 11 条；加 parts_maker 的 6 条，耗侧恰 17 条速率 1 的道，与产侧 17 条完美匹配。**鸽巢是均摊约定制造出来的，不是实例固有的。**

**探针二（`maxmin_segment_probe.py`）**：既然分流不可避免，那被迫的细流段最厚能做到多厚？求 `m_k` = max over 路由 min over 段（端口级运输问题 + 车道数变量，对下界二分，CP-SAT 整数精确）。

| 商品 | m_k | | 商品 | m_k |
|---|---|---|---|---|
| buckwheat | 1/3 | | sandleaf_powder | 14/33 |
| buckwheat_seed | 1/3 | | sandleaf_seed | 4/11 |
| steel_block | 1/3 | | sandleaf | 7/22 |

其余 11 种中间品 m_k ∈ {5/6, 11/12, 1}。

**关键的逻辑**：`m_k` 是 max-min，所以**每一个**路由都至少有一段速率 ≤ `m_k`。于是对任意两种商品 j,k，只要 `m_j + m_k ≤ cap`，就不存在任何路由能从速率上排除它们共道。均摊约定下这样的**不同中间品对有 15 对**（另有 6 对同商品，属同商品拥塞不属混流）。

**上表同样是均摊约定下的值。**放开占空后（`REJUDGE_REPORT.md` §3）：翻案的四种商品不再有被迫细流段；站住的两种最厚的最细段都升到 **1/2**（分流点各从 13、23 个降到 1 个），而 1/2 是**任何**分配下的上界——被劈开的产道速率恰为 1，各段总和恰为 1，最细一段必 ≤ 1/2。

**结论（升格为无条件命题）**：前件 L 在本实例上不可满足，纯流强制无法升格为网络级模型约束——而且这一条现在**不依赖任何占空约定**：

> buckwheat 与 sandleaf 在任何合法分配下都必然分流，各逼出一段 ≤ 1/2；二者是不同中间品，两段之和 ≤ 1 = 带容量 ⇒ 速率合法的共道窗口恒存在。

**两个端点都破，没有第三条路**：往均摊靠，端口残道最厚（5/6，安全）但中段细流最细（7/22，破）；往阶梯靠，中段细流最厚（1/2）但端口残道掉到 1/2（破）。全网最细段在所有分配上的可达最大值恰为 1/2，而纯流强制需要它严格 > 1/2。混流窗口对数随分配变（均摊 15 对、阶梯 10 对），但**恒不为零**。

因此严格读法（「车道条数处处最小」）的前件族仍然是空的，但空得比本节初稿说的少得多——只差 buckwheat 与 sandleaf 两种商品、各一个分流点、各两段 1/2，而不是六种商品。宽松读法（「只在端口处最小」）下中段稀释不受约束，结论不变。

### 2.5 建议的 canonical 措辞加固（可选，非本批必做）

**本节的定位已由勘误反转**：`rate_lemma_scope` 需要的不再是「可选加固」，而是**必办的措辞修正**——该条目的残道速率集合是在均摊约定下算的（`docs/research/canonical_batch_20260807/rate_lemma_recompute.py:36`），而这条约定不在它声明的前件 (i)(ii) 里。更硬的一点：前件 (ii) 的字面是「用最少车道」，而均摊对 5 种商品用了**多于**其速率所需的车道（全网 628 条 vs 阶梯 622 条 vs 下确界 616 条）——**最满足前件 (ii) 的那份分配，恰是破掉结论的那份**。前件与结论互斥，不是补一条前件能了的。

处置：**本批不动 canonical**，挂下一批 canonical freeze-ritual。可直接取用的措辞 diff（两个方向的 `statement` 原文）见 `refute_round1/REJUDGE_REPORT.md` §4，其中推荐**方向 b**（改成分配无关版本：逐口纯流强制标成均摊下的条件式结论，另把无条件成立的定理 1/定理 2 写进去）。

原拟的两条加固仍然有效、可并入同一批：① fill-first 是最小车道分配下的下确界，故引理与**车道速率**约定无关（§2.2；注意它与**占空**约定无关性是两回事，后者不成立）；② 终端成品可混流的是子速率和 ≤ 带容量的段，不是整条终端段（§2.3）。

---

## 3. 约束族清单

先把建模底座回源钉准（**本稿逐行回源，不引用他稿的行号**，`src/models/routing_subproblem.py` 主仓 main）：

- `use_vars` 的键是 `RouteStateKey = (x, y, layer, flow_in, flow_out, commodity)`——**已经是 per-commodity**（`:770` 声明、`:1053-1058` 创建）。
- `phys_vars` 的键去掉 commodity、换 `component_type`（`:1030-1042`）；`use_var ≤ phys_var`（`:1066`）；`AddMaxEquality(phys_var, use_vars_on_it)`（`:1075-1076`）——**同一物理组件可被多商品共享**，这是当前语义。
- 容量约束 `_add_capacity_constraints`（`:1119-1122`）= `AddAtMostOne(phys_vars_on_cell_layer)`，即**每 (cell, layer) 至多一个物理状态**，纯空间互斥、无时序维度。
- 端口贴合 `_add_port_adherence`（`:1297`）：每个 active 端口 `sum(vars_for_port) == 1`（exact-one）。

这意味着：**加吞吐谓词不需要新变量结构**，per-commodity use 变量与 physical/commodity 的两层已经在那里。以下四个行族都建在现有变量上。

### P1 —— 每物理状态的商品纯度行（**受限族专用**，不可无条件加）

```
对每个 phys_key：AddAtMostOne(_phys_uses[phys_key])
```

形态上就是把 `:1075-1076` 的 `AddMaxEquality` 旁边再加一行 `AddAtMostOne`，作用于同一批 `_phys_uses[phys_key]` 列表。系数全 1，纯组合，**零有理算术**。

- 行数：每个物理状态一行 = `len(phys_vars)`；上界 = 活跃 free cell × layer × (flow_in, flow_out, component_type) 组合数。
- **soundness 警告**：由 §2.4，这一行族**不是**对全部布局有效的松弛，而是**限制**。它只能进见证侧（丙案的下界半边），**绝不可进上界/cut 侧**，否则会剪掉真可行解、破坏 lex 最优性。

### P2 —— 每物理状态的容量行（**无条件有效**）

```
对每个 phys_key：Σ_k φ[phys_key, k] ≤ cap        （φ 为有理流变量）
对每个 (phys_key, k)：φ[phys_key, k] ≤ cap · use_var[…, k]
```

这是诚实版本：**流是变量**，不是常数系数。系数是 1 与 cap，变量是新引入的 `φ`。

- 行数：容量行 = `len(phys_vars)`；耦合行 = `len(use_vars)`。
- 有理化：全部车道速率的公分母 = **660**【实测】；乘 660 后全部系数为整数，可直接进 CP-SAT 整数域（若走 CP-SAT），或保留有理走 LP（若走 v2 的 Farkas 路线）。
- 这一族对**任何**布局有效，是丙案的上界半边基石。

### P3 —— 逐商品单商品流可行性（**特化的真正收益所在**）

在 P1 成立的受限族里，每个物理状态只属于一种商品 ⇒ 多商品流**分解**成 19 个独立的单商品流问题：

```
对每商品 k：在「分配给 k 的物理状态集合」上，
            从 k 的产口 terminal 到耗口 terminal 存在容量 cap 的可行流，流量 = F_k
```

- 数量：19 个子问题（扣终品则 17 个）。
- **为什么这是收益**：单商品流的容量与需求在乘公分母后全整数 ⇒ 由最大流最小割定理，LP 松弛的最优解整数，且**不可行证书就是一个最小割**——一个组合对象（一组格位 + 一条计数不等式），天然 exact-safe，不需要 v2 §4.3 的有理 Farkas 规范形、row-family manifest、`constraint_id` 重建那一整套。这是特化相对一般吞吐真正省下来的东西。
- 代价：P3 的合法性依赖 P1，而 P1 是受限族。所以 P3 是**见证侧**工具，不是上界侧工具。

### P4 —— 汇流点与箱口

- **汇流点（merger）**：canonical 的 merger 是 2-3 入 1 出的单格器件，其出边受同一条 P2 容量行约束，**不需要额外行**。owner 给的「汇流器 2s CD、双入满速竞争最坏减半」是 tick 层的仲裁语义，属 v2 的 A8/TP7-D 层，静态层用聚合容量行已经保守覆盖（聚合和 ≤ 1 件/tick 比「最坏减半」更松还是更紧，取决于仲裁模型——**列为开放问题 Q6**）。
- **协议箱口**：canonical `protocol_storage_box_wireless` 定谳箱有 3 个物理输入口、每口 30 件/分钟 ⇒ **箱的物理进料上限 90 件/分钟**；无线段是箱→仓库、每 10 秒清空 6 个缓存槽，不构成进料侧瓶颈。`slot_count_clause` 的 6 槽阻塞是**离散动力学**（槽占满即堵，与商品类型数无关），属 TP7-D 层，静态层不表达。
  - 静态层需要的箱口行：`Σ_k φ[箱输入口 p, k] ≤ port_cap`，即 P2 的端口特例。
  - **当前实例里箱口不承重**：两个终品的需求各 1 个 generic input slot（`generic_io_requirements.json`），流量 16.5 + 18 = 34.5 件/分钟，远低于 90。

### 行数量级小结（生产规模 266 实例、70×70×2 层）

| 行族 | 行数量级 | 系数 | 有效性 |
|---|---|---|---|
| P1 纯度 | `len(phys_vars)`（≤ 9,800 格-层位 × 组件形态数） | 全 1 | **仅受限族** |
| P2 容量 | `len(phys_vars)` + `len(use_vars)` | 1 / cap（公分母 660） | 无条件 |
| P3 单商品流 | 19 个子问题 | 整数（乘 660） | 仅受限族 |
| P4 箱口/端口 | 端口数量级（574 制造口 + 52 源口 + 2 终品汇口） | 1 | 无条件 |

`len(use_vars)` / `len(phys_vars)` 的真实规模由 `_record_state_space_stats`（`:1080` 定义、`:1099-1100` 计数）在运行期报出，本稿未跑生产规模 build，**故上表只给上界形式、不给实测数字**——列为开放问题 Q1。

---

## 4. 前件缺口：三案对比与推荐（本稿核心）

问题的形式：认证要对**全部可行解**说话（lex 最优性要求「任一更优候选必不可行」），而速率引理带前件。不满足前件的解怎么办？

先把方向摆正——**两个方向的代价完全不对称**：

- 往**紧**里做（加限制）：只会漏掉真可行解 ⇒ 报出的最优值**偏小** ⇒ 见证仍然真实可行，但「这是全局最优」的断言**失效**。
- 往**松**里做（用松弛）：只会多算一些不可行解 ⇒ 报出的上界**偏大** ⇒ 上界仍然真实，但不够紧。

认证链要的是两边都有：一个真可行的见证 + 一个不小于它的上界。**只要两者相遇，最优性就闭合，前件问题自动消失。**

### 甲案：把前件写成模型约束（限制可行域）

加 P1 纯度行，强制解落在网络级纯流族内。

- **优点**：便宜到不像话——P1 是现有变量上的一行 `AddAtMostOne`，零有理算术；且它顺带把多商品流分解成 P3 的 19 个单商品流，不可行证书退化成最小割。
- **原判「该族对本实例是空的」已撤回**（勘误）。那一步是非蕴含：P1 说的是「每个物理状态只承载一种商品」= 禁止**混流**，而 §2.4 证的是「中段稀释不可避免」= 存在**细流段**。分流器下游的两条带各自仍只跑一种商品，每个物理状态仍只承载一种商品——**分流不违反 P1**。正确的蕴含方向是「网络级纯流 + 速率引理 ⇒ P1」，反过来不成立。已回源核实：`phys_key = (x, y, layer, flow_in, flow_out, component_type)`（`routing_subproblem.py:1031-1038`）不含 commodity 维度，同层两条带共格另由 `_add_capacity_constraints`（`:1119-1122`）挡住，跨层交叉 layer 不同故不触发 P1。
- **P1 族空不空是个几何问题**（19 种商品能不能在 70×70×2 层上路由到从不共用同一个物理组件），**本稿与勘误批都未测**。所以甲案不是死路，而是**欠两条未证支配义务**（下）+ 族空性未知。
- **可修版本**（若族确实为空时才需要）：把 P1 放宽成「只在能承载最小车道速率的段上要求纯度」，即对每个物理状态附一个「是否细流」的判别。但那要引入流变量，就退回 P2，不再有「常数系数」的便宜。

> **义务 OB-D（最小车道支配引理，未证）**：对任何满足六谓词 + 吞吐的布局，存在一个在受限族内、且 `lex(area, min_side)` 不更差的布局。
> 直觉支持：稀释一种商品需要 splitter + 侧枝再 merger 收回，只增格位不减；而聚合格位账 `Σ_c Σ_k φ ≤ cap·|cells|` 与混流与否无关（容量守恒），所以稀释拿不到聚合上的好处。
> 直觉**不构成证明**：稀释改变的是几何可行性（哪条道能拐到哪里），不是聚合量；把一个稀释布局「收拢」回受限族需要一个保面积的重路由构造，本稿没有。

> **义务 OB-D2（占空分配支配，未证）**【勘误新增】：对任何合法占空分配下的可行布局，存在一个采用指定分配（如均摊或阶梯）、且 `lex(area, min_side)` 不更差的布局。
> 为什么它是新的一条：常数系数形态要求端口速率是常数，而端口速率 = 满速速率 × `u[i]`，`u[i]` 是 42 维多胞形里的自由变量（§1.1 命题 S2）。**「选定一份占空分配」本身就是一条前件**，不是目标钉死的推论。
> OB-D2 不是 OB-D 的特例：OB-D 谈「不稀释」，OB-D2 谈「按哪份占空跑」，两者的反例构造方向不同。
> **这两条是甲案的未闭合项；丙案两条都不需要。**

### 乙案：谓词限定在前件族内 + scope 声明

照 `semantics.port_commodity_scope` 的先例：认证命题声明在「最小车道分配」这个游戏片段上，模型可行域 ⊆ 裁定规则可行域，保守正确。

- **优点**：零新数学，有现成先例，措辞机制成熟。
- **原「问题一：会把命题声明在空集上」已撤回**（勘误，同甲案）——那条建立在「族已证为空」的错判上。
- **真正的问题**：发布口径会退成「该族内最优」。这与 owner 已定的「zmd 目标不设降级退路」（全局 `max_lex` certified 无退路）冲突，属于 owner-only 拍板项。
- **附带代价**（勘误新增）：受限族现在还得把「占空按某份指定分配」写进 scope（见 OB-D2），而这条前件在 certified 语境下 discharge 不了，声明面比原设想更宽。
- **定位**：乙案不是当前可用方案；它的价值是**兜底措辞模板**——若丙案的夹逼长期合不拢，乙案是诚实降级的形式。

### 丙案：双侧夹逼（**推荐**）

把 P7 拆成两个**分别 sound** 的半边，各自不需要前件：

```
上界半边（松弛，对全部布局有效）：P2 容量行 + 聚合格位计数
                                  ——不含 P1，不含任何车道分配假设
下界半边（见证，充分条件）：任何能构造出真可行见证的受限族
                            ——可以用 P1+P3，也可以用别的启发式族
闭合判据：上界 = 下界 ⇒ 全局 lex 最优性成立，无需 OB-D 支配引理
```

- **为什么这是对的**：上界半边是松弛 ⇒ 它给的 `A ≤ U` 对全部布局成立；下界半边给的见证是真可行的 ⇒ `A* ≥ L`。若 `U = L`，则 `A* = U` 是全局最优，**受限族是否覆盖全部解完全不重要**——它只是找见证的手段。
- **上界半边已经有现役件**：flowbound 线的 `A ≤ 1167`（无条件）正是 P2 的聚合特例（`docs/research/p2_0_area_bound_20260806/AREA_BOUND_THEOREM_REPORT.md` 定理 1）。它的证明链只用了「每件过 ≥1 个 route state」+「每 state ≤ 30 件/分钟」+ 格位分账，**零车道分配假设**。§8 展开。
- **下界半边可以先用最便宜的族**：直接加 P1+P3 试构造——**勘误后这一步没有已知障碍**（分流不违反 P1，见甲案；族空性未测）。构造不出来再放宽到「允许分流但限制分流点数」等族。**下界侧的族选择是纯工程自由度，选错了不损 soundness，只是夹不拢。**
- **代价**：要维护两个模型；夹不拢时给不出结论（`UNKNOWN`，fail-closed，与现有纪律一致）。

**推荐理由（一句话，勘误后重写）**：丙案是唯一一个**零前件**的路线——上界半边对全部布局有效、下界半边选什么族都不损 soundness，因此不需要任何支配引理就能保住全局 lex 最优性；甲案便宜，但欠两条未证支配义务（OB-D、OB-D2）且族的空性尚未测过；乙案撞 owner 的无降级退路口径，且要把一条 discharge 不了的占空前件写进 scope。

**这条推荐不因勘误而动摇**：丙案的优点是「不需要 OB-D」，而勘误只增加了甲乙两案的义务（新增 OB-D2）、没有给丙案增加任何义务。上界半边的五步链逐条与占空分配无关（[B] 只用聚合 `x_op`，[C] 只用聚合 `F_route`），§8 已逐条核过。

**给 owner 的选择点**：丙案不需要 owner 拍板（它不改 scope、不降级命题）。真正需要 owner 拍板的只有一件事——**若夹逼长期合不拢，是否接受乙案式的降级发布**。建议现在不定，等丙案跑出第一轮夹逼间隙的数字再定。

### 一条重要的陷阱（写给 flowbound 线）

本稿表 D 给出一个诱人的收紧：若每格只有一种商品，则 `L ≥ Σ_k ceil(F_k / C) = 308`（扣终品 306），比现役的聚合 `L ≥ ceil(F_route/C) = 305` 更紧，单层口径可从 `A ≤ 1015` 收到 `A ≤ 1012`。

**不要把它加进无条件上界链。**它依赖 P1（每格单商品），而 P1 是**限制**不是松弛（§2.4）——把限制推出的界当无条件上界，就是把「我们搜的族内没有更好的」错说成「不存在更好的」。这条只能进「受限族内的上界」台账，与无条件的 1167 分开记。

---

## 5. 与六谓词的关系

### 5.1 P7 的语义位置

沿用 v2 的接口：谓词 (1)-(6) 不动，新增 **P7 = 吞吐可行**，`P′ := P ∧ P7`，目标 `max_lex(area, min_side)` 不变。

严格空地（owner 2026-08-05 裁决）已挂在谓词 (5) 上（`PROJECT_LOCK.md:77-95`），所以 P7 的输入 `selected_route_states` **天然已过严格空地检查**，P7 不需要再加 ghost 排除逻辑。

单调性方向与 v2 §2.5 一致：`P′`-可行 ⇒ `P`-可行 ⇒ 全部「候选不可行」的旧证据保持 sound；全部旧 `CERTIFIED` 结论降级待定。

### 5.2 `PROJECT_LOCK.md` §1A 修改面

| 位置 | 现状 | P7 落地后 |
|---|---|---|
| §1A A 块谓词清单（`:53-107`，谓词 (1) 起于 `:58`、lex 最优性 `:105-107`） | 6 条 gating 谓词 | 增第 7 条；需写明 P7 的精度边界（证什么、不证什么），与谓词 (4)(5)(6) 的「精度边界」段同格式 |
| §1A A 块谓词 (5) 红线（`:93-95`） | 「routing FEASIBLE ≠ 吞吐可行」 | 改写：连通仍归 (5)，吞吐归 (7)；红线从「不证吞吐」改成「(5) 不证吞吐，(7) 证的是**哪一层**吞吐」 |
| §1A B-1（`:143-148`） | 物料离散吞吐 / belt 带宽容量未证；flow 子问题锁成 diagnostic-only | 整条重写。注意 `flow_subproblem.py` 的 diagnostic-only 锁**建议保留**——P7 应建新模块（§9），不改造它，理由同 v2 §6 |
| §1A B-2（`:149-156`） | 离散容量流是 open research problem、无 cut family 表达 | 改写为「已有 P7 表达」+ 保留仍未覆盖的部分（离散动力学 / 启动可达性） |
| §1A B-3 第 ③ 条（`:157-160`，③ 在 `:159-160`） | 物料离散吞吐充分性「未 certified」 | 改为已 certified，并写明是哪一层（静态平均层 vs 离散周期层） |
| `:215` | 「吞吐、belt 带宽和离散容量流仍明确 OUT-OF-SCOPE」 | 同步改写 |

### 5.3 freeze-ritual 面清单

P7 落地属 sealed-file 连锁，按仓库既有 reseal 铁律（pin sha 按 LF 字节算、绝不用 Python 直写 tracked 文件、提交 pathspec 覆盖全集）：

1. `rules/canonical_rules.json`——若 §2.5 的措辞加固被采纳，或新增 `throughput_semantics` 块（v2 §6 要求的 frozen artifact）。**改它 = 全套 freeze-ritual**（pinned hash 更新 → 依赖产物重生成 → gate 重跑）。
2. `PROJECT_LOCK.md`——§5.2 全部条目；注意 LOCK 有 sha 继承链（3 个测试 pin + 3 个 D6 脚本常量 + antecedent 重算重钉），漏一处会潜伏到全量门才炸。
3. close-kernel sealed 面：V99 floor 常量、`check_p1_2_proof_obligations.py` 的 obligations JSON、`check_strong_status_write_allowlist.py` 的 allowlist，**checker 自钉最后算**。
4. schema 升版（全部 fail-closed 面）：`candidate_proof`、fixed-witness verdict stable fields、capsule request/response、`final_result` 白名单、delivery manifest、proof_summary。
5. `EXACT_*` env 白名单增补（P7 子问题的 worker / 时限旋钮）——闭合白名单 deny-unknown，新 knob 必须同时更新 allowlist/lock/tests。
6. 文档同步：`README.md`、`docs/项目说明/01_overview.md` §1.1/§1.3（谓词权威）、`specs/01/08/09`、`NAV_MAP.md`。

**排期依赖**：v2 §6 最小落地路线第二步（terminal 输出 `selected_route_graph_digest`）已不再被严格空地修复批阻塞（该批已落地，`f16a22c`），可以排。

---

## 6. 与 fluid P7（v1/v2）的关系：T1–T6 在特化下变成什么

**本稿是 v2 的实例化，不是推翻。**逐条映射：

| v2 约束 | 一般形态 | 特化后 | 变化性质 |
|---|---|---|---|
| **T1 守恒** | 每商品每非终端节点 `Σin φ = Σout φ` | **不变**，仍是变量流的守恒 | 无变化。特化没有消掉流变量（§1.2） |
| **T2 组件容量** | 每 selected physical state `Σ_k through(φ,s) ≤ cap` | **= 本稿 P2**。定义域缩小（严格空地后孔内格不再是候选 state），约束形式不变 | 无变化 |
| **T3 端口容量** | `0 ≤ r[p] ≤ port_cap`，route-visible 口的 `r[p]` 等于 terminal arc 流量 | **降维，不常数化**：`r[p] = c_p · u[i]`，`u[i]` 仍是变量（S2），所以 `r[p]` 不是已知常数，而是 42 维占空多胞形的线性像。真正常数化的只有**每 operation 的端口速率之和** | **部分塌缩**。约束仍在，定义域降到 42 维 |
| **T4 机器耦合** | `Σ_p r[p] = u[i]·inputs/tpc`，`u[i] ∈ [0,1]` 变量 | **降维，不消失**：`u` 族真实基数 **219**（266 减 46 个 `boundary_io`、1 个 `protocol_core`，它们无配方无占空）。其中 170 个被 `Σu = n` 钉死为 1，49 个仍是变量、受 7 条聚合等式约束 ⇒ **净剩 42 维** | **部分塌缩**。`u` 族 219 → 42 维 |
| **T5 目标满足** | `Σ r[p] = target_rate(t)` | **被吸收，不消失**：它自动满足不是因为常数化，而是因为它**就是** T4 留下的那 7 条聚合等式 `Σ_i u[i] = x_op`（本稿 `rate_table.py` 的零残差断言已复算这些等式的解） | **形式塌缩**。整族并入 T4 的聚合等式 |
| **T6 边界供给 / routing-free 汇平衡** | 外部源口 ≤ cap、routing-free 汇平衡 | 源口 52 个恰好打满（34 blue_iron_ore + 18 source_ore，需求数 = 供给数、零冗余）⇒ **全部取等**，退化成常数 | **真塌缩** |

**总结（勘误后重述）**：T6 因目标钉死而完全常数化；T3/T4/T5 三族**降维但不消失**（T5 并入 T4 的聚合等式）；**T1/T2 两族原样保留**——它们正是「流分配是变量」的部分，也正是难点所在。所以特化的净收益不在约束数量，而在：

1. **变量数**：`u[i]` 族从 219 个自由变量降到 **42 维**（170 个钉死为 1，其余 49 个受 7 条聚合等式约束）；`r[p]` 全族（574+52+2 个）不再自由，而是由这 42 维参数化。相对 v2 的一般形态（`u` 与 `r` 完全自由）仍是大幅塌缩，但不是整族清零。
2. **求解结构**：在受限族内，T1/T2 分解成 19 个单商品流（本稿 P3），不可行证书从有理 Farkas 降级为**最小割**——这是 v2 §4.3 那套 `lp_digest` + row-family manifest + `constraint_id` 重建基建的**替代品**，工程量小一个量级。
3. **未被特化触及**：v2 §2.3 的 TP7-D 离散周期层、§3 的公理组 A1–A13、v3.1 的启动三件套（bootstrap manifest + startup trace + 周期日历）**全部原样需要**。钉死目标不消解任何一条离散语义鸿沟。

---

## 7. 与 mixflow / U-01 / 箱口的关系

**说明**：本节只用主仓已落地的 canonical 裁定与 `.artifacts/` 里的设计摘要，**不依赖未合并分支的代码细节**；下面的结论对「聚合 use 变量」与「per-commodity 子图」两种 use 结构都成立（本稿 §3 已确认主仓当前就是 per-commodity，`:1053-1058`）。

### 7.1 共乘容量行正是速率引理场景

mixflow 线处理的是「一条带上混载多商品」的建模。canonical `mixed_commodity_flow` 允许带级混流，`terminal_clause` 把它三分：(1) 有线仓储口（core 14 个输入）无限吸收；(2) 协议箱 6 槽有界；(3) 机器输入口无选择性 ⇒ 混流终止在机器口**不安全**。

本稿的贡献是给这条裁定配上**速率侧的边界**：

- 混流在**主干**上（464 条满带宽车道 + 全部残道）**速率上不可能**——这是逐口纯流强制（§2.1）。**但这条只在均摊约定下成立**（勘误）：`maxmin_segment_probe.py` 复用 `split_free_probe.py` 的 `solve_duty()`，同染均摊硬编码。换一份合法分配（阶梯），半速机器的端口残道就掉到 1/2，混流窗口直接开在**端口**上。
- 混流在**细流段**上速率合法（均摊约定下 15 对窗口；阶梯约定下细流段升到 1/2、窗口 10 对，但改开在端口残道）。

所以给 mixflow 线的**无条件**收窄只剩一句：**混流窗口的段速率永远 ≤ 1/2**（因为全网最细段在任何分配下都 ≤ 1/2，`REJUDGE_REPORT.md` §3 定理 2），而**涉及哪些商品、开在主干还是细流段，随占空分配而变**。原稿「混流只可能发生在分流细流段」是均摊约定下的结论，不可无条件外送。窗口恒不为零这一点则是无条件的。

de-mix 的危险性判断不变（机器口无选择性，错货进缓存槽即永久占位，kernel A9 + `terminal_clause` 类 (3)）——变的只是「需要防的场景在哪里」不再可以按均摊约定枚举。

### 7.2 U-01 准入口的速率前提如何闭合

canonical `item_admission_port_exclusion.rationale_restated` 已把准入口建模必要性裁为**零**，理由三条，其中 (a) 直接引用 `rate_lemma_scope`：「中间品在钉死满产下不能合法共道，所以中间道上没有东西可分拣」。

**本稿 §2.4 对这条理由构成一个精确的边界**：该理由在**均摊约定下的逐口车道**层面成立，在细流段层面不成立——细流段上确实可能出现两种中间品共道，也就确实可能需要分拣。**勘误进一步收窄**：换一份合法占空分配，逐口车道本身就可能只有 1/2，共道在端口处即合法，所以「逐口层面成立」也是约定相关的。

**但裁决结论不受影响**，因为它有另外两条独立支撑：(b) 唯一合法混流域（终端段）终止于无拒收的有线仓储口，不需要分拣器；(c) 分拣终端定理（kernel 推导 #21）——分拣结构只有在每条侧枝终端都吸收得下时才 sound，而有非拒收终端可用时分拣就不必要。(b)(c) 都不依赖速率算术。

**建议动作（勘误后调整）**：不改裁决，但在 P2.0 实现批里把 (a) 的措辞从「中间道上没有东西可分拣」精确到「**在均摊占空约定下**，中间品的最小车道之间没有东西可分拣」——占空条件必须写进去，因为最小车道本身在别的合法分配下就只有 1/2。并挂本稿 §2.4 与 `REJUDGE_REPORT.md` §3 作为边界注记。**这是文档级精度补丁，不是裁决翻案**（裁决靠独立的 (b)(c) 两条，不依赖速率算术）。——列为开放问题 Q7。

### 7.3 箱口

见 §3 的 P4。当前实例里箱口不承重（终品流量 34.5 件/分钟 vs 箱物理进料上限 90 件/分钟），`slot_count_clause` 的 6 槽阻塞属离散动力学、静态层不表达。**箱口成为瓶颈的唯一路径是 `production_targets` 上调**——若未来上调，箱口行（P4）会从冗余变成 gating，届时需重算。

---

## 8. 与 flowbound 面积上界线的关系

**flowbound 线的产物就是丙案的上界半边。**

`docs/research/p2_0_area_bound_20260806/AREA_BOUND_THEOREM_REPORT.md`（v8，六轮对抗审查转正）的定理 1 链：

```
[A] 格位分账（严格空地 ⇒ 六类不交）  A ≤ 1356 − 4P − R
[B] 聚合吞吐唯一（17×17 非奇异）      F_route = 9,135
[C] 容量计数（每件 ≥1 state、每 state ≤30） L ≥ 305
[D] 足迹（每格 ≤2 state）             R ≥ ⌈L/2⌉ ≥ 153
[E] 电杆（覆盖装填 IP，K=396）        P ≥ 9
⇒ A ≤ 1167 无条件；A ≤ 1015 单层【条件·待 OB6】
```

**逐条核对该链有没有用到车道分配前件**：[A] 是几何分账；[B] 是聚合平衡（本稿 §1.1 的 S1 独立复算并互证）；[C] 只用「每件过 ≥1 个 state」+「每 state ≤ 30 件/分钟」的**聚合**容量；[D] 是每格 ≤2 state；[E] 是覆盖装填。**五步全部与商品怎么分道无关。**

⇒ **该链在 P2.0 语义下是无条件有效的松弛，可以直接当 P7 的上界半边现役化。**

现役化路径（三步，无数学缺口）：

1. **语义标签对齐**：该报告已执行 OB7 纪律（结论标在 P2.0 语义下、与六谓词的 `U=(1188,18)` 并存不互斥）。P7 落地后这条纪律反转——P2.0 口径成为现役 U 语义，六谓词口径转历史。报告 §7 已预写这一步。
2. **证书工程化**（报告 §G6）：定理 1 链 → row-family manifest。报告已指出该链**不是纯 Farkas 非负组合**（三处整数取整：`L ≥ ⌈304.5⌉`、`P ≥ ⌈3325/396⌉`、`R ≥ ⌈L/2⌉`），证书要么显式携带 Chvátal-Gomory 舍入行，要么把 `L≥305 / P≥9 / R≥153` **三条都**当外部已验证 input cut 挂进 manifest（**漏一处取整就不成立**）。报告判为「无数学缺口，一个批次」。
3. **接进 P7 的上界侧**：作为候选枚举的剪枝上界（`area > 1167` 的候选直接排除）。

**本稿对该线的两条净输入**：

- **一条陷阱**（§4 末）：`L ≥ Σ_k ceil(F_k/C) = 308` 这条更紧的界**不可**进无条件链——它依赖每格单商品（限制，非松弛）。请记进该报告的 §5.4「已证无效的路线」同侧，标为「仅受限族内有效」。
- **一条互证**：本稿 `rate_table.py` 独立复算 `F_route = 9,135` 与 `F_target = 9,169.5`，与 OB1 收据逐字符相等（脚本内断言）。这是该数字的**第三次独立复算**（flow_account → OB1 → 本稿）。

---

## 9. 工程量、风险、外审攻击面

### 9.1 工程量（相对 v2 的净变化）

| 项 | v2 估计 | 特化后 | 说明 |
|---|---|---|---|
| 新子问题模块 | `src/models/throughput_subproblem.py`（新建，不改造 `flow_subproblem.py`） | **同左** | 建议不变：`flow_subproblem.py` 的 diagnostic-only 锁是刻意契约（`test_exact_contract.py::test_exact_mode_uses_flow_only_as_diagnostic` monkeypatch flow→INFEASIBLE 仍断言 CERTIFIED），改造它要动红测 |
| 变量结构 | 需新建 per-commodity 流变量 | **底座已在**：`use_vars` 已 per-commodity（`:770`/`:1053-1058`），只需加 `φ` 流变量层 | 净省 |
| 有理 Farkas 基建 | v2 §4.3 全套（规范形 + row-family manifest + `constraint_id` 重建 + `lp_digest`） | 受限族内可用**最小割**替代；上界侧仍需 manifest（§8 步骤 2） | 净省一半，**不是全免** |
| `u[i]` / `r[p]` 求解 | 变量 | 常数（本稿 receipt 可直接当输入表） | 净省 |
| TP7-D 离散层 | 全套 | **原样需要** | 无变化 |
| v3.1 启动三件套 | 全套 | **原样需要** | 无变化 |
| freeze-ritual | §5.3 清单 | 同左 | 无变化 |

**净判**：特化省掉的是**静态层的一部分求解与证书基建**，省不掉离散层与启动层。若按 v2 的分期，特化影响的主要是 P2.0b 的静态半边。

### 9.2 风险

| # | 风险 | 等级 | 缓解 |
|---|---|---|---|
| R1 | 丙案两侧长期夹不拢，P7 落地后长期 `UNKNOWN` | **高** | fail-closed 已是现有纪律；先跑一轮拿夹逼间隙数字再定要不要降级（§4 owner 选择点） |
| R2 | 见证侧找不到任何受限族能产出可行解（P1 族已证为空，替代族未知） | **中高** | 这是 §4 丙案下界半边的头号工程未知数；建议第一批就先做「能不能构造出**一个**吞吐可行的完整布局」的可行性 spike，不做认证 |
| R3 | 把受限族推出的界误当无条件上界 | **中** | §4 末已点名；建议在 flowbound 报告里落一条显式禁令 |
| R4 | P7 落地后最优解**变小**，旧 incumbent 与 frontier dominance skip 全失效 | **中** | v2 §2.5 BLOCK-3 迁移规则已写：只继承 ¬P(x) 型结论；旧 CERTIFIED 降 `P7_PENDING`、被 dominance 跳过的候选恢复 `UNRESOLVED_UNDER_P7` |
| R5 | LBBD 内环（CP-SAT × 流层）不收敛 | **中** | v2 §7 已列头号算法问题；特化把泛化 cut 从有理 Farkas 换成最小割，**可能**改善但未实测 |
| R6 | 离散动力学（队头阻塞 CE4 / 空启动死锁 CE3）在特化下一条都没消解 | **中** | 明确写进 scope：P7 静态层不承诺离散可达性 |

### 9.3 外审攻击面（预判外审会打哪里，先自己写出来）

0. **【实际命中的向量，本清单原本没有】「探针的输入前提是怎么来的、有没有被辩护」**——`split_free_probe.py:97` 把每台机器的占空硬编码成均摊，六例判决全部建在这条未辩护的约定上，放开后四例翻案（§2.4）。本清单第 1 条打的是「建模是否等价于无分流」，即**建模等价性**；实际被打穿的是**输入前提的正当性**，在清单之外。教训：自审攻击面时容易只审建模，不审喂给模型的前提。

1. **「你凭什么说前件族是空的」**——攻击点在 `split_free_probe.py` 的建模是否等价于「无分流」。防守：勘误后仅存的两例（buckwheat/sandleaf）是**纯计数**论证、对任意占空分配成立、无需信 CP-SAT（§2.4）。**这两例可手验**是本稿勘误后保留的防线；原「六例全部可手验」的说法已失效——手算复核本身没错，错在复核的是均摊约定下的算式。
2. **「max-min 探针的方向对不对」**——`m_k` 是 max over 路由 min over 段，所以每个路由都有段 ≤ `m_k`，方向是「混流不可被速率排除」。若写反成 min-max 就会得出相反结论。**本稿在 §2.4 显式写出了这一步的逻辑**，请外审优先打这里。
3. **「混流窗口存在 ≠ 混流真会发生」**——正确。本稿只主张「速率不能排除混流」，不主张「混流一定发生」。这已足以否定把纯度当模型约束，但不足以否定别的用法。
4. **「丙案的夹逼有没有隐藏的不对称」**——上界侧用聚合松弛、下界侧用受限族，两者的**语义标签必须一致**（都在 P2.0 语义、同一套前提栏）。若上界算的是「吞吐守恒」而下界见证还满足别的隐含约束，夹逼无效。
5. **本稿自身的证据等级**——`rate_table.py` / 两个探针都是【实测】，但**都没有跑生产规模的 routing build**（§3 行数只给上界形式、Q1 未闭合）。任何关于「行数可接受」的工程判断在 Q1 闭合前都是【假设】。
6. **裁判自产文书风险**——按仓库家规（`memory/referee-authored-docs-blind-spot.md`），本稿是起草席自产的承重推理文书，入库/外发前应过一个独立 refute 席。**refute 第一轮已于 2026-08-07 完成**（`refute_round1/REJUDGE_REPORT.md`），查出三处缺陷：命题 S2 假（本清单第 0 条）、canonical `rate_lemma_scope` 同源缺陷、§4 甲案族空判断是非蕴含。本稿已按其 §8 勘误表就地订正。该报告自身的独立审查另行排期。

---

## 10. 开放问题清单

| # | 问题 | 承重程度 | 归属 |
|---|---|---|---|
| **Q1** | 生产规模（266 实例）下 `len(use_vars)` / `len(phys_vars)` 的实测值——§3 全部行数量级判断悬在它上面 | **承重**（§3 全表、§9.3 第 5 条） | 跑一次生产规模 routing build 读 `_record_state_space_stats`（`src/models/routing_subproblem.py:1080` 定义、`:1099-1100` 计数） |
| **Q2** | 丙案下界半边用哪个受限族？**先直接试 P1+P3**（原「P1 族已证为空」的前提已撤回，见 §4 甲案；族空性未测，且分流不违反 P1） | 工程 spike（不再承重） | 第一批做可行性 spike（R2）：P1+P3 在生产规模上能否构造出见证 |
| **Q3** | OB-D 最小车道支配引理**与 OB-D2 占空分配支配**（§4 甲案）是否成立 | 中（甲案的存亡；丙案两条都不需要） | 研究线 |
| **Q11** | canonical `rate_lemma_scope` 措辞修正：前件与结论互斥（最满足前件 (ii) 的分配恰是破结论的分配），须重写而非补前件。可直接取用的 `statement` diff 见 `refute_round1/REJUDGE_REPORT.md` §4 | 中（叙述层承重，不进认证链） | owner 拍板 + 下一批 canonical freeze-ritual |
| **Q4** | 上界侧的松弛能收多紧？现役 `A ≤ 1167`（无条件）/ `1015`（条件·待 OB6）与真实最优的间隙未知；L 侧仍 absent | **承重**（夹逼能不能合） | flowbound 线 §9 排期（G1×OB6 耦合杠杆优先） |
| **Q5** | 最小割作为泛化 cut，其强度是否足以让 LBBD 内环收敛 | 中（R5） | 实测才知道 |
| **Q6** | 汇流器仲裁（owner 给的「2s CD、双入满速竞争最坏减半」）与静态聚合容量行 `Σ ≤ 1 件/tick` 哪个更紧——若仲裁更紧，静态层不保守，P2 行要收紧 | **承重**（soundness 方向） | 需 owner 游戏实测或 v2 `throughput_semantics` 机器化 |
| **Q7** | `item_admission_port_exclusion.rationale_restated` 理由 (a) 的措辞精度补丁（§7.2） | 低（不改裁决结论） | P2.0 实现批的文档批 |
| **Q8** | §2.5 的两条 canonical 措辞加固是否采纳（可与 Q11 并入同一批） | 低 | owner |
| **Q9** | 终品口径：`F_route` 扣无线终品是保守选择；P2.0 谓词最终把终品交付计入路由则单层界白拿 1 格（flowbound §G4）。P7 的谓词定义要不要计入终品交付 | 低 | P7 谓词定义批 |
| **Q10** | ~~本稿未过独立 refute 席~~ **已闭合**：refute 第一轮 2026-08-07 完成（`refute_round1/REJUDGE_REPORT.md`），本稿已按其 §8 勘误 | 已闭合 | — |

---

## 附：可复跑锚点

```bash
cd /home/zhuran24/zmd-pj
python           docs/research/p2_0_specialized_20260807/rate_table.py           # 表 A/B/C/D/E
.venv/bin/python docs/research/p2_0_specialized_20260807/split_free_probe.py     # 前件族是否为空
.venv/bin/python docs/research/p2_0_specialized_20260807/maxmin_segment_probe.py # 细流段厚度与混流窗口
```

收据：`rate_table_receipt.json`、`split_free_probe_receipt.json`、`maxmin_segment_receipt.json`；stdout 存档同名 `*_stdout.log`。
后两个脚本需 `ortools`（走 `.venv`）；`maxmin_segment_probe.py` 复用 `split_free_probe.py` 的 `solve_duty()`，两文件同目录。

**互证锚点**：`rate_table.py` 内断言 `F_route` / `F_target` 与 `.artifacts/p2_0_refresh_20260805/area_bound_work/ob1_flow_caliber_receipt.json` 逐字符相等；`n_op` 与 `data/preprocessed/mandatory_exact_instances.json` 的 266 实例普查逐 op 相等。断言不过脚本直接抛错。

---

*v1 完。本稿是设计稿批产物，不改生产代码、不改锁面、不改 canonical；落地立项待 owner 过目。*
