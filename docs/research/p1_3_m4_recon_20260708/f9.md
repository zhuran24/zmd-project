# f9（density_envelope）— 第一轮 codex 侦察（配额耗尽前唯一完成路）

## 身份
- family key = "density_envelope"（lifecycle.py:73，F9 geometric）
- ORACLE_NAME="density_envelope_v1", FAMILY_VERSION="v1.0", VALIDATOR_VERSION="v1.0", CERT_KIND="density_envelope_v1"（density_envelope_oracle.py:48-51）
- generator = generate_density_envelope_cuts(...)（oracle :101-110），无族级 env 开关

## 数学断言
ghost-bound area-only window envelope：对窗口 W 和 group g，禁止 g 在 W 内占用去重 cell 面积严格 > max_allowed_area；Phase 1.2 只接受 K == static safe_ub = |W| - |(ghost∪exterior)∩W|（families/density_envelope.py:275-305），K < safe_ub 直接 unsound（:326-363，无 replayable proof）→ tight-K 非平凡 cut 被 fail-closed。PROJECT_LOCK.md:453-461 记 F9 实质停用。

## cert payload（cert_schema.py:101-109，全必填）
cert_kind / witness_kind（只收 "area_capacity_overflow"，:58-59）/ window_rect [x,y,h,w] / group_id / max_allowed_area / oracle_assignment_witness（[group_id, pose_id] 列表，multiset ≤ demand）/ ghost_rect_repr（byte-equal state.ghost_rect）

## step_8 候选
- 候选 A：F1 式加权 presence，但系数=每 pose 对 W 的 overlap area（不是 cells_per_pose 整面积！）；K==safe_ub 恒真但冗余；K<safe_ub 依赖未来 cert 门禁
- 候选 B：witness presence nogood（sum ≤ n-1），依赖 tight-K proof；witness 允许 multiset，布尔 OR 丢 multiplicity
- 需新 master API

## BState 对照
- cell_owner={} → evaluator 恒 False（evaluator 按 cell_owner 计数，:517-566）
- available_oracle_versions 缺 "density_envelope_v1" → step_6 HOLD
- witness 参数完全未组装；attach 链只调 F1

## validator/replay/evaluator
- FAMILY_VALIDATORS 已注册（replay.py:63-72）；step_7 dispatch 到 evaluate_geometric_density_envelope（lifecycle.py:1091-1105）
- 代码路径全在，但生产不具备 firing 条件

## 测试
test_family_density_envelope.py 覆盖 validator/evaluator/generator/watcher（tight-K 拒 :211-219、非 area witness 拒 :231-241、evaluator tight-K fail-closed :573-580 等）；step_8 无 F9 专属测试

## 风险
- F9 系数是窗口内 overlap area 不是整 pose 面积——照抄 F1 会错
- K < safe_ub 在无 replayable proof 前违反 validator/PROJECT_LOCK fail-closed 决策
- witness nogood 布尔 OR 丢 multiset multiplicity
- pose_id str vs master pose_idx 映射（同 literal 族问题）
- 解封 tight-K 需「与 F5 v1.0 信任 INFEASIBLE 同类升级」（PROJECT_LOCK:461）
