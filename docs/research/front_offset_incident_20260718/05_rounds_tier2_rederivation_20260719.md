# Rounds 重跑·第 2 梯队完成记录（2026-07-19 值夜）

> 承 rounds 重跑三梯队清单（04 号 §5）。第 2 梯队 = 免重搭的案头件：Round 1 T-负算术重推（RND-02 后继）+ Round 2 min-cut 结论摘出（RND-09 同源）。本文数字在 revision `b1cf014` 后、新池 82,829（`f05b1291…`）语义下推得；计数脚本为一次性重推（`operation_profiles` 现算，未复用任何旧 harness 数字）。

## 1. Round 1 T-负算术重推（doc 12 §2 的 identity 语义重版）

**分子（routing-visible 端口需求）= 628**（312 in / 316 out）：由 mandatory 实例 × `OPERATION_PORT_PROFILES` 槽数现算——制造配方口 + 边界仓 46 出 + core 6 出 + core 收货 2 进（当前 generic 需求 2、box 下界 0）。与批 3+5 落地时的路由账预期"620→628"独立吻合（+2 = core 收货进口，+6 = 终品 producer 输出口回归路由；旧 620 是 routing-free 剔除 + 无线箱时代的数）。

**需求账腰斩**：identity 语义下每个使用中端口只需其 stored 格（带子格自身）空置——批 1 已删除 connector 概念，"每端口 connector+front 双格链"不复存在。相对口还可共享中间格（本账保守取 0 共享，即 628 为需求上界）。

**预算对照**（free = 4,900 − 3,544 = 1,356，未扣电力塔本体，与旧账同口径）：

| ghost | 预算 | identity 需求 628 → 富余 | 旧双格账 1,256 对照 |
| --- | ---: | ---: | ---: |
| 6×6 | ≈1,320 | **+692** | +64 |
| 6×7 | ≈1,314 | +686 | +58 |
| 7×7 | ≈1,307 | +679 | +51 |

**判读**：

- 当年"connector+front 最坏需 1,240 vs 预算 ~1,202-1,339 → 最坏 -38、预算真紧"的整个紧张结构是错位语义产物。identity 下富余 ~700 格量级，**任何 Hall/计数类"挤不下"矛盾都无从谈起——T-负"基本死"维持且加固**（RND-02 撤销的旧数字由本节取代；结论标签仍是 undetermined 而非"障碍不存在"，与 doc 12 对抗验证的认识论纠正一致）。
- **doc 12 §5.1 的"ghost 面积阈值"上界线索实质死亡**：需求恒 628 下，"最优打包也挤不下"要求 ghost 面积 ≥ 1,356 − 628 = 728（≈27×27），与 A* ≈ 42 相差一个数量级。该线索当年"看起来有救"完全依赖双格账，不必再列为活跃上界路线。
- "46/45 边界打包刚性"（doc 12 §2 第三条）失去结构基础：刚性来自 connector+front 双格链在边界带的挤压；identity 下边界仓每口只需自身 stored 格，无 connector 撞车机制。**不据此宣称边界无约束**——仅撤销"必然 ≥1 重叠"的旧结构结论。
- 诚实边界：belt **路径**格（连通 312↔316 的路径占格）不在本账内（当年亦未计入）；路径可多商品共享、无可证下界，正是计数类证书够不着的部分。

## 2. Round 2 min-cut 结论摘出（已落 doc 13 banner 注记）

min-cut 轨的结构性结论——"sound 连通需求无流量下界，唯一 sound 连通割 = 完全不连通（cut_size 0），min-cut/Menger 给不出非平凡不可行证书"——其论证对象是**连通性作为证书来源的结构极限**，与 front 坐标落在哪一格无关（front 修正只改端点集合，不改"连通无下界"前提）。**保值、免重跑**；被撤销的只是当年据此并入的全称"所有有界方法不可达"判词。注记已直接落在 `cut_framework_review_gpt56pro_20260710/13` 的 banner。

## 3. 梯队状态更新

- 第 1 梯队 ✅（04 号：FCL 生产 A/B + witness 重建）；
- 第 2 梯队 ✅（本文）；
- 第 3 梯队（Round 3 必要条件在实体口模型下重证；Round 4/5 bespoke 紧凑 master 重建）：FCL 生产臂已证生产 lift 回答不了 6×6 判定（04 号 §2），bespoke 紧凑 master 的重建价值确认上升——但它是重工程，排期待 owner 过目 rounds 全清单后定。
