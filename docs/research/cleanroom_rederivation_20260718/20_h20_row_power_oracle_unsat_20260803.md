# 20 号：H20 row-power 微型判定器 UNSAT（2026-08-03）

**性质**：research-only 执行与验收记录。不建立/不推翻任何 bound；
`U=(1188,18)`、`L=absent` 不变。判定对象 = 16 号文书（H20 双极沙漏梳）自述的
唯一主要开放义务（§9 供电层）+ §11 微型判定器规格。

## 结论

> 当前 H20 行语法族（固定核心位/保护矩形/20 杆 token 语法 + 被搜索的行序
> 空间）不可行。

不外推 benchmark 不可行，不外推其他杆数/行序/语法（措辞按 16 号 §9 限定）。
R1「2 个空列」的两种自然解读（连续宽 2 / 两个独立宽 1）**均 UNSAT**。

## 执行与验收

- 执行：codex 端到端（`.artifacts/h20_row_power_oracle_20260803/`：oracle、
  stdlib-only 独立 checker、双解读 result+checker 收据、RESULT.md 含 14 行
  语法逐行对照表）。冻结输入：HEAD `3b4e859`、16 号文书 SHA-256
  `b3980ecc…25ca8f`。CP-SAT 9.15.6755，双支各 ~0.018s INFEASIBLE
  （1151/4238 与 1152/4240 变量/约束）。
- 主会话验收（2026-08-03）：
  1. 两份 checker 按其运行时合同（`-I -S`、无 ortools import）独立复跑，
     双 PASS，与 codex 收据实质逐字段一致（差异仅 --self-test 组与解释器
     元数据）；
  2. **核心计数论证独立解析验证**：R2（y=12..14）22 台 M3 只能被 R1/R3 各
     1 根杆触达（R0 halo ≤ y10、R4 halo ≥ y17、R2 自身 token 多重集无 P 且
     22×3+1=67 恰满行）；12 宽 halo 与 3 宽机身的相交窗口 = 14 列 → 单杆
     ≤5 台、双杆 ≤10 台 < 22，严格缺口 12。该矛盾在放宽的杆横锚超域上枚举
     成立，不依赖 R1 gap 解读、不依赖行序。

## 对 W0 方向决策的含义

结合 19 号（pinned seed 死刑复算成立）：
- **16 号 H20 备胎在第一道门出局**（as constructed；若未来改杆数/行语法是
  新构造族，需重开判定）；
- 17 号处方（front-aware pattern generator → exact-cover → G1/G2/G3）成为
  唯一在案候选新方向；其修复设计须用 repo 端口语义（19 号第 4 步偏差记录）。
- 方向拍板仍属 owner；本文书只供决策证据。
