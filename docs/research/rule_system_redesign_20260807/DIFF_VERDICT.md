# 两法对勘：第一性推导版 × 病例驱动版

**状态**：对勘判读，未实施。
**日期**：2026-08-07。
**对勘双方**：`FIRST_PRINCIPLES_DESIGN.md`（从"游戏是形式系统 + 要证全局最优"两点推导）× `FINAL_DESIGN.md`（从 52 病例分类学 + 两份对抗稿归纳，含合稿席机器复核）。
**方法**：推导版在写完并落盘前没有读过病例驱动版的任何文件，两法的独立性成立。本文只判读，不改任一方的设计文件；推导版因本次对勘而做的修正已直接写回 `FIRST_PRINCIPLES_DESIGN.md` §1.9 与 §1.12，本文记录修正的来源。

**三个数**：收敛 **15** 点、分歧 **8** 点、判为赘物或错位 **2** 项。

---

## 0. 总判读

两法在**结构骨架上高度收敛**：分层（经验层冻结 / 派生层不冻结且自动失效 / 生成视图层非权威）、双向保真作为出发点、三类审计的名字、前提集作为一等对象、修正必须原地改写、消费侧闸重于声明——这些是两条完全独立的路径各自到达的，可信度高于任何一方单独的论证。

分歧集中在三处，各有胜负：

- **推导版胜在结构的完备性**——它有一张按**模型约束**分行的表，病例驱动版没有；它把"我们在证什么"（问题层）与"游戏是什么"分开，病例驱动版没有分。
- **病例驱动版胜在纪律的可执行性**——推导版原本主张"推得出来的东西不必写下来"，这一条被病例 C-15（六轮逐行审查仍漏一条前件）直接证伪。推导版已按此修正。
- **成本与工作组织**（批型、席位、预算、降级档）病例驱动版有整套，推导版原本一片空白，其中一条（成本决定纪律是否真的存在）是真原理漏了，已回填为补-12。

对勘期间还发生了第三件事：病例驱动版的一条"机器复核"结论（箱案应判待核）被查出其**输入是一份陈旧副本**，正确判读仍是"物理不可达"（详见 §4-①）。本文件上一版曾照单吸收该结论——这条错误由 owner 认出自己的裁决而纠正，不由任何机制纠正。它作为病例 C-53 进了推导版的校验集，并推出补-13。

一句话：**骨架用推导版，工具与工程落地用病例驱动版，两者不冲突**——推导版给出的是"哪些工具是必需的、为什么"，病例驱动版给出的是"这些工具具体长什么样、谁来做、多少钱"。

---

## 1. 收敛点（两法独立到达，高置信）

| # | 收敛的结构 | 推导版出处 | 病例驱动版出处 |
|---|---|---|---|
| 1 | **双向保真是出发点，且过严专伤证明类结论** | §1.3（从"要证全局最优"推出） | §1.1 公理 I（owner 下达）+ 四象限 |
| 2 | **过严的机制性无声**：它只产生缺失的答案，不产生错误的答案，没有报错通道 | §1.3 | §1.1 根因段 |
| 3 | **派生层不冻结、自动失效**；冻结成本与派生层的高频节拍不匹配 | §1.8（hash 是不可重现内容的手段） | §3.1 L2 三条理由 |
| 4 | **生成视图层非权威、可重生** | §2.1 | §3.1 L3 |
| 5 | **前提集是一等对象，且要能机械求失效传播** | §2.2 + §2.4 前提图 | §3.7 `premise_fingerprint` + `STALE` 状态机 |
| 6 | **持久化结论必须绑规则版本** | 补-3 | §3.9 八段 digest（并各自否掉了两个错误方案） |
| 7 | **复算脚本里的聚合/写死常数是未声明前提** | 补-4（收据的输入集即前提集） | §4.4 聚合问（M-01），双方引的是同一处证据 `rate_lemma_recompute.py:34/37` |
| 8 | **修正必须原地改写父句，旧读法进 superseded** | §1.6（引用需要一条当前为真的命题） | R-08 / 批 4 / `superseded_readings[]` |
| 9 | **例子不是界**：举例不得被当全称命题消费 | §1.6（条目 = 命题，例不是命题） | 量词问 + R-13 + §2.5-① |
| 10 | **义务在引用者身上，闸放消费侧且机器化**（"头部写非权威声明"已被证伪） | §1.9 (纪-1) + 检查器禁令 | D-17 / §3.8 三条消费侧闸 |
| 11 | **参数缺项必须显式登记，不是留白** | 补-8 | §3.5 `missing[]` 必填 |
| 12 | **同一条规则的多处实现要各自登记 + 等值哨兵** | 补-1 | §3.2 `implementation_anchors[]` + `parity_sentinel` |
| 13 | **裁决要有反向索引** | 补-7（裁决 → 条目的全函数映射） | 批 1 `OWN-M*` 抽取索引表（并如实记 M25/M26/M28 缺号） |
| 14 | **墙审计禁止按公理分行**——按公理分行会系统性丢掉无对应公理的语义轴 | §1.7（两表行索引由两个包含方向定死） | §4.7（直接引 C-21 根因） |
| 15 | **预算删失的结论不是结论** | §1.9 (纪-2)：删失数据只授权删失级结论 | §1.4 反向刹车 `terminated_by: budget` |

**一条半收敛（未计入 15）**：病例驱动版 M-08 把派生登记册裁定为"单一家 = L2 `rules/derived/`"，与推导版补-13"一条经验事实只许一个陈述位置"是同一个形状。但它是合稿席对两份姊妹稿冲突的一次仲裁，管的是**派生层**的落点；补-13 是从 (纪-3) 推出来的，管的是**经验层**，而经验层的多副本正是 C-53 的病灶。两者可以合成一条通则：**任何一条命题，无论哪一层，只许有一个陈述位置。**

第 14 条值得单独说：**两法对同一个失效（连通性整轴在一致性矩阵里消失）给出了同一个形式诊断**——索引方式决定了什么看不见。推导版是从"健全与完整两个方向的量词跑在不同集合上"推出来的，病例驱动版是从病例根因归纳出来的。这是两法收敛里最硬的一处。

---

## 2. 分歧点（逐点判）

### 分歧 1 — `semantics` 区是否留在冻结件里　**推导版胜**

病例驱动版把公理与全部条目留在 `rules/canonical_rules.json` 的 `semantics` 区（L1，冻结），代价是四个 freeze-ritual 批（批 2/3/4/7），每批要做八段 byte-identical 断言、17 处直接 pin 与四条连锁链。推导版 §1.8 判：机器消费的冻（参数），不可重推的记版本（公理），可重推的不冻（定理）——公理层需要的是**自己的版本号与哈希**，不是与机器输入同锁一个文件。

判据不是偏好，是病例驱动版**自己的论证的一致外推**：它用"进冻结件 ⇒ 每次改动付 ritual 底价"这条理由把 `consumers[]`（D-03）和拒真席收件箱 `candidates`（D-04）赶出了 canonical，却没有把同一条理由用在 `semantics` 区本身。更强的内证是它的**八段 digest**：持久化结论的失效戳被刻意定义为"canonical 除 `semantics` 外的八段"——即它自己认定，结论的正确性依赖的是那八段，`semantics` 不在其中。既然如此，把 `semantics` 与那八段锁在同一个字节里就没有承重作用，只有成本。

**反向权衡（诚实记录）**：拆出去意味着多一条 pin 继承链（C-49 的病），且 `semantics` 是 owner 裁决的落地处，冻结件身份有"不可悄悄改"的治理价值。推导版的答复是拆出去的文件**照样自带 sha pin**——变的不是"冻不冻"，是"改一条定理要不要连带重钉候选池那条链"。这一改动改变裁决权威的载体，属 owner 决策项。

### 分歧 2 — 第三类审计审的是什么，以及它怎么被触发　**推导版胜**

两法都叫它"界审计"，审的对象不同：

- 病例驱动版：**在册限制的数值界在物理参数下够得着吗**，枚举面 = 所有 `statement` 里含"当且仅当 / exactly when / blocks when / triggers when / fails when"形态判据的条款。
- 推导版：**命题陈述的范围限定条**（§1.7 界）。而"界够不够得着"在推导版里不是一类审计，是**正当表每一行蕴含步骤的必填栏**（补-5）。

推导版胜在两点。其一，**触发条件从词形改成角色**：按措辞触发是脆弱的，同一件事换个写法就逃逸，而病例驱动版自己在 §7.1 已经注意到原稿"三步独立命中"不成立，部分正是因为三步悬在同一个词形判定上。按角色触发（凡被用来正当化一条限制的命题）与措辞无关。其二，**范围更准**：作为表行的必填栏，它出现在且仅出现在承重的地方；作为独立台账，它既会收进没人消费因而不威胁任何结论的界，又会漏掉承重但措辞不含关键词的界。

同时推导版的"界"（命题的范围限定）在病例驱动版里没有对应的审计位——它散在 `scope` 字段、`dischargeable_in` 与 conditional 证书里。**建议：改名并合并**——可达性核算并入正当表的蕴含栏；"界审计"这个名字留给命题范围限定条的审计。

### 分歧 3 — 推理纪律的形态　**病例驱动版胜**

推导版原本主张：纪律只有一条公理加五条推论，**推得出来的东西不必单独写下来**（现推即可）。这一条被 C-15 直接证伪——全文前件写得最显式的那条引理，六轮逐行审查仍漏掉一条前件。无界的义务（"核所有前提"）在实践中必然退化，这正是病例驱动版 K-4 有界性要求的内容。

推导版已按此修正（§1.9 现行版）：准入判据改为**两问三格**——推不出来的删；推得出来但没有停机判据的删（它只是把公理换个说法）；推得出来且有停机判据的**收**。第三格是纪律正当的增长方式。按这个判据回看病例驱动版的工具，逐条都过：聚合问（有界：脚本行数）、闭合层枚举（有界：六层）、九行账目对表（有界且 append-only）、保证方受控词表六选一、搜索面留痕表——**它们不是清单膨胀，是有界工具**。推导版原来的反清单论证会把它们全部误杀。

这是本次对勘里推导版唯一实质性落败的一点，也是最值得记的一点：**"能推出来"不等于"不用写"，因为要人现推的那一步正是人会漏的那一步。**

### 分歧 4 — 有没有一张按模型约束分行的表　**推导版胜**（结构差异里最重要的一条）

病例驱动版的三张台账，**行集全部在游戏侧或条款侧**：孔审计按现行防线，墙审计按能力/语义轴（五路：实体自由度 / owner 裁决台账 / 模拟器判例 / 上游注册表 / 六谓词量词展开），界审计按带数值界的条款。`model_faces` 只登记**已经被发现并经 owner 裁定**的过严/过松面，是发现物的登记册，不是模型约束的全集索引。

于是"这条约束，谁正当化它"这个问题**在病例驱动版里没有一个常驻的行集来问**。它靠批次触发补：改动触及守卫文件集时勾选"本批新增过严面：有/无"（D-19），生成器/剪枝批必跑双向保真（§4.1）。批次触发覆盖的是**新改动**，覆盖不了**存量**——而 C-04（生成器过剪 3,096 个合法 pose）与 C-20（219 台制造机每个输出口的对称过严）都是存量，都靠人偶然发现。

推导版 §1.7 的正当表把每一条塑造可行集的决定（含候选池生成期剪枝、预处理定死的实例集与需求量、目标与 admissibility 的编码）都排成一行，判读非"等价"的行即欠账，由 §2.3 的双射检查保证行集与代码里的约束一一对应。这正是 §1.7 那条推论要的效果：**生成期剪枝不长得像约束，把它排除在行集外就等于给这一类事故留永久盲区。**

代价是唯一一项代码侧要求：约束要有**声明在施加处的稳定编号**。病例驱动版 D-09 的顾虑（"参数 → 代码 call site"不可靠机器求出，拼接字符串骗过 grep、`.rgignore` 藏活代码）在这里不适用——编号是**登记**出来的，不是扫描发现的；而它自己的 `implementation_anchors[]` 是同一个机制的另一侧写法，两者可以合并成一件事。

### 分歧 5 — 登记一个过严面是否以"举得出实例"为前提　**各占一半**

病例驱动版：拒真席的产出必须是"候选过严面 + 一个游戏合法但被模型拒的具体实例"，`excluded_game_legal_instance.kind = "none"` 的不得进 `stricter`，只能待在收件箱。这条防的是 C-10（把正确的保守编码误判为保真缺口，整份分析一天内作废），有效。

推导版判：**放开动作要实例——收敛；登记要实例——分歧，推导版胜。**理由：正当表的行**由约束的存在自动产生**，与有没有人找到见证无关；找不到实例只是让该行的判读停在"更严·未清欠账"。把登记本身押在见证构造的难度上，会让"见证难构造的真过严面"被归档成不存在——而见证难构造恰恰是几何类过严面的常态。病例驱动版自己用搜索面留痕表（F-03）缓解了"空手而归 = 零成本合法终态"，但缓解的是席位的诚实度，改不了"登记以实例为条件"这个门槛。

反过来，**放开（删限制、松编码）是健全性方向的动作**（§1.3：过松直接产假见证），按健全性标准立论、要求具体实例是对的。病例驱动版的三签（可达性签 + 拒真签 + owner 签与 freeze-ritual）在这一侧完全成立，推导版照单接受。

### 分歧 6 — 派生层怎么"跑"　**互补，不判胜负**

病例驱动版给的是**搜索程序**：层 0 种子 = 本结论前提集 + 本批新入账项，层 1 = 资源账邻接（九行账目）+ 实体邻接，只做 pairwise + 结晶回灌，深度上限 3 轮，停机 = 一轮零新结晶。推导版给的是**优先序**：工作队列由两张表的非干净行生成，每条脏行就是一个精确陈述的数学任务。

两者管的是不同的东西，合起来才完整：只有优先序没有程序，"去证一条 WLOG 定理"不知从哪下手；只有程序没有优先序，扫描会自我繁殖（这一点病例驱动版自己在 §1.4 反向刹车里承认，并用 `UNREVIEWED` 状态机堵）。附带一处收敛：推导版的"盯零余量的账"与病例驱动版的"塌点几乎总是两本账相乘/相除产生"是同一个观察的两种说法。

### 分歧 7 — 问题层（我们在证什么）是否独立成层　**推导版胜**

病例驱动版的 L0 叫"求解器事实"，内容是 `globals` / `facility_templates` / `recipes` / `production_targets` / `commodity_metadata` / `routing_rules`，权威写作"owner 游戏实测 > 模拟器规则层"。这一层里混着两类完全不同的东西：

- `globals.empty_rectangle.objective` 与 `min_side_admissibility` 是**我们提的问题**，不是游戏的性质。游戏不关心你在地图上划的那个框。
- `routing_rules.layers = {ground:0, elevated:1}` 是**模型编码**——同一份文件的 `routing_cross_junction` 条目明写它是"a MODELING representation of a single-cell cross-junction"。把它冻在"求解器事实"层并标注权威为 owner 游戏实测，是一次类型错误，而且是这份文件自己写了一条条目去纠正的那次误读。

推导版把问题单列为 L0（§1.2），把模型编码归到正当表的模型侧栏（§2.5 归位表）。这不只是整洁问题：**P2.0 要把流量纳入 Q**，届时问题变、公理不变（§1.2 推论：公理不设范围，命题才设范围）。分层正确时这是改一份文书；分层混着时要逐条款重议 scope。

### 分歧 8 — 运行期计量的地位　**推导版胜（框架），机制吸收病例驱动版**

病例驱动版把 fail-closed 计数器定为第五卡点、排在批 5，并如实把 C-24 / C-47 的覆盖强度降为"部分（批 5 后挡）"。推导版补-6 判：凡行为取决于运行期分支的地方，**静态读码得到的是"代码可能怎么走"，不是"它实际怎么走"**，所以表行的模型侧事实必须来自运行期观察。这使计数器不是一个排期靠后的埋点任务，而是**那一行能否被核对的前提**——没有计数，那一行的模型侧事实就是一条无法验证的断言。

机制照单吸收病例驱动版的形态（计数器 + 理由码、剥落/降级计入拒绝率、"零信号"改二选一必填 `真零（有埋点，计数=0）` / `无埋点（欠账号 #N）`）。

---

## 3. 它有而推导版推不出的元素（逐个判）

| # | 元素 | 判 |
|---|---|---|
| 1 | **批型判定（§4.0）+ 触发矩阵（§4.1）** | **真需要，属流程编排面**。它决定一整套义务对本批是否生效，是让纪律负担得起的机制。推不出来是因为它不是关于知识的，是关于工作组织的。fail-closed 兜底（触及可行集/证据链即不算工程批）与"工程批必须写一行'本批不含科学断言的依据'"两条设计得好，直接接受 |
| 2 | **成本表 + 轻量档（§4.9）** | **真原理漏了**，已回填为补-12（无法被执行的义务不产生依据 ⇒ 成本决定纪律是否真的存在；每条纪律须带代价与明写的降级档，降级档产出自带强度标记）。"轻量档批不得出厂证明类结论"正是强度标记的正确形态 |
| 3 | **provenance 门槛（§3.5-③）**：进冻结件的承重量必须能在仓内复验（vendored 产物 + 脚本 + 收据 + 上游 commit） | **真原理漏了**，已回填为补-11。它其实是 (纪-3)"在源头观察"的推论：来源没被留存，"在源头观察"这个动作后来的人执行不了，只能读转述 |
| 4 | **席位分工与异源化（§4.9）**：立论席不自我验收、拒真面登记的复核不归拒真席 | **真需要，属流程面**。独立性不是逻辑要求，是错误率的经验措施；推导版的三种依据里没有"谁来做"这一维，本就不该有 |
| 5 | **schema 级硬判据**（`NOT_ASSESSED` 禁令、`discharge_evidence == MISSING` ⇒ 不可 `certified`） | **合法，且推导版的面判据能解释它们为什么合法**：它们收紧的是"能不能写进台账"，不是模型的可行集。按 §2.6 的判据（收紧 = 更安全 → 发布面），台账是一个发布面，fail-closed 在这里符号为正 |
| 6 | **八段 digest 的具体构造（§3.9）** | **机制，照单吸收**。推导版只推出"持久化结论要绑前提版本"（补-3），病例驱动版给出了构造并各自否掉了 `_epoch`（L0 一改旧 nogood 静默存活）与整文件 sha（纯语义批全量过失效） |
| 7 | **搜索面留痕表（F-03①）** | **可推出，推导版漏了机制**。"没找到"是一条否定断言，同样需要依据；与补-6"零信号的保守回退是无法被核对的断言"同型 |
| 8 | **外发登记台账（M-06）** | **机制层，吸收**。推导版只推出"外发件必须声明它依据的理论版本，因此旧版本可枚举"，台账是那个枚举的载体 |
| 9 | **开放欠账台账的"阻断范围"（M-07）** | **吸收，且它给了推导版 §1.4 推论的牙齿**：欠账未清期间依赖该语义面的证明类结论只能出 conditional 并在方向暴露栏标欠账号。推导版说"未清欠账使最优性命题变成有条件命题"，这条把它变成出厂动作 |
| 10 | **反退化条款（§4.9）**：触发矩阵是硬的、退化信号常设监测、**加不出停机判据的检查不许进** | **部分吸收**。最后一条已并入推导版 §1.9 的准入判据（见分歧 3）；前两条属流程面 |
| 11 | **L3 视图 + 消费侧红线**（`docs/generated/` 路径进前提集即红） | **收敛**，推导版 (纪-3) 直接给出：生成视图是转述 |
| 12 | **`parameter_fingerprint` 使参数账可豁免（M-08）** | **机制，吸收**。它是补-12 的一个实例——把重复劳动降到一行"引用 + 指纹"，纪律才跑得动 |
| 13 | **能力册的 `out_of_scope` 追问**（"若该能力被表达，解空间会不会变大"，答 yes 照样计入 `not_verified`） | **收敛**。它对应推导版补-10 的覆盖账，且这条追问堵的正是"把过严面正当归档"的出口，设计得好 |
| 14 | **`direction` 字段挂在 canonical 条目上（§3.2/§3.6）** | **错位，判为应移**。方向（stricter / looser / exact）是**模型与游戏比较**的结果，是表行的属性；一条关于游戏的公理没有"方向"可言。挂在条目上会诱导"这条规则本身偏严"这种没有意义的读法。应移到正当表/覆盖账的行上 |
| 15 | **界审计作为独立台账（§4.7）** | **应合并**，理由见分歧 2：可达性核算并入正当表的蕴含栏，触发条件从词形改为角色 |

**判为赘物或错位的只有第 14、15 两项**，且两项都不是"多余的工程防御"，是**放错了层**。这是对病例驱动版质量的一个正面判读：它虽然是从病例归纳出来的，但几乎没有为了对付某个具体病例而增设推不出来的装置。

---

## 4. 机器复核的三处事实：两处吸收，一处反向订正

三条都由本席**在源码/JSON/git 上亲自复核**，不是转述采纳。第①条的复核结果与病例驱动版相反。

**① 协议箱堵塞判据："物理不可达"成立，病例驱动版的"待核"改判是错的——它自己读到了陈旧副本。**

合稿席 §2.5-①/② 依据 `AXIOM_KERNEL_PROPOSAL_20260806.md:115` 的"P2 待判"，把 C-17 的判读从"物理不可达"改成"待核"，理由是槽数维度押在一条未决项上（同种货是填满已开槽才另开新槽，还是可直接另开新槽）。

**该待判项其实已于 08-06 晚由 owner 定谳：填满已开槽才另开新槽。**这条裁决当时有三个登记处，只更新了两处（记忆卡、canonical `slot_count_clause` 的 `adjudicated` 字段），`AXIOM_KERNEL_PROPOSAL.md` 的实体表与推导矩阵两行漏翻，仍挂"待判"——而机器复核恰好读的是漏翻那份。owner 08-07 当场抓获，"物理不可达"判决恢复（事故与修正见 `00_master_roadmap.md` 08-07 台账行，commit `c7f7e70`）。

于是两个维度都不可达：件数维度 15 件/冲刷周期 vs 300 件容量；槽数维度在 fill-first 下满不了 50 就开不出第二槽，而 10 s 冲刷无条件清空。

**两条承重前提当前都不在理论文书里**：fill-first 未落 canonical 正文（`slot_count_clause` 现文只写"可占多槽"），单槽容量 50 是 owner 口述、冻结件缺；两项均挂下一批 freeze-ritual。所以它在 §1.4 里是 `已证 WLOG`，而**证明用到的前提住在文书之外**。

**对推导版的影响**：`FIRST_PRINCIPLES_DESIGN.md` §3 的 C-17 行已按上述订正。**本文件此前也吸收过那条错误改判**（上一版把它写成"照单吸收：应为待核"）——纠正它靠的不是任何机制，是 owner 认出了自己的裁决。这件事本身作为病例 C-53 进了 §3 的校验集，并推出了补-13（经验事实只许一个陈述位置，其余只能持指针）；对勘的结论因此多了一条：**本席在这一点上犯的正是自己 (纪-3) 写明的错——"机器复核"这个标签让我省掉了对它输入的复核。**

**② `ruling_level_inputs` 与 `model_stricter_faces` 都是散文字符串。**
本席复核：两者 `type` 均为 `str`。`canonical_anatomy.md` §5 称 `ruling_level_inputs`"显式、闭合、形态良好"的表述作废，结构样板应改取 `globals.empty_rectangle.emptiness_adjudication`（对象 + `required` + `additionalProperties: false`）。
**对推导版的影响**：无需订正——推导版对 `ruling_level_inputs` 的判读是"这个字段本身是缺层的产物，在五层结构里它会自动消失"（§1.6），不依赖它形态好坏。附带印证：`model_stricter_faces` 是一个字符串，这就是推导版 §1.3 说它"是单方向读数"的物理形式——一个名词列表装不下三值判读。

**③ `semantics` 区不是零消费方。**
本席复核：`src/tests/cuts/test_helpers_power_cover_stencil.py:100` 读 `rules["semantics"]["power_coverage_stencil"]`。正确表述是"**无 solve-path 消费方，但已有一条 canonical ↔ 实现的等值哨兵**"。同时本席另核：`rules/canonical_rules.schema.json` 里 `semantics` 是 `{type: object, additionalProperties: true}`，无 required、无字段枚举——形态要求在机器层面当前完全不设防。
**对推导版的影响**：§1.8 已订正（原文说"没有任何东西消费它"，过强）。这条同时是推导版补-1（同一条件的多实现要各自登记 + 等值哨兵）的**现成先例**——那条哨兵正是 `implementation_anchors` / `parity_sentinel` 要推广的形态。

---

## 5. 合并建议

按上面的判读，两法的合并形态是：

1. **骨架取推导版**：L0 问题 / L1 参数 / L2 公理 / L3 定理 + 三个行集（执行表按游戏条件、正当表按模型约束、覆盖账按游戏自由度）+ 档案层，并对全部层施加**单一陈述位置**通则（补-13）：批次文书里的实体表与推导矩阵由真本机器生成，注记字段只持编号与出处、不复述命题。分歧 1/2/4/7/8 按推导版落。
2. **工具与工程落地取病例驱动版**：八步流程的有界工具（聚合问、闭合层枚举、九行账目对表、保证方受控词表、搜索面留痕表、三级 MISSING 阶梯）、八段 digest、`implementation_anchors` + `parity_sentinel`、两张台账（外发登记 / 开放欠账含阻断范围）、五个卡点与席位矩阵、成本表与轻量档。分歧 3 按病例驱动版落。
3. **两处移位**：`direction` 从条目移到表行；界审计的可达性核算并入正当表蕴含栏、触发条件从词形改为角色。
4. **落地排序照搬病例驱动版的原则**（零 freeze-ritual 的止血排最前），但批 2/3/4/7 的范围要按分歧 1 的结论重算——若 `semantics` 拆出冻结件，这四批里相当一部分 ritual 成本消失，排期要重做。

**留给 owner 的决策点**（本文不代拍）：`semantics` 是否拆出 `canonical_rules.json` 单独版本化（分歧 1）——它改变裁决权威的载体，且直接决定后续四个 freeze-ritual 批的规模。
