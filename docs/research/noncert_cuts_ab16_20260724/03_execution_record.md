# Archived AB16 execution record

Document kind: historical locator, not execution authority

Cutoff date: 2026-08-01

The earlier AB16 effort produced 38 Gate-A roots, `a001` through `a038`, over
approximately 26.5 hours. None reached an organic arm: scientific output was
`0/16` arms. The last root also failed closed before it could authorize the
experiment.

Those roots were removed from local worktrees and archived under:

```text
/mnt/wd_external/archives/zmd-codex-autonomy-20260801/
```

This file intentionally records no former local artifact path. The archived
bytes are historical diagnostic evidence only: they are not current inputs,
cannot be resumed or spliced into a new run, and grant no cut, witness, bound,
production, certified, or scientific-result authority.

The current experiment keeps the original 16-arm scientific preregistration
but replaces permanent whole-root freezing with append-only retry attempts.
See `README.md` for the current contract. No arm was run as part of the
2026-08-01 slimdown.
