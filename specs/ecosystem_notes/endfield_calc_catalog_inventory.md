# endfield-calc catalog inventory

Borrowed concepts:

- item / recipe / facility separation
- snapshot-friendly data organization
- clear calculation/data-layer boundary
- cycle/throughput vocabulary

Local interpretation:

- build `NormalizedCatalog`
- add a build-time snapshot ingest adapter
- keep the current certified runtime on frozen preprocess artifacts
