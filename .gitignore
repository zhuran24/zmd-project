# Generated artifacts and runtime logs
.artifacts/
.codex_test_logs/
.codex_child_sessions/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pytest_tmp/
.pytest_tmp_*/
.pytest_debug*/
__pycache__/
**/__pycache__/
*.py[cod]

# Checkpoint/proof/final delivery boundaries
data/checkpoints/
data/solutions/final_solution.json
data/solutions/certified_delivery_manifest.json
data/blueprints/optimal_blueprint.json
data/telemetry/
**/exact_campaign_state.json
**/exact_campaign_telemetry.json
**/certified_delivery_manifest.json
**/final_solution.json
**/optimal_blueprint.json
release/
viewer/
frontdoor/

# Codex historical archive (read-only reference)
_codex_archive/

# Local upstream clones for offline browsing/diffing (50 MB+, not vendored)
# CLAUDE.md: `.upstream_clones/` is gitignored — vendored slices live under third_party_snapshots/
.upstream_clones/

# PoC ephemeral artifacts (regenerable by re-running the demo runner)
scripts/pumpkin_poc/proof.drcp
scripts/pumpkin_poc/run_log.txt

# Claude Code internal lock files (regenerated on session start)
.claude/scheduled_tasks.lock

# Claude Code agent worktrees (per-agent isolated repo copies, regenerable, not source)
.claude/worktrees/

# Local scratch and package outputs
*.log
*.tmp
*.bak
*.zip
*.7z
*.tar
*.tar.gz
*.tgz
*.tar.xz
*.txz
*.xz
*.zst
*.rar

# 依赖包 (GPT 上传用, 从 requirements.lock regenerable, 巨大不入库; .whl 默认不被 *.zip 挡)
cc_context/review/deps/
.reaudit_v25_extract/

# Codex 接手期外审包/补丁工作目录 (v29-v78 审查工件; zip/7z 已被上面的规则挡,
# 解包出的散文件同样不入库 — 审查工件归档走 cc_context/review/)
补丁包/
